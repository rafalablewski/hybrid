import AuroraExplore from "../components/aurora/explore";

/** Explore — the discovery surface (search · coaches · plans · community). */
export default function Explore() {
  return <AuroraExplore />;
}
