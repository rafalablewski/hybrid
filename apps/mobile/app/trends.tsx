import AuroraTrends from "../components/aurora/trends";

/**
 * TRENDS — a destination again.
 *
 * It had been folded into the Performance page, whose exit rows now carry the
 * week's two figures and the door. The eight-week series and the per-exercise
 * table live here.
 */
export default function Trends() {
  return <AuroraTrends />;
}
