/**
 * Web screen strings for the "account" domain (EN baseline + PL/DE), merged OVER the
 * base TRANSLATIONS in i18n.ts. Web Aurora screens consume these via t("w.account.…").
 * Keys are namespaced "w.account.*" so they never collide across domains.
 */
export const web_account = {
  en: {} as Record<string, string>,
  pl: {} as Record<string, string>,
  de: {} as Record<string, string>,
};
