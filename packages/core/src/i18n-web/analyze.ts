/**
 * Web screen strings for the "analyze" domain (EN baseline + PL/DE), merged OVER the
 * base TRANSLATIONS in i18n.ts. Web Aurora screens consume these via t("w.analyze.…").
 * Keys are namespaced "w.analyze.*" so they never collide across domains.
 */
export const web_analyze = {
  en: {} as Record<string, string>,
  pl: {} as Record<string, string>,
  de: {} as Record<string, string>,
};
