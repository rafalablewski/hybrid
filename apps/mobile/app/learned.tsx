import AuroraLearned from "../components/aurora/learned";

/**
 * WHAT WE LEARNED ABOUT YOU — the monthly story of the athlete's own model.
 *
 * A screen of its own rather than another disclosure inside the Volume card:
 * the ceilings, the measured clearance rate and the readiness pattern are three
 * different engines' output and the athlete's question spans all of them. Led
 * from the You tab (components/aurora/profile.tsx).
 */
export default function Learned() {
  return <AuroraLearned />;
}
