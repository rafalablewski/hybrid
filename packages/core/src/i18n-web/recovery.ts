/**
 * Web screen strings for the "recovery" domain (EN baseline + PL/DE), merged OVER the
 * base TRANSLATIONS in i18n.ts. Web Aurora screens consume these via t("w.recovery.…").
 * Keys are namespaced "w.recovery.*" so they never collide across domains.
 */
export const web_recovery = {
  en: {} as Record<string, string>,
  pl: {} as Record<string, string>,
  de: {} as Record<string, string>,
};
