{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM"],
    "jsx": "react-jsx",
    "noEmit": true,
    // the e1rm-headline guard reads apps/ off disk, so it needs node builtins
    "types": ["node"]
  },
  "include": ["src"]
}
