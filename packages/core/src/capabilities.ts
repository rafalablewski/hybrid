/**
 * Capabilities registry — the single source of truth for everything HYBRID can
 * (and can't yet) do. Surfaced in the web admin "Capabilities" screen.
 *
 * KEEP THIS CURRENT: whenever a feature ships, gets blocked, is planned or is
 * RETIRED, update this file in the same change (see CLAUDE.md).
 *
 * `retired` means the code was deliberately DELETED — not paused, not deferred,
 * not blocked on a credential. The entry stays so the registry records the
 * decision AND its reasoning, and so a future session cannot helpfully rebuild
 * something that was cut on purpose. The code is in git if it is ever wanted
 * back; the argument for why it went is only here.
 */

export type CapabilityStatus = "shipped" | "blocked" | "planned" | "retired";

export type CapabilityArea =
  | "Core"
  | "Web"
  | "Mobile"
  | "Backend"
  | "Auth"
  | "Data"
  | "AI"
  | "Integrations";

export interface Capability {
  id: string;
  area: CapabilityArea;
  title: string;
  detail: string;
  status: CapabilityStatus;
  /** For `blocked` items: what's needed to unblock it. */
  blockedBy?: string;
  /** For `retired` items: why it was cut, and what would have to be true to
   *  revisit it. */
  retiredBecause?: string;
}

export const CAPABILITIES: Capability[] = [
  // ---- Core (shared) ----
  { id: "monorepo", area: "Core", status: "shipped", title: "Monorepo + shared core", detail: "Turborepo/pnpm workspace; engines, types, plan library, sport engine, session helpers and brand tokens written once in @hybrid/core and consumed by web + mobile." },
  { id: "engines", area: "Core", status: "shipped", title: "Training engines", detail: "Fatigue (decay), readiness (+ biometric adjustment), per-lift progression signal, periodization (macrocycle/microcycles), and the prescription engine. Pure, unit-tested." },
  { id: "plans-lib", area: "Core", status: "blocked", title: "Plan library", detail: "Goal-first plan tree of 19 goals spanning strength (Powerlifting, Olympic Weightlifting, Strongman), physique (Bodybuilding, Fat Loss), endurance (Triathlon, Running, Cycling, Swimming), functional/sport (Hyrox, CrossFit, Hybrid Athlete, Calisthenics, Kettlebell, Tactical & Military, Sport Performance) and health (General Fitness, Mobility & Longevity, Pre & Postnatal), each with full plan detail (split, every training day fully spec'd, progression). Each goal carries a category; GOAL_GROUPS exposes the goals grouped by category (Strength/Physique/Endurance/Functional & Sport/Health) and both the web/mobile Plans screens and the onboarding goal picker render them in those groups. PLAN MODEL REDESIGN — the old one-size-fits-all gym plans (exercise – sets×reps – rest – RPE) were retired because that single shape is wrong for most goals. Plans are now DISCIPLINE-SHAPED: a PlanProgram (plan-program.ts) is tagged with a discipline that selects its structure, loading units, volume metric and progression — `strength-percent` (Olympic WL / Powerlifting: % of 1RM, ramped sets, complexes, tempo, AM/PM sessions, NL number-of-lifts volume counting, date-anchored peaking), with `hypertrophy` (Bodybuilding: sets×reps×load), `endurance` (Running/Cycling/Swim: distance/pace/mileage, NO gym) and `conditioning` (Hyrox/CrossFit/Fat-Loss: circuit blocks of exercises with a rounds count, each prescribed as a sets×reps OR a time/hold scheme, no % or paces). CONSISTENCY IS THE POINT — however differently a source plan is worded, it ends up rendered in ONE identical HYBRID card+table layout (the shared ProgramDays component on web — used by BOTH classic + Aurora — and its mobile twin in percent-program.tsx, off the SAME view model) whose row body ADAPTS per discipline: bodybuilding shows Sets×Reps + an intensity-coloured RPE 'heat' bar (rpeColor); weightlifting shows a %-ramp prescription with each load colour-graded (loadColor) and AM/PM doubles split by lime/blue session bands; running renders the whole week as one card of Day rows with a workout-type dot (workoutColor) and muted rest days. THE LAYOUT IS CHOSEN FROM CONTENT, not the plan label: an all-prose week (pure running) becomes the single week card; any week containing gym work renders one card per day, and a day's content is grouped by KIND so the formats never collide — a RUN block (prose), a MAIN block (%-of-1RM barbell work, the colour-graded %-ramp) and an ACCESSORIES/STRENGTH block (classic sets×reps×RPE, the heat table) each render in their own labelled section (liftKind in @hybrid/core). So an Olympic day with a biceps-curl accessory, or a run day with a strength block, splits cleanly instead of mixing %-ramps and RPE rows in one table. The intensity thresholds live once in @hybrid/core so the colour wave can't drift across clients. planProgramView() builds a single render-ready view model that ALL four renderers consume (web classic + Aurora, mobile classic + Aurora): a week selector, a discipline-aware volume label, day cards (AM/PM sessions OR weekday columns), a 'fill in your numbers' panel, a 'peaks to …' note, progression, and enroll. The panel and labels generalize: strength shows numeric MAXES (→ derived kg) and an 'N lifts' volume; hypertrophy shows working weights and an 'N exercises' volume; endurance shows text GOAL PACES and no counter; the peak day reads 'Competition' or 'Race day' (or none) per program; a single-week plan hides the week selector. FOUR programs shipped so far: (1) the SOVIET 8-WEEK Olympic-weightlifting block (`oly-soviet-8wk`, strength-percent) — %-first loading kept as written, 1RM derives working kg via stepKg, a notation parser (parsePercentSteps) turns coach shorthand into steps, NL derived (Σ (reps+complex)–sets) and unit-checked vs the source's own running totals (Week 1 Day 1 = 160 lifts); (2) the HANSONS 5K BEGINNER 9-WEEK running plan (`run-5k-beginner-9wk`, endurance) — a Mon–Sun grid of PROSE workouts (intervals/tempo/hills/easy/long run, 'or cross-train' alternatives as notes), goal-pace inputs, a taper into race day, NO gym; (3) the 6-DAY PUSH/PULL/LEGS bodybuilding split (`bb-ppl-6day`, hypertrophy) — a one-repeating-week weekday split of exercises with sets×reps ranges, each day built on a progressively-overloaded main lift; (4) the SATURDAY KETTLEBELL + FAT-LOSS circuit (`fatloss-kb-saturday`, conditioning) — ONE ~90-min session modelled as seven block cards (warm-up → five work blocks → cool-down), each carrying its round count in the title and its exercises as time/hold or sets×reps schemes; each block has an EFFORT TIER (conditioningColor) so the circuit rides the SAME intensity wave the %-plans and RPE-plans do — the prescription/dot colours blue→lime→amber→red as the session builds (warm-up easy → finisher peaks red → cool-down ash); volume counts MOVEMENTS like hypertrophy ('N exercises'); the 4-week emphasis rotation (form → speed → volume → heavier) + the recovery routine live in the progression note; (5) the 12-WEEK KETTLEBELL block (`kb-12wk-strong`, hypertrophy) — a strength/muscle program whose split ROTATES every week (full body → push/pull/legs+core → upper/lower, weeks 1/4/7/10, 2/5/8/11, 3/6/9/12), rendered as the bodybuilding sets×reps table with a 12-week selector; reps are SINGLE numbers (per the project rule — source ranges collapsed to the top, '15-20' → '20'), with per-side '10/leg' and holds '30 s' kept as-is, via the free-text scheme and NO RPE invented (the source gives sets × reps + rest, not effort); instead the sets×reps rides the TRAINING-ZONE wave (repZoneColor) — each prescription coloured strength/amber (≤6) → hypertrophy/lime (7–12) → endurance/blue (13+, and timed holds) from its OWN rep count — so it carries the same coloured intensity dimension as the oly %-wave and the bb RPE heat, derived not fabricated. (6) the HYBRID ATHLETE strength-and-engine base (`hybrid-engine-base`, hypertrophy) — the hybrid case the other five don't cover: a single week (run on repeat) that carries BOTH heavy barbell strength AND an aerobic engine, authored as a direct PlanProgram because it mixes strength/conditioning/endurance content that no single builder fits. Its point of difference is the DAY SHAPE — two key days are THREE-A-DAY (AM strength / MID conditioning / PM easy run) and Saturday is a two-a-day (AM long run / PM full-body), which is what exercises the AM/MID/PM session model end to end; each session is a single content kind so a day reads as clean, separately-startable blocks. Reps are single numbers, kettlebell work uses the KB prefix. All six are wildly different source formats rendered in ONE identical UI. The remaining 13 goals still carry 0 plans and degrade gracefully (web/mobile show '0 plans' / 'No plans here yet'; onboarding returns no recommendation) until authored in their fitting shape. See reference/plan-model-redesign.md. PLAN PROVENANCE — the model has THREE kinds of plan, by who authors them and who they're for: (1) PRE-MADE app plans — the curated library shipped with the app, free for casual users to browse, enroll & follow as written (this `plans-lib` content); (2) ATHLETE-built plans — a paid athlete composes their own via the Builder (own templates/programs); (3) COACH plans — authored by a coach and assigned to THEIR clients only (private to that coach↔client relationship; see `coach-assign-plans`). Today (1) carries five discipline-shaped plans so far (the Soviet OWL block on Olympic Weightlifting, the Hansons 5K plan on Running, the 6-day PPL split on Bodybuilding, the Saturday kettlebell circuit on Fat Loss, and the 12-week kettlebell block on Kettlebell; the rest await authoring), the coach path (3) assigns by goal / a named pre-made plan / a coach-AUTHORED multi-week program (the program builder is shipped — see coach-program-builder), and deeper athlete program-building for (2) is the remaining authoring step.", blockedBy: "The discipline-shaped plan model ships with six programs (Soviet 8-week OWL, Hansons 5K running, 6-day PPL bodybuilding, Saturday kettlebell fat-loss circuit, 12-week kettlebell strength block, and a Hybrid Athlete strength-and-engine base). The other 13 goals still need plans authored in their fitting shape — more endurance/no-gym (Cycling, Swimming, Triathlon, longer-distance running), conditioning (Hyrox, CrossFit), strength (Powerlifting, Strongman), etc." },
  { id: "engine-name-resolution", area: "Core", status: "shipped", title: "Engine exercise-name resolution (library-named lifts attribute load)", detail: "BUGFIX + hardening. Every muscle-attribution engine indexed the MOVEMENTS map by EXACT key and silently skipped a miss, so a lift logged under a name the built-in catalog doesn't carry verbatim contributed ZERO load — the tissue simply read as untrained. That is most of what athletes actually pick: the shipped exercise library (reference/sql-exercise-seed.sql, 209 rows) names lifts differently from the built-in DB, and 153 of its 209 names missed by exact key — equipment-qualified twins ('Barbell Deadlift' vs 'Deadlift', 'Barbell Bench Press' vs 'Bench Press'), case-only differences ('Pull-up' vs 'Pull-Up', 'Chin-up', 'Step-up'), and 148 library-ONLY lifts with no built-in twin at all ('Dumbbell Bulgarian Split Squat', 'Seated Cable Row', 'Cable Crossover'…). Reported symptom: a full second workout (deadlift, pull-up, bench, Bulgarian split squat) left the injury-risk tissue map bit-for-bit unchanged, with quads/glutes/posterior/back reading '—' (no chronic history) while the athlete had just trained them. FIX, in two parts. (1) movementFor(name) in engines/movements.ts is now the ONE resolver every engine shares — exact key → trimmed → built-in rename breadcrumbs (GYM_ALIASES) → the shipped library's equipment-qualified display names (GYM_LIBRARY_ALIASES) → case-insensitive — mirroring gymExercise's order so the property sheet and the engines can never disagree about whether a lift is known; musclesFor(name) is the []-returning convenience the volume callers want. Wired into fatigue, injury, session (inferBlockKind + toTrainingLog's energy-system lookup), analytics (volume-by-muscle), landmarks, records, exercise-browse, prescription and sports. (2) setExerciseCatalog(custom) publishes the admin-managed library to those engines, so library-ONLY lifts resolve too. The catalog is GLOBAL admin-authored data (identical for every athlete), not per-request state, so a module-level registry is correct on both clients and in a server route — and it keeps every engine signature unchanged (no 20-call-site churn, no per-screen memo-dependency drift). Ordering is a guarantee, not a race: the engines run on SESSION data, so both clients publish the catalog from inside their session fetchers (web lib/exercise-catalog.ts ensureExerciseCatalog() awaited in useSessions; mobile the same helper in lib/api.ts awaited in fetchSessions + querySessions), and useExercises() tops it up on a later refetch so the engine catalog and the picker's never drift. Server-side engine paths publish it too (lib/cache.ts publishExerciseCatalog() → athleteState, /api/ai-coach, /api/coach/squad). Degrades to the built-ins alone when signed-out / the table isn't seeded. 12 new unit tests (engines/movement-resolution.test.ts) pin the resolution order, the registry lifecycle, volume-by-muscle on a library name, and the reported regression: the second workout now raises risk instead of leaving it identical." },
  { id: "catalog-attribution-guard", area: "Core", status: "shipped", title: "Catalog attribution guard — a pickable exercise must log load", detail: "PREVENTION for the engine-name-resolution class of bug. The failure mode is silent by construction: a logged lift reaches the engines as a NAME, and a name that doesn't resolve — or resolves to an EMPTY muscle list — is indistinguishable from 'this tissue was never trained'. The output is a plausible-looking zero, not an error, so nothing anywhere surfaces it. THE INVARIANT is now pinned in CI instead: packages/core/src/engines/catalog-coverage.test.ts parses the shipped library seed (reference/sql-exercise-seed.sql — the actual rows the picker offers) and fails the build if any row is unresolvable, carries no muscles, uses a muscle group outside the engine enum, has an unresolvable alias, or would drop out of catalogNames (exactly what the two pickers render). A degraded-path case covers the state the original bug ran in — the library NOT published (signed out / table unseeded / fetch failed), engines on the built-ins alone while stored sessions carry library names — via the rule that THE ENGINES MUST NEVER BE BLINDER THAN THE PROPERTY SHEET: if gymExercise() resolves a name, movementFor() must too, or the exercise page shows a full muscle map for a lift the engines score as zero. Mutation-tested: reverting movementFor to the old exact-key lookup fails 7 tests, deleting one GYM_LIBRARY_ALIASES bridge fails 4, and emptying a single seed row's muscles fails 3 — with the offending exercise NAMED in the assertion message. SECOND DEFECT FOUND AND FIXED by the new guard: 55 seed rows (every curl, calf raise, ab and grip movement — categories Biceps, Traps & Forearms, Calves, Abs & Core) shipped with muscles deliberately left EMPTY, on the assumption that only the per-muscle volume chart would skip them. It doesn't stop there — they contributed zero to fatigue, ACWR, injury risk, landmarks and records too. They now map to their coarse home exactly as core's ENGINE_GROUP already does for the built-in catalog (biceps + forearms + traps -> back, calves + abs + obliques -> posterior); a rough attribution beats none. RUNTIME ADDITIONS are covered by the existing admin write path — api/admin/exercises/shared.ts parseExercise already rejects an exercise with no muscles ('at least one muscle required'), so the only way to introduce an empty row is raw SQL, which is what the CI guard now watches. LIVE: reference/sql-exercise-muscles-fix.sql has been run in the Supabase SQL Editor, so the 55 already-inserted rows now carry their muscle group and curls / calf raises / ab work / grip work attribute load on the live app. A fresh install needs only the seed (it now carries these values); the repair script stays in reference/ as the idempotent fix for any database seeded from the older file." },
  { id: "prescribed-name-attribution", area: "Core", status: "shipped", title: "Plan- and sport-PRESCRIBED movements must attribute load too", detail: "SAME silent-drop class as engine-name-resolution / catalog-attribution-guard, from a name source those fixes do NOT cover. Exercise names reaching the engines come from three places: (1) the exercise PICKER — fixed and guarded; (2) the shipped PLAN PROGRAMS (plan-programs.ts PlanLift.name and the structured gym PlanEntry.label); (3) the SPORT S&C pools (olympic-sports.ts PoolExercise.name). Sources 2 and 3 are authored free-hand in TypeScript and were never checked against the movement catalog, so they miss it the same way library names used to. MEASURED with the library catalog published (i.e. the real runtime state after the resolver fix): 154 of 214 prescribed plan movements and 22 of 34 sport S&C movements resolve to NO muscle group — they log zero fatigue, ACWR, injury risk, volume-by-muscle, landmarks and records. Worst case is kb-12wk-strong at 108/121: enrol in the 12-week kettlebell plan, follow it exactly, and almost the entire program reads as untrained. Also bb-ppl-6day 14/28, fatloss-kb-saturday 14/19, oly-soviet-8wk 16/26. Two distinct causes, needing different fixes: (a) NAMING VARIANTS of catalog lifts that just need an alias bridge — 'Squat' -> Back Squat, 'Dips' -> Dip, 'Dumbbell Curl' -> Dumbbell Biceps Curl, 'Lateral Raises' -> Dumbbell Lateral Raise, 'Skull Crushers', 'Hamstring Curl', 'Chest Fly', 'Pullover'; and (b) movements the catalog genuinely lacks and that need real DB entries with muscle data — most of the kettlebell vocabulary (KB Halo, KB Windmill, KB Slingshot, KB Figure 8, KB Gorilla Row…), the Olympic derivatives (Hang Snatch, Clean Extension, Snatch Balance, the Eccentric/Slow deadlift variants), and the athletic/plyo pool (Pogo Hops, Depth Jumps, Lateral Bound, Shuttle Sprints, Front Lever Progression). Warm-up and mobility labels (Jumping Jacks, Hip Circles, Arm Swings) are a third, lower-stakes bucket — arguably fine to attribute little, but they should resolve deliberately rather than by accident. SHIPPED. 52 new exercises authored into the built-in DB with real primary/secondary muscles — the kettlebell vocabulary (KB Halo, KB Windmill, KB Slingshot, KB Figure 8, KB Gorilla Row, KB Renegade Row, KB Arm Bar, KB Z Press, KB Chop, KB Front Rack Lunge, Suitcase Deadlift, Man Maker…), the bodyweight core work (Hollow Body Hold, Superman, Reverse Crunch, Mountain Climber), unilateral/lateral legs (Cossack Squat, Curtsy Lunge, Shrimp Squat, Sumo Squat), the athletic/plyo pool (Jump Squat, Pogo Hop, Lateral Bound, Shuttle Sprint, Trap Bar Jump, Med-Ball Rotational Throw, Jumping Jacks, High Knees, Bob and Weave, Hip Circles, Arm Swings), the sport-pool accessories (Band Pull-Apart, Cable Straight-Arm Pulldown, Scapular Pull-Up, Front Lever Progression, Hangboard Repeaters, Towel Pull-Up Hold, Landmine Punch Press), two bodybuilding accessories and two Olympic derivatives (Clean Deadlift, Snatch Balance). Plus 140 GYM_ALIASES bridges pointing each authored name at a real entry. Bridge targets are BUILT-IN entries on purpose: a plan is CODE and must attribute without waiting on the DB-backed library to load, which the guard now asserts separately. The bridges also collapse duplicate spellings of the same lift inside one program ('Crush Grip KB Pushup' / 'KB Crush Grip Pushup' / 'Crush Grip Push-up'; 'High Pull' / 'High Pulls' / 'Single-arm High Pulls'), honouring the KB-prefix naming rule. RESULT: 0/214 prescribed plan movements and 0/34 sport movements now attribute nothing, down from 154 and 22. GUARD EXTENDED: catalog-coverage.test.ts gained three cases covering the third name source — every PlanLift.name and structured PlanEntry.label resolves, every PoolExercise.name resolves, and both do so WITHOUT the library published. Mutation-tested: renaming one new entry or deleting one bridge fails 2 tests, naming the offending plan and movement. TWO BEHAVIOUR CHANGES fell out and are pinned by updated tests: 'Calf Raise (slow)' got its own bodyweight entry rather than a bridge to the loaded machine lift (a runner's slow tempo raise must not inherit a 60 kg base load), and exerciseBucket now prefers a RESOLVED movement over its keyword guess, so 'Eccentric Snatch Deadlift' buckets as posterior — agreeing with its canonical Snatch-Grip Deadlift entry — instead of being pulled into 'olympic' by the word 'snatch'." },
  { id: "sport-engine", area: "Core", status: "shipped", title: "Sport S&C engine", detail: "Per-sport demands + exercise pools; prescribeForSport ranks the strength & conditioning that transfers, by level, and doses today's working load from the athlete's OWN logged lifts (a level-appropriate % of their best e1RM) — falling back to a starting estimate for known barbell lifts or a bodyweight/tempo scheme, never a fabricated number. Pure + unit-tested. UNIFIED (Jul 2026): there is now ONE sport database — the engine no longer keeps its own list. Every prescribable sport (Running, Swimming, Cycling, Boxing, BJJ, Squash, Climbing) is a catalog entry in olympic-sports.ts carrying an optional `sc` block (family + performance marker + ranked demands + level-tagged pool); sports.ts derives SPORTS/SPORT_NAMES/prescribeForSport as a projection over exactly those entries (same pool array by reference — one source of truth), so the S&C engine and the manual-logging catalog can never again disagree on what a sport is or drift apart. Add a sport once, in the catalog; give it an `sc` block to make it prescribable. sports.test.ts asserts the projection." },
  { id: "signal-ontology", area: "Core", status: "shipped", title: "Signal ontology (Athlete Twin foundation)", detail: "Universal time-series in @hybrid/core: every measurement (HRV/RHR/sleep today; GPS load, force-plate, blood tomorrow) is one Signal shape from any source, with rolling baselines + oriented z-scores. A toBiometrics() adapter feeds the readiness engine unchanged. End-to-end loop live: Signal table (reference/sql-signal-table.sql) ← /api/signals (GET/POST, kind-validated) ← manual check-in writes it; dashboard reads it back via useSignals to drive the Twin. Pure + unit-tested." },
  { id: "sport-subpage", area: "Core", status: "shipped", title: "A page for every sport", detail: "SHIPPED on BOTH clients (design: design/sport-page.html). The 65-sport catalog had no destination — the Sport screen was a chip picker over one shared body, so a sport was a filter rather than a place. Now every catalog sport has its own page, rendered from ONE self-configuring model in @hybrid/core (sport-page.ts, `sportPageModel`) that branches on the sport's own record: `metrics` decides whether the distance cell, the volume bars, the pace trend and the effort split exist; `distanceUnit`/`pacePer` decide the units (km + /km for the road, metres + /100m for the pool); an `sc` block decides whether the transfer section exists at all (7 of the 65). No new aggregates — totals, weeks, zones and the prescription come from runTotals/weeklyMileage/paceEffortSplit/prescribeForSport, so a figure here and the same figure on Today cannot disagree; the slice runs through deviceTrueSessions() first, so every number is the watch's read where a watch recorded it and pace derives from cardioSeconds, not from display minutes. THE PAGE: a `cover`-rank Hero (eyebrow = category – family, meta = efforts/distance/since, level in the rail's accessory, a docked 'Log a … session'); the ONE figure — the sport's own marker (a 5 km time, a 100 m time, an FTP) where the athlete has typed one, else best pace, else distance, else time; totals on hairlines; 8-week volume; the reversed pace trend + the easy/steady/hard split for a paced sport; a full-bleed Bests rail; the ranked demands + prescribeForSport's blocks with a live level control that re-doses off the athlete's own e1RM, then the pool's why-lines (locked entries name the level that unlocks them); recent efforts reading through device-truth, tapping into the session. REDESIGNED Aug 2026 (see sport-records + sport-page-reduction): the hero is unchanged, the ONE figure is now the record ladder's promoted rung, the totals row and the Bests rail's card chrome are gone, and the transfer sections moved to their own screen. THE MARKER IS TYPED, so a delta needs kept history: SportStore.markerLog (core `recordMarker`/`markerHistory`, one shape written by both clients under 'hybrid.sport') keeps up to 24 typings and the page shows a delta + sparkline only from the second one — never an invented trend. ENTRY POINTS: the Sport screen is now the INDEX (`sportIndex` — sports you actually train first, then the prescribable ones, the whole catalog behind a search) and Today's other-sport tiles open the sport's page directly instead of dead-ending at the index. EVERY LINK LANDS ON THE SPORT, NOT THE PICKER: a sport page has an ADDRESS on both clients — web `/app?s=sportpage&sport=<slug>` (the slug comes from core `sportSlug`, so the shell's deep-link parser keeps its strict [A-Za-z0-9_-] boundary; `sportPageUrl()` builds a shareable link and `sportFromSlug` resolves either a slug or a display name back to the catalog, degrading to the INDEX rather than to an empty page), mobile `/sport-page?name=<slug|name>` (scheme `hybrid://`), which means a link survives a refresh, a bookmark, a paste and a hand-off between the two clients. Every surface that NAMES a sport now opens that sport's page rather than the index: Today's other-sport tiles and the Cockpit's Sport S&C module + breakdown tab on both clients. The Sport nav item still lands on the index, which is what a nav item means. Starting today's transfer session hands the logger the same blocks on both clients (core `transferSessionBlocks`). Colour is NOT per sport — every sport page wears the one action accent; what distinguishes Running from Swimming is the unit." },
  { id: "sport-page-followups", area: "Core", status: "shipped", title: "Sport page — the three follow-ups it raised", detail: "All three closed. (1) A HOLD IS NOT SIX REPS. prescribeForSport now doses each movement in the measure the exercise database says it IS (`GymExercise.measure`): reps stay sets×reps, a `time` movement becomes a hold in seconds (Swimming's Hollow Body Hold reads 3×20 s at Beginner and 5×45 s at Elite — growing with the level, because there are no reps to take away), and a `distance` movement becomes a carry in metres (Squash's Shuttle Sprints 3×20 m → 5×50 m). SportBlock gained `measure` + `amount`, and `reps` is now set ONLY for reps-measured work, so nothing downstream can read a 30-second hold as 30 repetitions; the reconciler carries the measure through (the week's envelope scales the SET COUNT, never the per-set quantity's unit), and both the sport page's and the reconciler's logger seeds fill the per-set field in that measure — which is what the logger already labels off exerciseProfile (reps / s / m). A hold and a carry get NO load: the engine derives working load from an e1RM, blockBestE1rm returns 0 for non-reps measures, and claiming a percentage of a number that does not exist would be fabricated, so those blocks prescribe the effort and leave the load to the athlete. Also fixed the data hole behind it — Boxing's 'Assault Bike Intervals' resolved to nothing (its alias pointed at an 'Assault Bike' entry that did not exist), so it had fallen back to reps. (2) ENDURANCE HUB vs SPORT PAGE — decided. The app now has ONE destination per depth: ALL your endurance (the Endurance hub) → ONE sport (the sport page) → ONE move (the exercise page, which already owns per-move pace analytics). The hub's discipline picker and its duplicate volume/pace/zone charts are gone; it is the COMPARISON now — every logged discipline side by side (the shared enduranceLanes block, uncapped and headless), each lane opening that sport's page, and Today's lanes open the sport page too. `sportForDiscipline` in core is the one place the lanes' discipline vocabulary meets the catalog's names, and it is an EXPLICIT map: inferring it from catalog order filed a cross-country ski under SKATEBOARDING (cardioDiscipline's skiing pattern matches 'skate'). `walking` is deliberately unmapped — the catalog's only walking sport is Race Walking, and filing a hike under a track event is a lie a link then repeats, so a lane with no sport shows no exit. (3) A DRAWN SPORT MARK. theme/sport-marks.ts: 15 stroke marks in the same 72-unit box as the icon set, resolved per sport where its instrument is distinctive (oar, sail, blade) and per CATEGORY otherwise — there are not 65 distinctive silhouettes, and the title already says which sport it is. Both Hero Systems gained `artPaths` (drawn art wins over `glyph`, same box, same parallax, same ghost floor), so the mark holds its shape at 152pt and can survive into the collapsed bar as texture, which a desaturated emoji could not. Every closed shape is drawn as TWO arcs: a single self-returning arc is degenerate, two of the first-cut marks rendered as nothing, and it took a screenshot to notice — a test now guards the shape of the path data itself." },
  { id: "cover-rank-mark-set", area: "Mobile", status: "planned", title: "Drawn marks for the other cover-rank pages", detail: "THE `cover` HERO RANK HAS ONE CALLER and the reason is a missing drawing, not a missing decision. core/hero.ts names five kinds of page the rank was built for \u2014 a goal, a plan, a recipe, an exercise, a workout \u2014 and mobile ships 45 `title` screens, one `bar` and one `cover` (the sport page). A cover resolves its backdrop to `art` only when the screen hands it `artPaths`, and `sportMarkPaths` (theme/sport-marks.ts, 15 stroke marks in a 72-unit box, resolved per sport where the instrument is distinctive and per CATEGORY otherwise) is the ONLY producer of those in the codebase. Without a mark an exercise or plan cover falls through to a bare `wash`: 252pt of accent gradient with nothing in it, which reads worse than the 132pt title hero those pages wear today. WHAT IT NEEDS: the same treatment sport-marks got, for exercises (by implement \u2014 barbell, dumbbell, KB, machine, bodyweight; the exercise database already carries the implement, so this resolves per KIND rather than per lift, which is what kept the sport set at 15 rather than 65) and for plans (by discipline, which plans.ts already colour-codes). Every closed shape must be drawn as TWO arcs \u2014 a single self-returning arc is degenerate and renders as nothing, which cost a screenshot to find the first time; sport-marks.test.ts guards the path data\u2019s shape and a new set joins that guard. WHY IT MATTERS BEYOND LOOKS: a rank with one caller has a TEST SURFACE OF ONE. Nothing else exercises cover geometry, the cover backdrop branch or the cover collapse track, so a regression there is invisible until somebody opens a sport page \u2014 which is the same blind spot that let the duplicate-AuroraField bug sit in the sibling `field` branch. Three or four callers turns the rank from a special case into a tested path. The alternative \u2014 demoting the sport page to `title` \u2014 was considered and REJECTED ON REVIEW, with both options rendered side by side: the drawn mark is the one thing that tells 65 sport pages apart before the title is read, and the cover is the only place a sport is a SUBJECT rather than a row. So this item can only ever be worked in one direction \u2014 more callers, never fewer. A future session that finds a rank with one consumer and reaches for the tidy fix should stop here: the singleton is known, the reason it holds is written into core/hero.ts, and the debt is the missing marks." },
  { id: "ink2-radius-triage", area: "Mobile", status: "shipped", title: "Every raised surface names its corner \u2014 and two of them were circles", detail: "THE `ink2` RESIDUE, worked rather than swept. 21 raised surfaces carried a RAW integer radius across SIXTEEN distinct values (6, 8, 10, 14, 18, 20, 22, 24, 27, 42) in a five-token system. The finding is that they were not one mistake repeated, and a blanket sweep to `RADIUS.field` \u2014 the obvious codemod \u2014 would have BROKEN two of them. Each was classified by what the object IS: SIXTEEN went to `field` (a bounded block of content on a screen: three text inputs at 10 and 14, an admin media thumbnail, two user-page blocks, three quick-log sport tiles at 18, both nutrition-hub stat tiles at 20, the compact feel prompt, both PR-attestation blocks and the side menu\u2019s upgrade CTA at 22); ONE to `inner` (admin audit\u2019s expanded detail block, which is nested inside a row and takes the nested rung); ONE to `card` (the tour\u2019s bottom-presented modal panel at 24, which is a sheet); and THREE to `pill`. Those last three are the point. `42` on an 84\u00d784 profile avatar and `27` on a 53\u00d753 social one are ARITHMETIC \u2014 half the side \u2014 not a style choice, and RN clamps a radius to half the box anyway, so `RADIUS.pill` renders them IDENTICALLY while saying what they are; a sweep to `field` would have squared both avatars. (The third is the logger\u2019s superset link badge at 6, a ~15pt lozenge, which is a badge and takes the badge idiom.) Comments at both avatar sites now state the arithmetic so the next sweep cannot mistake it. A FIFTH CATEGORY exists that no pattern can see: `concentricRadius(parent, pad)` means a panel inset inside a card is SUPPOSED to differ from the token \u2014 a child is only truly concentric at `parent \u2212 pad` \u2014 so normalising those would break the rule iOS 26 made non-negotiable. RESULT: zero raw radii on `ink2` surfaces app-wide, and the raw-borderRadius ratchet fell 141 \u2192 120 (re-pinned, with the classification written into the rule so the next reader knows why it is a ratchet with a human in it rather than a codemod). Core + mobile suites green, all three typechecks clean." },
  { id: "hero-field-and-cover-channel", area: "Mobile", status: "shipped", title: "The hero stops painting its own field, and the cover wears the sport\u2019s channel", detail: "THE OPEN ITEMS from the sport-page repaint, worked through. (1) THE COVER WASH TOOK THE CHANNEL. The page never passed `hero.accent`, so it fell back to the brand chartreuse and all 65 sports got the same cover while the charts a scroll below spoke Lyons Blue or Fleur De Lis \u2014 the top of the screen and the middle of it disagreeing about what kind of thing the page is about. It passes `channel` now. Measured rather than assumed: the wash at 0.52 over ink puts chalk at 9.58:1 on Lyons Blue and 5.84 on Fleur De Lis against 4.65 on the old chartreuse, and the eyebrow chip (color-mix #fff 82%) stays 15\u201317:1 for its ink on every one \u2014 so the change IMPROVES the cover\u2019s contrast rather than spending it. (2) THE HERO STOPPED PAINTING A SECOND FIELD, and this one was a real defect on all 45 title-rank screens, not just the sport page. `HeroBackground` returned its own `<AuroraField />` for a `field` backdrop while the shell already mounted one, which was wrong twice: at rest it DOUBLED the tint over the hero band, cut off hard at the frame\u2019s `overflow: hidden` edge; and in motion it was worse than a doubling, because that frame is `zIndex: 20` and is rendered AFTER the scroller, so its field painted OVER the content passing underneath on the way to the collapsed bar. The bar has its own substrate a few lines down (a BlurView plus an ink wash on the hairline\u2019s ramp), so nothing there ever needed a ground. The branch returns null; the field is the shell\u2019s, on every rank but the takeover. (3) A HARD RULE WITH A THREE-SITE BLIND SPOT. `alpha is withAlpha(), never a hex suffix` matches a template literal ending in LITERAL hex, so `${C.lime}${Math.round(op * 255).toString(16).padStart(2, \"0\")}` \u2014 the same defect with the byte worked out at runtime \u2014 sailed straight past it. Three sites did exactly that: the calendar\u2019s heat cell, the exercise page\u2019s consistency heat and the profile\u2019s achievement grid, each one a withAlpha() call with the arithmetic spelled out. All three converted (byte-identical by construction \u2014 withAlpha emits the same round(a * 255)), and the rule now also matches `toString(16).padStart(2` with withAlpha\u2019s own body as the sanctioned exception. Verified the guard bites by reinstating one. NOT DONE, AND WHY. The exercise page was on the list as \u2018the same disease\u2019 and is NOT: it already resolves its hue through the shared `kindStroke` (chartreuse IS the lift channel), and its charts are a deliberate full-bleed slide PAGER \u2014 two comments in the file say \u2018unboxed & full-bleed\u2019 \u2014 so boxing them would fight the design rather than fix it. One chart at a time inside a pager is a different object from a vertical stack of charts on bare ink, and it wants no panel. The ~55 other ink2 surfaces across the app stay open: they are panel-SHAPED but genuinely disagree on radius and padding, which is a judgement per site." },
  { id: "shared-panel-and-effort-bar", area: "Mobile", status: "shipped", title: "APanel + AEffortBar \u2014 two surfaces that were written seven times", detail: "FOLLOW-ON to sport-page-palette, and it started as a self-inflicted wound: that change hand-rolled a `panel` const (ink2 + hairline + RADIUS.field + 12dp) on the sport page, which made it the EIGHTH verbatim copy of the same four properties. THE PANEL. `APanel` is now in the mobile kit \u2014 the surface a READING sits on, as against ACard\u2019s surface for a THING (a card takes the SwiftUI glass, RADIUS.card, CARD_PAD 20 and a shadow because it is an object you tap into; a panel carries a chart or a stat column where translucency costs contrast and 20dp costs plot width). It owns the SURFACE and nothing else \u2014 width, minHeight and gap stay with the caller through `style`, because those are layout. `onPress` renders it as PressScale (so a tappable panel cannot drift to a different press feel) and `bind` spreads a chart\u2019s scrub handlers, typed structurally as ViewProps because chart-scrub imports the kit and the type cannot come back the other way. SEVEN CALL SITES CONVERTED: the endurance lanes\u2019 Tile, the Other-sports lane (both its pressable and its static branch), Today\u2019s exercise-favourites card, the endurance summary (keeps its 14dp pad and its shadow through `pad` + `style`, so zero visual change), the Exercises browser\u2019s slab and the favourites sheet\u2019s slab (both `paddingVertical: 4` over the base pad \u2014 RN\u2019s more-specific-wins rule), and the sport page\u2019s own charts. THE INTENSITY TRACK. `AEffortBar` is the three-band easy/steady/hard bar. It existed twice and disagreed about everything that matters: the lanes\u2019 ZoneTile drew three hues, the sport page drew one chartreuse at three densities, and the two also differed on band radius (a raw 2 per band vs one pill on the track) and on the zero-band rule \u2014 the same reading, from the same engine (paceEffortSplit), looking like two different measurements. The BAR is shared and the LEGEND is NOT, deliberately: ZoneTile had already written down why its legend stacks (\u2018three zone words side by side blow a 152dp rail tile apart the moment Steady becomes Gleichm\u00e4\u00dfig\u2019), and the sport page\u2019s section is full-bleed, so sharing the legend would force one of them to be wrong. THE PERCENTAGES MOVED TO CORE with it: the sport page rounded each band independently and could print 64 / 24 / 13 \u2014 it reads `zonePercents` now, which settles the remainder so the three integers sum to exactly 100. ALSO ON THE SPORT PAGE: the record sheet\u2019s save button was the last hand-rolled chartreuse pill on the screen (its own padding, its own face, no commit state) and is `<APill size=\"compact\" />`; the bespoke `MarkerSpark` (a 74\u00d744 SVG line) gave way to the kit\u2019s `Spark`, so the app has ONE spark drawing again and the marker trend reads in the same grammar as the volume bars below it \u2014 a FORM change (line \u2192 bars), not just a refactor; and the local `SectionHead` had already gone to `ASection` in the previous change. RATCHETS: raw-borderRadius 142 \u2192 141 (ZoneTile\u2019s three raw band corners became one RADIUS.pill on the track), chartreuse-pill 29 \u2192 28, and the AMeter ratchet 6 \u2192 5 \u2014 with `AEffortBar` added to that rule\u2019s SANCTIONED list for exactly the reason `AMeter` is on it (the answer must not be counted as a violation of itself, or zero is unreachable), and with `EffortSplitBar` RENAMED to `EffortLegend` because with the bar shared it draws no bar \u2014 the file\u2019s own UndoBar \u2192 UndoToast lesson, the code moving with the name rather than a reclassification to shrink a number. Core + mobile suites green, both typechecks clean, iOS bundle exports clean." },
  { id: "sport-page-palette", area: "Mobile", status: "shipped", title: "Sport page — the one screen the app's colour never reached", detail: "REPORT: \"the individual sport page is missing new color schema? Why is it so black? It\u2019s visually totally different than the whole app.\" It was, and for three compounding reasons, each fixed at its own level. (1) THE GROUND — the real one. AuroraScreen mounts the ambient AuroraField (the chartreuse corner, the terracotta counterweight, the teal depth glow) behind every screen, but the HERO shell gated it on `backdrop === \"field\"`, and core\u2019s heroBackdrop returns art/wash for rank `cover`. The sport page is the ONLY cover-rank screen in the app (45 screens are `title`), so it was the only screen in the app whose body sat on flat #0c0d0c. The rule that motivated the gate \u2014 \u2018the page\u2019s wash must not show through the cover\u2019 \u2014 is about the HERO, not the screen, and it still holds without the gate: the hero frame paints opaque HERO_INK across its own box and the duotone wash sits on that, so the field is only ever visible under the BODY. hero.tsx now mounts it for every non-takeover rank (a takeover keeps its bare ground \u2014 it does not scroll and its subject is the numbers on it). (2) THE HUE VOCABULARY. Every chart on the page was CHARTREUSE \u2014 volume bars, pace line, marker spark, and an effort split drawn as one hue at three densities. Chartreuse is the \u2018go\u2019 colour (the docked Log-session pill, a PR, an improving delta), so the page had one hue for everything and nothing that read as go; worse, it disagreed with both surfaces that push INTO it \u2014 the Endurance lanes draw the same eight-week volume and the same pace trend in TEAL, and Today\u2019s Other-sports tiles draw a sport\u2019s weeks in SAND (\u2018teal already means cardio on the lanes directly above this block\u2019). A sport was tapped in one colour and its page opened in another. SportPageModel now carries `discipline` (the same cardioDiscipline() call sportSessions already narrows the slice with \u2014 the catalog decides, not the client), and the page paints its charts TEAL for an endurance discipline and SAND for the `sport` bucket. Chartreuse retreats to what it means everywhere else: the PR dot, the improving delta, the transfer tag and the docked action. The effort split moved to the app\u2019s cool-to-warm cost ramp (chartreuse / sand / terracotta) \u2014 the identical reading from the identical engine (paceEffortSplit) was already coded three-hue in the lanes\u2019 ZoneTile, so the one-hue argument written in the file had already lost. (3) THE SURFACE. The page drew ZERO surfaces \u2014 no card, no panel, no ink2 anywhere in 681 lines \u2014 so it was text and hairlines on black while its twins sit on tiles. The two charts and the effort bar now sit on the ink2/hairline/RADIUS.field panel the endurance lanes\u2019 Tile and the Other-sports tiles use; the ladder, the bests and the recent efforts deliberately stay hairline rows on the ground, because they are LISTS and the app draws lists that way (the Sports index one screen back included). ALSO: the local SectionHead reimplementation gave way to the kit\u2019s ASection; the unlit volume bar moved off ALPHA.rim (a BORDER rung, used on a surface) onto ALPHA.line, which is the shared HistoryStrip\u2019s own pair, so a sport\u2019s weeks read identically here and in the rail that opened this page; both chart dots ring in ink2, the panel they now sit on. Two stale comments that would have re-taught the wrong colour were corrected in passing \u2014 other-sports.tsx still called its sand strip \u2018Violet\u2019, and history-strip.tsx\u2019s doc named brand colours (Lyons Blue, Fleur De Lis) that had outlived their values. Core + mobile suites green (the colour-arithmetic ratchet fell 105 \u2192 104 and was re-pinned), mobile typecheck clean." },
  { id: "sport-page-reduction", area: "Mobile", status: "shipped", title: "Sport page — eleven sections down to six", detail: "SHIPPED on mobile (teardown + five drafts: design/sport-page-redesign.artifact.html). The page had grown to eleven stacked sections of equal rank and said the same thing three times before it said anything new. WHAT WENT, and why each one was a repeat rather than a trim. (1) THE TOTALS ROW. Its cells were efforts / hours / distance / this week; the hero's own meta line two hundred points above states efforts, distance and since. On a TIMED sport it was worse than a repeat — the primary figure falls through to `time`, so the 46px headline and the 20px Hours cell printed the same string. The hero counts; the page states a performance. SportTotal and m.totals are RETIRED from the model with it — a model whose whole discipline is that no figure exists without a consumer cannot keep a dead one. The duration-formatting regressions that row used to pin were repointed onto the figures that still render (the primary, the longest session, the week series). (2) THE FASTEST BEST. Best pace was the headline, then Pace › Best, then the Bests rail's first card — one reduction over one array at three visual weights. Retired from SportBest entirely: a sport with a ladder states pace per rung, and a paced sport without one states it as `primary`, so every sport says it exactly once. (3) THE BESTS RAIL'S CARDS. 176-wide bordered cards on a page whose totals row was commented 'facts on hairlines, no cards' — two materials for one job, with the fourth card off the screen edge. Now hairline rows, all visible at once. (4) THE INLINE MARKER FIELD, the only writable thing on a reading page, sitting between the headline and the totals. It is a Sheet now, behind a door row at the end of the ladder — ringed glyph, because it leaves. (5) TRANSFER + WHY THESE LIFTS → their own screen, see sport-strength-screen. WHAT WAS ADDED: the record ladder (sport-records) and the door row at the end of Recent efforts, which used to show three and stop with no way to the other thirty-one — the project's own exit rule, unclosed on this screen until now. THE HERO IS UNTOUCHED: an earlier draft moved the figure into the cover and was withdrawn on review. It is the hero system's — one rail, one collapse track, one title ladder shared with Plan, Goal, Recipe and Exercise — and its meta line staying a count line is precisely why the page below it must stop counting. THE TWO CHARTS STAY TWO: volume is bars, pace is a line, in two sections with two grammars, deliberately not merged into one frame." },
  { id: "sport-strength-screen", area: "Mobile", status: "shipped", title: "Strength that carries — the prescription as its own screen", detail: "SHIPPED on mobile (app/sport-strength.tsx + components/aurora/sport-strength.tsx). The S&C prescription was the sport page's ninth and tenth sections, and it was in the wrong tense: the sport page answers 'where am I?' in the past (a record, a chart, a log) and a prescription is the future. Three costs, all fixed by the move. (1) TWO PRIMARY ACTIONS: the page's dock logs a run while the prescription's inline pill starts a gym session — two chartreuse CTAs at the same rank, which is the exact ambiguity the docked slot exists to prevent. The prescription owns a dock of its own here; the sport page is left with one verb. (2) AN EMPTY PAGE THAT WAS NOT EMPTY: a sport with nothing logged printed 'Nothing logged yet' and then ~1200dp of level picker, demands, blocks and rationale. A prescription needs a sport and a level, NOT a history, so it is a door that works on day one rather than a section buried under an empty record. (3) A SECOND LIST OF THE SAME SIX LIFTS: 'Why these lifts' repeated the pool with a sentence each; the sentence now rides its own prescribed block and the section is gone. ALSO FIXED HERE: the demands' rank was encoded three times — an ordinal, a bar whose width stepped 56 − 11i, and an opacity that stepped 1 − 0.2i. The ordinal is true and the bar was arithmetic on the list index wearing the look of a measurement, so only the ordinal survives. The locked lifts were full rows at 45% opacity (unreadable and still full height) and are now one expander — bare ＋, no ring, ash not chartreuse, per the exit grammar: it grows in place, it goes nowhere. The LEVEL is the same `hybrid.sport` store the sport page reads, so picking Advanced here picks it everywhere — one setting, not two." },
  { id: "sport-records", area: "Core", status: "shipped", title: "Personal records — a time at a distance", detail: "SHIPPED on mobile (design: design/sport-page-redesign.artifact.html). The sport page could state a best PACE, a longest run and a biggest week, and not the one number a runner actually answers with: what they run five kilometres in. A swimmer's 100 m and a rower's 2 k were the same hole. THE LADDER IS CATALOG-DRIVEN, like every other section: `OlympicSport.records` (SportBenchmark[], in km — a 100 m swim is 0.1) declares the distances a sport is conventionally measured at, and the rung flagged `marker: true` is the one the sport's typed marker states. Eight sports carry one — the road ladder (1/5/10/half/marathon, shared by Running and Marathon), the pool (100/400/1500), the erg (500/2000/5000), plus Race Walking, Canoe Sprint, Cycling and Track Cycling. Mountain Biking deliberately has NONE: there is no conventional MTB distance, and a ladder of invented rungs is the fabricated metric this catalog exists to prevent. THE MATCHING RULE, and its asymmetry is the whole point: an effort counts for a rung when it covered AT LEAST the distance and no more than 5% over (RECORD_BAND). 5.2 km counts as a 5 km because the extra 200 m can only make the time WORSE; 4.9 km never counts because it would flatter. A rung is therefore a FLOOR — never better than the truth, occasionally a little worse — which is the right direction for a figure somebody might quote out loud. SEGMENTS NOW FILL RUNGS TOO (see sport-records-segments, since shipped): where a recording carried a route, the fastest window covering exactly the rung is stored as a `best` lap and fills it, marked so the athlete can tell a raced rung from one found inside a training run. That changed the matching rule into TWO rules, and the asymmetry is deliberate — a logged effort matches on RECORD_BAND because nobody runs exactly 5.000 km, while a segment matches on EQUALITY because it was derived at the rung; banding an already-exact figure would let a 5.25 km window stand for a 5 km and understate the athlete for nothing. Where one session offers both (a 5.02 km parkrun with a route), the SEGMENT wins and the effort is dropped — they are the same run measured twice, and keeping both made the ladder report the athlete beating themselves by four seconds on a run they did once. The explainer sheet (sport-records-sheet.tsx) states both rules to the athlete rather than leaving them to be discovered, and reads RECORD_BAND itself so the sentence cannot drift from the rule. THE FIGURE IS THE WATCH'S: the time reads SportEffort.seconds (cardioSeconds where a recording exists), so a 22:41 parkrun logged as 23 min still shows 22:41 — rounding to minutes would have lost the record by 19 seconds. format.ts gained clock() for it, since mmss alone renders a 1h52 half marathon as '112:10'. THE DELTA is against the best that STOOD, walking the efforts chronologically — a delta against the second-fastest-ever would report the same figure on a rung improved in one jump as on one chipped at for a year. TYPED AND MEASURED BOTH SHOW, and never look alike: the athlete's typed marker fills its own rung (and only its own) when it beats what was measured, carries the TYPED mark, and hands the rung back to the measurement the moment a logged effort is faster. A typed rung derives NO pace — there is no measured distance behind a claim. A marker that is not a time at a distance (Cycling's FTP, in watts) fills no rung and keeps its own line beside the ladder (`markerAside`). 14 tests." },
  { id: "sport-volume-intensity-fill", area: "Mobile", status: "planned", title: "The week's intensity mix, in the volume bar's own fill", detail: "DELIBERATELY DEFERRED from the sport-page redesign, and worth stating why rather than letting it look forgotten. The Effort section is a 12px stacked percentage bar showing easy/steady/hard across the WHOLE eight-week window — a fourth chart grammar on a page that already had three, and one that cannot answer the question an athlete actually has (\"was LAST week the hard one?\"). The proposal: fold it into the volume bars, so each week's bar is a stack of easy/steady/hard in ONE hue at three densities. Height stays the week's distance, the fill becomes its intensity mix, and a whole section goes. THE ONLY MODEL WORK is a per-week split: paceEffortSplit already exists and already computes exactly this — it is simply run over the whole slice today, so it needs the bucket as its input rather than the window. Not a new aggregate, not a new engine. IT WAS HELD BACK because it is the one piece of the redesign with a real design risk: three densities of chartreuse in a 104dp bar, stacked, may not separate on a phone in daylight, and that is a device question rather than a simulator one. The rest of the redesign shipped without it and reads well — the bars are plain and the split keeps its strip. Ship it behind a device round on the next TestFlight build, with plain bars + the strip as the standing fallback. RE-SPEC (Aug 2026, after sport-page-palette + shared-panel-and-effort-bar): THE PREMISE ABOVE HAS MOVED and this entry would otherwise send the next session to build the wrong thing. (1) The split is no longer one hue at three densities \u2014 it is the app\u2019s cool-to-warm ramp (chartreuse / Fleur De Lis / Muskmelon) in the shared `AEffortBar`, so the stated risk (\u2018three densities of chartreuse \u2026 may not separate on a phone in daylight\u2019) is not the risk any more. The new question is whether THREE HUES stacked inside a bar whose HEIGHT already encodes distance reads as one quantity or as two fighting ones \u2014 a different device question, and a harder one, because hue is louder than density. (2) The volume bars are no longer chartreuse either: they wear the sport\u2019s CHANNEL, so a folded-in intensity mix would put the cost ramp INSIDE the channel colour. On an endurance page that is Lyons Blue bars holding a chartreuse/Fleur De Lis/Muskmelon stack \u2014 four hues in one 104dp bar, which is the thing to prototype first. The obvious worry is the Fleur De Lis channel, where the bar\u2019s own hue would equal the split\u2019s middle band; AUDITED, and it cannot happen today \u2014 all 41 sports on the `sport` channel are unpaced (`metrics: TIME`), so `split` is null and the Effort section never renders on an amber page. It is a constraint on the CATALOG rather than a live collision: the day a paced sport resolves to the `sport` discipline, the fold has to answer for it. (3) The bar now sits on an ink2 APanel rather than on bare ink, which is the one change that helps: a stacked bar needs a ground to read against and now has one. STILL WORTH DOING \u2014 the argument for folding a whole section away is unchanged, and the model work is still just handing paceEffortSplit the bucket instead of the window. But it needs a fresh design pass, not this one\u2019s." },
  { id: "session-muscle-map", area: "Core", status: "shipped", title: "Where a session's work landed, per muscle", detail: "SHIPPED — engine and panel. session-muscle-map.ts joins two things the app has had for a year and never introduced to each other: exercise-anatomy.ts ranks the muscles a LIFT drives to the percentage point, and body-map.ts can draw them lit on a front/back figure — both wired only to the single-exercise page. Meanwhile the session summary's muscle read runs on the engine's seven coarse buckets (volumeByMuscle), which cannot tell a press day from a raise day because both are 'shoulders'. WHAT IT COMPUTES: each strength block's tonnage — by exactly the arithmetic volumeByMuscle uses, so the two can never disagree about how much work happened — split across that lift's own activation shares. Bench Press is chest 61 / triceps 22 / front delts 17, so 2 900 kg of benching puts 1 769 kg through the chest; a session's map is the sum over its blocks, ranked, with the rounding drift fixed on the top row so the column reads a clean 100%. FOUR EXPORTS: sessionMuscleMap (the ranked rows, each with share, tonnage, sets, tier and an optional delta), sessionMuscleGlows (the same MuscleGlow shape the per-exercise figure renders, so ONE renderer draws both), muscleBaseline (per-muscle mean over the sessions that TRAINED it — averaging in the six days you did not bench would read +400% every time and mean nothing), and muscleCoverage (days since each muscle was last DRIVEN, for the neglect line; assisting somebody else's press is not training). FOUR DECISIONS WORTH KEEPING: (1) a lift the catalog does not know is REPORTED in `unmapped`, never attributed by guesswork — a session of entirely custom work returns an empty map so the client can say so instead of drawing a body lit at zero; (2) holds and carries are excluded, because seconds times a load is not work, matching every other tonnage site; (3) tiers are driver/assist only — stabilisers live in the anatomy model as prose strings ('Core (abs & obliques)'), not typed muscles, so a third 'brace' tier would have to guess at a mapping, and the union is where they land when they become typed; (4) a delta is withheld until BASELINE_MIN_SESSIONS sessions back it, because one session is not a norm. levelFor was exported from exercise-anatomy so the session rows band by the same thresholds a single lift's rows do rather than re-deriving them. 17 tests. THE PANEL: components/aurora/session-body.tsx, second in the summary sequence — the hero says HOW MUCH, the body says WHERE. It draws the SHARED mannequin (aurora/body-map.tsx now exports BodyFigures, which the exercise page also uses) fed sessionMuscleGlows, so one renderer serves both and the two cannot come to disagree about what 'lit' means. THE LEDGER UNDER IT IS THE PICTURE'S TEXT ALTERNATIVE, not a second read: every muscle the figure lights is named in order with its tonnage, so nothing on the panel is carried by colour alone. Tapping a muscle — on the figure or in a row — moves the headline to it, which is how an athlete asks 'how much of that was actually triceps'. The panel is ABSENT, not dark, for a session with no mapped lifting. THE COARSE READ IS GONE WITH IT: the seven-bucket MuscleFocus card in the details section (and the private MUSCLE_LABEL map it carried) was deleted — two muscle vocabularies on one screen was the defect, and keeping the blunt one beneath the sharp one would have been the same defect with more scrolling. New keys session.body.* in EN/PL/DE." },
  { id: "session-summary-redesign", area: "Mobile", status: "shipped", title: "The session summary — one screen per section, and an instrument family with no bars", detail: "ONE SCREEN PER SECTION, EVERY SCREEN FULL, AND NOT A BAR CHART LEFT. THE FORMAT WAS NEVER THE DEFECT. The summary went panels → document → panels again, and the round trip is the useful part of the record: a panel is a FIXED 844pt screen, so something must take up whatever the content does not, and the first sequence gave that job to `<View style={{ flex: 1 }} />`. A bare spacer pools all the leftover height into ONE hole — four of five screens photographed as black ink with a figure at the bottom — while the same file carried the law 'no flex spacer ever holds a panel open' two screens above the spacer that broke it. THE FIX IS THE INSTRUMENT ZONE: it measures itself and hands its height to the CHART, so a taller screen draws a taller picture instead of a longer gap. Four zones per panel — MARK, SUBJECT (exactly one magnitude), INSTRUMENT (absorbs the slack), LEDGER — and with no instrument the LEDGER takes the flex and spreads, so space runs THROUGH the content rather than pooling above it. A PANEL EXISTS ONLY WHEN IT CAN FILL ONE: the record rides the hero as a strip until it has a history to draw. Guarded as BUILD FAILURES, not ratchets ('no MORE spacers' is a budget for the ones that made the holes). THE INSTRUMENT FAMILY — the second thing the reporter rejected, and the more interesting one. Four charts on one scroll had converged on a row of rectangles for four different questions. A bar is the least specific picture available: a rectangle standing in for a number, saying nothing about what KIND of number it is. Four of them read as filler because that is what they were. The form is now chosen by the FACT. TRACE — a continuous quantity over time, drawn unbroken because it never stopped existing between samples (the tonnage curve, the heart-rate trace, the route). PATH — discrete events in order, each a MARK at its value joined to the next, so a ramp reads as a climb, straight sets as a level run and a drop set as a fall; as bars all three were the same picture with slightly different heights (`SetPath`, `LiftTrend`). RANGE — a handful of things compared, one dot each on a shared axis, because a bar encodes the value twice (position AND length) and buries the differences at the top of the scale where they matter most (`EffortRange`). RIBBON — parts of ONE whole, a single strip cut, because five zone meters asserted five separate quantities and left the reader adding them back into the session they came from (`Ribbon`, serving both the zones and the splits). One hue per instrument varying in opacity, so intensity reads as intensity rather than as five unrelated categories. `LiftTrend` is the one worth naming: a record is only legible AS A HISTORY, so the standing best is a dashed RULE across the plot and today's mark sits above it — the picture is a ceiling breaking, not a row of rectangles of which the last is tallest. A guard asserts no `<Rect>` returns to any summary instrument. RETIRED with the bars: `SetSpine`, `LiftHistory`, `ExerciseTops` and `SplitRows`. THE RECORDING IS ON A SCREEN, and the correction matters: this was recorded as blocked on a missing endpoint and it never was. GET /api/sessions/[id]/streams has been complete since the streams work landed — streams, laps, and seconds-per-zone derived ON READ because that denominator moves as the athlete's measured max goes up — and the phone had been POSTing to it the whole time. Nothing called the GET. The missing piece was one fetch. THE INTENSITY PANEL: average heart rate as the subject IN DEVICE BLUE (a wrist counted it, so it wears neither the tilde nor the chalk a logged figure wears), the whole-session trace as the instrument, the zone ribbon as the ledger, and the zones stated relative to the measured max IN WORDS, since zones without their denominator read as physiology rather than as a ratio against one figure. THREE READINGS AN AVERAGE CANNOT GIVE, all pure and tested in core: `secondsAbove` (time-weighted, never sample-counted — an irregularly-sampled stream over-counts wherever the watch sampled densely), `hrDriftPct` (split by the CLOCK, not the sample index, so dense early sampling does not misplace the midpoint), and `hrFallLastMinute` — DELIBERATELY NOT CALLED RECOVERY HEART RATE, because the clinical figure is measured a minute AFTER the effort stops and a workout recording ends when the effort does. Presenting it as that would be inventing a measurement; what it honestly reports is the recording's own last minute. THE ROUTE is the measured panel's instrument — `routePoints` normalises to a unit box with longitude scaled by cos(latitude), without which every route north of the tropics renders stretched sideways, and the shorter axis is centred so an out-and-back does not hug an edge. A TRACE, not a map: no tiles, no basemap, no network, no attribution — the streets are somebody else's data and somebody else's licence, and the SHAPE is the part that is recognisable at thumbnail size, which is the test that matters for the thing people post. THE FIGURE IS A TRACE NOW, and that is what closed the last open case rather than buying it. 'Commissioned anatomical illustration' had been carried as the remaining item, priced as a purchase. It is not one: what separates an anatomical drawing from a silhouette is not resolution, it is that the muscles are DRAWN — a contour, and fibres running the length of the belly — and both are geometry that was already here. So the body joins the instrument family as a TRACE. Step one, `bodyPath`: a Catmull-Rom spline fitted as cubic Béziers passing through every authored point EXACTLY, so the anatomy stays where it was placed and the segments between arrive curved (the figure was straight-edged polygons of four or five points — the arms were literally rectangles). Silhouette and all twenty regions re-authored with real taper. Step two, `muscleFibres`: the grain, as lines along each muscle's own long axis, which the client clips to the muscle's shape. THE AXIS IS COMPUTED, NOT AUTHORED — the principal component of a polygon's vertices IS the direction its belly runs, so a lat gets a broad diagonal grain and a bicep a long vertical one without anybody hand-placing a stroke, and nobody has to re-place them when a shape is retouched, which is how illustration goes stale. Step three: INTENSITY RIDES THE INK. A worked muscle gets a firmer contour and darker fibres; an untouched one stays a hairline but is still anatomy. The fill survives only as a whisper, so a muscle remains a tappable area rather than a set of lines with gaps between them. One change, in core, so the panel figure, the exercise page and the share card all get it — smoothing or hatching applied per client would have been two bodies waiting to disagree. `bodyPath` at tension 0 returns the polygon it started as, so the old reading stays reachable without a second code path. THE SKETCH SEAM STAYS, and is no longer this surface's business: `SKETCH_BODY_ART` belongs to exercise-video-library's commissioned-art plan alongside SKETCH_MEDIA, and deleting it to tidy up here would have taken another capability's option with it. Populating it is now a taste decision about a figure that already reads, not a dependency anything waits on. THE DATA DEFECTS the reporter's screenshots proved, all fixed: the magnitude is the SESSION'S figure, not the record's load (`heroFigure` set '75 kg' at display size on three of six screens while the 10.2 t actually moved sat in a bordered tile); `fmtWeight` groups DETERMINISTICALLY instead of against the handset ('3.229 kg' beside a '10.2 t' whose dot is a decimal — precisely the defect feed-grammar diagnosed, wrote up and pinned for `feedStatText`, left standing at the formatter forty-six call sites use); the e1RM and its trend NAME their lift (`topLift` picks by highest estimated max, so both figures were a second exercise's while every other surface led with the pull-up, and an unlabelled red −16 read as the session going backwards); the seven-bucket muscle fact is gone (it put 'BACK 7,020 kg' two scrolls under a ledger reading 'LATS 3,229 / UPPER BACK 1,787'); the neglect line excludes what the session just drove, since it is the one line that says what to train TOMORROW; bodyweight sets read their true load via `setLoadText` (five sets of a 75 kg PR printed '– × 7'); the warm-up legend only claims what it draws; 'What this set really did' headed a panel about a session. Two more found while rebuilding: the premium panel led with the session's TONNAGE while its chart drew a LIFT'S estimated max, and it repeated the hero's magnitude — its key is now gated on the FILTERED fact list, or a session whose only fact was the headline deals a dot, a snap offset and a blank screen. SHARING IS ONE COMPOSED CARD, previewed — deliberately NOT restored to per-panel capture even though panels are back, because 'every panel is already the share frame' is the exact law that made an empty panel structurally mandatory. Built from the values the panels read, so it cannot disagree with the screen it came from; the preview is sized to the sheet and the capture node sits off-screen at export width. NOTHING ON THIS SURFACE IS OPEN." },
  { id: "sport-records-segments", area: "Core", status: "shipped", title: "Records from inside a longer effort (laps, splits, routes)", detail: "SHIPPED, and it closes the ladder's oldest hole. THE GAP WAS: a record rung had to be an effort logged AT the distance, so the fastest 5 km inside an 8 km run was invisible — a real miss against the best-efforts every runner who has used Strava expects. THE BLOCKER WAS THE DATA, and it is gone: session-streams imports the GPS route, integrates the cumulative distance series from its own fixes, and `bestEffortsFromDistance` finds the fastest window covering each catalog distance by a two-pointer sweep with BOTH ENDS INTERPOLATED — so a 5 km best is the time to cover exactly 5 km, not the time to reach whichever sample first passed it. Stored as `best` rows on SessionLap and indexed on (userId, kind, distanceKm, durationSec), because a personal record has to be one indexed lookup rather than a scan over every recording. THE RUNGS ARE CATALOG-DRIVEN: `lapDerivationFor` reads `OlympicSport.records` (and splits at the sport's own `pacePer` — 100 m in the pool, 500 m on the erg, 1 km on the road), so the distances DERIVED and the distances the ladder ASKS FOR are the same list by construction. Add a rung to a sport and the next upload derives it; a sport the catalog gives no rungs (Mountain Biking, deliberately) derives none rather than inventing benchmark distances. THE READ SIDE: `sportRecords` takes `segments` beside the efforts and merges them into ONE chronological walk, so a segment can set a rung, be displaced by a later race, and name what it displaced — the delta still means 'the best that stood'. Four decisions worth keeping: (1) an effort matches on RECORD_BAND, a segment on EQUALITY (RUNG_EPSILON_KM is a float tolerance, half a metre, not a second band); (2) where one session offers both, the SEGMENT wins — a 5.02 km parkrun with a route yields both candidates and they are the same run, so keeping both had the ladder reporting an 8-second improvement over a run done once; (3) a segment is attributed by SESSION ID against the sport's already-narrowed slice, which is what stops a swim's best landing on the running page and means the client can hand over the athlete's whole set without reimplementing the narrowing; (4) a typed marker still beats a segment when it is faster, and still says TYPED. PROVENANCE IS SURFACED, not hidden: `SportRecord.segment` marks the row and the mobile ladder prints it (w.train.sportPage.recordSegment, EN/PL/DE), because a 5 km taken from inside a long run is a true 5 km and is still not the same story as a race — an athlete about to quote a figure out loud is entitled to know which they have. The explainer sheet was INVERTED to match (it used to say a 5 km inside a longer run does not count) and now states when it does: the recording must carry a route, since a bare summary has no trace to look inside. Wiring: GET /api/sessions/bests (owner-only, archived excluded, soft-degrading to an empty set before the lap table exists) → mobile `fetchSessionBests` → `sportPageModel({ segmentBests })`. 9 tests on the ladder, 4 on the catalog-driven rungs." },
  { id: "chart-scrub", area: "Core", status: "shipped", title: "Hold a chart to read the value under your finger", detail: "SHIPPED on BOTH clients, on the sport page's two charts (volume bars + pace trend). A trend answers \"which way\" and never \"how much, that week\": the eight-week bars named only their first and last bucket, and the pace line named none of them, so the six weeks in between were a shape with no figures. Holding the chart — the stock-app gesture — now states the held week's own figure, formatted in the sport's own unit (26.0 km, 2 400 m, 75 min, 4:32 /km, 2:12 /100m), with the week's date and its effort count. CORE: chart-scrub.ts — scrubIndex()/scrubPosition()/scrubFraction() hit-test in FRACTIONS of the plot width (the two clients draw the same chart at different widths, and a fraction survives both) in two modes: `band` for bars, where each bar owns an equal slice, and `point` for a line, where the NEAREST plotted point wins and an `inset` keeps both ends reachable past their own dot. sport-page.ts formats the readout (sportVolumeReading / sportPaceReading) rather than either client branching on unit — a swim reading '2.4 km' in the browser and '2 400 m' on the phone is exactly the drift that puts the maths in core. The pace trend SKIPS the weeks with nothing paced in them, so `pace.weekStarts` now ships alongside `pace.trend`: without it a held point would be dated by the volume bars' index and name the wrong week (a two-point trend eight weeks apart dated its second point six weeks early). 20 unit tests, including the round-trip that the position of a point reads back as that point. CLIENTS: only the GESTURE differs — web uses pointer events with `touch-action: pan-y` (a vertical drag still scrolls the page; a mouse reads on HOVER with no press at all; the arrow keys, Home/End and Escape walk the series for a keyboard, and the readout is an aria-live region inside a labelled role=group, not a role=img whose contents would be presentational), mobile a PanResponder that claims the touch on CONTACT (a still finger is the gesture) but grants every termination request, so the enclosing ScrollView takes the drag back the moment it turns out to be a scroll. Mobile reads `pageX` against a measured box rather than `locationX`, which is relative to whichever bar the finger is over and changes every few millimetres mid-drag; it fires `haptic.selection` on each crossing into a new week, Apple's own use for a discrete step. THE READOUT never hides its own answer: it pins to the top of the plot on the side the finger is NOT on, so it needs no reserved row and the chart never shifts the page when held. Held, the finger's week is the lit bar (the 'this week' accent would otherwise compete with the answer that was asked for), a hairline crosshair runs the plot height, and the line chart carries a chalk dot at the point beside its lime PR dot. WAVE 2 — TODAY'S ENDURANCE LANES (both clients, and with them the Endurance screen, which renders the same block headless). Every lane's volume strip and pace trend now hold too, off `laneVolumeReading`/`lanePaceReading` and the same `paceTrendWeeks` alignment fix the sport page needed. TWO THINGS ARE DIFFERENT IN A RAIL, and both follow from the rail. (1) THE TARGET IS THE WHOLE TILE, not the drawing: a 24px strip is a fair chart and an unfair touch target, so the press lands anywhere on the 176px card while the fraction is still measured against the plot (the hook's `plotRef`, added for exactly this). (2) THE READOUT IS THE TILE'S OWN TWO SLOTS — holding swaps the LABEL for the week ('Week of 27 Jul') and the FIGURE for that week's value, which is the stock-app reading (the header figure changes as you scrub) and costs the tile no extra row: nothing moves, because nothing was added. A floating pill would have covered most of a card this size. THE RAIL'S SCROLL ALWAYS WINS — it is how the athlete reaches the rest of the cards: web leaves `touch-action` alone inside a rail (`SCRUB_STYLE_IN_RAIL`) so the gesture is a DWELL (hold still and the figure appears, move and the rail carries on), and mobile grants every termination request as before. Web also gained a `HOLD_MS` dwell before a TOUCH shows anything, on every held chart: without it every swipe that begins on a chart flashes a readout for the frame before the browser takes the pan — the mobile twin gets that free from `delaysContentTouches`. The hook itself moved out of the sport page into components/aurora/chart-scrub.tsx on BOTH clients, so a third chart is a hook call rather than a copy. HistoryStrip (the shared 8-bar strip behind the exercises rail, the lanes and the other-sports tiles) gained an optional `held` so the held bar lights instead of the latest one. Mobile re-measures the plot's window box on every press, because a rail that has been scrolled since layout would otherwise answer the first press off a stale x. `chart.weekOf` is a BASE i18n key (EN/PL/DE), not a per-screen one — the sport page and the lanes say the same sentence and the parity guard exists to stop that being two keys. WAVE 3 — THE ANALYTICS SURFACES (both clients). THE EXERCISE PAGE: the weight/e1RM trend, the pace trend, weekly tonnage, weekly minutes and the run deltas all hold, off `exerciseSlideGeometry` + `exerciseSlideReading`. Core decides WHICH slides answer at all, and five deliberately do not — the load×reps scatter, the tonnage surface, the rep-max grid, the consistency heat map and the meter rows (zones, pace curve) already name their own cells, and there is no single series under the finger to report; a test asserts the nulls so a future slide has to opt in rather than inherit a gesture that would answer nothing. On WEB this REPLACED recharts' `<Tooltip>` on those five charts: a tooltip is a hover affordance and half the athletes are on a phone, so the readout that works for both is the one that ships. The crosshair and the held dot are recharts' own ReferenceLine/ReferenceDot so they land in DATA space, while the gesture is measured against an inset overlay carrying the hook's `plotRef` — recharts owns the plot geometry, and hit-testing the container would be off by its margins. The slides are SWIPED, so both clients use the in-rail gesture and the pager keeps every drag. TRENDS: the sets and tonnage bands hold, off `volumeTrendReading`; a 160×46 spark has no room for a pill, so the band answers in the slots it has — the FIGURE becomes that week's and the average line beneath it becomes the week. TODAY'S EXERCISE RAIL: each card's strip holds too, which needed the one genuinely new gesture problem — the card is ALSO a button. The dwell settles it: a tap ends before HOLD_MS and opens the movement's page, a hold passes it and reads the strip (web cancels the click that would have followed; mobile's PanResponder fires `onTap` when the press ended before the dwell). A mouse gets both at once — hover to read, click to open. The card's strip is not a second focus stop inside the button; its figures stay reachable because the page it opens has the same chart full size and keyboard-driven. The card model gained `sparkBy` + `sparkAt` for it: the strip is one point per SESSION for a lift with parseable loads and one per WEEK for a movement measured in volume or minutes, and a readout that says 'Week of 12 Jul' over a single session's lift is the same class of lie as dating a trend point by the wrong bucket. `ChartReadout` + `readoutSide` moved into components/aurora/chart-scrub.tsx on both clients alongside the hook, so a chart with room of its own pins the shared pill and a card-sized one swaps its own label and figure — two placements, one component. WAVE 4 — TODAY'S OTHER-SPORTS TILES, the last surface. It was held back one wave for a reason the model had to fix first: `OtherSportLane.weeks` was a bare `number[]`, so a held bar could have shown a figure it could not date — the very misdating the feature exists to prevent. `weekStarts` now ships aligned with it (computed once per call, since the trailing 7-day buckets are identical for every lane) and `otherSportReading` reads a bar. THE READOUT LANDS IN THE FOOT, not the headline: the headline is ALL-TIME EFFORTS and the strip is MINUTES PER WEEK — two different measures — so the hours cell becomes the held week's minutes and '3 days ago' becomes the week itself, and the tile never puts a quantity in a slot that never meant it. `efforts` on the reading is deliberately null: these buckets count minutes, and reporting a number the series does not hold would be inventing one. The tile is a button, so it reuses the exercise rail's dwell (tap opens the sport's page, hold reads the strip). 5 tests, including that each bucket's start really does precede the efforts bucketed into it. WAVE 5 — THE SESSION SUMMARY'S PER-LIFT TRENDS, which closes the feature: every chart in the app now holds. This one was held back twice as a judgment (web already draws labelled x/y axes there, mobile's strip sits under rows stating the same figures) and the judgment was wrong on the half that mattered: web had a recharts hover TOOLTIP and mobile had NOTHING, which is a parity gap under the project's own rule — the same page answered a question on one client and not the other. Both now use the shared gesture: `e1rmPointReading` / `pacePointReading` format the figure, web replaced its Tooltip with the pinned readout + a ReferenceLine/ReferenceDot in data space (keeping its axes, which are real context a 30dp strip cannot carry), and mobile's 30dp strips answer in the row they already have — the DELTA line becomes the held point's figure and its date, the held bar lights instead of the latest. Web's hit-testing insets the plot by the y-axis width (60px for a load, 48 for a clock), because a chart with visible axes starts well right of its own card. 4 tests." },
  { id: "hpi", area: "Core", status: "shipped", title: "HPI — Hybrid Performance Index", detail: "The headline 0..100 number that fuses strength + endurance + recovery (distinct from readiness, which is muscle-only). Reports its three pillars, its limiter, and a band; sport-weightable. Unit-tested and surfaced in the web dashboard Performance State card + the mobile Home. GATED: HPI is a Full feature — free (casual) users don't see the score (the Cockpit/Performance screens are athlete-nav-gated, the Today Performance-State block is athlete-only, and the Profile HPI hero shows a locked upsell instead of the number; see free-tier-gating)." },
  { id: "free-tier-gating", area: "Core", status: "shipped", title: "Free-tier feature gating (single source of truth)", detail: "packages/core/src/access.ts is the one place that decides what a FREE (casual) user can and can't do, so web + mobile gate identically (parity rule). Keyed on the resolved Persona (not raw entitlement) so it composes with resolvePersona — a client only reaches the athlete/Full surface when they've BOTH chosen athlete mode AND carry a paid entitlement; coaches/admins are Full by role. Predicates (unit-tested): isFullAccess, canSeeHPI, canEditEnrolledPlan, canScanFoodLabel, canSaveMealsAndProducts, canUseRecipes, canSaveRoutine (count-aware — free keeps up to FREE_TEMPLATE_LIMIT saved templates, see workout-routines). Enforced in the UI on both clients: (1) HPI — the Profile HPI hero + the spec-strip/ID-card index show a locked '🔒' + an 'Unlock with Full' upsell for casual users instead of the score (Cockpit/Performance are already athlete-nav-gated); (2) PLAN EDITING — a free user can browse, enrol in and follow a plan, but cannot edit the plan they're enrolled in (the reconciled 'This week' scheduler/re-sync only renders for athlete-or-coached on Today/Home, so casual users never get the edit control); (3) NUTRITION — free users add macros manually (kcal/protein/carbs/fat), but scanning a product label for an automatic add and saving meals/products to a library are Full features (the Nutrition screen shows a locked upsell for casual users; for Full users the buttons are present but tied to the blocked food database — see nutrition-fooddb). The upsell routes to the upgrade screen on both clients." },
  { id: "vbt-engine", area: "Core", status: "shipped", title: "Velocity-based training engine", detail: "velocity.ts: per-set summaries (best/mean/final mean concentric velocity, velocity loss %, ROM), velocity zones (absolute-strength → starting-speed with %1RM bands), a linear load–velocity profile fit (least squares, r²) that resolves an estimated 1RM at each movement's minimal velocity threshold, %1RM↔velocity conversion, and an autoregulated load recommender (target a bar speed → plate-rounded kg). Reads (load, velocity) straight off logged sessions (StrengthSet.vel) and adds a barVelocity Signal kind. The prescription engine is now velocity-aware: pass velocityProfiles(sessions) and the working load anchors to the velocity-estimated 1RM (which moves with daily readiness) with a target bar speed for the work sets — wired into the web + mobile loggers and the AI coach. Pure + 25 unit tests. Capture-agnostic: sensor, camera, or manual all feed the same math." },

  { id: "accountability-engine", area: "Core", status: "shipped", title: "Accountability Engine (v1) — predict the quit", detail: "accountability.ts: scores disengagement RISK (0..100) from training cadence — gap since last session vs target interval, week-over-week frequency decline, target miss, dormancy — BEFORE the user churns, classifies an engagement band (thriving→steady→wobbling→at-risk→dormant), ranks the drivers, and picks the single best re-engagement intervention (onboard/celebrate/nudge/ease/winback) with urgency + copy. Heuristic v0, documented; built so labeled save/churn outcomes can refit it through the data network. Pure + unit-tested. Surfaced in the Fitness GPS home (web + mobile)." },
  { id: "habits-streaks", area: "Core", status: "shipped", title: "Habits & streaks", detail: "habits.ts: strict day-streaks (consecutive training days — a full day without a session ends the run; yesterday's run stays saveable until tonight), weekly consistency, and a recency-weighted 0..100 habit-strength score so a comeback rebuilds quickly. Reads only session dates. Pure + unit-tested." },
  { id: "nutrition", area: "Core", status: "shipped", title: "Adaptive nutrition (manual macros)", detail: "nutrition.ts: manual macro logging lands on the Signal ontology (energyIntake/protein/carbs/fat/water — summed per day), and the engine ESTIMATES maintenance from energy balance (avg intake vs bodyweight trend, ~7700 kcal/kg) — MacroFactor-style adaptation, not a static calculator — then sets goal-aware targets (lose/maintain/gain; protein from bodyweight, fat ~25%, carbs fill the rest). Pure + unit-tested. Nutrition screen on web + mobile: today vs adaptive target bars, quick-add, recent days, maintenance readout. Bodyweight feeds it from the weekly check-in — the weigh-in is mirrored into a bodyMass signal — so targets are driven by the athlete's real weight; until maintenance can be estimated (no weigh-in / not enough intake) the targets card prompts for a weigh-in instead of presenting a population default as a personal target. The food database + barcode scan is a separate, blocked layer (nutrition-fooddb). GATED: manual macro logging is free for everyone; free users can also build + save up to FREE_MEAL_LIMIT (4) of their OWN meals (see nutrition-library); free users also keep up to FREE_PRODUCT_LIMIT (4) saved custom PRODUCTS (mirrors meals); only scanning a label stays Full-only (see free-tier-gating). PANEL (07+08 redesign, web + mobile at parity — pixel-matched to the approved mockup): first-run runs a GUIDED 3-STEP onboarding (goal → activity + real weigh-in that posts a bodyMass signal → a plan step offering the ✦ Full trial OR 'Continue on Free' — the free plan spells out what's included (daily macro logging, 4 saved meals, 4 saved products, insights) and drops the user straight into the tracker without an upgrade; the onboarding is its OWN flow (an early return), never stacked above the tracker). Once targets personalize the HOME leads with a time-of-day greeting + a tick-RING calorie hero (the app's kit Ring idiom — web conic/tick, mobile kit Ring) carrying a P/C/F macro trio — the hero no longer presses into the Diary (that door stays in the 'Diary →' link + the bento); instead it wears the Diary's ‹ › day stepper, SHARING the Diary's viewed-day scope (with a 'Today →' reset), so any past day's ring + macros are reviewed in place, with today's training-fuel badge shown only on today — then the coach-voiced NUDGE line (nutritionNudge — protein-short / calories-left / over / on-track) and a QUICK-ACTION row (Add / ✦ Scan / Meals). The SUMMARY dashboard (nutritionSummary — week/month toggle, goal-progress strip from the 28-day weight change, avg intake, adherence, protein-hit, macro balance) rolls up the last 7/30 days and, for free users, ends on the deep-insights ✦ Full lock. HISTORY carries a 7-day streak strip above the recent-days list; the products library gains an on-device search over the user's own foods. nutritionSummary + nutritionNudge are pure + unit-tested in @hybrid/core so the two clients show identical numbers. REFINED (design pass, web + mobile at parity): the surface was re-cut against a harder restraint bar — EVERY emoji is gone, replaced by ONE monoline icon voice (NUTRITION_GLYPHS in @hybrid/core/theme/icons.ts — sunrise/sun/moon/cup meal times, water, scan, mic, spark, target, bowl, chevron — rendered as true 72×72 vectors: inline <svg> on web, react-native-svg on mobile, no PNG assets). The calorie tick-ring is now the undisputed hero (bigger, thinner ticks, room to breathe) with the P/C/F macros as full-width hairline LINES beneath (label + value + 4px bar) rather than a cramped trio; the nudge is a quiet spark-glyph line (no boxed accent-bar card); quick meals + saved meals + presets are type-first rows with time-of-day glyphs (no gradient lock tiles); the streak is shown as a plain number + week strip (consistency as data, not a trophy); and colour is confined to meaning only (lime = go; blue/amber/violet = protein/carbs/fat; generic stat tiles neutral). Onboarding choice cards dropped their emoji for label+sub+radio. HUB (web + mobile at parity): the subpage is no longer one long scroll — it's a HUB (an internal view-state, no routing change) whose landing keeps only the daily essentials (goal card → calorie ring + macros → nudge → 'Log a meal') then a MENU of rows into sub-screens (Diary = week strip + recent days; Insights = the summary dashboard; Body & weight = weigh-in + bodyweight trend; My meals; My foods), each opening with a back-to-hub header. The goal itself is changed through a deliberate Sheet opened from a card (never a live toggle, which an accidental tap could flip and recompute every target)." },
  { id: "nutrition-forget", area: "Core", status: "shipped", title: "Forget it — hold to delete anything the app remembered", detail: "THE APP COULD REMEMBER AND COULD NOT FORGET. Four things a person saves in Nutrition, four different answers to deleting them, and one of them missing outright. A saved food deleted by a SWIPE — a gesture with nothing on screen saying it was there — and by two different swipes at that: the pantry held its delete for six seconds behind an Undo, while the picker's identical gesture on the identical food fired `removeProduct` outright, so whether a mistake was survivable depended on which tab you were standing in. A saved meal by a bare ×. A RECENT by nothing at all: the MRU is written on every log and was never editable, so one mistyped quick entry sat at the top of the picker for the next twenty meals. And a remembered PACK by nothing at all either — four sources could put 'the whole bottle' onto a food's unit switch (the catalog's net quantity, a scan, the Create form, 'remember as the whole pack') and there was no route in the app to take one off. A wrong pack is worse than a missing one: logging through it writes a confidently wrong number into the day. THE GESTURE IS THE ONE THE PHONE ALREADY TRAINED. Hold the thing, feel it, pick Delete from the small card that comes up — the home-screen reflex, which is discoverable in the way a swipe is not, costs two taps instead of one-plus-a-secret, and reads identically on a full-width row and on a 44dp chip, which a revealed swipe action never could. components/hold-menu.tsx is the primitive: `AnchoredMenu` (the card, in its own native Modal window because drawn inline it is clipped by the list it sits in — and by the row itself on Android — so it is placed from the held thing's measured rect, flipping above when there is no room below and growing out of the anchor's own corner), `useHoldMenu` for rows whose inner Pressables keep the touch, and `HoldMenu` for anything that is one pressable object. It is the FEED'S ⋯ CARD, lifted: that card already existed inside feed-menu.tsx and a second copy is how five rails once drew five different tails, so the ⋯ and the hold are one control reached two ways. Wired to saved foods (pantry + picker: Edit, Delete), saved meals, recents (Forget — which drops the food's logged HOURS with it, since a habit is the thing being forgotten), favourites (Remove from favourites) and remembered packs (Remove pack), the last of those on the pack chip in the row AND on the unit chip in the portion editor, which is where a wrong size is actually SEEN. Removal is by unit id, never index (core `removeFoodPortion`), because the list re-sorts by size on every write. THE LEGACY COLUMNS HAD TO BECOME CLEARABLE for a delete to stick: a food saved before `portions` existed keeps its pack in FoodProduct.packSize/packLabel, which core folds back in at READ time, so filtering the stored list alone deleted a pack for exactly as long as it took to reload — PATCH /api/nutrition/products/[id] now accepts a CLEAR of that pair (a value is ignored; this is a migration path closing, not a field reopening), and every client read goes through one `productPortions()` helper rather than `parseFoodPortions(p.portions)` written out at each site, which is the spelling that dropped the legacy pack and had the pantry row offering a bottle the portion editor did not. Both destructive routes on a saved food now hold for Undo. The swipe STAYS — a gesture people have in their fingers is not worth taking away to prove a point about the new one — and the hold is what the tap budget counts, precisely because it is the visible one. One quiet line at the end of the pantry says the gesture exists. IT MOVES, on the app's own tokens. The held thing RISES and stays risen while its card is up (springs.press, scale 1.02 — a press goes in, a pick-up comes out, and PressScale's own dip has already released by the time the card arrives, so there was a gap in the feedback exactly where it presents); a deleted row COLLAPSES and the list closes over it, and an undone one arrives the same way, through one animateListChange before the state update (durations.collapse), which is also what the create form's pack rows grow and collapse on; the UNDO BAR DRAINS a hairline over the full UNDO_MS, linear, because the delete is final at the end of a window nobody was ever told about and a clock that eases lies about how much time is left; and the portion sheet's 48pt kcal figure now rolls (RollingNumber), which was the last big number in Nutrition that jump-cut while the day's gap, the meal total and the ledger three inches away all travelled. HAPTICS FOLLOW lib/haptics' OWN TABLE rather than taste: `medium` when the card presents itself, `selection` on each stepper detent and on the unit switch (a segmented control moving between discrete values — it was `light`, which the table reserves for a control COMMITTING, and switching units commits nothing), `rigid` when the stepper or the pack list refuses to go further, `warning` on every destructive commit including the hold's, and `soft` when food actually lands in the day — placed inside logEntry rather than at its seven call sites, so a path added later cannot arrive silent, which is precisely how four ungated haptics got into the app before the `haptic` gate existed. Every one of them is dropped under Reduce Motion with the meaning intact: the haptic, the card and the scrim still say the row was taken. i18n EN/PL/DE. Mobile only, per the mobile-first rule." },
  { id: "nutrition-pack-list", area: "Core", status: "shipped", title: "Packs are a LIST, and one tap logs the whole bottle", detail: "TWO HALVES OF THE SAME COMPLAINT: you could not say what the whole bottle was when you created the food, and you could not log it without arithmetic afterwards. (1) THE CREATE FORM OFFERED EXACTLY ONE PACK while the data model has held a list since portions shipped — FoodProduct.portions is [{label, size, source}] and MAX_FOOD_PORTIONS is 5 — because real foods have several: a cheese has a slice and a block, a bread has a slice and a loaf, and the kefir this was asked for has a 400 ml bottle AND a 250 ml glass. One field could only ever record whichever was typed first; the second had to be found later, at the bottom of the portion editor, one food at a time. The form now carries the list: rows of size + the serving's own measure + the athlete's word for it, a bare ＋ that GROWS IN PLACE (the exit rule's grammar — a ring there would promise a screen that does not exist) and a × per row. It shows EVERY pack the food has, whatever put it there, each labelled with its source, because a pack you cannot see is a pack you cannot correct — and a wrong multipack net quantity from the catalog is exactly what somebody opens this form to fix. Editing a row makes it the athlete's own, which is the tie-break core's SOURCE_RANK already encodes. The form is then written WHOLE, with the legacy pair cleared alongside it, so a removal made here cannot be undone by a fold-in at the next read. A serving with NO measure ('1 slice') leaves the packs untouched rather than writing an empty list: the form has nothing to say about sizes it has no unit for, and an edit that only changed the name must not silently delete what a scan recorded. (2) LOGGING THE PACK TOOK SIX TAPS — picker, source, row, open the portion editor, choose the bottle on the unit switch, Log — four of which exist to answer a question the food answered when the pack was recorded, and the whole reason for recording it was that the athlete drinks the bottle. The packs are ON THE ROW now, as chips beside the ⊕ ('Whole bottle', then its size), one tap each, writing exactly what the editor writes at amount 1 on that unit (core `oneOfPortion`) so the diary row reads '1 bottle' and not '4'. They appear on the pantry and on all four picker sources, including recents, which have carried the food's portions all along. The chip is `AChip tone=\"action\"` — the accent's rim and the accent's label, the ⊕'s own grammar at chip size, with no selection to report — rather than a sixth hand-rolled pill in the app, which is what the chip ratchet in the mobile design-token guard exists to prevent, and it inherits that component's 44dp floor, which a row-level chip drawn by hand is exactly the kind that misses it. THE FORM'S DEFAULTS WERE BOTH WRONG, and building the packs list is what surfaced it (Aug 2026). The serving opened BLANK while the row displayed \"per 1 Grams\" through the field's placeholder, and every reader guarded on the text being non-empty — so a fresh form had no measure, `portionMeasure` returned null, and THE PACKS BLOCK DID NOT RENDER AT ALL until a number was typed into a field that already looked filled in. The registered flow 'save a new food and what the whole bottle is' could not be walked. Worse underneath: the default UNIT was \"gram\", which is an ALIAS and not a registry id — `unitById` (what composeServingLabel and the form's own unitLabel both call) matches ids exactly, and the alias table is read by parseServing on the way IN from free text and by nothing else. So the serving row printed the raw string (\"per 1 gram\", where every other unit prints a symbol) and the composer fell through to its 'serving' fallback: every food created without opening the unit picker was stored as a COUNT with no measure, which could never afterwards be weighed and could never carry a pack. Picking Grams by hand always worked, because the picker writes ids straight off SERVING_UNITS — only the DEFAULT was wrong, which is exactly why it survived: it is the one path nobody clicks through when testing the thing they just built. The form now opens on 100 g (the shape a label is actually written in, and core's own worked example), composes the label unconditionally so what the row SHOWS is what the row STORES, and apps/mobile/lib/nutrition-form.test.ts pins both halves by reading the default out of the source and running it through the real core readers. Foods already stored as '1 serving' are unaffected and stay counts; changing the unit on the edit form gives them a measure, which is the only honest fix — nothing can guess a weight nobody stated. A CLEANNESS PASS over the two surfaces, run against the house rules rather than taste (Aug 2026). ONE SECTION HEAD: the Products block hand-rolled the documented standard (F.black/fs.title plus a mono count) while the Packs block used a GroupMark — two spellings of one object forty pixels apart in one form. Both are `ASection` now, and the Packs meta carries the figure the athlete actually needs, `n / MAX_FOOD_PORTIONS`, since the ＋ disappears at the cap without otherwise saying why. ONE FIELD OBJECT: the label-panel fields sat on `ink` with a border while the three macro tiles thirty pixels above them sat on `ink2` with none — the same thing, a labelled numeric field, drawn twice, and the bordered one read as a different KIND of field rather than as more of them. The panel fields take the tile. ONE EXPANDER GLYPH: the bare ＋ moved to fs.title, the rung endurance-lanes' All-sports control sets; it keeps this list's own left edge rather than that control's 32dp box, because these rows lead with a FIELD at x=0 and the box would put the label 44dp inside every row above it and draw a step down the block's left edge. THE PORTION SHEET'S LABEL PANEL NOW FOLDS, and the sheet is the argument: it was always open directly under three tiles reading protein, carbs and fat, while its own first four rows are energy, fat, carbs and protein — the same three figures twice, two hundred pixels apart, which is the one arrangement that makes a reader stop and check whether they agree. Which gives way was already written down on this screen: 'a sheet is for adding a food you already trust, a page is for deciding whether to trust it', so a full label panel open by default is the PAGE's job arriving on the sheet. It is one tap away behind the same control, in the same words, as the Create form's own 'More detail' on the sibling screen — and it still carries what the tiles cannot (saturates, sugars, fibre, salt, the kJ figure, the per-100 g column, and the em dashes that say NOT STATED rather than zero). A shared `FoldChevron` TURNS on both folds, which drew a static ⌄ pointing down whether the block was open or shut; it holds still under Reduce Motion, where the arrow still points the right way, because direction is meaning and not motion. And the hold menu's row radius is DERIVED through `concentricRadius` instead of hand-tuned: it was 14 over 10 where concentric is 14 over 9 — one pixel, on the one corner a finger rests beside while it reads the menu. Mobile only. i18n EN/PL/DE." },
  { id: "tap-budget", area: "Core", status: "shipped", title: "The five-tap budget — nothing costs more than five", detail: "A product rule with a number in it, and a test that fails at six. packages/core/src/tap-budget.ts is a REGISTER, and says so in its own header: nothing here reaches into a screen and counts its buttons, because a client cannot be instrumented from a pure package and a guard that pretended otherwise would be a ratchet counting its own answer — a mistake this codebase has already had to fix twice. What it does is make the count STATED and therefore arguable: a flow that grows a step has to come here, write the step down next to the number it now breaks, and face a failing test. HOW A TAP IS COUNTED, fixed here so it cannot be renegotiated per flow: from the app's own ROOT (counting from halfway through a flow is how every flow fits any budget) to the outcome being on record; a HOLD is a tap and its chosen menu row is another; TYPING IS NOT a tap (the amount is the information the athlete came to give, and counting keystrokes would fail every form identically and tell us nothing); scrolling and swiping cost nothing and are not allowed to be the only route to anything either — which is why the hold, not the swipe, is what a delete is counted through. NINE NUTRITION FLOWS are registered and four of them moved to get under the line. Logging the whole bottle: 6 → 4 (the pack is on the row). Logging a weighed 150 g: 6 → 5, because the portion editor opened counting SERVINGS, so 'grams' had to be chosen before a weight could be typed — it opens on the food's own MEASURE now, which is not a change of amount at all (a measure's `initial` is one serving's worth) but a change of the unit the same portion is expressed in, and it brings the athlete's learned amounts on screen at once since those are offered on the measure. Forgetting a pack and forgetting a recent: from IMPOSSIBLE to 5 and 4. Correcting a saved food: 6 → 5, and the door is no longer inside the thing being corrected (the form used to be reachable only from the bottom of the sheet you open to LOG a food). A defect fell out of the counting: ⊕ opened the portion sheet on a saved food and logged outright on a recent — one glyph, two meanings, on two tabs of one list. ⊕ logs on every row of every source now, and the row BODY opens the editor on every row of every source." },
  { id: "nutrition-premade-meals", area: "Core", status: "shipped", title: "Premade meals (one-tap macro logging)", detail: "MEAL_PRESETS in @hybrid/core (nutrition.ts) — a shared set of common meals (breakfast/lunch/dinner/snack) with approximate single-number macros, plus mealPresetSignals() which writes the SAME energyIntake/protein/carbs/fat Signal kinds as the manual quick-add, so a preset log is indistinguishable downstream. Surfaced as a 'Quick meals' grid on the Nutrition screen (web + mobile) and gated on access.canSaveMealsAndProducts: free (casual) users see the grid LOCKED (🔒 tiles → upgrade), Full users tap a tile to log it. The manual macro path (kcal/protein/carbs/fat) stays free for everyone. This is the lightweight in-app preset layer; the full food-search/barcode DB remains separate + blocked (nutrition-fooddb). EDITABLE (Jul 2026): logging a preset now routes through the editable food-log (/api/nutrition/log) on the Nutrition screen, web + mobile — so a preset appears in the Diary as a named, edit/delete-able entry (was four anonymous source='preset' Signals that the per-part hub grouping ignored and nothing could remove). Attributed to the preset's natural part of the day (its id prefix breakfast|lunch|dinner|snack), so it also groups correctly on the hub. The mirrored Signals the engines read are still written by the same endpoint, so totals are unchanged; needs the FoodLog table (nutrition-diary-edit) for the entry row, and soft-degrades to Signals-only without it." },
  { id: "nutrition-pantry", area: "Core", status: "shipped", title: "The Pantry â the saved-foods screen, shelved", detail: "The products screen was a card with a search box at the top, a flat newest-first column of every saved food, a ⊕ and an ×, and an inline bordered 'New product' form under it. Every other tab of this app answers 'here is a lot of your stuff' with structure — Today's four clusters, Performance's four sections — and this one answered it with a scroll. It is now a screen: aurora/pantry.tsx on BOTH clients, over packages/core/src/pantry.ts (pure + unit-tested, 24 tests). THE SHELVES ARE DERIVED, and that is the central decision. The obvious way to categorise foods is a keyword lexicon (‘chicken’ is meat, ‘bread’ is grains) and it is wrong twice over: it GUESSES — files anything unseen under Other, puts coconut oil under produce — and a category that is sometimes wrong is worse than none, because the shelf it hides a food on is the shelf nobody looks at; and it does not survive the app's own languages, so a lexicon is three lexicons, drifting. A food's shelf therefore comes from ITS OWN NUMBERS — where the energy in a serving actually is, at 4·4·9 on the macros' own energy rather than the label's stated kcal (which carries fibre and rounding no macro accounts for). foodRole() returns protein | carb | fat | mixed | light. PROTEIN WINS AT A LOWER BAR (PANTRY_PROTEIN_SHARE 0.30) than carbs or fat (PANTRY_DOMINANT_SHARE 0.55), because a flat majority rule reads an EGG (52 kcal protein vs 99 kcal fat) as a fat — arithmetically true and useless to somebody stocking a protein shelf — while whole milk, which really is balanced, stays MIXED instead of being forced onto a shelf. A serving under PANTRY_LIGHT_KCAL (20) of macro energy is LIGHT: shares of nearly nothing are noise, and rounding them would file pepper as a carbohydrate. SEARCH MOVED TO THE HEAD'S RIGHT SLOT and became an affordance rather than a permanent bar (web draws its own head; mobile uses the HERO's `accessory`, the shell's own top-right control — which is why searchOpen is the host's state on both, so the screen cannot be open on one client and closed on the other). It searches YOUR foods instantly and locally (matchesQuery — name, subname and serving label, accent-folded, words in any order) with the Open Food Facts results offered UNDERNEATH: a food you already saved must never be answered by a stranger's version of it further up the screen. THE HERO states what a food library can honestly state: the count, the lead shelf as a sentence ('Mostly protein'), a one-bar MIX in the shelves' own macro colours (blue/amber/violet for P/C/F; chrome tones for mixed/light, because those are not nutrients and colouring them would invent a fifth macro), and LABEL DATA — the share of the library that states the full panel (factsCompleteness). Not a calorie total: a pantry has no calories, it is a list of things you might eat. Not a streak. Label completeness is a fact about the data, it limits what every other nutrition screen can say, and it is fixable by hand. THE FILTER is the shared DockRail in `mode` role, one chip per NON-EMPTY shelf carrying its own count — a filter that empties the screen is a filter that lied — with shelves headed by the shared GroupMark when nothing is selected and headless when one shelf is (the chip already says which; a heading repeating it would be the title twice in 60px). THE ROW HAS THREE GESTURES MEANING THREE THINGS: ⊕ logs ONE serving to the current meal immediately (the 'add by tap' this screen never had), the body opens the portion editor for a quantity and the full panel, and a swipe left deletes. THE SWIPE IS NOW THE APP'S ONE SWIPE: FoodRow drew its own gesture on web (a pointer-delta translate with an −84px clamp, a 60px threshold and a .22s ease — none of them the numbers components/swipe-row.tsx swipes every set row by) and on mobile drew a trash BUTTON instead, so 'swipe to delete' was a web-only gesture in an app whose parity rule says otherwise. Both now delegate to their client's SwipeRow, which means one set of physics from @hybrid/core — velocity projection, rubber-band, the iOS full-swipe. Its three hard-coded English aria labels ('Add', 'Delete', 'Favorite') were localised in the same pass. DELETE IS UNDOABLE AND THE UNDO IS REAL: the DELETE request is HELD for UNDO_MS (6 s) rather than fired and compensated, because undoing a completed delete would have to re-CREATE the product, minting a new id and quietly breaking every recipe ingredient that pointed at the old one — holding the request means the row that comes back is the row that left. A second swipe commits the first (two rows can leave; only one can be the one Undo returns), and unmounting FLUSHES the pending delete rather than dropping it, so a delete never silently un-happens on the next load. The free cap gates the ADD only — every food already saved stays loggable, searchable and deletable at the cap. TWO THINGS WERE REMOVED rather than kept beside the new screen: the inline bordered 'New product' builder (its form state and its own POST) on both clients, because the Create Food form has been the primary path since the de-clutter pass and two forms writing the same row is how a field lands on one and not the other; and web's addProductToMeal, whose entry point went with it. foldFoodName moved into food-facts.ts — it is a fact about food NAMES rather than about any one search, and verified-foods.ts now imports the same one the pantry matches on, since two copies of a fold is two definitions of whether 'małe' is 'male'. i18n EN/PL/DE (18 keys under w.recovery.nutrition.pn.*). No schema change." },
  { id: "nutrition-library", area: "Core", status: "shipped", title: "Personal meal & product library (build-your-own)", detail: "The redesigned Nutrition panel lets a user BUILD and SAVE their own meals + custom products, persisted server-side (Prisma SavedMeal + FoodProduct; RLS in reference/sql-nutrition-meals.sql — run it in the Supabase SQL Editor, the sandbox can't migrate). API on both clients: /api/nutrition/meals (GET/POST/DELETE) + /api/nutrition/products (GET/POST/DELETE). A saved meal stores single-number macros (kcal derived 4·4·9 when omitted) and logs via the SAME energyIntake/protein/carbs/fat Signal kinds as manual quick-add. Create-a-meal has TWO builders (both clients, at parity): a manual macro form, OR compose the meal FROM your saved PRODUCTS — an 'Add product' sheet picks from the products library, each component carries a serving count, and the meal's macros are the summed total (sumMealComponents in @hybrid/core, unit-tested). Empty component list → the form falls back to manual macros. GATING (access.ts, unit-tested, mirrored client + server): free (casual) users may keep up to FREE_MEAL_LIMIT (4) saved meals — canSaveMeal(persona,count); POST /api/nutrition/meals returns 403 upgrade_required at the cap. Free users ALSO keep up to FREE_PRODUCT_LIMIT (4) saved custom PRODUCTS — canSaveProduct(persona,count); POST /api/nutrition/products 403s upgrade_required at the cap (mirrors saved meals). Full is unlimited on both. The live food-search/barcode DB remains a separate blocked layer (nutrition-fooddb) — the products screen builds against the user's OWN products until a food-DB key lands. Web + mobile at parity. THE PRODUCTS SCREEN ITSELF was rebuilt as the Pantry (nutrition-pantry above) — derived shelves, search in the head's right slot, tap-to-log, swipe-to-delete with a held undo — and the inline 'New product' builder this capability shipped was removed with it, leaving the Create Food form as the one way to write a FoodProduct." },
  { id: "today-focus-hierarchy", area: "Core", status: "shipped", title: "Today focus & hierarchy (3-tier home)", detail: "The Today/Home screen (web today.tsx + mobile home.tsx, at parity) is ranked into a clear visual hierarchy so a returning user lands on one thing. The day-streak moves up into the header row to free the greeting line (as a terracotta PILL beside the bell — since superseded: the streak now rides under the wordmark as the header lockup's second line, see today-header-redesign); a compact TIER-2 glance strip (Quick Log – Readiness – Done today) sits under the plan hero — Quick Log opens the sport-log carousel in a modal, Readiness opens the check-in, and Done today opens a MODAL listing everything logged today with a 'See full calendar' link (replacing the old always-expanded inline done-list + the standalone quick-log section, which were redundant). The screen is then TRIMMED to that focused shape: the Stories row, the plan⇄AI-coach pager (collapsed to a single hero), the check-in/nutrition square widgets, the week-adherence + calendar pair, the reconciled 'This week' block and the Connect feed/coach rails are removed from Today — those features stay reachable via their own screens (Cockpit, Calendar, Coaches, Feed, AI coach). Below the hero: a violet 'Go Full – your edge' band (Cockpit + Sport baits) and a Follow-a-coach row (the section that once carried nutrition alongside it is gone — fuelling lives on its own Nutrition screen). Keeps chartreuse reserved for the single primary action; premium routes wear violet/lime accents per the semantic spectrum." },
  { id: "plan-week-rail", area: "Core", status: "shipped", title: "Plan week rail (date-anchored 'today')", detail: "The count-based 'Your plan today' (dayIndex = sessionsLogged % trainingDays — which rolled the card to the next day the instant you finished one, and could never represent a missed or skipped day) is replaced by a DATE-ANCHORED week rail on Today/Home (web today.tsx + mobile home.tsx, at parity). @hybrid/core's planSchedule() pins the enrolled program's days onto consecutive calendar dates from the plan's start (Macrocycle.startedAt) and reconciles each date against logged sessions + skip overrides, giving every day a readable STATUS: done (a session logged that date, with its id linked), missed (a past training day with nothing logged), skipped (explicit, no adherence penalty), today, upcoming, or rest. A horizontally-scrollable seven-day strip shows the week with per-status visuals (done = filled lime + check, missed = amber, skipped = struck-through slate, today = lime rim + pulsing dot, upcoming = faint outline, rest = moon); chevrons page a week; tapping any day opens a STATE-AWARE detail card that decides the action — Start / Do it now / Start early / View in history / Skip / Undo skip / Postpone (move to a later day, which relocates the session onto that day's card as a catch-up item) / Un-postpone. planAdherence() derives a done-vs-due % (skips + postpones + rest excluded). Pure + unit-tested (plan-schedule.test.ts); both clients render the same schedule off the SAME programCalendarDays() day-builder that the legacy count-based planProgramToday now also uses, so display + prefill can't drift. Skips persist client-side (localStorage on web, AsyncStorage on mobile) through a storage-agnostic override map the engine consumes — the server-backed sync + the postpone action are the plan-schedule-persistence follow-up. SESSION TABS (Jul 2026): a day's content is now GROUPED BY SESSION in @hybrid/core (planSchedule → ScheduledDay.sessions, built by the shared programCalendarDays day-builder, with the flat rows/blocks preserved) so a multi-session day (the Soviet block's AM/PM doubles, or any day the plan authors as several trainings) renders one TAB per session instead of one heading stacked over every lift — the title stays welded to its own lifts. Each session carries a timeOfDay ('AM'/'PM' when the plan sets one) + a 1-based ordinal; the tab label is the time-of-day, or a plain 'Training N' fallback when the plan gives no time (the ordinal, never a fabricated time, is the anchor), and the primary Start now prefills just the ACTIVE session's blocks (not a merged AM+PM blob). DESLOP: the detail card's accent LEFT BORDER was removed (status reads from the state pill, not a glowing rail), and the two cramped header chevrons became round pagers that hug the rail edges with an edge-fade (a CSS mask on web, expo-linear-gradient on mobile) so the strip visibly runs past them. Both clients at parity (web week-rail.tsx + mobile week-rail.tsx); unit-tested (plan-schedule.test.ts asserts the grouping re-flattens to the day's rows/blocks and carries the AM/PM band); new i18n key w.home.rail.session (EN/PL). SESSION IDENTITY IS CONSISTENT EVERYWHERE a plan day is laid out (the Today week-rail tabs AND the Plans program table, program-days.tsx + percent-program.tsx): the time-of-day model widened from AM/PM to AM/MID/PM (SessionTimeOfDay) so a three-a-day is expressible, and a shared pure sessionColor(label, index) in @hybrid/core gives each session marker a stable hue (AM lime, MID amber, PM blue; untimed sessions cycle by ordinal) so the same session can't read one colour on Plans and another on Today. The Plans table now also shows a marker for UNTIMED multi-session days ('Training 1/2/3' from the ordinal) instead of silently merging them under no band — so whether a day is split by clock (AM/MID/PM) or just numbered, its sessions are always distinguished. sessionColor unit-tested (plan-program.test.ts)." },
  { id: "history-calendar-views", area: "Core", status: "shipped", title: "History × Calendar — three switchable merged layouts", detail: "The History screen on BOTH clients carries a view switcher offering FOUR merged History × Calendar layouts, so the athlete picks how their training story reads (choice persists per device — localStorage / AsyncStorage 'hybrid.historyView'): (1) AGENDA (the default) — a pinned current-week strip with load dots, the next plan days as dashed 'planned' ghosts (from the date-anchored planSchedule), a highlighted TODAY group, then the past newest-first with rest gaps collapsed to hairlines; (2) WEEKS — Mon–Sun week chapters, each with a 7-day load sparkline, weekly totals and slim session rows; (3) TIMELINE — a vertical diary rail with date nodes sized by load (teal for sport days) and rest gaps as literal empty space; (4) TREND — the RETIRED Statistics screen, folded in Jul 2026: its week/month/year range toggle, session-count bar chart (peak bucket in chartreuse) and window totals (active days, distance, minutes), off the same sessionBuckets() / weeklyRecap() engines, so no number changed on the way over. Statistics was a second destination charting the SAME sessions History already held, one grain coarser — the same screen twice. Its nav entry is now promotedTo \"history\" (menus drop it; route, icon, label and gate stay live), and the \"This week\" card on Today points its History row here. RETIRED LAYOUTS: the classic LIST and the HEATMAP were trialled and dropped early; the JOURNAL (month grid) and BLOCKS (plan-week chapters) layouts were removed Jul 2026 (user request) — normalizeHistoryView maps their persisted ids (and anything unknown) to the agenda, so a device with an old choice degrades gracefully; the list markup survives only as the archived-management screen, and because its swipe actions went with it, archive/delete for LIVE sessions moved onto the session breakdown (web SessionDetail gets optional onArchive/onDelete/manageBusy rendered as a manage row of shared Button pills — the lib/ui Button primitive now carries a fill|outline variant on BOTH clients, outline tinted by `color` for destructive actions; mobile /session/[id] uses the shared useSessionActions hook — one busy/confirm/invalidate/error flow with the archived list — and reads sessions from the react-query cache; archived cards are non-openable on both clients since the detail route serves only live sessions). ALL grouping/aggregation math is shared + unit-tested in @hybrid/core engines/history-views.ts (historyStream, upcomingPlanDays, weekChapters, HISTORY_VIEWS registry, shared day-key/loadLevel/WEEKDAY_LABEL_KEYS from the calendar engine; the journalMonth + blockChapters aggregates were deleted with their views, as was the heatmap-only historyStats) — web + mobile only render via a compile-checked view→component table; same encoding as the calendar (chartreuse = lifting, teal = sport/cardio, shading = sRPE load); i18n EN/PL/DE (histview.*). POST-REVIEW HARDENING: every aggregate (day chips, weekly totals) is bodyweight-aware (BodyweightLookup threaded into every engine call, matching the per-session cards); every view function accepts the clients' memoized PR lookup (PrLookup) so PR detection never re-runs O(n²); the agenda surfaces TODAY's still-open plan session as a chartreuse due-today ghost (not just future days); ghost labels localize Week via histview.weekLbl; conditioning-only sessions (match/circuit) read summed minutes everywhere including the archived list; mobile hydrates the persisted view choice behind a loader (no default→saved-layout flash). KNOWN LIMITS: on mobile the aggregate layouts render inside the FlatList header (not virtualized — the archived list still is). Day-keying is unified on the athlete's LOCAL calendar day app-wide (see history-daykey-unification). JUL 2026 CARD REDESIGN — the 'headline number' treatment (user-picked from 10 mocked concepts, reference/history-mockups/history-redesign-10.html): the old title+metric-chip+raw-blockSummary card said everything twice (single-block sport logs echoed 'Tennis, 75 min' in title AND block line; swims restated distance/time in two unit systems; lifting cards dumped every set). Cards now lead with ONE large mono figure via the shared engine sessionHeadline(session, units, bwKg) — tonnage for lifting/mixed (bodyweight-aware, kg 't' / lb), distance in the sport's NATURAL unit for cardio (metres for swim/row via displaySportDistance, km otherwise; pace only for single-activity sessions — a summed pace would lie), minutes for timed sports, block count as last resort — followed by one mono meta line (title – N lifts – minutes – pace – ↑PRs in lime) where each fact appears EXACTLY once (minutes suppressed when minutes IS the headline). The agenda's day-level tonnage/PR chips were removed (the cards now carry the numbers) and the weeks-view rows read from the same headline; i18n histview.liftLbl/liftsLbl (EN/PL/DE); unit-tested in engines/history-views.test.ts. NEXT: fold the standalone Calendar screen into this merged History." },
  { id: "history-daykey-unification", area: "Core", status: "shipped", title: "One day-key convention — the athlete's LOCAL calendar day, app-wide", detail: "The app previously split day-keying two ways: the calendar engine (sessionsByDay/trainingHeatmap), history views, habits/streaks, recap, nutrition days and body-progress keyed days by UTC (iso.slice(0,10)) while plan-schedule.ts keyed by LOCAL midnight — so an athlete in a negative-UTC-offset zone logging an evening session could see it on one date in the week rail / Blocks view and the NEXT date in Agenda/Journal/Heatmap/Calendar. Now unified on the athlete's LOCAL calendar day via one canonical module, packages/core/src/day-key.ts (localDayKey / localTodayKey / localMidnightMs / addLocalDays [DST-safe day stepping — a local day can be 23/25 h] / localMondayMs / dayKeyDiff [pure label math]). Converted in one sweep: engines/calendar.ts (sessionsByDay groups local; trainingHeatmap anchors on the LOCAL Monday and steps calendar days; monthMatrix unchanged — it enumerates calendar-date LABELS), engines/history-views.ts (stream/stats/journal/weeks + gap math via dayKeyDiff), engines/habits.ts (activeDays, streak day-numbers via label math, weekly counts, dailyChecklist 'today'), engines/recap.ts activeDays, engines/nutrition.ts daily buckets + todayNutrition, engines/composition.ts point dates, body-progress weigh-in days, and plan-schedule.ts now delegates its dateKeyOf to the shared helper. Clients: both calendar screens' today-highlight + selected-day session filter, both history-views' week strip/today keys/selection, and the date-input defaults (coach assign date, coach-programs, competition target) all use localTodayKey/localDayKey. stats.ts was already local (toDateString) and is untouched. VERIFIED cross-timezone: the full core suite (830 tests) passes under TZ=UTC, TZ=America/New_York, TZ=America/Anchorage and TZ=Pacific/Auckland — fixtures for calendar/history-views/heatmap/nutrition were rewritten to LOCAL-constructed timestamps so the expectations are timezone-invariant, and day-key.test.ts pins the 23:30-local boundary case explicitly." },
  { id: "plan-schedule-persistence", area: "Core", status: "shipped", title: "Plan schedule — server-backed skips + postpone", detail: "The week rail's per-day overrides now persist SERVER-SIDE for cross-device sync, and POSTPONE is a first-class action. Built: (1) the PlanDayOverride table (prisma/schema.prisma) — one row per (user, plan, local date key) holding only explicit intent, status 'skipped' | 'postponed' + a postponedTo target date; 'done'/'missed' stay derived so the table never fights reconciliation. (2) /api/plan-days (GET returns the plan's override map, POST upserts/clears one day) — every handler tolerates the table not existing yet and degrades to a no-op, so the rail keeps working off its client cache until the migration runs. (3) Both client stores (web lib/plan-overrides.ts localStorage + mobile AsyncStorage) are now write-through: they hydrate from the server on mount and POST optimistic writes, keeping the offline cache as the immediate source. (4) POSTPONE in the engine (planSchedule): a postponed day is excluded from the adherence penalty, points at its target via postponedTo, and its session is RELOCATED onto the target date's card via postponedIn (a catch-up list) — surfaced on both clients with a violet 'Postpone' action (moves to the next day), an 'Un-postpone', a 'Moved to {date}' state, and the target-day catch-up section. Unit-tested (plan-schedule.test.ts). The PlanDayOverride table is APPLIED in production (sql-pending.sql run, Jul 2026) — skips/postpones now sync across the athlete's devices." },
  { id: "today-plan-vs-done-separation", area: "Core", status: "shipped", title: "Today — scheduled plan vs 'Also today' off-plan separation", detail: "Today/Home now separates THE PLAN'S scheduled workout from WHAT WAS ACTUALLY DONE (user-picked 'Design 1 — stacked cards' from design/, Jul 2026). Previously an off-plan workout (e.g. a quick-logged tennis match) was invisible on Today except behind the done-count ring's modal — AND it silently marked the plan day 'done' (planSchedule counted ANY session on the date), so the rail showed 'View in history' instead of Start/Skip/Postpone for a day whose prescription was never touched. Two changes, shared in @hybrid/core: (1) MATCHING — planSchedule now claims a session for a plan day only when it's recognisably that day's workout (sessionMatchesPlanDay: the client-composed plan title '<plan>' or '<plan> – …', OR ≥1 exercise/block name shared with the day's prescription — the block-overlap arm covers the web logger's auto-titles like 'Evening workout'); the result carries fulfilledSessionIds, and offPlanSessionsOnDay() returns today's unclaimed sessions (everything, when un-enrolled). Unit-tested (plan-schedule.test.ts: sport log can't complete a day, auto-titled plan session still does, the split, title-form edge cases). (2) UI — a teal-coded 'Also today — off-plan' card on BOTH clients (web aurora/today.tsx AlsoTodayCard + mobile aurora/home.tsx twin) between the plan card/week rail and the feeling card: one row per off-plan session (shared core sessionIcon tile, title, sport-adaptive meta + shared sessionClockTime logged-at time, lime check; tap → the session's breakdown on both clients — mobile /session/{id}, web deep-links History via the app-shell's pendingSessionId → AuroraHistory initialOpenId), a count badge, and a dashed '+ Log another workout' footer that opens the quick sport-log sheet. The week rail now correctly keeps Start/Skip/Postpone on a day where only unrelated training was logged. i18n EN/PL/DE (w.home.today.alsoToday, w.home.today.alsoTodayLog); with no schedule the kicker falls back to plain 'Done today' — nothing is 'off-plan' relative to a plan that doesn't exist. REVIEW-HARDENED (same change, post 8-angle review): (a) the content arm needs ≥2 shared exercise names when the day prescribes ≥2 blocks, so a freestyle session containing one common lift (Back Squat) can't silently swallow the day; (b) RUN days (endurance discipline, or any cardio '…run…' block) accept a cardio-only logged RUN — the quick sport log's natural output — while a racket/team sport still doesn't; (c) claiming is ORDERED, content-match first then plan-generic title as fallback, and a POSTPONED day claims a content-match on its TARGET date, so the catch-up 'Do it now' flow credits the source day instead of being mislabeled off-plan (or stealing the target's day); (d) WEB now stamps the plan-composed title at save time (rail/plan-card onStart carries title → app-shell pendingTitle → logger initialTitle), at parity with mobile's pendingPlanSession — the fuzzy content arm remains for pre-existing auto-titled sessions; (e) planSchedule surfaces every fulfilledSessionId (not just each day's first) so an AM/PM day's second session is still credited to the plan on Today; (f) plannedToday (both clients) guards on isToday so an ended/not-yet-started plan can't leave a phantom scheduled count. The done-count ring still counts EVERYTHING logged today (it tracks training done, not plan compliance). UPDATE (Jul 2026, user-requested): the Done-Today card now lists EVERY session logged on the viewed day as a row — the plan-claimed workout included, wearing a lime tile + a mono 'Plan' tag (off-plan rows keep the teal tile) — so the display-weight count and the visible rows always agree (previously the plan workout hid behind the count and only off-plan extras were rows, which read as a missing workout). The 'off-plan — beyond the schedule' sub-line was retired with its i18n key (alsoTodayCopy simplified to {doneCount, isToday}, unit tests updated); rows come from sessionsOnDay with plan membership from PlanScheduleResult.fulfilledSessionIds. Both clients at parity (web today.tsx + mobile home.tsx). KNOWN TRADE-OFFS (deliberate strictness): a plan-titled session on a REST day shows as an extra; an AI-prescribed or routine/freestyle session done INSTEAD of the plan day leaves that day open (it appears under 'Also today' and the day stays actionable — Start or Skip); switching historical data to the strict matcher can flip old loosely-matched days to missed (the athlete can Skip them, no penalty); renaming a plan in the library breaks the title arm for old sessions (the content arm still matches unedited ones). UPDATE (Aug 2026, user-requested): the row meta line NO LONGER ends with the session's clock time. '0.2 km – 10 min – 21:33' read as the time the swim happened, but for a quick-logged sport startedAt is stamped at save time — so it was the moment the RECORD was typed, presented as a property of the workout. Bookkeeping, and wrong at that; dropped on both clients (web today.tsx, mobile home.tsx) leaving the row to state only what was done. sessionClockTime still serves the readiness-read timeline and the session detail header, where 'when it was written' is the actual subject. The freed space now carries PACE (user-picked idea 1 of five in design/done-row-tail-ideas.html): '0.2 km – 10 min – 5:00 /100m'. Distance and duration say how much, pace is the only figure on the line that says how hard, and it needs no new data — sessionCardioTotals already derives secPerKm, and formatSportPace/sportPacePerMeters already label it in the sport's own split (/km, /100m for Swimming, /500m for Rowing & Canoe Sprint). No tail when the session can't honestly be paced: a timed sport (Tennis 75 min), a distance with no clock, or — the case worth naming — a session MIXING sports with different splits, where the distance-weighted secPerKm would print one figure under one sport's label while describing both. In the SAME change, sessionMeta itself moved into core (engines/session.ts): it had been hand-written identically in web today.tsx and mobile home.tsx, so both clients now import one function; unit-tested (session.test.ts — per-sport splits, every no-tail branch, the mixed-split refusal, strength tonnage, and a regression that the line never ends in a wall clock)." },
  { id: "log-sport-vs-workout-clarity", area: "Core", status: "shipped", title: "Today — 'Log a sport' vs 'Log a workout' disambiguated", detail: "Two adjacent Today actions read as the same thing but did different work, so users couldn't tell them apart: the 'Also today' card's dashed footer said 'Log a workout' / 'Log another workout' yet opened the QUICK SPORT log (QuickSportLog — a single cardio activity: a run, a swim, a match, time + optional distance → /api/sessions), while the STRUCTURED block logger (the Train hero / launcher, the logbook rail's empty-day CTA, the first-run 'Just train' chooser) is the one that actually logs a 'workout'. Fix (copy only, no behaviour/schema change, shared in @hybrid/core so both clients move together): the sport quick-log now says SPORT everywhere and 'workout' is reserved for the structured logger. alsoTodayCopy (plan-schedule.ts) returns new logKeys — w.home.today.alsoTodayLogSport ('Log a sport', empty) / alsoTodayLogSportMore ('Log another sport', once anything's logged) — instead of the old alsoTodayLog*/workout keys; the QuickSportLog sheet header (w.home.quickSport.title/.sub) changed from 'Already trained?' / 'Log it — it still counts' to 'Log a sport' / 'A run, a swim, a match — it still counts'. The logbook rail's full-workout CTA keeps w.home.today.alsoTodayLogFirst ('Log a workout') — it opens the real logger, so the word is now correct and unambiguous. i18n EN/PL/DE; alsoTodayCopy unit tests updated (plan-schedule.test.ts). Both clients at parity automatically — the AlsoTodayCard label (web today.tsx + mobile home.tsx) and the sheet title render straight from these core keys. FOLLOW-UP (same task, 'go further'): the structured-logger side was then unified on the word WORKOUT so every entry to the block logger reads consistently and parallels 'Log a sport'. The first-run + logbook chooser card 'Just train' (chooserLogTitle) → 'Log a workout' (sub reworded off the doubled 'Log…log a workout' to 'One workout, right now — no plan needed.'); PL 'Po prostu trenuj' → 'Zapisz trening', DE 'Einfach trainieren' → 'Training loggen'. Verb/word alignment so each language pairs cleanly (EN Log a workout / Log a sport; PL Zapisz trening / Zapisz sport — the logbook rail CTA alsoTodayLogFirst PL moved 'Dodaj'→'Zapisz' to match; DE Training loggen / Sport loggen — slimLogSub DE 'Workout'→'Training'). The 'Just train' label is fully retired from the UI (the sand ↯ glyph and the card's onStart→logger behaviour are unchanged); the two 'Follow a plan / Build your own / …' code comments updated. NOT touched (deliberate, out of scope): the Train nav tab / centre FAB / launcher ('Train', 'Start training') — those name a navigation destination, not a log action, so renaming them is an IA change, not a copy fix; and nav.log ('Log session'), which is the same-screen nav item, not one of the confusable Today controls. SUPERSEDED IN PART (Aug 2026) — see action-vocabulary-nouns: the logbook day card's three actions are NOUNS now ('Gym', 'Sport', 'Rest'), so the verb-phrase alignment this entry built holds only for the SHEET titles and the chooser card, which it also set and which are unchanged. The reasoning is not reversed — the sport log and the structured logger still read apart, and that is the whole point of the entry — only the form on that one row is." },
  { id: "live-logger-empty-session-redesign", area: "Core", status: "shipped", title: "Live logger — the empty session audited and rebuilt (docked primary, toolbar capsule)", detail: "A full audit of the screen an athlete sees more than any other — one exercise on the board, nothing banked — found five real defects, seven violations of rules this codebase had already written down for itself, and one ergonomic inversion. Written up with annotated before/after device renders in design/empty-session-audit.html. BUGS FIXED: (1) THE HEADER DREW OVER THE STATUS BAR — the clock sat on the carrier row and Finish landed on the battery. SafeAreaProvider was mounted with no initialWindowMetrics, so every consumer started at a ZERO inset and only corrected after the provider's own view laid out; a screen presented as a native fullScreenModal (the logger, and every other COVER_SCREEN) lives in its own view controller outside that measurement and kept the zero. Seeded initialWindowMetrics in app/_layout.tsx — synchronous and correct from the first frame, which is the only frame a cover gets. (2) BODYWEIGHT LIFTS HAD NO 'LAST TIME' — blockSummary always formatted load×reps, so a set of pull-ups printed '–×5, –×5, –×5': the one line that exists to give the athlete something to beat said nothing. New shared strengthSetsSummary() reads the lift's own loadMode + measure from the exercise DB — bodyweight IS its reps (or seconds for a hold, metres for a carry), bodyweight-plus shows what went on the belt (+20×5), assisted what came off. (3) AN EMPTY SET COULD BE BANKED — Log set carried no disabled state and toggleDone validated nothing, so one tap wrote a 0-rep set into the session, the tonnage and history. Shared setIsLoggable() now gates the primary on both clients. (4) '1 exercises' — the count was interpolated against a hard-coded plural noun in all three languages. New pluralForm(n, lang) + exerciseCountKey() handle Polish's three forms including the 12–14 / 22–24 trap. (5) TWO PLUS SIGNS ON ONE CONTROL — the add strings carried a literal '+' that rendered beside the add glyph; Add set was already papering over it at runtime with .replace(/^\\+\\s*/). Strings fixed in EN/PL/DE (base + web train bundle), strips deleted. (6) CARD INSIDE A CARD — the active-set block is written flat and its comment said so, but SwipeRow painted backgroundColor: background ?? C.card on its content layer, dropping an opaque panel inside the translucent Aurora card. The swipe actions now fade in with the drag (they were the only reason the content layer had to be opaque), so the logger passes background=\"transparent\". THE REDESIGN, on BOTH clients: THE DOCK — one primary, pinned, whatever the session's length. Log set was floating mid-screen and travelling further down the page with every exercise added, while the brightest, largest, lowest control on the screen ENDED the session. Now a 56pt Log set sits in a bottom dock flanked by 44pt Pause and Finish satellites, with a hint line above it saying what you are about to bank and what happens after — and becoming the rest countdown while one runs, so a countdown costs a LINE instead of a banner that shoved the layout every time you finished a set. The in-card Log button is gone from both clients (mobile app/workout.tsx, web workout-blocks.tsx in live mode); the dock is told which set it means by the new shared nextSetCursor(), with queuedSetCount() for the confirm. FINISH CONFIRMS IN PLACE — tapping the flag swaps the dock to 'Finish this session?' naming what is still queued, no sheet, no alert: that is what buys back the word a bare satellite doesn't carry, and a terminal action beside the most-tapped button in the app owed a confirm anyway. THE TOOLBAR CAPSULE — the header's two controls fused into one lozenge (the Apple Music arrangement, user-requested): the rest timer keeps its own tap because you arm it MID-workout, and its durations sit one level in as the menu's inline picker. This also fixed a real defect — the old ⏱ chip lived inside the ScrollView, so it left the screen as soon as there was a second exercise. ONE QUESTION, ONE MENU — set type (a bare ＋ that CYCLED, inverting the kit's grammar), the ⚡ special-set tile and the ⋯ preset rail were three controls answering 'what kind of set'; they are now one menu with a type picker and the rest as rows, and the standalone preset rail is deleted. New shared setTypeTo() replaces cycle-only stepping. MARKS OBEY THE GRAMMAR — Add set GROWS the list so it is a bare ＋ with no chrome (it was a RINGED plus in a filled bordered box, the mark for something that LEAVES); Add exercise LEAVES to the picker so it is a door row with the list's own hairline and a ringed chevron (it was a field wearing a lime ⊕, a literal '+' and a ▾ that promised a dropdown). SEEDED FROM LAST TIME — a lift you have done before arrives with its last session's WORKING sets queued and filled in, chalk not a grey placeholder zero, so set one is one tap. ONE ACCENT — chartreuse now appears exactly once per screen, on Log set: the exercise mark went to ash (on a LOGGER every card is the lift you are doing), Finish lost its fill, the add glyph lost its tint. The meta row went with them — an exercise count that restated the cards, a permanent 'tap ✓ as you go' the dismissible tip card already teaches, and a standing 'What's RPE?'. On web the same row of standing preference chips (Simple/Detailed, RPE/RIR) moved into the options menu. COUNTS: 20 controls → 12, five accents → one, three plus glyphs → one, two Finishes → one, 30% of the screen unused → docked. Shared logic + tests in core (set-focus: nextSetCursor/queuedSetCount/setIsLoggable; session: strengthSetsSummary/setTypeTo; i18n: pluralForm/exerciseCountKey). KNOWN GAPS: web's 'What's RPE?' still lives in the block editor's add-block row rather than the logger's menu (mobile has it in the menu); SF Symbols cover the native leaves only — the RN/web floor still draws the shared vector marks, and the remaining text glyphs are the design-system-unification sweep\'s job. CORRECTION (from the TestFlight build): bug (1) was only half fixed. Seeding the provider fixed what the HOOK reports — the dock's bottom pad was right from that build on — but the logger's shell was still a NATIVE SafeAreaView, and a native safe-area view mounted inside a fullScreenModal never applies its top edge at all, seed or no seed. So the header went on shipping across the status bar: the lift's name and the clock on the carrier row, the chevron in the notch band. The cover now pads itself from the hook (lib/layout `coverInsets` — MAX with the window's top, because a screen's own reading can only be larger than the status bar it must clear and a zero there is the bug; the WINDOW's bottom, for the reason `sheetInsetBottom` takes the smaller of the two, since a cover covers the tab bar), on both phases of the route — the live screen and the finish summary, whose ✕ was landing on the status bar for the same reason. Both backdrops (AuroraField, FinishField) sit OUTSIDE that pad: they are absoluteFill, and an absolute child lays out inside its parent's padding box, so padding above them would have stopped the wash at the status bar and drawn a seam. The header also stands on the system's own spatial constant now — HERO.rail (a 44pt row, 4pt below the inset, 8pt of breathing under it) at the screen's GUTTER instead of a hand-measured 10pt/16pt of its own, so the logger's chevron sits exactly where every other screen's back circle sits and on the same column as the cards below it. No web twin: a browser page has no status bar to clear, and web's logger header is a floating pill inside the shell's own padding. FOLLOW-UP — THE SHAPE OF AN AVATAR, AND THE GRIP DROPPED. Two changes to the same header row. (1) A LIFT IS SQUARE, A PERSON IS ROUND. The exercise's mark now wears a SQUARE tile (RADIUS.mark) wherever a lift is pictured, against the circle (radius 999) that social-kit's Avatar draws for a FACE. The shape carries the noun, so one glance separates thing from person with nothing to read. THE BOX IS THE KIT'S — AMarkTile (aurora/kit.tsx), wrapped by AuroraExerciseAvatar (aurora/exercise-media.tsx) with the lift's mark already inside. Squaring one surface at a time was the trap: the first pass converted the logger card header, the picker's result rows and the Exercises browser, and immediately left the pin-exercises sheet (exercise-favourites-sheet.tsx — the SAME browse entries as the browser, at 40/12, holding the lift's INITIALS, the thing exercise-marks retired for colliding: Cable Chest Press and Cable Crossover both 'CC') and the picker's own Rooms grid (40/12, scrolled directly under the rows that had just gone square) drawing the old shape — one sheet, two radii. Both are converted, and the Builder's block header — the last surface picturing a lift with NO box at all — now takes the same 36dp avatar, keeping its modality tint because a Builder list mixes kinds. A room takes the square too: it is a group of lifts, still a thing, and only a person is round. Six hand-rolled tiles are now one component; AuroraExerciseMedia variant=\"thumb\" survives for a surface that owns its own frame but has no caller today. THE MONOGRAM IS GONE FROM CORE TOO: with the pin sheet converted, ExerciseBrowseEntry.initials had no reader on either client, so the field, `exerciseInitials` and its tests are deleted rather than left computed for nobody — a description sitting unused in the shape every tile reads from is how a retired idea gets rebuilt, and this one had already been retired once (exercise-implement-marks). Retired in the same pass: the i18n key w.train.blocks.dragToReorder (en, pl, de), orphaned when the web client was retired and left without a possible future caller when the logger's grips went. (2) DRAG-AND-MOVE IS GONE FROM THE LOGGER — the ⠿ grip on every exercise card and every set row, and the reorder behind it (useDragReorder, moveExerciseTo, the per-exercise set drag). A grip had to sit at the HEAD of each row, ahead of the lift's own avatar, to advertise a gesture for ARRANGING a session on the screen where you DO one, and it made the card header read as a handle rather than as the lift. Arranging stays where it belongs — the Builder (components/aurora/builder.tsx) keeps its grips — and the logger keeps swipe-to-delete on set rows and ✕ on cards. (3) REORDER CAME BACK, IN ONE PLACE, WITH NO CHROME. Dropping the grips dropped the only path to moving a lift mid-session, so the exercise sheet — the one a long-press on a card already opens — grew an ORDER block: the whole board as a short list of inert rows (position, square avatar, name, set count), where you HOLD a row until it lifts and then drag it. New components/hold-drag-row.tsx: the row IS the handle, on the same lib/use-drag-reorder mechanic the Builder's ⠿ drives, so there is still one reorder in the app with two doors into it. The gesture has to declare itself because the rows sit in a scroller — hold still for 260ms and the row is yours, move more than 8dp before that and it was a scroll and nothing was ever claimed, so tapping and scrolling are both untouched. It could NOT simply go back onto the logger's cards: a card's long-press is what opens this sheet, so the two gestures are the same gesture. VoiceOver cannot hold a moving target, so each row also carries explicit Move up / Move down accessibility actions onto the same reorder (workout.orderTitle / orderHint / moveUp / moveDown, en+pl+de). (4) THE LIST PARTS UNDER THE CARD — a defect the reorder had carried since it was written, found by animating the gesture frame by frame rather than by reading the code. `move()` computed the destination on every frame and wrote it to a ref that NOTHING rendered: only the held card ever moved, so the drop was a guess, and a card held at exactly one row's offset sat on top of the row it was passing and that row disappeared underneath it. The rows the drop would displace now step aside while the finger is still down (use-drag-reorder `part`, on springs.slide), so the gap under the card IS the answer. Each row's target is the y of the SLOT it would land in, not an assumed row height — exact under variable heights and whatever margin sits between rows (the Builder's block cards are not all the same height; one is expanded). The rule itself is core `displacedIndex(k, from, to)`, sitting beside the `moveItemTo` it predicts and tested exhaustively against it in both directions, because a preview that opens the gap in the wrong place is worse than no preview. THE COMMIT BECAME A HAND-OVER: with the gap already open the displaced rows are ALREADY standing where the commit puts them, and each one's layout gains exactly what its transform gives up, so closing the gaps in the same tick means nothing moves — the `animateListChange` this used to call is now deleted from that path, since a LayoutAnimation would drag them back to their old slots and re-travel a distance they had already crossed. That leaves one card to land: it keeps the lift THROUGH the reindex (the drag key follows it to its new index rather than being dropped on whichever row took its old one) and springs home from the residual between the finger and the slot, so the gesture ends by setting the card down rather than by it vanishing. Fixes the Builder's two grips in the same change — one mechanic, every caller. (5) AND THE SPORTS TOOK THE SQUARE TOO. The shape rule had only reached LIFTS, so five surfaces went on drawing an ACTIVITY as a naked emoji with no tile at all — the Sports index, the quick-log sport picker and its sheet header, Today's Other-sports lanes, and the Endurance lanes. No tile is no shape claim, so none of them contradicted the rule; but the quick-log sheet header is the exact analogue of the logger card header that HAD taken an avatar, and a sport is a thing you did, not a person. Four of them now wear AMarkTile with the sport's catalogue glyph inside. THE FIFTH DELIBERATELY DOES NOT: the Endurance lane's title is a SectionHead (its own comment says so), and the no-decorative-dot rule exists precisely to keep a marker off the left of a heading — so that sweep took the discipline emoji OFF rather than boxing it, since a tile before a section label would have been the same violation drawn more deliberately. TODAY'S EXERCISE RAIL GAINED A MARK: the pinnable lift cards were the last lift surface in the app with no picture of the lift on them at all, which is the one place a mark earns most — you swipe a rail, and an implement is what finds the barbell card without reading four of them. It costs the name 30dp, which is why the delta was moved off that row originally; a delta is a variable-width number that reads as well from the footer and a fixed 24dp mark is not the same bargain. Ash, not the kind's colour: the strip already draws kind as colour and the mark now draws it as a shape, and that card's own note argues against a third channel for one fact. AMarkTile now documents its FOUR rungs — 40 list row, 36 full-width card or sheet header, 28 compact row, 24 rail card — so the next surface picks the rung its neighbours use instead of measuring a fifth, which is how this drifted to six different boxes the first time. NOT converted, and on purpose: the PR trophy and runner glyphs in a session's block names are status badges inside a text run, and week-verdict's movement-group rows are a dense stats breakdown closer to a chart legend than a list of entities." },
  { id: "logger-dock-satellites-native", area: "Mobile", status: "shipped", title: "Native SwiftUI — the toolbar capsule and the dock satellites", detail: "Two new leaves in apps/mobile/components/aurora/swiftui.tsx. GlassToolbarGroup is the Apple Music ⋯: not a glyph with a circle drawn round it but a real SwiftUI Button and Menu sharing ONE lozenge inside a GlassEffectContainer, tagged with glassEffectId in a shared Namespace at spacing 2 so the system fuses them and flows the glass between them as the timer's readout appears and disappears. The three pieces already existed and had drifted apart — GlassMenuButton drew a menu, GlassSelectMenu drew a picker trigger, GlassPillRow knew how to fuse capsules but only as decoration (pointerEvents none, React Native taking the taps) — and this joins them up. It is also a STRICTER reading of the composition rule than what it replaces: the logger's timer chip and its ⋯ were two separate Hosts standing next to each other pretending to be a group. GlassSatellite is the neutral glass button that orbits a filled primary — a circle when its glyph speaks for itself (pause.fill) and a capsule when it must not be guessed at, sized by the same chain as GlassNavButton with the shape opened up (glassEffect takes shape: 'capsule' in the installed @expo/ui). CORRECTION TO AN EARLIER READ: the doctrine 'never nativize the brand CTA' still holds for Log set, but it did NOT apply to Finish. An earlier draft argued Finish must stay app-drawn because it carries the celebration flight out of itself; the flight is anchored on the PR BADGE in the exercise card (prBadgeRefs), which finish() arms on the way to the summary's trophy chip — the button is only the trigger. Stripped of the chartreuse fill there is nothing brand-shaped left in it, so it goes native. This is not the reverted GlassPillButton either: that revert protected the brand's one 'go' surface, and a neutral satellite is the opposite of that surface — precisely what .glass is for, the same call the kit already makes for the soft APill. SF Symbol names are typed via a new SFSymbol alias derived from the native Image's own prop rather than importing sf-symbols-typescript directly (it is @expo/ui's dependency, not ours, and deriving keeps them in step through an SDK bump without a package the Expo alignment test would have to police). There is no timer.slash in SF Symbols 7, so the capsule uses one `timer` mark and carries its state in colour plus an accessibility label naming the value. New shared vector marks `pause` and `flag` in core theme/icons.ts are the floor that draws the same two controls on Android, pre-26 iOS and web. NEXT: the rest countdown to the Dynamic Island (live-activity-rest), native .swipeActions on the set rows (swipe-actions-native), a keyboard toolbar with Done plus a Stepper on reps, and SF Symbols for the remaining text glyphs." },
  { id: "live-logger-set-focus", area: "Core", status: "shipped", title: "Live logger — focus the active set, sunset the rest ('Glass + ghost add')", detail: "A live workout with many sets used to render every row at equal weight — eight near-identical outlined rows with no answer to 'which set am I on?' (the screenshot problem). The logger now lifts exactly ONE set and recedes the others (user-picked concept 01 'Glass + ghost add' from design/sets-focus-apple-2types.html, Jul 2026). Shared model in @hybrid/core set-focus.ts (unit-tested, 10 tests): activeSetIndex() = the first un-banked set (the next thing you do; role-agnostic; −1 when the exercise is complete), setFocus() classifies each row done | active | upcoming, and addSetIsNext() encodes the TWO logging habits from one rule — true (prominent) when nothing is queued below the active set (the one-at-a-time lifter, whose next move IS adding a set), false (quiet) when a plan already sits below (the plan-ahead lifter). PRESENTATION only — changes no data. Both clients at parity, gated to the LIVE logger so the Builder (which edits a plan, has no 'now') is untouched: the LIVE logger drops the set TABLE entirely and renders the active set as a FLAT hero section directly on the exercise card (late-Jul 2026 de-slop pass, on user review: the earlier frosted lime glass card read as a card-inside-a-card and washed the screen green, so the inner border/tint/glass/glow were removed — focus now comes from SCALE and breathing room, and the only lime left in the logging loop is the Log CTA itself): a mono eyebrow ('Set n — up now', gaining an 'of N' denominator only when a queue waits — from addSetIsNext), weight and reps as two EQUAL big editable numbers (46px display, one shared baseline so 'kg' and 'reps' sit level) whose UNIT labels are tap targets that focus the field (web wraps input+unit in a <label>; mobile focuses via an inputRefs map), a planned-rest hint on the label row (prefs.restSeconds when the rest timer is on, else hidden — both clients read the same logger pref; web passes it via the new restSec prop), and — since the Jul 2026 'set-logging-redesign.html' pass — RPE tucked behind a QUIET CHIP on the label row (rpe/rir + value, or '–'; tapping it reveals a ONE-TAP PILL ROW of the core RPE scale — late-Jul 2026, replacing the earlier full-width text input the user flagged as unnecessary for a single number: one pill per RPE_SCALE step, RIR labels when swapped, tap sets the value and closes, tap the picked value again to clear, the RPE⇄RIR toggle leading the row; Simple mode hides it entirely), and a proper full-width 'Log set ✓' BUTTON below the numbers (one tap banks the set + starts rest); the earlier floating ＋ circle was retired as oversized, and per-set m/s entry moved to the exercise detail sheet (see live-logger-exercise-sheet). Banked + queued sets COLLAPSE to quiet one-line hairline-separated LEDGER ROWS (late-Jul 2026: the boxed mini-card rows were flattened — no card-in-card) that tap/click to re-open (banked → active again); bodyweight lifts put reps in the hero (no load), plank/carry read s / m. Per-set drag-reorder (grip) is preserved on both the hero and the collapsed rows; set DELETE is now swipe-only — swipe a row left to reveal a red Delete (the always-visible '−' icon buttons were removed as clutter, Jul 2026). Mobile uses the existing SwipeRow (PanResponder); web gains a matching components/swipe-row.tsx (Pointer Events — works with touch or a mouse drag) that starts a swipe anywhere except a text input or the grip and swallows the tap once a horizontal drag is claimed, so the banked-row 're-open' tap and the log '＋' still fire. The action row is now two glass tiles (Jul 2026 redesign): '+ Add set' is a SPLIT tile — the wide zone quick-adds one carry-over set (still following addSetIsNext: a brighter chalk hairline + bold label when it's the next move — no lime fill since the de-slop pass — quiet card when a queue sits below), a ⋯ zone opens the presets/plan panel; and a GLYPH-ONLY '⚡' Special tile (the text label was dropped) holds the warm-up / ramp / cool-down / drop menu. The ⋯ panel is a single POPULAR-PRESETS RAIL (Jul 2026 redesign — the earlier 2×2 preset grid AND the manual plan-ahead stepper were removed as over-nested clutter, on user request): a horizontal, edge-bleeding scroll of scheme cards — 3×3 (heavy), 5×5 (strength), 3×12 (hypertrophy), 4×8 (volume), 10×10 (GVT) — each showing its total working reps; one tap (applyPreset) lays out that whole scheme as the exercise's working sets (each rep count a SINGLE number, carrying the current load; banked sets kept, the un-banked plan replaced). The rail bleeds to its container's edge by the gutter width with matching inner padding (mobile ScrollView marginHorizontal −space.lg inside the exercise Card; web overflow-x rail with margin/padding ±14 inside the popover), per the full-bleed-rail idiom, so cards slide under the edge. addPlannedSets and the Straight/Pyramid shapes are retired. To close the incremental lifter's tap-tap loop, the WEB logger's addSet honours the carryOver logger pref (default on) — a new set seeds from the last set's load/reps/rpe as a clean working set, at parity with mobile's emptySet(from); the Builder still adds blank sets since it never passes the pref. i18n keys workout.setWord / ofWord / upNow / logSet / restShort / presetsTitle / presetReps / schemeHeavy / schemeStrength / schemeHypertrophy / schemeVolume / schemeGvt (EN/PL/DE, web mirror under w.train.blocks.*); no schema change. AUG 2026 — THE INVISIBLE RPE, and two things beside it (design/rpe-invisible-after-banking.html carries the annotated frames and the four treatments considered). REPORTED AS: set an RPE, log the set, move to the next one, and the effort is gone — 'locked in indeed, but invisible'. Exactly right, and NOT a state bug, which is what decided the fix: s.rpe held the value, the save wrote it onto the block, and the session breakdown printed it back as 'RPE 8' at home. The collapsed LEDGER ROW simply never read it — its summary was built from [loadPart, repsPart].join(' × ') and nothing else — so the one surface in the chain that dropped the number was the one the athlete reads while training, and because the freshly-active set's chip then showed '–', the card positively asserted that no effort had been rated anywhere in the exercise. THE FIX (option 'the token', picked over three alternatives): the row grows a fourth column carrying the chip's own word and colour — 'RPE 8' / 'RIR 2' in mono nano sand, through rpeRirSwap so the RIR pref follows — and renders NOTHING when the set is unrated, which is what makes a fourth column affordable on a small phone at all. Deliberately NOT folded into the summary string as the lifter's '@ 8' notation (the cheaper change): that Text already turns amber/terracotta for a PLAUSIBILITY concern, so effort riding inside it would inherit the warning colour and the row could no longer say which of the two facts it was reporting. Also rejected: effort as a colour with no number (same collision, returns no value, fails the a11y floor) and a per-exercise 'EFFORT 8 → 8 → 9' series (the better reading of a session, but it cannot tell you which row to tap to correct a typo — recorded as a possible later change on its own argument, with the token underneath it). ONE FORMATTER FOR ONE SET: the row had rolled its own load × reps line and the two implementations had already drifted — it special-cased only pure `bodyweight`, so an ASSISTED pull-up printed '20 kg × 5' in the ledger (reading as if 20 kg had been added) while the 'last time' reference three lines above it, off core's strengthSetsSummary, correctly wrote '−20×5'. New shared strengthSetSummary(name, set, { style, units }) is now the per-set half of strengthSetsSummary and the row reads through it: style 'compact' is the unchanged comma-list reading (raw stored kg, a dash holding each empty half, because that line is read as a SHAPE and a gap must keep its column), style 'row' is the ledger's (converted to the athlete's unit and named, empty parts dropped, an em dash when nothing was typed, because one set alone on a line reads '–×–' as a typo). 7 unit tests, including the assisted/weighted signing in both styles. TWO MORE, in the same change: (1) CARRY-OVER NO LONGER COPIES THE EFFORT — emptySet carried rpe forward with load and reps, so a brand-new set arrived pre-rated at 8 on an amber chip, indistinguishable from one the athlete had judged, and if they never touched it that guess was what saved. Load and reps are a PLAN worth carrying; an RPE is an OBSERVATION of a set that has happened. (The Builder's carry-over still copies it, and should: there an RPE is a prescription, authored in advance.) (2) THE RPE DISCLOSURE IS KEYED PER SET, not per exercise — rpeOpenUid was keyed by exercise on the reasoning that only one set is active per exercise, true but the state outlived the set it was opened for, since banking never cleared it: open the pill row, pick nothing, log the set, and the row was sitting open on the NEXT set. Now rpeOpenSet holds the set's own uid, unique across the session, so the row closes with the set that owns it. Mobile only — the web logger went with the web client. FOLLOW-UP, SAME DAY — THE MOTION WAS NEVER THERE. Reported as 'I think I never saw them in my app', and correct. Four things were checked before anything was changed, because a no-op in motion code is invisible and guessing at it wastes a build: (1) the LayoutAnimation plumbing is SOUND on the shipping platform — against the installed RN 0.85.3, the JS gate `ReactNativeFeatureFlags.isLayoutAnimationEnabled` defaults true and the C++ `enableLayoutAnimationsOnIOS` defaults true, so iOS + Fabric does animate (the first suspicion, that newArchEnabled makes LayoutAnimation a no-op, was WRONG for this version and is recorded here so nobody re-derives it); (2) `useReducedMotion` defaults to false and only turns true if the OS says so, so it was not silently suppressing everything; (3) `durations.collapse` 180 / `springs.slide` are real values; (4) the actual defect — `animateListChange` sat on the EXERCISE mutations (add, remove, reorder) and on exactly ONE of the SEVEN set mutations, plain Add set. The presets rail replacing a whole queue, all three special-set adders, the auto warm-up ramp, and — worst — BANKING A SET all teleported. Banking is the most-repeated interaction in the app and the biggest layout change the screen makes (a hero block four rows tall is replaced by a one-line ledger row while the next set's hero opens below it), and it was the one moment with no motion at all. It sat under a comment reading 'EVERY MUTATION OF THE LIST TRAVELS', which is how it survived: the sentence meant the exercise list, said 'the list', and read like a guarantee. THE FIX IS A DOOR, NOT SIX MORE CALLS: new `commitSets(u, fn)` arms `animateListChange(reducedMotion)` and then commits, and all seven shape-changing mutations route through it — nothing about a scattered call before each setExercises makes the next one added remember. The exclusions are deliberate and permanent, not debt: `setSetField` runs per KEYSTROKE and `bumpLoad` per tap (a value changing inside a row that stays put is not a layout change, and animating it would spring the card on every digit), and `removeSet` stays out because its only caller is a SwipeRow and closing the gap after a swipe belongs to the gesture that opened it. The RPE DISCLOSURE animates too — opening the pill row grows the card and shifted everything below it instantly; it is not a set mutation so the door cannot cover it, and it arms the animation itself on both the chip toggle and the pill tap. GUARDED: apps/mobile/lib/list-motion.test.ts (6 tests, added to the mobile vitest include list) reads the logger as TEXT and proves the door arms BEFORE it commits, that each of the seven routes through it, that no other setExercises call rebuilds a set array unless it is named in an EXEMPT allowlist with its reason, that every exemption still matches a real call site, and that the disclosure animates. An allowlist rather than a ratchet count, precisely because the exemptions are judgements: naming them forces whoever adds the next set-writing path to decide which kind theirs is, which is the decision that got skipped six times. Verified to bite — reverting toggleDone to a raw setExercises turns 6 passes into 3 failures naming it. list-motion.ts now records where LayoutAnimation actually runs (iOS+Fabric on; Android+Fabric OFF, since `enableLayoutAnimationsOnAndroid` defaults false in 0.85.3 and `setLayoutAnimationEnabledExperimental` is the pre-Fabric switch — a note, not a blocker, for an App-Store product, but list motion must not be described as cross-platform until it is) and warns that RN 0.85.3's `LayoutAnimation.setEnabled()` is an upstream self-assignment that does nothing. NOT VERIFIED ON DEVICE: this sandbox has no simulator, so the code path, the flags, the tokens, typecheck and the iOS bundle are proven and the ON-SCREEN result is not — it needs a look on the next TestFlight build. AND THEN THE REST OF THE CLASS, on the question 'did you fix all?' — no, it had covered the SET mutations and stopped there. Four more surfaces in the same logger were changing the same screen's layout between two frames, none of them a set mutation so the door could not reach them: the WHAT-KIND-OF-SET panel (the exact analogue of the RPE pill row, one card over — fixing one and not the other is the inconsistency the door was meant to end), DISMISSING THE COACH TIP (a whole card leaving the top of the scroller, so everything below jumped up by its height — the call from toggleDone was incidentally covered once banking started animating, but the explicit ✕ was not), STOPPING THE REST COUNTDOWN (a two-row banner unmounting, reachable three ways — the banner's own ■ Stop rest, the capsule's toggle, and the 'off' pref — so it got its own door, `stopRest`, re-homed beside the `restSince` state it owns so every caller sits below it and there is no forward reference), and JOINING A SUPERSET (a ⛓ A1/A2 badge entering the card header and reflowing the lift's name beside it — small, but a layout change the user asked for). Deliberately still NOT animated, and each for a reason: `rename` writes per keystroke; `moveExerciseTo` is the drag COMMIT, where the displaced rows are already standing in their new slots and a LayoutAnimation would drag them back across a distance they had crossed (see reorder-sets-exercises — that call was deleted from this path on purpose); and the hydration paths (`blocksToExercises`) replace the whole board on load, where there is no prior position to travel from. The guard grew a second half for the block changes, keyed by the SETTER that performs each one, and it also fails if a setter appears TWICE — a second path to the same layout change has to be routed through the first, which is what `stopRest` is. ONE MORE PASS, on 'is everything implemented?' — two things, one a non-bug worth recording so it is not re-found. THE EFFORT YIELDS THE COLOUR: the token's comment claimed it was drawn in sand, NOT the summary's amber-for-concern. That was false — C.amber IS sand, one constant, so a caution set painted both columns the same colour with nothing to say which fact was being reported. It is a weaker form of the exact objection that ruled out folding the effort into the summary string, and it applied to the column beside it. The token now carries the chip's amber normally (matching the chip is the whole argument for the token) and drops to ash on a FLAGGED set, where the amber belongs to the figures — still legible, no longer competing for a meaning it does not own. NOT A BUG, checked and left alone: per-set BAR VELOCITY is also absent from the collapsed ledger row, which looks like the same defect as the RPE and is not. Velocity is entered in the exercise detail sheet, and that sheet renders every set's value as a bar chart with the figure above each column — so it is displayed exactly where it is typed. The RPE bug was that a value entered ON THE CARD vanished FROM THE CARD; velocity never claimed the card, so adding a fifth column would cost the row width to restate something already visible in its own home." },
  { id: "live-logger-exercise-sheet", area: "Core", status: "shipped", title: "Live logger — exercise detail sheet (per-set bar speed) + live per-exercise summary", detail: "From the empty-logger UI review (late-Jul 2026): every STRENGTH card in the LIVE logger now ends in a tappable LIVE SUMMARY BAR — sets banked of total, tonnage so far, top set (load × reps) and, once entered, mean bar speed — derived by the new @hybrid/core exerciseLiveStats() (live-stats.ts, unit-tested; reuses strengthBlockStats so it can never disagree with the Builder's signal math; bodyweight-aware). The EXERCISE DETAIL SHEET (the shared aurora Sheet on both clients — mobile ExerciseSheet in app/workout.tsx, web ExerciseDetailSheet in components/workout-blocks.tsx) opens two ways (entry picked by the user, review round 3): PRESS-AND-HOLD anywhere on the strength card — mobile a bare Pressable onLongPress wrapper (400 ms, medium haptic; interactive children keep their own touches), web the pointer twin (pointerdown starts a 400 ms timer; starting on an input/button/grip is ignored, >8 px movement or pointer up/leave/cancel aborts, so scroll/swipe/drag never false-fire) — or tapping the summary bar, the visible entry. V2 layout after a second review round (the first cut's boxed stat cells re-introduced card-in-card, duplicated 'm/s' between a stat cell and the input caption, and had a redundant set-pill selector): FLAT totals (big number over a small mono label, no boxes — total volume + the picked set as load × reps), then ONE velocity module where the unit is named exactly once — a 'BAR SPEED (M/S)' header carrying the mean, above a bar chart that IS the set selector: each set is a column with its m/s value riding above its bar, the SELECTED column's value is the editable input (underline style, no boxed field), bars share a hairline baseline, and set numbers (✓ on banked) sit underneath as secondary tap targets — the manual-VBT twin of a bar-sensor app's set screen, modelled on the user's reference shot. This replaces the old inline m/s field on the active-set hero (the velocity logger pref still shows the m/s column in the non-live Builder grid); entered speeds keep feeding StrengthSet.vel → the vbt-engine/Velocity screens unchanged, and live sensor capture remains the blocked vbt-capture capability. i18n: workout.setsWord / topWord / meanWord / totalVolume / barSpeed / exDetail / velHint (EN/PL/DE)." },
  { id: "today-day-scoped-cards", area: "Core", status: "shipped", title: "Today — Also-today + feeling cards follow the rail's selected day", detail: "Tapping a day chip on the plan week rail now scopes the WHOLE Today screen to that day, not just the scheduled-plan detail: the rail lifts the tapped ScheduledDay to the caller via a new onSelectDay prop (web + mobile week-rail.tsx), and Today/Home (web today.tsx + mobile home.tsx, at parity) re-keys the 'Also today' card (done count via sessionsOnDay(sessions, day.ts), off-plan rows via offPlanSessionsOnDay(…, day.ts)), the Done-Today sheet, AND the feeling card to the viewed day. Until the first tap (or on tapping today's chip) everything stays pinned to the real today. Day-aware presentation: on a non-today day the done label/sheet title read 'Done {date}' (new i18n w.home.today.glanceDoneOn, EN/PL/DE), the empty sub-line goes past-tense ('nothing logged this day' — alsoTodayCopy in core grew an isToday flag + w.home.today.alsoDayEmpty; unit-tested), the sheet's empty line drops the 'start your session' invitation (doneModalEmptyDay), and the quick-log ghost row hides (a quick log always saves at 'now', so it can't land on another day). FEELING: the clients keep the raw /api/checkins list and derive the VIEWED day's check-in, so a past day shows that day's logged feeling; tapping a face on a past day BACK-LOGS it (weekOf = that day's local noon — the API already accepted a client weekOf); a future day is read-only (you can't feel the future). The card shows the viewed date next to the title, and the 6h re-log cooldown now mirrors the server exactly — computed from the newest check-in's createdAt (a global write-lock across days), with the 'Next in Xh Ym' chip also shown alone when the faces are locked on a day without its own check-in." },
  { id: "today-pill-rail", area: "Core", status: "planned", title: "Today \u2014 sticky pill rail (the contracted calendar) \u2014 REMOVED", detail: "RETIRED. The sticky rail Today left behind on scroll \u2014 up to three pills (DATE from the week strip, DONE from the day's result, READINESS from the check-in) captured as their source cards passed under a 46dp bar \u2014 has been DELETED from both clients, along with packages/core/src/today-rail.ts, its unit tests, the 11 i18n keys (w.home.pill.*, EN/PL/DE) and the week-row measurement hooks both week rails (plan + logbook, both clients) carried only to feed it. WHY: it was a SECOND head competing with the screen's own chrome, and it answered questions the athlete had already read rather than taking them anywhere \u2014 a status bar, not navigation. The scroll-persistent slot on Today is being re-spent on the HUB itself: the Dashboard / Performance / Feed switcher detaching into floating pills as you scroll, so the persistent element is a way OUT of the current view instead of a summary of it (see today-hub-floating-pills). THAT SUCCESSOR HAS SINCE BEEN RETIRED TOO (Aug 2026): the slot is now deliberately EMPTY, and two removals in a row are the argument against re-spending it a third time. The capture/hysteresis/ceiling model (todayRailState, TODAY_RAIL_MOTION) is worth recovering from git history if a contracted status rail is ever wanted again \u2014 but it must not return as a screen-top overlay." },
  { id: "today-hub-floating-pills", area: "Core", status: "retired", title: "Today hub \u2014 the tabs detach into floating pills on scroll", detail: "Replaces the removed sticky rail (today-pill-rail). The Dashboard / Performance / Feed switcher used to scroll away with the profile header, so reaching Performance or Feed from deep in the dashboard cost a full scroll back to the top. It now DETACHES and floats. Two rules, both in one shared engine \u2014 packages/core/src/today-hub-dock.ts, 24 unit tests \u2014 so the clients cannot drift on when a pill appears. SPLIT: once the in-flow segmented control has scrolled off, the three destinations stop being segments of one track and become three independent glass pills; the equal-width constraint that forces glyph-only labels only exists INSIDE a track, so exactly one pill \u2014 the one you are in \u2014 carries its WORD and the two siblings contract to a 44pt glyph. Selecting a sibling is a width EXCHANGE (one inflating to its word as the other contracts), which is the physics the LiquidSeg lens carried inside the track. RETURN: the row answers scroll DIRECTION, not depth \u2014 reading down sends it off the top edge (180ms, flat: something leaving should not ask to be watched), and the first upward run of 12px or more brings it straight back (220ms on the pin curve). The dead zone is a RUN accumulated across frames, not one event's delta, so a slow drag commits and momentum decay does not; offsets are clamped at zero before anything is compared, because iOS rubber-band and Android overscroll both report negative offsets and an unclamped diff turns the settle of a bounce into a phantom flick at the very moment you arrive at the top. Re-attach uses a looser threshold (10px) than detach so crossing the line cannot make the row flicker. THRESHOLD: detach is 'the real control has left the viewport', never a fixed depth \u2014 detaching at the nav bar's 48px would draw the floating row while the in-flow control is still on screen, i.e. two copies of one control. Web measures the switcher's exact bottom edge (getBoundingClientRect) and passes it; mobile passes none and takes the shared HUB_DOCK_FLOOR (116), deliberately a touch late, because rebuilding the switcher's content-space y from its parent chain is the exact fragility the deleted rail carried and the three views nest it differently. REDUCED MOTION falls back to DOCK, the runner-up treatment: one capsule, glyph-only, permanently on screen while detached \u2014 a shipped shape rather than a degraded one, since RETURN's whole value is the motion and a control that vanishes without one has simply disappeared. The row is LEADING-ANCHORED rather than centred, and it lines up with the CONTENT COLUMN rather than the raw screen edge \u2014 the pills sit exactly where the in-flow switcher's own left edge was, so detaching reads as the control lifting straight up instead of sliding sideways on its way out. Web measures that edge from the same rect as the threshold (its column is inset by the shell's sidebar, so a row flush to the viewport would float over the nav \u2014 verified at 1280 wide: pill left 272, column left 272); mobile's 16dp screen gutter IS the column, so it takes the shared HUB_PILL.inset. MOTION IS THE APP'S SPRINGS, AND ON iOS THE GLASS IS REAL (Aug 2026, from live feedback that the floating row 'should look more like Liquid Glass with the native transition'). It shipped wearing glass as a MATERIAL: three static glassEffect tiles whose widths were tweened in JS on five bezier curves the dock had minted for itself (HUB_DOCK_MOTION), plus a 40ms per-pill scale stagger (hubSplitDelay) standing in for the split. Both are gone. (1) THE GLASS IS ONE BODY: apps/mobile/components/aurora/swiftui.tsx GlassPillRow is a native SwiftUI GlassEffectContainer whose three capsules share a Namespace and carry a glassEffectId, so the system treats them as one volume of glass — adjacent capsules FUSE, and a frame that changes makes the glass FLOW between the old shape and the new (rim travelling, refraction re-sampling, a droplet stretching and pinching off) instead of a blur being resized. (2) SPLIT IS NOW THE GAP, on both clients: the row arrives with the pills touching — which under Liquid Glass is not three shapes but ONE lozenge — and springs them apart; leaving, it runs backwards and they merge. One animated number replaces the stagger, and it finally renders the split in the material rather than miming it with three views scaling in. (3) ONE PHYSICS, THREE RENDERERS: HUB_DOCK_SPRINGS binds the dock to the shared tokens (motion.ts) — EXCHANGE is springs.lens (the very spring the in-flow switcher's selection flies on, because it is the same gesture one layer up) and SPLIT/MERGE is springs.sheet; CONCEAL stays a bezier because it is opacity and a short lift with nothing positional to interrupt, and it stays faster than the arrival. hubMotion() returns each transition as {ms, spring, css, bezier}, so SwiftUI gets response/dampingFraction natively, RN gets the same token through springToRN, and CSS gets a sampled linear() — all three integrating ONE differential equation rather than three curves that agree at the endpoints and drift in between. That is what keeps the glyph on the capsule it is riding: the marks, the words and the taps stay in React Native ON TOP of the glass (same vector glyphs as the in-flow switcher, same type, same i18n, same accessible names), laid out from the same shared widths (hubPillWidths). Nothing is driven frame-by-frame over the bridge — the native side is handed target numbers and animates itself, which is what made the earlier JS-driven glass jank. The Host is pointerEvents='none', so if the native layer never renders (pre-iOS-26, a failed mount) the RN row above is still the entire working control; Android and Reduce Motion keep the RN treatment unchanged. ALL THREE VIEWS carry it: web mounts it in the shared hub header, PORTALLED TO THE BODY \u2014 the screen-transition wrapper carries a transform, and a transformed ancestor becomes the containing block for position:fixed, so a nested fixed row scrolled with the page (measured); the portal also keeps the dock out of the hub's own view transition, which is what 'the chrome holds still' requires. Mobile layers it OVER each view's scroller (Performance and Feed own their own) and rides the nav-scroll signal every scroller already publishes \u2014 extended with a subscribe() for the raw offset \u2014 so there is no second scroll listener anywhere; the dashboard's safe-area inset moved from the shell to a padded layer inside it so the dock mounts in the same coordinate space on all three. Selecting a hub tab on web now scrolls to the top: the three views share one window scroll and the dock made it possible to switch from deep inside a page for the first time. Both clients keep each pill's accessible name from TODAY_TABS (the glyph stays decorative), the row is hidden from assistive tech while concealed, and mobile fires a selection haptic. Design study of the seven candidate treatments, the storyboards, the comparison matrix and the open decisions: the artifact linked on the PR.", retiredBecause: "RETIRED (Aug 2026, by request: “in the today tab, I’d remove that sticky thing at the top once it’s scrolled down”). DELETED, not disabled — apps/mobile/components/aurora/today-hub-dock.tsx, packages/core/src/today-hub-dock.ts and its 24 unit tests, the nav-scroll provider’s raw-offset subscribe() (the dock was its only subscriber), the safe-area layering the dashboard shell carried so the floating row could retract clear off the screen, and GlassPillRow in swiftui.tsx — the fusing SwiftUI capsule row that had no other consumer. WHY: it is the second thing to occupy that slot and the second to be cut from it, which is the finding. Today’s top edge is where the athlete’s attention starts, and anything pinned there is charging rent on the most valuable strip of the screen for a control that is wanted a few times a session. The sticky rail before it was a status bar; this was navigation, which is the better answer to the same question — and it still meant a row of glass hovering over every read of the dashboard, the feed and the Performance page, moving on its own as you scrolled. The switcher now scrolls away with the header it belongs to, like every other in-flow control in the app, and the viewport below the status bar belongs entirely to the view you chose. THE HUB STILL HAS ITS OTHER WAY IN: the app header’s side menu carries the three hub rows and switches the tab in place from any scroll depth (aurora/side-menu.tsx), so “scroll back to the top” was never the only route. WHAT SURVIVES ELSEWHERE, so nothing has to be rediscovered: the shared spring vocabulary the dock bound itself to (motion.ts springs.lens / springs.sheet) is unchanged, and glass-as-behaviour — capsules sharing a Namespace so the system fuses them and flows the material between shapes — lives on in GlassToolbarGroup, which took the taps as well as the pixels. Revisit only with evidence that athletes actually need to cross hub tabs from deep inside a scroll; the answer then is more likely a gesture or a menu than a permanent object at the top edge, since two shipped attempts at that object have now been removed." },
  { id: "today-themed-groups", area: "Core", status: "shipped", title: "Today — dashboard grouped into four themed clusters (the first one unlabelled, then Recover / Progress / Explore)", detail: "Today's dashboard had grown into a flat scroll of ~9 blocks — plan hero, exercises rail, done-today, feeling, week verdict, endurance lanes, other sports, Go Full, coach rail — each with its own head but no higher-level structure, so the page read as chaos. It is now organised into FOUR NAMED CLUSTERS, each opened by a GroupMark. REVISED (Aug 2026, the cluster-marker study): the marker is the HEADLINE TIER — the cluster's name as pure type in the display face at 23 (serif in Kyoto Hour, like the masthead), sitting between the masthead (34) and the block heads (18), with 36px/dp of whitespace above doing all the separating and NO other chrome. The first cut (mono-uppercase label + a trailing hairline) was retired: label-plus-rule is the default divider generated layouts reach for, and the rule restated a separation the whitespace already performed. TRAIN = the scheduled hero (week rail / logbook / chooser / plan card) + the Done-today card (schedule and receipt are two sides of the same day). RECOVER = the feeling/check-in card. PROGRESS = This week's verdict, the favourite-exercise widget rail (MOVED here from its old slot between the hero and Done-today — it is per-movement trend, not part of the day's job, and now sits directly under the verdict whose lifting columns it breaks down), endurance lanes and other sports. EXPLORE = Go Full + the coach marketplace rail (the label the retired Explore tab left behind). Nothing was removed; every block keeps its own head and behaviour. Web today.tsx + mobile home.tsx at parity via shared i18n keys w.home.group.* (EN/PL/DE); markers are headings to screen readers (role=heading / accessibilityRole=header). REVISED AGAIN (Aug 2026): THE TRAIN MARKER IS GONE — the first cluster now opens with no name at all, and the key `w.home.group.train` is deleted in all three locales rather than left dangling. A marker earns its place by naming a TURN in the scroll (\"you have left the training and arrived at recovery\"); the first cluster turns from nothing. It opened directly under a masthead already naming the day, on a hub tab the pills already label Dashboard, so the word restated in the display face what the screen was visibly about — and it made Dashboard the only one of the hub's three views that did not open straight into its content (Performance and Feed both do). The gap the marker used to hold (it sat at mt={0} because the head emits HUB_MASTHEAD.gap.below) transferred to the row beneath: all five branches of the plan-hero ternary dropped their own marginTop, since RN does not collapse margins and any one of them keeping it would sit a full gap too low. Guarded in hub-masthead.test.ts — the key may not reappear on any hub surface or in the i18n file, and no branch of that first row may re-grow a top margin. The other three markers are untouched." },
  { id: "performance-themed-groups", area: "Core", status: "shipped", title: "Performance page — four themed runs, the last three marked (State unnamed / Training / Season / Explore)", detail: "The Performance hub tab (Today's second pill) was a flat scroll of ~11 blocks — state card, trajectory, injury risk, week recap, breakdown, volume, trends, horizon, goal+season, return-to-play — numbered in comments but visually undifferentiated: the same disease Today's dashboard had before its clustering. It now wears the SAME taxonomy system: four named clusters, each opened by the shared headline-tier GroupMark (extracted to aurora/group-mark.tsx on both clients when this second consumer arrived; Today imports it too). STATE = Performance State → 14-day trajectory → injury risk → return-to-play ('how is the body right now'). TRAINING = this week's recap → discipline breakdown → volume vs landmarks → eight-week trend ('the work'). SEASON = goal + season widgets + setup ('the long arc'). EXPLORE = the horizon doors (Sport S&C / Velocity / Endurance / AI Coach), closing the page exactly as Explore closes Today. Two moves, nothing removed: RETURN-TO-PLAY moved up from the page's foot into the State cluster — it is injury management, and a flagged tissue's protocol now sits beside the flag instead of a whole page away; the HORIZON doors moved from mid-page to the end. The GroupMark's `mt` prop keeps the optical 36px above every marker inside containers that contribute their own spacing (web's grid gap). New i18n w.home.group.state/training/season (EN/PL/DE); Explore reuses the existing shared label. Web + mobile at parity. CORRECTED (Aug 2026) — THE MARKERS WERE NEVER IN THE CODE. This entry claimed four GroupMarks on Performance and there are none: `grep GroupMark apps/mobile/components/aurora/performance.tsx` is empty and always has been (`git log -S` on that path returns nothing), and the web twin was deleted with the web client without ever growing them either. What DID ship is the ORDER and the two moves — state to training to season to the exits, with return-to-play up in the State run and the horizon doors at the end — carried by numbered comments (1 to 7) over cards that each head themselves with an ASection. The clusters are real as a sequence; they are simply not announced. The three labels minted for the markers (w.home.group.state/training/season, EN/PL/DE) rendered nowhere for months and are now DELETED, guarded by hub-masthead.test.ts — every surviving w.home.group.* key must be rendered by something, so a label can no longer outlive the marker it was written for. THE OPEN QUESTION this leaves is a divergence, not a bug: Today marks its runs (Recover / Progress / Explore) and Performance marks none, so the hub's two long scrolls are structured by different grammars. Deciding it means picking one — mark both or mark neither — and it is tracked as `hub-cluster-grammar`. RESOLVED (Aug 2026) — MARK BOTH, MINUS THE FIRST RUN. The markers are now in performance.tsx: TRAINING above the level card, SEASON above the season card, EXPLORE above the exits, all three the shared GroupMark off the shared w.home.group.* keys (training + season restored in EN/PL/DE, explore reusing the key Today already renders — one label rendered by both tabs is what makes this one vocabulary rather than two). THE STATE RUN TAKES NO MARKER, and that is the decision's real content. It opens the screen directly under the masthead, so a name there announces what the screen is visibly about — the Train defect — and it was additionally the one label that would have echoed its own first card word for word (\"State\" over a card headed \"Your state\"). Today's first run is unmarked for the identical reason, so the rule now reads the same on both tabs: A MARKER NAMES A TURN, AND THE OPENING RUN TURNS FROM NOTHING. Guarded in hub-masthead.test.ts by two tests that must be read together: no hub screen may place a GroupMark as the first rendered thing after its masthead (mutation-tested — injecting one fails it), and every surviving w.home.group.* key must be rendered by something, so a `state` label cannot come back without a marker and the marker cannot come back without failing the first guard. Post-marker spacing is the blocks' existing marginTop 16, inside the range Today already uses under its own markers (0 / 20 / 14)." },
  { id: "hub-cluster-grammar", area: "Core", status: "shipped", title: "One cluster grammar across the hub's two long scrolls", detail: "Today and Performance are two tabs of ONE hub and they structured their scrolls differently: Today named its runs with the headline-tier GroupMark, Performance named none of them — its four runs existed only as an order and as numbered comments in the source. Both were defensible alone and the pair was not, because the same athlete swipes between them and the wayfinding tier appeared and vanished with the pill they tapped. DECIDED: mark both. Performance took three markers (Training / Season / Explore) rather than four — the run that OPENS a tab stays unnamed on both tabs, the same rule that deleted Train off Today the same week. The alternative considered and rejected was mark-neither (delete Today's four and put a SectionSeam at each junction): coherent, but it spends the wayfinding tier on the scroll that actually uses it, and it needed three new seams plus a new home for the endurance lane-order chip. THE COUNTS ARE DELIBERATELY NOT SYMMETRIC — Today marks four turns, Performance three — because the grammar is about what a marker MEANS, not how many there are: every run after the opening one is named, and Today simply has one more run. Do not 'fix' the mismatch by adding a fourth. See performance-themed-groups for the implementation and its guards." },
  { id: "progress-head-and-exits", area: "Core", status: "shipped", title: "Progress cluster — one head grammar + one 'see more' (consistency wave 1)", detail: "First of the Progress-cluster consistency waves (see the 'Progress, made consistent' design study). ONE HEAD GRAMMAR: the exercises rail's nano-mono kicker head ('Exercises – your favourites') is promoted to the Explore-standard SectionHead the other three blocks already wear (bold display 800, fs.title, chalk; new i18n w.home.exw.title EN/PL/DE), so the cluster no longer mixes two head tiers; and the right-hand slot now follows one codified rule — a FACT sits in ash (date range, count), an ACTION sits in lime ('All ›', the sort toggle), and a head carries at most one. ONE 'SEE MORE': the cluster used to end its blocks four different ways (dashed ghost card in-rail, two bare '+N' outline buttons, door rows). The rule is now by container: a RAIL ends in a trailing ghost tile (other-sports' '+N' button became a dashed ghost tile inside the rail carrying its count, the exercises rail's ghost card already complied), and a FULL-WIDTH block ends in a door row (endurance's '+N' button now wears the This-week card's door-row anatomy — ink2 fill, 32px glyph tile, display title, count on the right). Expand/collapse behaviour, caps and counts unchanged. Web + mobile at parity." },
  { id: "progress-tile-skeleton", area: "Core", status: "shipped", title: "Progress cluster — one tile skeleton + one chart language (consistency wave 2)", detail: "Second Progress-cluster consistency wave (see the 'Progress, made consistent' design study). ONE TILE SKELETON: every rail card in the cluster now reads name row → headline figure → chart zone → footer meta, top to bottom, radius 16, mono figures at 26 — an athlete who has learned to read one tile has learned to read them all. The exercises rail's 340×200 radius-28 sparkline heroes came down to 200px instrument tiles obeying the contract (name + ticker delta, figure — still the shared-element that flies into the exercise page — strip, label/kind footer); the endurance VolumeTile leads with this week's km as its figure over the strip; the TrendTile carries its pace delta on the label row, the current pace as its figure, and draws its line — the one honest exception to bars (pace is a level, not a quantity per week) — in the strip's own 24px zone so even the exception aligns; the endurance/sports tiles' shared Tile primitive gained the name-row `right` slot the exercises tiles use. ONE CHART LANGUAGE: a shared HistoryStrip primitive (aurora/history-strip.tsx on both clients — up to 8 bars, one 24px zone, past periods at 34% tint, current period full-strength, in the block's semantic hue: lime lifts / teal endurance / violet sports) replaces the three drawings the cluster had (92px area sparkline, 46px bars, 26px bars). Normalization lives in core history-strip.ts (unit-tested): historyStripBars() maps LEVEL series (weights, paces — pace reversed so faster is taller, the old sparkline's rule) onto a floored band so the change's shape shows; zeroBasedBars() keeps ZERO-BASED weekly series honest (an empty week reads as zero); exerciseStripBars() dispatches per widget-card metric so both clients bucket identically. All metrics, tap-throughs, semantic hues and full-bleed rails unchanged. Web + mobile at parity; new i18n w.home.end.window8 (EN/PL/DE)." },
  { id: "progress-parentage", area: "Core", status: "shipped", title: "Progress cluster — the verdict is the index, the rails are its chapters (consistency wave 3)", detail: "Third and final Progress-cluster consistency wave (see the 'Progress, made consistent' design study). The This-week card's columns are the PARENTS of the rails beneath it, and that parentage is now visible: each rail's head meta quotes the figure it decomposes. SUPERSEDED IN PART by progress-cluster-dedup (Aug 2026): quoting a parent WHOLE turned out to be indistinguishable from repeating it, so the two total-quoting heads are gone — Exercises now quotes its own COVERAGE ('3 of 11 movements') and Endurance quotes nothing, its slot given to the lane-order control. Other sports keeps the original quote ('3.1 of 5.2 h this week') because it was always a SHARE, which is information rather than an echo. The reconciliation the quotes existed to prove survives as a TEST assertion (progress-parentage.test.ts), not a label. THE RECONCILIATION RULE: core progress-parentage.ts (unit-tested) computes nothing of its own — it calls the exact activitySummary the verdict renders over the exact Mon–Sun 'week' range, so a head quote and a column can never disagree to the decimal. The right-slot rule holds — one item per head. Exercises' 'All ›' lives in the rail's trailing ghost tile (since wave 1). Endurance's sort toggle spent a few months on its own quiet right-aligned row under the head, which stacked two right-aligned mono uppercase lines on top of each other; with the quote retired it moved INTO the head's slot as a bordered ash chip (see progress-cluster-dedup). THE DOORS: the History + Analytics door rows moved OUT of the This-week card to the END of the cluster (rendered by today.tsx / home.tsx via the now-exported DoorRow), so Progress reads summary → breakdowns → one exit point instead of a detour between the summary and the rails that decompose it. DELIBERATE DEVIATION from the study: verdict column taps still open their inline breakdown panels (shipped behaviour, better than scrolling away) rather than becoming scroll anchors. New i18n w.home.group.metaWeek / metaOf (EN/PL/DE) — metaWeek since retired, metaMoves in its place. Web + mobile at parity." },
  { id: "week-rail-done-receipt", area: "Core", status: "shipped", title: "Week rail — done-state receipt ('All done for today')", detail: "A DONE plan day on the week rail now collapses to a RECEIPT instead of re-listing the settled prescription (user-picked concept 1 'The receipt, corrected' from design/done-card-redesign-ideas.html, Jul 2026): a lime check + 'All done for today' headline ('Completed' when viewing a past done day), a quiet stat row, and 'View in history →' as an uppercase mono TEXT link (the completed state stands down — no pill). THE TRUST RULE, in @hybrid/core done-receipt.ts (doneReceipt/doneReceiptStats, unit-tested): completedAt−startedAt is the span of the LOG, not necessarily the WORKOUT — a session typed in after the fact spans a minute of typing while carrying a full day of sets, which previously read as an absurd '1 min'. A wall-clock duration below the plausibility floor (≥1 min per logged set) is DROPPED, falling back to athlete-entered cardio/conditioning minutes when present, else the stat simply doesn't render — a figure that can't be vouched for is omitted, never shown. Stats carry the unit INSIDE the value ('48 min', '3.0 t' via fmtTonnage in the athlete's units, '7.2 km') under bare uppercase labels (one grammar), ordered duration – volume – distance – sets, each included only when non-zero. Both clients at parity off the same core model (web + mobile week-rail.tsx; the catch-up list for sessions postponed onto the date is preserved on the receipt); new i18n keys w.home.rail.allDone / duration (EN/PL/DE). (The finishing-time meta line this shipped with, 'finished 11:18', was later deleted outright — see receipt-no-clock.)" },
  { id: "rail-day-stamp", area: "Core", status: "shipped", title: "Rail corner — distance from now (and the run), never the date the strip already showed", detail: "Both week rails printed the absolute date in the corner of the day detail (‘Fri 31 Jul’). Directly above it the rail draws that same day as a chip — weekday over day-of-month, highlighted — and on the done state it sat beside a headline reading ‘All done for TODAY’, so five words managed to repeat both the week strip and the headline. Replaced by the DAY STAMP (@hybrid/core day-stamp.ts, dayStamp/dayStampText, unit-tested): the corner reports how far the day sits from NOW, and never says something the card has already said. Inside a ±6-day window it reads ‘Yesterday’ / ‘3 days ago’ / ‘In 2 days’; an open today reads ‘Today’; a DONE today spends the slot on the one thing the card cannot otherwise show — the athlete’s current run (‘6-day streak’, from engines/habits streak() — strictly consecutive days) — and stays EMPTY when there is no run yet, because a first day is not a streak. Past the window the absolute date returns, since ‘in nine days’ stops meaning anything. Pure label math on YYYY-MM-DD keys, so all four surfaces stamp identically: plan week rail + logbook rail, web and mobile, in every state (done, open, missed, skipped, postponed, empty past day). New i18n keys w.home.rail.stampToday / stampYesterday / stampTomorrow / stampDaysAgo / stampInDays / stampStreak (EN/PL/DE)." },
  { id: "rail-done-floor", area: "Core", status: "shipped", title: "One day, one card — the Done-today rows moved INSIDE the week rail", detail: "Today drew the same day twice: the week rail summarised it under the calendar, and the Done-today card three hundred pixels below listed the very same sessions with icons and figures. receipt-names-once fixed the copy; this fixes the structure. The rows now render as the rail card's LOWER FLOOR (aurora/done-floor.tsx, web + mobile twins), and the separate card is gone from both rail paths — roughly 90px of Today's scroll returned (one border, one card's padding, the 16px gap and a display-weight numeral that only ever counted to three). Chosen as option 01 of a seven-option merge study, over the variant that also dropped the receipt's stat tiles: the day's totals keep their display weight. THE SEAM IS THE WHOLE DESIGN. Above it is what the plan ASKS of the athlete (title, prescription, Start / Skip / Move, the catch-up list); below it is what they DID. A tennis row sitting under Trap-Bar Deadlift with only a hairline between them reads as the last line of the prescription, so the seam carries the old card's count as a mono label ('2 done today', '1 done Sun 2 Aug' on a scrubbed day) plus the arrow into the Done-today sheet; at zero it drops the arrow and speaks the invitation instead ('a match, a run, a swim — it lands here'). The floor composes with EVERY day state, which is what made 01 survive plan mode: a missed plan day now shows its terracotta note directly above the 8.4 km run that day actually held, and a rest day shows the moon and the easy swim in one card instead of two unrelated ones. The floor is built by the SCREEN (today.tsx / home.tsx) and passed into the rail as a `doneFloor` node, because the screen owns the sessions, the quick-log sheet and the Done-today sheet — the rail owns only the surface. Day scoping, the Plan tag, the lime/teal tile split and 'Log another sport' are unchanged. The logbook rail's past-empty day gets no floor (it already says 'nothing logged'), and the count-based plan hero — which has no rail to sit in — keeps the floor in a card of its own so the confirmation loop never vanishes. Zero new i18n: the seam reuses w.home.today.glanceDone / glanceDoneOn and the alsoTodayCopy state machine." },
  { id: "logbook-action-pair-everywhere", area: "Core", status: "shipped", title: "The action pair reaches every day state — correcting what logbook-action-pair claimed", detail: "A TestFlight build showed the previous entry was not true of the shipped code. logbook-action-pair said the dashed +-tile was retired and that Log a sport sits on every empty day; two of the three parts were wrong, and a screenshot caught it a release later than a test would have. (1) THE DASHED TILE WAS NEVER REMOVED — done-floor.tsx still drew it, so an empty today rendered the new action pair AND the old dashed row forty pixels apart, both saying 'Log a sport'. The claimed deletion is now real: the row is a neutral capsule drawn by the same AActionPair component, and the floor takes a `logRow` prop that the screen sets false in logbook mode, where the rail already carries the action. The empty branch stops rendering the floor at all — a logbook day is `logged` exactly when it holds sessions, so on that branch the floor could only draw its 'a match, a run, a swim' caption, which was the block's third saying of one sentence. (2) THE PAIR ONLY EVER RENDERED ON EMPTY DAYS, so a LOGGED day had none: a hybrid athlete's second session of the day had no entry point but the dashed row, and a PAST day with training on it had no way to add the match it forgot — the exact case sport-log-past-day exists for. The pair now renders in the logged branch too, both actions neutral because the accent retires the moment the work is real. (3) The plan-hero path's floor was never given onDelete, so swipe-to-delete was missing on that surface; wired. Locked by a HARD case in apps/mobile/lib/design-tokens.test.ts asserting the floor draws no dashed border, the rail mounts AActionPair in BOTH day states, and the screen gates the floor's row — verified to fail against the shipped code before the fix." },
  { id: "rail-chrome-parity", area: "Core", status: "shipped", title: "Both week rails wear ONE separation device — locked by a HARD test", detail: "The logbook rail and the plan week rail are the SAME object in two modes (enrolling changes the card's fill, never its shape), so shedding the logbook card's border and shadow without touching the plan rail immediately put the two on different chrome — a drift introduced and caught inside one change. Both now carry the fill alone: borderWidth, borderColor, shadowColor/Opacity/Radius/Offset and elevation are gone from each. apps/mobile/lib/design-tokens.test.ts gains a HARD case (not a ratchet) asserting neither file contains a shadowOpacity, shadowRadius or elevation declaration, so the pair cannot drift apart again the way they did on hairlines, day-chip anatomy and the done floor before this. Also lands concentricRadius(parent, pad) in aurora/kit.tsx — iOS 26 concentricity as a function: a child inset on all sides by pad inside a container of radius parent is only truly concentric at parent - pad, and drawn at any other value the two arcs run on different centres, which is why a 12dp tile inside a 28dp card reads as pasted on rather than set in. Deliberately NOT applied to glyph tiles: those are marks, and marks take RADIUS.inner." },
  { id: "logbook-rail-scrolls", area: "Core", status: "shipped", title: "The week rail SCROLLS, and the checkmark becomes a load bar", detail: "Seven flexed chips could only ever show seven days, which is why the card's header had to carry a 'Last 7 days' caption explaining the control's own limits — a control that can show its own extent needs no sentence describing it. The logbook rail is now a horizontal ScrollView over a FOUR-WEEK window (LOGBOOK_SCROLL_WINDOW = 28, one mesocycle: far enough to see a pattern, near enough that building every day up front stays free), fixed 44dp cells rather than a flexed seventh-of-whatever-is-left, bleeding by the card's own padding so a day passes under the card edge instead of clipping mid-cell with a gutter beside it, with matching internal padding so a resting day still lands on the content column. It parks on today (scrollToEnd) on FIRST layout only — never on every content change, or a re-render mid-scroll would yank the athlete back from the week they were reading. The w.home.logbook.window caption is retired from the header, whose right slot now belongs to the exit into History. AND THE TICK BECOMES A FIGURE: LogbookDay grew loadMin (the day's trained minutes, summed across its sessions and read through the DEVICE's recording wherever there is one, since doneReceipt is device-true by default — so a watch-measured 52:18 counts as 52, not the 50 that was typed) and load (0-1 against the HEAVIEST day in the window; relative rather than absolute because there is no honest universal ceiling for 'a hard day'). A checkmark says something happened; a bar the width of the day's relative load says how much, in exactly the same pixels — and a screenful of them is a training block at a glance, which is the only reason the strip exists. A trained day with no trustworthy duration (a strength log with no clock) gets a visible floor rather than zero, because 'trained' and 'trained for zero minutes' are different facts and only one of them is true; an untrained day fills nothing (silence, never terracotta). VoiceOver reads the minutes, not the label. Unit-tested in logbook-week.test.ts (summing, normalisation, the untrained zero, the no-duration floor, the 28-day window)." },
  { id: "logbook-card-chrome", area: "Core", status: "shipped", title: "One separation device — the logbook card sheds its border, its shadow and two hairlines", detail: "The card said 'this is a surface' four times over: a solid ink2 fill AND a 1px border AND a drop shadow (opacity .18, radius 14, elevation 3) AND three full-bleed hairlines inside it. A grouped Section — what the system would draw here — is a PLANE, so the plane stays and the drawings around it go: border, shadow and elevation deleted from logbook-rail.tsx. Of the three hairlines exactly one survives, the week/day seam, and it earns its place because above it is the week and below it one day. The done floor's own rule (done-floor.tsx) becomes 22dp of whitespace — its seam already carries a mono-caps COUNT that announces the change of subject, so the line under it was the third horizontal rule in one card; this applies the house rule that stacked rows separate by whitespace, not a rule. The receipt's 'View in history' exit loses its border-top for the same reason. Shared with the plan week rail, which mounts the same floor." },
  { id: "logbook-swipe-delete", area: "Core", status: "shipped", title: "Delete a mis-logged session by swiping its row on Today", detail: "Until now the only way to remove a session was not from this card at all: you opened it, and then you were inside the session, which is the wrong place to decide it should not exist. The done-floor rows now wrap the shared SwipeRow (the same gesture the live logger's set ledger and the notifications list use: short swipe opens the action, full swipe commits it, release resolved by velocity projection), backed by the existing deleteSession API and a reload. The confirmation is the APP's confirm sheet, NOT Alert.alert — apps/mobile/lib/design-tokens.test.ts bans system alerts outright as a HARD rule, and it is right to: a user who has learned this app's sheet gesture should not meet an OS modal at exactly the moment they most need to feel oriented. Destructive styling puts the affirmative second so the deleting choice is never the one under a resting thumb. The row swipes only where the host passes onDelete, so surfaces that cannot reload after a delete are unchanged. i18n EN/PL/DE (w.home.today.deleteTitle/deleteBody)." },
  { id: "logbook-empty-day-grammar", area: "Core", status: "shipped", title: "The empty day — ONE block, three tenses (SwiftUI ContentUnavailableView grammar)", detail: "A day with nothing logged on it is the most repeated state in the app, and the logbook rail drew it THREE different ways inside one card: today rendered a baseline row with a chalk headline and a mono sub-line (emptyToday/emptyTodaySub), a past day rendered the same shape with an ash headline and a different sub-line (emptyPast + doneModalEmptyDay), and the invited action underneath was a dashed +-tile with a whispered caption ('a match, a run, a swim'). Three sets of type, three registers, one state — which is how the card ended up with five headings before it reached a fact (Today / Train / Your training / Last 7 days / Nothing logged yet). Now ONE component: mobile aurora/empty-day.tsx renders SF-Symbol-shaped glyph, title, one description line, then the actions, with the tense logic in core (empty-day.ts emptyDayCopy, unit-tested) so it cannot fork per client. THE TITLE IS ONE STRING IN EVERY TENSE — 'No training logged' (w.home.logbook.emptyTitle) is true of a first run, an open today and a Saturday three weeks back; only the sentence under it changes, because only the tense does (emptyFirstRunBody / emptyTodayBody / emptyPastBody, EN+PL+DE). A first run is separated from an ordinary open day by hasHistory, not by a different drawing. The grammar is SwiftUI's ContentUnavailableView; the native view is deliberately NOT mounted, because it styles its own type (SF Pro in the middle of a card set in Archivo) and has no slot for the app's own buttons — where a system view IS the right call is a system CONTROL, see sport-log-native-controls. Design study: reference/today-train-swiftui-ive-studies.html (invariant 1 of 3)." },
  { id: "logbook-action-pair", area: "Core", status: "shipped", title: "A primary and a secondary on one baseline — the dashed tile retired", detail: "The logbook rail's empty day offered two actions at the SAME weight in two different vocabularies: a full-bleed glowing chartreuse pill ('Log a workout'), then a hairline, then a whispered caption, then a dashed square with a lime word ('Log a sport') — four devices spent saying 'and also this', and a dashed border is a web affordance that appears nowhere in iOS. Both actions belonged there (a session you START, live and structured, is a different job from a sport you LOG after the fact) — only one can be first. New shared aurora/action-pair.tsx draws SwiftUI's .borderedProminent beside .bordered on one baseline, in the app's own paint because the primary carries the brand fill and no system button style does. The hierarchy is a FILL, never a layout: an open day fills the primary, a day that already holds training fills neither and both go neutral, so the card stops asking the moment the work is real without anything moving — and there are never two accent surfaces on one screen. Log a sport is now present on EVERY empty day, today's included. Design study: reference/today-train-swiftui-ive-studies.html (invariants 2 and 3)." },
  { id: "sport-log-past-day", area: "Core", status: "shipped", title: "Log a sport on a day that has already passed", detail: "The quick sport log stamped new Date() at save time and nothing else, so a match played on Saturday could only ever be recorded as having happened at the moment it was typed. That is why the done floor HID its log row on every day but today (done-floor.tsx isToday gate) — there was no date to disagree about, so there was nothing to offer, and the card could not answer the most ordinary request an athlete has: 'I played on Saturday and forgot to log it.' QuickSportLog now takes an optional date; the logbook rail's empty PAST day offers 'Log a sport on {weekday}' (w.home.logbook.sportOn, EN+PL+DE) and hands the viewed day up through a new onLogSport prop; home.tsx normalises it to that day's LOCAL NOON so no timezone can slide the record into the day next door, and the save clamps at now (a logbook holds evidence; there is none from the future). The sheet's own When row is editable, so the seeded day is a default rather than a cage. The device-import bar and the 2x2 grid of one-tap sport cards are unchanged — the sheet already offered 'Import from your watch' above the cards, and both stay exactly where they were." },
  { id: "sport-log-native-controls", area: "Core", status: "shipped", title: "SwiftUI DatePicker + Stepper in the sport log (real native controls, not a skin)", detail: "First mounting of @expo/ui SwiftUI views that are CONTROLS rather than materials — the distinction the Liquid Glass work never made. NativeDateField (aurora/swiftui.tsx) is a SwiftUI DatePicker in its compact form: the value reads as a tinted token and tapping it presents the system's own calendar popover, clamped to now via the range prop. It is the control that makes sport-log-past-day possible at all. NativeStepper is a SwiftUI Stepper nudging the duration in fives, bringing repeat-on-hold, disabled ends at min/max and the adjustable VoiceOver trait that a pair of hand-drawn +/- buttons never gets; it is ADDITIVE — the numeric field above it stays the exact-entry path and is the only one off-iOS — so nothing depends on the native layer having rendered. A system date control is exactly where native styling is CORRECT: a calendar popover is the platform's, everyone already knows how to drive it, and an app has nothing to add to it. Both are self-contained leaves per the composition rule in swiftui.tsx and return null off-iOS, where the caller keeps its own field. Verified by typecheck + the iOS export bundle; native rendering still needs a device round (see swiftui-kit)." },
  { id: "receipt-names-once", area: "Core", status: "shipped", title: "The day's work is named ONCE — the Done-today card keeps the names", detail: "Today said the same thing twice within one screen. The week rail's done receipt led its meta line with the day's work — the logbook rail joined every session logged that day ('Tennis – Afternoon workout – finished 14:33'), the plan rail printed the scheduled day's title ('Upper + Engine – finished 11:18') — and a few hundred pixels below, the Done-today card lists those very sessions by name, one row each, with an icon, its own meta figures and a PLAN tag. Two namings, one day, no new information in the second. The receipt now drops the names entirely and keeps the ONE fact the card below deliberately withholds (see the row's own note: no clock time on a row, because the save time is not the workout's time) — the clock the day finished at, rendered as the whole meta line ('finished 14:33') and omitted altogether when nothing stamped a completion. Everything else on the receipt is untouched: headline, day stamp, trusted stat row, 'View in history →', the plan rail's catch-up list. All four surfaces at parity (plan week rail + logbook rail, web and mobile). @hybrid/core stripWeekdayPrefix is deleted with the line it existed to trim. SUPERSEDED IN PART: the clock that survived this pass has since been deleted too — see receipt-no-clock." },
  { id: "receipt-sets-are-strength", area: "Core", status: "shipped", title: "Sets are a strength figure — a swim, a match and a squash game never show one", detail: "The done card poured every session through the gym’s mould: a 10-minute pool swim rendered ‘1 SETS’ because DoneReceipt.sets counts one EFFORT per cardio/conditioning block (the unit the duration plausibility floor needs). True of the data model, meaningless to the athlete — swimming, tennis and squash have no such thing as a set. doneReceipt() now also carries `strengthSets` (sets logged on strength blocks, by the same reps-or-load-filled rule as liveSessionStats), `sets` is documented as the effort counter it is, and doneReceiptStats() renders `strengthSets` — so a swim shows duration + distance and stops, a match shows its time, and a day that lifted AND swam reports the 11 sets it lifted instead of the 12 the effort counter padded. mergeDoneReceipts sums it for the logbook’s multi-session days, and the Wrapped’s sets tile reads it too (its discipline gate already kept sets away from a swim; this keeps a MIXED day’s count honest). Same principle as session-wrapped-discipline, applied at the source. Regression-tested for swim / tennis / squash / lifted-and-swam." },
  { id: "receipt-energy-total", area: "Core", status: "shipped", title: "The day’s receipt reports its energy — total kcal beside duration and distance", detail: "The done receipt summed the day’s duration and distance across every session logged on it, but stopped there: the athlete who swam and then played tennis saw ‘62 min / 2.6 km’ and no answer to the one question a mixed day raises — what did all of that cost? doneReceipt now carries `kcal` + `kcalMeasured`, resolved through the SAME engine the Wrapped uses (energy.ts sessionEnergy): the DEVICE’s measured burn when the session is matched to one, else the MET model (Compendium of Physical Activities) scaled by the athlete’s bodyweight at that session’s date — which every caller of doneReceipt already passes. The two honesty rules travel with it. NO BODYWEIGHT, NO NUMBER: the model is linear in mass, so without a weight the stat is omitted rather than invented. AND THE ESTIMATE SAYS SO: the value wears a ‘~’ (`~620 kcal`, the Wrapped’s idiom, plus an `estimate` flag on the stat) unless a device counted every calorie in it — mergeDoneReceipts sums a multi-session day and keeps `kcalMeasured` true only when EVERY contributing session was measured, so a watch-counted run plus a typed gym session reads as the part-modelled total it is. ENERGY renders last in doneReceiptStats (duration – volume – distance – sets – energy), after everything logged or measured, and the stat row now wraps so a five-figure day doesn’t run off a phone. All four rails inherit it off the one shared model — web + mobile week-rail.tsx (the enrolled day) and logbook-rail.tsx (the plan-less logger’s day, where the totals are the day’s sum). SECOND GAP CLOSED IN THE SAME ROW: the receipt has ALWAYS summed `elevationM` across the day (device-true, exactly like the distance) and never rendered it — a day with two hilly runs kept its climb in the model and showed nothing. CLIMB now renders beside the distance, in metres, only when the ground actually rose. New i18n w.home.today.energy + w.home.today.climb (EN/PL/DE); unit-tested for the estimate, the measured case, the no-bodyweight case, the mixed-day merge and the flat-ground case." },
  { id: "receipt-no-clock", area: "Core", status: "shipped", title: "The done receipt drops the finishing clock", detail: "The finished-day receipt carried a quiet meta line under its headline — 'finished 16:32' — and on a day trained more than once it was wrong. The plan week rail builds its receipt from the ONE session that fulfilled the scheduled day, so the clock stamped the FIRST workout while the hero and the stat row beside it described the day; the logbook rail merged every session of the day and kept the LAST clock, so the same line meant two different things on two rails. Neither reading was a fact the card needed: the receipt is a statement of what the day amounted to (duration, tonnage, distance, climb, sets, energy), and a single wall-clock instant is not part of that sum. Any clock a workout genuinely owns already lives on the session itself (the workout page's date line, the Wrapped). So the line is DELETED end to end rather than patched: DoneReceipt loses its `finishedClock` field and doneReceipt() stops stamping one (sessionClockTime is no longer imported there), mergeDoneReceipts loses the last-clock rule, both receipt-block.tsx twins lose the rendered line (the hero now sits directly under the headline), and the i18n key w.home.rail.finishedAt is removed in all three languages (EN/PL/DE). All four surfaces at parity (plan week rail + logbook rail, web and mobile). Tests updated: the two clock assertions in done-receipt.test.ts collapse to one that the field is gone, and the merge test asserts the sums alone." },
  { id: "receipt-hero-gutter", area: "Core", status: "shipped", title: "The done receipt, redesigned — one left edge, one figure at size", detail: "The finished-day receipt looked ragged and nobody could say why. The cause was measurable: the card ran THREE left edges at once — hairlines at 0, the ✓ and the headline at the card's 20px padding, and every line beneath them at 20+31 (31 being the width of a ✓ glyph plus its gap), aligning to a mark the eye cannot see because the glyph above it is half that wide. Three edges with no hierarchy between them read as ragged rather than indented, and the seam made it worse by starting its hairline at 20 and its label at 51. Picked from 15 layout studies (design/receipt-layouts-02-05-07, concept 02-B 'hanging tick'). TWO CHANGES, both in one new shared component per client (web + mobile aurora/receipt-block.tsx, twins), now used by BOTH rails — the enrolled week-rail day and the logbook-rail day — so the done state exists once instead of four times. (1) THE GUTTER: the ✓ moves into a 31px GUTTER COLUMN of its own and EVERY line of the receipt including the headline starts at the same edge (a real flex column, not an absolutely-positioned glyph — Yoga and CSS have disagreed about whether a parent's padding applies to an absolute child, and the two clients have to lay out identically; the ✓ and the headline share one 24px line box so they sit on one baseline from separate columns); the seam keeps its full-width hairline with its label on that same edge (RECEIPT_GUTTER is exported so the rails align to it rather than re-typing 31). The only edges left in the card are hairlines. (2) ONE NUMBER EARNS THE SIZE: new core doneReceiptHero() picks the single figure the day was ABOUT and hands back the remainder — hero at 44/42px display, the rest as one quiet mono line. The priority MIRRORS sessionHeadline (engines/history-views.ts) — tonnage, else distance, else duration — so the day card and the History rows beneath it can never headline two different facts about the same day. With one caution a DAY needs and a session does not: distance only leads when the new DoneReceipt.cardioLead says the day's ground was covered in ONE discipline. A swim plus a tennis match sums to a true 2.6 km and a meaningless headline (nobody trains '2.6 km' of swimming-and-tennis), so that day leads with the 62 minutes it did train; mergeDoneReceipts keeps the lead only while every session that covered ground covered it the same way. Climb, sets and energy never lead — two are supporting detail and the third is MODELLED, which must not outrank a measured figure. DoneReceiptStat now carries `figure` and `unit` apart (as well as joined in `value`) so the hero can set them at two sizes on one baseline without any client parsing a string, plus `needsLabel` for the values that cannot stand alone — the supporting line drops labels, but re-attaches an UPPERCASE MONO one for climb and sets (the house grammar for a label, and the only casing correct in every language — lowercasing the German 'Höhenmeter' would not be German), because '320 m' beside '9.4 km' is unreadable as a climb and a bare '11' says nothing. No separator glyph anywhere in the line (house middot rule); the flex gap does it. Unit-tested: the split and its order, figure/unit apart, tonnage leading a lifted-and-ran day, the two-discipline refusal (the reported day), the same-discipline double run keeping its lead, needsLabel, and the no-figures day. AUG 2026 — THE TICK GOES, AND THE GUTTER WITH IT (design/receipt-tick-removal.html, on user review). The gutter was the right fix for the layout it was handed, but it was a fix for the ✓, and the ✓ was the thing that had not earned its place: the day chip above the hairline already marks the day as trained, the headline already says 'Logged', and a receipt of real figures is not in doubt — three statements of one fact, the loudest of them the least informative, and the only chartreuse in a card whose one accent should be the thing you can DO. Removing the glyph collapses its 31px column, so every line of the card — headline, hero figure, supporting figures, the seam's count, the rows under it — now starts on the card's own 20px content edge. ONE left edge, which is what the gutter was reaching for; RECEIPT_GUTTER is deleted rather than defaulted so nothing can re-indent to a column that isn't drawn. THE EXIT GOES ENTIRELY, which took two attempts to see. 'View in history' was a line of its own under the day's action pill, indented to that same dead column, and in the plan rail it sat ABOVE the catch-up list behind a second hairline — two rails, two arrangements, six words. It was first made one component riding the card's last baseline beside the action pill, and it did not FIT: a 187px 'Log a sport on Mon' pill and a 133px 'VIEW IN HISTORY →' label do not share a 311px measure (375pt screen, less the shell's 12dp gutters and the card's 20dp padding), so the row wrapped and handed the exit back the orphan line the arrangement existed to remove — and at 320pt it was never close. The collision was the symptom; the redundancy was the cause. THIS SCREEN ALREADY HAS A DOOR INTO HISTORY: the DoorRow after the retrospective clusters, glyph ▤, titled 'History' and subtitled 'Every session, weeks and trend', placed there by the rule wave 3 established — one exit point after all the breakdowns. The day card's link was a second door to the same room, 400px above the first, drawn in a different vocabulary, and it was the one that could not fit. So both rails now END ON WHAT THERE IS TO DO (the logbook day on its action pair, the plan day on its catch-up list); the plan rail's second hairline goes with the link; aurora/history-exit.tsx is deleted, AActionPair's `trailing` slot is withdrawn rather than left as an unused API, the rails' `onNavigate` prop and its two call sites in home.tsx are removed, and the w.home.rail.viewHistory string is dropped from all three locales. The card states the day and offers the next action; leaving is the screen's job. Net on a logged day: two lines and one hairline fewer, one accent fewer, one left edge instead of two, and one door to History instead of two." },
  { id: "dashboard-logbook-rail", area: "Core", status: "shipped", title: "Today — logbook week rail for the plan-less logger ('The Constant')", detail: "A free user with NO enrolled plan but real logged history used to see the three first-run chooser cards forever — five workouts by Friday and still no calendar on Today (user-picked concept C1 'The Constant' from reference/first-log-dashboard-c1-the-constant.html, Jul 2026). Now, after the very first logged session, the SAME week-rail object the plan state ships mounts in LOGBOOK MODE on both clients (web aurora/logbook-rail.tsx + mobile twin, identical anatomy to week-rail.tsx: one ink2 surface, header row, seven day chips, full-bleed hairline, state-aware day detail). The shared engine is @hybrid/core logbook-week.ts (unit-tested): logbookWeek() maps the trailing 7 LOCAL calendar days ending today onto LogbookDay entries reconciled against logged sessions (field names match ScheduledDay so Today's day-scoped cards accept a lifted day from either rail), plus mergeDoneReceipts() to sum a multi-session day into one honest receipt (per-session untrusted durations stay dropped; last finish clock wins). The logbook vocabulary is deliberately promise-free: a day is either logged (chalk number + ✓) or quiet greyscale — no 'missed', no terracotta, because a logbook makes no promises. Day detail states: logged → the plan rail's done receipt (✓ 'All done for today' / 'Logged' on past days, the finished clock, trusted stat row, the day's sessions as the card's lower floor per rail-done-floor, 'View in history →'); today empty → 'Nothing logged yet' + the one lime 'Log a workout' pill; past empty → quiet 'Nothing logged'. Tapping a chip scopes the masthead + Also-today + feeling cards to the viewed day, exactly like the plan rail (shared railDay state, ScheduledDay | LogbookDay). The chooser DEMOTES instead of disappearing: an Explore-standard 'Train your way' section head (mono 'Optional' on the right) over a horizontal SNAP SLIDER of compact StructureCard cards (Follow a plan / Build your own / Just train — the ChooserCard's corner-glow anatomy at ≈72% rail width so the next path peeks in; mobile ScrollView snapToInterval, web scroll-snap, the exercise-widget rail's idiom), so the paths stay reachable without re-onboarding a regular every day. Day zero (no plan, no history) keeps the full three-card chooser; enrolling swaps the logbook rail for the plan week rail — same geometry, new fill. Premium athletes with history keep their AI-prescription hero (unchanged). New i18n w.home.logbook.* (EN/PL/DE); the masthead's 'Free' caption now also shows in logbook mode." },
  { id: "nav-explore-train", area: "Core", status: "shipped", title: "Bottom-nav Explore + Train destinations (Explore SUPERSEDED)", detail: "HISTORICAL — the Explore half of this is superseded by nav-nutrition-tab (below); Train is unchanged and still shipped. EXPLORE (retired Aug 2026) opened a discovery surface (aurora/explore.tsx — search, a coach rail, the plan library as a COVER FLOW, and a community-feed preview) off the second bar slot on both clients. TRAIN (the centre slot) opens the Train launcher hub (AuroraTrain — start today's session / AI / repeat-last / routines / resume a draft) on BOTH clients; previously mobile bypassed the hub and dropped straight into the live logger, and web opened the logger directly with no hub — both now open the SAME launcher (web mounts it as the app-shell 'train' screen off the pill-nav centre + the Train sidebar/springboard item). LAUNCHER REDESIGN (Minimal + adaptive hero): the hub is a calm single hairline list of ways to start (Empty session – From a routine – Repeat last), topped by ONE adaptive slot — for a paid athlete it's the big lime hero for today's AI-prescribed session; for a free/casual user it's the Premium pitch (routines themselves are free — up to 2 saved templates, see workout-routines — so the routines row lists them for every persona); and once ANY session is logged today (sessionsOnDay) the slot collapses to a compact 'done' marker (lime check + today's session, taps through to the summary) while the list stays for an extra session. From the hub one tap still starts the live session." },
  { id: "nav-nutrition-tab", area: "Core", status: "shipped", title: "Bottom nav — Nutrition replaces Explore; Follow a coach moves to Today", detail: "The second bar slot now carries NUTRITION, and the Explore tab is gone. The reasoning is in @hybrid/core nav-bar.ts, where the tab table lives: a five-slot bar is the scarcest real estate in the app, and Explore spent one of those slots on DISCOVERY — something a user does a handful of times ever — while eating happens several times a day, every day, and the tracker sat two taps down inside More. Every block Explore carried was a PREVIEW of a screen that still exists: its search opened Find friends, its plan Cover Flow opened Plans, its community rail opened the Feed — all three are still in More, so nothing became unreachable. The one block with a daily job, FOLLOW A COACH, moved onto Today. WHAT CHANGED, identically on both clients: (1) @hybrid/core nav-bar.ts AURORA_NAV_TABS reads today – nutrition – train – more – profile (AuroraNavTabId lost 'explore', gained 'nutrition'), and motion.ts NAV_ROOT_ORDER follows it, so a Today↔Nutrition move slides sideways like any two bar roots. The feed/discover/coaches/leaderboard ROOT_ALIAS entries that pointed at the Explore root are gone with it — those screens are leaves reached from More (or from Today's coach rail) now, so they push and pop instead of sliding. Pinned by nav-bar.test.ts + motion.test.ts. (2) A new kit glyph, `fork-knife` (theme/icons.ts AURORA_ICON_PATHS + the 216px mobile PNG), drawn as the Fuel widget's PlateGlyph redrawn in the kit's 72×72 stroke box so it carries the same optical weight as village/grid beside it; AURORA_NAV_ICONS.nutrition points at it instead of the borrowed `store` shopfront, so Nutrition reads the same in the bar, the More springboard and the web sidebar. On iOS the tab takes the SF Symbol `fork.knife` for both states — it ships with no plain .fill counterpart (only circle-wrapped variants, which would change SHAPE on selection), so the tint carries selection, which is what iOS itself does for symbols without a fill pair. (3) MOBILE: app/nutrition.tsx moved into app/(tabs)/nutrition.tsx (a native tab bar renders every route in its folder), so router.push('/nutrition') from Today's Fuel sheet now switches tabs rather than pushing a screen. AuroraNutrition gained a `root` prop that drops the masthead back button — a back arrow on a tab root is a dead control. (4) WEB: the app-shell 'explore' screen and aurora/explore.tsx are deleted; 'nutrition' was already a shell screen, so the pill nav lands straight on it. (5) FOLLOW A COACH on Today is now a RAIL ON THE PAGE, not a row that opened a sheet — the rail sells coaches by showing them, and a row reading 'Follow a coach' sold nothing. It mounts headerless under a section head (display title left, mono 'Find a coach for your goal' right — the Explore SectionHead vocabulary, no marker) with bleed + seeMore, so the cards run full-bleed under the screen edge per the slider rule and the trailing 'See more' carries through to the marketplace. The coach Sheet and the DeferRow component it fronted are deleted from both clients. Today's order below the premium cards is now Fuel → Follow a coach. i18n: nav.explore and the nine Explore-screen-only keys (w.explore.title/sub/search/plans/all/plansCard*/community/feed) are removed in EN/PL/DE; the coach-card and seeAll/seeMore keys stay — they are shared by CoachRail, FeedPreview, the endurance lanes and the Nutrition rails. FOLLOW-UP (Aug 2026): the FOURTH slot changed the same way and for the same reason — More was a directory of ~40 screens, and opening a directory is not part of anyone's daily loop, so it moved to the side menu behind the Today header's avatar (`side-menu`) and MESSAGES took the slot (`direct-messages`, a stated placeholder for now). The bar then read Today – Nutrition – Train – Messages – Profile on both clients, from the same AURORA_NAV_TABS table (Train has since left the capsule for the detached action circle — nav-split-action); mobile registers app/(tabs)/messages.tsx with the SF Symbol envelope/envelope.fill pair and the kit's mail.png for Android, and the web pill nav lost its More SHEET entirely (it no longer opens any menu of its own)." },
  { id: "plans-cover-flow-rehome", area: "Core", status: "planned", title: "Rehome the plan Cover Flow + the community rail (ex-Explore)", detail: "RECORDED SO IT IS NOT LOST, per the capabilities rule. Retiring the Explore tab (nav-nutrition-tab) took its screen with it, and two blocks on that screen had no other host. (1) The PLAN COVER FLOW — three curated full-width 'album-art' covers (discipline-accent duotone over a fixed-dark base, oversized glyph, name + hairline meta) over a full-bleed micro-rail of the remaining previews, with press-scale and cover-glyph parallax under reduced-motion gates. Its curation survives in @hybrid/core (FEATURED_PLAN_IDS → FEATURED_PREVIEWS, still exported and still unit-tested in explore-plans.test.ts), and the cover RECIPE itself is alive on the Plans screen (goal tiles + CoverHero/plan-hero use it at tile and screen scale) — what is gone is the curated three-up entry point. (2) The COMMUNITY RAIL — RESOLVED, by deletion. aurora/feed-preview.tsx sat unmounted on both clients while Today's CONNECT surface grew the REAL feed as a hub tab (SocialFeed in aurora/today.tsx, FeedView in aurora/home.tsx), which is a better front door than a preview strip: it is the whole stream, and its cards now open to the whole session (connect-feed-open-post). The stub was the choice this entry asked for — mount it somewhere or delete it deliberately — so both files are deleted rather than left to rot as a component nothing renders. Three older entries (kyoto-hour-light-theme, feed-timeline-density, theme-discipline-rollout) still name feed-preview.tsx in their PAST-TENSE record of sweeps that touched it; that history is accurate and is left as written. What remains open here is only (1), the plan Cover Flow: neither loss is a regression in REACH (Plans and Feed are both nav destinations), but the curated three-up entry point still has no host. Decide where it belongs — a 'Discover' section on Today, a curated header on the Plans screen, or a shelf in More — and mount it there." },
  { id: "bottom-sheet-quick-actions", area: "Core", status: "shipped", title: "Slide-up bottom-sheet quick actions (Today + upgrade)", detail: "A shared slide-up bottom-sheet primitive (aurora/sheet.tsx on web — a fixed overlay with a scrim fade + CSS transform slide-up, Esc/scrim to dismiss and a body-scroll lock; aurora/sheet.tsx on mobile — a transparent RN Modal with an Animated translateY panel + scrim + grab handle, kept mounted through the exit animation) backs every Today quick action on BOTH clients. Each sheet is a FOCUSED, purpose-built quick action (not the whole screen crammed in): Quick Log → 'Already trained?' a 2×2 sport grid (tap-to-log, + Other opens the searchable picker); Readiness → 'How ready do you feel?' a 4-choice picker (Primed/Good/Flat/Wrecked, spectrum dots) that logs a daily check-in in one tap; Done today → everything logged today + a calendar link; Nutrition → 'Add a meal' a compact quick-add (today-vs-target kcal, manual name+kcal → energyIntake signal, the premade-meals grid, and a 'Targets, trends & history →' deep-link to the full tracker); Follow a coach → the coach rail. The full check-in + nutrition SCREENS stay reachable on their own routes; the sheets share the same write paths (/api/checkins, /api/signals). The Full upgrade paywall is the same slide-up sheet: web app-shell renders it as an OVERLAY via a central navigate() intercept (any onNavigate('upgrade') pops the sheet, not a screen switch); mobile keeps its transparent-modal /upgrade route. Concise COCKPIT-badge sheet — benefit list, $9.99/month, a 7-day free-trial note, a violet 'Start free trial' CTA, 'Maybe later' — over the same Stripe Checkout / Apple IAP billing (live purchase still blocked on credentials, see full-billing). The Today 'Go Full' cards (Cockpit/Sport) were restyled to match: a soft violet fill (no lime rim), a value sub-line, an explicit 'Unlock with Full →' CTA, and a 'See plans →' action on the section header." },
  { id: "readiness-load-adjust", area: "Core", status: "shipped", title: "Subjective readiness → today's load auto-adjust", detail: "The Readiness pick (Primed/Good/Flat/Wrecked) now MECHANICALLY scales the prescribed session, not just the copy. prescribeSession gained an optional subjectiveReadiness (ReadinessFeeling) — a load multiplier applied on top of the progression-signal dose: primed ×1.05, good ×1.0 (neutral, identical to omitting it), flat ×0.94, wrecked ×0.85 AND −1 work set (a real deload). The factor lives in @hybrid/core readiness-feeling.ts (READINESS_LOAD_FACTOR / readinessLoadFactor), so the feeling→load-effect mapping is one source of truth; the explanation string calls out the adjustment ('You checked in feeling flat, so I eased the load back'). Unit-tested (engines.test.ts: primed > good > flat > wrecked load, good == neutral, wrecked sheds a set). BOTH clients feed TODAY's READINESS ANSWER (quickCheckinFeeling of the day's row, independent of the week rail's selected day) into every athlete-facing prescribeSession call — this used to be checkinFeeling, the average of all four metrics, which meant the pill quoted the athlete words they never said; see checkin-answers-survive: the Today/Home readout (web today.tsx rx also flows into onStart → the started session; mobile home.tsx rx) AND the AI-session generators the athlete starts (mobile app/workout.tsx source='ai' + the empty-state loadPrescribed; web logger.tsx in-logger 'AI session'). Still layered on top of the OBJECTIVE readiness score (fatigue + wearable bio via toBiometrics), which the pick does NOT overwrite — subjective load nudge and objective recovery score are separate, and the 'why' explains both. SURFACED (Jul 2026): the adjustment is no longer buried in the 'why' prose — prescribeSession now returns a structured readinessAdjust ({ feeling, loadPct, setAdj }, present only when a feeling actually moved the load/sets, absent on neutral 'good'), and both clients render a glanceable pill: the feeling's own ReadinessFace + a mono line tinted in its semantic accent ('Eased to 94% — you're feeling flat today', 'Boosted to 105%…', 'Deloaded to 85%, −1 set…'; a bodyweight wrecked day shows the shed set only, since it carries no % load). New i18n w.home.today.rx{Primed,Flat,Wrecked,WreckedBw} (EN/PL/DE); the load-% delta assertions extend engines.test.ts. RELOCATED (post-merge): the Today/Home redesign moved the readiness-driven AI prescription OFF Today (premium-no-plan users now land in logbook mode) onto the COCKPIT, so the pill lives there now — web cockpit.tsx + mobile cockpit.tsx render it in the Performance-State card's readiness block, beneath the readiness Ring + rx.why, off the same readinessAdjust. Both Cockpits fetch TODAY's check-in feeling (fetch /api/checkins on web; fetchCheckins folded into the load Promise.all on mobile) and pass it as subjectiveReadiness, so the Cockpit prescription both scales and explains the nudge. The scaling itself still applies to the SESSION the athlete starts (workout.tsx / logger.tsx), unchanged by the relocation." },
  { id: "nutrition-training-aware", area: "Core", status: "shipped", title: "Training-aware nutrition targets (eat for the work)", detail: "Nutrition targets now flex with training. @hybrid/core/engines/load.ts gains sessionEnergyKcal (a MET-style estimate of a logged session's energy cost from each block's minutes × intensity × bodyweight — resistance work is metabolically flatter, conditioning climbs with RPE) and trainingEnergyOnDay (sum across today's sessions). adaptiveTargets takes a trainingKcal and adds it ON TOP of the goal target entirely as CARBOHYDRATE, so a hard training day earns a higher carb + calorie target while protein (bodyweight-set) and fat hold — a periodised 'fuel for the work you did' model, not a static number. Both clients fetch today's sessions (web useSessions / mobile useSessionsQuery), compute the bump from the athlete's latest weight, and surface a '+N kcal training fuel' chip under the calorie ring on rest→train days. Pure + unit-tested (sessionEnergyKcal, trainingEnergyOnDay, adaptiveTargets fuel path). Web + mobile at parity; i18n (EN/PL/DE). Also in this pass: bodyweight is now DERIVED FROM THE PROFILE — POST /api/body mirrors weightKg into a bodyMass Signal (source 'profile', like the check-in bridge), and both nutrition weigh-ins (onboarding + the Body sub-view) write to /api/body, so the profile owns one canonical weight that the maintenance estimate + smoothed trend read." },
  { id: "nutrition-meal-picker", area: "Core", status: "shipped", title: "Meal-based add-food picker + Create Food (redesign)", detail: "The redesigned add-to-meal flow, web + mobile at parity, pixel-matched to the approved mockups. The Nutrition hub now leads with today's MEALS (Breakfast / Lunch / Dinner / Snacks) — each row shows the kcal already logged to that meal and opens the PICKER attributed to it. Logs from the picker carry the meal as the Signal `source` (breakfast/lunch/dinner/snack), so the hub groups today's intake by meal. The PICKER (view 'add') is a full screen: a meal chooser in the header ('Dinner ▾'), a text-or-barcode search box, Quick Log + Create Food actions, and Recent / Favorites / Personal tabs. Recent (a per-device MRU written on every log) and Favorites (a per-device starred set, toggled by the row star) persist in localStorage / AsyncStorage so the tabs work with no backend change; Personal is the saved-products library (swipe-left to delete). A row's lime add-circle opens the shared portion editor. CREATE FOOD (view 'create') is the redesigned product builder as its own screen: a photo/label affordance (repurposed to the Full label-scan), Food Name, Serving + Unit (unit sheet: gram/ml/oz/piece/serving → composed into servingLabel), and colour-coded Calories / Carbs / Protein / Fat, saved via the existing /api/nutrition/products. Quick Log is the fast kcal+macro sheet (4·4·9 fill). i18n EN/PL/DE." },
  { id: "nutrition-food-subname", area: "Core", status: "shipped", title: "Food subname + de-cluttered Create form", detail: "Two changes, web + mobile at parity. (1) SUBNAME — SavedMeal + FoodProduct gain an optional `subname`, the user's OWN label for a food (a dish named \"Tuna Bowl\" with subname \"w/ sushi rice\"; \"Whey scoop\" / \"the usual way\"). Threaded through the products/meals API (POST accepts it, GET returns it) and shown as a lighter second line everywhere a food appears: picker rows, saved meal + product lists, the portion editor, and carried into the Recent MRU. (2) The add-food form was rebuilt to kill the card-in-card-in-card border clutter (see the reported screenshot): ONE blend layout — a title plate holding Name + Subname, then a macro HERO (calories as the big number, protein/carbs/fat as three light tiles), with serving on a single quiet line (products only). The same form serves BOTH targets via a createMode: 'Create Food'/'Add manually' → product (serving + unit → servingLabel), 'Create meal' → meal (logs as one serving). This replaces the old inline bordered builders as the primary path. NEEDS the subname column in production — run reference/sql-nutrition-subname.sql in the Supabase SQL Editor (idempotent ALTER TABLE ADD COLUMN); until then POST/GET still work but silently drop subname. i18n EN/PL/DE." },
  { id: "nutrition-hub-prefs", area: "Core", status: "shipped", title: "Nutrition prefs: onboarding once, saved goal, custom parts, meals-or-products picker", detail: "Five fixes to the Nutrition hub, web + mobile at parity. (1) ONBOARDING SHOWS ONCE — the first-run wizard was gated on a per-device localStorage flag that was only set on the 'Continue on Free' button, so starting the ✦ trial OR just weighing in left it unset and re-showed onboarding every visit. Completion is now persisted SERVER-SIDE (/api/nutrition/prefs, stored under answers.nutrition on the shared OnboardingState row, soft-guarded + update-only so it never falsely flips the GLOBAL onboarding gate), with localStorage/AsyncStorage as a fast cache and a derived 'has any energyIntake/bodyMass logged' safety net; it's marked done on EVERY wizard exit. (2) GOAL IS SAVED — the chosen goal persists in the same prefs and is restored on load (was resetting to 'maintain' each visit). (3) WEIGHT FROM PROFILE — the Body sub-view now leads with the current weight READ from the profile (latest bodyMass) labelled 'synced with your profile', and the onboarding weigh-in reuses an existing profile weight instead of asking again — one canonical source, no duplicate silos (weigh-ins already write to /api/body → bodyMass). (4) CUSTOM PARTS OF THE DAY (Full) — resolveMealParts/mealPartKey in @hybrid/core merge the four built-ins (breakfast/lunch/dinner/snack) with a user's own parts (e.g. 'Pre-workout'), persisted in prefs (mealParts, capped at MAX_CUSTOM_MEAL_PARTS=8) and rendered in the home list, diary + the picker's part chooser; free users keep the four. Logs carry the part key on Signal.source. (5) MEALS-OR-PRODUCTS PICKER — the add-to-meal picker gained a 'Meals' tab so any part of the day can be filled with either a full saved MEAL or a single PRODUCT (was products-only). Pure helpers unit-tested; i18n EN/PL/DE." },
  { id: "nutrition-diary-edit", area: "Core", status: "shipped", title: "Editable food diary (edit quantity / delete logged entries)", detail: "Logged foods were four anonymous Signals (energyIntake + P/C/F) with no id, name or grouping, so nothing could be edited or removed. Two layers now make every logged item editable. (1) The FoodLog model (prisma/schema.prisma) is the RICH record: one row per logged food/meal storing PER-SERVING macros + a qty + the mirrored Signal ids. All logging routes through /api/nutrition/log (POST creates the FoodLog AND the mirrored Signals the engines read); /api/nutrition/log/[id] PATCH rescales an entry's quantity (and its Signals), DELETE removes the entry + its Signals. (2) SIGNAL-DERIVED ENTRIES (Jul 2026) make the Diary work with NO migration at all: because every log writes its four Signals at one exact ts + source, the server regroups Signals that no FoodLog row owns back into individual entries (derivedFoodEntries in @hybrid/core, pure + unit-tested) and returns them from GET /api/nutrition/log with a `sig:<signalIds>` id. PATCH on such an id rescales those Signal readings by a relative factor, DELETE removes them — so an item logged before the FoodLog table shipped, or on a database where the migration hasn't run, can still be corrected or removed. The Diary (web + mobile at parity) lists the selected day's entries grouped by part, each with a −/＋ stepper (an absolute serving count for a FoodLog row, a ×multiplier for a derived one) and a delete; entries whose source isn't a part of the day (a quick log, a food-search hit, a preset) group under 'Other' so nothing logged is unreachable; the recent-days list selects any past day (120-day window) to edit/delete its records. DIARY DAY-SUMMARY: the Diary leads with a day-summary card read from the always-on Signals (dailyNutrition) — a ‹ › date stepper picks ANY calendar day (never past today, with a 'Today' shortcut), a big total-kcal-vs-target number, and P/C/F progress bars vs target. New i18n w.recovery.nutrition.{prevDay,nextDay,diaryTotalsOnly,loggedEntry,otherEntries} (EN/PL/DE). What still waits on the FoodLog migration is only the NAME on an entry logged without the table (a derived entry is labelled by its time of day) — see nutrition-foodlog-table." },
  { id: "nutrition-foodlog-table", area: "Core", status: "shipped", title: "Food diary — named entries (FoodLog table)", detail: "The FoodLog table stores the NAME, the brand/serving subname and the per-serving macros + qty behind a logged item, so the Diary shows 'Chicken & rice — 2 servings' and edits it as an absolute serving count. reference/sql-nutrition-log.sql was RUN in the Supabase SQL Editor (Jul 2026) — the table, its (userId, ts) index and its owner-only RLS policy now exist, so POST /api/nutrition/log writes the row alongside the mirrored Signals and every newly logged item carries its name across devices. Items logged BEFORE the migration stay Signal-derived (their names were never stored anywhere to recover) and remain fully editable + deletable via the derived-entry path — see nutrition-diary-edit. The soft guards on both routes stay in place, so a database without the table still degrades cleanly." },
  { id: "nutrition-recipes-library", area: "Core", status: "shipped", title: "Recipes library (read-only, cook-along)", detail: "A curated, READ-ONLY recipes library shared by both clients (RECIPES in @hybrid/core/recipes.ts — pure + typed, no DB, no external image assets). Each recipe carries per-serve single-number macros (the no-range rule), a meal + active time, ingredients (quantities stored for baseServes) and a method (steps with optional per-step timers). The hero is a warm brand-accent gradient keyed to the dish (recipeHeroBg) with the dish emoji — self-contained, no photo hosting. UI (web + mobile at parity): BROWSE is the library root — see THE LIBRARY IS THE PLANS TAB below; DETAIL (view 'recipe') is the hero + a per-serve Energy/Protein/Carbs/Fat strip + a serves stepper that SCALES the ingredient quantities live (scaleIngredientQty / formatIngredient); COOK (view 'cook') is a read-only step-through with a progress bar, per-step timer chips and Back/Next. The library is a Full feature (access.canUseRecipes, unit-tested, mirrored on both clients): the Nutrition-hub recipes entry shows a ✦ for free (casual) users and tapping it routes to the upgrade screen. DETAIL also offers 'Create meal' — recipeToMeal saves the recipe's per-serve macros (name + dish emoji) into the personal meal library (POST /api/nutrition/meals, respecting the free meal cap) so a favourite recipe becomes a one-tap saved meal. Logging a cooked recipe straight into the day is a separate planned capability (nutrition-recipe-cook-log). HUB PRESENTATION (Jul 2026): the hub's Recipes menu row was replaced by a left/right RAIL at the very bottom of the Nutrition hub — every recipe as a swipeable card under a SectionHead-anatomy head ('Recipes' + 'See all →' on the right, carrying the ✦ and the upgrade route for free users; every card tap routes free users to upgrade too). Full-bleed like every screen-level rail (web `margin: 0 calc(-1 * var(--page-pad-x))` with matching padding, mobile `marginHorizontal: -16` + snapToInterval against AuroraScreen's 16dp gutter). 'See all' lands on the library root. THE DETAIL NOW OPENS WITH THE PLAN DETAIL'S COVER (Jul 2026), not a lookalike: the collapsing full-bleed cover scaffold was lifted out of plans.tsx into apps/web/components/aurora/cover-hero.tsx (web) and reuses the already-generic CoverScreen (mobile plan-hero.tsx), both driven by core's new `recipeCoverView` — one view-model, so a recipe cover and a plan cover cannot drift. A plan and a recipe are the same kind of object from the athlete's side (a shape, a cost in time, a set of numbers, that you commit to and then follow); they were arriving through two unrelated headers only because they were built in different months. SLOTS: the tint becomes the duotone accent, the meal the chip, the time the top-right label, and '2 serves / 7 ingredients / High protein' the meta line. NEW `recipe` VARIANT on the cover spec: the dish emoji is the cover art at emblem scale and FULL COLOUR — a ghosted 7%-white emoji is a grey smudge, not a dish — which is also why it is the one variant that fades to NOTHING rather than to a residue (a monochrome mark can drift into the pinned bar as texture; a colour emoji only smears behind the bar title). The old boxed four-tile macro strip is now the cover's HEM — the same rule-topped editorial columns a plan uses, straight on the ink — at FOUR columns rather than the plan's three, because a recipe's headline numbers are its four macros and dropping one to fit a borrowed grid would be the layout choosing what the athlete sees; both clients tighten the type at four. BLURB: `Recipe` gained a `note` — one line saying what the dish actually is, the job `GoalPlan.desc` does on a plan cover — written for all eight recipes and unit-tested for presence and length, because a cover with an empty blurb slot reads as clipped. METHOD ON THE DETAIL: the steps now render on the recipe page (numbered, with their timer chips) instead of living only inside the cook flow. You could previously read a recipe end to end without ever seeing how it is made, which made 'Start cooking' a decision taken blind; it also gives the page the length a collapsing cover needs to be worth having (the bar only pins once there is roughly a screen-height of content beneath it). THE COOK VIEW WEARS THE PLATE (Aug 2026): its 150px warm-gradient band with a centred emoji — the last surface still using the retired `recipeHeroBg` / `RecipeHero` treatment, now deleted from both clients — is the recipe cover compressed into a card, driven by core's `recipeCookView`: the same duotone wash and full-colour dish, the meal as the chip, the dish as the title, the step counter in the slot the cover gives to time, and one tick per step along the plate's bottom edge (so the method's length is stated by the plate rather than by a separate progress row and a separate 'Step 2 of 5' line, both now gone). The back button is the cover's own HeroNav ON the plate, so the screen no longer carries a centred title repeating the dish's name. It is deliberately NOT the full collapsing cover: the cook screen doesn't scroll and ends in a sticky action bar, so a collapsing hero would promise a collapse that never comes. `recipeCookView` also CLAMPS the step index and reports `last`, so neither client can advance off the end of the method. NO DOCKED CTA, deliberately: the plan detail docks Enrol because its button sits in flow, while this screen already ends in a sticky action bar, so Start cooking never leaves the screen. THE LIBRARY IS THE PLANS TAB (Aug 2026): browsing now has the same THREE LEVELS Plans has — library root → collection → recipe — and every level rides the cover scaffold the detail already rode, so the whole stack is one object at three compressions. ROOT: the soft 'library' cover (a geometric ◉, the level named in the chip, the recipe count top-right, the collections on the meta line — recipeLibraryCoverView), a docked collection-chip rail that JUMPS to a shelf rather than filtering to one (narrowing to a single collection would just empty the screen), a search field over names, notes AND ingredients (searchRecipes — typing 'avocado' finds the toast), and one FULL-BLEED shelf per non-empty collection (recipeShelves). Each shelf's head is the Explore SectionHead anatomy and is itself the way in to that collection, stating the count ('3 recipes →'); its tiles are recipe COVERS at tile scale (recipeTileView — the accent wash, the dish emoji in FULL COLOUR, the time top-right, the energy under the name), not the old two-up cards. COLLECTION: 'Breakfast' (and Lunch / Dinner / the high-protein cross-cut) now has a HERO of its own the way a goal does in Plans — recipeCollectionCoverView, riding the `recipe` (plate) variant because its cover art is a dish emoji, with the shelf's extremes on the meta line ('From 5 min', 'Up to 22 g protein') and a plain-text blurb per collection in core. NO aggregate hem on it: macros averaged over a shelf say nothing, so each recipe below carries its own three differentiating numbers (recipeCardStats: energy, protein, time) as a rule-topped hem on a plan-shaped card. A recipe opened from a collection goes BACK to that collection (the hub rail's recipes still go back to the root). A full-bleed rail INSIDE the cover scaffold cancels `COVER_GUTTER` (the scaffold's 16dp child padding, exported from plan-hero.tsx), not AuroraScreen's 12dp gutter — the Plans SHELVES had been bleeding by the gutter and resting 4dp inside the true screen edge, which the house rule forbids, the same mismatch that had left the Plans chips 4dp off their column; every consumer now imports the one constant. The HUB RAIL carries the same cover tiles as the library shelves (RecipeTile at the rail's card width, still snapping, still ✦-gated) rather than its own gradient-hero cards — a recipe reads as one object wherever you meet it, and the hub is where most people meet it first. The old filter chips + 2-up grid are gone; filterRecipes / RECIPE_FILTERS stay in core for the filter vocabulary the chips' labels come from." },
  { id: "nutrition-label-panel", area: "Core", status: "shipped", title: "Full label panel — saturates, sugars, fibre, salt, kJ", detail: "Nutrition logging was four numbers (kcal + protein/carbs/fat), which is not what a food label states and cannot answer the questions an athlete actually asks of a burger: how much of that fat is SATURATED, how much of those carbs is SUGAR, am I over on salt. packages/core/src/food-facts.ts is the new single source of truth for a food's nutrition: NutritionFacts = the four macros (required) + an OPTIONAL MicroFacts panel (satFat / sugar / fiber / salt, grams). THE CENTRAL RULE: null means NOT STATED, never zero — an unstated sugar content is not a sugar-free food, so the clients render an em dash and never a fabricated 0 g. Pure + unit-tested: kj() (energy in kJ derived from kcal at the labelling definition 1 kcal = 4.184 kJ — deliberately NOT stored, since a second copy of the same fact can only drift), sodiumMg()/saltFromSodiumMg() (the EU salt field ↔ the US sodium field), scaleFacts (a scaled unknown stays unknown), sumFacts (a total is stated only when EVERY contributor states it, and reports which fields it had to drop — three foods with sugar plus one unknown is NOT a known sugar total), atwaterKcal + auditFacts (the 4·4·9 reconciliation, saturates ≤ fat, sugars ≤ carbs), per100g (fair comparison between two different serving sizes), factsCompleteness, and nutritionPanel() — the EU-order rendered panel (energy / fat / of which saturates / carbohydrate / of which sugars / fibre / protein / salt) that BOTH clients render from, so they can never disagree about what a food says. THE STACK: SignalKind gains satFat/sugar/fiber/salt (better: 'low' except fibre — these are ceilings, not totals to chase) and NutritionDay rolls them up per day; foodLogSignals() in the engine is the ONE builder deciding which Signals a log writes, so a food that doesn't state a field writes NO Signal for it (absence in the stream is how a day knows it is only partially described). Prisma: SavedMeal / FoodProduct / FoodLog gain nullable satFat/sugar/fiber/salt, FoodProduct also servingGrams + verifiedId, FoodLog also verifiedId — run reference/sql-nutrition-label-panel.sql in the Supabase SQL Editor (idempotent). Every write is SOFT-GUARDED with a two-attempt fallback (with panel, then without), so a database that predates the migration still logs and still saves, just without the panel. Open Food Facts hits now carry the panel too (saturated-fat/sugars/fiber/salt, read on the SAME serving-vs-100 g basis as the macros, plus a parsed serving weight). UI (web + mobile at PARITY): the portion editor gained the full facts panel under the macro tiles — scaled live by the quantity stepper, with a per-100 g column when the serving weight is known; the Create Food form gained an optional, folded-away Saturates/Sugars/Fibre/Salt block where a blank field stays NOT STATED; and the Diary day-summary carries the day's panel with an explicit 'from the foods that state it' caption so a partial total never reads as a complete one. i18n EN/PL/DE." },
  { id: "nutrition-verified-foods", area: "Data", status: "shipped", title: "HYBRID Verified foods (team-checked catalog)", detail: "The trusted tier of the food search. Open Food Facts is broad and free, but crowd-sourced — duplicates, per-100 g numbers filed as per-serving, missing saturates, a 'cheeseburger' that is somebody's guess. A HYBRID VERIFIED item is one a human on our team transcribed from the operator's OWN published nutrition table, reconciled against 4·4·9 (auditFacts) and DATED. packages/core/src/verified-foods.ts is the catalog: a VerifiedSource (the business) plus its VerifiedFood[] (English canonical name, the operator's own-language name as a search alias, serving label + weight, the full label panel, verifiedOn, provenance). Pure data + lookup, no DB, no key, no migration — adding a business is adding a literal. SOURCES: Max Premium Burgers (PL) with three checked items — Cheeseburger (121 g / 327 kcal / 1368 kJ, 19.6 g fat of which 7.3 saturates, 22.9 g carbs of which 4.1 sugars, 14.2 g protein, 1.7 g salt), Chicken Jr (123 g / 311 kcal — saturates, sugars and salt are NOT published for this item and are therefore null, never 0), and Fries (small) (172 kcal, 6.9 g fat of which 0.7 saturates, 25.4 g carbs of which 0.2 sugars, 1.9 g protein, 0.6 g salt; the operator states the portion but not its weight, so servingGrams stays null rather than being invented). SECOND SOURCE — LIDL (PL), the first RETAILER rather than a restaurant, with Rye Sourdough Bread (yeast-free) (per 100 g: 196 kcal / 830 kJ, 1.1 g fat of which 0.26 saturates, 39.0 g carbs of which 2.66 sugars, 7.1 g fibre, 4.06 g protein, 0.99 g salt; 450 g pack). A shelf item declares PER 100 G, so that is the serving stated — inventing '1 slice' would be inventing a slice weight the pack never publishes — and the pack weight is carried separately as packSize. A packaged good also declares what a restaurant dish never does, so VerifiedFood gained OPTIONAL ingredients + mayContain (the precautionary allergen line), both stored in ENGLISH like the rest of the catalog and rendered as a 'from the pack' card under the facts panel on both clients, with an explicit line saying the text is OUR translation of someone else's pack (an allergen statement is the last place to imply we quoted verbatim). A dish has none of these fields and its card is ABSENT rather than rendered with blanks. FIBRE CARRIES ENERGY: this bread is the item that exposed a real gap in the audit — on an EU label 'carbohydrate' EXCLUDES fibre (declared on its own line, not as an 'of which'), so 4·4·9 alone derived 182 kcal against a published 196 and an honestly-transcribed food would have failed auditFacts as mis-transcribed. atwaterKcal now adds fibre at 2 kcal/g (EU 1169/2011 Annex XIV) when it is stated, which reconciles the bread EXACTLY; an unstated fibre contributes nothing, so every existing item is unaffected. The pack's other unit (830 kJ) and our derived 820 kJ are BOTH right — an EU label computes each unit from its own factor table (fat 37 kJ/g vs 9 kcal/g, carbs 17 vs 4, fibre 8 vs 2) and those ratios are not 4.184 — so energy stays stored ONCE, in the stated kcal, rather than carrying a second figure that could drift. ONE NAME PER FOOD: the operator's own-language name (menuName, now nativeName/nativeLocale) is a SEARCH ALIAS ONLY and is no longer rendered anywhere. Printing 'Hamburger z serem' under 'Cheeseburger' put a second name on screen for a food the app had already named, which read as clutter rather than help; searching 'frytki', 'male', 'chleb zytni' or 'zakwasie' still finds the right item. SEARCH: /api/nutrition/search is now two-tier — verifiedHits() runs locally and instantly, mergeFoodHits() pins those rows ABOVE the community results and drops the community duplicate of a verified item (so the trusted row never sits beside a stranger's guess); the search accent-folds Polish (ł→l), so 'frytki'/'male' finds Fries (small) even though the app is English. Because the tier is local, an Open Food Facts outage now DEGRADES the result list instead of emptying it. UI (web + mobile at parity): the picker row carries the same quiet lime ✓ the verified-coach badge uses, and the portion editor leads with a provenance card naming the business and the date our team checked it. Provenance is persisted (verifiedId on FoodProduct + FoodLog) so a saved or logged item can be traced back to the catalog entry. auditVerifiedCatalog() is asserted in the unit tests — a mis-transcribed item fails CI rather than shipping under the badge. SOURCE MARKS: a VerifiedSource may carry the operator's own logo as SELF-CONTAINED inline SVG (SourceMark — never a URL: a remote logo breaks offline, hotlinks someone else's bandwidth, leaks a request per food opened, and is blocked by the artifact CSP; the app already resolves this the same way for nutrition glyphs and recipe heroes). The provenance card keeps the two claims APART — the operator's mark under a 'Nutrition data published by' label, then a divider, then the lime HYBRID ✓, then the trademark line — because a third-party logo beside a green tick otherwise reads as a partnership, and these operators have neither partnered with us nor reviewed the app. Marks never appear on picker rows (a logo per row is an advertising rail). Web renders the mark through a data URI on an <img> (never innerHTML, so a mark can't become an injection surface), mobile through react-native-svg's SvgXml; auditVerifiedCatalog() rejects any mark carrying a remote reference, an <image>, an xlink:href or an @font-face, so a broken mark fails CI rather than an athlete's screen. LIDL ships with its roundel — the operator's OWN logo vector, supplied by the HYBRID team: the blue field, the yellow disc, the red keyline ring and the LIDL lettering with its red i-dot and red diamond are all the artwork's own coordinates at its own colours. Two mechanical normalisations: the hard-coded width/height dropped for the viewBox, and the exporter's PAINTED WHITE 0.5-unit edge frame removed with the blue field run full-bleed instead. White paint is not a hole — on the charcoal card that frame would have drawn a keyline around the badge that the real logo does not have, which is the same failure mode MAX's evenodd knockouts exist to avoid. No coordinate inside the badge was touched. Unlike MAX's it is a SOLID badge rather than knockouts, so it carries its own ground and reads identically on both themes; verified by rendering it at 24/26/34/120 px on #1a1e17 and #f7f6ec in headless Chromium, which is the whole theme check with no simulator involved. MAX Premium Burgers ships with its REAL wordmark (packages/core/src/source-marks.ts — one enumerable home for every third-party mark): traced to vector paths from the operator's own artwork with potrace on the PNG's alpha channel, verified numerically against the source mask (IoU 0.9994, 0.034% of pixels disagreeing) rather than by eye. It is ONE path with fill-rule='evenodd', so the white keylines between the letterforms are true HOLES, not painted white — they take the colour of whatever the mark sits on, which is what keeps it legible on BOTH the AURORA charcoal card and the Kyoto Hour washi one (a raster or a painted-white knockout leaves white slabs on the dark theme). 2.9 KB. A source WITHOUT artwork stays a supported state, not a broken one: the clients render a WORDMARK FALLBACK — the business name in our own display face inside a hairline chip, visibly ours so it can never pass as an approximation of their logo. Adding artwork is one literal + one field — see reference/verified-source-marks.md, which also covers the licensing position (a Commons logo is typically PD-textlogo AND trademarked; MAX's mark came from artwork supplied by the team, so redistribution terms should be confirmed with the operator before a public release) and sourceMarkCredits(). FRESHNESS: every item was already dated and nothing ACTED on the date, so a five-year-old transcription looked exactly as confident as this morning's. verifiedFreshness()/staleVerifiedFoods() (pure + unit-tested, VERIFIED_STALE_AFTER_DAYS = 365 — the honest interval for a restaurant menu, which gets reformulated quietly but not monthly) age a check, and a stale item KEEPS its ✓ (the numbers were true when we checked; withdrawing the claim would be its own dishonesty) while the product page says in amber that it is due another look. The mark's credit line is now rendered at the foot of the source page under the trademark — until then 'third-party artwork stays enumerable' was a claim with nowhere to read it. i18n EN/PL/DE." },
  { id: "nutrition-verified-pages", area: "Core", status: "shipped", title: "Verified product pages (a page per checked food)", detail: "A verified food now has its OWN screen, and so does the business behind it — web + mobile at parity (NutView gains 'food' and 'source'). WHY A PAGE AND NOT JUST THE SHEET: the portion editor is a modal that exists to log a food you ALREADY trust — fast, in-context, dismissed on log. It is the wrong surface for deciding WHETHER to trust one, and it was carrying the mark, the provenance, the panel and the per-100 g column in a space that had to stay short. The two now split by job and the sheet is unchanged. FOOD PAGE: the source mark leads under a 'Nutrition data published by' row (tap → the source page), then the English name (ONE name — the operator's own-language name is a search alias, no longer printed beneath it), the serving line, an energy hero in BOTH units, the P/C/F strip (the recipe-detail idiom), the full facts panel with per-100 g where the serving weight is known — and an explicit line where it ISN'T, so a missing column reads as a fact about the food rather than a gap in the app. Then the claim: ✓ HYBRID Verified, the date, the operator's own wording of where the numbers came from, and the trademark line. Then MORE FROM THIS BUSINESS (relatedVerifiedFoods) so a checked item is a way into a checked menu instead of a dead end. A sticky footer carries Save to my foods + Log this, and Log this opens the SAME portion sheet — the page never re-implements logging. SOURCE PAGE: the mark at hero size, the business note, items-checked + last-checked (sourceCheckedOn = the newest verifiedOn across its items), the full item list, and the trademark line. NAVIGATION: in the picker a verified row's ⊕ still logs immediately (the fast path is untouched) while the row BODY opens the page — FoodRow gained an `onOpen` distinct from `onAdd`, and a non-verified row keeps both on add. The portion sheet's provenance card is now tappable straight through to the product page. `pageBack` remembers the origin so Back never lands somewhere the athlete didn't start. Both views guard on the RESOLVED catalog entry rather than the id, so a stale id falls through to the hub instead of setting state during render. SCOPE: pages are for VERIFIED items only — a community (Open Food Facts) hit has no stable id, no provenance and no sibling menu, so a page for one would be an empty frame around four numbers. New core helpers relatedVerifiedFoods + sourceCheckedOn, pure + unit-tested. i18n EN/PL/DE (noServingWeight, packSize, mayContain, labelTranslated, moreFrom, logThis, verifiedSourceTitle, itemsChecked, lastChecked, checkedItems); 'See all' reuses the shared w.explore.seeAll rather than minting a duplicate string. NOT BUILT YET: a URL/deep link for a product page (the web app shell is a screen switch, not routed) — see nutrition-verified-deeplink." },
  { id: "nutrition-verified-deeplink", area: "Core", status: "shipped", title: "Deep-linkable verified product pages", detail: "A verified product page now has an ADDRESS on both clients, so a checked item can be sent to someone, bookmarked, or landed on from a push. WEB: apps/web/lib/deep-link.ts mirrors the app shell's current screen into `?s=` and the open verified page into `?food=` / `?source=`. Deliberately NOT next/router — pushing a real route would remount the shell and throw away the directional screen transition — so it writes with history.replaceState (tapping through five tabs must not cost five Back presses to leave) and reads on mount plus on popstate, which makes the browser's Back button work across screens for the first time. The URL is a MIRROR of state, never the source of truth: state changes, then the URL follows. Params are bounded and pattern-checked before use (a link is attacker-controlled input), an unrecognised param is IGNORED rather than an error (a link from an older build must never land someone on a broken screen), and the mirror is cleared on the way out so a stale ?food= can't point at a page the user has left. Landing on a link uses setScreenRaw, not the transitioning setter, so arriving doesn't animate from a screen you were never on. MOBILE: app/food/[id].tsx and app/source/[id].tsx (expo-router, scheme `hybrid`) pass an id into the Nutrition screen's openFood/openSource props — the route only says WHERE to land, so there is one implementation of a product page rather than two; an id missing from the catalog falls through to the hub. SHARE: the product page header carries a share action — navigator.share where the browser has it, clipboard otherwise (a denied permission stays silent rather than erroring); mobile uses React Native's Share sheet and shares the HTTPS form, not hybrid://, because a link is only worth sending if it opens for someone who hasn't installed the app. STILL NEEDED for a tapped https link to open IN the iOS app: the associated-domains entitlement + an apple-app-site-association file on the domain, which needs a TestFlight build to verify — until then a shared link lands on the web page, which is the right fallback rather than a dead scheme. HISTORY SEMANTICS (fixed after first ship): opening a product page PUSHES a history entry while a tab switch only REPLACES — a page is a destination, a tab is not — so Back closes the page instead of leaving the app; the popstate handler closes an open page when the param is gone (otherwise the URL and the screen disagree and Back looks broken on the one screen we just gave history support to); and the screen clears its params ON UNMOUNT, without which leaving Nutrition left a stale ?food= behind and returning later dumped the athlete on that product instead of their hub. The parse/serialise half is PURE (parseDeepLink/applyDeepLink over plain strings, DOM only in the thin read/write wrappers) and unit-tested in apps/web/__tests__ — path traversal, script tags, over-long ids and junk query strings all degrade to 'the app opened', never to an error. i18n EN/PL/DE." },
  { id: "nutrition-verified-browse", area: "Core", status: "shipped", title: "Verified foods — browse the businesses we've checked", detail: "The verified tier was only reachable by STUMBLING INTO one of its foods through search: the source page existed but had no front door, so nothing in the app told an athlete the tier was there at all. The Nutrition hub menu now carries a 'Verified foods' row (the lime ✓ as its icon, the source count as its badge) opening a new 'sources' view — every VerifiedSource as a card with its mark, name and item count, into the existing source page and on to each product page. Web + mobile at parity; i18n EN/PL/DE. This is also the surface the catalog needs before it grows past one business. HUB PRESENTATION (Jul 2026): the menu ROW was replaced by a left/right RAIL at the very bottom of the hub — the businesses themselves as swipeable cards (mark, name + lime ✓, items-checked), under a SectionHead-anatomy head naming the rail ONLY ('Verified foods', no marker on the left), with the chromeless tail at the end of the scroll (see rail-tail-exit) opening the full 'sources' view. A menu row hid the catalog behind a chevron; the rail puts the companies on the screen. Full-bleed like every screen-level rail (web `margin: 0 calc(-1 * var(--page-pad-x))` with matching padding, mobile `marginHorizontal: -16` against AuroraScreen's 16dp gutter + snapToInterval), so cards slide under the physical screen edge. Card taps open the source page with pageBack='home'. THE LOGO LEADS THE CARD (Jul 2026): a new MarkPlate (both clients) gives the operator's mark a FIELD to sit on rather than dropping it straight onto the app's charcoal — a hairlined tile a touch lighter than the card (chalk at 7%), the mark contained and centred, never cropped or stretched. This is not decoration: MAX's wordmark is one evenodd path whose keylines are true HOLES, so on ink they fill with ink and the letterforms close up. The plate is deliberately NOT white — a white slab reads as a broken image on AURORA and as a sticker on Kyoto Hour. It is used EVERYWHERE a business appears on a card: the hub rail (full-width plate leading the card, since recognising the business at a glance is that rail's whole job), the sources list, the food page's 'published by' row, the portion sheet's provenance card, and the source page's own hero at plate scale. A source with no artwork keeps the WORDMARK FALLBACK inside the same plate, which is honest (visibly ours) rather than an approximation of a logo we do not hold. Marks still never appear on picker ROWS — a logo per row is an advertising rail." },
  { id: "nutrition-reference-intakes", area: "Core", status: "shipped", title: "Reference intakes for the label panel", detail: "The four panel fields (saturates / sugars / fibre / salt) were recorded and rolled up but had no yardstick, so a day's 24 g of saturates meant nothing on screen. referenceIntakes(kcalTarget) + panelStatus(day, kcalTarget) in @hybrid/core (pure + unit-tested) give them one. THEY ARE NOT PERSONAL TARGETS and the UI never presents them as such — the adaptive macro targets come from THIS athlete's own maintenance and bodyweight, whereas these are WHO/EFSA adult POPULATION REFERENCE INTAKES, and the Diary says so in a caption. Saturates (≤10% of energy) and sugars (≤10% of energy) SCALE with the athlete's energy target, because an athlete eating 4 000 kcal to support training is not 'over' at the same grams a sedentary adult would be; salt (≤5 g) is flat, because a big training day doesn't earn more of it. Fibre (30 g) is stored as a FLOOR, not a ceiling — the one field where more is the goal — and is never coloured as a breach. The Diary day panel now shows value / reference with a hairline bar, terracotta only once a ceiling is actually passed, and keeps the existing 'from the foods that state it' caveat: the totals are a floor by construction, so 'over' means 'already over on what we can see', never 'definitely over'. Web + mobile at parity; i18n EN/PL/DE." },
  { id: "nutrition-hub-bento", area: "Core", status: "shipped", title: "The Nutrition hub's five destinations, as a bento with a real Diary chart", detail: "The back half of the Nutrition home — Diary, Insights, Body & weight, Your meals, Your products — rendered as a flex-WRAP row of five bare buttons in mono uppercase ash at caption size: no surface, no glyph, no arrow, no press state and no data, wrapping onto a second line so the group read as a caption belonging to the block above it. Five destinations dressed as a footnote, with the one opened daily (Diary) at exactly the same weight as the one opened monthly (Your products). REPLACED by a BENTO (apps/{web,mobile}/components/aurora/nutrition-hub.tsx, one component per client, same props): Diary leads as a full-width tile, the other four follow as a 2x2 of stat tiles — each a NUMBER first and a link second (7-day average kcal, bodyweight + weekly rate, saved-meal count, saved-product count), so the block informs even when nobody taps it. The grid does the ranking flat text refused to do. FOUR NEW MONOLINE GLYPHS in NUTRITION_GLYPHS (theme/icons.ts — diary/chart/scale/box, same 72x72 stroke box as the rest, true vectors on both clients, no emoji and no PNG). THE DIARY CHART IS TWO LINES, NOT A SPARKLINE: the day's TARGET against what was actually LOGGED across the last seven days, with the gap between them shaded — a single bar per day only says how much was eaten, two lines say whether it was ENOUGH. The comparison is only interesting because the target MOVES: adaptiveTargets is training-aware, so nutritionHubSeries adds each day's OWN trainingEnergyOnDay on top of the goal target and the dashed target line steps up on hard days — the shape of the week's training is legible in the same picture as the eating. Today's point is the endpoint dot (partial by definition) and the tile always prints the raw pair ('1 840 of 2 600 kcal today'), so the exact figures never have to be read off the pixels. TWO DELIBERATE DEPARTURES FROM spark.ts, whose doctrine is a true zero baseline: (1) this chart is NOT zero-anchored — both series live in a narrow band around 2 000-3 000 kcal and a zero baseline would collapse them onto each other, killing the comparison, so the domain is fitted to both series with a tenth-of-span of padding; (2) a day with NOTHING logged BREAKS the logged line instead of drawing through zero, because 'I didn't log' and 'I ate nothing' are different claims — the target line stays continuous, so the break reads unambiguously as missing data, and a lone logged day with no neighbour is drawn as a dot rather than dropped. Series + geometry are pure and unit-tested in @hybrid/core nutrition-hub.ts (nutritionHubSeries / nutritionHubChart), so the line on the phone and the line in the browser are the same line. Mobile MEASURES the tile (onLayout) and draws in real pixels rather than stretching a fixed viewBox — react-native-svg has no dependable non-scaling-stroke, so a stretched viewBox would draw the stroke thicker across than down; web keeps the scaled viewBox, which browsers rasterize correctly. The delta chip takes the app's existing colour grammar (terracotta past the same +5% grace the hero ring uses, sand under 90%, chartreuse inside the band). The Insights tile is pinned to a SEVEN-day average and never follows the Insights dashboard's own 7/30 toggle — a tile whose number changes because of a control on another screen is a tile you can't trust. Recipes and the verified tier stay OUT of the bento: both are libraries you browse, so they keep their own full-bleed rails below it. Web + mobile at parity; i18n EN/PL/DE (15 new keys)." },
  { id: "nutrition-water", area: "Core", status: "shipped", title: "Water tracking (training-aware hydration target)", detail: "`water` had been a real SignalKind and a real NutritionDay field since the nutrition engine shipped, and NEITHER client ever wrote one or drew one — the column summed zeros forever and the day's hydration was invisible on both. This is the missing middle, built as a complete vertical: packages/core/src/engines/hydration.ts (pure + unit-tested, 25 tests) is the single source of truth, so the figure on the phone and the figure in the browser cannot disagree. THE TARGET IS COMPOSED, NOT FLAT: eight glasses is folklore — neither a published intake nor scaled to anybody — so the target is built exactly like the calorie target, a bodyweight baseline (35 ml/kg, landing a 75 kg athlete near 2.6 L, consistent with the EFSA adequate intake for total water once you account for the fact that an app can only count what is DRUNK) plus a sweat allowance of 500 ml per training hour. That is the LOW end of the ACSM replacement range (real sweat rates run 0.5-2.0 L/h) and it is low ON PURPOSE: this is a target the athlete is measured against, and a target nobody can hit is a target that gets ignored. When a caller has training KCAL but not minutes — which is every current caller, since the nutrition screens already compute trainingEnergyOnDay for the calorie bump — the minutes are derived at a documented 600 kcal/h IN THE ENGINE rather than being invented at two call sites two different ways. Target clamps to 1.5-6 L and rounds to 50 ml (35 ml/kg does not support printing '2 627 ml'). THE STATE IS TIME-OF-DAY AWARE: '1.1 of 2.6 L' is a different sentence at 09:00 than at 21:00, so expectedMl() paces the target linearly across a 07:00-22:00 waking window and 'behind' means behind FOR NOW — the same 500 ml reads on-track at 9am and behind at 7pm. States are empty | behind | on-track | met. THERE IS DELIBERATELY NO 'OVER' STATE: over-drinking is a real risk but a risk of RATE (roughly a litre an hour sustained), and a day total cannot tell 4 L across sixteen hours — unremarkable on a big training day — from 4 L in three, so colouring a day total red would be fake precision. UI (web + mobile at PARITY, aurora/water.tsx on both): a card directly under the energy hero, NOT a fourth hairline inside it — water carries no energy and has no place in the macro split, and sitting it under protein/carbs/fat would say that it does. It draws a VESSEL ROW rather than a proportional bar, because people count water in vessels ('three bottles') and because the row's LENGTH comes from the target while its FILL comes from the day, so the row does not appear to move on a training day when the target grows underneath it (hydrationVessels caps the row at 12 and shares the target across the drawn vessels). The pace is stated in PROSE ('340 ml behind for this hour') rather than marked with a tick that would need a legend. Presets are authored PER UNIT rather than converted — metric gets 250/500/750 ml, imperial gets 8/16/32 fl oz, because '8.5 fl oz' is nothing anybody has ever poured — while STORAGE is always millilitres. An ⓘ opens the basis sheet naming the 35 ml/kg + 500 ml/h rule and stating plainly that it counts what you drink (food carries roughly a fifth of total water and we cannot measure it). ⓘ is already a ring, so no second ring is drawn around it. UNDO REMOVES THE READING, NOT ITS EFFECT: the control appends one Signal per tap, so taking a tap back DELETES that Signal — appending a −250 ml reading would be a lie about the day that every downstream sum would faithfully carry. That needed a new owner-scoped DELETE /api/signals/[id] (the stream stays append-only for automatic sources; a MANUAL reading can be a mistake), plus three mobile helpers (fetchWaterLogs / logWater / deleteSignal) because querySignals deliberately drops the row id — CoreSignal is the engine's shape and has no id. Both clients write optimistically and reload on failure, so the row fills on the tap but millilitres the athlete never drank never survive a failed write. NO SCHEMA CHANGE — Signal already accepts the kind and /api/signals already validated it. i18n EN/PL/DE (13 keys)." },
  { id: "nutrition-hud", area: "Core", status: "planned", title: "Sticky \"what's left\" rail across the nutrition screens — REMOVED", detail: "RETIRED (see hero-system-sweep). The sticky HUD — four capsules (kcal left, then P/C/F grams left) that captured as you scrolled past the ring and macro cards and stayed pinned across the nutrition sub-screens — has been DELETED from both clients, along with packages/core/src/nutrition-hud.ts, its unit tests, its 12 i18n keys, and the AuroraScreen stickyTop / stickyTopReserve / onScrollY slot it was the only user of. WHY: it occupied exactly the HERO SYSTEM rail y (safeTop + 4, 44pt), so Nutrition was the single screen in the app whose head could not be the system's — two bars competing for one row. The decision was to REMOVE the HUD rather than dock it as a sub-rail: a persistent numeric rail on every sub-screen is a second head, and the rank rule says a screen has one. The budget it carried is still one tap away — the hub's calorie ring and macro card are the source, and every sub-view's back control returns there. IF IT COMES BACK it must return through HeroScreen's `rail` slot (which docks BENEATH the hero's rail rather than replacing it), never as a screen-top overlay; the capture/hysteresis/ceiling model in the old nutrition-hud.ts is worth recovering from git history if so." },
  { id: "nutrition-user-recipes", area: "Core", status: "shipped", title: "Your recipes — author, scale and log your own dishes", detail: "The largest hole in Nutrition: recipes existed only as a CURATED, READ-ONLY library (recipes.ts — editorial content with a written method and per-step timers), so a library you could not add to was a magazine. This adds the athlete's OWN recipes as a first-class object, web + mobile at parity. THE SHAPE FOLLOWS ONE RULE: a user recipe's macros are DERIVED, never typed. Nobody knows the macros of the pasta they invented; they know what went into it. So `UserRecipe` carries NO nutrition columns at all and neither client shows a macro field anywhere on the screen — you add foods and quantities and the numbers follow. packages/core/src/user-recipes.ts (pure + unit-tested, 27 tests) is the ONE place a recipe's arithmetic happens, so the total under the ingredient list, the per-serving strip and the entry that lands in the diary are the same computation rather than three. It routes through food-facts.sumFacts, which means a recipe total INHERITS the label-panel discipline the rest of nutrition obeys: a panel field is stated only when EVERY ingredient states it, `partial` names exactly which fields had to be dropped, and the clients say WHY a figure is missing instead of printing a wrong one. Per-serving figures divide through scaleFacts, so an unknown stays unknown at any serving count and a null over four servings is never a 0. AN INGREDIENT CARRIES ITS OWN NUMBERS: each line stores a SNAPSHOT of the food's facts even when productId/verifiedId link it back to a source. Two reasons pulling the same way — (1) a deleted product must not silently under-total a recipe, which is the exact failure the not-stated rule exists to prevent arriving through the back door as a confident wrong number; (2) a recipe is a record of what you MADE, and correcting a product in March did not change the food you ate in February. The link survives for provenance and for staleIngredients(), which REPORTS drift ('2 ingredients changed since you saved them') with a one-tap refreshIngredients() — visible and deliberate, never applied behind the athlete. An ingredient whose product was DELETED is orphaned, not stale: it is exactly as true as the day it was written. SERVINGS SCALE THE INGREDIENTS, not just the yield: scaleRecipeTo rewrites every quantity so a serving stays the same size (a yield changed alone would silently halve every serving), rounding to 2 dp because '133.33 g of pasta' is a false precision that makes a list look machine-generated. LOGGING writes a NORMAL food entry — per SINGLE serving with a separate qty, subnamed '1 of 4' so a two-month-old diary entry still says whether that was a quarter of the tray — so the Diary's existing stepper rescales it and a recipe never becomes a row the diary cannot edit. THE EDITOR IS THE DETAIL VIEW on both clients: no read-only recipe page with an edit button that reveals the same fields again, because a recipe is a document you keep amending ('a bit less pasta next time') and a read mode would be one navigation layer whose only job is to hide a pencil. Ingredients come from the saved-products library (the same rows that back staleness), added from a search sheet, quantity-stepped by a magnitude-following increment (0.25 under one serving, 0.5 under three, 1 above) so no ingredient needs a bespoke stepper. Both UIs live in their own aurora/user-recipes.tsx rather than inside nutrition.tsx, which is already ~3 000 lines holding sixteen views — the first step of the split that file needs. GATING mirrors meals and products exactly: free keeps up to FREE_RECIPE_LIMIT (4) via access.canSaveRecipe, deliberately the SAME number rather than a stingier one (three different free allowances for three shapes of 'a food you keep' is a rule nobody can predict, and a recipe costs more to author than a meal, not less); POST 403s upgrade_required at the cap and both clients route to the paywall instead of surfacing an error. API: GET/POST /api/nutrition/recipes + PATCH/DELETE /api/nutrition/recipes/[id], owner-scoped, sharing ONE coercion module (lib/recipe-shape.ts) so a field cannot be validated one way on create and another on edit — and where an absent panel field stays NULL rather than being coerced to 0, since a route quietly writing zeros would collapse the whole sumFacts discipline. PATCH replaces the ingredient list wholesale inside a transaction rather than diffing it (a recipe is edited as a document — lines added, removed, reordered and re-measured in one sitting), so a recipe is never briefly ingredient-less on disk. GET is soft-guarded to return an empty library on an un-migrated database, so the tables' absence costs the shelf and never the whole Nutrition screen. MIGRATION APPLIED (Aug 2026): reference/sql-nutrition-user-recipes.sql was run in the Supabase SQL Editor (idempotent; creates UserRecipe + UserRecipeIngredient with owner RLS — the ingredient policy JOINS back to its recipe rather than duplicating a userId column, so a recipe and its lines can never disagree about who they belong to). The soft-guards stay: GET still answers empty and POST still returns 503 not_migrated on a database that lacks the tables, so a future environment without the migration degrades instead of failing. i18n EN/PL/DE (27 keys)." },
  { id: "nutrition-copy-day", area: "Core", status: "shipped", title: "Copy a day (or one meal) into another day", detail: "The highest-frequency action a food diary has that this app did not: most people eat the same handful of things, and re-entering Monday's breakfast on Tuesday, Wednesday and Thursday is the tax that makes people stop logging. packages/core/src/copy-day.ts (pure + unit-tested, 21 tests) is the whole model, shared, so the copy on the phone and the copy in the browser move the same entries to the same clock times. WHAT GETS COPIED IS AN ENTRY, NOT A TOTAL: a copied day is a set of new diary ENTRIES, each with its own name, its own per-single-serving macros and its own quantity — so the copy is exactly as editable as anything typed by hand (delete one item, halve another). A copy that produced one 2 140 kcal row would be a dead end the moment the athlete skipped the banana. THE CLOCK IS PRESERVED, THE DATE IS NOT: breakfast logged at 08:14 on Monday arrives at 08:14 on Tuesday, because the diary orders by ts and a copy stamped with `now` would pile the whole day into one minute and shuffle dinner above breakfast. retimeToDay() rebuilds the timestamp from LOCAL date parts rather than adding a day-count in milliseconds — a local day is 23 or 25 hours across a DST boundary, and epoch arithmetic would shift 08:14 to 07:14 twice a year. The retiming is done on the CLIENT and the server never computes 'the same time of day tomorrow': only the client knows the athlete's timezone, and getting that wrong would scatter a copied day across the wrong dates. COPYING APPENDS, NEVER REPLACES: the athlete asked to copy, not to overwrite, so a target day that already has food keeps it — and copyDayPlan REPORTS the collision (targetEntries) so the confirm step says 'today already has 3 items' rather than the athlete discovering it afterwards. An unstated panel field stays unstated in the copy (the food did not learn its sugar content by being moved) and verifiedId provenance is carried forward. copySources() lists the last 28 days that actually hold food, newest first, each with its entry count, energy and the parts it holds, so the picker offers CONTENT rather than a bare date, and skips empty days entirely rather than listing them as dead options. UI (web + mobile at parity, aurora/copy-day.tsx on both): a quiet bare control on the DIARY's day header — the one screen where 'a day' is the subject rather than the container — opening a two-step sheet. The second step is the point: it states the plan (item count, energy, and the append warning) BEFORE writing it, computed by the SAME copyDayPlan that produces the rows, so the sentence the athlete agreed to and the entries that land cannot disagree. Part chips narrow the copy to breakfast, or lunch, or the whole day, since 'yesterday's breakfast' is the common case and should not require copying yesterday's dinner too. Selecting a part can also RE-FILE it (yesterday's dinner → today's lunch) via copyDayPlan's toPart, and copying a day onto itself is refused while moving a part within one day is allowed. API: POST /api/nutrition/log/batch, a batch rather than N calls because a twelve-item day would otherwise be twelve requests each writing up to eight Signals — slow on a phone and leaving a half-copied day behind whenever the network drops mid-run. It is deliberately NOT transactional: a FoodLog row is best-effort by design (it retries without the label panel on an un-migrated database and falls through to Signals-only if the table is absent), so a transaction would turn a partial success the diary can display into a total failure it cannot — the response reports written/failed and both clients say 'copied 9 — 2 couldn't be written' rather than claiming a clean copy. REFACTOR: the single-entry write was extracted from POST /api/nutrition/log into apps/web/lib/food-log-write.ts and is now shared by both routes, because the write is not one insert (mirrored Signals, plus the editable row, plus the two-attempt panel fallback) and a second copy of it would drift SILENTLY — a copied day whose Signals were written slightly differently would total differently from the day it was copied from. No schema change. i18n EN/PL/DE (15 keys)." },
  { id: "nutrition-analytics", area: "Core", status: "shipped", title: "Nutrition trends — every nutrient, three windows, findings in words", detail: "nutritionSummary averaged a window but read exactly TWO nutrients (kcal and protein) over ONE span and produced no sentences, so the Insights screen showed adherence and a macro split and then had nothing to say about the fibre, sugar, saturates and salt the label panel had been collecting for months. packages/core/src/nutrition-analytics.ts (pure + unit-tested, 26 tests) is the widening: nine nutrients (kcal, protein, carbs, fat, fibre, sugars, saturates, salt, water), three windows (7/30/90), per-day series, trends, and ranked findings. THE HARD PART IS NOT THE AVERAGING, IT IS THE ZEROS. A NutritionDay sums Signals and foodLogSignals writes NO Signal for a field a food never stated, so a day where nothing declared its fibre sums to fibre: 0 — indistinguishable, in the stored day, from a day that genuinely contained none. Averaging naively across thirty such days would under-report fibre by whatever share of days lacked the data, and would do it CONFIDENTLY: a real number, badly wrong, with nothing on screen to suggest it. So a panel nutrient is averaged over the days that STATE it (taken as the days whose total exceeds zero) and every stat carries statedDays beside loggedDays, so the UI says 'from 18 of 30 days'. Treating a whole-day zero as not-stated is a deliberate approximation and a safe one — a day containing food but genuinely zero salt essentially does not exist, while a day with no panel data is common. The four required macros have no such ambiguity and average over every logged day; WATER gets the same treatment as a panel field, because a day with no water logged is a day nobody told us about, not a dry day. A FINDING IS A SHAPE, NOT A SENTENCE: insights come back as typed objects with numbers ({ kind: 'protein-short', value: 22, weight }) and the clients localize them, exactly as nutritionNudge already works — core ships no English. Twelve kinds: logging-sparse, kcal-under/over/on-track, protein-short, protein-rest-gap, fiber-short, salt-high, sugar-high, trend-up/down, coverage-low. THE REST-DAY GAP is the finding the whole window exists to produce — an athlete who eats for training and forgets to on the days between — and it needs real session data, so it is ABSENT rather than guessed when no sessions are passed; it also only fires when the gap is both material in grams (>15 g) AND a real share of the target (>8%), since 6 g on a 180 g target is noise wearing a percentage. Below MIN_DAYS_FOR_INSIGHT (4) logged days the ONLY finding is logging-sparse: claiming 'your protein is 12% low' from two days is a claim about a habit built from an anecdote. A panel nutrient known for under half the logged days produces coverage-low INSTEAD of a confident average. Trends compare the first half of the stated days to the second (halves rather than a regression, because the answer must survive being explained in one sentence and checked against the chart beside it), need 8+ stated days, are flat under ±5%, and are never claimed on a 7-day window. Targets are computed WITHOUT today's training bump — this is a window average, and adding one day's session fuel to the yardstick every other day is measured against would make the comparison lean. UI (web + mobile at parity, aurora/nutrition-trends.tsx on both): FINDINGS LEAD and the per-nutrient detail follows as the evidence, because nine nutrients over ninety days is a lot of numbers and a screen that opens on numbers makes the reader do the analysis the app was supposed to do. A CEILING IS NOT A TARGET and is not coloured like one: salt/sugars/saturates are population reference MAXIMA, fibre is a reference MINIMUM, the macros are personal targets — so the accent comes from stat.kind rather than from the percentage (being at 60% of the salt reference is fine; being at 60% of the protein target is not), and getting that wrong would turn every well-eaten day amber. The day strip is BARS, not a line, because bars have an honest way to say 'no data' — no bar — where a line would either break (reading as a rendering fault at 46px) or bridge the gap (claiming an intake nobody logged); it is zero-anchored, since this is an amount. The reference-intake caveat travels with the panel group rather than being buried in a tooltip. ONE WINDOW CONTROL PER SCREEN: SummaryDashboard's own 7/30 toggle was REMOVED on both clients and it now reads the screen's window as quiet meta — two window controls offering different spans is a screen that cannot say what period it is describing. The goal overview survives because it carries what the per-nutrient view has no place for: the chosen goal and the measured 28-day weight change it is actually being judged by. No schema change. i18n EN/PL/DE (33 keys)." },
  { id: "nutrition-quick-add-nl", area: "Core", status: "shipped", title: "Quick add — logging by typing what you ate", detail: "\"+ 40g protein\". \"chicken 200g\". \"2 eggs\". The fastest logging surface any tracker has is the one with no screens in it, and the app already had every ingredient — a saved library, a recents MRU, a serving weight per product — behind three taps and a stepper. packages/core/src/quick-add.ts (pure + unit-tested, 35 tests) is a GRAMMAR, NOT A MODEL: it recognises numbers, units and a small vocabulary, and everything it cannot account for becomes the food NAME, matched against foods the athlete has ALREADY SAVED. Nothing is invented — a phrase that resolves to nothing returns `unknown` and the caller falls back to the normal picker rather than logging a plausible 400 kcal. THE GRAMMAR: a leading + is optional; a macro keyword takes the number on EITHER side (\"500 kcal\" and \"kcal 500\" are the same claim, and insisting on one order would make it feel like a syntax rather than a sentence) with a mass unit in between treated as noise (\"40 g protein\" == \"40g protein\"); several macros parse from one line; a macro-only line derives kcal at 4·4·9 and SAYS it derived them, so the day's ring always moves. Food lines read a name plus either a mass (g/kg/ml/l/oz, converted to grams) or a bare count of servings (\"2 eggs\", \"3 x whey\"). Numbers accept a DECIMAL COMMA, and the tokeniser deliberately does NOT split on commas — \"12,5\" is one number in the two locales this app ships beside English, and splitting there would read half of it; a comma used as a separator is stripped from the token instead. THE VOCABULARY IS A PARAMETER, not a literal: \"protein\" is a word and words are localised, so QuickAddVocab defaults to English (a caller with no t() in reach still works) and quickAddVocab(t) builds the athlete's own from i18n — the same shape durationUnits(t) uses. The i18n values are comma-separated WORD LISTS the parser matches against, so PL adds \"bialko/białko/b\" and DE \"eiweiss/kh\" without touching core; an untranslated key (which returns the key itself) falls back to the default rather than leaving the parser matching on \"w.recovery.…\". GRAMS ONLY CONVERT WHEN THE CONVERSION IS KNOWN: \"chicken 200g\" against a product with servingGrams 100 is two servings; against a product that never recorded a serving weight it is NOT 2 and NOT 1 — it is unanswerable, so the match comes back flagged `needsPortion` and the client opens the portion editor instead of logging a number nobody computed. Silently treating \"200g\" as one serving of an unknown weight is exactly the confident-wrong-number failure the rest of this codebase is built to avoid. Resolution searches the athlete's OWN foods only, never the network: a quick add that waited on a request would not be quick, and \"chicken\" should resolve to the chicken this athlete eats rather than to a stranger's guess. Ranking is exact > prefix > word-boundary > substring > subname, with a source tie-break putting RECENTS above products above saved meals (the food logged yesterday is a better answer to two words than one saved in March and never used) — and the score is deliberately NOT clamped to 1, because an exact match scores 1 for every candidate and clamping would erase the tie-break in precisely the case it exists to settle. Names are accent-folded including Polish ł, the same fold the verified search already applies. UI (web + mobile at parity, aurora/quick-add.tsx on both): originally one field at the TOP of the food picker, above the database search. SINCE SUPERSEDED by nutrition-picker-redesign, which merged the two: quick add is no longer a box above the search box, it is what the picker's ONE field does FIRST — the grammar still runs over every keystroke and still owns the interpretation row, and only a phrase that names a food it cannot resolve falls through to the database. Nothing described here was removed; it stopped being a second input. IT SHOWS ITS WORKING: the field renders WHAT IT UNDERSTOOD as a row you tap (the food matched, the QUANTITY computed, the macros read) and does nothing until you tap it, so \"chicken 200g\" resolving to the wrong chicken is a visible mistake rather than a wrong entry discovered three days later in the diary. Enter commits the FIRST interpretation, which is the one on screen, never a second-best the reader cannot see. A quick-added entry writes per SINGLE serving with a separate quantity, exactly as every other logging path does, so the Diary's own stepper rescales it afterwards; verified provenance is carried forward; and a macro line's unnamed PANEL fields stay NULL rather than becoming zeros. No schema change. i18n EN/PL/DE (12 keys, four of which are the parser's vocabulary)." },
  { id: "nutrition-picker-gap", area: "Core", status: "shipped", title: "Nutrition picker — what the day still owes (Concept 02)", detail: "CONCEPT 02 of reference/nutrition-picker-design-concepts.html, the other half of the spine beside One Field. THE ARGUMENT: nobody opens the food picker to search — they open it because the day still owes them something, and the picker never said what. The figure lived one screen away on the hub ring. WHAT SHIPPED: packages/core/src/nutrition-gap.ts (pure + unit-tested, 12 tests) computes the remainder once for both clients, and the picker states it at the top in the Builder One Number vocabulary — a display-weight mono figure, a quiet \"{a} of {b} logged\" line, and the macros as thin meters. FOUR RULES IT ENFORCES, each of which is a way of not lying: (1) NO TARGET, NO HEADER — nutritionGap returns null when there is no usable energy target and the clients render nothing, the same discipline the targets card follows when it asks for a weigh-in rather than presenting a population default as a personal number. (2) A MACRO NOBODY SET STAYS UNSET — per-field manual targets mean protein can be fixed while carbs keep adapting, so a macro with no target reports want: null and is skipped, never drawn as a bar at 0% (which reads as \"you have eaten none of your carbs\" when the truth is \"you have no carb target\"). (3) OVER IS A BAND, NOT A LINE — landing on 2 003 against 2 000 is arithmetic, not overeating; the 5% tolerance both clients were already applying to the hub ring now lives in core as KCAL_OVER_TOLERANCE, retiring the two local copies that held the same number under two different names (KCAL_OVER_THRESHOLD on the phone, KCAL_OVER_FACTOR in the browser) — one sum in two places is how a ring and a header end up disagreeing about whether you are over. (4) THE WORD FOLLOWS THE SIGN, THE COLOUR FOLLOWS THE BAND — 50 past a 2 000 target reads \"50 kcal over\" (it is) and is drawn calmly (it is inside the tolerance); reading the word off the tolerance flag printed \"50 kcal left\" at 2 050 logged, which the review caught before it shipped. THE ROW SIGNAL: wouldOvershoot marks a food that would take the day past target by setting its figure line in SAND — the sport/caution accent, never the alert red, because going over is a fact about the day and not an injury. It is the one thing a row cannot be read off the screen (it needs today's running total and the target as well as the food's own figure) and it is a STATEMENT, never a block: an athlete who wants the extra 400 kcal is not asking permission. AGAINST TODAY, NEVER THE SCRUBBED DIARY DAY: the picker writes to today, so the header reads todayNutrition, not the day the diary happens to be showing. WHAT IS NOT BUILT from concept 02: the re-ORDERING by gap fit. Recent is already ranked by the hour (nutrition-picker-hour) and two rankings competing for one list is exactly the drift this codebase keeps removing — what the athlete actually eats at this hour is a better answer than what arithmetic says fits. Web + mobile at parity; the component is DayGap in nutrition-kit.tsx on both (deliberately not named *Header*: the kit has one section head, ASection, and the mobile design-token ratchet exists to keep it that way). i18n EN/PL/DE (3 keys)." },
  { id: "nutrition-picker-hour", area: "Core", status: "shipped", title: "Nutrition picker — Recent, ranked by the hour (Concept 04)", detail: "CONCEPT 04 of reference/nutrition-picker-design-concepts.html, built on top of the One Field picker (nutrition-picker-redesign). THE ARGUMENT: the screenshot that started this was taken at 21:12 with the meal already set to Snacks, and the screen still opened on a blank field — asking a question it mostly knew the answer to. An athlete does not eat a database at 21:12; they eat one of about four things, and the app has watched them do it for months. WHAT SHIPPED: packages/core/src/hour-recents.ts (pure + unit-tested, 16 tests) is MEMORY, NOT PREDICTION — a count of what actually happened, ranked, with the count kept so the row can say why it jumped the queue. `recordLog` stamps an MRU entry on every log (capped at 40, oldest falling off first, because a habit is what you have been doing lately and a routine abandoned in March must not outrank one started last week); `usualAtHour` returns what was logged within ±90 minutes of now, ranked by DISTINCT DAYS first and total logs second (something eaten once a day for nine days is a stronger habit than nine helpings in one evening — which is also why the row's label says days), tie-breaking on recency and then on the caller's own MRU order. THE WINDOW IS CIRCULAR: 23:45 and 00:15 are half an hour apart, so a late-night snack does not fall out of its own habit because the clock rolled over. TWO DAYS MINIMUM, because once is a coincidence — a food that does not clear the bar simply does not qualify, the list stays in plain MRU order, and NOTHING is said (a cold start has no empty state; there is no habit to report and the screen does not invent one). THE SCREEN: Recent is drawn as two groups — 'At this hour you usually eat' (with the live clock as the head's mono meta) over the qualifying rows only, then 'Everything else' over the remainder. The head deliberately does NOT span the whole list: a recent that happens to sit in Recent is not a habit and must not be labelled as one. Each qualifying row carries its own evidence in the meta line ('9 days at this hour'), the same show-your-working discipline quick add already follows. ONE TAP: a Recent row's ⊕ now LOGS at the stored portion instead of opening the portion editor, because an MRU key is the food PLUS its serving — the entry already is the portion it was last logged at, so there is no question to answer. The portion editor stays one tap away on the row BODY for the evening the amount is different, and the picker now renders the confirmation line (the hub's) because a one-tap add is invisible unless the screen says so. THE CLOCK TICKS ONLY IN THE PICKER (a 30 s interval gated on view === 'add'), so a screen holding seventeen views does not re-render all of them for a label one of them shows. NO SCHEMA CHANGE, NO NETWORK, NO MODEL: the timestamps ride the existing per-device recents MRU (AsyncStorage / localStorage) beside the food and its serving. An entry saved before this shipped simply has no history yet and earns one as it is used. Web + mobile at parity; the same review pass also fixed web's relogRecent, which had been dropping the micros and the verified id that mobile's twin sends — invisible while it served one surface, a real divergence once the picker routed through it. i18n EN/PL/DE (3 keys: pick.usualHour, pick.atThisHour, pick.everythingElse)." },
  { id: "nutrition-picker-redesign", area: "Core", status: "shipped", title: "Nutrition add-to-meal picker — 'One Field' redesign", detail: "The add-to-meal picker (aurora/nutrition.tsx view \"add\", both clients) was rebuilt to CONCEPT 01 of the six-concept exploration in reference/nutrition-picker-design-concepts.html. THE DIAGNOSIS it fixes, from a live phone screenshot: the screen stacked SIX containers of near-identical weight (quick-add field, its mono caption, the search field, the Quick Log / Create Food pair, the four-tab pill bar, the result row's card) so nothing on it was the subject; TWO TEXT FIELDS sat 90px apart in the same shape with the same left glyph, making the athlete choose a mechanism before saying a word; the ACCENT WAS SPENT ON NAVIGATION (the loudest chartreuse object was a selected TAB); a MONO CAPTION under the quick-add field explained how to use it; the screen HAD NO FLOOR (~55% black under one result row); CREATE FOOD, used a handful of times ever, took a third of the top at the same weight as logging; and the head drew a ⌄ on the left beside the meal switcher's ⌄ — one glyph, two jobs, one row. NOTHING WAS REMOVED FROM THE SCREEN'S FUNCTION: all four sources (Recent / Favorites / Meals / Foods) stay, quick add stays, Quick Log stays, Create Food stays, the barcode stays. WHAT SHIPPED: (1) ONE FIELD. packages/core/src/food-picker.ts (pure + unit-tested, 26 tests) reads the typed text ONCE with the existing quick-add grammar and returns the picker's whole answer — `resting`, `macros`, or `matches` — which the field, the list beneath it and the database search all consume, so the three cannot disagree about what was typed. Quick add is not a second box any more; it is what the one field does FIRST, and it still SHOWS ITS WORKING (the interpretation is a row you tap, and a gram phrase against a food with no serving weight on record still routes to the portion editor rather than logging a number nobody computed). (2) RANKED ACROSS ALL FOUR SOURCES. `QuickAddCandidate.source` gained `favorite` (SOURCE_RANK recent .06 > favorite .045 > product .03 > meal 0 — gaps deliberately small enough that a better NAME match always outranks a better source), so typing searches every list at once: today you had to know which list a food was in before you could look for it, and the honest answer is usually 'the one I'm not on'. Each result carries a mono PROVENANCE tag (RECENT / FAVORITES / MEALS / FOODS) because one list mixing four sources without saying which is worse than four lists, not better. `dedupeCandidates` collapses the same food appearing in three of them (identity is the folded NAME plus the serving label, not the id — a recent is written from a product under a different id, which is exactly the duplicate). (3) THE SOURCE LINE replaces the pill bar (SourceLine in nutrition-kit.tsx on both clients): same four labels, same order, counts as mono meta on each label, selection carried by WEIGHT AND A RULE (chalk 700 + a 2px chalk underline). No border, no radius, no fill, and not a scrap of the accent — a tab is not a 'go'. It now opens on RECENT rather than Foods: at 21:12 the list you want is what you just ate, not the whole library. (4) THE NETWORK IS THE LAST RESORT. The debounced Open Food Facts search is driven by `pickerRemoteQuery`, which sends the PARSED name ('kefir 200g' asks about kefir), never fires for a macro line (there is no food in '40g protein' to look up) and never fires on one character. Results sit under their own section head below the athlete's own foods. (5) QUICK LOG + CREATE FOOD became DOOR ROWS at the end of the list (PickerDoor, both clients), taking the list's own hairline and chevron. They wear the exit rule's 32px RING, not a bare + — the concept sketch drew bare pluses, but both of these genuinely LEAVE (a sheet and a screen), and a bare + promises something that grows in place. With a query that matched nothing, the second door names it ('New food: zupa'). (6) The head's dismiss chevron became a BACK chevron, and the result row's card chrome is gone (rows sit in the content column on a hairline, like every other list in the app). (7) ONE MERGE-DRIVEN PARSER FIX, in quick-add.ts: a ONE-LETTER macro keyword now only binds when the line has no leftover NAME words. Every locale's short forms collide with something, and Polish 'w' (weglowodany) is also the commonest preposition in the language, so 'tunczyk 170 g w oleju' (tuna, 170 g, in oil) read as 170 g of carbohydrate and 680 kcal. That used to cost a wrong row under a field whose search box still worked; with ONE field it costs the only input on the screen, which is what made it worth fixing here. Multi-letter keywords are untouched ('obiad 800 kcal' is still a macro line with a name in front of it) and the terse form the abbreviations exist for still works ('40g p', '170 g w'). REVIEW FIXES in the same change: a favourite is a per-device STAR over a food that lives elsewhere, so a favourite of a saved product is resolved back to the PRODUCT when building candidates — the stored QuickFood carried no serving weight, no micros and no verified id, and since favourites outrank products, dedupeCandidates would have let that thinner copy win ('skyr 300g' dead-ending in the portion editor and logging null micros); the products-to-QuickFood mapping now carries those fields so a newly starred food is whole; web's recent candidates read the serving weight commitPortion stored rather than only the one the label implies (parity with mobile); the My Foods pantry screen shares this query state, so its database block is gated on the same condition as the fetch instead of on the raw text (a macro-looking query rendered a block that never loaded); and the 'New food: {query}' door seeds the create form with the name it quotes. WEB↔MOBILE AT PARITY, with one recorded gap: the field's barcode glyph is a live scan button on mobile and a static hint on web, because point-and-scan is a camera capability the browser side does not have (see nutrition-barcode-camera) — a TYPED barcode works on both. i18n EN/PL/DE (9 new `pick.*` keys; `qa.hint`, `qa.placeholder` and `searchPh` retired with the caption and the second field they labelled). The other five concepts remain drawn in reference/nutrition-picker-design-concepts.html; 04 (21:12) shipped next — see nutrition-picker-hour — and 02 (What's Left) after it, see nutrition-picker-gap. NOT built: 03 The Ledger and 06 The Shelf (two answers to the same question — how the list is drawn; ship one, not both), and 05 The Tray, whose premise was undercut by what shipped: it exists to spare the athlete three round trips through the diary, and one-tap logging already removed those trips, so the tray would now cost a second tap on every single-food log to save one on the rarer multi-food case." },
  { id: "nutrition-picker-consistency", area: "Mobile", status: "shipped", title: "Nutrition picker \u2014 one vocabulary with the hub (hero pass)", detail: "A phone screenshot of the Snacks picker, read against the nutrition HUB one screen away: the two surfaces show the SAME day's figures and were drawn by two people who never met. THE DIAGNOSIS, ten items. (1) THE FIGURE: the hub sets the remaining-kcal number in Archivo Black on chalk, the picker set it in mono-bold CHARTREUSE \u2014 so the largest object on a screen whose every add button is chartreuse wore the app's one 'go' colour while doing nothing. Now the hub's face, in chalk. (2) IT DID NOT ROLL: the hub's figure rolls (RollingNumber / contentTransition(.numericText)), the picker's swapped in a frame \u2014 on the ONE screen where the number visibly moves under your thumb as you log. Now it rolls. (3) OVER WAS TWO COLOURS: red on the hub, sand in the picker and on the rows. Sand wins on both, per theme/palette.ts (`red is kept strictly for risk`); the WHO/EFSA label panel keeps red, because a saturated-fat figure past a reference intake is a different claim from 'you ate more than you planned to'. (4) THE MACROS LOST THEIR MEANING: the hub gives each macro its own accent (teal / sand / violet), the picker painted all three chartreuse \u2014 three identical bars you have to read the label of. Restored via MACRO_FILL. (5) NO PROPORTION: the picker stated '0 of 2325 logged' under a hero that had just said 2325, and never drew the RATIO \u2014 which is the whole reason the hub draws a ring. The day's energy is now a meter, and that line is its readout. (6) A SIXTH METER: the 3dp/radius-2 bar here was another spelling of the shared AMeter (6dp, RADIUS.mark) \u2014 the very drift the meter ratchet exists to stop. All four tracks are AMeter now, so the three macro tracks also share a baseline in German, where a wrapped 'Kohlenhydrate' used to drop its column. (7) THE SYSTEM SEGMENTED CONTROL \u2014 PROPOSED FOR REMOVAL, THEN KEPT. This pass replaced the iOS SwiftUI Picker(.segmented) with the underline SourceLine on every platform, arguing it re-introduced a filled radiused track on a screen of type-on-the-ground, squeezed four unequal questions into equal widths, and dropped the counts the parent already computes. THE PRODUCT DECISION WENT THE OTHER WAY and the fork is restored: on iOS the four sources ARE the system segmented control, because the platform's own switch is what an iPhone user reaches for without reading it, and this is the one screen in the app where four peer collections are swapped between. The counts are the accepted price (a native segment carries labels only, so the list is the count); SourceLine remains the Android / iOS<26 form, keeps them, and kept the travelling-rule work from round two. Recorded in SourceLine's header so the swap is not attempted a third time. (8) THE HEAD WAS OFF-SYSTEM: both branches of the meal switcher drew Archivo Black at 19, a face+size on no other screen head; it takes HERO_INLINE_TITLE (F.bold, fs.subtitle) like every screen it pushes from. (9) THREE LEFT EDGES: the day header indented 4, the source line 2, the rows and doors 6, the empty lines and section heads 0. One PICKER_EDGE. (10) MAGIC NUMBERS: 44/-1 and 3dp/2 gave way to fs.stat, tracking.display and the AMeter geometry \u2014 in the hub too, so the two figures are now ONE spec. ROUND TWO \u2014 the half the first pass left undone, plus the motion. (11) BOTH HEROES DRAW ONE ROW: the hub's three macro hairlines were the last hand-rolled proportion in the pair (a 4dp pill under a coloured mono-caps label) and are now AMeter as well, so the picker and the hub differ in NOTHING but layout \u2014 stacked there, three-up here. (12) THE MEAL'S OWN TOTAL: the header answers what the DAY owes and the title answers WHICH MEAL, and nothing answered the question actually being asked at 23:08 with Snacks open \u2014 how much is already in Snacks, a figure the hub has listed per meal all along. It rides the figure row opposite the number and ROLLS, so it doubles as proof the food landed in the right part of the day; it renders only above zero, because 'Snacks 0' is not information. (13) THE FILL TRAVELS: AMeter's share now animates on durations.collapse with RollingNumber's ease-out, so the figure and the track beside it move as ONE event instead of one rolling while its twin jump-cut. Timing and not a spring, for RollCell's reason \u2014 a bar that overshoots is briefly reporting a number that is not true. No animation on mount; Reduce Motion sets the width outright. Shared, so every meter in the app gains it. (14) THE SELECTION RULE FLIES: SourceLine's 2dp rule was a per-tab border that vanished under one label and appeared under another, so the one thing the control exists to say was the one thing it never showed happening. One indicator on springs.lens (the app's named selection token, the Today pill's spring), measuring each tab so it lands on the label's true width; haptic.selection marks the commit. (15) THE SOURCE SWITCH IS A TRANSITION: Recent \u2192 Favorites replaced the whole list in a frame with the doors teleporting; it goes through useListMotion now, the same call a deleted diary entry already uses. STILL OPEN, recorded not swept: the insights card's macro-balance row (a horizontal label|track|value) and the WHO/EFSA label panel (a 3dp reference bar, a different claim) are the last two hand-rolled proportions in nutrition. ROUND THREE \u2014 THE GRID. Colour, type and motion were right and the objects were still landing wherever each block put them. A grid is three numbers and the screen had four answers to the first, two to the second and three to the third; they are constants at the top of nutrition-kit now (PICKER_EDGE / ROW_LEAD / BLOCK) so the next block written here inherits them. (16) THE OBJECT EDGE INCLUDES THE FIELD: content stands 6dp inside the 12dp gutter, and the field spanned the full column \u2014 so the one bordered box on a screen of type-on-the-ground was also the only object 6dp wider than its neighbours. It now shares its left and right verticals with the energy meter directly above it. Hairlines stay the deliberate exception (rule on 12, content on 18): a separator inset to its own content reads as a boxed group rather than a fold in the page. (17) ONE LEADING COLUMN: a food row's \u2295 is a 44dp target, a door's ring is 32 (the exit rule's size, kept) and an interpretation row used a 12dp gap where the others used 16 \u2014 three title starts in one list (78 / 66 / 74) with a visible step where the doors follow the rows. Every row reserves ROW_LEAD (= HIT_TARGET, because that is what set it) and the door's ring is CENTRED in it; width only, so a door keeps its lighter height. (18) ONE OWNER FOR THE HEAD MATTER'S GAPS: the header's 20dp bottom pad, the confirmation line's 12dp top margin and the source line's 16 were three unrelated decisions summing to a rhythm nobody chose, each block deciding how much room the NEXT one got. It is one `gap: BLOCK` stack with no outer margins on its children \u2014 the sheet rule's discipline, applied to a screen. (19) THE MEASURED GAP IS THE SEEN GAP: leading(fs.stat, 'tight') parked ~7dp of dead air above digits that have no ascenders to use it, so the 8dp declared under the rail was 20-odd on the glass and no number in the file said so. The display figure's box is its own size, as pantry's already was. (20) THE FIELD DECLARES THE TOUCH FLOOR: 13dp of padding around a 14dp line lands at ~43, under the 44 minimum, on the control the screen exists to use \u2014 exactly the failure the HIT_TARGET guard was written for, missed because that guard only polices the shared primitives. minHeight: HIT_TARGET, stated. Before/after: design/nutrition-picker-hero-before-after.html. ROUND FOUR — THE SWITCH ITSELF, and it settles item (7) for good. The picker's four sources were the LAST hand-drawn selection control in the app: an underline line with a travelling rule, one tap away from Nutrition TRENDS, which switches on the shared segmented track. Two surfaces of the same feature, two ways to change what a list shows — which is the same 'drawn by two people who never met' this whole capability exists to end, arriving through the one object nobody re-read after #423. The three objections item (7) raised were all raised against the NATIVE control, and #423 deleted that one for a structural reason (a SwiftUI Host sizes its RN box from its content once at mount, so the segment drew outside its own frame — the picker was one of the surfaces it broke on). What replaced it, ASegment → LiquidSeg, is laid out entirely by Yoga and carries NO accent (the lens is a neutral step of the text colour, never chartreuse), so two of the three objections do not apply to it at all. The third — that it drops the counts — was true and is now fixed at the SHARED level rather than by keeping a bespoke control to hold them: ASegment gained a `meta` slot per option (the section head's mono figure, inside the segment, beside its label), so 'Recent 12' is something the app's switch can say anywhere. The fourth objection nobody had made yet, equal widths against unequal words, is answered the way the system control answers it — the label SHRINKS before it truncates (adjustsFontSizeToFit, minimumFontScale 0.75), because German's 'Lebensmittel' wants ~86dp in an ~82dp quarter-track and numberOfLines={1} alone answered that with 'Lebensmit…'. Every call site gains that. `SourceLine` is `SourceSwitch` now, ~70 lines of measured-slot / Animated.spring / PanResponder-free selection machinery deleted in favour of the shared one, and the picker's list keeps the transition, the haptic and the spring it had — they are LiquidSeg's, and always were the same tokens. ONE SPACING CONSEQUENCE, stated because it is the kind of thing that gets re-derived: the underline's hairline was doing two jobs, saying which tab was selected AND giving the list its top edge, so the control and the first row touched. A track is an object, not a rule; at rest the head-matter stack now carries a BLOCK step under it and the list opens on its first row. The TYPED branch stays at 0 — it opens on an ASection, which brings its own marginTop, and adding to that would be the sum-of-unrelated-decisions this stack exists to end. AND THE GERMAN LABEL MOVED, because the equal-width form found the one place the shrink is not enough: 'Lebensmittel 28' lands EXACTLY on the 0.75 floor at 390pt and past it at 375, the smallest phone the app supports, so on that phone the label clips again by a hair. The tab is 'Produkte' in German now — the word this vocabulary already uses for the same saved list (noProductsYet), four characters shorter, and better as the mono PROVENANCE tag a ranked row carries; personalEmpty follows it so the list and its own empty state do not name the same thing twice. The general German word for food stays 'Lebensmittel' everywhere else in nutrition — this renamed ONE LIST, not a vocabulary. The alternatives, both declined: dropping the floor below 0.75 puts a label under 10pt, and teaching a segment to shed its count when it cannot fit both makes the figure disappear on exactly the phone that has least room to explain why. ROUND FIVE — ONE LEDGER, ONE LINE. Round one gave the day's energy a meter (item 5) and left it a full-width block of its own, stated as a SENTENCE — '0 of 2325 logged' — with the three macros one line below it as '0/150' three-up. Same day, same question (what is logged against what was planned), asked twice in two vocabularies, so the four figures of one day read as two unrelated readouts and the eye had to do the conversion. (21) ENERGY IS THE FIRST COLUMN OF THE MACRO ROW: kcal / protein / carbs / fat, four equal columns, every one of them `label` over `have/want` over its AMeter track — 'KCAL 0/2325  PROTEIN 0/150  CARBS 0/250  FAT 0/70'. The 'of target' sentence is retired from the screen and survives as the columns' ACCESSIBILITY label, which is the one place a sentence beats a ratio. (22) THE HEAD STACKS, and that is a WIDTH fact rather than a taste one: AMeter's own label-left/value-right readout is right at three columns and impossible at four — the widest phone leaves ~75dp a column and 'Protein 145/150' wants ~100, so the shared head would have ellipsised the label on every column carrying a three-digit target (and 'Kohlenhydrate' on all of them). The TRACK is still the shared AMeter; only the readout above it is laid out for four-up. Over stays flagged on the ENERGY alone, in sand. ROUND SIX — THE LEDGER LEAVES THE PICKER, AND IT ROLLS. Round five fixed the meal screen and left the DIARY's day card speaking a third dialect of the same four figures, one tab away: single-letter labels (P/C/F), a `g` suffix the picker drops, a hand-rolled 4dp bar instead of the shared 6dp AMeter, no energy column at all, and the day's energy split off above it as a big mono figure with a slashed target. (23) ONE COMPONENT, TWO SCREENS: the ledger is `MacroLedger` in nutrition-kit.tsx and both screens draw it — label over `have/want` over the shared meter, four-up. The two callers differ in exactly one honest way, expressed as one prop: the picker passes `onlyTargeted` because its subject is the GAP and a figure with nothing to be short of has no place in it, while the diary keeps an untargeted macro because a diary is a RECORD of the day — that column states the amount and draws NO track, never a bar at 0 %. (24) ONE ARITHMETIC: `gapFigure` and `nutritionFigures` are exported from core/nutrition-gap.ts so the diary measures through the same sum as the picker rather than re-deriving a percentage inline (its own bar used to compute `val/tgt*100` by hand). `nutritionGap` is now that function plus its one guard — a screen about the gap needs a target to have a subject, a screen about the day does not. 8 new unit tests. (25) THE DIARY'S HERO DROPS ITS SLASH: the card's figure said `1675 kcal / 2325` while the row under it stated the same target again in a different form. The target moved into the ledger's kcal column, beside the other three; the hero states the day's energy and nothing else, on fs.hero/monoBold instead of a raw 34 with a fontWeight override. (26) THE FIGURES ROLL: these are the numbers the picker exists to move — log a food and all four change under your thumb — and the block jump-cut while the hero above it, the meal total beside it and every meter fill travelled, so one event read as two. The whole `have/want` string goes through RollingNumber (only changed digits travel, the '/' is punctuation and stays put, a changed target rolls too). (27) AND ROLLINGNUMBER LEARNED A CEILING: it had no `maxFontSizeMultiplier`, so swapping a clamped `<Text>` for it silently dropped a Dynamic Type clamp the caller had — invisible on a hero with a row to itself, breaking on four columns of ~73dp. The prop is now on the shared component (undefined by default, so a hero still grows with the reader's setting) and the ledger passes FIXED_FONT_SCALE. ROUND SEVEN — THE LAST HAND-DRAWN PROPORTIONS, SPLIT ON PURPOSE. Round two recorded two and swept neither: the insights card's macro-balance row and the WHO/EFSA label panel. They are NOT the same object, and the difference is the whole decision. (28) THE MACRO-BALANCE ROW FOLDS ONTO AMeter: it was `label | 4dp track | 30dp value` on one row, three stacked — a fifth track height (4 against the shared 6), a fifth radius, a label column sized to a magic 52dp, and the label wearing its macro's accent as TYPE while the fill beside it wore the same accent (one quantity painted twice, in the one place the app had already settled: the fill carries the colour, the label is ash). It broke the two ways a hand-sized label column always breaks — 'Kohlenhydrate' in a 52dp box, and a value column wide enough for '9%' but not for what came after. AMeter puts label and value on a row ABOVE the track, so both take the width they need and all three tracks share a baseline. It stays its own block rather than joining the ledger: these are shares of ENERGY, and the ledger's form is `have/want`, which a split has no `want` for. (29) THE LABEL PANEL KEEPS ITS 3dp BAR, deliberately and permanently: it measures against WHO/EFSA population REFERENCE INTAKES, not against the athlete's targets, and it is the one figure in nutrition that goes RED (a saturated-fat figure past a reference intake is a different claim from 'you ate more than you planned to'). A figure that means something different should not look identical to one that doesn't. Do not 'finish the sweep' by taking it onto the shared meter — that is the sweep finishing the wrong way. The wizard's step bar is likewise not a labelled proportion (it is navigation state, no label, no value) and stays as it is. Nutrition now has exactly one hand-drawn proportion left, and it is the one that should be. ROUND EIGHT — THE AUDIT, AND THE TWO SURFACES ROUNDS FOUR-TO-SIX MISSED. Reading rounds 5–7 back against the code found the claim 'one form for the day's figures' was true of the three screens that had been touched and false of the two that had not — and the HUB is the most-seen of all. (30) FIVE SPELLINGS OF ONE FIGURE: the ledger wrote `118/150`, the hub's macro meters `118 / 150 g`, the hub's ring `1675 / 2325`, the Today sheet `1675 / 2325 kcal today`. `figureText` in core is the one spelling now — have/want, both halves rounded, no spaces, no unit (the unit is the label's job) — and every one of those five reads through it. Rounding at the spelling rather than the call site matters: resolveTargets takes an overridden target EXACTLY as typed, so a target of 2325.5 reached the glass in the two places that interpolated it raw. (31) THE HUB'S RING PAINTED AMBER AGAINST A TARGET NOBODY SET: it flagged over with `kcal > targets.kcal * 1.05` inline, which reduces to `kcal > 0` when there is no target — so the first food of the day turned the ring to the over-colour on an athlete who has not been given targets yet. It reads `gapFigure`'s `over` now, which reports no target as no target. Its share is `figure.pct` for the same reason; no screen in nutrition computes a percentage by hand any more, and the local KCAL_OVER_THRESHOLD alias is deleted. (32) AND THE SAME RING PRINTED A NEGATIVE REMAINDER: `targets.kcal - heroDay.kcal` with no target is `0 - 1675`, i.e. '-1675' under a CALORIES LEFT label — the confident wrong number the picker refuses to draw (DayGap renders nothing without a target). It shows an em dash, the app's own mark for a figure that does not exist yet, and RollingNumber already declines to roll a change of SHAPE. (33) THE TWO CLAIMS NOTHING WAS VERIFYING ARE NOW TESTS: figureText's spelling (5 tests, including the fractional target and the NaN floor) and the ledger's roll — that handing RollingNumber the whole `118/150` string travels only the changed digits, leaves the '/' in its own column, and swaps rather than rolls when a dash becomes a figure (4 tests in numeric-text.test.ts). 3 175 core tests. THE STANDING LESSON: a design claim stated in prose is a claim nothing enforces. If it is worth writing down here, it is worth a grep or a test that fails when it stops being true." },
  { id: "nutrition-manual-targets", area: "Core", status: "shipped", title: "Manual calorie + macro targets (per field)", detail: "Every figure in Nutrition is DERIVED — adaptiveTargets estimates maintenance from the athlete's own weight trend, applies the goal, and adds the day's training fuel. That is the right default and it is what makes this a performance tool rather than a calculator. But an athlete on a coached protocol, or one peaking for a weight class, has numbers of their own, and until now the goal picker was the only lever. packages/core/src/nutrition-targets.ts (pure + unit-tested, 23 tests) is the escape hatch. THE OVERRIDE IS PER FIELD, AND THAT IS THE WHOLE DESIGN: you can fix protein at 180 g and let calories keep adapting, because the realistic case is not 'I have four numbers' but 'my coach gave me a protein floor' or 'I hold calories flat and let the macros move'. A field that is NOT overridden is left EXACTLY as the engine computed it and is never rescaled to fit an overridden neighbour — rescaling would quietly change a number the athlete did not touch in order to satisfy one they did, and the whole point is that the athlete's arithmetic wins. AND SO THE FOUR NUMBERS CAN DISAGREE, VISIBLY: override calories to 2 000 while the adaptive macros still describe 2 460 and the figures no longer reconcile. That is a real state and it is the athlete's own doing, so targetMismatch() MEASURES it (4·4·9, explicitly fibre-free — a macro TARGET states no fibre, so the reconciliation must not let atwaterKcal's 2 kcal/g fibre term inflate it) and both clients say it in one amber line with 'both numbers are yours — nothing was changed for you'. Hiding it would leave a screen whose own numbers contradict each other with nothing to explain why; silently fixing it would undo an explicit choice; stating it is the only honest option. Tolerance is 5% so rounding never cries contradiction. TRAINING FUEL SURVIVES BY DEFAULT: a manual figure replaces the BASE and the day's bump is still added on top unless the athlete turns it off, because the app's whole thesis is eating for the work done and an override that silently discarded the bump would leave a hard day under-fuelled with nothing on screen to say why — the opt-out exists because '2 800 kcal every day, no exceptions' is also a legitimate protocol. resolveTargets() is now the SINGLE place that decides whether the bump applies (both clients call adaptiveTargets with trainingKcal: 0 and hand the bump to resolveTargets), so the adaptive and the manual path cannot add it two different ways; the bump is routed to energy and to the carbs that carry it, the same composition adaptiveTargets performs. Coercion clamps a typo rather than rendering nonsense (kcal 800–12 000, carbs ≤1 500) and accepts a decimal comma. UI (web + mobile at parity, aurora/target-sheet.tsx on both): a DOOR ROW under the goal card — the goal is what DERIVES the targets, so setting them by hand is the exception to it — reading 'Adaptive' or 'Set by you'. FOUR BOXES, AND AN EMPTY ONE MEANS SOMETHING: blank is not zero and not unset, it means that figure keeps adapting, and the PLACEHOLDER shows what the engine currently computes so the athlete sees the number they are choosing to leave alone. The fields are deliberately NOT pre-filled with the adaptive values — pre-filling would silently convert every figure into a manual one the moment anybody opened the sheet. Emptying every box IS a clear, in the client and again in the API, so the state can never be a stored override with no fields that leaves the screen claiming a manual target it does not have. NO SCHEMA CHANGE: the override rides in the existing `answers.nutrition` JSON on OnboardingState alongside the goal and the custom meal parts, with the same soft-guard (an un-migrated database accepts the call and the client keeps its local state). i18n EN/PL/DE (14 keys)." },
  { id: "nutrition-serving-units", area: "Core", status: "shipped", title: "Serving units — a serving with structure, and no migration to get it", detail: "A serving was FREE TEXT: the Create form composed '100' + 'gram' into the string '100 g' and threw the structure away. Nothing downstream could scale it, convert it or compare two foods measured differently, and servingGrams — the field that makes per-100 g comparison and gram-based quick-add possible — sat null on almost every product because the old rule only set it when the unit was literally 'gram' (so an OUNCE serving never got a weight even though ounces convert exactly). The unit list was also a local array in ONE client, so the other never offered units at all. packages/core/src/serving-units.ts (pure + unit-tested, 26 tests) is the shared model: a 14-unit registry (mass g/kg/oz/lb, volume ml/l/floz/cup/tbsp/tsp, counts piece/slice/scoop/serving), alias resolution for every spelling people type AND the ones the old form wrote, parse/format, conversion, and derived weight. IT SHIPS WITHOUT A SCHEMA CHANGE, and that is the design: parseServing('100 g') RECOVERS { qty: 100, unit: 'g' } from text written before any of this existed, so every product already saved becomes measurable today rather than only the ones created after a column landed. A label with no structure ('1 medium', 'a handful') parses to a COUNT carrying its own word in `freeUnit` rather than being flattened to 'serving' — '1 medium' and '1 serving' are different claims, and flattening would lose the only description the athlete gave. GRAMS FROM MASS ARE EXACT; GRAMS FROM VOLUME ARE AN ASSUMPTION: 8 oz is 226.8 g and always will be, but a cup of water and a cup of honey differ by 40%, so servingGrams() returns { grams, assumed } and flags every water-density conversion. Returning a bare number would let a cup of flour be logged as 237 g with nothing on screen admitting the guess. Both clients therefore store ONLY exact conversions into the measured field — writing a guessed weight there would make it indistinguishable from one somebody weighed — and derive assumed ones at read time where they can be labelled. convertServing REFUSES mass ⇄ volume outright: that needs a density this app does not have and must not invent. compatibleUnits offers nothing for a count, because '1 slice' is not 28 of anything. composeServingLabel is the WRITE half of parseServing, so what the form saves is exactly what the parser reads back — asserted by a round-trip test rather than by two hand-written string builders agreeing. THE IMMEDIATE WIN is quick-add: 'chicken 200g' now resolves against every product saved as '100 g' years ago, because the weight is recovered from the label instead of requiring a servingGrams nobody ever typed; the same recovery feeds per-100 g comparison and recipe ingredient scaling. UI (web + mobile at parity): the unit picker now offers all fourteen from the shared registry instead of five on one client and none on the other. i18n is deliberately MINIMAL — a mass or volume SYMBOL (g, kg, oz, ml, cup, tbsp) is the same token in EN/PL/DE and comes from the registry, so only the four COUNT words are translated; the old gram/ml/oz keys are retired. i18n EN/PL/DE (5 keys, 3 retired)." },
  { id: "nutrition-portions", area: "Core", status: "shipped", title: "Custom portions — log the weight you measured, or the whole pack", detail: "The portion editor counted SERVINGS and nothing else. A food saved the way a label is written — Kefir, 100 g, 50 kcal — could therefore be logged as 1 serving, 1.5 or 2, and none of those is what anybody eats: you weigh 35 g of cheese on the kitchen scale, and you drink the whole 400 ml bottle. The old control answered both with the same shrug, and asking an athlete to divide by the serving in their head before they can log breakfast is the tracker doing arithmetic AT the user instead of FOR them. THE FIX IS THAT THE NUMBER HAS A UNIT. packages/core/src/portion.ts (pure + unit-tested, 30 tests) returns the units THIS food can honestly be logged in, each carrying servingsPer — how many servings one of it is — so every unit converts through one multiplication and the diary keeps storing exactly what it stored before: per-serving macros with a quantity. 35 g of a 100 g serving is qty 0.35; the whole bottle is qty 4. Nothing downstream changed shape. THE MEASURE FOLLOWS THE SERVING'S KIND AND NEVER CROSSES: a mass serving is measured in grams, a volume serving in millilitres, and grams are never offered for a food sold in millilitres — that conversion needs a density this app does not have, the same line serving-units.ts already draws when it flags a volume→gram conversion `assumed`. A STORED weight beats a derived one (so '1 scoop' with a measured 30 g becomes measurable), and a bare count with no weight on record gets no measure at all rather than a control that cannot work. MOST OF THIS NEEDED NO MIGRATION: the measure is read off the serving label already stored, so every food saved years ago becomes weighable the moment the app updates. A FOOD'S PORTIONS COME FROM WHEREVER THEY ARE CHEAPEST, and this is the part the first cut got backwards. It stored ONE pack per food and made the athlete type it — fine for the four foods anybody has patience for, hopeless across a supermarket, because there are millions of products and one of you. FoodProduct.portions (jsonb: [{label, size, source}], size in the serving's OWN measure) is a LIST from four sources, tried in order of how little the athlete has to do. (1) CATALOG: Open Food Facts records the net quantity printed on every product it holds, and the app was not even asking for the field — OFF_FIELDS now requests quantity/product_quantity/product_quantity_unit, so a searched food arrives already knowing its pack, before it has even been saved. A MULTIPACK yields TWO portions ('6 x 250 ml' → the 250 ml somebody drinks AND the 1.5 l somebody bought), and a pack stated in the wrong measure for the food is DROPPED rather than converted — mass⇄volume needs a density this app does not have, and OFF's data is not clean enough to assume one. `cl` joined the serving-unit registry for this: half of Europe labels a drink in centilitres. (2) SCANNED: a barcode identifies one specific PACKAGE, so a scanned hit's portions are re-sourced 'scanned' and outrank the same figure found by typing a name. (3) LEARNED: usualAmounts() reads the athlete's OWN diary — the amounts they actually enter, three times or more, matched exactly rather than clustered (35 g and 36 g are two weighings, not one rounded thing) — and offers them as prefills in the portion editor. This is the answer for the deli counter and the homemade loaf, which no catalog has ever heard of, and it costs nothing because FoodLog.amount already records what was entered. A learned amount is deliberately NOT promoted to a named portion: its name is its size, and inventing a word would put a label in the database the athlete never chose. (4) TYPED: the Create form, and 'remember as the whole pack' in the editor for a food already saved — a PATCH (PATCH /api/nutrition/products/[id], new), never delete-and-recreate, because a new id breaks every recipe ingredient pointing at the old one. `source` rides on each portion so they never blur: dedupePortions collapses duplicates keeping the best-sourced copy (typed > scanned > catalog), which matters because a scan and the catalog entry behind it ARE the same bottle and would otherwise draw two chips. PortionSource has exactly those three — 'learned' sat in the union for one commit with nothing able to write it, which is a value the type invites somebody to set by accident, so it went. HYBRID VERIFIED FEEDS THE SAME PIPE: the curated catalog already recorded a `packSize` string ('450 g') that was a display line on the product page and nothing else — the identical oversight to OFF's unrequested field, one tier up — and verifiedFoodToHit now parses it through the SAME reader, so a curated item and a community one produce identical portions. The picker's quick-add match routes back through the saved PRODUCT when the match is one: the ranked candidate is a projection carrying macros and a serving weight but not portions or a row id, so opening the editor from it would have dropped the bottle unit and the remember control on the picker's own primary path. WHAT IS NOT A SOURCE: a category guess ('cheese ≈ 30 g a slice'). Every rule in this codebase points the other way, and a wrong portion is worse than a missing one because nothing on screen admits it guessed — gaps stay honestly empty. The unit switch scrolls (up to MAX_FOOD_PORTIONS=5 named portions + servings + the measure) and each portion chip states its own size, so choosing between 'bottle' and 'block' is not a memory test. A portion unit is keyed by WHAT IT IS (`portion:<label>|<size>`) rather than by its index, because the list re-sorts by size whenever a portion is added and an index would silently re-point the athlete's selection at a different portion. The selected portion also states WHERE ITS SIZE CAME FROM under the stepper — a 400 g the food database published must be distinguishable from one the athlete set, or there is no way to know whether it is worth correcting. packSize/packLabel are KEPT and folded in at read time (foodPortions) so every bottle recorded under the first cut survives; nothing writes them any more. THE STEPPER STEPS ON THE UNIT'S OWN GRID BUT DOES NOT SNAP TO IT: 35 g +5 is 40 g, and 0.35 servings +0.5 is 0.85 — rounding an off-grid amount onto the grid would silently discard a weight somebody measured, which is the whole point of the feature. A PORTION IS THE ONE EXCEPTION, and it was missing (fixed Aug 2026): a named container is COUNTED, not weighed, so an off-grid amount of it is never something anybody measured — it is what a unit switch left behind. Switching a 100 g serving to a 400 g bottle carries the portion rather than the number (right: it must not silently change what is about to be logged) and lands on 0.25 bottles; with no snap the grid stayed offset by a quarter for good — 0.25, 0.75, 1.25 — so ONE WHOLE BOTTLE was not reachable by pressing + at all, and the athlete had to select the field and retype the number the chip they had just pressed already named. It snaps in the DIRECTION OF TRAVEL rather than to the nearest, because rounding 0.25 + 0.5 lands on 1 and skips the 0.5 being stepped towards. Found by building the interaction as a clickable simulation rather than by reading it: the flow reads fine in the source and is unusable in the hand, which is the class of defect a diff review cannot catch. The Diary row's stepper was fixed to the same rule, since it used to round every press to the nearest half and would have destroyed a 0.35 entry on the first tap. The unit switch only draws when the food HAS a choice (a single chip reading 'servings' decides nothing), switching units keeps the PORTION rather than the number (1 serving becomes 100 g, not 1 g), and a quiet line under the stepper says what the amount is in the food's other terms. The pack rides in the Recent MRU too, so re-logging a recent still offers the bottle. THE DIARY ROW SAYS WHAT WAS ENTERED. A diary entry stores per-serving macros and a QUANTITY — the right shape for every engine downstream and the wrong thing to show a person, because 35 g of a 100 g food read '0.35' in the day's list: correct, and not the number the athlete typed. FoodLog.amount + .amountUnit now record the portion AS ENTERED beside it, so the row reads '35 g — 123 kcal' and the stepper counts GRAMS (five at a press) or BOTTLES (half at a press) rather than servings. `qty` remains the source of truth for every total; the amount is a record kept in step because a quantity edit rescales it by the same ratio (rescaleLoggedAmount) rather than re-deriving it — a re-derivation could disagree with the original, and then the row and the total would describe one meal two ways. amountUnit stores a SYMBOL ('g', 'ml' — the same token in all three languages), the athlete's own container word ('bottle', already in their language because they typed it), or one of two CANONICAL tokens ('serving', 'pack') localized at read time: storing a translated word would leave last month's entries speaking a language the athlete has since switched away from. An entry with no amount — logged before this shipped, or a quick macro line — reads and steps exactly as it always did. A copied day carries the amount through (copy-day.ts), since a copied 35 g of cheese is still 35 g. writeFoodLog now falls through THREE tiers (with amount → without amount → macros only) instead of two, so a database missing only the amount columns does not silently lose its label panel as well. QUICK ADD READS THE SAME MEASURE. Its unit table used to normalize EVERY unit to grams — a millilitre became a gram before any food had been consulted — so 'kefir 400ml' silently logged 400 g at water density against a food sold by weight, and could not be answered at all for a food sold by volume (servingGrams is deliberately never stored for one). It now carries the MEASURE beside the factor and converts through portionMeasure, the same reader the editor's unit switch is built from: a mass typed at a volume food (or the reverse) comes back needsPortion and opens the editor rather than inventing a density. Volume foods became quick-addable in the same change, and 'cl' with them. A SAVED FOOD CAN BE EDITED. PATCH /api/nutrition/products/[id] now takes the name, subname, serving, serving weight, macros, label panel and portions — validated by the SAME readers the create route uses, so an edit cannot write a figure a create would have refused — and the mobile Create form doubles as the edit form (a second form would drift from the first, which is why the inline builder was deleted in the de-clutter pass). Reached from the portion sheet, which is where you find out a food is wrong. The free cap does not apply: it gates how many foods you keep, not whether you may fix one. SUPERSEDED by nutrition-forget (Aug 2026): the edit form used to carry the catalog and scanned portions through untouched and rewrite only the typed one, on the argument that this form is where the athlete states what they know rather than where they overwrite what the database published. Right about authorship, wrong about the screen — a pack you cannot SEE is a pack you cannot correct, and the form now shows every pack the food has, each labelled with its source. The late-migration columns go in a second update attempt, so a database missing `portions` can still rename a food. THE HABIT REACHES THE ⊕. usualLogPortion turns a learned amount into a quantity, so a pantry row for a food weighed the same way three times or more reads 'usually 35 g' and its ⊕ logs THAT — not a serving the athlete has never once eaten. The row states it because the button acts on it; a tap that logged 35 g under a line reading '100 g' would be the screen disagreeing with itself. A food with no habit, or one whose measure has moved out from under it, falls back to the one serving it always logged. Mobile only, per the mobile-first rule; the API and the engine serve it. i18n EN/PL/DE (22 keys under w.recovery.nutrition.pt.*). SCHEMA, three files, all idempotent and all safe to run early: reference/sql-food-pack.sql (FoodProduct.packSize/packLabel — superseded but still read), reference/sql-food-log-amount.sql (FoodLog.amount/amountUnit) and reference/sql-food-portions.sql (FoodProduct.portions), mirrored by prisma/migrations/20260815120000_food_pack, 20260815130000_food_log_amount and 20260815140000_food_portions. Grams and millilitres need NEITHER — the measure is read off the serving label already stored, so the weighing half works on an unmigrated database." },
  { id: "nutrition-file-split", area: "Core", status: "shipped", title: "Split nutrition.tsx — the vocabulary becomes importable", detail: "nutrition.tsx was 3 685 lines on web and 3 381 on mobile, holding seventeen views plus every primitive those views draw with. It was named in the Aug 2026 nutrition audit as the real scalability risk on the feature: every capability added to it made the next one harder, and five of the last six nutrition features had to start OUTSIDE it for that reason. STEP ONE, DONE: everything that was ALREADY a standalone component — a pure presentational piece with explicit props, sharing the file only because that is where it was first written — moved into three modules per client. aurora/nutrition-kit.tsx (the shared vocabulary: Glyph, the stroke-icon set, VerifiedMark, MarkPlate, SourceMarkView, FactsPanel, FoodRow, the QuickFood type + its MRU storage, presetGlyph, and the full-bleed railScroller/shelfScroller idiom); aurora/recipe-library.tsx (the CURATED, read-only shelves: RecipesLibrary, CollectionRail, RecipeShelf, RecipeTile, RecipeCard, CookPlate, RecipeCollectionScreen, RecipeDetail — deliberately a separate module from aurora/user-recipes.tsx, because a RECIPES entry is editorial content with a written method and a user recipe is an arithmetic object, and putting them in one file would keep implying they share a model); aurora/nutrition-panels.tsx (SummaryDashboard, OnboardingGoal, the coach nudge, the calorie Ring, WeightTrend, CDivider, and the count-up hook). WHY THIS IS NOT COSMETIC: a primitive nobody can import is a primitive that gets re-drawn the next time a screen needs it — which is exactly how this repo produced five differently-sized rail tails, two copies of the serving-unit list and a unit picker that existed on ONE client. FoodRow, FactsPanel and the mark plate are now importable, so the next nutrition surface inherits them. ONE RENAME, forced by the move: the local `NutritionNudge` component collided with core's `NutritionNudge` TYPE. TypeScript tolerated that inside one file (a type and a value may share a name); across a module boundary it is a real collision, so the component became `NutritionNudgeLine` — which is what it always was, one line. VERIFIED AS BEHAVIOUR-PRESERVING, not just as compiling: every one of the 80 moved top-level declarations was diffed against its pre-split text and is byte-identical apart from the `export` keyword, and a full line-diff of both screens shows every ADDED line is an import statement or the single rename — everything else is pure removal. Web 3 685 → 2 809, mobile 3 381 → 2 752; 1 679 lines now live in six importable modules. Core tests (2 870), both typechecks, the web build and the iOS bundle export are green. WHAT REMAINS is the harder half and is tracked separately: the ~2 750-line component that is left holds the seventeen VIEWS, which share roughly a hundred pieces of state. Splitting those needs either prop-drilling (which for several views means forty-plus props and is worse than the monolith) or a context, and a context changes re-render behaviour — so it is a deliberate second step with its own verification, not something to bolt onto a move that is provably inert." },
  { id: "recipes-list-pass", area: "Mobile", status: "shipped", title: "The recipes list — the shelf head, the tail, the tile, and one search", detail: "A design pass on the library root, all of it against rules this repo had already written down and then drifted from. (1) THE SHELF HEAD WAS AN EXIT. Each collection's head was a pressable row reading '3 RECIPES →' — the exact pattern aurora/rail-tail.tsx exists to retire ('every rail used to carry a See all up in its section head … it spends the head's right slot, which per the SectionHead standard is for the section's META'), and it put the door at the TOP-LEFT of a rail you read left-to-right, so the way in was behind you by the time you ran out of cards. The Nutrition HUB's own recipes rail has ended in a RailTail since it shipped, so one screen disagreed with another about the same object. The head is now the shared ASection (title + the count as meta, not pressable) and the way in is the chromeless tail at the end of the scroller, at the tile's own width so the new snap grid cannot half-cut it. (2) THE SAME SENTENCE, TWICE. 'Build a dish once from real foods — the macros work themselves out' rendered as the empty state of Your recipes AND as the door row's subtitle, sixty points apart on a first-run screen. A door row says where it goes; the explanation belongs where the emptiness is. The door keeps a subtitle only in the state the section cannot explain — the free cap, which now says what it is. (3) THREE SHELVES, THREE HEAD ANATOMIES → one: your recipes, the saved shelf and the curated collections all render ASection now. (4) THE SEARCH READ ONE THIRD OF THE SCREEN. The field filtered the curated library only, while the screen lists three kinds of recipe — so typing 'pasta' with a pasta recipe of your own answered 'nothing in the library matches'. One query now reads your own recipes (name and ingredients, the same two fields core reads on a curated one), the saved shelf and the library; the personal shelves drop their resting chrome while it runs (no door row among results, no 'you haven't written one yet' under a search that simply missed); and a miss that misses EVERYTHING is the shared Empty, not one grey line. The field is ASearch rather than a bare AField, which is where the CLEAR button lives — the affordance that matters most when a query returns nothing, and the one this screen lacked. (5) THE TILE COULD NOT SHOW WHAT THE SHELF SORTS ON: it stated time and energy while the rail above it offered HIGH PROTEIN as a collection. It states protein now, beside the energy in a real flex row with a gap (no middot, per the house rule), and a dish that clears the bar prints that figure in the accent. A KEPT dish also wears a bookmark, which the shelves could not say at all — saving shipped before the tile knew about it, so a recipe you had kept looked exactly like one you had not on the one screen listing both. (6) The shelves snap like every other rail in the app. i18n EN/PL/DE." },
  { id: "recipe-high-protein-derived", area: "Core", status: "shipped", title: "The High protein shelf is derived from the numbers, not typed", detail: "`Recipe.highProtein` was a hand-typed boolean and had drifted exactly as a hand-typed classification does: LENTIL STEW (24 g of protein, 20% of its energy) sat OFF the shelf while SHAKSHUKA (22 g, 21%) sat on it. Two dishes, the same numbers, different shelves, and nothing in the file could say why — which makes shelf membership a fact about who typed the entry rather than about what is in the bowl. `isHighProtein(recipe)` replaces the field, and the doctrine is the PANTRY'S applied to a dish (pantry.ts foodRole: a food's shelf comes from its own numbers). TWO TERMS, because either alone misfiles something real: GRAMS (HIGH_PROTEIN_MIN_G 20), because a share test alone would admit a 90 kcal snack carrying nine; and SHARE OF ENERGY (HIGH_PROTEIN_MIN_SHARE 0.2), computed at 4 kcal/g on the protein itself rather than against the stated kcal — which carries fibre and rounding no macro accounts for — because a 620 kcal bowl containing 25 g is a big meal that happens to have some. On the shipped library the rule keeps all five dishes the flag had and ADDS the lentil stew, the one the numbers always said belonged there. Every reader goes through the function (the filter, the collection, the cover's meta line, the tile, the card's chip, global search), so the shelf and the badge can never disagree again. Moving the bar is now a two-constant edit with a test that states the resulting membership by name." },
  { id: "nutrition-recipe-share", area: "Mobile", status: "shipped", title: "Share a recipe — the Liquid Glass control at the rail's trailing edge", detail: "Every recipes screen can now send what it is showing. THE CONTROL IS THE BACK BUTTON'S TWIN, and that is the whole design: the cover scaffold's rail has always drawn a 40pt interactive-glass circle at its LEADING edge on every screen in the app (HeroNav), and its trailing edge held metadata and nothing else — so a screen with something to DO there had no shared way to say it, and the alternative was a screen drawing its own glass circle, which is the exact drift the native-leaf rule in design-tokens.test.ts exists to stop. `HeroAction` (aurora/hero.tsx) is that twin: the same HERO.nav geometry (40pt visual, 44pt hit, Apple's minimum), the same `glassEffect(regular, interactive)` where Liquid Glass renders, the same white-12% + blur + hairline fallback everywhere else, and it takes either an `onPress` (it acts) or `items` (it PRESENTS — the system's own SwiftUI Menu on iOS 26, zoom-morphing out of the circle's glass; the shared AnchoredMenu card off it, exactly as the feed's ⋯ does). CoverScreen gains a `trailing` slot; the metadata keeps its slot and moves inboard 44pt, because the count is what the cover SAYS and the control is what you can DO — folding them into one capsule would make '8 RECIPES' look tappable. WHAT GETS SENT IS THE RECIPE, NOT A LINK, and that decision is forced rather than stylistic: there is no apple-app-site-association in the repo and no associatedDomains in app.json, and the web client was retired in Aug 2026 — so hybrid.app/app?… has no handler on either side and a bare URL reaches somebody who cannot open it. `recipeShareText` in @hybrid/core renders one shared RecipeShareView (title, meta, the per-serving macro line, the scaled ingredients, the numbered method, the credit, then the link) and BOTH kinds of recipe project onto it, so a shared dish reads the same whoever wrote it. Quantities follow the serving stepper — set it to 4 and the message says 8 eggs and 800 g of tomatoes, through the same formatIngredient the screen uses. The meta line joins on a spaced en dash, never a middot (house rule, and there is a test asserting it across every recipe). The library and a collection share themselves the same way (the library link is `?recipes=1`, now read by the Nutrition route so it LANDS on the library rather than the hub). A recipe you authored shares as TEXT with no link at all — a private dish has no public address — from the same renderer, with its DERIVED per-serving numbers. i18n EN/PL/DE. VERIFY ON DEVICE: that the trailing circle reads as the same glass as the back button, and that the menu morphs out of it — this sandbox cannot render Liquid Glass." },
  { id: "nutrition-recipe-share-card", area: "Mobile", status: "shipped", title: "Share a recipe as a CARD (image)", detail: "The answer to a link that cannot be opened. The app already captured a React view to a PNG and handed it to the share sheet — that is how the workout card and the Wrapped stories ship (react-native-view-shot + expo-sharing in lib/share.tsx) — and Nutrition used none of it. `RecipeShareCard` is the recipe's own COVER compressed: the same duotone wash off `recipe.tint`, the same ghost dish mark, the title, the note, and the three figures the detail hem leads with, over the wordmark. It reads the recipe's own tint and mark rather than choosing its own, so it cannot drift from the screen it was shared from. The card mounts OFF-SCREEN only while a capture is in flight (view-shot needs the view laid out and drawn, not visible) and is unmounted after. The capture helper was renamed `shareWorkout` → `shareCardImage` in the same change: a helper named after one of its two callers is how the second one ends up copying it." },
  { id: "nutrition-recipe-save", area: "Mobile", status: "shipped", title: "Save a library recipe — your own shelf of the curated ones", detail: "The library was read-only in the strongest sense: the only way to KEEP a dish was 'Create meal', which flattens it to per-serve macros (recipeToMeal) and drops the ingredients and the method. Saving is now a set of ids the device holds (core owns the shape, the storage key and the pruning rule; lib/recipe-saved.ts is the AsyncStorage copy and nothing else), surfaced as a full-bleed shelf on the library root between YOUR recipes and the curated collections — what you wrote, what you kept, then what we wrote. Hidden while a search is running, for the same reason your own shelf is: the screen is answering a question about the curated library and a shelf ignoring the query would read as a result that matched. An id from an older build is PRUNED on read, so a recipe that no longer exists leaves a shorter shelf and never a blank row. WHY THE DEVICE AND NOT A TABLE: the saved thing is editorial content that ships inside the binary — there is no row to own it, no privacy to re-check at read time and nothing to resolve — so an id list is the whole feature, and the cost (it does not follow you to a second device) is a deliberate trade against a migration this sandbox cannot run. WHY IT IS NOT A COPY: saving and copying are different jobs and both now exist — this keeps the curated dish AS the curated dish, with its editorial macros intact, while recipe-copy-to-mine hands you an editable version of it." },
  { id: "recipe-copy-to-mine", area: "Core", status: "shipped", title: "Copy a library recipe into your own, editable — and a model that can say \"I don't know\"", detail: "'Make it mine' on a curated recipe: it lands in Your recipes as an editable document at the serving count you were looking at. THE BLOCKER WAS THE MODEL, NOT THE FEATURE, and fixing it is most of this entry. A user recipe's macros are DERIVED from each ingredient's own snapshot, and NutritionFacts states kcal/protein/carbs/fat as REQUIRED numbers — while a curated recipe carries per-SERVE macros an editor typed and no per-ingredient figures at all. So the copy could previously only fill seven lines with zeros and hand back a recipe reading '0 kcal' out of real food: the exact confident-wrong-number failure the derived-macros rule exists to prevent. WIDENING THE FOUR COLUMNS TO NULL WAS THE OTHER OPTION AND WAS REJECTED — they are read by every engine, total and screen in the app, and 'a macro might be null anywhere' is an enormous blast radius next to one boolean that says 'these four are not a measurement'. `UserRecipeIngredient.unstated` is that boolean. MIGRATION APPLIED (Aug 2026): reference/sql-recipe-ingredient-unstated.sql was run in the Supabase SQL Editor — idempotent, one column, DEFAULT false, so every existing row stays a measurement. The soft guards stay anyway (POST and PATCH both answer not_migrated rather than 500-ing), so a future environment without the column degrades instead of failing. WHAT READS IT: recipeTotals leaves an unstated line OUT of the sum (never adds its zeros) and returns the NAMES of what it could not count, so the totals present as a FLOOR — 'Per serving, so far' — with the missing lines listed by name rather than as a count you would have to hunt for; canLogRecipe refuses the diary, because an entry that under-reports what was eaten is invisible to everything downstream; the editor row drops its stepper (stepping a quantity whose numbers are unknown changes nothing readable) and offers Link a food instead, through the SAME picker sheet the add flow uses, and linkIngredient keeps the recipe's own name for the line while taking the food's serving, snapshot and provenance. THE MATCHER (packages/core/src/recipe-match.ts, pure, 20 tests) is deliberately dumb: exact normalised names and exact unit arithmetic in the line's own dimension — mass to mass, volume to volume, count to count — with a MEASURED serving weight as the one bridge. Every ambiguity resolves to NO MATCH: two candidates matching equally well (picking one is a coin toss with a confident number attached), an overlap rather than an identity ('tomato purée' for 'chopped tomatoes'), a conversion servingGrams had to ASSUME (a volume at water density is a guess about the food), a line measured in something nobody can size ('1 handful'). No fuzzy distance and no synonym table: a near-miss threshold is a dial someone tunes until the demo looks good, and every notch buys matches with wrong numbers. A wrong match is worse than no match now that no match is representable. The METHOD does not come across — UserRecipe holds foods and quantities, not prose, and inventing a text column for one feature would fork the model. Web API: recipe-shape.ts carries the flag (true only when the client says so), and PATCH gained the soft-guard POST already had, so a database without the column reports not_migrated instead of 500-ing the editor." },
  { id: "nav-action-recipes-plus", area: "Mobile", status: "shipped", title: "The recipes library's own verb in the detached circle", detail: "The bottom bar's detached circle is contextual by contract (@hybrid/core nav-bar.ts): Train everywhere, Add post on the feed, Find on the add-to-meal picker — and now NEW on the recipes library, a bare plus that opens the recipe editor. `screen`-kind, because the editor is a VIEW inside the Nutrition screen rather than a route: the native trigger is marked `disabled` (which still emits tabPress) and the press comes back through runNavAction, the pattern nav-action-picker-search proved. THE GATE TRAVELS WITH IT — the press runs the same access.canSaveRecipe check the shelf's own door row runs and routes to the paywall at the free cap, because a circle that walked past it would be a way to hit a 403 with no explanation attached. The door row STAYS: it is the discoverable path with the explanation attached, and the circle is the thumb-reachable shortcut for somebody who already knows. THE LIBRARY ROOT ONLY, and there is a test for it: on a curated collection or a recipe detail a plus would read as 'add to this', which is not what it does. The label is 'New', measured rather than chosen — nav-bar.ts already records that 'Find a food' truncated to 'Find a f…' under the 56pt circle, and 'Add recipe' is ten characters; the glyph carries the verb and the surface carries the object. Off iOS 26 there is no detached slot and the trigger degrades to a trailing tab that swaps glyph and label, which is the same correct degradation the rest of the bar has." },
  { id: "recipe-cook-timers", area: "Mobile", status: "shipped", title: "Cook mode — the step timers RUN, and the screen stays awake", detail: "Two gaps in an already-shipped feature, both found by reading it rather than by a report. (1) `RecipeStep.timerSec` is documented in core as 'surfaced as a tappable chip' and rendered as TEXT on both clients — a stopwatch glyph, '8:00', and nothing behind it; Shakshuka carries three. A number that looks like a control and answers no press teaches you the app's affordances are decorative. `CookStepTimer` (aurora/cook-timer.tsx) is that chip, running: THE CLOCK IS A DEADLINE, not a counter — it stores `endsAt` and derives the remainder from the wall clock every tick, so a dropped frame or a minute spent in another app cannot make the pan cook longer than the recipe says, and the interval only decides how often the label redraws. The chip's own fill IS the progress bar. It schedules a local notification for the same instant (the rest timer's exact pattern, through the one global presentation handler in lib/push.ts) so the cue arrives with the phone face-down on the counter, cancelled on stop, on step change and on unmount — 'your onions are done' an hour after you ate is the failure that cancellation is copied from. (2) expo-keep-awake was installed and imported in exactly ONE file (the live logger). The cook step-through — wet hands, a phone on the counter, a method you are halfway through — let the display time out. `CookAwake` is a component rather than a hook call, because the cook view is one branch of a component that renders sixteen and a mount is the only way to scope an effect to a branch. i18n EN/PL/DE. NOT CHANGED: the recipe DETAIL's method list still states each step's duration as text — there it is information about the recipe, not a control; the cook view is the hands-free surface." },
  { id: "recipe-shopping-list", area: "Core", status: "planned", title: "A shopping list from a recipe", detail: "There is no shopping list anywhere in the repo, and the two halves of one already exist: the recipe knows what it needs at the serving count on screen (formatIngredient scales it), and the Pantry knows what the athlete keeps (pantry.ts). 'Add to list' would join them — merging quantities across several recipes, ticking off what the Pantry already holds. It is the feature the category's leaders treat as the REASON a recipe library exists, and it is genuinely large: new persistence, a new screen, and unit merging across ingredients written in g, ml, tbsp and 'nothing' (the model's unit is a free string). Deliberately not smuggled into the share/add change. What shipped in the meantime is 'Share ingredients' on the recipe menu — the scaled ingredient list alone, through the share sheet, where every OS offers Copy. It is a SHARE and not a clipboard write because expo-clipboard is a native module this repo may only add through `npx expo install` (the one-set rule in CLAUDE.md) and api.expo.dev is blocked in the sandbox that wrote this." },
  { id: "recipe-import-link", area: "Backend", status: "planned", title: "Import a recipe from a link", detail: "Paste a URL and land in the editor with the ingredients and the method pre-filled, every quantity still yours to correct. Authoring is entirely manual today — ingredients added one at a time from your saved foods — which is the ceiling on how many recipes anybody actually keeps, and the importer is the single feature that lifts it in the apps this one is competing with. Server work: a parser route reading the page's structured recipe metadata (JSON-LD Recipe), an allowlist, a size cap, and honest failure states for a page that carries no structured recipe at all. ITS HARDEST PART IS ALREADY BUILT: matching an ingredient LINE to a food that states its numbers, and representing the unmatched honestly, shipped with recipe-copy-to-mine — packages/core/src/recipe-match.ts plus UserRecipeIngredient.unstated. What is left here is genuinely the fetching and the parsing." },
  { id: "nutrition-recipe-cook-log", area: "Core", status: "shipped", title: "Log a cooked recipe to the day", detail: "A curated recipe now goes into the diary, from the two places you finish with it: 'Log a serving' on the recipe DETAIL (paired with 'Create a meal' as the two secondaries under Start cooking) and as the accent CTA on the COOK flow's last step, where 'Finish' demotes to a ghost exit — the end of cooking is the moment you eat it, but cooking for the family and logging nothing is a real ending too. The draft comes from libraryRecipeToLog(recipe, qty, serves) in @hybrid/core/user-recipes.ts (unit-tested), which sits beside recipeToLog so ALL recipe→diary logic has one home and one subname convention. Two deliberate decisions: (1) it logs ONE serving, not the tray — the detail's serves stepper scales the INGREDIENT list (how much you're making), a different number from how much you ate, and Recipe.macros is already per-serve; the serves count only names the portion ('1 of 4'). (2) satFat/sugar/fibre/salt stay NOT STATED rather than becoming zeros, because a curated recipe states four macros and no more. It writes an ordinary food entry through the same logEntry path a typed food takes (per single serving + a separate qty), so the Diary's own stepper rescales it afterwards and the same energyIntake/protein/carbs/fat Signals land, attributed to the current meal part. Both clients at parity. (recipeMacrosForServes stays for whole-tray display; the log deliberately does not use it.)" },
  { id: "nutrition-barcode-camera", area: "Core", status: "blocked", title: "Live camera barcode scan (point-and-scan)", detail: "The camera was the ONLY missing half of the barcode flow: resolving a code to a food, the not-found path, and creating the food from there have all shipped for months, and a typed or pasted barcode already works on both clients. apps/mobile/components/aurora/barcode-scan.tsx adds the camera (expo-camera CameraView) and hands whatever it reads to the SAME searchFoods(code, { barcode: true }) call the typed path uses, so a scanned code and a typed one cannot resolve differently. The code also lands in the search box, so a barcode the database has never heard of leaves the athlete holding it and able to create the food from there rather than re-reading the pack. IT FIRES ONCE: a scanner emits the same code many times a second while the label is in frame, so a ref latches the first read and the scanner is dead until the sheet reopens — without it that is dozens of identical lookups and a sheet closing and reopening under the thumb. The barcode format list is RESTRICTED to what a food package carries (ean13/ean8/upc_a/upc_e/code128), which is not tidiness: an unrestricted scanner reads QR codes off anything in frame and reports them as barcodes. PERMISSION IS A STATE, NOT AN ERROR — three screens (not asked, denied, granted), and a denied permission is never re-prompted in a loop because iOS ignores the second ask and would leave a button that visibly does nothing; the typed fallback is named in every state. ONE PERMISSION STRING, and finding that took a prebuild: iOS has exactly one NSCameraUsageDescription and expo-image-picker was already claiming it, so the athlete would have been asked for the camera with a sentence about PROGRESS PHOTOS while the app was actually trying to scan a barcode. Both plugins now declare the same combined string, so plugin order cannot change what the athlete is told. NO WEB TWIN, deliberately: expo-camera is a native module and the browser equivalent (getUserMedia plus a decoder) is a different implementation with different permissions and different failure modes — the parity rule is satisfied by this registry entry rather than by a stub, and web keeps the typed/pasted barcode it already had. BLOCKED ON DEVICE VERIFICATION: the sandbox has no simulator and no camera, so what IS verified is the JS bundle (expo export), the TypeScript, and — the part that could have broken the release pipeline — `expo prebuild` generating the native project with the plugin and the corrected Info.plist string. What is NOT verified is a real scan: point the next TestFlight build at a food package and confirm the code resolves, the permission prompt reads correctly, and the not-found path offers Create Food. This is the first native dependency added to the app in this workstream, so watch the first `Mobile — build & TestFlight` run for it. WATCHING IT WAS RIGHT: the first build carrying expo-camera, 1.0.0 (82223058), was dead on arrival — every launch aborted in dyld with `Symbol not found: ExpoModulesCore.Record.from(dictionary:appContext:)`, referenced from ExpoCamera and missing from the ExpoModulesCore the app linked. The camera code was not at fault; the VERSION was. `expo@56.0.8` pins `expo-camera: ~56.0.7`, the dependency was declared `~56.0.8` (npm-latest, what `pnpm add` picks and `expo install` does not), and camera 56.0.8 was published against expo-modules-core 56.0.16 — the release that added that method to the `Record` protocol — while the app linked 56.0.14. Nothing catches that at build time: each Expo module is its own framework, and the core it was compiled against is only checked when dyld resolves symbols, on the phone, before the first frame. Fixed by aligning the whole Expo set to `expo@56.0.19`'s `bundledNativeModules.json` (core 56.0.23), and guarded from here by apps/mobile/lib/expo-alignment.test.ts.", blockedBy: "A device check on the next TestFlight build (the one after 82223058, which could not launch at all): point the scanner at a food package and confirm the code resolves, the permission prompt reads about barcodes rather than progress photos, and an unknown code offers Create Food." },
  { id: "nutrition-label-scan", area: "Core", status: "blocked", title: "AI nutrition-label scan (Full)", detail: "The 'Add a meal' sheet has a Full-only 📷 Scan label action on BOTH clients: web uses a file input (accept=image/*, camera capture on mobile browsers), mobile uses expo-image-picker (base64). The photo is sent to a new server route /api/nutrition/scan which asks Claude (vision, claude-opus-4-8, server-side only) to read the per-serving macros and returns {name, kcal, protein, carbs, fat}; the sheet flips to the Macros tab and prefills the fields for a one-tap add. Auth + rate-limited (15/min/IP) + Full-gated server-side (entitlementOf, 403 → client routes to upgrade) so the paid feature can't be called directly. NEEDS: ANTHROPIC_API_KEY in the deployment env — until then the endpoint returns 503 {configured:false} and the button shows an error toast. (Distinct from the barcode/food-DB approach in nutrition-fooddb; this is a vision-LLM read of the label, no food database required.)", blockedBy: "ANTHROPIC_API_KEY in the Vercel/server env (same key the AI coach + agents use). No other dependency — the UI, upload, gating and endpoint all ship." },
  { id: "profile-volume-stats", area: "Core", status: "shipped", title: "Profile — total sessions + lifetime tonnage", detail: "The Profile/You screen (web profile.tsx + mobile profile.tsx, at parity) shows a second hairline stat strip beneath the HPI/Streak/PRs spec strip: total SESSIONS (sessions.length) and LIFETIME TONNAGE (sum of sessionVolume(s.blocks) across every logged session, formatted via fmtTonnage to the athlete's units — tonnes for kg, total lb for lb). Both degrade to '—' with no data (no fabricated numbers)." },
  { id: "profile-overview-thisweek", area: "Core", status: "shipped", title: "Profile Overview — 'This week' snapshot", detail: "The Overview tab leads with a current-focus band: sessions logged + tonnage moved in the last 7 days (computed from sessions' startedAt + sessionVolume). It sits above the lifetime highlight tiles and is deliberately distinct from both those tiles (lifetime windows) and the 26-week Activity heatmap (daily dots), so it adds signal without duplicating either. Hidden when there are no sessions this week (no demotivating zeros). Web + mobile at parity. i18n (en/pl/de) w.account.profile.ov-tw." },
  { id: "settings-drill-in", area: "Core", status: "shipped", title: "Settings — search + drill-in sub-pages + value summaries", detail: "The account/settings hub (web account-settings.tsx + mobile aurora/settings.tsx, at parity) was rebuilt from an accordion into an Apple/iOS-style structure on BOTH clients: (1) a SEARCH box filters every category by title/subtitle/keywords via the shared matchSettings() helper (@hybrid/core settings-nav.ts, which now carries per-category `keywords`); (2) DRILL-IN sub-pages — tapping a category opens a focused sub-view with a back button instead of expanding inline (mobile keeps its dedicated routes for social/logger/coaching/subscription; the rest drill into the shared renderBody); (3) each category ROW shows a live CURRENT-VALUE summary on the right (e.g. Preferences → 'Aurora – EN – kg', Notifications → '3/4', Subscription → 'Full'); (4) the shared groups were reorganized/renamed (General – Visibility & reach – Account & billing – Data & danger zone). The per-category control bodies + all read/write logic (Supabase auth, useAccountSettings, logger prefs, billing) are unchanged — only the shell + IA changed. New i18n (search/no-results/back, en/pl/de). BENTO PASS (both clients, at parity): the grouped LIST-of-rows was replaced with a BENTO TILE GRID — each SETTINGS_CATEGORY renders as a rounded tap-target tile (tinted icon chip + title + live value summary/subtitle) in a 2-column grid under the existing group headers, with a full-width wide tile for odd-count groups and the red danger treatment preserved; search, drill-in sub-pages, the FREE/FULL membership pill and all useAccountSettings wiring are unchanged (web account-settings.tsx CSS grid + mobile aurora/settings.tsx flexWrap). PARITY/FIDELITY HARDENING: web↔mobile audited 1:1 and against the design mock — the web TONE was unified to the mobile multi-hue chip palette (it was lime-forward, and the `${accent}24` lime chip-tint was silently invalid because LIME is a CSS var not hex) — since SUPERSEDED: both clients' TONE were later collapsed to a single neutral ash chip (red kept only for danger) per the one-accent discipline, see design-system-unification. The profile hero anatomy (bordered card, rounded-square initial avatar, name, secondary line, role/provider pills, membership pill) was made identical on both clients; mobile membership + title copy moved to i18n." },
  { id: "weekly-recap", area: "Core", status: "shipped", title: "Weekly recap (retention loop)", detail: "recap.ts: weeklyRecap rolls the last 7 days into one summary — sessions, tonnage, sets, minutes, active days, distinct lifts, total cardio DISTANCE (km), the week's PRs (best per lift via pr-detection) AND cardio PRs (distance/pace), the top trained muscle (volumeByMuscle), and deltas vs the prior 7-day window. The 'Your week' cards (web + mobile) show weekly distance + a 🏃 cardio-PR chip when there's running. Pure + 5 unit tests. Drives the 'Your week' card on web + mobile Today and a branded, shareable recap image on mobile — giving 'come home and review' a weekly beat and a natural social moment. The same engine file also exports weekAdherence() — a glanceable Mon→Sun strip (done / today / missed / future per day + a done/target count) that powers the collapsible week-adherence card in the Today PLAN section on both clients (it sits above the detailed reconciled 'This week', which stays expanded)." },
  { id: "pr-detection", area: "Core", status: "shipped", title: "Personal-record detection + muscle volume", detail: "records.ts: pure session-analysis helpers. PRs — bestE1rmMap (all-time best per lift), newPrsInSession (records set in a session vs everything done before it, first-time lifts included, ranked by improvement) and prsForSession (PRs for one session pulled from the full history by date) — power the post-workout PR celebration + share line and the session-detail / History PR badges. CARDIO PRs too: newCardioPrsInSession / cardioPrsForSession detect a DISTANCE PR (furthest ever for a cardio move) or a PACE PR (beat your best pace among prior runs of that move that were this distance or SHORTER, so a quick short jog can't fake a long-run record) — surfaced as a 🏃 celebration on finish, a share line, and a cardio-PR banner/badge on web + mobile session detail. volumeByMuscle attributes each strength set's tonnage to every muscle the movement trains (via the MOVEMENTS map) for the 'what did this session work?' breakdown on web + mobile. exerciseHistory lists every exercise the athlete has logged (most-recent-first, with counts) to power the live workout's fast picker. Pure + 10 unit tests." },
  { id: "workout-summary-slides", area: "Core", status: "shipped", title: "Shareable workout-summary slides + fun comparisons (2 'wrapped' styles)", detail: "The post-workout 'Workout complete' screen is a swipeable carousel of slides — Overview (name + minutes/sets/volume), then Spotify-Wrapped-style single-stat SPOTLIGHT slides (Time trained, Total load — one giant hero number each), PRs & bests, Muscle focus (volumeByMuscle), and a Hevy-style Fun comparison — floating as the hero object of the Liquid-Field finish screen (see finish-liquid-field). ALL slides ship on every client (web, responsive/mobile web, native mobile); on web the carousel captures horizontal swipes (touch-action: pan-x) and the page dots are tappable so every slide is reachable on a phone browser, not just by swipe. Each slide is rendered as the REAL full 9:16 Instagram-story card right on screen (what-you-see-is-what-you-share), not a compressed preview box, and TAPPING the card cycles between 2 looks before sharing (the old segmented style toggle folded into the card itself — see finish-liquid-field): Liquid Glass (a BORDERLESS frosted translucent slab over a lime×blue blur — the old rim stroke read as a 'weird' floating box, so it was dropped) is now the DEFAULT (DEFAULT_STORY_STYLE in story-styles.ts, so both clients open in Liquid Glass), with Aurora (signature dark + lime glow) the alternate. (A third 'SwiftUI' share style + its native iOS renderer were REMOVED — the cross-platform card is the one source of truth.) Every card's footer signs off with the HYBRID LOGO — the 'Tracked with HYBRID.' line renders the trailing brand as the real wordmark (display type + lime accent dot) on all three renderers (web canvas, web DOM preview, mobile RN), locale-safe (the brand mark is terminal in EN/PL/DE so the dot never lands mid-sentence). The styles live in ONE shared source of truth — packages/core/src/story-styles.ts (STORY_STYLES / storyStyle()): renderer-agnostic looks (base bg, optional diagonal gradient, soft glow discs positioned as fractions, optional translucent glass panel, text/accent/bar colours) so web and mobile paint the SAME card. The model also supports gradients (canvas linear-gradient on web; expo-linear-gradient on mobile) so new styles are a one-entry addition. The single Share button exports whichever slide is on screen, in the chosen style, as a 9:16 (1080×1920) PNG (mobile: the on-screen SlideStoryCard captured via react-native-view-shot + expo-sharing; web: the <canvas> painters drawSlideStory(slide, …, styleId) → navigator.share, downloads/text as fallbacks). Style names localized EN/PL/DE (summary.style.*). comparisons.ts is the pure, i18n-safe fun-fact engine: workoutFunFact/sessionFunFact map a session's volume (kg), reps, or distance to a playful tiered equivalent ('over a tonne — about a small hippo'), choosing the most impressive metric so it varies naturally; copy lives in i18n (en/pl/de). Built on the existing stats engines (sessionVolume, newPrsInSession, volumeByMuscle). Web + mobile parity; unit-tested; web build + iOS bundle export green." },
  { id: "finish-liquid-field", area: "Core", status: "shipped", title: "Finish workout — Liquid Field redesign", detail: "The post-workout summary was redesigned on BOTH clients around one principle: celebrate first, decide second, one action. The 9:16 story card now FLOATS in a Liquid Field — an intensified pocket of the ambient Aurora field (web: .ff-a/.ff-b radial glow discs drifting on the shared lgDrift keyframes over .lg-field, with the Kyoto-Hour washi retint and prefers-reduced-motion honoured; mobile: FinishField — two oversized lime/teal glow orbs faked from concentric translucent circles with a slow Animated drift, Reduce Motion parks it, KYOTO HOUR swaps to the warm washi tones). The five stacked near-equal CTAs collapsed into ONE pill cluster: the lime Share pill (the only filled action, milestone-aware label + glow — 'Share your first workout' / 'Share your PR' / 'Share your win') flanked by two glass satellite orbs — ★ Routine (expands the save-as-routine composer in place via a new startOpen prop on SaveRoutine/SaveRoutineCard) and → Analysis (history) — plus a glass ✕ top-left as the one exit ('Done — back to Today'; guests exit to welcome, keeping their sign-up path below the cluster). The segmented STYLE TOGGLE is gone: tapping the card cycles the wrapped look (selection haptic on mobile; role=button + Enter/Space on web), with a one-line mono hint naming the current look ('LIQUID GLASS — TAP TO CHANGE THE LOOK', summary.cardHint). The old hero header (badge + 'Workout complete' + name) was removed — the card IS the headline (it already carries the title and the 'First workout 🎉' treatment); the optional rename + private note stay as quiet dashed pills under the hint; pending-sync and guest states unchanged. The entrance spring + success haptics/vibration now land on the floating card. Dead keys removed across EN/PL/DE (summary.styleLabel/digDetail/shareNudge/complete/firstTitle/firstSub + w.train.logger.firstDone/sessionComplete/shareStory); new keys summary.cardHint/orbRoutine/orbAnalysis." },
  { id: "session-pr-reveal", area: "Core", status: "shipped", title: "PR reveal on the individual-workout page (celebrate first)", detail: "The individual-workout page (web SessionDetail + mobile /session/[id]) opened on a flat '🏆 N new personal records' banner — a receipt, not a moment. It now LEADS with an animated PR REVEAL when a session set a record: ONE headline record lands as a hero (the trophy springs in, a lime sweep crosses once on web, the est-1RM/distance/pace counts up from zero, then rests on the exact value), with 'Share your win' as the payoff CTA wired to the existing story share. Which record headlines is chosen by a NEW shared pure model — packages/core/src/session-celebration.ts (sessionCelebration(prs, cardioPrs) → the heaviest strength lift leads, headlined by the WEIGHT ACTUALLY MOVED (see session-summary-heaviest); a cardio distance/pace PR only headlines a PR-but-no-lift session; carries firstEver + the total PR count so the hero reads 'first ever' / '+Δ' / 'furthest yet' / 'fastest yet' and lists 'and N more' below) so web + mobile headline the SAME record with the SAME copy. Count-up reuses the shared statCountUp (same core as the summary carousel + mobile CountUpText); web uses CSS keyframes (prTrophyPop/prSweep/prRise, all prefers-reduced-motion-guarded → static card) and mobile an Animated spring. When a session set >1 record the full PR list (🏆 strength + 🏃 cardio) stays in a quiet card beneath the hero. New i18n EN/PL/DE (summary.prOne/firstEver/furthestYet/fastestYet). Unit-tested (session-celebration.test.ts). FIDELITY PASS: the reveal also fires a rotating gold ray halo + a deterministic confetti burst (web CSS keyframes prRaysSpin/prConfetti + module-level fan; mobile Animated rotate loop + LinearGradient halo + Animated confetti fan), both prefers-reduced-motion-guarded. Web + mobile typecheck green." },
  { id: "session-wrapped-flow", area: "Core", status: "shipped", title: "Workout-page Wrapped + multi-story share picker", detail: "The individual-workout page's PR reveal (session-pr-reveal) now opens a full 'Wrapped' TAKEOVER — the reference/ prototype (reference/pr-wrapped-flow.html) built for real on both clients (web components/aurora/workout-wrapped.tsx overlay + mobile components/workout-wrapped.tsx Modal). Two steps: (1) WRAPPED — a hero (session title + the PR headline or total tonnage) over a free BASICS strip (sets / reps / volume / time / distance) then a premium ANALYTICS panel GATED behind isFullAccess(usePersona()): est-1RM, e1RM trend (gain from first-logged to this session, coloured up/down), the session's top muscle by tonnage, and logged readiness — REAL derived data only, no fabricated power/energy/percentile; free users see the panel obscured (web blur / mobile dim) under a ✦ upsell using the admin-set premium accent (web VIOLET / mobile usePremiumAccent) that routes to /upgrade. (2) SHARE — the multi-story picker: swipeable 9:16 cards (overview / time / load / PRs / muscle / fun) that TAP-cycle the STORY_STYLES look, reusing the SAME primitives as the finish carousel (web StoryCard — extracted from logger.tsx into components/aurora/story-card.tsx so both screens share one card — + shareWorkoutSlide; mobile the already-shared SlideStoryCard + shareWorkout capture). The reveal's 'Share your win' opens the takeover; a no-PR session gets a '✦ See your Wrapped' entry so every workout can reach it. CLEANUP (post-TestFlight review): the pre-existing OLD share UI on the workout page was retired so the Wrapped is the single share surface — mobile dropped the inline WorkoutShareCard + its 'Share your win' button, and web repointed its header share pill at the Wrapped (removing the old shareWorkoutStory path); the page no longer shows two competing share buttons. PAGE-AS-EXPERIENCE (mobile, post-TestFlight): the individual session screen (apps/mobile/app/session/[id].tsx) was rebuilt to BE the reference prototype 1:1 — opening a session renders full-screen: the PR reveal panel (trophy spring + rays + confetti + count-up, when there's a record) → the premium WRAPPED panels you SCROLL through (snapToOffsets: hero 'your workout, wrapped' + big number + basics; premium 'What this set really did' facts; 'Where you stand' Top N% + the session-signature ribbon) with a sticky share dock (dots + 'Share this win') → the slide-up story SHEET ('Choose your story' + swipeable SlideStoryCards + dots). The set breakdown + archive/delete ride along as a trailing 'The session' details section. New i18n session.wrapped.scroll/premiumLead/signatureCap + session.theSession. WEB PARITY PENDING: web session-detail is embedded in the app-shell History view (not a full route) and still uses the tap-to-open Wrapped overlay — bringing the same full-screen scroll experience to web is the immediate follow-up (tracked under parity-followups). The panel data is one shared pure model — packages/core/src/session-wrapped.ts sessionWrapped(session, all, {units, bw}) → basics[] + facts[] (each present only when computable, honesty rule inherited from done-receipt), unit-tested (session-wrapped.test.ts). The Wrapped hero also draws the SESSION SIGNATURE — a per-set effort ribbon from the shared pure sessionSignature() (RPE → load-relative → neutral, deterministic, unit-tested), gated on SIGNATURE_MIN_BARS so a 1-set session doesn't draw two sticks. New i18n EN/PL/DE (session.wrapped.*). Both clients typecheck; web build + iOS bundle export green. PROTOTYPE-FIDELITY GAPS still open (tracked as session-wrapped-prototype-gaps): the 'Top X%' percentile panel (needs sex/age + a population dataset — see benchmarks.ts synthetic priors / exercise-analytics-rankings) and the 4 bespoke story-card DESIGNS (Trophy / Signature / Wrapped-stat / Minimal) — the picker currently reuses the shared 6-slide story system in 2 looks rather than a fabricated percentile or a second story renderer." },
  { id: "session-wrapped-web", area: "Core", status: "shipped", title: "Web parity — individual session as the full-screen Wrapped experience", detail: "DONE — web now matches mobile 1:1. apps/web/components/aurora/workout-wrapped.tsx was rebuilt from the earlier tap-to-open overlay into a FULL-SCREEN, vertically-scrolling takeover (fixed inset-0, scroll-snap-type y): opening a session renders the same panels as the mobile prototype (reference/pr-wrapped-flow.html) — the PR REVEAL (trophy pop + rotating gold rays + confetti + count-up via the shared prTrophyPop/prRise/prRaysSpin/prConfetti keyframes, when there's a record) → the premium WRAPPED panels you scroll (hero '✦ your workout, wrapped' + session title + big number + basics grid + the sessionSignature ribbon; premium 'What this set really did' facts, blurred behind a ✦ upsell for free users; 'Where you stand' Top N% + signature) → a sticky bottom share DOCK (page dots + 'Share this win') → the slide-up story SHEET ('Choose your story' + the shared StoryCard pager + dots). apps/web/components/session-detail.tsx was rewired: it computes the charts (Muscle focus + e1RM/pace trends) + the per-exercise breakdown + the archive/delete manage row into a trailing `details` node and returns <WorkoutWrapped … details={details}/> — so the workout page IS the experience on web too, not an overlay grafted onto the old page. Reuses the SAME shared pure core as mobile (sessionWrapped / sessionSignature / sessionCelebration) so the two clients can't drift; the Full-gated premium facts inherit the honesty rule (real derived data only). Web typecheck + build green." },
  { id: "session-summary-heaviest", area: "Core", status: "shipped", title: "Workout summary headlines the weight lifted, not the estimated 1RM", detail: "Follow-through on #231, which retired e1RM as the headline across the EXERCISES surfaces but left every SESSION-SUMMARY surface still leading with it — so a deadlift session topping out at 100 kg was celebrated as '120 kg', a number the athlete never lifted. Fixed at the source rather than per-screen: PrHit (engines/records.ts) now carries `topLoad` + `previousTopLoad` (the heaviest ACTUAL working load, bodyweight-aware via the existing blockTopLoad/topLoadMap primitives #231 added) ALONGSIDE the e1rm/previous pair. e1RM still DETECTS records and is deliberately untouched there — a rep PR (100 kg x 5 -> 100 kg x 8) is a genuine record that no weight comparison would ever find — it just no longer DISPLAYS as 'your best'. Celebration (session-celebration.ts) gained topLoad/previousTopLoad plus a `repPr` flag and now picks the headline by heaviest actual lift (e1RM only breaks ties), so the record chosen is the one whose number goes on screen. New shared formatter formatStrengthPr (engines/session.ts, mirroring formatCardioPr) renders the three honest shapes -- 'Deadlift 100 kg (first!)' / 'Bench 82 kg (+6 kg)' / 'Pull-up 88 kg (more reps)' -- replacing four duplicated client prLine copies, so the wording and delta math can't drift. RETIRED at parity on web + mobile: the Wrapped reveal + hero big number, the per-exercise breakdown badge ('120 kg e1RM' -> '100 kg'), the PR list lines, the post-workout summary, the story-share slides/cards (ShareBest.e1rm -> .weight, sourced from blockTopLoad), the weekly-recap share line, and the Cockpit 'your week' PR rows (which would otherwise have contradicted the summary for the SAME PR). e1RM deliberately KEPT where it is labelled as such and honest: the premium 'Est. 1RM' Wrapped fact, the labelled e1RM trend charts, %e1RM intensity zones, rep-max matrix, load-reps isolines, Team Compare, sport-engine prescription, and the 'Where you stand' percentile (the benchmark norms are built on e1RM, so it keeps an e1RM input on purpose). i18n EN/PL/DE: new summary.morePrReps + summary.heaviest, and summary.todaysBests no longer promises '(est. 1RM)'. PARITY SWEEP (follow-up): the summary surfaces were audited pair-by-pair and the shared logic hoisted into core so the two clients cannot drift again — strengthPrDelta (the first/+X kg/more-reps branch, previously hand-written in SIX places across both clients; it also formats through fmtWeight, fixing a float artifact where a 0.1-rounded topLoad subtraction printed +4.799999999999997 in the Cockpit rows) and workoutShareCaption (the share blurb, which on web was hardcoded ENGLISH and headlined the session's heaviest lift rather than the record actually set — both clients now emit the same localized caption and headline the SAME record the reveal hero showed, taken from the celebration rather than prs[0], which is ordered by e1RM gain and could name a different lift). Drifts closed at the same time: web session-detail's cardio PR line and readiness label were hardcoded English; the post-workout PR row used w.train.logger.firstTime on web vs summary.firstTime on mobile (different copy in all three locales); the PR-slide headline pluralized on web only (now pluralized on all four summary surfaces); web's cardio PR row showed '1.5 km / faster pace' where mobile used the sport's natural unit with a delta (both now formatCardioPr); the story row list truncated at 7 on web vs 6 on mobile; web's canvas card hardcoded 'TODAY'S BESTS' so it never picked up the new '(heaviest)' qualifier; and its overview slide used 'MIN' + w.train.logger.* labels vs mobile's summary.* ones. The coach weekly-recap rows — the same PrHit data — still headlined e1RM AND differed between clients (web showed a 'first' label, mobile omitted it); both now use formatStrengthPr, so coach, cockpit and the session summary agree on one number for one PR. The unused summary.heaviest key added in the first pass was removed. A re-audit then caught two things the sweep had half-done: the story-card label fix had been applied to web's DOM preview but NOT to the canvas painter that produces the actual shared PNG — breaking that file's own 'the preview is exactly what we export' contract and leaving the exported image's minutes cell hardcoded English — and the two Wrapped surfaces still rendered cardio PR rows as raw km (a 400 m swim PR read 'Swimming 0.4 km', with no delta) while the two post-workout surfaces used formatCardioPr; all four now share the one formatter. KNOWN + DELIBERATELY LEFT: the Cockpit/coach PR rows hardcode kg (identical on both clients, so not a parity break, but a lb athlete sees kg there — needs units plumbed into those components), and apps/web/lib/workout-share.ts still carries the legacy drawStoryCard/shareWorkoutStory painter, which has had no callers since the Wrapped became the single share surface — dead, but left in place rather than removed as drive-by scope. GUARD (packages/core/src/e1rm-headline.test.ts): #231 had to be fixed twice — first across the Exercises surfaces, then again for the whole workout summary — because nothing stopped a new screen from reaching for the best-e1RM helpers and labelling the result 'best'. Every client reference to blockBestE1rm / bestE1rmByLift / bestE1rmMap is now listed with a reason, and a NEW one fails the core suite with a message naming the topLoad replacement (blockTopLoad / bestTopLoadByLift / topLoadMap); a second assertion deletes stale entries so the list can't rot. Verified in both directions — it fires on an injected regression and on a stale path, and passes when clean. e1rmSeries is deliberately unguarded (a time series, always plotted under an explicit 'e1RM trend' label). e1RM itself is untouched and still ships wherever it's LABELLED as an estimate: the premium Est. 1RM fact, the trend charts, %e1RM zones, the rep-max matrix, velocity profiles and the relative-strength percentile — the formulas (Epley/Brzycki/Lombardi) are valid across their rep range and are NOT capped. NEW GAP the guard surfaced, recorded rather than silently changed: apps/web/app/api/social/posts/route.ts publishes an athlete's PR post as bestE1rmByLift, and core profileStats().topLifts does the same for the public profile (rendered unlabelled as '{n} kg' in apps/web/components/social-profile.tsx) — so a 250 kg x 10 deadlift is announced socially as 333 kg while that athlete's own Profile PRs grid (topLoadMap) says 250 kg. Not a drop-in swap: both write a PERSISTED { lift, e1rm } shape (Post.data JSON + ProfileStats), so converting needs a read path that still renders already-stored posts. DETECTION now matches display: newPrsInSession fires a record on EITHER basis — a heavier top load than ever, or a better estimated 1RM. Previously detection ran on e1RM alone while the display had moved to top load, so a heaviest-ever lift that followed a high-rep block set no record at all: 80 kg x 15 (e1RM 120) then 100 kg x 5 (e1RM 117) is the heaviest that athlete has ever squatted and produced no trophy, no PR row and no Cockpit entry. The rep PR (100x5 -> 100x8) still fires exactly as before — it qualifies on the e1RM basis — so nothing that used to be a record stopped being one; the change only ADDS the load basis. PrHit.e1rm is now documented as 'this session's best e1RM', NOT necessarily greater than previous, since a load-only hit can carry a lower estimate than the athlete's prior best. Ordering changed to heaviest-first across newPrsInSession and weeklyRecap, matching the basis sessionCelebration picks the hero by — so prs[0] is now the same record the trophy shows (they could previously disagree, which is how a share caption ended up naming a different lift than the trophy above it). The DERIVED social feed PR chips (social.ts, computed live from sessions) were converted to top load in the same pass — they had rendered an unlabelled '{e1rm} kg'; the PERSISTED post route is now converted too, with an expand-and-fall-back read rather than a rename: apps/web/app/api/social/posts/route.ts writes { lift, topLoad, e1rm } (topLoad is the headline, e1rm retained so the estimate isn't lost and nothing expecting the old shape breaks), and the new shared reader social.ts prPostFigure() prefers topLoad, falling back to a LABELLED '{n} kg e1RM' for rows written before #231 — those can't be backfilled, since an estimate can't be reversed into a bar weight, so they stay honest instead of being passed off as a weight the athlete lifted. ProfileStats.topLifts is DERIVED (computed per request from sessions, never stored) so it moved straight to bestTopLoadByLift with the field renamed e1rm → topLoad, and its one consumer (apps/web/components/social-profile.tsx) renders the real weight; mobile never surfaced top lifts, so there is no mobile counterpart to update (a pre-existing gap, not a new drift). Four migration tests pin BOTH stored shapes and were verified to fail against a naive rename that drops the legacy branch. Net effect: an athlete's public profile and shared PR posts now announce the same number their own Profile PRs grid shows. Six tests cover the new detection, verified to fail against the old e1RM-only logic. Core suite green (1049 passing, +10 new covering the reported bug, the rep-PR case, the weight-vs-e1RM pick and the formatter); core + mobile typecheck clean, web clean apart from the pre-existing nutrition/Prisma-client errors on main." },
  { id: "session-wrapped-prototype-gaps", area: "Core", status: "shipped", title: "Workout Wrapped — prototype-fidelity pieces", detail: "reference/pr-wrapped-flow.html is now reflected end-to-end. SHIPPED across the passes: the reveal rays + confetti; the Wrapped-hero signature ribbon; TWO bespoke share designs as first-class slide kinds across all three renderers (web DOM StoryCard + web canvas drawSlideStory + mobile SlideStoryCard) — 'trophy' (🏆 + PR value + 'new personal record') and 'signature' (the sessionSignature effort ribbon) — which LEAD the workout-page story picker; and, for a while, a 'Where you stand — Top X%' percentile. THAT PANEL IS GONE (Aug 2026): its only cohort source was the opt-in talent profile, and the Talent Graph was retired, so liftStanding(), the fetch, the panel and its EN/PL/DE copy all came out rather than leaving a card that could never render. The signature panel it shared a slide with stands alone now. A live standings board is still wanted — see `exercise-analytics-rankings`, which starts from real logged data instead of synthetic norms. REMAINING (intentional, not a gap): the mockup's in-app share DESTINATION buttons (Instagram/Messages/Copy-link) are delegated to the OS native share sheet, which already surfaces those targets; the 'minimal' card ≈ the existing 'stat' slide. Both clients typecheck; web build + iOS bundle export green." },
  { id: "session-wrapped-discipline", area: "Core", status: "shipped", title: "Workout Wrapped is discipline-shaped (a swim is not a gym session)", detail: "The Wrapped poured EVERY logged session through the gym's Sets/Reps/Volume mould, so a 1 500 m swim read '1 SET' (a cardio effort counts as one set in doneReceipt) and its hero showed '0.0 t' of tonnage — numbers that are true of the data model and meaningless to the athlete. Fixed in the shared model, not per-screen: sessionWrapped() now returns a `discipline` (strength / endurance / sport / conditioning / mixed, from wrappedDiscipline()), a `headline` (the ONE number that defines that kind of session) and a discipline-shaped BASICS strip capped at four tiles, each tile included only when it has something true to say. STRENGTH keeps sets/reps/volume/minutes; ENDURANCE gets distance/time/pace/calories in the sport's own units (metres and /100m for swimming, /500m for rowing, km elsewhere — via the existing formatSportDistance/formatSportPace); a timed SPORT (tennis, judo, football) gets time/calories/effort and never a set count; CONDITIONING gets time/rounds/calories/effort; MIXED gets time/distance/volume/calories. The hero follows the same rule — distance for a swim, tonnage for a lift, time for a match — instead of tonnage for everything. Premium FACTS gained the endurance side too (derived pace, climb, MET-minutes). Both clients render the same model (web components/aurora/workout-wrapped.tsx, mobile components/workout-wrapped.tsx) and the shape is unit-tested per discipline, including the 'never show sets or volume for a swim' regression. ALSO FIXED here: on mobile the floating Liquid-Glass pill nav is rendered at the ROOT over every screen, so it sat ON TOP of the Wrapped's sticky Share dock — the share button was literally behind the tab bar. The Wrapped is a full-bleed story takeover with its own back button, exactly like the live logger, so the `session` route joined global-nav's HIDE_ON. That matches web, where the Wrapped is an opaque fixed overlay ABOVE the pill nav and the bar was never visible. FOLLOW-UP CAUGHT IN REVIEW: widening the value vocabulary from '11.3 t' to '1500 m' and '2:20 /100m' overflowed the slots those sizes were chosen for — the 96px hero wrapped to two lines and a tile value pushed its label out of line with the three beside it. fitScale() in session-wrapped.ts now shrinks a value to one line, off a per-glyph em table MEASURED from Archivo Black rather than a character count: '11.3 t' and '1500 m' are both six characters and measure 66px and 85px in the tile, because a dot and a space are a third the width of a digit, so counting characters is off by 30%. It lives in core because mobile could lean on adjustsFontSizeToFit and web has no equivalent — two implementations would mean two different answers to 'how big is this number'. Both clients apply it to the hero (with the slot's negative tracking) and to the four tiles; tests assert every value the panel can produce fits its measured slot width." },
  { id: "session-wrapped-measured-precision", area: "Core", status: "shipped", title: "The Wrapped reads the watch's numbers, at the watch's precision", detail: "A matched pool swim's Wrapped showed 500 m at 4:00 /100m while the comparison panel two screens away showed the watch's own 510 m at 3:52 /100m — so a correctly matched session looked as though the sync had never landed. Neither figure was the typed one; both were the measurement, mangled by our own rounding. THREE FIXES, all in shared core so both clients inherit them. (1) doneReceipt rounded distance to 0.1 km, which erased the ten metres the watch measured (0.51 → 0.5) and then paced the swim over the wrong distance; it now rounds to the METRE, with the coarse tenth-of-a-km applied where a rail stat renders it (doneReceiptStats) rather than in the model. displaySportDistance caps km sports at two decimals so an unrounded measured distance can't print '10.234 km'. (2) The receipt gained `durationSec` — the recording's second-accurate moving time — and every DERIVED rate reads it in preference to the whole minutes: 510 m in 19:41 is 3:52 /100m, and dividing by a rounded 20 min turns that into 3:55. CardioBlock gained the same optional `seconds`, written ONLY by the device projection (device-truth.ts), so cardio PRs, pace trends and the block's own pace line agree with the watch too; the duration itself still DISPLAYS in minutes. (3) A cardio PACE PR headlined `${paceClock(value)} /km` hard-coded on both clients, so a swim PR read '39:13 /km' — it now goes through formatSportPace and reads '3:52 /100m'. Regression-tested against the reported swim (500 m / 20 min typed, 510 m in 19:41 measured) in session-wrapped.test.ts + device-truth.test.ts. FOURTH FIX (Aug 2026, same class, one layer lower): the STORED read was on a 10 m grid. sanitizeDeviceWorkout rounded distanceKm to two decimals of a kilometre, so a 34 m pool swim (0.034 km) was written as 0.03 — the app showed '34 m' in the logged column and '30 m' in the measured one beside it, and paced the swim 12:33 /100m against the watch's own 11:06. Ten metres is invisible on a 10 km run and is the whole workout on a warm-up length, and distance is the one measured figure spanning three orders of magnitude — there is no single grid that fits it. So the rounding is GONE from the model layer entirely: sanitizeDeviceWorkout stores the device's figure verbatim, doneReceipt and mergeDoneReceipts carry it unrounded, and every surface rounds where it RENDERS (deviceDistanceLabel — extracted from the comparison panel and now shared with the match picker, which was printing the raw float — displaySportDistance for the session's own figures, doneReceiptStats for the rail's coarse headline). Nothing derived may divide by a rounded distance, which is the whole reason the summary's pace and the watch's pace can agree. Its lower bound dropped from 0.01 km to 0.001 km too — the old floor discarded any recording shorter than 10 m outright. doneReceiptStats reads a sub-kilometre distance in metres for the same reason (tenths of a km made that swim '0 km' on the week + logbook rails). Already-matched sessions heal themselves: Connections → Apple Health → Sync re-reads every stored uuid (refreshMatchedWorkouts) and re-PATCHes the rows whose read changed — it needs the new mobile build, because the rounding happened on-device before the PATCH." },
  { id: "session-edit", area: "Core", status: "shipped", title: "Edit a workout you already logged", detail: "Logging is a hurry: a distance gets skipped, minutes get fat-fingered, a set lands with the wrong load — and until now the only remedies were to live with it or DELETE the workout and log it again, which throws away its PRs, the feel report and any device match. An 'Edit workout' sheet on BOTH clients (mobile components/session-edit.tsx, web components/session-edit.tsx, opened from the session summary's manage row) now corrects the figures in place: the workout name, each cardio effort's distance / time / climb / RPE, each conditioning block's rounds / time / RPE, and every strength set's load × reps × RPE (clear a row to delete that set, or add one). The model is SHARED (core/session-edit.ts, unit-tested): sessionEditDraft turns the stored session into editable strings — distance in the sport's own unit (metres for swimming/rowing), loads in the athlete's own unit — and sessionEditPatch FOLDS the draft back onto the ORIGINAL blocks rather than rebuilding them, so everything the sheet never showed (stroke, incline, zone, superset group, planned rest, a set's role/drop flag, the live logger's measured rest and bar velocity) survives an edit; a draft that no longer lines up with the session leaves that block untouched instead of guessing. An UNTOUCHED load keeps its stored string verbatim, because kg → lb → kg does not round-trip and would otherwise quietly rewrite every load of an lb athlete who opened the sheet to fix one distance. editableBlockFields decides which inputs a block shows (no distance for a timed-only sport like judo; no climb for a pool swim unless one was already logged), so the two clients can't drift. PATCH /api/sessions/[id] now accepts `blocks`, validated by sanitizeSessionBlocks — unlike the create route, which stores what our own logger built, an edit OVERWRITES an existing workout, so anything that doesn't sanitize is a 400 rather than a partial write. On a MATCHED session the sheet says so: the measurement still leads everywhere, and what is edited here is the logged column beside it. i18n EN/PL/DE." },
  { id: "session-energy-estimate", area: "Core", status: "shipped", title: "Estimated calories for every session + the connect-a-device prompt", detail: "Athletes with no watch had NOTHING on a sport log beyond a duration. packages/core/src/energy.ts now models the session's energy cost with the standard MET method (Compendium of Physical Activities 2011): kcal = MET x 3.5 x bodyweight / 200 x minutes, where the MET comes from the best input the session actually carries — a measured pace first (ACSM running/walking equations; Compendium speed bands for cycling, and per-100m / per-500m pace bands for swimming and rowing), then the sport catalog's own MET (per sport, falling back to its category), then RPE, then bare duration. Two honesty rules, in the done-receipt tradition: NO BODYWEIGHT, NO NUMBER (the formula is linear in mass, so a 'typical 70 kg athlete' would be inventing the answer — it returns null and the UI asks for a bodyweight instead), and the `basis` is carried, not hidden, so a weak estimate can be labelled as one. Every estimated figure renders with a leading '~' on both clients and never appears as a measurement. ONE MET MODEL, not two: engines/load.ts already carried a cruder RPE-only sessionEnergyKcal for the nutrition fuel target, so it now delegates to this one and its own blockMet was deleted. The two callers keep different HONESTY BUDGETS on purpose — the nutrition target must produce some number and so keeps its documented 75 kg default, while the Wrapped must not show a stranger's calories as the athlete's and so gets null. Paired with a CONNECT-A-DEVICE panel in the Wrapped, shown only when the athlete has no active connection AND the session is genuinely thin (no pace, no RPE, no usable estimate — `sparse`): it names exactly what a watch/strap/Apple Health would add (heart rate & zones, measured calories, precise moving time, recovery & sleep) and routes to Connections. hasActiveConnection() in connectors.ts is shared so both clients decide identically when the prompt is honest." },
  { id: "effort-model", area: "AI", status: "shipped", title: "Effort model — the reported feeling becomes training load", detail: "The ML foundation under session-feel, built on the idioms PR #267 established. THE PROBLEM: two athletes run 10 km in 40 minutes; one floats home, the other is destroyed. The log records the same session, so fatigue, ACWR, readiness and the prescription treated them as the same athlete doing the same work. engines/effort.ts closes that gap. (1) OBJECTIVE EFFORT — objectiveSessionRpe() is the minutes-weighted mean RPE the log implies, derived as sessionLoad / sessionMinutes (sessionMinutes newly exported from load.ts) so it is identical BY CONSTRUCTION to the load engine rather than a second opinion about the same quantity. (2) THE RESIDUAL — reported minus objective, per rated session, is the signal: a consistently positive residual means this athlete pays more for the same work. (3) THE LEARNED BIAS — deriveEffortModel() shrinks the mean residual toward the population prior (0) by a prior weight of 6 and clamps it to +/-2.5 RPE, exactly the shrinkage idiom personal.ts uses for the ACWR spike onset and datanet.ts for cohort norms: one rated session barely moves it, twenty move it a long way, no history pushes it somewhere absurd. (4) IT SCORES ITSELF LEAVE-ONE-OUT — for each sample the bias is refitted on every OTHER sample and used to predict the held-out one, then compared against the unpersonalised engine (baselineMae). An in-sample fit would beat the baseline whether or not the athlete has a real bias, so the model can and does report no improvement for an athlete who has none. (5) FITNESS TREND — effortTrend() regresses the residual against time, holding the objective work fixed and watching only what the athlete says it cost: a falling residual is the one honest fitness read a self-report can give. Null under 6 rated sessions or a 14-day span, because a slope through four points in one week is a line through noise. WIRED, NOT JUST AVAILABLE: toTrainingLog() gained a feelRpe hook and personalTrainingLog() threads the athlete's own answer through it, replacing the CONSTANTS every intensity used to fall back to (7 for a lift, 6 for cardio, 8 for conditioning) — all 17 call sites across both apps now use it, so Today, the AI coach, the squad endpoint and the prescription can't disagree about the same athlete. An RPE entered per block still wins: it is more specific than a whole-session rating. MEASURED EFFECTS, pinned in tests rather than assumed: injury risk moves strongly (28 days of the identical squat session read ACWR 1.43 / risk 36 when the last week is reported all-out vs ACWR 0.40 / risk 23 when it is reported easy — ACWR is an sRPE ratio, so a reported effort is literally its input); readiness moves only while the athlete's raw load sits under the fatigue engine's 40-unit normalisation floor, because muscle fatigue is normalised against the athlete's own max and a uniform shift otherwise cancels exactly — both regimes are pinned so nobody builds a 'your feeling sets your readiness' claim the engine never made. An earlier cut also back-filled UNRATED sessions with the model's prediction; that was reverted, because substituting one guess for another silently moved every existing athlete's numbers with no new information behind it. The prediction's honest use is forward (how hard will this session feel), not rewriting history. Four formulas registered in ENGINE_FORMULAS under a new 'effort' group, so the admin Engine Room's formula sheet renders the real math, drift-guarded against the live constants. CONSOLE SURFACE: the Engine Room gained an 'Effort model' card beside the personal-ACWR one, fed by GET /api/admin/engine (which now computes the model server-side, since it needs the RAW sessions the TrainingLog has already collapsed away — athleteInputs returns them alongside the log). It shows the learned bias on a -2.5..+2.5 scale with the prior marked, the rated-session count, and — the part that matters — the LEAVE-ONE-OUT error beside the unpersonalised engine's, with a verdict line that can and does say 'no better than the engine, this athlete reports roughly what their log implies'. An in-sample number would always have flattered the model, so the console would have shown a win for every athlete including the ones with nothing to personalise. Plus the fitness trend, or an explicit note of how many more rated sessions it needs. 24 unit tests." },
  { id: "session-feel", area: "Core", status: "shipped", title: "\"How did that feel?\" — post-workout effort + fatigue (training-load foundation)", detail: "The log alone cannot tell two athletes apart. User A runs 10 km in 40 minutes and floats home; user B runs the identical 10 km in 40 minutes and is destroyed. Identical rows, identical engine input, and prescribing the same next session for both is how you break the second one. The Wrapped now asks, once, right after the workout: how HARD it felt (1..5, easy -> all out) and how SPENT you are now (1..5, fresh -> wrecked). packages/core/src/session-feel.ts is the shared model — FEELS/FATIGUES (labels, emoji, tone), the 1..5 -> 1..10 session-RPE mapping, feltSessionLoad() = sRPE x minutes (Foster's internal training load, the field-standard measure), loadBand(), and the ML-facing pieces: feelSamples() reduces history to labelled {minutes, feel, fatigue, load, distance, tonnage} rows, loadBaseline() takes the athlete's own 28-day mean (null under three rated sessions, so 'relative effort' is never computed from noise) and relativeEffort() reports this session against that personal baseline — always the athlete vs themselves, never a cohort. Distinct from the two self-reports already in core: readiness-feeling is asked BEFORE training, notes MOOD is an optional private reflection; this is the only one that feeds training load. ASKED AT THE RIGHT MOMENT, on both clients: the prompt ships as ONE shared component (feel-prompt.tsx) with TWO homes, because the question has two natural moments. Primary is the FINISH screen — straight after the last set, while the athlete is still holding the phone and the answer is most accurate; a first pass put it only on the Wrapped, which an athlete reaches by re-opening a logged session later, so most sessions would never have been rated at all. Secondary is the Wrapped panel, which catches a session that was opened later and never answered. Both seed from the stored value, so nobody is asked twice, and either entry point fills the same columns. Taps save optimistically, one field at a time, via PATCH /api/sessions/[id] -> sanitizeFeelLevel; the read-out shows the session load, its band, and the personal +/-% once there is a baseline. Schema adds Session.feel + Session.fatigue (Int?), owner-only like note/mood/tags. LIVE: reference/sql-session-feel.sql has been run in the Supabase SQL Editor, so Session.feel + Session.fatigue exist and the prompt now persists. Reads had to be widened to match: every SERVER-side path built its LoggedSession with an explicit field list that dropped feel, so the engines there would have fallen back to the old constants while the clients (which receive the full row from GET /api/sessions) used the real answer — the AI coach and the athlete state would have been reasoning about a different athlete than the app displayed. api/ai-coach and lib/athlete-state now carry it. The COACH squad endpoint carries it too but as ENGINE INPUT ONLY: that response serialises per-athlete aggregates and a session count, never session objects, so a coach's readiness/ACWR/risk reflects what their athlete reported without the per-session answer leaving the server. The one endpoint that does return raw rows to a coach (coach/links/[id]/sessions) keeps its explicit select, which excludes feel/fatigue alongside the private note. A THIRD HOME, for the sessions that never pass a finish screen: an imported watch workout and a quick-logged sport both arrive with nobody having been asked, and until now their only answer point was the Wrapped panel — which requires opening the session and scrolling past six panels to volunteer a number. feel-sheet.tsx (both clients) is the same prompt as a one-tap sheet, opened from a bare lime 'Rate' on the done floor's row (and on the web import receipt); the mobile import sheet ends on the prompt inline. `isRated` in feel-schedule.ts is the single definition of 'this session has no effort value', so the two clients cannot disagree about which row wears the ask, and FeelPrompt gained an `onAnswered` callback so a host waiting on the answer can stop offering to skip it."},
  { id: "exercise-picker", area: "Core", status: "shipped", title: "Searchable exercise picker (grouped by muscle/pattern)", detail: "Adding an exercise in the logger + routine builder now uses the same field-styled trigger → searchable modal as the 'Log a sport session' sport picker, replacing the old wall of chips. exercisesByCategory (movements.ts) groups a catalog by movement pattern (Squat/Hinge/Push/Pull/Conditioning, with free-typed/library lifts in Other), sorted A–Z, empty groups dropped, headers localized (exercise.cat.*). The modal also lists recent lifts (logger), sports (olympicSportsByCategory → cardio) and a '+ custom name' entry. Web (workout-blocks.tsx ExercisePicker) + mobile (workout.tsx, builder.tsx, aurora/builder.tsx). Pure categorization unit-tested." },

  { id: "future-self", area: "Core", status: "shipped", title: "Future Self Simulator", detail: "future-self.ts: projects the athlete forward from current behavior — a lift's e1RM trajectory or bodyweight path (least-squares fit), a goal ETA, and an honest goal-by-date probability (normal CDF over fit uncertainty). Consistency bends the curve (adherence factor 0.5..1.2) so the simulator shows the cost of slipping and the reward of showing up. Pure + unit-tested. The motivational hook on the Fitness GPS home." },
  { id: "volume-landmarks", area: "Core", status: "shipped", title: "Volume landmarks (MV / MEV / MAV / MRV)", detail: "Renaissance-Periodization-style weekly set-volume landmarks per muscle group: MV (maintenance), MEV (minimum effective), MAV (maximum adaptive range), MRV (maximum recoverable). landmarks.ts: VOLUME_LANDMARKS holds starting per-muscle ranges mapped onto HYBRID's seven muscle groups; weeklySetsByMuscle counts this week's (last 7 days) performed WORKING sets per muscle (warm-ups/cool-downs already excluded by the session engine, empty sets skipped, each set counting toward every muscle the movement trains via MOVEMENTS); classifyVolume buckets a count into under / productive / peak / overreaching; muscleVolumeStatus + volumeStatus report where each muscle sits with its landmark numbers; and volumeAdvice gives the PRESCRIPTION — only the actionable muscles, ADD sets when below MEV (toward the MAV low end) or REDUCE/deload when at/over MRV, most-urgent first (over-MRV before under-MEV). Pure + 9 unit tests. SURFACED on a new Volume screen (analyze group, athlete persona) on BOTH clients: web components/volume.tsx + mobile app/volume.tsx — a per-muscle bar with the MEV/MAV-band/MRV markers + zone colour, and a 'this week — adjust volume' advice list; honest empty state until working sets are logged. Nav entry localized EN/PL/DE (screen body copy is English for v1, matching the web analytics screens). SCOPE delivered: tracking + an advisory prescription layer. NEXT: fractional secondary-muscle weighting, per-athlete adaptive landmarks (from fatigue/recovery data), and auto-applying the nudge inside the prescribed next session (deeper progression-engine integration); volume trends over time land in the training-analytics-hub." },
  { id: "volume-screen-redesign", area: "Core", status: "shipped", title: "Volume screen — hero redesign", detail: "The Volume screen rebuilt on both clients (apps/web/components/aurora/volume.tsx + apps/mobile/components/aurora/volume.tsx) off a new shared view model, packages/core/src/volume-view.ts (railX / railGeometry / volumeSummary / sortByUrgency / setsLabel / deltaLabel, 14 unit tests). What changed and why: (1) HERO — the screen now opens on one verdict, `4/7 muscles in their productive range`, drawn as a seven-column week-shape you read before you read a word; tapping a column swaps the verdict line for that muscle's count and zone. The old opener was a paragraph of acronyms. (2) NORMALISED RAIL — every muscle's landmarks are different (Triceps cap at 18 sets, Back at 22), so an absolute-scale bar put every band at a different x and the stack was unreadable. railX maps each muscle against its OWN landmarks onto fixed anchors (MEV .34, MAV-high .72, MRV .86, then an overshoot tail saturating at 1.5x MRV), so the rows stack into one comparable picture; the absolute count is printed alongside. (3) NOTCHED TICKS — MEV/MRV are cut out of the rail in the card colour rather than drawn as coloured lines, so they stay legible under a full fill. (4) The per-row legend loses its SWATCHES but keeps its LABELS. Two earlier cuts were wrong and are recorded here so they are not repeated: dropping the whole legend cost the athlete the values themselves, and re-adding bare numbers under a single named header row left no way to tell which number was which once you had scrolled past it. What ships: every row carries its own named scale (railScale in volume-view.ts) — MEV 8 / MAV 12–18 / MRV 20 — with each value POSITIONED under the mark it belongs to (MEV and MRV directly beneath their rail notches, MAV under the middle of the band) rather than left-packed, and with the label as the quiet part (9px ash) against the number a size up in chalk. No coloured square in front of any label, per the no-marker-before-a-label rule. Because every row now names its own values, the shared legend rail above the list is gone. The row header carries the count AND the zone word ('24 sets — over MRV'), and the track itself is a key: the MAV band lit, the territory past MRV tinted, so the zones read even on an empty rail. Expanding a row therefore adds only what the scale does not already say — the MV floor and the prescription sentence. (5) PRESCRIPTION — the three verbatim-identical 'Over your recoverable ceiling…' sentences collapse into Ease off / Add volume chip rows (muscle + signed delta), with the reason stated once. (6) Rows sort by urgency (over the ceiling, then under the minimum, then the rest). (7) TYPE — the display face carries names and headings; mono is reserved for numbers and eyebrow labels, retiring the all-monospace terminal look. (8) PROGRESSIVE DISCLOSURE — the MV floor and the per-muscle advice live behind a row tap (the scale stays on the surface); the acronym glossary behind a 'What the bands mean' disclosure. (9) The separate 5x7 landmark-editor grid is retired: Edit landmarks opens every row in place and the values become fields where they are already shown. (10) SPOTLIGHT — the row's scale is a left-aligned three-column table (values line up down the whole list rather than floating at three indents, which read as chaos on a phone) and every cell is a CONTROL: tapping MEV / MAV / MRV scrims everything outside that band on EVERY row at once, caliper edges frame the lit slice so it reads even when empty, the tapped label lights across the list, and the definition appears under the row you touched (mobile fires a selection haptic). Tapping MRV instantly shows which muscles poke into the ceiling zone; tapping MAV shows who has reached the productive band. bandRegion in volume-view.ts, shared by both clients. New copy localized EN/PL/DE." },
  { id: "volume-provenance-redesign", area: "Core", status: "shipped", title: "\"Whose numbers are these?\" — the provenance card rebuilt", detail: "The SourceCard on both Volume screens (apps/web/components/aurora/volume.tsx + apps/mobile/components/aurora/volume.tsx) was doing seven jobs at one type size: provenance, profile completeness, fitness level, the personalization factors, the adaptive toggle, the ceiling replay and the profile form — six explanatory paragraphs stacked in identical ash body copy with nothing to look at. Rebuilt around three questions, each at a different weight. (1) THE PROVENANCE LADDER. `athleteLandmarks` layers population → profile → observed → manual and returns `layers` + `source`; that is a four-rung climb, and the card was reducing it to one caption ('Learned from your training') set on the SAME row as the title, where the longer of the two forced the title to wrap. It is now drawn as what it is: four rungs, each with a spine segment lit when that layer contributed, at full strength on the layer that actually named the numbers, and carrying the confidence THAT layer can claim (the profile rung shows profileConfidence, the observed rung observedConfidence). A column that stops at rung one says 'textbook averages' before a word is read; an unlit rung is also the thing to do next. Rungs are radio controls — tap one to read what that layer did, which is where the four sourceWhy sentences now live instead of only ever showing one. New shared view model `provenanceLadder` / `rungMeta` / `LANDMARK_LAYERS` in volume-view.ts so the rungs, their order and their confidences cannot drift between clients (5 unit tests). (2) ONE THING TO DO NEXT. The completeness meter kept its weighted score but the 'Next: Training age' line, previously inert text, is now a CONTROL: tapping it opens the About-you form in place (its own local state, so it no longer has to borrow the screen-level `editing` flag and expand every landmark field on the muscle rows above). The bar thins to 3px — it is a reading, not a subject. A complete profile gets a sentence saying so rather than an empty rail. (3) SHOW THE WORKING. Level-from-your-lifts, the factors, the adaptive toggle and the ceiling replay fold behind one disclosure, matching the 'What the bands mean' idiom already on the screen. FOUR HONESTY BUGS FIXED ON THE WAY: the factor rows rendered with NO heading directly under 'Your level, from your lifts', so 'Sleep 3/5 −6%' read as evidence of how strong you are — they now carry their own head ('What moved your bands') and each row names WHICH END of the band it moved (`affects`, which the engine has always returned and the UI threw away — factorAffectsKey); the orphan '25% confidence' line is gone, its number now sitting on the ladder rung it belongs to; the adaptive toggle's ON label was 'Done', which is not a state of 'Learn from my training' — it reads common.on/common.off; and the replay block no longer prints a second 'not enough evidence yet' paragraph directly under the first, staying silent until a week has actually been tested. New copy localized EN/PL/DE. Core tests + web/mobile typecheck green." },

  { id: "ui-templates", area: "Core", status: "shipped", title: "Aurora — the single UI template (Classic removed)", detail: "AURORA IS NOW THE ONE AND ONLY UI look — the old industrial 'classic' template was REMOVED: there is no skin switch, no per-device template preference, and no classic screen variants are rendered. @hybrid/core theme/templates.ts collapsed to a single-entry registry: TemplateName is just 'aurora', resolveTemplate() always returns 'aurora' (a stored 'classic' is coerced), isTemplateName only accepts 'aurora'; the Template switcher was removed from web + mobile Settings. The clients' useTemplate() hooks are kept (always 'aurora') so the residual `template === 'aurora'` style ternaries compile and always pick the Aurora value — the dead classic component files have now been REMOVED (web: ~34 classic screen/helper files deleted, SessionDetail extracted to components/session-detail.tsx, the classic ⌘K command menu + header dropped from app-shell; mobile: the inline ClassicX body removed from all ~35 template-switched route files, each now rendering only its Aurora screen). ORIGINAL DESIGN (history): a template layer that re-skinned the app's SCREENS (layout/structure/component shapes) without touching data or brand tokens — distinct from theme/palette.ts (which flips light/dark palettes). AURORA is a soft, rounded look adapted from the uploaded mobile Figma kit (big corner radii, pill buttons/inputs, segmented controls, schedule-first home), built ON the existing HYBRID brand tokens (lime accent, ink surfaces, Archivo) via the shared theme palette so it stays on-brand and theme-aware. The active template is a per-device preference: mobile lib/template.tsx (TemplateProvider + useTemplate over AsyncStorage, mounted in app/_layout.tsx) and web lib/use-template.ts (localStorage hook), both keyed off the SHARED @hybrid/core TEMPLATE_STORAGE_KEY so the two clients can't drift on the key string. SWITCHABLE from Settings on both clients (mobile app/settings.tsx Template card; web account-settings Preferences → Template). MOBILE Aurora slice shipped: Welcome, Login/Register, the stepped Onboarding wizard, and the schedule-first Home (components/aurora/*) — each route renders the Aurora variant when the template is active and the classic screen otherwise, all on the SAME auth/onboarding/engine logic (Supabase auth incl. the onboarding-pending flag, recommendPlan, prescribeSession/computePerformanceState). WEB Aurora slice shipped for AUTH: components/aurora/login.tsx mirrors the rounded Figma login and reuses the identical Supabase flow (OAuth – email – MFA step-up – demo roles) so there's no 2FA regression. ICON SET wired in: the uploaded design-kit line icons (icons1/2/3) are the canonical Aurora glyphs — @hybrid/core theme/icons.ts holds the name union (AuroraIconName) + 72×72 SVG path data (single source of truth); web renders them as inline <svg stroke=currentColor> (components/aurora/icons.tsx) and mobile renders the matching transparent PNGs (apps/mobile/assets/icons) recoloured via <Image tintColor> (no react-native-svg dep), names kept in lockstep. Used across the Aurora auth fields (mail/lock/eye/user), back buttons, the onboarding selected-choice check, the Home search/bell + Start (play) buttons, and the mobile bottom-nav glyphs (which swap to the line-icon set when Aurora is active). iOS bundle export is green with the assets bundled. GLOBAL SKIN so EVERY screen (not just the hand-built hero flows) picks up the Aurora look without a per-screen rewrite: on mobile the shared lib/ui primitives are template-aware (Card → larger radius, Button → pill) so all screens built on them shift automatically; on web TemplateSync mirrors the active template onto <html data-template> and globals.css [data-template=\"aurora\"] rounds the shared surfaces (liquid-glass cards, inputs, rounded-xl/lg) across the whole app shell. DEDICATED Aurora layouts now also cover mobile Settings (components/aurora/settings.tsx — avatar header + grouped icon list, keeps every control) and the web LANDING (components/landing.tsx, client-gated, uses the icon set). Remaining bespoke Figma layouts are tracked under 'ui-template-aurora-web'. THEME FOLDER: the shared theme is now ONE self-contained, swappable unit (WordPress-theme style) at packages/core/src/theme/ — tokens.ts (brand tokens), palette.ts (light/dark palettes), templates.ts (the Classic⇄Aurora skin switch), icons.ts (the Aurora icon set), an index.ts barrel and a README.md documenting how to re-tint, tweak, or add a whole new skin. The rest of the app keeps importing from the @hybrid/core barrel (no churn). The per-client RENDERERS necessarily stay with each app (apps/mobile/components/aurora/* + lib/theme.tsx; apps/web/components/aurora/* + globals.css) since React Native and Next.js can't share component code. Stale design source was removed in the same pass (the raw Figma export 'hybrid app deisgn/', design-mockups/, and the Aurora design-exploration HTML under reference/) — reference/ SQL + BUILD_BRIEF kept." },
  { id: "ios-privacy-manifest", area: "Mobile", status: "shipped", title: "Apple Privacy Manifest (PrivacyInfo.xcprivacy)", detail: "App Store submission requires a privacy manifest since spring 2024, and any app using a required-reason API must declare it — HYBRID bundles @react-native-async-storage/async-storage (UserDefaults, category CA92.1), so it was a hard upload blocker (ITMS-91053). app.json now declares ios.privacyManifests (Expo generates PrivacyInfo.xcprivacy at prebuild): NSPrivacyTracking=false + empty tracking domains (no ATT SDK), the collected data types (Fitness, Health, Email, Name, UserID, Photos — all Linked, non-Tracking, AppFunctionality purpose), and required-reason API declarations for UserDefaults (CA92.1), FileTimestamp (C617.1), SystemBootTime (35F9.1) and DiskSpace (E174.1) that RN/Expo trigger. Expo modules add their own manifests during prebuild and are merged. VERIFY after `expo prebuild`: confirm the generated aggregated manifest, and confirm the App Store Connect data-collection questionnaire matches (Fitness data, contact info, identifiers, user content)." },
  { id: "legal-pages", area: "Web", status: "shipped", title: "Privacy Policy + Terms of Service (public, linked in-app)", detail: "App Store Connect requires a resolvable privacy-policy URL to submit and Guideline 5.1.1 requires it be reachable in-app; a paid subscription also needs Terms — HYBRID previously had neither (hard blocker). Added public /privacy and /terms pages on the web app (apps/web/app/privacy, /terms; shared contact + effective date in app/legal/legal.ts) covering data collected, use, sharing (incl. coach links + service providers), the access/export/reset/delete rights, retention, security, children, and contact. Linked on BOTH clients: web login (aurora) + Settings danger zone, mobile login (with an 'agree' line) + Settings (Legal section) via a shared LegalLinks component that opens the hosted pages. Localized labels EN/PL/DE (legal.* keys). OPERATOR TODO before submission: set the real legal-entity name, postal address and contact email in app/legal/legal.ts (currently HYBRID + privacy@hybrid.app placeholders) and set the same privacy-policy URL in App Store Connect. Have counsel review the templates for your jurisdiction." },
  { id: "app-store-submission-notes", area: "Mobile", status: "shipped", title: "App Store Connect submission notes", detail: "reference/app-store-submission.md — the operator-facing checklist for submitting the iOS build: the App Privacy data-collection answers kept in sync with app.json's ios.privacyManifests (Health, Fitness, Email, Name, User ID, Photos — all Linked, non-Tracking, App Functionality; no ATT since there's no tracking SDK), export-compliance (usesNonExemptEncryption=false is accurate — TLS + Keychain only), why Sign in with Apple (4.8) doesn't apply (iOS login is email-only), where in-app account deletion lives (5.1.1(v)), the IAP/subscription config + reviewer note, a reviewer demo-account + review-notes template, the metadata/age-rating checklist, and the pre-submit sanity list (apply RLS SQL, set IAP/encryption/rate-limit env). Bracketed [...] values (demo creds, real legal entity/contact, privacy/terms URLs) are the operator's to fill." },
  { id: "api-health-check", area: "Backend", status: "shipped", title: "Health check endpoint (/api/health)", detail: "GET /api/health does a trivial `SELECT 1` DB round-trip and returns 200 { ok:true, db:'up', latencyMs } when reachable, 503 when not — no user auth (exposes nothing), generously per-IP rate-limited (120/min) so it can't hammer the DB. Point an external uptime monitor (e.g. BetterUptime / Pingdom / a Vercel cron) at it so real outages page someone instead of waiting for user complaints." },
  { id: "error-boundary-reportable", area: "Core", status: "shipped", title: "A caught crash says what broke, and can be left behind", detail: "The render boundaries on both clients used to catch an exception and then throw the only copy of it away: mobile's fallback (apps/mobile/components/error-boundary.tsx) said 'Something went wrong. Try reloading.' and web's (apps/web/app/error.tsx, global-error.tsx) printed at most a digest. On a device there is no console and no screen left to read one from, so a TestFlight crash arrived as 'the page doesn't work, it just asks me to reload' with nothing attached — unfixable by construction. Both fallbacks now render the error name, message and the first four stack frames (mobile adds the component stack, and a Share action so the detail can leave the device) in a bounded, scrollable, selectable mono block. Mobile also gained the SECOND action web always had: its boundary sits above the navigator, so 'Reload' re-rendered the same crashing route — a loop whose only exit was force-quitting, the exact failure the boundary exists to prevent. 'Back to Today' clears the error and jumps off the crashed route one frame later, once the navigator is back. Deliberately provider-free (no theme, i18n, session or app font): any of those may be the thing that failed. A crash is also CONTAINED now: the app-wide boundary is mounted above the navigator, so one broken route blanked the whole app — no tab bar, no back gesture, nothing to navigate with, which is the gap between that boundary's own promise ('one screen's crash never bricks the app') and what it did. `RouteErrorBoundary` is expo-router's per-route convention — a route exporting it keeps the navigator mounted around the failure, so the broken screen reports in place and every other screen stays reachable. Wired on the Plans route (the one known to crash on device); rolling it out to the remaining routes is tracked as route-error-containment. Verified by forcing a throw in app/plans.tsx and booting the export: the fallback names the error, the app-wide boundary never fires, and navigating away still works. This is the stopgap until crash-reporting below is wired." },
  { id: "route-error-containment", area: "Mobile", status: "planned", title: "Route-level error containment on the remaining screens", detail: "`RouteErrorBoundary` (apps/mobile/components/error-boundary.tsx) is expo-router's per-route ErrorBoundary convention and is currently exported only from app/plans.tsx, the one route known to crash on device. Every other route still falls through to the app-wide boundary above the navigator, which blanks the entire app for one screen's throw. PLAN: add `export { RouteErrorBoundary as ErrorBoundary } from '../components/error-boundary'` to the remaining ~40 route files (a one-line addition each), and guard it with a test that every file in app/ exports one, so a new route cannot ship without containment. Left out of the first change deliberately: the fix that mattered was the crashing screen, and a 40-file sweep would have buried it." },
  { id: "crash-reporting", area: "Core", status: "planned", title: "Crash reporting + error monitoring (Sentry)", detail: "Neither client currently has a crash reporter, so native crashes and JS errors on real devices are invisible and there's no post-release regression triage. PLAN: add @sentry/react-native (Expo plugin) to mobile and @sentry/nextjs to web, initialized behind the existing feature-flag kill switch, with the root ErrorBoundary (mobile) reporting caught render errors. Scrub PII from events (the analytics layer is already a no-op stub, so nothing leaks there today). NEEDS: a Sentry account + DSN (SENTRY_DSN / EXPO_PUBLIC_SENTRY_DSN) in the env; can't be verified in this sandbox without it." },
  { id: "staging-environment", area: "Core", status: "planned", title: "Staging vs production environment separation", detail: "Today one Supabase project + one Vercel deployment serves web + mobile, dev + prod (apps/mobile/lib/supabase.ts hardcodes the prod project as a fallback), so test writes hit production data and there's no safe place to rehearse a migration — a SOC2 change-management gap. PLAN: stand up a separate staging Supabase project + Vercel preview environment, drive every URL/key purely from per-environment env (no hardcoded prod fallback), and point staging mobile builds at it via EXPO_PUBLIC_* build vars. NEEDS: a second Supabase project + the env wiring; operational, not code." },
  { id: "audit-remediation-2026-07", area: "Core", status: "shipped", title: "iOS production-readiness audit — remediation", detail: "Fixed the release-gating findings from audit/06-ios-production-readiness-audit-2026-07.md (14 commits). SECURITY: closed the RLS policy-logic escalations that the anon key could exploit even with RLS enabled — User UPDATE column-restricted (no self-escalate to ADMIN), CoachLink insert forced to PENDING with client-only activation (no forged-ACTIVE cross-tenant read), org/team/membership writes server-only, CoachInvite claim scoped to own-email, ProcessedWebhookEvent RLS enabled, blanket anon REVOKE (rls-policies.sql + sql-all.sql). Coach GET routes require an ACTIVE link (no reading a non-consented user pre-acceptance). Account export redacts OAuth tokens (+ CI regression test). Entitlement mirror moved to app_metadata. OAuth callback validates `next` (open-redirect), clientIp uses the trusted-proxy hop (no XFF spoof), rate-limiter alarms when unconfigured in prod, coach-invite throttled, hot write routes body-capped. APP STORE: self-serve account deletion (+ complete erasure shared by reset/admin), PrivacyInfo.xcprivacy, /privacy + /terms pages linked in both clients, IAP restore + launch listener + StoreKit price + paywall legal links + Apple manage-subscription. RELIABILITY: fetch timeouts + root ErrorBoundary + password reset, offline-onboarding clobber + workout load-race fixed, one-shot flag/persona/plan-maxes stores reset on auth change, throwing query layer so History shows a real error/retry state, /api/health probe. Bounded coach/squad + datanet queries. NOTE: the RLS/index/cascade SQL must be RUN in the Supabase SQL Editor (sql-all.sql) — it's inert until then (see schema-tenant-isolation-rls)." },
  { id: "mobile-password-reset", area: "Mobile", status: "shipped", title: "Password reset on mobile", detail: "The mobile login's 'Forgot password' was a dead <Text> with no handler (users were locked out on mobile — web already had reset). Now a real control that calls supabase.auth.resetPasswordForEmail(email) with a confirmation notice; requires the email field first. Localized EN/PL/DE (w.account.login.forgot-*)." },
  { id: "mobile-secure-store", area: "Mobile", status: "shipped", title: "Store the auth session in the Keychain (SecureStore)", detail: "The Supabase session (access + long-lived REFRESH token) now persists in the iOS Keychain / Android Keystore via expo-secure-store (~56.0.4), not plaintext AsyncStorage where it was recoverable on a jailbroken device / via an unencrypted backup / forensic image (session theft — audit H-7). apps/mobile/lib/secure-store.ts is a Supabase auth `storage` adapter that (1) CHUNKS the session across Keychain items to stay under SecureStore's ~2KB per-value limit, (2) transparently MIGRATES an existing AsyncStorage session into the Keychain on first read and deletes the plaintext copy — so no one is force-logged-out on upgrade — and (3) falls back to AsyncStorage on the Expo web bundle (where SecureStore is unavailable and the at-rest risk doesn't apply). Wired in apps/mobile/lib/supabase.ts. Mobile typecheck + iOS bundle export green; the native Keychain round-trip itself needs an on-device/EAS build to exercise (can't run in the sandbox)." },
  { id: "schema-migrations-reproducible", area: "Core", status: "blocked", title: "Reproducible Prisma migrations (retire manual-SQL drift)", detail: "DONE in code (audit C-3): prisma/migrations/0_init/migration.sql was regenerated from schema.prisma via `prisma migrate diff --from-empty --to-schema-datamodel` into a COMPLETE 64-table baseline (all tables, enums, 110 indexes, 50 FKs incl. the 47 ON DELETE CASCADE the old 7-table 0_init lacked) — verified byte-identical to a fresh diff. So `prisma migrate deploy` now reproduces the ENTIRE schema on any fresh env (staging / preview / DR), which it couldn't before (the other 56 tables lived only as hand-run reference/*.sql). prisma/MIGRATIONS.md documents the fresh-env flow, the go-forward `migrate dev` workflow, and the two-layer model (Prisma owns the schema; reference/*.sql owns the RLS/grants/policy-indexes). The reference SQL layer (sql-all.sql) stays the source for RLS + security. blockedBy below is the one thing the sandbox can't do: reconcile the EXISTING prod DB (its _prisma_migrations still records the old 0_init).", blockedBy: "Reconcile production per prisma/MIGRATIONS.md (needs live-DB access the sandbox lacks): back up, confirm no drift (`migrate diff --from-url $DIRECT_URL --exit-code`), delete the stale `_prisma_migrations` 0_init row, `prisma migrate resolve --applied 0_init`, verify `migrate status` clean. Then gate CI on a clean migrate status." },
  { id: "swiftui-kit", area: "Core", status: "blocked", title: "Aurora × SwiftUI native kit (Liquid Glass)", detail: "The mobile Aurora kit now renders REAL native SwiftUI on iOS via @expo/ui (56.0.18; newArch/Fabric already enabled, autolinked native module — no config plugin or babel plugin needed since we don't use Icon.select; react-native-worklets stays an optional soft-require so it's not installed). New native layer apps/mobile/components/aurora/swiftui.tsx exposes: GlassSurface (a SwiftUI glassEffect RoundedRectangle that fills its parent and sits BEHIND ordinary RN content, with a translucent RN floor under it so the surface is still visible on iOS < 26 / pre-mount), GlassSegment (SwiftUI Picker + .pickerStyle(.segmented), tinted with the brand lime), GlassButton (content-sized SwiftUI Button with .buttonStyle(.glass/.glassProminent)) and GlassPanel (RN content on a glass backdrop). These are wired into the SHARED kit (apps/mobile/components/aurora/kit.tsx) so screens inherit them with no per-screen change: ACard drops a Liquid Glass surface behind its content (transparent RN base) on iOS, the neutral `soft` APill becomes a glass pill, and ASegment delegates to the native segmented Picker. The native treatment is ALWAYS ON on iOS (the single gate is swiftui.tsx LIQUID_GLASS_SUPPORTED = Platform.OS === 'ios') — the per-device opt-in toggle that originally shipped here was REMOVED by request (Aug 2026): the native look is the product, not a preference. Android falls back to the existing RN Aurora treatment and the web client (Next.js) keeps its CSS Aurora — SwiftUI is iOS-only by construction. The classic Aurora backdrop (AuroraField in kit.tsx) is a soft layered LinearGradient wash (the RN parity of the web .lg-field's blurred blobs) so it reads as a smooth gradient rather than hard-edged discs, and renders in BOTH modes. TOGGLE REMOVED (Aug 2026): the 'Liquid Glass (iOS)' Settings switch, its LiquidGlassProvider (lib/liquid-glass.tsx), the LIQUID_GLASS_STORAGE_KEY persistence and the w.account.settings.liquid-glass* strings were all deleted — a stale AsyncStorage value on old installs is simply never read. Composition rule honoured: SwiftUI subtrees live entirely inside a <Host>; RN children are never nested in a Host (glass is a backdrop, not a wrapper). Mobile typecheck + iOS export bundle are green. SOLID OPT-OUT (Aug 2026): ACard grew a `solid` prop that skips the glass surface even on iOS — the Performance surface (performance.tsx + its inline volume.tsx/trends.tsx) and the Nutrition tab (nutrition.tsx) now use it on every card, because the translucent material washed out their data-dense charts/stat columns (and left Nutrition half-glass, half-solid); solid ink2 there matches Today's dominant card treatment and the web Performance screen. WEB↔MOBILE PARITY: the web already simulates Liquid Glass via CSS (.liquid-glass) and mobile already had a blur-based GlassCard (lib/ui.tsx); this adds the TRUE native material on iOS only. GLASS AS A BEHAVIOUR, NOT ONLY A MATERIAL (Aug 2026): everything above uses glassEffect as a surface — one pane at a time, a prettier background. The Today hub dock (today-hub-floating-pills) is the first place that uses what the material actually DOES: swiftui.tsx GlassPillRow is a GlassEffectContainer whose capsules share a Namespace and each carry a glassEffectId, so touching capsules FUSE into one lozenge and a frame change makes the glass FLOW between shapes. @expo/ui 56 exposes the pieces that make this possible — GlassEffectContainer, Namespace and the glassEffectId/animation modifiers — which the screen-transitions-wave3 notes had recorded as missing; they are not missing, and the pattern is now proven at the JS/bundle level. Two rules learned the hard way and honoured here: the animating view must PERSIST (a view that inserts or removes has no source and destination to morph between, which is why the old nav's conditional lens snapped), and every animatable value must arrive as a plain target number the native side springs to ITSELF (driving it frame-by-frame from JS re-samples the backdrop every frame and janks). THE NAV BUTTON IS A NATIVE BUTTON NOW (Aug 2026): HeroNav — the one back/dismiss circle on every screen head, hero.ts §7 — no longer layers a static GlassSurface under an RN ring + PressScale on iOS 26. swiftui.tsx GlassNavButton is a real SwiftUI Button (buttonStyle plain) whose 40pt label circle wears glassEffect({interactive: true, shape: circle}) inside the 44pt hit frame (contentShape circle), so the material itself answers the touch — specular shimmer and press bounce — and the drawn hairline ring/fill are gone where the real material renders. The tap is handled by SwiftUI (same leaf contract as GlassSegment's Picker); the glyph is the platform's SF Symbol (arrow.left / chevron.down — the same drawings as the kit's back / chevron-down marks). New gate swiftui.tsx LIQUID_GLASS_RENDERED (iOS 26+, where glassEffect actually renders) — below it, and on Android, HeroNav keeps the full RN fallback circle (white-12% + blur + hairline, AuroraIcon glyph, PressScale). The `clear` material stays the SAME native button with glass variant `identity`, so the field screens' clear↔glass cross-fade never swaps renderers mid-scroll. Web keeps its CSS glass circle (browser has no system material — the standing parity constraint of this capability). THE MENUS, THE WHEEL AND THE MINIMIZE CIRCLE FOLLOW (Aug 2026): (1) the feed's ⋯ overflow menu is the SYSTEM's on iOS 26 — swiftui.tsx GlassMenuButton is a SwiftUI Menu leaf (trigger + items one native view, selection handled by SwiftUI), presented as real Liquid Glass zoom-morphing out of the anchor; feed-menu.tsx's FeedMenuTrigger owns trigger + menu + actions for both feed-card and the post screen, with the measured-anchor RN card as the everywhere-else fallback. A system menu dismisses on select, so BOTH clients' menus now close-and-toast instead of holding a row open to tag it — the outcome lands in the new shared toast chip (mobile components/aurora/toast.tsx mounted in the root layout outside the recede plane; web components/aurora/toast.tsx mounted in the app shell), one message at a time, never pressable, new key feed.menu.unfollowed added EN/PL/DE. The web menu keeps CSS but joins the pill nav's glass grammar (glass film + rim, .feed-menu-zoom anchor-origin entrance) — Aurora's cards stay solid; a menu is a transient overlay, not a content surface. (2) The date filter's month list is the system WHEEL on iOS 26 (swiftui.tsx GlassWheel — Picker + .pickerStyle(.wheel)) inside the app's own sheet: selection applies as the wheel settles and the sheet stays up; preset rows still pick-and-close; a single month keeps its row; Android/iOS<26/web keep the grouped rows. (3) The live logger's minimize circle — the last bespoke nav circle — is GlassNavButton on iOS 26 (34pt circle in a 44pt hit), with the web twin swapping its drawn ring for the same rim-only glass recipe (theme-aware --inner-hi/--inner-lo). DEVICE ROUND 1 (Aug 2026, first screenshots from the TestFlight build): the loudest 'not native' reads came from the two surfaces that still opted OUT, so (a) the nav circle's `clear` material is RETIRED — heroNavMaterial returns glass on every ground now (one control, one material; the nutrition screenHead and the profile field editor drop their hardcoded clear), the signature keeps its inputs so a future split has somewhere to live; and (b) the live logger's ⋯ is the system Menu on iOS 26 (one destructive row, Discard session, still landing in its confirm sheet) with the options Sheet as the everywhere-else fallback. DEVICE ROUND 2 (Aug 2026), and THE DIRECTION it settled: the goal is native SwiftUI CONTROLS, not the Liquid Glass skin — where an element maps to a real system control it goes native; where it has no system counterpart it keeps the app's own drawing, on every platform. What went native: (a) GlassSelectMenu — a system Menu whose trigger is the value itself as text and whose body is an inline Picker (checkmark on the one in force) plus optional action rows behind a Divider; wired to the nutrition meal switcher ('Breakfast ⌄' — meals + Add a meal part) and the logger's rest chip ('⏱ 90s' — Off/60/90/2m/3m, writing the same persisted prefs the toggle wrote). (b) The picker source tabs render as the native segmented control (GlassSegment) on iOS 26 — labels only; the underline form with its counts stays on every other platform, an accepted platform-idiom fork since the count's job is done by the visible list itself. What was BUILT AND REVERTED on that direction, so it doesn't come back by accident: a GlassPillButton (.glassProminent with the chartreuse as tint) briefly wore the brand CTAs (Log set, Finish, APill primary/light) and a glass film briefly sat under the picker's One Field on both clients — both were the SKIN, not a control (the CTA has no system counterpart; the field's input was always a native UITextField), and both are back to the app's own drawing. swiftui.tsx documents the CTA decision in place. DEVICE ROUND 3 (Aug 2026), from a phone shot of the picker's source segment: the native segmented control is what the athlete wants MORE of, not less, so the TODAY HUB switcher — Dashboard / Performance / Feed, the most literal segmented control in the product and the first thing on the screen after the brand — is a real SwiftUI Picker on iOS 26 too (apps/mobile/components/aurora/today-tabs.tsx, gated on LIQUID_GLASS_RENDERED like the source tabs, NOT on LIQUID_GLASS_SUPPORTED: below 26 a segmented Picker is the flat UIKit control with no material, and the LiquidSeg lens beats it there). Two decisions inside it. (1) THE NATIVE SEGMENT CARRIES WORDS, reversing the glyph-only rule that holds everywhere else, and only inside the system control. Glyphs were chosen because three labels of three different lengths never centre as a set — but that is a fact about OUR track; the system control lays the three words on exact thirds and centres each in its own segment, which is the platform's own answer to the same problem, and a word says 'where am I' faster than a mark you have to learn. The three marks were deliberately NOT redrawn as SF Symbols to keep it glyph-only: all three were purpose-built to be UNLIKE the system's (the bento is explicitly not the plain 2×2 that means 'all apps', and the kit has no chart glyph at all), and substituting them would also draw the same three marks two ways — the exact drift HubGlyph exists to prevent, since the floating dock goes on drawing them in RN. So the fork is words-in-the-native-segment, marks everywhere else, one accessible name either way (SwiftUI announces the segment's own text, which IS the label the glyph version had to spell out by hand). (2) NO TINT is passed, so the selection stays the system NEUTRAL — the same choice the LiquidSeg already carried (a near-solid neutral pill, chosen over the old chartreuse fill in the user-approved Liquid Glass preview): a hub tab goes nowhere the accent needs to point. The selection HAPTIC moves into the caller, since SwiftUI owns the tap. KNOWN COST, recorded rather than discovered later: the hub swaps the whole screen tree on selection, so the switcher REMOUNTS mid-move — LiquidSeg reaches through that seam with its flightKey flight memory and the lens stays in the air, and a native Picker has no such seam, so its indicator SNAPS to the new segment instead of sliding. The content dissolve and the still chrome are unchanged, and the floating dock's split/merge is untouched. NOT converted: the This-week date filter's LiquidSeg (aurora/range-filter.tsx), because one of its segments INTERCEPTS selection to open the month sheet rather than taking the pill, and a segmented Picker has no way to express a segment that declines to be selected. (It was converted anyway one round later, and then round 4 took both of them back — read on.) DEVICE ROUND 4 (Aug 2026) — THE NATIVE SEGMENTED CONTROL IS RETIRED, and this is the round that draws the LINE this capability was missing. Phone shots of the Today hub and the This-week card showed the same defect on both: the control drew OUTSIDE its own box — the hub pills landed on 'Tuesday, 11 August', the date filter over the card's head, with the month trigger beside it stranded at the bottom of a row whose height it no longer shared. The cause is structural, not a tuning error. A SwiftUI <Host> sizes its RN box from the SwiftUI content ONCE, at mount (matchContents — the prop's own documented limit), and EVERY segmented control in this app mounts before it knows its content: the labels arrive from useLang asynchronously, the date filter's segments are derived from the session history, and the Today hub REMOUNTS on every selection because picking a tab swaps the whole screen tree. So the DRAWN frame never tracked the LAYOUT frame, and — the detail that makes this structural rather than a spacing bug — it missed DIFFERENTLY at each call site: on Today the box reserved its ~60pt under the profile header and the control painted some 70pt below it, while on the This-week card the row took its height from the month trigger beside the track (~38pt) and the control painted above that row's centre. Chasing the exact native offset would answer the wrong question; a control whose height in Yoga comes from a measurement taken before its content exists has no reason to agree with the screen around it at all. So GlassSegment is DELETED from swiftui.tsx (with a tombstone comment in its place), ASegment is LiquidSeg on every platform, the Today hub is back to its three marks with flightKey flight memory, the date filter's month is a SEGMENT again with its intercept, the nutrition picker's four sources are back on the underline form WITH their counts, and the heat sheet's three preset rows are the app's own again. GUARDED, because this one is a single line of prose away from being re-added — lib/design-tokens.test.ts fails on any code reference to GlassSegment or to pickerStyle('segmented'). THE OTHER NATIVE LEAVES ARE UNAFFECTED and all stay: they either size against content they already have (NativeStepper, NativeDateField, GlassSelectMenu, GlassNavButton, GlassSatellite, GlassMenuButton) or sit inside a sheet that gives them a height (GlassWheel). The direction round 2 set — native where an element maps to a real system CONTROL — survives with one clause added: and where that control can take its size from RN rather than hand its layout to a mount-time native measurement. ONE SHARE MARK, AND THE SUMMARY'S SHARE JOINS THE SATELLITES (Aug 2026). The finish summary's Share was the last control on the funnel still drawn by hand: a chartreuse pill with a `\u2197\uFE0E` TEXT CHARACTER in it, RN on every platform, sitting between two ASatellites. So the screen an athlete reaches by finishing a workout answered the thumb with plain RN where a RECIPE cover — one tab away — answered it with real interactive glass and the platform's own `square.and.arrow.up`. Two changes, both in the shared layer rather than at the call site. (1) SHARE_MARK (apps/mobile/components/aurora/share-mark.ts) names the pair ONCE — the SF Symbol and the house vector (an arrow leaving a tray, the same drawing) — and the recipe's four HeroActions and the summary's two satellites now read it instead of spelling it out; the app had three marks for one verb (`\u2197\uFE0E`, `\u2197`, the symbol) and none of them shared. (2) The share itself is ASatellite, which means it is a real SwiftUI Button wearing interactive Liquid Glass on iOS 26 and the same rim as its neighbours everywhere else. It keeps the primary's WIDTH through a new `fill` word capsule: the cluster's proportions were never what the chartreuse fill was for. `fill` is also the round-4 clause honoured rather than dodged — a `matchContents` capsule measures its content ONCE at mount, and this word arrives from the language preference read off disk after the first frame, so a filled capsule takes an RN size on the Host (flex in the caller's row) and lets SwiftUI lay out inside that proposal; the word shrinks to one line on both renderers (`minimumScaleFactor` / `adjustsFontSizeToFit`, floor 0.75) because `summary.shareFirst` is 32 characters in Polish beside two 44pt circles. THE ACCENT SURVIVES AS FOREGROUND, NOT SURFACE: chartreuse on the mark and the word, via the sanctioned `fg` override. The rule it appears to bend — a satellite carries no brand paint — exists so a cluster never shows two \"go\" colours beside its primary, and this cluster no longer has a primary; the screen's one \"go\" still belongs on the share. The guest branch got the same control (its filled surface is Create an account, which is the right one to be the only one). This does NOT reopen the reverted GlassPillButton: that revert protected the brand's full-width CTA as a SURFACE, and this control gave that surface up to become a neutral one. FOLLOW-UP, the same tab's last one: the VERIFIED FOOD page's share — the page whose entire job is to be worth passing on — was a bare 40pt box with a flat ash glyph in it, the only share on Nutrition that was neither the recipe's HeroAction nor anything else shared. It is the recipe editor's control in the recipe editor's slot now (screenHead's `right`), off the same SHARE_MARK, so every share on the tab is one control. THE SWEEP THAT FOLLOWED found share drawn SIX ways in all, and closed the rest: the WRAPPED rail's share was a lime-washed box with a text arrow in it, sitting opposite a HeroNav — HeroAction's own description of itself — and is that control now; the PUBLIC PROFILE page rendered the WORD \"share\" through HeroAccessory, the METADATA component, in the slot HeroAction was built for, while the comment directly above it described HeroAction; and a Settings chip's text arrow is simply gone, since its sibling chip carries no mark and the label already says what it does. NOT converted, and the reason is recorded so it is not 'found' again: feed-card's private ShareOut vector STAYS. Its glyph row is four marks drawn as one set in a 16-unit box at stroke 1.5, and the shared vector is a 72-unit box on auroraIconStroke — 4.5 at this size, which is ~6.3% of its box against the row's 9.4% — so the shared mark would draw a third lighter than the bolt and bubble beside it. A set that agrees with itself beats a mark that agrees with a set it is not in. Coach-invite's \"Share\" is a word beside \"Revoke\", a matched pair of text actions, and stays a word. GUARDED (lib/design-tokens.test.ts), in three parts, one per shape the drift actually took: the SF Symbol may appear only in share-mark.ts, so a screen composes the pair instead of respelling half of it; a file that PERFORMS a share may not draw a text arrow — scoped to those files because the character stays legal where it means what it draws (the warmup ramp badge, elevation gain, the external-link marker); and the house vector may not be named by hand as a glyph/mark value, which is the FOOD PAGE's case and the one a first pass of this guard would have missed — the right vector in a bare box is still a screen reaching past SHARE_MARK for half the pair. All three were verified to fire on an injected regression.", blockedBy: "Native SwiftUI rendering can't be built/verified in this sandbox (no Xcode/simulator, Supabase+native build host blocked) — it needs an EAS dev/preview build (see mobile-preview) on an iOS 26 device to confirm the Liquid Glass surfaces, segmented Picker and glass buttons render and lay out correctly, then to tune tint/opacity on-device. Also confirm HeroNav's native GlassNavButton on an iOS 26 device: the interactive shimmer answers the touch, onPress round-trips from SwiftUI (back must never dead-end), the 44pt contentShape hit circle takes taps outside the 40pt visual, and the clear↔glass identity/regular cross-fade on field screens. Same build must confirm the ⋯ Menu leaf (menu opens on tap, rows fire, destructive tint, zoom morph), the month wheel (settle applies the pick, sheet stays up, labels localize), and the minimize circle. Round 2 adds: the select-menu triggers' Archivo/mono labels resolving (expo-font family names must reach SwiftUI's Font.custom — if a trigger falls back to the system face, pass the postscript name instead), the meal menu's inline Picker checking the meal in force and Add a meal part opening its sheet, the rest menu writing the prefs the toggle wrote, and the source segment switching tabs. That now includes the Today hub dock's GlassPillRow: on device, confirm the three capsules fuse at gap 0 and separate on arrival, that the selected pill's glass STRETCHES into its word rather than snapping, that the lime glass tint reads as a tinted pane (not a coloured film), and that the RN glyphs sit dead-centre on their capsules through the whole morph — the one thing two matched springs cannot prove without eyes on it. Round 3's checks — the Today hub and the date filter as native Pickers — are RETIRED rather than pending: the device shots answered them, the answer was that the control does not lay out at all, and round 4 deleted it (see detail). Nothing about the segmented control needs a device now; it is RN on every platform and covered by the JS suite. HUB_DOCK_FLOOR (116) is measured against the 44pt RN track again, which is what it was written for. Until the next build only typecheck + JS bundle are verified for what remains. ONE OWNER PER LEAF (Aug 2026). The leaves are the app's most copyable objects — a screen can mount GlassNavButton itself, pick its own diameter and its own fallback, and look native while agreeing with nothing — and the live logger did exactly that: a 34pt minimize circle at chalk-6% whose own comment called it \u201cthe same control family as HeroNav's back circle\u201d while HeroNav draws 40 in a 44 hit box from HERO.nav, on BOTH clients, in the same words. The logger's chevron is HeroNav now (mode=takeover for the chevron-down a cover leaves by, plus a `label` override because this one MINIMIZES where a dismiss closes), and the satellites are ONE component per client (aurora/satellite.tsx, geometry + floor rim from core SATELLITE): mobile hands the whole button to SwiftUI where the material renders and draws the rim over a GlassSurface everywhere else, web draws the same alphas in CSS. That collapsed four recipes inside the logger alone — a 44pt dock satellite at chalk 6/14, a 54pt summary orb at 8/14 with its own on-state, a toolbar capsule at 5% inside `line`, and a word-only Keep-going pill — into one control at one size, so the dock's cluster and the finish summary's cluster are finally the same sentence drawn once. Also folded in: the live scoreboard's PR cell was hand-rolled beside its three LiveStat siblings (which is how it became the only figure that did not roll), and the logger's add-exercise door wore a FILLED chalk plate where DoorRow and the rail tail draw a bare `line` ring. GUARDED by lib/design-tokens.test.ts: GlassNavButton belongs to aurora/hero.tsx and GlassSatellite to aurora/satellite.tsx, and mounting either anywhere else fails the suite. The share satellite needs the same eyes as the rest: on an iOS 26 device, confirm the FILLED capsule draws inside the RN box it was given (the one thing a filled capsule can still get wrong, and the exact failure that retired the segmented control), that the chartreuse symbol and word read on the glass over the finish field, and that the word SHRINKS rather than truncates in Polish." },
  { id: "native-face-resolution", area: "Mobile", status: "shipped", title: "The SwiftUI leaves draw ARCHIVO — not San Francisco", detail: "A phone screenshot of the nutrition head: 'Breakfast ⌄' set in the SYSTEM font, one row under a status bar and beside a screen of Archivo. It had been 'fixed' more than once — the picker's hero pass (nutrition-picker-consistency item 8) moved BOTH branches of the meal switcher onto HERO_INLINE_TITLE (F.bold at fs.subtitle), the prop was passed, the code read correctly, and the glass kept showing the wrong face. THE CAUSE, three layers down and silent at every one of them: `F` holds expo-font ALIASES ('Archivo_700Bold'), which are not font names — expo-font makes them work by SWIZZLING `UIFont.fontNames(forFamilyName:)`, and React Native's text path is the only thing that asks that method. @expo/ui's `font({ family })` modifier ends in `Font.custom(family, size:)`, i.e. Core Text directly, which knows the face only by the PostScript name in the binary ('Archivo-Bold'). `Font.custom` with an unresolvable name does not throw and does not warn: IT DRAWS SAN FRANCISCO. So the alias reached SwiftUI, resolved to nothing, and fell back — which is why every screenshot disagreed with the code and nothing in the repo disagreed with either. FIVE SURFACES were affected, all of them the ones told to wear 'the caller's own face': the nutrition head's meal switcher (the reported one), the logger's set-type menu and its timer capsule's readout, the satellite's word, and the quick-sport minutes stepper. THE FIX is one translation at one boundary: `F_POSTSCRIPT` + `nativeFace()` in apps/mobile/lib/ui.tsx, applied at every `font({ family })` inside swiftui.tsx. Call sites keep passing F.bold / F.mono exactly as they pass them to an RN <Text> — a call site that has to remember which of two names a face goes by is a call site that will forget. A satellite with no family now asks for the SYSTEM face at that size explicitly, rather than reaching it by way of `Font.custom('')`. GUARDED, because this is the failure mode a guard is for — the defect is invisible in code review and visible only on glass: apps/mobile/lib/native-face.test.ts parses the `name` table (ID 6) out of the .ttf files @expo-google-fonts actually ships and fails if any map entry stops matching the binary (so a package bump that renames a face breaks the build instead of quietly falling back), asserts the map covers every entry in `F`, and greps swiftui.tsx so a new native leaf cannot pass a family through raw. THE STANDING LESSON, and it is the one this repo already wrote down: a design claim stated in prose is a claim nothing enforces — and a silent platform fallback will keep re-opening a bug that every diff says is closed." },
  { id: "liquid-glass-segments", area: "Core", status: "shipped", title: "Liquid Glass segmented controls (Today hub + date filter)", detail: "The two in-screen segmented controls — the Today hub switcher (the three HUB_GLYPHS marks) and the This-week date filter (Week / 7 days / 30 days / YTD / Month) — now behave like the iOS 26 SYSTEM control, per the user-approved interactive preview (artifact, 'rest pill, glass on touch'): AT REST the selection is a quiet near-solid NEUTRAL pill (the chartreuse fill is retired from these two controls — the user chose system-neutral over the brand tint when offered both); ON TOUCH the pill inflates past the track's edges into a clear glass lens with a hairline rim, DRAGGING scrubs the lens across segments (select on release, selection haptic on mobile), and a TAP sends the lens over glassy IN FLIGHT, condensing back into the pill on landing — SwiftUI's default spring (response 0.55 / dampingFraction 0.75 ≈ stiffness 130 / damping 17). ONE shared primitive per client: apps/mobile/components/aurora/liquid-seg.tsx (PanResponder scrub that yields to vertical scroll + native-driver Animated springs; honours useReducedMotion) and apps/web/components/aurora/liquid-seg.tsx (pointer capture + rAF spring physics with gel stretch; honours prefers-reduced-motion; CSS-only initial position so SSR paints the pill in place), consumed by aurora/today-tabs.tsx and aurora/week-verdict.tsx on BOTH clients. The date filter's Month segment INTERCEPTS selection (opens its picker sheet; the pill springs home and only lands on Month once a month is in force) — same contract on both clients. THE LENS MATERIAL: on iOS the inflated lens is REAL native SwiftUI glassEffect via the existing GlassSurface — always on, no user toggle (see swiftui-kit); on Android and web a translucent simulated lens carries the identical interaction — on web by construction, since a browser has no system material. Verified: core tests + both typechecks + web build + iOS export bundle. The native-glass lens itself shares swiftui-kit's blocker: confirm on an iOS 26 device with the next TestFlight build. BOTH CONTROLS ARE THE SYSTEM'S OWN ON iOS 26 NOW (Aug 2026, device round 3 — see swiftui-kit): where the material renders, each is a real SwiftUI Picker + .pickerStyle(.segmented) and the LiquidSeg lens described above is the treatment for Android, iOS < 26 and web. The Today hub switcher goes over whole and swaps its marks for the three WORDS inside the native control (reasoned out in today-hub-tabs). The DATE FILTER goes over as its FOUR PRESETS ONLY, and the month leaves the track — because it must: a segmented Picker only reports a selection that CHANGED, so a segment that is already selected can never be tapped again, and the Month segment's whole job is to open its sheet on EVERY tap. Left in the native control it would go dead the moment it was used (pick July and August becomes unreachable), so LiquidSeg's `intercept` is the one thing the system control cannot express. The month is now its own trigger beside the track ('Jul ▾', the app's own mono drawing at the 11pt the segment labels took, since a word that opens a sheet has no system counterpart) opening the SAME sheet as before — so the system month WHEEL shipped in device round 2 is untouched and still the way months are chosen on iOS 26. While a month is in force NO segment is lit: none of the four presets is the period, the trigger is, it says so in chalk, and the block's head prints the month and its span directly above it. NOT changed: the shared choice/store, the segment list and the lit-segment arithmetic all still come from core's activity-range-view.ts, so the two clients still cannot drift on what the control MEANS — only on how it is drawn. AND THEN BOTH CAME BACK (Aug 2026, device round 4 — see swiftui-kit for the mechanism): the native Picker drew outside the box Yoga gave it, over the heading beneath the hub and over the This-week card's own head, because a SwiftUI Host takes its RN size from the SwiftUI content once at mount and both controls mount before their labels are translated — the hub then remounting on every selection on top of that. GlassSegment is deleted; the LiquidSeg above is the segmented control on every platform, which restores exactly the two things the native fork had to give up. The Today hub keeps its three MARKS and its flightKey, so the pill stays in the air across the remount instead of snapping. The date filter's MONTH IS A SEGMENT AGAIN, fifth in the track, intercepting to the same picker sheet on every tap — including the taps after a month is in force, which the system control silently dropped — and the pill rests on it while that month is the period, so the lit segment and the head finally agree. The system month WHEEL inside that sheet is untouched. GUARDED by lib/design-tokens.test.ts: any code reference to GlassSegment or pickerStyle('segmented') fails the suite." },

  { id: "context-menu-previews", area: "Mobile", status: "blocked", title: "Long-press previews (native ContextMenu) — the feed-card trial", detail: "Peek-and-actions: hold a feed row and it lifts off the receding screen with the same menu the ⋯ opens riding under it. BUILT (Aug 2026) as the ONE sanctioned, contained exception to the kit's composition rule: @expo/ui's ContextMenu.Trigger requires the pressed content INSIDE the native Host, so swiftui.tsx GlassContextMenu carries the RN card in an RNHostView — allowed because it contains its own risk. Containment: (1) a WATCHDOG — the RN child reports its first layout, and if none arrives within 1.2s the native wrapper is declared dead and the children remount as plain RN (the failure case costs a beat of blank, never the content; a false positive merely loses the long-press); (2) the trial surface is ONE card type — the feed card (feed-menu.tsx FeedContextMenu wraps the row body; feed-card.tsx splits its full-bleed frame from its body so the preview snapshot carries the row, not the stream's divider). The rows and handler are the same useFeedMenu instance the ⋯ uses, so the two doors cannot fire differently. The ⋯ remains the accessible door everywhere; the long-press is additive. WEB PARITY in the same change: pointer-hold (~500ms, 8px drift cancels, the ending click swallowed in capture) opens the same glass menu on the web feed card. Rollout to plan covers / history rows / exercise tiles waits on the device verdict below.", blockedBy: "An iOS 26 device build (next TestFlight) must prove the seam before this leaves trial: the feed still scrolls cleanly with cards in native hosts (FlatList mount/unmount churn), taps pass through to the RN pressables inside (avatar, zones, kudos), long-press lifts the card with the menu and does NOT fight scroll or the row's own press, the nested Host (the ⋯ Menu inside the hosted card) still opens, and the watchdog stays quiet on real devices (no false fallbacks). If the trial fails, GlassContextMenu is deleted and the entry returns to planned with what was learned." },
  { id: "cta-pill-convergence", area: "Mobile", status: "planned", title: "CTA convergence — hand-rolled chartreuse pills onto APill (ratcheted 49 → 31)", detail: "An audit for 'are all buttons consistent?' found ~35 bespoke `backgroundColor: lime` Pressables across 17 files that never went through APill at all (nutrition.tsx alone has 12; workout-wrapped 3; train, sport-page, mfa-settings 2 each; plus tour, quick-sport, week-rail, progress, nutrition-panels, logbook-rail, home, exercise-picker, body-progress, notifications, invite), each hand-rolling its padding (8-16), radius (14/16/999) and face (mono/bold/black). MEASURED AND RATCHETED (Aug 2026): apps/mobile/lib/design-tokens.test.ts now counts lines carrying a lime background AND a pill/rounded radius outside kit.tsx, and caps them — 49 at first measurement, tightened to 42 and then 31 as batches landed, and the cap may only ever fall. 18 CTAs migrated across 17 files so far, taking APill from 61 call sites to 80; four of them were the spinner-swap bug (session-edit, mfa-settings, body-progress and quick-sport replaced their label with an ActivityIndicator or an ellipsis mid-save, so the button resized under a resting finger and VoiceOver got no busy state). REMAINING AND DELIBERATE: 15 sites under 14px vertical padding would grow visibly against APill's 18 and want a device round rather than a blind edit \u2014 though the reason they had nowhere to go is now GONE: APill gained `size=\"compact\"` (content-sized, fs.note, lighter padding, with minHeight: HIT_TARGET on the same node so only the DRAWING shrinks) when the social kit was retired, and that is exactly the size class these 15 were hand-rolling. What is left is a migration rather than an open design question, and the burn-down now dates it 2026-12-31; 5 are fixed-width or carry a glyph beside the label, which APill's string label cannot hold. The earlier '~35' was an eyeball; a separate count of 80 'lime backgrounds' that circulated briefly was wrong in the other direction, since 26 of those are marks and discs rather than buttons. First migration done: quick-sport's Log button, which was the exact case APill's commit state exists for (the label swapped to an ellipsis mid-save and the button resized under the finger still resting on it). ALSO SETTLED HERE: this is not an argument for a native SwiftUI Button. A GlassPillButton wore these very CTAs once and was reverted — the brand CTA has no system counterpart and stays the app's own drawing; APill's neutral `soft` variant is already a real Liquid Glass surface, which is the half of the pill that genuinely wanted the material. This is pure design-system drift, independent of any native work — the brand CTA deliberately stays the app's own RN drawing on every platform (no system counterpart; see swiftui-kit device round 2). The sweep: route each through APill, collapsing the drift the same way the retired `Button` merged in. Screen-by-screen with eyes on layout — several sit in flex rows or carry icons/saving states — not a blind regex pass. Web twins converge on the web kit pill in the same change where they drift the same way." },
  { id: "outline-pill-convergence", area: "Mobile", status: "planned", title: "The OTHER half of the button vocabulary — hand-rolled outline pills onto APill (ratcheted at 23)", detail: "THE FINDING UNDER A FINDING. `cta-pill-convergence` has been counting hand-rolled CTAs since Aug 2026 and its number was never a statement about the button vocabulary — only about the FILLED part of it. Its pattern matches `backgroundColor: C.lime` paired with a pill radius, so a bordered pill with NO fill never touched it: the app's secondary CTA, every Skip, every step-Back, \"Continue on Free\". Those were free to grow the whole time the filled count was being burned down, which is the exact failure the ratchets exist to prevent — and it surfaced not from an audit but from reading one wizard's button row beside another's. A guard's COVERAGE is a thing that needs auditing, not just its count. THE NEW RULE (apps/mobile/lib/design-tokens.test.ts) counts a pill radius + a 1px border + CTA-SIZED paddingVertical, minus anything the filled rule already owns. The vertical measure is the discriminator and it has to be: a pill radius with a hairline is equally a CHIP, a BADGE and a meter track, none of which want APill — a CTA pads 12–24 (APill's own is 18), a chip pads 4–8, a track pads nothing. IT ALSO COUNTS ONE SHAPE THAT DECLARES NO HEIGHT AT ALL, which is the site that motivated the rule. check-in's step-Back carried paddingHorizontal and nothing else — no paddingVertical, no height, no minHeight. It looked correct on screen only because the APill beside it stretched the row; rendered alone it collapsed under the 44dp touch floor, and it was invisible to a padding-based pattern for precisely the same reason it was broken. A pill whose height is a side effect of its neighbour has not been designed. FIRST MEASUREMENT 25, AND 25 → 23 IN THE SAME CHANGE: check-in's Back and Skip are `APill variant=\"outline\"` now, gaining the 44dp floor, PressScale feedback and the a11y contract, and losing the ability to disagree with the pill beside them about how tall a button is. The Skip's comment also stopped citing the retired web wizard as its source. THE BLOCKER IS GONE: APill grew the `glyph` slot (see apill-glyph-and-ink) and the six icon-bearing sites converted with it, 23 → 17. THE REMAINING 17 are label-only outline pills across nutrition, workout, pantry, progress, run-track, user-recipes and nutrition-panels. They need no component change — this is call-site work plus a device round, since converting them moves several labels from F.monoBold onto APill's F.bold and that is a visible change on screens no simulator here can show. Date held at 2027-08-31." },
  { id: "apill-glyph-and-ink", area: "Mobile", status: "shipped", title: "APill grows a glyph slot and a MEASURED ink — and the second one fixed a WCAG failure on three destructive CTAs", detail: "TWO PROPS, BOTH OF WHICH WERE LOAD-BEARING BY THEIR ABSENCE. (1) THE GLYPH SLOT. `outline-pill-convergence` had to date its burn-down a year out on the grounds that \"several carry an ICON beside the label and APill takes one `label` with no glyph slot, so those want a component change before a call-site change\". Nine buttons drew `<Glyph/> + <Text/>` inside a hand-rolled pill for want of one prop. `glyph` is a FUNCTION of the pill's current foreground, not a node, so the icon cannot be painted in a colour the pill has already resolved differently — the mistake every hand-rolled copy made, passing `C.lime` beside a label the pill had resolved to `txt(palette, C.lime)`. It fades WITH the label rather than beside it: an icon left at full opacity under a \"Saving…\" overlay reads as a third state nobody designed, and sharing one opacity node keeps the width-holder property intact. `AChoice.glyph` takes the same shape. Six icon-bearing outline CTAs converted the same day (check-in's Edit, pantry's Add manually, nutrition's Add product / Save to foods / Create meal, and Train's Build routine — whose ＋ had been a CHARACTER IN THE LABEL STRING, which is how a glyph gets read aloud by VoiceOver as part of the sentence). Outline ratchet 23 → 17. (2) THE INK IS MEASURED NOW, and this one was not drift. `onAccent` is near-black and it is right for every LIGHT accent the brand has — lime reads 11.9:1 on it, amber 8.7, Muskmelon 8.3 — and wrong the moment a caller hands in a DARK fill, which callers do (FEEDBACK.error.fill takes chalk at 7.0 and near-black at 2.6). Meanwhile every hand-rolled destructive pill in the app had made the OPPOSITE mistake: `color: \"#fff\"` on Muskmelon, which measures **2.36:1** — below WCAG AA and below even the 3:1 large-text floor — on \"Delete account\", \"Erase everything\" and \"Leave plan\", the three controls where misreading the label costs the most. Nothing failed because nothing was looking: the ink was a literal at a call site, and a literal has no opinion. APill now picks whichever of the two brand inks actually reads on the fill it was given, via core `inkOn` (a CHOICE between the brand's two inks, not a generated colour — a lightened/darkened ink would pass the maths and leave the palette). packages/core/src/contrast.test.ts holds every fill the app can hand a filled control to the AA bar, asserts the chosen ink is the BEST of the two rather than merely passing, and keeps a receipt of the 2.36:1 that started it. Seven hand-rolled FILLED CTAs in non-lime accents converted with it (Settings' erase + delete, leave-plan, user-recipes' delete, run-track's start/pause, builder's and Wrapped's premium unlocks). Three of them also hand-rolled `{busy ? <ActivityIndicator/> : <Text/>}` — a spinner REPLACING the label, which is the resize-under-a-resting-finger defect APill's `state` was built to stop, still live in the app's two most destructive dialogs. Ratchets: lime pill 35 → 29, hex literal 58 → 51, content-spinner 18 → 15." },
  { id: "guard-coverage", area: "Mobile", status: "shipped", title: "A ratchet on the ratchets — classified counts, dead-exemption detection, and a coverage partition", detail: "THE RATCHETS COULD NOT AUDIT THEIR OWN BLIND SPOTS, and both pill rules failed that way rather than by breaking. The lime rule matches `C.lime` + a pill radius, so the entire OUTLINE CTA vocabulary was invisible to it and grew unwatched while the filled count was being burned down. Both rules together still could not see a hand-rolled FILLED pill in any other accent, so seven destructive / premium / amber CTAs sat outside every guard — three of them at 2.36:1 (see apill-glyph-and-ink). Neither miss was a ratchet breaking; both were the ratchets being asked a question they had no way to answer, and both were found by a person reading one screen beside another, months later. THREE MECHANISMS SHIPPED, in apps/mobile/lib/design-tokens.test.ts. (1) CLASSIFIED COUNTS. `burnDown` takes `exempt` matchers, each with a reason: hits whose source line matches one are not debt, and the ceiling is measured against what remains. A ratchet's number is only a statement about debt if everything it counts IS the debt — the lime rule counted nutrition's progress BAR as a pill for months, because a bar and a pill are the same two properties, and that knowledge lived in a COMMENT where no test could act on it. The ceiling now means \"sites that should change\" rather than \"lines the regex found\", so burning one to zero is actually reachable. (2) A DEAD EXEMPTION FAILS THE BUILD, and this is the assertion that earns its keep. An exemption matching nothing is slack of a nastier kind than a loose ceiling: it silences a whole CATEGORY that no longer exists, so the next thing to land in that shape is waved through on the strength of an argument about something else. Same reasoning as \"a ceiling above its count is a budget\" — an exception nobody needs is a permission nobody asked for. (3) COVERAGE, AND IT IS HARD RATHER THAN RATCHETED. Every tappable surface in the tree that draws a pill radius must be CLAIMED: it is APill, or one of the ratchets counts it, or it matches a declared NOT-A-CTA shape (a glyph disc, a chip/badge, a measured square — each a CATEGORY with a reason, never a manifest of individual lines, because a manifest is churn and churn gets rubber-stamped). Nothing may be unclaimed. A new hand-rolled pill in a colour nobody thought of fails on the day it lands, naming itself, instead of waiting for someone to read two screens side by side. Hard is affordable only because the sweep that came with it took the unclaimed set to zero; a ratcheted coverage rule would be a budget for more blind spots, which is the one thing it exists to refuse. BOTH NEW ASSERTIONS WERE VERIFIED TO FIRE before landing — a fake blue pill injected into train.tsx failed coverage by name, and a deliberately dead exemption failed the lime rule. A guard that has never been seen to fail is a guard nobody has tested. The pattern generalises past pills: any rule that counts a SHAPE rather than a semantic can be paired with a coverage partition over the universe it means to govern." },
  { id: "swipe-actions-native", area: "Mobile", status: "planned", title: "Row actions behind the standard swipe (native SwipeActions)", detail: "Food rows and history rows carry their star/delete affordances IN the row; the native answer is SwiftUI's swipeActions — clean row, actions behind the drag, full-swipe to commit. Blocked on architecture, not API: @expo/ui's SwipeActions is coupled to SwiftUI List rows, and these rows are RN components (FoodRow renders RN children, which the kit's composition rule keeps out of native Hosts) with FlatList/FlashList virtualization planned (mobile-list-virtualization). Adopting native List for these surfaces would be a rebuild of the row layer, not a material swap — revisit after virtualization lands, or if a contained RNHostView exception proves out in context-menu-previews. Web twin would be pointer-drag reveal on the same rows." },
  { id: "aurora-spectrum-palette", area: "Core", status: "shipped", title: "Aurora Spectrum — palette refresh (chartreuse/teal/sand/terracotta)", detail: "The dark Aurora theme was retinted from a single flat acid-lime to the brand 'Spectrum' coolors palette, applied ONLY as text/accents/ornaments — the dark surfaces (ink #0c0d0c / ink2 / card / line) are unchanged. The five brand accent tokens now hold: lime → CHARTREUSE #c7ef00 (primary action / 'go'), blue → TEAL #3c787e (conditioning / info), amber → SAND #d0cd94 (sport / caution), red → TERRACOTTA #d56f3e (alert / injury / streak); violet #c9a9f0 is kept for the premium/coach slot (the palette has no purple). Source of truth: packages/core/src/theme/tokens.ts (colors) + palette.ts (THEMES.dark.accentText — teal/terracotta are lifted to #6cb6bd / #e58a5c so they clear WCAG AA as TEXT on the card; chartreuse/sand pass verbatim, guarded by palette.test.ts), mirrored in apps/web/app/globals.css (@theme --color-*, the dark --*-text vars, and every hardcoded lime glow rgba 196,240,53 → chartreuse 199,239,0). SPECTRUM SECTION-CODING (the signature): the Today daily-flow sections are colour-coded across the palette so the screen reads as a guided gradient — Train=chartreuse, Feel=teal, Plan=sand, Connect=terracotta — via a shared decorative map @hybrid/core semantic.ts SECTION_COLOR (DailySection → AccentKey, kept apart from the state-colour ROLE_COLOR so meaning can't drift), consumed by the section kickers on BOTH clients (apps/web/components/aurora/today.tsx + apps/mobile/components/aurora/home.tsx); the streak chip wears the warm terracotta accent (pairs with the 🔥) while chartreuse stays reserved for the primary action. The whole app picks up the brighter chartreuse primary for free (buttons, rings, brand wordmark, focus ring, nav active state) since it flows through the lime token. Hardcoded brand-hex literals across shipped surfaces were updated in the same pass for consistency (plans.ts, story-styles.ts, engines/periodization.ts phase colours; web workout-share.ts, landing, nutrition, onboarding, calendar, today-quick, invite/not-found/error/global-error; mobile calendar). JAPANDI (light) is untouched — its accentText stays the warm darkened set and the clay accent fill is preserved (the light theme only overrides --color-lime → clay; its blue/amber/red FILLS inherit the new teal/sand/terracotta, which suit the oat-paper palette). WEB↔MOBILE PARITY: identical token values + the same SECTION_COLOR map drive both clients. Five standalone explorations were rendered in reference/aurora-palette-explorations.html and 'Aurora Spectrum' was chosen. Core tests + both typechecks green. REFRESH (Jul 2026): (1) the primary chartreuse was retuned #c7ef00 → #c6f84f (rgb 198,248,79) — a softer, less-acid green — across every source (tokens.ts, palette.ts dark accent-text, web globals.css --color-lime/--lime-text + the glass-glow rgb, mobile ACCENTS via colors.lime, and the hardcoded literals in web error/not-found/global-error/invite/workout-share + core periodization phase colours + plans.ts + story-styles glow). (2) the LIGHT theme is WARM JAPANDI again, matching reference/today-cockpit-design-concepts/04-japandi.html (the interim cool 'MIST' grey is retired): oat-paper surfaces (ink #efe9df → warm off-white card #fcfaf4, chalk #3a352c, ash #6d665a) with a SOFT WARM hairline (line #e2d9c9) — never pure white, never a hard black box. The brand CHARTREUSE stays the accent (mossy dark-green #47630a as text, bright #c6f84f as fill with dark on-accent text); the display face stays Archivo sans (no clay-accent / no Fraunces serif — those japandi-identity/japandi-serif notes are superseded). blue/amber/red accent-text keep the darkened Spectrum hues. Mirrored in palette.ts THEMES.light + web globals.css [data-theme=light] + mobile paletteFor. WCAG AA guarded by palette.test.ts (677 green). BORDER FIX (the 'black borders are awful' report): web lib/ui.tsx `LINE` was a RAW dark hex (colors.line) used for inline borders, so it painted near-black hairlines on the light Japandi paper — a hard contrast break. LINE is now the themed `var(--color-line)` (soft warm in light, subtle in dark); a new `LINE_HEX` keeps the raw hex for the recharts/SVG spots where CSS var() can't resolve (all ~27 chart stroke/fill call-sites repointed). Mobile already used the themed palette line, so it inherited the warm hairline for free." },

  { id: "japandi-clay-sage", area: "Core", status: "shipped", title: "Japandi light theme — Clay & Sage on Oat", detail: "The warm light theme was redesigned off the acid-chartreuse-on-oat look (harsh: white-on-lime buttons measured 1.34:1 — a WCAG fail — and the bright lime glared on paper while page #efe9df and card #fcfaf4 sat at almost the same value, so nothing floated). NEW PALETTE 'Clay & Sage on Oat': an OAT surface RAMP with real steps so cards lift — ground #eae3d4 → raised #f2ecdf → near-white card #f9f5ec, soft warm hairline #e0d7c6. The PRIMARY action is a muted CLAY #a4543a carrying a warm paper ink #faf6ef (4.99:1); the SECONDARY is a calm SAGE #5f6d4b on the blue/conditioning channel. Chartreuse survives ONLY as small accent-TEXT (mossy clay #8f4a30 on the primary channel, sage #4f5c3a on the secondary), never as a full fill. ARCHITECTURE: added per-theme `accent` (action fill) + `onAccent` (ink ON the fill) to ThemePalette (packages/core/src/theme/palette.ts), so the light theme stops borrowing the FIXED dark-theme chartreuse fill from tokens.ts — dark keeps chartreuse + #0c0d0c ink, light gets clay + paper. palette.test.ts now GUARDS onAccent-on-accent ≥ AA for both themes (the exact pairing the old theme broke). Mirrored across all three surfaces per the web↔mobile parity rule: web globals.css [data-theme=light] (--color-lime→clay, --color-blue→sage, --on-accent→paper, --accent-text/--lime-text→clay-text, --blue-text→sage-text, glass-glow rgb→clay); mobile lib/theme.tsx paletteFor (the accent FILLS now flip per theme — lime→accent, blue→sage on light — and onAccent comes from the theme) + the shared lib/ui.tsx Button/Chip (defaults resolve the primary fill through the palette). ON-ACCENT TEXT FIX: 28 web bespoke buttons used `color: C(\"ink\")` as their on-fill text — an idiom that only reads in dark (near-black on lime); repointed to `var(--on-accent)` (identical in dark, correct paper on clay in light). Core tests (692) + web build + both typechecks green. This SUPERSEDES the aurora-spectrum-palette note claiming the light theme keeps chartreuse as fill. MOBILE parity COMPLETE: the bespoke Aurora screens alias the theme palette as `C` (`const C = useTheme().palette`), so their `C.lime`/`C.blue` FILLS flip to clay/sage automatically once paletteFor maps the accent fills per theme — no per-screen fill sweep was needed (the earlier '~76 raw fills' estimate mistook the palette alias for the fixed brand constant). Two follow-ons landed in the same effort: (1) `txt()` now recognizes the theme's OWN fill values (palette.lime→clay / palette.blue→sage), not just the fixed brand constants, so the ~151 accent-as-TEXT sites resolve to the clay/sage text tones in light instead of falling through to the raw fill hex; (2) ~30 direct `color: C.<accent>` accent-as-text sites across ~10 themed screens (red error messages, amber rating stars, the percent-program load-table numbers + section Band labels) were wrapped in `txt()` so they clear AA on the Oat card — red errors were ~2.6:1 and amber stars ~1.3:1 on paper before. Share cards (lib/share.tsx) intentionally keep the FIXED dark brand (`C` = colors) since a shared image is not theme-dependent. Mobile typecheck green. SETTINGS/NAV FOLLOW-UPS: (a) the decorative per-cluster section-header dots were removed from the mobile springboard menu (web app-shell) + the mobile More tab — the ash label + count already name each cluster; (b) the Appearance theme-picker swatches now preview the real palettes (Aurora ink/chartreuse/ash, Japandi oat/clay/sage) instead of lime; (c) the Settings profile card's leftover lime is themed — the completeness ring (web used a fixed LIME_HEX SVG stroke → now var(--color-lime)=clay), the avatar gradient (hardcoded #9bd400 → the sage secondary), and the Edit-profile chip's lime tint → clay; (d) a new theme-aware `gold` palette token (dark #e6c34e / light #b58a24, decorative so not AA-guarded) gives coach ★ ratings a real Pantone-gold instead of the washed-out sand `amber`, wired across every star site on both clients; (e) the middot ` – ` separators in the Settings row subtitles + profile completeness line now render through the shared MetaLine hairline (matching the rest of the app). (REMOVED Aug 2026: the light theme was deleted whole — see light-theme-removed.)" },
  { id: "kyoto-hour-light-theme", area: "Core", status: "shipped", title: "Kyoto Hour — light theme redesign (washi / sumi / pine / vermilion)", detail: "The light theme was redesigned from 'Clay & Sage on Oat' to KYOTO HOUR, chosen from a 9-concept exploration (design/japandi-light-05…13-*.html; the winner is design/japandi-light-10-kyoto-hour.html). DIAGNOSIS of the old theme: the oat ground #eae3d4 was a MIDTONE, so the surface ramp had no real light in it — cards couldn't float and the brick-clay action #a4543a read as mud sitting in shadow ('cheap and plain… depressing'). NEW PALETTE: a lifted WASHI ramp — ground #f6f3ea → raised #efebdf → near-white card #fcfaf3, hairline #e6e1d2 — with sumi ink text (chalk #2b2a26, ash #6f6b5e), a deep PINE primary action #44584c carrying an ivory ink #f2f5ef (6.95:1), the SAGE #5f6d4b secondary kept on the blue/conditioning channel, and the `red` accent-text channel retuned to the hanko VERMILION #a3442f. accentText: lime→pine-text #3c4f43 (8.41:1), blue→sage-text #4f5c3a, violet/amber unchanged, red→vermilion — every pairing AA-guarded by palette.test.ts (chalk/ash on card AND ground, onAccent on accent). Mirrored across all three sources per the parity rule: core palette.ts THEMES.light, web globals.css [data-theme=light] (surfaces, --color-lime→pine, accent/on-accent/accent-text vars, --glass-glow-rgb→pine 68,88,76, the ambient .lg-field washes retinted to washi+pine #e7e0cc/#d9ddd0/#e3dcc8), and mobile (palette flows from core; the AuroraField light washes retinted to match). DISPLAY FACE: light-theme hero headings now use SHIPPORI MINCHO, a Japanese book serif — web --font-heading swaps to it under [data-theme=light] (Fraunces dropped from the font import; weights 500–800), and mobile serifIf() now returns the ShipporiMincho_500/600/700/800 cuts for the light scheme (F.serif* remapped, @expo-google-fonts/fraunces → @expo-google-fonts/shippori-mincho, loaded in _layout.tsx); Aurora (dark) keeps Archivo everywhere. THE HANKO: a small vermilion seal (力, 24px rounded square, rotated −3°, decorative/aria-hidden) is stamped beside the Today masthead headline — only on the true 'Today', never on scrubbed days, light theme only — web .hanko-seal in globals.css + today.tsx, mobile home.tsx at parity. NAMING: the theme picker label is now 'Kyoto Hour' (was 'Japandi') with real-palette preview swatches (washi/pine/vermilion) on both Preferences screens; the mobile Preferences summary line also dropped a stray middot for the spaced en dash per the separator rule. SUPERSEDES the palette/typography notes in japandi-clay-sage (its architecture — per-theme accent/onAccent, txt() routing, on-accent text fixes — all still stands and now carries pine instead of clay). REFINEMENT (post-live review, 'plain and basic' report): (1) the first cut shipped light ink2 #efebdf DARKER than the ground — an inverted ramp that sank every ACard/ink2 surface into the page; corrected to #faf7ef so the washi ramp rises ground → raised → card. (2) ~45 web aurora card sites hardcoded the DARK-theme shadow (0 6px 22px -12px rgba(0,0,0,.55)) — a black band under washi cards, visibly TRUNCATED at the horizontal-rail scroll clips (the 'cut gradient' report); all swept to a new themed --shadow-card var (dark keeps the black bloom, light gets a soft warm sumi-wash 0 14px 30px -18px rgba(88,74,52,.38)), the coach-rail + feed sliders got internal padding pulled back by negative margins so shadows render inside the clip, and the mobile coach/feed rail cards gained the scheme-aware twin (warm #584934 on light / black on dark). (3) the feed-preview post cards rendered EVERYTHING in one mono voice ('cannot distinguish anything'); the lead (workout name / 'PR') is now the card TITLE in the display face (Mincho on light) with prose sans and mono reserved for the fact line + counts, and the coach-rail coach names + section headers moved onto the display face too — three voices per card, both clients. (4) the readiness faces ('primary school') were redrawn as a crafted kaomoji pictogram set — one consistent 2px round-capped stroke, with the EYES carrying the expression (delight = happy ⌒ arcs, calm/flat = level strokes, wrecked = outward-drooping slants) over the mood mouth; web readiness-face.tsx SVG paths + the mobile plain-View twin (clipped-ring arcs, no new deps), verified by headless-Chromium render. (5) FULL-BLEED RAILS: the Explore coach/community sliders clipped at the CONTENT COLUMN (the web app-shell <main> pads 16px with overflow-x:clip; the mobile AuroraScreen pads 16dp), so cards + shadows vanished visibly before the physical screen edge. The shell now publishes --page-pad-x and both rails take an opt-in `bleed` prop — negative margins the width of the page gutter with matching internal padding, so resting cards align with the column but scroll + clip at the true screen edge; Explore passes bleed on both clients, while the Sheet-hosted Today rail stays column-bound by design. The web rails also set scroll-padding to the same gutter — mandatory scroll-snap ignores the scroller's own padding, so without it the browser snapped the FIRST card to the scrollport start, glued to the bezel at rest. Mechanics (bleed + resting alignment) verified in headless Chromium against overflow-x:clip. The same treatment was then SWEPT across every page-level horizontal rail on both clients: the profile Achievements tile rail (web; the mobile counterpart is a wrap grid, no scroll), the Today 'Exercises — your favourites' widget rail (its centre-snap now centres cards on the physical screen), the Cockpit context-pill rail, and the History ViewSwitcher chip rail. In-card table scrollers (analytics/team/plans etc.), full-width pagers (logger, exercise-page charts) and sheet-hosted rails deliberately stay column-bound. The web profile section heads also moved onto the display face per the SectionHead standard (mobile twin via serifIf). SPARKLINE FIX ('gradient bottom weirdly looking'): the web exercise-widget spark (recharts) pinned a flat/zero series to the card's bottom border — a half-clipped stroke that read as a broken card edge with a torn area wash — and its KIND_STROKE hexes were the DARK-theme accents (Aurora teal on washi). The recharts spark was replaced with the same hand-rolled SVG the mobile card already uses (identical 340×92 path math, 18%-padded domain so the baseline floats, round joins, end dot), and the stroke/up/down hexes are now theme-resolved (kindStroke/upHex/downHex + web useTheme): Aurora keeps chartreuse/teal/sand, Kyoto Hour draws pine/sage-text/amber-text with vermilion for regressions — wired through the exercise page's charts too. (REMOVED Aug 2026: the light theme was deleted whole — palette, serif face, hanko, retints and the theme picker are all gone; see light-theme-removed.)" },
  { id: "light-theme-removed", area: "Core", status: "shipped", title: "Light theme (Japandi / Kyoto Hour) removed — the app is dark-only", detail: "The Japandi light theme (latterly KYOTO HOUR — washi/pine/vermilion) was deleted whole, on request: AURORA (dark) is now the app's one theme. CORE: ThemeName narrowed to 'dark', THEMES.light deleted from theme/palette.ts, heroStatusBar() deleted (a dark app's status bar content is always light). WEB: the [data-theme=light] override block, the light navglass/lg-field/ff-disc retints, the hanko seal (.hanko-seal + the today.tsx mark) and the Shippori Mincho font import all removed from globals.css; lib/use-theme.ts (cookie + toggle) deleted; the app-shell header theme toggle and the Settings Appearance section removed; layout.tsx no longer stamps data-theme; color-scheme is dark. MOBILE: lib/theme.tsx is a static dark provider (ThemePref/system-follow and the AsyncStorage pref removed); the Settings Appearance picker removed; serifIf() and the F.serif* Shippori faces deleted (headings read Archivo directly; @expo-google-fonts/shippori-mincho dropped); the home.tsx hanko, the AuroraField/FinishField washi retints and every scheme===light branch (shadows, BlurView tints, cover bleed gates, EffortSplitBar alphas) collapsed to the dark values. I18N: w.account.settings.appearance/theme-dark/theme-light removed (EN/PL/DE); settings-nav Preferences subtitle/keywords updated. Tests updated (palette/premium-accent/hero/feed-typography/design-tokens); the design/japandi-light-* exploration set deleted. RETIREMENT OVERLAP: this landed alongside the web-client retirement, which deleted most of the web surfaces named above outright (account-settings, app-shell, the aurora/* screens) — the removals still hold for what survives: globals.css, layout.tsx, template-sync, lib/ui.tsx and the admin panel carry no light theme, and web ships no theme toggle because it ships no user UI. Mobile is the whole live surface and is dark-only. Core + web + mobile suites green, both typechecks green, web build and the iOS bundle export clean." },
  { id: "design-system-unification", area: "Core", status: "shipped", title: "Design-system unification — one-accent discipline", detail: "The app's single typographic/colour discipline, enforced after a 'de-AI-slop' pass. THE RULE: within any cluster of PARALLEL items (a stat row, a list of tiles, a set of section headers) the hue lives in ONE small MARKER or carries real semantic meaning — it never floods labels or repeats arbitrarily; parallel values + labels stay NEUTRAL (chalk value / ash label). What's PRESERVED because it actually encodes something: the Aurora section wayfinding (SECTION_COLOR kicker dots, semantic.ts — Train=lime/Feel=teal/Plan=sand/Connect=terracotta), the genuine state/role colours (ROLE_COLOR, danger=red), and the ✦ premium signifier (the SAND ✦ on every Full badge / unlock CTA / locked tile — recoloured from violet, see premium-accent-sand). Only DECORATIVE multi-hue — colour with no meaning — is collapsed. FOUR Today/Settings surfaces swept so far, web + mobile at parity: (1) the Today glance strip (Quick Log – Readiness – Done today) — its three values were lime/blue/amber, now one chalk value colour (today.tsx / home.tsx); (2) the GO FULL section header — the whole label was violet, now ash with the accent kept only on the ✦ marker (that marker is now SAND, not violet — see premium-accent-sand), matching the shared Kicker hue-in-marker pattern; (3) the Settings icon tiles — the per-category TONE map gave every row a different hue (account=lime, preferences=teal, notifications=violet…) with no logic behind the mapping; collapsed to one neutral ash chip, red kept ONLY for the destructive danger section (account-settings.tsx / aurora/settings.tsx). This SUPERSEDES the earlier 'web TONE unified to the mobile multi-hue chip palette' note in settings-drill-in — both clients are now the single neutral chip. (4) the RECOVER & MORE header + its Nutrition/Follow-a-coach rows — the header gained a neutral ash marker dot so all three Today headers share the marker+ash-label structure, and the two DeferRow icon chips (blue/violet) were unified to the neutral ash tile (DeferRow now colours its glyph from the token directly, since there is no --ash-text var). Both typechecks green on every pass. SEPARATOR PURGE — the training-plans surface (web + mobile at parity): the middot ` – ` separator (read as AI-slop) is gone from every plan surface. A shared MetaLine helper (apps/web/components/aurora/meta.tsx + the mobile twin) is now the app's ONE consistent way to divide inline PEER FACTS — a thin vertical HAIRLINE rule (1px, currentColor at ~0.32 opacity) instead of a dot, so it reads as structure not punctuation; it accepts either an array of `parts` or a `–`-joined `text` string (split internally) so a stored/i18n meta with ` – ` renders as hairlines WITHOUT changing the data. Wired across: the Train launcher ListRow meta + Repeat-last meta, the Explore plan-preview cards (p.meta), the Plans list/detail/percent-detail header metas (weeks │ ×/wk │ tag │ level │ peak-note), and the Today prescription meta (goal │ block │ wk) on both clients. PRIMARY+ANNOTATION joins (not peer facts) were instead reworded to PARENTHESES — the hairline stays reserved for peer-fact meta lines: the plan-detail day-card band labels (`Main (3 exercises)`, `Strength/Accessories (n exercises)`), the AM/PM session band (`PM (12k)`), and the prose/week prescription+note detail (`… (cue)`) in web program-days.tsx + mobile percent-program.tsx. HARDCODED ELEMENTS removed on the same surface (new i18n, en/pl/de): `train.aiCoach` replaces the literal 'AI coach' eyebrow (both clients' PremiumHero) + the mobile Start-button caption fallback; mobile `plans.title`/`plans.popular`/`plans.perWk`/`plans.week1`/`plans.wkShort`/`plans.enrollError` replace the hardcoded 'Plans' heading, 'Popular', '×/wk', '1 week', 'Wk', and the 'Couldn't enroll…' error; web `w.train.plans.week1`/`wkShort`/`thisWeek` replace the '1 week'/'Wk'/'this week' literals in aurora/plans.tsx; the ` – AI` dot on the mobile Start button was dropped (the ✦ already signals AI) and the `train.aiReadiness` i18n value was de-dotted ('AI coach – readiness' → 'readiness-aware'). The Explore plan-preview rail no longer duplicates the library in a hardcoded array (custom emoji + tints + drifting names like 'Hansons 5K' / '12-Week Kettlebell') — it now DERIVES from a shared @hybrid/core `PLAN_PREVIEWS` (top plan per goal that has one, carrying goalName + icon + colour), so both clients render the real plan name, the goal's own geometric glyph tinted in the goal colour, and a derived `goalName │ scheme-tag` meta — the preview can't drift from the real plans and the emoji/tint hardcoding is gone. Both typechecks + web build + 690 core tests green. Remaining screens tracked in design-system-unification-sweep. THE SEGMENTED CONTROL, Aug 2026 — one form for switching, and the accent back where it belongs. Three surfaces still drew their own tab row: the nutrition picker's four sources (an underline with a travelling CHALK rule, so no accent, but a bespoke control) and the profile's Overview / PRs / Activity plus the public profile's tabs, both of which wore a 2dp CHARTREUSE rule — the app's one 'go' colour, on a control that goes nowhere, which is the same defect the picker's own first pass deleted from its pill bar. All three are `ASegment` now (the shared LiquidSeg track: neutral lens, springs.lens travel, haptic.selection), and ASegment gained a `meta` slot per option so a segment can carry the count the underline forms were keeping, plus shrink-to-fit labels so equal widths survive unequal words. Each conversion also dropped a hairline that was doing double duty as the following content's top edge — a track is an object, not a rule, and what follows sets its own step. ONE THING THE PUBLIC PAGE NEEDED THAT THE OTHERS DID NOT: its tab set is DERIVED (userPageTabs — a coaching tab only for coaches, activity/people only when results are visible) and the wanted tab is resolved against it (resolveUserPageTab falls back to Overview when a coach stops coaching or a page turns private), so the control is fed `shown` and not the raw `tab` state. A segmented control finds its index by identity; handed a want that is not in the list it would park the lens on segment 0 while the page rendered Overview — the same drawn-frame/layout-frame split that killed the native control, arrived at through state instead of layout. AND THE LAST ONE, the day card's session toggle (week-rail.tsx), which was held back for a product call: a full track may be heavier than a card wants. The call went to convert, and the weight turned out to be the point — a switch that changes what the card below it shows should look like a control, not like two words that happen to be underlined, and its rule was CHARTREUSE while the card's real actions (Start, Do it now) wear the same green three rows down. Converting it found the one thing none of the screen-level rows did: the default track is `ink2`, ONE STEP UP from the screen's `ink` ground, which is what makes it read as a container — and a card is already ink2, so the same track inside one is invisible, identical fill with a hairline. ASegment gained `surface: 'card'`, which steps the track DOWN instead of up: on a raised surface the control is a WELL cut into it, which is also how the system control reads on a grouped background. Not another raised surface — stacking those is how a screen grows six containers of one weight. The rows under it also took a real 12dp step, replacing a 4 that only worked because an underline's descender pad was doing the spacing. THE GUARD LANDED WITH IT, and it corrected the claim it was written to protect. `design-tokens.test.ts` now carries two rules beside the GlassSegment one: HARD, no `accessibilityRole=\"tab\"`/`tablist` outside liquid-seg.tsx (the shared control's own implementation, which has to spell them) — that one is zero and clean, no exception list, because an allowlist is a place for the next one to hide; and a RATCHET at 2 on the underline SELECTION rule (`borderBottomWidth: 2` with a colour ternary on the same line), which is the same defect spelled WITHOUT the a11y role and therefore invisible to the first rule. Writing the second one found two rows nobody had counted: the period switches in trends.tsx and exercise-page.tsx, both a row of labels with a 2dp CHARTREUSE border under the selected one — a segmented control with the track taken off. It shipped as a ratchet rather than a HARD zero for one honest reason: the Trends one shares a band row WITH ITS SECTION LABEL (label left, switch right — a head-level control, not a full-width switch), so a track changes that row's whole shape, and that is a design decision a guard has no business making. THE DECISION WAS MADE and both converted, so the ratchet graduated to HARD in the same breath it was written — the ceiling is zero. On Trends the row survives: the label keeps its natural width, the track takes the rest, and the band's own padding drops 12 → 8 because the track brings a 52dp height of its own (the band grows ~20dp, not ~44). The band is `ink`, the darker ground, so the DEFAULT raised track reads against it — `surface: 'card'` is for a track on an ink2 card, which this is not. The Trends switch also carried a comment calling itself 'an underline that moves'; it never moved (the border was per-tab), which is the third time in this thread that prose outlived the code it described. Every guard here was proved to FAIL on an injected violation before being committed, which is the only way to know a green rule is guarding anything. FINALLY, THE AUDIT THE `surface` PROP DEMANDED: shipping a fix for the invisible-track case and leaving the call sites that already had it would have been half a job, so all thirteen ASegment sites were read for the ground under them. THREE were drawing an invisible track and had been since before the prop existed — Settings' LANGUAGE switcher and the logger's four preference groups (both inside an ACard) and the heat sheet's modality switch (a Sheet panel, also ink2). All three take `surface: 'card'` now. Note what hid them: an ACard is GLASS on iOS 26 and solid ink2 everywhere else, so on the phone a reviewer is most likely to hold, the track was faintly visible and merely looked flat — the failure was worst on the devices least likely to be checked. The other ten sites stand on the screen's ink ground (including the Trends band, which is `ink` inside an ACard) and correctly keep the raised default." },
  { id: "typography-named-styles", area: "Core", status: "shipped", title: "Typography — the named type styles", detail: "THE GAP: `scale.ts` gave the app its type PRIMITIVES (fs's 11 rungs, lh's ratios, tracking, trackFigure, TABULAR_NUMS) and nothing composed them. So `fs.caption` has 591 call sites on mobile and every one independently decides the face, weight, leading and ink that go with it — one caption SIZE and a spread of caption STYLES, which is the same class of drift trackFigure found in twelve hand-multiplied letterSpacings and sheetPadBottom found in four bottom pads. `packages/core/src/theme/typography.ts` is the missing layer: `cut` (sans for every word a person wrote, mono for every value a machine measured), `weight` (four roles, named by job so a face swap does not rename call sites), and `text.*` — 18 named styles that hold ROLE NAMES and resolve through the existing maps, so a change to a rung still moves everything referencing it. It deliberately re-declares NOTHING from scale.ts and carries NO colour (ink is a theme concern and a style is used on the page ground and on a card alike, so `ink` names a role the renderer resolves). ONE LADDER, no parallel numeric scale: a figure and the heading beside it are the same rung in a different cut, which is what makes them optically related by construction rather than by somebody checking — the first cut had five figure sizes of its own and every one landed on a rung that already existed. THREE THINGS IT ADDS. `FLUSH` (1.0) — the leading rung lh lacked: lh.tight is 1.15 and on a figure that buys 7dp of line box at fs.stat that nothing can ever occupy (no second line, no descender past the cap band), which across a row of four stat tiles is a visible band of nothing INSIDE the text node that no padding adjustment explains. `unitFor(figureSize, unit)` — a unit is DERIVED from its figure, never declared beside it, because its size is a ratio of the figure's and there are six figure rungs; six hand-typed unit sizes is how the twelve figure trackings happened. It encodes the law that the figure is mono and the unit is NOT: they must differ in face, weight, size and ink or `92.4kg` reads as one seven-character token and the figure stops being scannable. The 0.42 ratio CLAMPS AT BOTH ENDS ([11,16]) — 0.42×fs.stat is 19, above `subtitle`, and a unit must never reach heading size — so the ratio is a target and not an identity; the test that caught that contradiction is the reason it is written down. And `TIMES` (U+00D7) for the app's most-read string, `100 kg × 5`. GUARDED by theme/typography.test.ts: every style resolves through the shared primitives (a raw number means the ladder has been forked), mono never exceeds 600 (a monospaced 700 closes its counters at exactly the sizes and the arm's-length distance this product is read at), 700 is display-only, uppercase is mono-only at the two smallest rungs, every mono style is tabular and nothing else is, and the retired rungs are unreachable. MIGRATION STEPS 2-5 OF THE SÖHNE PLAN ARE DONE AND EACH SHIPPED ON ITS OWN. (2) TRACKING IS AN EM: the dp map is gone and `tracking(size, role)` derives it from the size — for TEXT the role name went away entirely, because the correct tightening is a function of optical size and a caller could otherwise put a hero's -0.5 on a 13dp row, which three did. Only the two uppercase voices survive as names. 531 sites across 118 mobile files and five core contracts. VERIFIED rather than asserted: the em figures were picked so the dominant sites do not move — label at fs.nano still 0.9 (201 sites), fs.micro 0.9 (48), caps at fs.nano 1.2 (72), text at fs.display -0.5 (19); 340 of 461 sized sites render byte-identically and the largest move is 0.5dp. Two things the typechecker caught that a grep would not: `track` was already taken (lib/track.ts exports an analytics track(name), two components declare a local three-arg animation helper), hence the name `tracking()`; and a size extractor grabbed `Math.round` out of `fontSize: Math.round(height * 0.48)`. (3) `lh.flush` (1.0): a figure has no second line and no descender past the cap band, so 1.15 at fs.stat buys seven dp of line box INSIDE the text node that no padding can explain. The app had already reached for it SIX ways — leading(size, tight) at six sites, a hand-typed `lineHeight: fs.stat` at one (which IS flush, arrived at by eye), plus 50, 44, 35 and a local FIGURE_BOX — and seventeen more figure sites declared no box at all. All 30 trackFigure sites now take it; the absolute-lineHeight ratchet fell 38 → 35. The `FLUSH` constant in typography.ts was DELETED with it: once flush is a real LeadingRole the constant is a second name for lh.flush. (4) `fs.note` (15) and `fs.heading` (20) RETIRED — 186 sites to bodyLg, 59 to headline, 96 files. Earned by DUPLICATED JOB, not by spacing: both pairs were one job under two names. Worth recording that a plausible-sounding rule failed here — 'neighbouring rungs differ by more than a dp' broke three times running, because at 10dp a 1dp step is +10% and reads as a level while at 34dp it is +3%; a ratio floor fails too (20→22 and 10→11 are both 1.10). What replaced it is true: the steps never shrink as the ladder climbs. The hub-title guard also broke and was right to — when heading's sites moved onto `headline` one of them was Feed's COMPOSER, a TextInput rather than a title, so a guard keyed to a token had to say what else makes a line a title. (5) `ty(C, kicker)` resolves a named style into an RN TextStyle, with the (family, weight) → binary map in the pure `lib/faces.ts`. 153 eyebrows migrated — only the shapes that render IDENTICALLY, because the heading weights (Archivo Black 900 → 600/700) are a real visual change that belongs with the face swap. THE FIRST ATTEMPT WAS WRONG AND THE NUMBERS CAUGHT IT: collapsing both eyebrow trackings onto one token silently moved 108 sites 0.9 → 1.2dp, so the pair is named instead — `kicker` (+0.085em) is the label above a card, `overline` (+0.115em) is a section label where the air is the division. One real change came with it and was measured rather than assumed: the inline shapes set no lineHeight and took JetBrains Mono's own 1.320em (13.2dp at fs.nano) against lh.snug's 13 — sub-pixel, and declaring it is what lets Dynamic Type carry leading with size. Remaining hand-rolled eyebrows ratcheted at 172. STILL TO DO: steps 1, 6 and 7, all of which need the retail font binaries — see typography-soehne. The full specification — three directions, the numeral system, the 25 hard rules, the Jony-Ive-style critique and what it removed — is `reference/typography-system.html`." },
  { id: "duration-clock-consolidation", area: "Core", status: "shipped", title: "One clock — formatClock moved to duration.ts, and gained hours", detail: "`interval.ts` owned an mm:ss clock formatter while `duration.ts` owned the app's prose span formatter (`formatDuration`, `1h 17min`) with the argument for how a span reads written next to it. Two formatters for one subject in two files, and the second one was in the interval-timer ENGINE — which is not where a general clock belongs and is exactly how a third one gets written. THE LATENT BUG this surfaced: interval's cut could not print an hour. Its comment claimed it 'supports >59 min as needed', but it only ever divided into minutes and seconds, so a 1h42m session read '102:18'. Consolidated into duration.ts with hours and a `live` flag, and the flag is the reason it is ONE function rather than two: a LIVE clock is zero-padded and never changes width (a running timer that drops its leading zero at the hour boundary shifts every digit beside it — the same defect a proportional numeral causes mid-roll, one level up), while a FINISHED one is read rather than watched, so `1:42:18` is correct in a summary and `01:42:18` there reads like a stopwatch somebody forgot to stop. Math.round preserved from the interval version — a countdown that floors shows 0:00 for a whole second before it ends. The mobile timer's big countdown now passes `live: true` explicitly (it is the one figure in the app that MUST be fixed-width); its 'Total' line is a summary and correctly unpadded. interval.ts keeps a note pointing at the new home rather than a re-export, so there is one name in one place." },
  { id: "typography-soehne", area: "Core", status: "shipped", title: "Söhne — the product's own face", detail: "THE FACE IS SWAPPED, on the EVALUATION cuts. The app bundles seven Söhne binaries from assets/fonts and web self-hosts the same faces as woff2 from public/fonts; Archivo and JetBrains Mono are gone from both clients, and the two @expo-google-fonts packages are removed. THE SWAP ITSELF WAS TWO CONSTANTS — `F` in apps/mobile/lib/ui.tsx and the (family, weight) map in apps/mobile/lib/faces.ts — with NOT ONE CALL SITE TOUCHED, which is the entire return on doing steps 2-5 first. The key names kept their meaning, so the weight correction rode along for free: F.reg → Buch 400, F.semi → Kraftig 500, F.bold → Halbfett 600, and F.black → Dreiviertelfett 700, which is the 900 → 700 drop across 294 sites landing with the face it was designed for rather than being applied to Archivo and judged against the wrong drawing. THREE THINGS THE STEP FOUND THAT THE PLAN DID NOT PREDICT. (1) THE FIRST BUILD WOULD HAVE SHIPPED DEAD. `sohne-extend.py` set `flavor = woff2` unconditionally, so files named .otf contained WOFF2 bytes — unregisterable by Core Text. iOS would have failed to register every face and drawn San Francisco throughout, silently. native-face.test.ts caught it by failing to find a PostScript name, which is exactly the class of failure that guard exists for; the flavour now follows the output extension. (2) STEP 7 IS A NO-OP AND THE PREDICTION WAS WRONG. The plan said Söhne Mono is narrower than JetBrains Mono and every width-from-character-count needed recalculating. Measured, both advance at EXACTLY 0.600 em on a 1000 upem, so MONO_ADVANCE_EM did not move. It is now asserted against the shipped binary by a test rather than left as a sentence — the prediction was wrong in the safe direction and the next face may not be. (3) WEB HAD FIFTEEN HARD-CODED FAMILIES. `lib/ui.tsx` spelled the family in disp/mono/body and thirteen SVG <text> elements across the admin charts spelled it again, so the charts would have drawn Archivo beside Söhne for however long it took someone to notice. All fifteen read `var(--font-display)` / `var(--font-mono)` now; globals.css owns the family, the call site owns the role. THE GLYPH GAP IS THE ONE THING STILL OWED, and it is step 1. The evaluation cuts carry 68 glyphs; `reference/sohne-extend.py` now extends them to 120 — the punctuation, the marks, and the PL/DE diacritics, each a composite of a letterform the font already draws plus a mark derived from its own measurements, so no letter is invented. Measured against the three locales' actual strings that is 99.917% of rendered characters. THE ~30 THAT REMAIN — & ß @ the curly quotes and the pictographs (✓ ★ ✦ ▶ ↑ ≈ ± ÷ ≥ Ø) — are real drawing rather than assembly, and a hand-drawn ampersand beside Klim's letters would look worse than the fallback does; Core Text cascades per glyph so they render in the system face, not as tofu. Dropping the RETAIL files in fixes all of it and lets sohne-extend.py be deleted: replace the seven binaries, edit the seven PostScript strings in F_POSTSCRIPT (the guard reads them out of the file, so getting one wrong fails the build), and re-run. THE MONO ELLIPSIS was a third bug worth recording: three periods spaced at the period's own advance ran two cells into the next glyph in the monospaced cut, because an ellipsis is ONE character and all three dots have to live inside a single advance. Visible only in a rendered proof, like the two before it. iOS bundle export green, 7 otf assets bundled, no Archivo alias left in source." },
  { id: "card-foot", area: "Core", status: "shipped", title: "Card footers — one status line, one link that expands", detail: "THE DIAGNOSIS: three cards on the Performance surface ended three different ways, and sixteen properties were being decided independently in each. Tissue drew a rail of THREE mono controls (two disclosures and a form) at identical weight; Your Level drew a full-width row whose LABEL WAS A STATISTIC ('55% confidence') and whose lime arrow pushed the entire /volume screen; This week's volume drew an eyebrow on the left and a lime CTA with a rotating down-arrow on the right that merely unfolded a drawer in place. Drifting: what a press DOES (expand / expand / push a screen), rule offset (16/14 vs 16/13), label face (mono vs Archivo), size (11 vs 12 vs 10+12), weight (600/400/700), case (UPPER vs Sentence vs both), tracking (.08em vs none vs .12em), the accent, the glyph, the glyph's motion, its gap, the open-state label, the panel's motion, the tap target and the haptic. THE ACCENT WAS THE WORST OF IT: lime meant 'leaves the card' on Your Level and 'unfolds in place' on Volume — one card apart — so it told a reader nothing. The two clients had also drifted from each other: the SAME Tissue rail drew an 8px text triangle that swapped up/down glyphs on web and a 12dp vector chevron that rotated on mobile, and only one of the five controls fired a haptic. THE RESOLUTION is a shape, not a style guide: EVERYTHING IN A FOOTER EXPANDS IN PLACE. Nothing navigates, nothing opens a sheet, nothing acts — so nothing earns the accent, and every card ends the same two-part way: an optional STATUS line above the rule (a figure or fact that qualifies the card, never pressable, never a button's accessible name) and exactly ONE link below it whose label names the noun of what unfolds and does NOT change when it does (the chevron's rotation is the only state anything reports, which retires 'Hide tissues'/'Hide'). Shipped as `CardFoot` in apps/web/components/aurora/card-foot.tsx + apps/mobile/components/aurora/kit.tsx — two props, deliberately no `kind`, no colour, no glyph and no array, so a caller cannot express a second shape. It owns the rule, the offsets, the type (mono 11/600/caps/.08em ash), the 12px vector chevron and its 340ms rotation, the drawer animation, the mobile haptic, and a 44px TAP TARGET (the one non-cosmetic value: none of the five controls it replaces cleared 20px — web set `padding: 0` on its rail buttons, mobile leaned on hitSlop 6). PER CARD: Tissue's calibration stops being a disclosure and PRINTS as a status line ('Risk model v4.2') — its entire payload was that one line, and a control whose whole content is a fact is not a control; its link is now 'By tissue' (`w.injury.byTissue`), because the panel is not an inventory but the same figure broken out per tissue, and the old label had already been truncating to 'HOW IS THIS CALC…' in English before DE/PL; and 'Log an injury' LEAVES THE RAIL for the panel, beside the body figure, since RiskBody's onPick already opened the same sheet with the region PRE-FILLED where the rail link opened it empty — the removal deduplicates rather than deletes. Your Level's `onOpenWorking` is retired outright: a whole screen was an oversized answer to 'why this level?', so the working (every effort the estimate read, the tier threshold, why the confidence is what it is) now unfolds IN the card from estimate.evidence; the Volume screen keeps its own provenance for its own numbers. Volume's two-string label collapses to one — the eyebrow was already the noun of what unfolds, so the lime word was naming the screen the athlete is standing on. NEW ACTION VOCABULARY: `ActionPill` (bordered, chalk, pill) for the one thing a card may do that is not unfolding; it lives inside a panel, never in the rail, so a reader never has to work out whether a footer control will unfold something or open a form. EDGE CASE CLOSED: with no logged training the rows panel never renders, so moving the injury entry inside it would have left an athlete hurt-before-they-have-logged-anything (a case the card's own source calls out) with no way in at all — the empty state now renders RiskBody plus the pill directly, which also fills a card that previously said only that it was still building. i18n en/pl/de: +byTissue, +riskModel, +theWorking; -allTissues, -howCalculated, -hideTissues, -hideDetail. Guarded by apps/web/__tests__/card-foot.test.ts (21 cases), which checks BOTH clients from one suite — because the failure it guards against is precisely the two drifting apart — and fails on a card not using the primitive, a hand-rolled footer rule, an accent applied inside either CardFoot, a colour/glyph/kind knob on the API, a returning open-state label swap or text triangle, an onOpenWorking anywhere, or an empty-state Tissue card that has lost its second way into the injury sheet. NOT IN SCOPE, logged deliberately: the Volume screen still answers the near-identical 'whose numbers are these?' with a Sheet (volume.tsx SourceBody) — a third response to the same kind of question, tracked under design-system-unification-sweep; and Tissue's running-protocol row still navigates to Today, correctly, because a daily protocol (steps, dates, checkboxes) cannot unfold into an analytics card and that row is a status line in the card BODY, not a link in the rail." },
  { id: "card-inset", area: "Core", status: "shipped", title: "The card inset — one number, named on both clients", detail: "THE DIAGNOSIS: the cards on Today read with a visibly tighter top than the cards on Performance, one tab across. Both screens draw the SAME surface (ink2, 1px line, r28, shadow-card) — but Performance takes its inset from the app's card padding (space.xl, 20: the value the mobile kit's ACard is built on, the value web's lib/ui Card uses, and the value the week rail, the logbook rail, tissue-card, level-card and volume all already carried), while three of Today's full-width cards had been hand-rolled with a bare `padding: 16`. Four pixels at every edge, and since the first child of each card is its header row, what the reader actually sees is two screens disagreeing about how far the title sits from the top. The three: the done-floor's own host card (the count-based plan path, where the floor has no week rail to sit in — it also passed `pad={16}` to DoneFloor, overriding that component's own 20 default so its seam hairline bled the wrong container), the feeling card, and the week-verdict card next door (whose detail compartment bleeds the card's padding on three sides, so the literal travelled into the drawer geometry too). FIXED on both clients in the same change: all three now take `CARD_PAD = space.xl`, declared at module scope in aurora/today.tsx + aurora/home.tsx and in both week-verdicts, and every bleed and re-pad inside those cards reads the constant rather than a number. NOT swept, deliberately: the compact rail cards (web StructureCard / mobile's logbook-mode twin) keep 16 — they are a smaller variant throughout (glyph 15 vs 19, title 18 vs 20, tighter shadow), not a full-width card, and the RTP protocol panel is an r20 surface, a different family. This closes on web the same defect the mobile gutter guard was written for (screen-gutter.test.ts): mobile's week-verdict had already NAMED its padding CARD_PAD after a sweep mistook it for the screen gutter — naming pinned which container it belonged to but not its VALUE, which was still 16. Web's twin was still spelling it as literals in three places. Both typechecks + 2501 core tests + 123 web tests green. THEN THE SWEEP RAN ACROSS THE REST OF THE APP (Nutrition, Profile, History, Plans), and the value stopped being per-file: CARD_PAD is now a TOKEN — `apps/web/lib/ui.tsx` and the mobile kit's `aurora/kit.tsx`, both `space.xl`, with ACard built on the kit's — and every card that had declared its own const now imports it (today.tsx, home.tsx, both week-verdicts). WHAT THE SWEEP FOUND, none of it visible from one screen: PLANS was inset 16 on web and 20 on mobile, because web hand-rolls its `card` while mobile renders the identical plan/day/Info cards through ACard — one detail screen, two insets, decided by which client you opened it on. HISTORY had the same split INSIDE one screen: the list view's swipe card was 20 on web and 16 on mobile (same component, two clients), and the alternate views (session cards, agenda placeholders, week cards, the trend chart) were 16 on both — so switching view moved every card's content 4px. NUTRITION's onboarding wizard stacked two SIBLING cards, the trial card at 20 and the free-plan card directly under it at 16; the create-food title plate was an asymmetric 16/16/20; the macro strip was 16 vertical (its 6 horizontal is real — four flex columns). PROFILE's 26-week heatmap, the one full-width card on the tab, was 16 against private-tab's CommandCenterCard at 20. All fixed, web + mobile in the same change. WHAT DELIBERATELY KEEPS 16, now NAMED in the code so the next sweep does not 'fix' it: rail items (the verified-source and recipe rails, the Plans GoalTile), grid + media cards whose image is flush and only the text block is padded (the recipe grid), stat TILES in a row (History's trend Minis), the icon-tile ROW idiom (profile's set-up nudge, private-tab's BodyBlock and Row — a list item, not a surface with a header), cards whose interior is banded rows that pad themselves (percent-program's DayCard, web program-days), and sheets (their own 20, a different container). THE WIZARD OPTION ROW, the one thing the padding pass had deliberately left: nutrition's goal/activity row was radius 28 on web against 16 on mobile — the same control card-shaped on one client and field-shaped on the other — and neither client stated SELECTION the way the app already does. The standard exists and is unambiguous: the onboarding wizard's own option row (aurora/onboarding.tsx `choice` on web, `Choice` on mobile) is RADIUS.field/16 at padding 16, with a 1px border swapping line -> lime and a lime wash at 8% behind it, sitting inside the wizard's r28/CARD_PAD card — so a wizard has a card radius and a field radius and the row is a field. Nutrition was off it three ways: web drew the CARD radius and faked a second border with a `0 0 0 1px` shadow ring; mobile widened its own border to 2 and took a pixel off the padding to compensate, so PICKING AN OPTION NUDGED ITS LABEL 1dp; and neither carried the lime wash. Both now read the standard (radius, 1px border, 8% wash, no compensation), so all four wizard option rows in the app are one control. Not changed, because both clients already agree and it is anatomy rather than drift: nutrition's row keeps its TRAILING radio circle where onboarding's uses a LEADING check. Both typechecks, 2501 core tests, 123 web tests, the web build and the iOS bundle export green." },
  { id: "wizard-option-row", area: "Mobile", status: "shipped", title: "The wizard option row is a component (AChoice), and the goal step keeps the wizard's rhythm", detail: "THE REPORT was a screenshot of onboarding's \"What's your main goal?\" step. FOUR THINGS on it, and three of them were the same thing. (1) THE ROWS TOUCHED. Every other question kind hands its rows straight to `Step`, which stacks them on space.ms; the goal step — the ONLY question with groups in it — wrapped each category in a View with no gap, so inside a category the bordered rows sat flush and their two hairlines doubled into a 2px rule, and the only air on the screen was between categories. The picked row's lime ring touched its neighbours. The rows take the wizard's own gap now and SEPARATION says \"new group\": ms inside a group, ms + md above a category's name, so a name always sits nearer its own options than to the group above it. (2) THE CTA ROW DISAGREED WITH ITSELF. Back was drawn at a hard height: 56 while APill DERIVES its height from its label — 18dp of padding around fs.subtitle plus Archivo's own line box at 16dp (17.41, which iOS rounds to 18), so 54. Two controls in one row, sized by two rules that have never heard of each other. THE COUPLE OF dp IS NOT THE SEVERITY, Dynamic Type is: the label scales to MAX_FONT_SCALE and takes the pill with it, and a hardcoded 56 cannot follow, so the row comes apart furthest for the readers who most need it to hold. (An earlier draft of this entry, and the commit message, put the mismatch at 'five pixels, pill taller'. That was eyeballed off a compressed screenshot of unknown device scale and was wrong in both size and direction; the figures here are computed from the code and the font metrics.) The row stretches now and Back takes its height from the pill, which is exactly what the CHECK-IN wizard next door already did: onboarding was the outlier, not the standard. The scroller is explicitly flex: 1 \u2014 which is what makes the row PINNED, as this screen's header has claimed since the motion pass: without it the scroller sized to its CONTENT, so a short step let the buttons float up under the answer while the long goal list pushed them to the floor, and the wizard's primary action moved between questions \u2014 and the row carries paddingTop, so the clipped last card no longer touches the buttons. (3) THE OPTION ROW WAS THREE ROWS. `Choice` lived inside onboarding.tsx and TWO other surfaces drew their own copy of it — nutrition's guided setup (which cited onboarding BY NAME in a comment as the standard it was drifting from: title a rung small at fs.bodyLg, blurb in MONO at fs.nano, picked label never tinting) and the nutrition goal SHEET (same drift, plus a private 22dp radio that popped in a single frame instead of ACheckMark). The goal sheet and the wizard's step 0 ask THE SAME QUESTION, so \"lose fat\" looked like two different choices depending on whether you were setting up or changing your mind later. It is the kit's `AChoice` at all three sites now — one anatomy, one 120ms cross-fade of border + wash + label, one mark. Its `sheet` prop is a boolean and not a colour (a sheet panel is already ink2, so the resting fill drops to ink): a free colour prop is how a shared row goes back to being five different rows. THEN THE SHEET PICKERS JOINED, and they are the reason there is no SECOND row component. Nutrition's meal-part and serving-unit sheets each drew a pick-one row — a glyph, a label, and a trailing check ICON that VANISHED when the row was not picked, so an unpicked row said nothing about being pickable. Two sites is below the threshold this kit extracts at, and they were already disagreeing with each other about the label's face (F.bold in one, F.reg in the other) 400 lines apart in ONE file. `AChoice` grew an optional `glyph` instead: one prop, and both rows gain ACheckMark's persistent ring, the 120ms cross-fade and the lime wash. Five surfaces, one row. A control that needs prose to stay in step wants to be a component. Raw-borderRadius ratchet 147 → 146." },
  { id: "wizard-back-affordance", area: "Mobile", status: "shipped", title: "One back per row-kind — the word at the foot, the arrow in the header", detail: "THREE WIZARDS HAD THREE BACKS: an arrow glyph in a 64dp box (onboarding), the word \"Back\" in a padded pill (check-in), a 36dp square (nutrition's guided setup). THE ARROW AT THE FOOT WAS THE ONE THAT COULD NOT STAY, and the argument is not taste. The hero system already owns ← for SCREEN-level back (hero-system, hero-system-sweep), so an arrow among the actions at the foot of a wizard made one glyph mean \"step\" on one row and \"screen\" on the row above it — the same defect `card-foot` named when lime meant \"leaves this card\" on one card and \"unfolds in place\" on the next. Onboarding's Back is `APill variant=\"outline\"` with `common.back` now (the string already existed in EN/PL/DE, so the change cost no copy), which also matches check-in's foot exactly and takes another site off the outline-pill ratchet. THE RULE IS ABOUT THE ROW, NOT THE WIZARD. Nutrition's setup keeps its arrow and keeps it compact, because that control sits in the step's HEADER beside \"Step 2 of 3\" — the hero system's own position and vocabulary — rather than among the foot actions where a bare glyph is the odd one out beside a labelled primary. It was drawn at 36dp with no hitSlop, i.e. under the 44dp floor, so it now declares the floor with `hitSlop` rather than growing the drawing. AND ONBOARDING'S BACK NO LONGER EXITS THE WIZARD. `back` fell through to `leave()` at step 0 — `router.replace(\"/(tabs)\")`, which is exactly what the \"skip\" control on the same screen does. Two vocabularies, one destination, on the FIRST screen a new athlete sees. The control is simply absent at step 0 now, which is what check-in has always done (`step > minStep ? backBtn : null`): there is nothing behind the first step but the wizard's own exit, and that exit is already on screen." },
  { id: "step-rail-merge", area: "Mobile", status: "shipped", title: "One step rail, three wizards — and the meaning stays with the caller", detail: "FOUR RAILS, FOUR DRAWINGS. Onboarding drew 5dp at RADIUS.mark on an 8 gap; the check-in wizard 5dp at RADIUS.pill on a 6 gap; nutrition's guided setup a SINGLE 4dp bar with a percentage width (a continuous readout of a discrete thing); recipe-library's cook mode 3dp ticks with no radius. Only ONE of the four animated, only one declared `progressbar` to VoiceOver, and none of the differences were decisions — nobody chose 6 over 8. THE MERGE'S DESIGN IS WHAT IS *NOT* MERGED. The callers genuinely disagree about what FILLS a segment and they are each right: onboarding fills by POSITION, check-in fills by ANSWERS (its own hard-won rule — filling by position drew a complete bar over a check-in holding one answer, the screen asserting \"done\" while the review card underneath showed dashes), nutrition by position across three steps. So `AStepRail` takes `marks: StepMark[]` — done | current | empty — and each caller keeps its rule as the marks it hands in, which is where a rule about one screen's meaning belongs. A `current` segment draws faint (lime at ALPHA.line), which is what check-in needs for \"you are here but you have not answered\"; a caller with no such state never sends it. VoiceOver's `now` is the count of `done`, which is correct under BOTH meanings without the component knowing which it serves. EVERYONE GAINS THE TRAVEL: the fill comes in from the leading edge on springs.slide, the wizard's own spring, so the bar and the step it describes move on one curve — the element whose entire job is to report travel is the one thing that must not snap. Reduce Motion substitutes a cross-fade. The shared segment also drops the onLayout + width state the original carried, since the travel is a percentage of the segment's own width. THE FOURTH RAIL DELIBERATELY DID NOT JOIN. recipe-library's cook-mode ticks are drawn OVER A PHOTOGRAPH, where the rail's `line` track (a near-black hairline) would vanish; that is a different surface class, not drift. Its unfilled tick moved from a raw `rgba(255,255,255,0.18)` to chalk at a tint-scale alpha, which is the part of it that WAS drift. Ratchet: the lime-pill count fell 36 → 35, because nutrition's progress bar had been counted as a hand-rolled pill all along — a bar and a pill are the same two properties, and that pattern cannot tell a button from a lime bar." },
  { id: "hero-system", area: "Core", status: "shipped", title: "The Hero System — one screen head for the whole app", detail: "Every screen head in the app is now ONE object at one of three RANKS, specified once in packages/core/src/hero.ts and consumed by both clients (apps/mobile/components/aurora/hero.tsx + apps/web/components/aurora/hero.tsx). Full spec, including the SwiftUI-native architecture and the migration plan: reference/hero-system.md. THE DIAGNOSIS: three shipped screens had invented three heroes — History (44x44 back square, radius 14, hairline border, IN CONTENT so it scrolled away, title 26 on the same row as the button, an \'Archived\' bordered pill), Workout Wrapped (40x40 back square, radius 12, black-40% fill, fixed at safeTop+6, title 40 — off the shared type ladder — under a gold mono kicker, figure at 96/112) and the Plan/Goal cover (38x38 back CIRCLE, white-12% fill, inside a collapsing bar at safeTop+4, title 31 under a filled white chip). Three sizes, three radii, three materials, three vertical positions, three type sizes, three names for \'what kind of thing this is\'. THE SYSTEM: three ranks — `bar` (the rail alone), `title` (the large-title hero, for INFORMATION pages whose subject is a collection: ambient field, no art) and `cover` (the art hero, for IDENTITY pages whose subject is one nameable thing: fixed-dark ink + accent wash + emblem) — plus `mode: takeover` for a paged full-screen experience (Wrapped), which is a MODE, not a fourth rank, because it keeps every anatomy rule and changes only two things that follow from having no scroll-to-collapse (fixed rail, dismiss instead of pop). A `bar` IS a `title` at collapse 1; a `title` IS a `cover` with its art removed — so the whole system is one container, one collapse track, one rail, one nav button, one title ramp, one metadata language. THE SPATIAL CONSTANT is the RAIL: a 44pt row at safeTop+4, present in every rank and mode, counter-translating the hero frame so the nav button never moves on screen and never scrolls away. ONE NAVIGATION BUTTON: a 40pt circle in a 44pt hit target (circular because it must float over arbitrary art and a rounded rect competes with the app\'s card language; because Apple\'s own art-backed surfaces use a circular glass button; and because a circle survives Dynamic Type without a re-tuned radius), whose MATERIAL — and only its material — varies with what is BEHIND it (`clear` over the plain field, Liquid Glass / white-12%+blur over art or once content scrolls under the rail), never with which screen it is on. Its accessibility label names the ORIGIN, not the action. TYPE: one ramp off the existing fs ladder (cover/takeover fs.hero 34, title fs.display 26, collapsed inline fs.subtitle 16, the takeover\'s one figure 76); a long title steps DOWN a rung rather than running to a third line or ellipsizing; the title block is BOTTOM-ANCHORED so a 1-line and a 2-line title share a last baseline and nothing below the hero moves when a name gets longer. METADATA: exactly three slots (eyebrow above the title = what kind; meta below = facts about this instance; the rail\'s trailing accessory = one label or one control), all in ONE mono/uppercase/11pt/+0.08em voice, joined by heroMetaLine() with a spaced en dash (never a middot). The accent-tinted white CHIP is reachable only over art, where an 11pt mono line has no contrast substrate — tone follows contrast need, never screen identity. BACKDROPS: the rank picks the ground (heroBackdrop() IS the rule), and the light source encodes hierarchy — a container is lit from the top-LEFT, the things inside it from the top-RIGHT. MOTION: exactly three transitions (`lift` a card becomes a hero; `deepen` same subject, more detail, nothing flies; `raise` a page becomes a takeover), all holding the rail fixed; if a move fits none of them the destination has the wrong rank. The collapse itself is finger-driven direct manipulation and is deliberately NOT suppressed under Reduce Motion (only the released-mid-track settle degrades to an instant jump). ANTI-DRIFT: core publishes the geometry (heroGeometry), the track (heroCollapse/heroSnapTarget), EVERY layer\'s interpolation (heroLayers — the thresholds that used to be hand-copied magic numbers like `delta * 0.62` in two separate trees), the type ramp, the metadata language and the backdrop/nav/status-bar rules, all unit-tested (hero.test.ts: 30 cases incl. \'the rail is at the identical y in every rank\', \'the display and inline titles are never both on screen\', and \'every transition holds the rail fixed\'). MIGRATED IN THIS CHANGE, both clients at parity: History (title off the rail row, the archived pill -> rail accessory, the view switcher -> a sub-rail that DOCKS beneath the collapsed bar instead of scrolling away, and the nav button no longer scrolls away; mobile keeps its FlatList via HeroScreen\'s `scroller` slot, so a screen never trades virtualization for a hero), the Plan/Goal/Library/Recipe cover (geometry, detents, nav button, chip -> eyebrow, meta line and title ramp all now read from core — which also closed a real 20px web-vs-mobile drift: web\'s cover was 272 tall against mobile\'s 252), and Workout Wrapped (one rail at the system y, the back square -> the dismiss circle with chevron-down, title 40 -> 34, figure 96/112 -> 76, gutter 24 -> 18; the shared-element clone now reads the same ramp as its destination so it lands on exactly the right type). Core tests (2023) + both typechecks + web build + iOS bundle export green. The remaining screens (Workout detail, Exercise detail, Exercise analytics, Statistics, Profile, and the ~40 sub-pages still on ABack) are tracked under design-system-unification-sweep and hero-system-migration. FIXED (follow-up): the sub-rail DOCKED ON WEB ONLY. Mobile has no `position: sticky`, so it measures where the rail would naturally sit and translates it down from there — but `onLayout` reports y against the rail's PARENT, and HeroScreen nests the rail inside the content's padded wrapper (and inside a custom scroller's list header), so the measurement came back 0 instead of the hero's height. The rail therefore pinned from the very first pixel of scroll and held a whole collapse track too low, leaving History's agenda/weeks/timeline/trend switcher floating with a 76dp gap of scrolling page between it and the collapsed bar. The pin rule is now ONE function in core (heroRailPin, unit-tested including the coordinate-space regression), read by both mobile heroes — HeroScreen adds the hero pad its measurement is missing, the cover screen measures in content space already — and the web's `top: barHeight` is that same rule expressed in CSS (verified in Chromium: bar bottom and rail top meet flush at every scroll offset)." },
  { id: "hero-system-sweep", area: "Core", status: "shipped", title: "Hero System — every screen head migrated, ABack retired", detail: "THE SWEEP IS DONE: every screen head on BOTH clients now comes from the HERO SYSTEM (hero-system, packages/core/src/hero.ts, reference/hero-system.md). ABack — the 44x44 line-bordered square that 45 mobile files hand-rolled a header row around — is DELETED from kit.tsx, so it cannot come back; web's bare `<h1 fontSize: fs.display>` head is gone from every screen too. THE BRIDGE: AuroraScreen (mobile) and HeroScreen (web) take a `hero` prop and hand the whole shell to the system, so a screen is one prop rather than a hand-rolled ABack + AHeading row. Both shells were split so the hero path and the plain path never share a hook order (AuroraScreen -> AuroraPlainScreen), and AuroraField + withAlpha moved to components/aurora/field.tsx because kit routes through hero and hero needs the field — otherwise an import cycle. `back` defaults to router.back() (the default ABack had); `back={false}` renders an empty leading slot on a root screen so the title's y never shifts between a root and a pushed screen. `scroller` hands a screen the scroll props so it keeps its own FlatList — History, Leaderboard and Feed all kept virtualization. HeroScreen also gained refresh, center and a NON-SCROLLING mode (a screen that owns its own flex layout below the hero has no scroller, therefore no collapse track — the same rule, not a special case). MIGRATED (mobile, ~44 screens): Statistics, Analytics, Trends, Volume, Velocity, Video, Force plate, Endurance, Longevity, Progress, Exercises, Exercise detail, Calendar, Check-in, Periodize, Builder, Run track, Sport, Coach, Connections, Connection page, Competition, Talent, Tactical, Team monitor, Team compare, Org, AI coach, Logger settings, Profile edit, Coach apply, Notifications, Interval timer, Admin sections, Coaches, Discover, Leaderboard, Feed, Performance, Settings sub-pages, Login (rank `bar`). WEB PARITY in the same change (16 screens): Checkins, Connection page, Connections, Endurance, Exercises, Force plate, Longevity, Performance teaser, Progress, Run track, Sport, Trends, Velocity, Video, Volume, Statistics — plus web's HeroScreen gained the `back={false}` variant mobile had, which was a real parity gap. JUDGEMENT CALLS, all recorded in the spec: (1) Exercise detail takes rank `title`, NOT the `cover` the original migration plan assumed — the test for a cover is 'one nameable thing with an accent and a mark' and an exercise has neither in this codebase, so promoting it would mean inventing a per-exercise accent system or giving every exercise page the same theme-primary wash (the 'looks like a product page' failure the rank rule exists to prevent); it becomes a cover the day exercises get an accent. (2) Login is rank `bar` — the mark and the form ARE the screen, so a display title would be chrome. (3) Editors that live inside a Card rather than on a screen (the social-profile field editor, Nutrition's sub-views) take the system's nav CONTROL at the system's geometry without a rail, since they have no screen head to own. (4) Four decorative trailing glyphs (Calendar's calendar, Longevity's heart, Check-in's heart, Endurance's GPS pin) were DROPPED rather than moved: the rail's trailing slot carries one label or one control, never decoration. (5) Heads that were already heroes in disguise became heroes — Periodize's lime kicker is an eyebrow and its 'now in <block>' line a meta line; Performance's bespoke 'living masthead' is an eyebrow plus a title; Interval Timer's bordered lockup is a title plus a meta line; Notifications' unread count and Volume's Edit-landmarks toggle are rail accessories. (6) Statistics' weekly-volume readout stayed in the BODY — it is data, not metadata, so it does not ride the rail. Screens that render both standalone and as a Today hub tab (Trends, Volume, Performance, Feed, Nutrition) take a hero only when standing alone and keep their inline masthead when embedded, replacing three spellings of `{!top && <ABack/>}`. NUTRITION RESOLVED (follow-up): the sticky HUD that owned the rail y was REMOVED on both clients (see nutrition-hud), so Nutrition is now fully on the system — the hub takes a title-rank hero with the time-of-day greeting as its EYEBROW (it was a hand-rolled mono line over a hand-rolled AHeading), the diary/insights/body/meals/foods views take the same hero with a back that returns to the hub, and the seven PRESENTED sub-views (food picker, create meal/food, verified source, verified foods, recipes, recipe detail) keep their in-content head but it is now the system's BAR rank: the same 44pt row, the same 40pt circular HeroNav, the same inline-title type and one trailing slot. With the HUD gone, AuroraScreen's stickyTop / stickyTopReserve / onScrollY slot had no users left and was deleted too. Core tests (2010) + both typechecks + web build + iOS bundle export green." },
  { id: "design-token-ratchet", area: "Mobile", status: "shipped", title: "Design-token enforcement — the ratchet that stops the drift", detail: "The iOS design audit's ROOT finding was not that the token system is wrong (it is unusually well authored) but that NOTHING FAILED when a call site ignored it — motion was the one healthy axis precisely because motion.test.ts holds it to its own rules. THE FIX, in three parts. (1) THE TWO MISSING AXES: packages/core/src/scale.ts gained `lh` (4 leading RATIOS: tight 1.15 / snug 1.30 / normal 1.50 / relaxed 1.62) plus a `leading(size, role)` helper, and `tracking` (display -0.5 / normal 0 / label 0.9 / caps 1.2). These had NO token at all, which is why apps/mobile carried 29 distinct absolute lineHeights (nine of them — 15,16,17,18,19,19.5,20,21,22 — all serving body text) and 18 letterSpacings. Leading is a RATIO on purpose: an absolute line box is also why Dynamic Type could not work, since the OS scales glyphs and leaves the box where it was. (2) THE TWO MISSING RUNGS, both previously magic numbers: `fs.headline` (22 — 26 sites, and exactly the 26→22 step-down heroTitleType already documented) and `RADIUS.inner` (12 — 68 sites, the second-most-used radius in the app) plus `RADIUS.mark` (3, for chart bars/ticks written as 1/2/3/4 interchangeably). (3) THE GUARD: packages/core/src/scale.test.ts holds the ladder to its own promises (strictly ascending, nothing below the 10dp floor, headline between heading and display, leading always clears the font size, ratios scale); apps/mobile/lib/design-tokens.test.ts scans the source tree as TEXT and enforces one HARD rule (zero fontSize below 10) plus seven RATCHETS whose ceilings can only ever go DOWN — raw fontSize, absolute lineHeight, raw letterSpacing, raw borderRadius, hex literals, Alert.alert and raw <Modal>. A failure prints the offending file:line list, so it says WHERE, not just that a number moved. The ratchet is deliberately unglamorous: it fixes nothing, it makes the debt visible and stops it growing while the sweeps land." },
  { id: "type-legibility-floor", area: "Mobile", status: "shipped", title: "Type legibility floor — no text below 10dp", detail: "98 text nodes on iOS were set at 8–9dp — below the type ladder's own `nano` rung (10) and below any size Apple ships as readable — and they were overwhelmingly the app's dominant eyebrow style: mono + UPPERCASE + letter-spaced, which is the least legible combination available. Fourteen of them were in app/workout.tsx, the LIVE LOGGING screen, read one-handed mid-set. All 98 raised to fs.nano across 35 files (four of which had to gain an `fs` import). Guarded HARD by design-tokens.test.ts — not a ratchet, because there is no rung below nano and there never should be." },
  { id: "takeover-surface-tokens", area: "Core", status: "shipped", title: "Fixed-dark takeover surfaces named (and a Kyoto Hour break fixed)", detail: "The literals #0e0f0d and #0e100d appeared at 12 mobile sites doing TWO DIFFERENT JOBS, which is why neither was safe. On the Wrapped TAKEOVER they were the deliberately fixed-dark raised surface (a printed object does not change colour because the room did) — now named HERO_TAKEOVER_RAISED alongside the existing HERO_TAKEOVER_INK in hero.ts. On ORDINARY THEMED SCREENS the same literals were a genuine Kyoto Hour BUG: in feel-prompt.tsx one sat on the same line as C.ink2, so the two sibling states of one control landed at opposite ends of the value scale in the light theme (ink2 → near-white washi, the literal → still black); device-import and device-match had the same defect. Those became palette tokens. Separately the four raw-Modal sheet panels using #0e100d became C.ink2 — the colour the real Sheet already uses for its panel — so they match the sheet they should eventually BE. Net: 148 → 136 hex literals, and the ratchet ceiling moved down with them." },
  { id: "hit-target-floor", area: "Mobile", status: "shipped", title: "Tap targets — the 44dp floor across every interactive primitive", detail: "Every interactive PRIMITIVE now declares the 44dp floor — APill, AChip, ASegment segments and the compact admin PillBtn — and design-tokens.test.ts holds that HARD. AChip's selected state also carries fill AND border, never hue alone (WCAG 1.4.1). What remains is per-screen: a touchable hand-rolled inside a screen is still on its own, which is the argument for reaching through the primitives. — The iOS audit measured five selectable pills across five screens at ~25–31dp tall, built from three different horizontal paddings, four vertical paddings and three type sizes — and found exactly ONE `minHeight: 44` in the entire app, with hitSlop on 85 of 473 touchables. SHIPPED SO FAR: lib/ui.tsx exports HIT_TARGET (44) and HIT_SLOP (8) as the stated floor, and the shared primitives declare it — Button, APill (which was ALSO the one button primitive with no accessibilityRole/Label/State at all, so VoiceOver announced the app's primary action as a plain view) and the ASegment segments. REMAINING: the 18 local chip/pill implementations, which is the same work as the AppChip merge below — one geometry, one target, applied once." },
  { id: "vocabulary-one-noun", area: "Core", status: "shipped", title: "One noun for the object — session, not workout", detail: "The design audit found THREE nouns for the app's central object in shipped English copy — session (296), workout (168), training (116) — including two versions of the IDENTICAL empty-state sentence on two screens ('Empty workout — add blocks below, or load a template.' on the builder vs 'Empty session — add blocks below, or pull today's prescription.' on the logger), and a tab labelled 'Log session' leading to a settings category called 'Workout logger'. A user learns a product's vocabulary in the first session; three words for one object teaches them the product does not know its own name. THE VOCABULARY: a SESSION is the object; TRAINING is the mass noun for the activity and never labels an object; WORKOUT has left the interface. 96 English strings reworded across i18n.ts and the six web bundles, plus 7 hardcoded mobile strings and the settings-nav category title. THE ONE EXCEPTION, kept deliberately: 'workout' is Apple's own noun for what the DEVICE recorded, and device.import.lead turns the distinction into the sentence that explains the feature — 'Workouts your device recorded. New ones become sessions'. Six device keys are exempted BY NAME in the guard (i18n.test.ts), because a blind sweep would have destroyed the one place both words are doing real work. NOTE: 'logbook' was flagged alongside this in the audit as a competing name for History — it is not. All 36 occurrences are code identifiers (logbook-rail.tsx, logbookWeek); zero reach the user. That half of the finding was an artifact of counting raw source occurrences rather than shipped strings." },
  { id: "vocabulary-pl-de", area: "Core", status: "planned", title: "Vocabulary sweep — Polish + German", detail: "The one-noun vocabulary (see vocabulary-one-noun) was applied to the ENGLISH copy only. The Polish and German baselines still mix their own equivalents — de has both 'Workout' and 'Einheit'/'Training' for the same object, pl similarly — so each language is currently self-consistent but has not had the same editorial pass. Deliberately NOT swept by the same regex: picking between 'Einheit', 'Training' and 'Workout' in German is a translation decision with register and compounding implications, and guessing it from English is how a localization acquires a subtle wrongness that no test can catch. Wants a native reviewer per language, then the same guard extended in i18n.test.ts." },
  { id: "drawer-collapse-merge", area: "Mobile", status: "planned", title: "Fold percent-program's Collapse into the kit's ADrawer", detail: "components/percent-program.tsx has the third hand-rolled measured collapse (the program accordion's day cards). It is NOT broken \u2014 unlike the two drawers in volume-detail-drawer it never pins its box to 0 while the panel is still unmeasured: open-and-unmeasured falls back to `undefined` (auto), so the panel measures its real height and the interpolation takes over on the next frame. The cost is a first-open POP (the first frame lands at the full height before the spring has anything to animate from), which ADrawer does not have because its panel is measured out of flow and is therefore correct while still closed. Folding it in needs one addition to ADrawer that no current call site wants: a drawer that MOUNTS OPEN must start settled (seed `grow` at 1 and `settled` true) rather than animating itself open on mount \u2014 percent-program opens day 0 by default and re-opens it on every week change. Until then apps/web/__tests__/drawer-measure.test.ts holds it on the register of measured collapses with the escape it relies on written down, so it cannot silently drift into the trap." },
  { id: "one-icon-language", area: "Mobile", status: "shipped", title: "One icon language — the glyph set and the drawn sport marks, emoji nowhere", detail: "The design audit found THREE icon languages sharing one product: the custom line-glyph set, the hand-drawn sport marks (used at exactly TWO sites), and Apple emoji everywhere else \u2014 medals on the leaderboard, trophies in the logger, a barbell for a strength session, faces for how a set felt, sometimes framed INSIDE the app's own bespoke mark tiles. An emoji in a mark tile is the tell: the tile is the app's drawing and its contents are the platform's, at a stroke weight nobody chose, in a colour the palette does not contain, redrawn by every OS. THE EMOJI WERE STRUCTURAL, NOT SPRINKLED, and that is why the fix is a type change rather than a sweep. A dozen core data types declared `icon: string` / `emoji: string` holding a literal pictograph \u2014 the sport catalog's 65, achievements, recipes, meal parts, the effort/fatigue/mood scales, twenty fun-fact tiers \u2014 and every renderer downstream drew it with <Text>. Sweeping the call sites would have left the fields, and the fields would have refilled on the next feature. So the FIELDS changed type: `Mark` (core theme/mark.ts) is a closed union of the two languages that survive \u2014 a glyph name, or a SPORT resolved through sportMark() \u2014 and `string` is not a member, so a pictograph can no longer be typed into one of these fields at all. ONE VOCABULARY, ONE RENDERER: four path maps ship in theme/icons.ts and they are four ORIGINS, not four languages (identical 72-unit box, identical stroke rule); `GlyphName` + `glyphPaths()` let a call site name a mark without knowing which file drew it, and <Glyph>/<SportMark>/<Mark> on both clients are the only ways to put a picture on the glass. WHAT WAS DRAWN, each replacing a named emoji: `medal` (\ud83c\udfc5\ud83e\udd47\ud83e\udd48\ud83e\udd49 \u2014 ONE drawing, the rank is the COLOUR, which is how a medal works and how the palette already speaks), `barbell` (\ud83c\udfcb\ufe0f), `link` (\u26d3), `warn` (\u26a0), `sync` (\u27f2), five FACES (the fourteen emoji spent across three self-report scales, now one drawn ramp with the brow dropping and the mouth turning in order \u2014 a scale, which an emoji ramp assembled from two Unicode blocks was not), four dish marks (leaf/egg/apple/grain, resolved by KIND per sport-marks.ts's rule applied to food: there are not eight distinctive silhouettes between ramen and rice, and the title already says which), and close/barcode/trash/star. THE SPORT MARKS NOW GO EVERYWHERE: they shipped in Aug 2026 and were drawn at two sites while every other surface naming a sport drew the catalog emoji. OlympicSport's `icon` field is DELETED outright \u2014 a sport's drawing is RESOLVED from its name, and a stored glyph beside a resolver is a second answer waiting to disagree with the first. Same for SportIndexEntry, SportPageModel and OtherSportLane, all of which already carried the name. NUTRITION'S DUPLICATE SET IS RETIRED: eleven icons in a 24-unit box at strokeWidth ~2 against the shared 72-unit box \u2014 one screen drawing the app's icons at a visibly different optical weight from the screen beside it \u2014 with a renderer defaulting to \"#fff\", a colour the palette does not contain. Seven were already in the shared set under another name; the other four were drawn into it. Nutrition also shipped a SECOND crescent moon under the same name 300 lines from the first; deleted, dinner reads the kit's. THE COVERS STOPPED WATERMARKING PICTOGRAPHS: a recipe tile drew its dish emoji at fontSize 78 and a cook plate at 118 \u2014 two of the sizes the type ratchet had to except as 'artwork, not type' \u2014 and they are stroked marks now, which is what sport-marks.ts already argued for sport pages (stroke geometry ghosts at 9% white; a colour picture desaturated to grey is a smudge). The exception retires with them, and the type ceiling fell 48 \u2192 44 as a side effect. WHAT IS NOT BANNED, stated so the next pass does not 'finish the sweep' the wrong way: the MONOCHROME TYPOGRAPHIC families are TYPE and stay \u2014 the geometric plan/goal/library emblems (\u25b2 \u25c8 \u25c9 \u2b21 \u25cd \u271a), the guidance section marks, the \u2726 premium signifier the house rules sanction by name, \u2713 \u2715 \u2605, the arrows. They take the text colour, they are set in a font, and no OS substitutes its own artwork for them. THE COST OF THE SWEEP, CAUGHT ON RE-READ AND WORTH RECORDING BECAUSE IT WAS NEARLY MISSED: an emoji is TEXT \u2014 VoiceOver reads \ud83e\udd47 as '1st place medal' and \ud83c\udfc6 as 'trophy' with no effort from the caller \u2014 and a stroked <Svg> is SILENT. So everywhere a pictograph carried meaning ON ITS OWN, swapping it for a drawn mark did not just change the drawing, it deleted the information for anyone not looking at the screen. Three sites did exactly that: the leaderboard's podium (ranks 1\u20133 lost their number entirely \u2014 the emoji had been the rank), and the PR marker beside a lift on the session detail and the share card. The fix is at the RENDERER rather than per call site: <Glyph>/<SportMark>/<Mark> take an optional `label`, matching AMarkTile's existing contract \u2014 pass one where the mark says something its surroundings do not; with no label the mark is declared DECORATIVE and hidden from the accessibility tree, so it is silent by default rather than silent by accident, and a screen reader never announces 'image' at a shape that means nothing. Every icon in the app now declares an a11y stance by construction, which the raw <Svg> never did. GUARDS: design-tokens.test.ts holds HARD that no emoji reaches the glass (pictographic ranges only, with the typographic marks listed by name) and that a mark is never rendered as a <Text>; theme/icons.test.ts holds that the four origins share no name (the drift that already happened once), that every name resolves, that every glyph is drawn in the 72-box and not some other one (what the 24-box kit would have failed), and that the effort ramp is five rungs of ONE head." },
  { id: "one-component-kit", area: "Mobile", status: "shipped", title: "One component kit — the shadow kit retired by build failure", detail: "The design audit found FOUR component vocabularies coexisting, and named the social kit's `SButton` as the sharp end: 23 call sites, ~36dp tall by default and ~28dp `small`, both under the app's own 44-point touch floor, with no accessibilityRole, no label and no state \u2014 and 'invisible to the enforcement ratchets BY NAME'. THAT LAST CLAUSE IS THE FINDING. Every guard in design-tokens.test.ts polices the shared primitives by name: the chip ratchet counts hand-rolls against AChip, the 44dp rule reads a list of primitive files. A second kit is therefore not merely duplication \u2014 it is a blind spot, and everything inside it is unregulated by construction. That is how a button sat ~8dp under the touch floor at 23 call sites for months while a test literally named 'every interactive PRIMITIVE declares the 44dp floor' passed on every run. WHY IT EXISTED, which is the part worth fixing rather than sweeping: APill had no size class. All 23 sites are buttons in a ROW \u2014 a Follow beside a handle, an Accept beside a Decline, a Post at the end of a comment field \u2014 and APill drew one geometry (18dp vertical padding at fs.subtitle, stretched to its parent). Told to choose between the shared primitive at the wrong size and the right size hand-rolled, six screens chose the second, which is how a parallel kit gets born. Migrating the call sites onto a primitive that still did not fit them would have re-created the same pressure. So APill gained `size=\"compact\"`: content-sized, lighter padding, fs.note \u2014 with `minHeight: HIT_TARGET` declared on the SAME node as `regular`, so the drawing shrinks and the TARGET does not. That is precisely the distinction SButton failed to make: it shrank the padding and let the target follow it down. All 23 sites moved and each inherits APill's a11y contract and its commit state (the width-holding label that SButton's `busyLabel` was a partial copy of). social-kit.tsx is DELETED; Avatar, Stars, Empty, initials and levelInk moved into aurora/kit.tsx, because none of them was ever social-specific \u2014 an avatar is a person, an empty state is the app's, not a tab's. What made the file a KIT rather than a folder was its button. RETIRED BY BUILD FAILURE, AS PRESCRIBED, NOT BY A CEILING: a ratchet would have said 'no MORE sub-44 buttons', which is a budget for the 23 that exist. Two HARD rules instead \u2014 the retired path cannot come back and `SButton` cannot be named anywhere (the failure mode is a helpful re-extraction: 'these six screens all need a small button, let me pull one out', which is exactly how the first one appeared), and a component whose name says button and whose body hand-rolls a Pressable is a shadow primitive being born, whatever file it is in. Two exceptions, recorded rather than hidden: admin's PillBtn (already declares the floor; folding it onto APill's compact is `admin-kit-merge` \u2014 66 call sites, a visible change to every operator screen, wants a device) and aurora/swiftui.tsx's native bridges (the platform owns the geometry, which is the entire reason to use them; a native control wearing the app's own drawn button would be the drift, not the fix). The nutrition kit's half of this \u2014 its duplicate icon set \u2014 is under `one-icon-language`." },
  { id: "ratchet-burn-down", area: "Mobile", status: "shipped", title: "Ratchets graduate — 'cannot get worse' becomes 'must reach zero', with dates", detail: "The design audit's verdict on the ratchets was a compliment with a sting in it: 'the ratchets guarantee it cannot silently worsen, which is rarer than any individual flaw' \u2014 and then, in the prescription, 'Ratchets graduate from cannot-get-worse to must-reach-zero with dated burn-down.' THE STING is that a ratchet with no date is a debt with no maturity. Strictly better than nothing, and not a plan. Every ceiling was a promise to sweep something 'later', and later is not a time; held at a ceiling long enough, a ratchet stops reading as debt and starts reading as the house style, which is how 459 raw border radii became a number nobody was embarrassed by. WHAT SHIPPED. `burnDown(found, ceiling, by, rule)` replaces `expectAtMost` in design-tokens.test.ts and makes three assertions where there was one: (1) the count never RISES above the ceiling \u2014 the original ratchet; (2) the ceiling never sits ABOVE the count; (3) after `by`, a non-zero count FAILS. The date is not a wish \u2014 it is what turns 'we know about it' into 'we are doing it', and moving it is a deliberate visible edit rather than the silence of an unmet intention. THE SECOND ASSERTION IS THE ONE THAT EARNS ITS KEEP IMMEDIATELY. A ratchet whose ceiling sits above its count is a budget for more of the thing; an audit in Aug 2026 found 178 sites of exactly that slack across seven rules, and it was fixed BY HAND \u2014 which is the same category of promise as 'later'. The slack accumulates honestly (a sweep aimed at one rule removes sites the others were counting), so 're-measure every ceiling after any sweep' is precisely the discipline a person forgets. It is an assertion now, and it fired on its first run: the icon convergence, a change about pictures, had silently lowered the TYPE ceiling by four and the LEADING ceiling by two. Nothing but a failing test was ever going to make anyone notice. THE LEDGER: a final test prints every dated ratchet, soonest due first, with a total \u2014 375 sites across 10 rules at graduation. That is not decoration. The reason ratchets drifted into house style is that nobody could see them all at once: each number lived beside its own rule, each looked reasonable in isolation, and the total was never written down anywhere. A number nobody totals is a number nobody is embarrassed by. THE DATES, picked from the shape of the remaining work rather than optimism: ASearch convergence (3) 2026-09-30; the four component-convergence rules \u2014 chips (5), section heads (6), meters (6), content spinners (18) \u2014 2026-10-31; raw fontSize (44) and absolute lineHeight (41) 2026-11-30, since both are long tails needing a person per site; hex literals (61) and the hand-rolled lime pills (31, tracked as `cta-pill-convergence`) 2026-12-31; raw borderRadius (160), the biggest, 2027-02-28. The web card-surface ratchet gained the same maturity (15 surfaces, DUE 2026-12-31, tracked as `design-system-unification-sweep`). ONE EXEMPTION, AND ONLY ONE: `floorAt` marks a count that is NOT debt and must not be driven to zero \u2014 the withAlpha remainder is gradient stops and content-tuned scrims, continuous values on a ramp rather than rungs missed, and 'adding rungs until the number reaches zero' would be inventing a scale where none exists. It keeps the anti-slack assertion and drops the date, and the caller has to state why. A second `floorAt` would be a date being dodged, which is the whole point missed." },
  { id: "admin-kit-merge", area: "Mobile", status: "planned", title: "Fold the admin _kit into the shared kit", detail: "components/admin/_kit.tsx is the operator console's parallel design system (D-2 in the iOS design audit). Its ACard/cardStack now come from the real kit and its Segmented/Empty duplicates are next, but the substantial item is PillBtn → APill as a `size=\"compact\"` variant: 66 call sites across 13 files, and a genuine size class (an admin table row cannot carry a 16dp-padded primary pill on every line). THE VARIANT NOW EXISTS \u2014 APill.size=\"compact\" shipped with `one-component-kit`, built for this exact size class, so what is left here is the sweep and its device round rather than the design. The HIG violation it carried IS fixed — PillBtn measured ~29dp and now declares minHeight: HIT_TARGET with PressScale feedback, so the drawing stays compact while the target clears 44 — but the restyle itself is a visible change to every operator screen and wants a device to verify rather than a blind sweep." },
  { id: "type-leading-derived", area: "Core", status: "shipped", title: "Leading is derived from the size, not typed beside it", detail: "apps/mobile carried 287 absolute lineHeight values. 210 of them sat on a line with an fs.* token and were converted to leading(size, role), each snapping to the nearest of the four leading roles. THE EVIDENCE WAS THE FINDING: body/19, caption/17, bodyLg/20, caption/18 and micro/16 — 129 sites between them — were all 'normal' body leading written five different ways, so two paragraphs at the same size read at different densities depending on which screen you were on. FOUR were left deliberately because their ratio is >0.09 from any role (a 1.04 display title, a 2.71 spacer): those are making a point rather than picking a leading, and forcing them onto the scale would have been the regex overruling the designer. 77 remain, all on a RAW fontSize rather than a token — fixing them means tokenising the size first, so they are gated behind the raw-fontSize ratchet rather than hidden. Ratchet lowered 287 → 77." },
  { id: "mobile-kit-merge", area: "Mobile", status: "shipped", title: "Merge the two mobile UI kits (the Liquid Glass split)", detail: "DONE. lib/ui's Card/Screen/Button/H1/GlassField are retired into components/aurora/kit: 231 ACard renders across 72 files on ONE material (Liquid Glass reached 20 admin sections, Feed, Discover, Coaches, Leaderboard and Session detail for the first time), APill absorbed Button's outline variant and colour, AuroraScreen gained an explicit hubTab prop, AHeading reads the Hero System's own title rung. lib/ui is now hooks, tokens and the four primitives the kit never had. Net −198/+35 lines there. — THE HEADLINE FINDING of the iOS design audit. apps/mobile runs TWO complete UI kits side by side: lib/ui.tsx exports Screen/Card/Chip/Button/H1/Kicker/Mono/Loading, components/aurora/kit.tsx exports AuroraScreen/ACard/APill/AField/ASegment/AHeading/ASub. Both are alive and heavily used, and they disagree on padding (Card 16 vs ACard 20), on whether the component ships its own outer marginBottom (Card does, which is why they can't be stacked together), and DECISIVELY on MATERIAL: ACard drops a native SwiftUI glassEffect surface on iOS and Card never can. Since LIQUID_GLASS_SUPPORTED is just Platform.OS === 'ios' with no toggle, this is a permanent split a user cannot reconcile — 234 card instances across two materials, decided by which file the developer happened to import from. The divide runs straight through the consumer app: GLASS on Today/Nutrition/Performance/Plans/Profile, SOLID on Session detail, Feed, Discover, Coaches, Leaderboard, the social profile, the coach tools and all 20 admin sections. On an iOS 26 device the material CHANGES WHEN YOU TAP INTO A SESSION, which is the most visible inconsistency in the product. FIX: retire lib/ui's Card/Chip/Button/Screen/H1 into aurora/kit, keeping ACard's spec and its `solid` opt-out for data-dense read surfaces; move the layout margin to callers; delete the surviving classic-template ternaries in the same change. Roughly half the audit's other findings resolve with it." },
  { id: "mobile-confirm-sheet", area: "Mobile", status: "shipped", title: "One confirm sheet — retire the 38 system alerts and 12 raw Modals", detail: "DONE. components/aurora/confirm.tsx: confirm() (14 sites), notify() (20 sites that had no decision in them at all) and confirmText() — the last replacing Alert.prompt, which was iOS-only and silently DROPPED the moderator's note on Android. All 11 hand-rolled bottom sheets now present through Sheet; tour.tsx keeps its Modal, exempted by name, because a full-screen fade coach-mark is a different presentation. Both rules are HARD in design-tokens.test.ts. — The app owns a Sheet that tracks the finger 1:1, rubber-bands, supports detents, resolves release by velocity projection and recedes the presenting screen — and hands its highest-stakes moments to the OS instead: Alert.alert is called 38 times (delete a session, leave a plan, erase an account, every destructive nutrition action) and twelve surfaces present as raw `<Modal animationType=\"slide\">` (the exercise picker, session editor, device import, device match, quick sport, the share sheet, the coaches profile, the anatomy viewer). Those twelve get the OS slide with no drag-to-dismiss, no detents, no scrim gesture and no parent recede — so the gesture a user learns on Today is DEAD on the controls they touch most. FIX: an AppConfirm built on the existing Sheet with shared copy per action in i18n.ts, and re-present the twelve Modals through Sheet (the API already matches). Both are ratcheted in design-tokens.test.ts so the counts cannot grow while the work lands." },
  { id: "mobile-component-merge", area: "Mobile", status: "shipped", title: "Component merges — chip, section head, meter, empty state, field, search", detail: "PARTLY DONE — chips landed: 18 implementations became two (Chip static / AChip selectable), the six disagreeing axes collapsed, and two chips that painted their label with the raw accent instead of txt() (failing AA on the Kyoto washi) were fixed. Three survivors were renamed for what they actually are rather than merged: MetaPill (a status readout), RailAction (an animated rail item), DayChip ×2 (date tiles). STILL OPEN: 13 bar/meter implementations, 10 section headers, 108 raw TextInputs against AField, 12 unshared search fields, 8 segmented controls, and no skeleton at all. — The iOS audit's redundancy ledger, each entry a set of implementations that solve one problem several ways. 18 CHIP/PILL designs (Chip in lib/ui plus MiniChip ×2, SPill, Pill ×4, Chip ×4, Tag ×2, DayChip ×2, PlanDockPill and a selChip closure) → one AppChip, two variants, one geometry, ≥44dp. 13 BAR/METER implementations (Bar ×2, Bars ×2, BarRow ×2, MeterRow(s) ×2, MuscleBar, StackBar, PaceBars, Spark, percent-program) → a horizontal meter row and a vertical column series. 8 SECTION HEADERS (SectionHead, SectionLabel ×2, Section, SectionHeader, Label, Kicker, admin _kit) → one AppSection absorbing Kicker's 348 eyebrow sites. 2 EMPTY STATES that disagree with each other on padding, size and measure → one spec. 107 raw TextInputs against 5 uses of the perfectly good AField, and 12 search fields with no shared primitive → adopt AField, add AppSearchBar on it. 6 SEGMENTED CONTROLS → ASegment becomes the single entry point, dispatching to GlassSegment on iOS and LiquidSeg elsewhere. No AppSkeleton exists at all, so arriving content pops in behind a spinner (33 Loading() + raw ActivityIndicator in 18 files) instead of reserving its space. NOTE ON PARITY: the web client is scheduled for retirement and the audit that produced this list was scoped to apps/mobile only, so these merges are deliberately mobile-first — the usual web↔mobile parity rule is suspended for them by explicit instruction, not by oversight." },
  { id: "mobile-primitive-sweep", area: "Mobile", status: "shipped", title: "The Medium tier — segment, section head, skeleton, meter, search", detail: "The iOS design audit's Medium findings, and in three of the five READING them before merging changed what the merge was. SEGMENTED CONTROLS 8 → 2: two of the eight (the admin Segmented, History's ViewSwitcher) were wrapping/scrolling chip RAILS, not segmented controls, so they became AChip rows (and cleared the 44dp floor doing it); what actually needed fixing was ASegment's non-iOS fallback, a static pill row, now LiquidSeg — so the gesture-tracked lens went from 2 surfaces to every segment. SECTION HEADS 8 → 1 (ASection): CLAUDE.md already NAMED that standard and it had been re-derived eight times, each agreeing on the shape and differing on title size, face, meta size, tracking and top margin — a standard that lives in prose gets re-derived. SKELETONS 0 → 46: every <Loading /> site was `if (data === null)`, i.e. arriving content behind a spinner; changing what Loading RENDERS fixed all 33 with no call-site edits, and the pulse stops under Reduce Motion. METERS: of '13 bar implementations', five were one labelled horizontal proportion (now AMeter, with the progressbar role and accessible value they never had), three were vertical column charts with real differences, and five were not bars at all — renamed RailSurface / FaceStroke / Rule, because the same misnaming is what hid `Pill` from the chip merge. SEARCH: thirteen fields, no two alike and NOT ONE with a clear button; the four standalone ones are ASearch (built on AField, which gained autoFocus/autoCorrect/onClear). STILL OPEN: the search fields nested inside bespoke rows (nutrition ×3, exercise picker, quick-sport) want their parents restructured; 108 raw TextInputs; the three vertical column charts." },
  { id: "mono-face-discipline", area: "Mobile", status: "shipped", title: "The mono face is for data — a correction to the audit's own metric", detail: "The audit reported that the monospace face was carrying the interface, on the raw ratio: F.mono 1,211 sites against 818 for all four Archivo weights combined. CLASSIFYING the 1,217 sites instead of counting them shows that framing was wrong. 57% are small labels and numerals, 27% are uppercase kickers — 84% is exactly what a mono face is for in an app full of loads, paces, counts and clock times. Only 173 sit at reading size without kicker treatment, and most of those are still data (step.rir, restSec, a set-type badge). FOURTEEN were unambiguously LANGUAGE in a monospace face, and those are the real finding: a coach's note, a coach's reply, a notification body, and the empty-state sentences on analytics/coach/org/plans/talent/team-compare/team-monitor/velocity. User-authored prose set in JetBrains Mono is the case that actually reads wrong; those moved to Archivo and the numerals stayed. The lesson is the measurement rather than the fix — a ratio counted the FACE, and the face was never the question. What the text SAYS is." },
  { id: "feed-timeline-density", area: "Core", status: "shipped", title: "Feed as a timeline + the 12dp screen gutter", detail: "The feed's posts are no longer cards. Each post is a FULL-WIDTH ROW closed by one edge-to-edge hairline (the X/Twitter treatment): no surface, no radius, no inner borders — the row bleeds by the screen gutter so its divider reaches the physical edge, and the action bar lost its own top border since the row's closing hairline is the only line a post gets. Moment still drives weight (a p0 record keeps the display headline + 46px figure); only the container changed. Own posts now carry the athlete's REAL profile name — the feed API no longer relabels them 'You', which read as a placeholder in a stream of names (leaderboard + compare keep 'You', where a ranked comparison makes the label the point). People-search moved off the stream into a bare icon on the tab row (SectionHead idiom), opening the existing Discover screen. DENSITY: every zone gap inside a row is 8 (10 above the actions, 12 row padding), avatar 40 -> 36, set lines 7 -> 5, header modules on 10 — replacing a 10/14 mix. THE GUTTER IS APP-WIDE: the side gutter dropped 16 -> 12 on BOTH clients from one token each — the mobile kit's GUTTER (AuroraScreen applies it to every plain screen; `padding={0}` opts a screen out for one that owns its own scroller) and the web shell's --page-pad-x at mobile widths (desktop keeps 24/32) — with core's HERO.gutter at edge 12 / hero 14, preserving the hero title's +2 optical inset. All eleven hardcoded full-bleed rail pairs moved to -12/12 and coach-rail/feed-preview now read the kit GUTTER instead of a private 16, so the slider rule in CLAUDE.md has one number again. Fixed en route: the feed hub screen was DOUBLE-padded (shell 16 + FlatList 16 = a 32 inset), which is why the feed always sat deeper than Today and why its dividers never actually reached the bezel. Vertical rhythm (16) is unchanged. Web + mobile at parity in the same change. FOLLOW-UP (Aug 2026): the sweep reached every screen that gets its gutter FROM the scaffold, and therefore missed the five surfaces that own their own scroller and hardcode it — Today's hub (its custom entrance + pager, so no AuroraScreen), History's FlatList, the Plans category rail, the live logger and the finish summary. Today stayed at 16 while every rail inside it bled 12, which left a 4dp sliver of gutter beside each cut card and moved the hub chrome 4dp sideways when the athlete switched to Performance or Feed (those DO come through AuroraScreen); web's Today was already 12 from the shell, so mobile was also out of parity. All five now read the kit's GUTTER, the Plans rail bleeds it back out so it is full-bleed like web's sticky nav, the summary's story pager takes its slide width from the content column (a hardcoded 36 drifted against `pagingEnabled`, which snaps by the scroller's width), and the GUTTER token documents that a screen owning its scroller must import it. Deliberately NOT swept: sheets (their own 20) and onboarding (24, a pre-app wizard) are separate containers, and card interiors keep their 16. THEN THE DRIFT WENT THE OTHER WAY, and that is the real lesson: week-verdict's detail compartment bleeds the CARD's padding (16), not the screen's — written as a bare -16 it was indistinguishable from a screen rail, so the sweep 'fixed' it to 12 and inset the compartment 4dp from the card it lives inside, off the figures above it (web, where the same value is spelled --page-pad-x rather than a literal, was never touched). One number, two meanings, no way to tell them apart. So every bleed now NAMES its container — GUTTER for a rail on a screen (eleven rails across ten files), CARD_PAD for one inside a card (week-verdict, week-rail, logbook-rail) — and apps/web/__tests__/screen-gutter.test.ts enforces it: no negative horizontal margin in the container band (12-32dp) may be a bare number, no scroller may set a side padding to a literal 12/16, and a contentContainerStyle using the `padding` shorthand must state the side it silently set. It also asserts its own reach (the surfaces that own their scroller, GUTTER === core's HERO.gutter.edge === web's --page-pad-x), because a guard that quietly stops matching reads exactly like a clean codebase — see accent-channel-guard-gaps. Both directions of the original drift fail the suite. WEB HELD ITS VALUE BUT NOT ITS FALLBACK: a web rail bleeds `var(--page-pad-x, 16px)`, and while the variable moved to 12 with the shell, the 26 fallbacks kept saying 16 — which is not decoration, it is what a rail resolves to in any shell that pads a page WITHOUT publishing the variable. The admin console was exactly that shell (its own <main>, 16px mobile gutter, no --page-pad-x), so a shared component with a full-bleed rail landing there bled a gutter nobody had set. Fallbacks now all read 12px, admin publishes its own gutter (it keeps 16 — a desktop-first tool at maxWidth 1280 — but now DECLARES it, so a portable rail resolves the padding actually in force), and the guard covers web too: no --page-pad-x fallback may disagree with what the app shell publishes, and any <main> that pads responsively must publish the gutter it pads by. Standalone pages (invite, login) pad a fixed value around one centred card and host no rails, so they have no gutter to declare. ZONE A IS ONE LINE (Aug 2026): avatar, name, handle and time now sit on a single row on BOTH clients, where the identity block used to stack a bold name over a mono meta line — two lines of chrome above every post, and the second one existed only to hold a handle and a timestamp that fit beside the name. What shrinks is the part that can be any length: the name shrinks, the handle shrinks FIRST (a name is what you recognise), and the timestamp shrinks never and wraps never, so a post always says when. The handle is skipped entirely when it IS the name (a profile with no display name already reads '@ada' in the chalk slot — printing it twice was the two-line layout's tell). A spaced en dash divides handle from time and nothing else — the two are the same face and the same ash, so a flex gap alone would read them as one string; the name needs no separator, since bold chalk against mono ash is the distinction. 'WHY YOU'RE SEEING THIS' MOVED OFF THE META LINE and rides ABOVE the row as a kicker: it is the feed explaining its own ranking ('Trains at your gym'), not a fact about the athlete, and joined onto the identity line it competed with the timestamp for the same truncation budget. The post's own screen (web feed-post.tsx, mobile app/post.tsx) carries the identical single line at its larger avatar — its identity block is deliberately the row's, one size up, so the two cannot drift." },
  { id: "hub-feed-meta", area: "Core", status: "planned", title: "Feed's hub meta row \u2014 a value only the feed knows", detail: "The shared hub head gives every tab a meta row: eyebrow on the left, one state value on the right. Dashboard fills it with the viewed day plus Free/the scrub tag; Performance with the season and the training phase. FEED'S IS EMPTY \u2014 deliberately, and it still reserves its height so the title's y matches the other two. Nothing true is available yet: the live count would restate the \"Now training\" strip 30 pt below it, and FeedResponse (core/social-dto.ts) carries only `feed` and `live` \u2014 there is no \"new since you last looked\". TO FILL IT: give the feed a read cursor (last-seen timestamp per athlete, server-side) so the head can say how much is new, which is the one fact the stream itself cannot show without scrolling. Until then the row stays empty rather than duplicating the strip. (Briefly closed by deletion when the feed's head was cut; the head came back \u2014 see feed-hierarchy \u2014 and so did this.)" },
  { id: "hub-masthead", area: "Core", status: "shipped", title: "One head for the Today hub — Dashboard, Performance, Feed", detail: "Dashboard, Performance and Feed are ONE screen in three states, but the shared chrome (`hubHeader` in Today) stopped at the segmented control and each tab invented the head beneath it. Measured off the shipped build they agreed on nothing: titles 34 (literal) / 32 (literal) / fs.headline 22, tracking -1 / -1 / unset, eyebrow date+meta / `season || \" \"` (a SPACE CHARACTER reserving 20 pt invisibly) / none at all, gap under the control 16 / 0 / 12, and a scroll collapse on Dashboard only. Web drifted again on top of that: Performance 34 against mobile 32 on the same screen, and NO Feed head whatsoever (`w.social.feedTitle` existed and only mobile rendered it). THE FIX is one contract + two twins: packages/core/src/hub-masthead.ts holds HUB_MASTHEAD (meta row fs.micro/tracking.label at a FIXED 15 height; title fs.hero 34 / lh 36 / tracking.display; gaps control=space.lg, meta=space.xxs, below=space.lg; collapse titleScale 0.76) and hubTitleType(), which reuses the hero system's long-title step-down via the newly extracted titleStepDown() rather than copying it. apps/{web,mobile}/components/aurora/hub-masthead.tsx render it; a tab passes DATA and CANNOT pass style, which is the escape hatch that let three screens diverge. HUB_MASTHEAD_HEIGHT (55) is identical on all three tabs, so the first content row no longer jumps by up to 22 pt on every pill tap. THE META ROW always renders and always reserves its height, retiring the space-character placeholder on both clients. NO SUBTITLE: Feed's (\"What your friends are training\" under a title that says Feed) is cut and its i18n key deleted; Performance's verdict sentence moved DOWN into the state card beside the freshness score it explains; the phase moved from the chip rail into the meta row, where it had been reading twice within 30 pt. THE COLLAPSE now lives in the component on both clients (mobile subscribes to useNavScroll().collapse; web keeps the .motion-masthead CSS but reads its floor from --hub-title-scale, published from the token, instead of a 0.24 typed into globals.css). Performance's casual TEASER wore an AHeading rung and now wears the same head, so the screen no longer changes size with entitlement. GUARDED by packages/core/src/hub-masthead.test.ts (11 tests): the contract sums to its declared height, every shipped title in EN/PL/DE resolves to the same one-line rung, and a source guard fails if any of the six hub surfaces re-grows a `fontSize: 34|32`, an `fs.headline` title, a `letterSpacing: -1`, a `|| \" \"` placeholder, a style prop on the head, or a reference to the retired feedSub." },
  { id: "screen-head-guard", area: "Core", status: "shipped", title: "One title rung, app-wide — the screen-head guard (and why Train keeps the plain scaffold)", detail: "The hub-masthead guard above fixed three heads and watched three files. Train is not a hub tab, so nothing watched it, and it shipped its head as `<AHeading style={{ fontSize: 28 }}>` — a size on neither the type ladder nor the hero's title ramp — directly beside Performance and Feed resolving to fs.display 26, with an intro PARAGRAPH under it ('Tap start, log as you go. The detail waits for you at home.') on the one screen where a sentence of instruction earns its place least: the card below it carries a Start session button. Feed's subtitle and Performance's verdict line were cut when the hub head was unified; Train was the last screen still explaining itself under its own title. THREE PARTS. (1) TRAIN'S HEAD IS A BARE AHeading ON THE PLAIN SCAFFOLD, and the intro and its EN/PL/DE strings are deleted — so is the trailing footnote that promised the summary 'lands in History and on the web for the deep dive', a web client retired in Aug 2026, i.e. the app was sending athletes somewhere that no longer exists, in three languages; the same stale promise in `history.emptyHint` is reworded in all three locales. THE HERO WAS TRIED HERE AND REVERTED, which is the part worth keeping: Train was handed to the hero system (`hero={{ rank: 'title' }}`, `back={false}`) on the argument that Performance and Feed wear one. That was the WRONG COMPARISON — those two wear a hero as PUSHED screens, and Train's actual peers are the other bottom-nav tabs, where the kit states the rule twice: AuroraScreen's `hero` prop ('a root tab has nothing to pop and no title to establish, so a rail there would be chrome for its own sake') and Nutrition at its own tab root ('AT THE TAB ROOT there is no hero, because a tab root has nothing to pop and no origin to name'). It was also measurably expensive: HERO.height.title is a FIXED 132dp (rail 44 + eyebrow 16 + title 32 + meta 16 + 24 of gaps) against the plain head's 48, and Train fills the title and nothing else — 76dp reserved and empty — which pushed today's session card from +64dp to +148dp, i.e. 34dp LOWER than before any of this work, on the screen whose whole job is starting a session in one tap. Reverted to the plain scaffold; the rung is unaffected either way, because AHeading resolves the SAME `title` rung the hero does, which is the consistency that was actually asked for. MESSAGES WENT WITH IT: that tab root carried a hero too (plus an `eyebrow` borrowing the NAV GROUP name 'Social' into a head slot, above a tab bar already saying Messages), which made the app's one counterexample also the file a reader would find when looking for precedent. It is on the plain scaffold with a bare AHeading now. AND THE RULE IS ENFORCED RATHER THAN RESTATED: the guard checks all five bottom-nav destinations — Today, Profile, Train and Messages are tab roots end to end and carry no `hero=` at all, while Nutrition is a tab root that also hosts pushed sub-views, so what is asserted there is the GATE (`hero={root && …}`) rather than the absence. Both halves verified failing before shipping. (2) AHeading TAKES NO fontSize. The rung is computed by heroTitleType, which STEPS A LONG TITLE DOWN a rung so it takes two lines instead of three; a caller that pins the size defeats the step-down, so even the well-behaved `fontSize: fs.display` was not a no-op — it forced 26 onto exactly the long titles the ramp exists to rescue. All ELEVEN call sites retired: five dropped a redundant `fs.display` (trends, settings, onboarding, coach, volume), the welcome splash asks for its rung BY NAME through the component's new `rank='cover'` prop instead of a typed 32 (a size on neither ladder), and five headings BELOW screen level moved to ASection — the section rung — where they always belonged (coach's two cluster heads, the nutrition wizard's two step heads, and the trial card's headline via ASection's documented titleStyle escape hatch). (3) THE GUARD, packages/core/src/aurora-head.test.ts: it walks EVERY .tsx under apps/mobile rather than keeping a list of screens to fall behind, and fails the build naming the file and the offending tag. It also asserts it can see the tree it claims to walk (a guard that silently walks an empty directory passes forever), pins Train's head to the hero system, and holds the retired web-client strings deleted. Verified failing on a reintroduced `fontSize: 28` before shipping." },
  { id: "app-header", area: "Core", status: "shipped", title: "One lockup row for every tab root — Today AND Nutrition", detail: "THE HEADER IN THE SCREENSHOT — avatar, the HYBRID wordmark with the day-streak under it, the bell — now sits at the top of NUTRITION as well as Today, on both clients. It is the app's identity strip, so it belongs to a bottom-nav TAB ROOT rather than to one screen; Nutrition took the Explore slot in the bar (see nav-bar.ts) and had been the one root wearing no identity at all. IT WAS AUTHORED TWICE before this: forty lines of JSX inside apps/mobile/components/aurora/home.tsx and forty more inside apps/web/components/aurora/today.tsx, each under a comment saying it mirrored the other, and each already wrong — the flanking tiles were 42dp on the phone against 44px in the browser (42 is UNDER the platform minimum tap target), and a nameless athlete got \"·\" on one client and \"A\", which reads as somebody's real initial, on the other. THE FIX is the hub-masthead shape: packages/core/src/app-header.ts holds APP_HEADER (tile 44/r12, wordmark 19 at tracking.display, the streak hairline at 9.5/tracking.caps with its flame and its 4dp gap, the 18px unread badge) plus avatarInitials() and unreadLabel(), and apps/{web,mobile}/components/aurora/app-header.tsx are twins that render it and CANNOT be passed a style. THE COMPONENT SOURCES ITS OWN DATA — name, streak (computeAccountability over the shared sessions cache, deduped by React Query) and unread count — so a second tab root wears the identical head by rendering the component: there is no streak prop to thread and no way for two tabs to disagree. Today passes only what is TODAY'S: what the streak opens (the done-today sheet, day-scoped) and the hub the drawer switches in place. THE SIDE MENU moved with the avatar that opens it, out of Today and into the header, so the drawer works from Nutrition too; its `onHubTab`/`activeHub` are optional now, and off the hub the three hub rows route to the standalone Dashboard/Performance/Feed screens instead of silently moving a control the athlete cannot see. NUTRITION'S ROOT also gains the shared HubMasthead under the header (greeting as the eyebrow, the screen's name as the title), replacing web's hand-rolled mono line plus `fontSize: 34` <h1> and mobile's NO head at all — the two roots now present themselves identically. GUARDED by packages/core/src/app-header.test.ts (11 tests): the contract's numbers come from the scale, the tile clears 44, the lockup fits the row, both placeholders resolve to the middot, and a source guard fails if any of the four tab roots stops rendering <AppHeader>, re-grows a hand-rolled `HYBRID<` lockup, keeps its own <AuroraSideMenu>, or if either component gains a style prop or stops sourcing its own data." },
  { id: "streak-mark", area: "Core", status: "shipped", title: "The day-streak is one mark, and it opens the history", detail: "The streak was drawn in THREE places and no two agreed. Under the wordmark: an uppercase terracotta hairline that opened Today's done-today sheet — but only on Today; on the Nutrition tab root the same caption rendered as dead text, because the sheet it pointed at is Today's. In the done sheet's own sub line: a sentence-case flame spliced into a run of mono copy. Under the profile's activity heat-map: a bare CHARTREUSE \"17d\" that did nothing at all, and that swapped in the LONGEST-WEEK figure under a label reading \"-week streak\" on web and \"-week best\" on mobile — the same number, mislabelled on one side as a current run — with the two clients even checking the two cases in opposite order. Same fact, three costumes, one working control. THE FIX: packages/core/src/streak-mark.ts states the mark once — two DENSITIES of one design (`hairline`, uppercase and tracked, standing alone under the wordmark or the heat-map at 9.5, below fs.nano so it cannot grow the 44 pt header row; `inline`, sentence-case at fs.micro, matching the line of mono copy it sits in), the terracotta accent, and STREAK_DESTINATION. apps/{web,mobile}/components/aurora/streak-mark.tsx render it. IT IS A CONTROL, NOT A DECORATION: the number answers \"how long have I kept this up\", and the only screen that answers \"kept WHAT up\" is the training history — so every instance opens HISTORY, and the destination is not a prop. A caller's entire say in the tap is `onDismiss` (close the sheet on the way out); there is deliberately no onPress/onStreak escape hatch, since that is exactly how the three copies came to behave three ways. IT SOURCES ITS OWN COUNT from the shared sessions cache through core `streak(sessions)` — the same call every other reader makes — and renders NOTHING at zero, so a surface shows the streak by rendering the component and no two surfaces can print different numbers. Consequences: the header no longer needs an `onStreak` prop (Today's done sheet keeps its own entry point, the done floor); the sheet's flame moved OUT of the sub <Text> into the sheet body on BOTH clients, because text can hold a glyph but not a control; the profile's lime copy is gone, and its two non-streak fallbacks (longest-week, nothing-yet) stay plain text and now read and order identically on the two clients. COPY: `w.account.profile.day-streak-suffix` was a second key holding the same English, Polish and German as `w.home.today.dayStreak` — the exact duplicate the copy-parity guard exists to catch, which slipped through only because both clients used both — and it is deleted along with the web-only `-week streak` mislabel. A new `w.home.today.streakAria` gives the control an accessible name that says where it goes. THE PROFILE'S STAT TILE goes too: the streak tile in the public highlight grid was the last inert instance — a figure in a grid of figures — and now a TAP opens the history while the LONG-PRESS still enters the grid's rearrange mode. It is not a special case inside the grid: a highlight tile may carry a `to` (a nav/screen id) and an `aria`, and the streak tile fills them from STREAK_DESTINATION and the mark's own aria key, so the tile and the mark can never point at two screens or announce two sentences. Only when there IS a current day streak — the tile falls back to the longest-WEEK figure, which the history's day grid does not answer. Guards on both clients keep the rearrange and the link apart (mobile gates the press on edit mode; web additionally refuses to navigate on a pointercancel, which is how a touch-scroll that began on a tile ends), and every tile's accessible name now carries its VALUE — a tile announcing only \"Streak\" told a screen-reader user everything except the number. GUARDED by packages/core/src/streak-mark.test.ts (12 tests): the rungs come off the scale, the hairline stays under fs.nano, caps only where the mark stands alone, the destination is history, the duplicate keys are gone, and a source guard fails if any of the six surfaces stops rendering <StreakMark>, re-grows a hand-rolled `name=\"flame\"` count, or if either component gains a style prop, an onPress escape hatch, or stops sourcing its own count." },
  { id: "feed-hierarchy", area: "Core", status: "shipped", title: "Feed \u2014 the hierarchy pass (headline, hero figure, hub head)", detail: "THE DIAGNOSIS: the feed read as chaos not because it carried too much, but because every card said everything in the same voice. Core already sorted each post into a MOMENT (p0 interrupts, p2 fills) and an ARCHETYPE, and the intent was written down in three files \u2014 'a p0 record gets the display headline and the big figure' \u2014 but two of the three mechanisms were inert. (1) ZONE B rendered `session.title`, and an unnamed session is titled by `defaultSessionTitle()`, a pure function of the clock \u2014 so a stream of twenty workouts was a column of 'Afternoon workout' set in the display face, the largest type on every card, carrying the one fact the timestamp beside the athlete's name had already given. (2) THE HERO FIGURE was gated on `archetype === \"stat\"` while `sessionDetail` always emits `\"sets\"`: the two conditions could never both be true, so a session PR could never draw the big number and a first-ever 90 kg trap-bar deadlift rendered at the same size as the bodyweight pull-ups two rows below it. p0 and p2 differed by exactly one font size, on a line whose text was identical on both. (3) The hub tab drew 'Feed' in the display face directly under a segmented control whose Feed pill was already lit. THE FIX puts all three decisions in core, so the two renderers cannot answer them differently. `isAutoSessionTitle()` (engines/session.ts) \u2014 ONE table now both generates the clock titles and tests for them, so a sixth time-of-day cannot be added to one and not the other, which would quietly make every such session look athlete-named again. `feedHeadlineEarnsLead()` gates ZONE B: an athlete-chosen title leads, a record headline leads (it names the lift), a clock-written title does not, and 'Posted' never sits over a status update whose words ARE the post. `cardLead()` returns the card's ONE big number and where it comes from \u2014 a shared PR's own figure (label null, because its headline already names the lift) or the loudest record a session set (labelled WITH the lift, because the headline no longer names anything). `cardRecords()` splits a session's records into the one that took the hero and the quiet lines beneath it, so no lift is drawn twice and the tier chip rides whichever of the two carries the loudest claim; it REPLACES `cardPrLines` rather than sitting beside it, because two ways to answer one question is how the clients drift. A p0 that carries a figure sets its headline at the ordinary title rung: one loud thing per card, and a display-weight heading above a 46px figure is two heroes arguing. AN ANONYMITY GUARD keeps the auto title when a card would otherwise have neither a hero nor a top set (a bare cardio session) \u2014 a generic name still beats a row of numbers with nothing holding them. `feedHeadlineText` is untouched: the share payload and the opened POST still compose the string, because a page may name itself and what you share must read like what you tapped. THE HUB HEAD WAS CUT AND PUT BACK (Aug 2026), and the reversal is the interesting part. Cutting it was defensible on its own terms \u2014 'Feed' in the display face directly under a segmented control whose Feed pill was already lit is the screen naming itself twice, and it cost ~70dp of an 852dp screen. But Dashboard, Performance and Feed are ONE SCREEN IN THREE STATES, which is the entire premise of the hub-masthead contract, and a head on two of them and not the third makes the title's y jump on every pill tap \u2014 trading a consistent hub for a reclaimed band. The head stays on both clients and emits its own gaps again; the `hub` prop web briefly grew, and the control-to-content margins that stood in for the head's own, are removed. `hub-feed-meta` is therefore LIVE AGAIN rather than closed by deletion: the empty meta row is back, still reserving height so the title's y matches the other two tabs, still with nothing true to put in it. MOVE A SHIPPED ONE HALF, AND THE OTHER HALF IS CLOSED BY DECISION (Aug 2026) rather than left in the backlog. The review also wanted Saved + people-search moved off the feed's filter row and up beside the notification bell, on the grounds that they are DESTINATIONS sitting among FILTERS. Not done, and not going to be: AppHeader (aurora/app-header.tsx, both clients) is a fixed-flank three-column row \u2014 one tile left, the wordmark on flex:1, one tile right \u2014 and its own source records that the flanks are fixed precisely so the lockup centres BY CONSTRUCTION; it was `space-between` once and the wordmark sat off-centre because the flanks never weighed the same. Two more glyphs in the right flank re-breaks exactly that. It is also SHARED chrome \u2014 Today's three hub tabs and the Nutrition root all render it \u2014 so feed-only destinations would appear on screens they do not belong to. Doing it properly would mean an optional `actions` slot filled by the feed hub tab alone PLUS re-solving the centring, and that is a change to the app's identity strip in exchange for tidying one row. The filter row keeps all four, which is honest enough: they are already split left-filters / right-destinations by position, and the ~70dp the review was chasing came from the head deletion, which shipped. GUARDED: hub-masthead.test.ts is back to asserting the shared component on all six surfaces (the two cases that pinned the head's ABSENCE went with the reversal), but its source-reading helper keeps the fix made en route \u2014 it stripped BLOCK comments before LINE comments, so a `//` comment mentioning a path like `aurora/*.tsx` opened a phantom block that ran to the next close-comment and silently blanked the web feed screen's root element, which means every assertion in that file had been passing on a file it could not fully see. feed-card.test.ts gains twelve cases across the hero, the record split and the headline predicate. Web + mobile in the same change. NOT IN THIS PASS \u2014 see feed-grammar-remaining." },
  { id: "feed-grammar", area: "Core", status: "shipped", title: "Feed \u2014 the grammar pass (one qualifier, one accent, a footer not a table)", detail: "Phase 2 of the same review that shipped feed-hierarchy, and where its rules stopped being aspirational. (D) A RECORD LINE CARRIED SIX TYPE TREATMENTS ON ONE BASELINE: a display-bold lift name, a mono-bold figure, a mono-ash unit, a mono-accent delta, the lowercase sentence 'first time trained' doing a badge's job, and a tier chip flung to marginLeft:auto. `deltaPct` and `firstEver` are MUTUALLY EXCLUSIVE BY CONSTRUCTION \u2014 a lift trained for the first time has no previous best to improve on, so `prLines` can never emit both \u2014 yet they held two slots. Core's new `cardQualifier()` gives them one, with the distinction carried by colour alone: a delta wears the card's accent, a first-ever is short and ash (the new `feed.firstShort` key, EN/PL/DE; the full `feed.firstEver` sentence stays on the opened post, where a line has room for prose). The TIER CHIP left the record line for the footer and renders in ASH \u2014 provenance qualifies the whole POST rather than one lift inside it, and the accent is the 'go' colour, already spent on the improvement. That restores the rule written at the top of feed-card.tsx and broken by the row directly beneath it: ONE accent per row. (E) THE STAT ROW WAS A TABLE \u2014 three equal columns each stacking a value over an uppercase descriptive label, competing with the content above it for the same attention, and giving tonnage no unit at all (it read '5.360 VOLUME'). It is one quiet line now: `feedStatParts()` returns the figure and the unit that belongs to it, chalk figure against ash unit, and NO SEPARATOR CHARACTER \u2014 the house rule prefers real layout to a joined string and a flex gap divides '50 MIN' from '5,360 KG' without a dash. The footer draws whenever there are stats OR a tier, so a shared-PR post with no aggregates can still say how its claim was corroborated. The HR figure is no longer teal on the card: on a row whose one accent is spent on the improvement, a single coloured number in the footer is the only colour left and reads as emphasis it has not earned \u2014 the opened post keeps the conditioning channel, where the figure has room to mean something. (F) TWO OF THE FOUR ACTIONS WORE WORDS while save and share stood bare, so the row read as two labelled buttons plus two unexplained marks. Four glyphs now, one visual class, with a count beside one only when it is greater than zero \u2014 '0' beside a bolt is worse than silence. The labels move to the ACCESSIBILITY layer, where mobile had none at all: the visible text had been the only name those controls carried, so removing it without aria/accessibilityLabel would have been a regression rather than a cleanup. THE LOCALE DEFECT, FIXED AND PINNED: `feedStatText` called `toLocaleString()` with NO locale, so grouping resolved against the DEVICE \u2014 5360 kg rendered '5.360' on a German or Polish handset while the interface was in English, which an English reader parses as five point three six. It is invisible to anyone testing on an English device, which is how it shipped. Every call site on both clients (the card and the opened post, four in total) now passes `useLang().lang`, `feedCardView` takes an optional `locale` for the same reason, and core's own test states all three groupings explicitly rather than comparing against the runner's default. GUARDED by a new block in apps/web/__tests__/feed-typography.test.ts (it reads BOTH clients): the clients route through `cardQualifier`, neither renders `feed.firstEver` itself, neither draws the chip in the accent, neither imports FEED_STAT_LABEL_KEY (the table's second row), every `feedStat*` call site passes the language \u2014 and the guard asserts it found exactly four of them, because a guard that quietly stops matching reads exactly like a clean codebase. Core gains three cases across the locale, the footer parts and the qualifier. Web + mobile in the same change." },
  { id: "feed-rhythm", area: "Core", status: "shipped", title: "Feed \u2014 the row's height is a function of its moment (G)", detail: "The last uniform thing about a feed row, and the move that makes feed-hierarchy and feed-grammar read as a SYSTEM rather than as six separate tweaks. Every row was padded 12/12 whatever it carried, so a first-ever deadlift and a Tuesday accessory day occupied the same slab of screen and weight was expressed in TYPE SIZE ALONE \u2014 which reads on one card and disappears across twenty. Scrolling a stream of identical heights, the eye has no rhythm to catch on, and that flatness is what 'the feed is chaos' actually describes. `FEED_ROW_PAD` in core maps moment to vertical padding off the shared space scale (p0 space.lg 16, p1 space.md 12 \u2014 the old uniform value \u2014 p2/p3 space.ms 10) and BOTH renderers read it rather than typing a literal, so a p0 opens up, a p2 tightens, and the difference between them is the ranking expressed as rhythm rather than as order. The quiet row is what makes the loud one loud. Guarded in core (the map covers every moment, p0 > p2, every value comes off the space scale) and in the shared feed-typography guard (both clients read FEED_ROW_PAD[moment]; neither carries the old literal). THE UNTRANSLATED SESSION TITLE, FIXED IN THE SAME CHANGE, and NOT the way it was first specced. `defaultSessionTitle()` returns plain English and must keep doing so \u2014 it produces STORED data, and a title already written to every existing row cannot retroactively become a translation key without rewriting them. So the translation happens on the way OUT: `sessionTitleText(title, t)` recognises the five clock-written strings, resolves them through the reader's own `t()`, and returns an athlete's own name untouched, because nobody's own words should be run through a dictionary. That works on every row already in the database, including ones logged before the keys existed. Applied at `feedHeadlineText` (which covers the feed row, both post screens and the share payload from one place) and at the History list + session hero on both clients. NOTE A VISIBLE COPY CHANGE: the English values read 'Afternoon session', not 'Afternoon workout' \u2014 the vocabulary guard in i18n.test.ts caught the first draft and it was right to: `workout` has left the interface except for what the DEVICE recorded, and the object is a session. The STORED string is unchanged, so nothing in the database moved. DE follows the `feed.hl.session` vocabulary ('Einheit'), PL follows its ('Trening'). Five keys, three locales." },
  { id: "feed-composer-retire", area: "Core", status: "planned", title: "Feed \u2014 retire the composer band (H)", detail: "The last move of the feed review. The always-open composer is the third-loudest element on the feed and asks for typed prose in a product whose posts are generated from sessions \u2014 ~92dp of permanent band, plus its hairline, spent on a verb used rarely. H was to delete it and let the detached nav circle carry the verb, morphing to Add post while the feed is the visible surface. Web is already most of the way there: `auroraNavAction` morphs the circle on the feed surface and the shell fires `hybrid:compose`, which today focuses the always-open box and would instead open a compose sheet. UNBLOCKED \u2014 THE PLATFORM CONSTRAINT WAS REAL AND IS GONE. This was `blocked` on mobile being unable to repurpose the native trigger's press; option (3) of the three recorded answers ('a tabPress intercept, if expo-router ever exposes one') has arrived, and is shipped and proven on another surface \u2014 see nav-action-morph-mobile and nav-action-picker-search. Retiring the band is therefore ordinary work on both clients rather than a parity impossibility: mobile needs the Today hub to publish 'feed' as its surface and a compose sheet behind a `screen`-kind action, web needs `hybrid:compose` pointed at that sheet instead of the inline box. THE SMALLER FOURTH OPTION ALREADY SHIPPED and stands either way: the always-open field stays for now, but its TOOLBAR (the PR-attach glyph and the Share pill, which sat dimmed and disabled through every read of the feed, and almost every visit is a read) is revealed by INTENT \u2014 on focus, or on there already being something to post. ~36dp back on both clients with parity intact. The focus term is not optional: a PR attached with no words is a valid post, so gating purely on content would make the attach glyph unreachable. Web tracks focus on the BLOCK with a relatedTarget containment check, because focusout fires before click and a bare blur would unmount the button under the cursor; mobile relies on the list's keyboardShouldPersistTaps=handled." },
  { id: "dock-rail-unification", area: "Core", status: "shipped", title: "The docked sub-rail — one DockRail for History and Plans", detail: "THE DIAGNOSIS (design sheet: reference/dock-rail-design.html, six rendered options scored on seven criteria): the sticky chip strip that docks beneath the collapsed hero was implemented FOUR times — History web (a hand-rolled button in history-views.tsx), History mobile (the kit's AChip), Plans web and Plans mobile (both hand-rolled CategoryRail) — and twelve properties were decided independently in each. SIX WERE DEFECTS: (1) web CoverHero had NO `rail` slot where mobile CoverScreen does, so web Plans hand-rolled its own sticky bar and drew ink 86%/blur 14/z 29 against the scaffold's ink 88%/blur 18/z 20 — the STRUCTURAL root cause, everything else downstream of it; (2) the selected fill broke parity — web a SOLID lime pill with --on-accent text, mobile lime@16% with lime text; (3) the chip face broke parity — web mono 12, mobile Archivo bold 13; (4) the hit target was ~29dp (History web) and ~33dp (both Plans rails) against the 44 the kit itself declares; (5) three of four were hand-rolled rather than a shared primitive, which is what let all of the above happen. FIVE WERE DRIFT: rest fill (ink2 vs transparent), tracking (+.08em on Plans web alone), a 400->700 weight swap on select that widened the chip and reflowed the row mid-tap, chip padding (6x16 vs 8x12), rail padding (10/14 vs 8/8). ONE difference was REAL and is now the ONLY one: History's chips SELECT a view (one always on, the layout below changes) while Plans' chips JUMP to a shelf and are never 'on' — the shelves already ARE the categories, so narrowing to one would empty the screen. SHIPPED (the sheet's recommended 'CODIFY' option): packages/core/src/dock-rail.ts publishes the whole contract (gap, padY, chip hit/padX/size/tracking/radius, the tint fraction) plus `DockChipRole` ('mode' | 'anchor') and `dockChipOn(role, selected)`, which MECHANICALLY refuses to light up an anchor whatever a caller passes — a jump chip claiming a selection it does not have is the failure mode of the unify-everything option, and it is now unreachable rather than merely discouraged. Twin primitives consume it: DockRail + DockChip in apps/web/components/aurora/dock-rail.tsx and in the mobile kit. A caller passes DATA and a ROLE and cannot pass a colour, size, radius, face or padding. Web's CoverHero GAINED the rail slot (twin of mobile CoverScreen's), so Plans stops hand-rolling its bar; both scaffolds' rail slots are now full-bleed and UNPADDED, because the rail owns its gutter — the web hero slot used to pad its child and History then negative-margined straight back out, the same gutter applied twice in opposite directions. JUDGEMENT CALLS, each recorded in the source: (a) the shared face is MONO 12, not AChip's Archivo bold 13 — a rail is CHROME, in the same band as the hero's eyebrow/meta/accessory, all of which speak the mono voice, and three of the four shipped rails were already mono; AChip is untouched and stays Archivo for its 37 IN-CONTENT call sites, since borrowing the content chip for the rail is exactly what caused the face drift. (b) tracking is ZERO, not the +.08em Plans web carried nor the .04em the design bench sketched — a rail label is a word, not a kicker, and there is no token between them. (c) selected = TINT (16% fill + accent border + accent label, three signals, WCAG 1.4.1) not the solid pill: web's solid lime chip was the loudest object on a screen whose cards are each one quiet figure. (d) weight is CONSTANT across states. (e) mobile's rail takes a `gutter` prop and Plans passes the newly exported COVER_GUTTER (16) — the cover scaffold pads its children at 16 where AuroraScreen/HeroScreen pad at GUTTER 12, and a resting chip must line up with the column it sits above; the Plans chips used to rest 4dp left of the shelf heads beneath them. (f) web's RAIL_H (the jump offset) is DERIVED from the contract instead of a hand-typed 49px fallback beside a runtime measurement. (g) web PlanWeekRail's bar material was aligned (blur 14 -> 18) so one screen does not draw two bar materials; the waveform itself was left untouched here — the argument being that its bar heights encode weekly volume, which is data, not chrome. That argument did not survive: the bars were deleted in Aug 2026 (plan-detail-bars-retired) and the week rail is now a plain `mode` DockRail like the other two. NOT DONE, deliberately: no scroll-spy on Plans (a position readout belongs to the shelf heads, not the chips). REJECTED OPTIONS and why, all in the sheet: UNIFY (a jump chip claiming a selection), UNDERLINE and SEGMENT (both add a selection form to an app whose kit already audited eight and collapsed them to two, and both clip under PL/DE where a chip rail scrolls), QUIET (removes the affordance along with the divergence). DEFERRED as the second move: LEDGER — see dock-rail-ledger. GUARDED by apps/web/__tests__/dock-rail.test.ts (35 cases), which checks BOTH client trees from one suite because the failure it guards against is precisely the two drifting apart: it fails on a rail not using the primitive, a chip with no explicit role, a `selected` passed to an anchor, a second hand-rolled sticky bar beside the cover, a solid accent fill, a weight swap on select, a rail component that sets any geometry of its own, a client restating a number core owns, a scaffold missing its rail slot, or a rail slot that pads the child that has to bleed past it. Plus dock-rail.test.ts in core (the 44dp floor, the scale-derived numbers, and that an anchor can never be on). The mobile design-token ratchet exempts DockChip by name rather than raising its ceiling — it counts hand-rolled chip implementations, and this is the third SANCTIONED primitive beside Chip and AChip. i18n en/pl/de: +histview.switchView (the History rail's accessible name). Core 2507 tests + both typechecks + web build + web suite (35 new) + mobile suite + iOS bundle export green." },
  { id: "dock-rail-ledger", area: "Core", status: "planned", title: "Dock rail — put each chip's own figure in the chip", detail: "The second move on the dock rail (see dock-rail-unification; option 'LEDGER' in reference/dock-rail-design.html). Each chip carries the number behind it — History's views the session count they render, Plans' categories the goals per shelf — as a two-row label+figure object in the app's own 'headline number' treatment. It used to be the only option that also absorbed the THIRD rail on this surface — PlanWeekRail, the waveform week picker, was already a label-plus-figure object — but that rail lost its bars in Aug 2026 (plan-detail-bars-retired) and now rides the shared primitive as a plain `mode` rail, so the ledger no longer has to account for it. BLOCKED ON A DATA PROBLEM, not on effort: two of History's four figures are the SAME number (Agenda and Timeline both render every session) and Trend has no count at all, only a range — so HISTORY_VIEWS has to carry a per-view figure before the ledger can ship honestly, otherwise it renders a rail that looks broken. Costs a taller, busier rail and every count must be cheap enough to compute per render." },
  { id: "design-system-unification-sweep", area: "Core", status: "planned", title: "Design-system unification — remaining screens", detail: "The one-accent discipline (see design-system-unification) has been applied to the Today + Settings surfaces; the rest of the app still needs the same audit before it re-grows a rainbow — the Train launcher, the live Logger, Nutrition, Profile/You, Explore, Coaches/Feed, Calendar and the Volume/analytics screens — each swept for arbitrary multi-hue clusters (colour reserved for a single marker or a genuine state/role, parallel items + labels left neutral), web + mobile at parity in the same change. (NOTE: the mobile Clay & Sage accent parity that was briefly tracked here is DONE — see japandi-clay-sage; the bespoke screens alias the theme palette as `C`, so their fills flip automatically, and the accent-as-text sites were routed through `txt()`. This item remains only for the one-accent / de-rainbow discipline pass on the screens listed above.) PROGRESS (Aug 2026, the panel-critique sweep): foundations landed — tokens.ts card/line re-synced with palette/globals (charts drew a stale grey), fs gains hero(34)/stat(46) rungs, web Button rebuilt to the mobile spec (Archivo 700/15, ≥44px, hover+press), a shared press-feedback primitive on BOTH clients (web `.pressable` CSS utility; mobile `PressScale` running Animated.spring from springToRN(springs.nav), Reduce-Motion → opacity dip) adopted by kit primitives + the audited screens' rows, theme-aware card shadow (warm umbra on Kyoto washi) via one cardShadow helper, mobile entrance on the real sheet spring, dead material deleted (~900 lines: web .cmd-* menu, .lg-thin/thick/vibrant, .aurora-enter, mobile liquid-glass.tsx/GlassCard/GlassButton/GlassPanel/AuroraSvgIcon, stories/today-quick orphans, SECTION_COLOR which had zero call sites), type floors on exercise-page (no more 7-8px labels), F.monoBold where fontWeight '700' was faked on the static mono face, hit-target fixes (web period tabs, builder set-row column), sheet exit no longer truncated (fast+complete on both clients), single nutrition over-target threshold, error red unified, white hairlines made theme-aware, profile wiggle guarded + staggered on mobile. THE FEED (Aug 2026) is swept for TYPE: it was off the system in three ways at once. (a) THE PROSE FACE — nothing set a font-family on web's <body> (Tailwind's preflight points html at its own --font-sans, a token this project never defines, since it defines --font-display), and RN inherits no font at all, so the feed's captions, comments, empty states and caught-up line drew in the PLATFORM UI font while every aurora screen, which sets the face on its own root, drew Archivo; the face now sits on `body` in globals.css (so no web screen can fall through again) with the feed's root naming it too, and every mobile feed Text names F.reg. (b) THE TITLE FACE — post headlines were hard-coded to the sans, so under Kyoto Hour the feed was the one tab still in sans, with its own 'Now training' section head in Mincho directly above it; both clients' headlines now read the heading face (web --font-heading, mobile serifIf), as do the social EmptyState/Empty titles and the web ScreenHead. (c) THE LADDER — the feed's hand-picked sizes (web tab pills at 11.5 against mobile's fs.micro, the mobile hub head at 24, assorted 13/12/10) are now fs.* rungs. Guarded by apps/web/__tests__/feed-typography.test.ts, which fails on a feed Text with a size and no face, a headline off the heading face, or any hand-picked fontSize in the feed files. CARD FOOTERS are done and split out as their own capability — see card-foot: the three Performance cards now share one two-part footer (a status line and one link that expands), with sixteen drifting values pinned and a guard test across both clients. THE TODAY-HUB HEAD is now off this list — see hub-masthead: the three tabs' mastheads were the last place in the app where one screen presented its own name at three sizes, and they are one component on one contract, guarded. STILL OPEN on that thread: the Volume screen's `SourceBody` Sheet answers 'whose numbers are these?' — the same shape of question the footers now answer by unfolding — so it is a third response left standing, and belongs in this sweep. TODAY'S CARD MATERIAL is off this list entirely — see today-card-material. SIX of its cards were hand-drawn copies of ACard (the check-in card, the done floor, the week verdict, the two chooser cards, and — with no fill at all — the Protocol card); all six now come from the kit, APressCard was added to close the gap that caused two of them, and Today is one material. THE CARD WORK ON THE REMAINING SCREENS IS NOW A COUNTED LIST rather than a sentence: card-surface-guard's RATCHET names all 25 hand-rolled card surfaces left in the app, by file, and fails the build if one is fixed without being struck off. Work this sweep's card half straight off that map. Three passes are DONE — percent-program + history-views (the densest entries, 25 → 19), then the nutrition surface, nutrition.tsx + pantry.tsx (19 → 16). What is left is history.tsx and fifteen more files carrying one card each, so the remaining card work is now shallow and mechanical rather than concentrated." },
  { id: "tint-scale", area: "Core", status: "shipped", title: "Colour — ALPHA, the tint scale, derived from 350 hand-rolled alphas", detail: "THE SETUP: converting the app\'s hex-suffix alphas to withAlpha() (see the alpha rule in apps/mobile/lib/design-tokens.test.ts) made 350 previously unreadable values legible as a set for the first time — and the set was 58 distinct numbers, eight of them inside the single band 7%–15%. Nobody chose eight; each call site converted a percentage in its head and wrote down the byte, and no two had any way to notice they disagreed. THE SCALE, in packages/core/src/theme/tokens.ts, is DERIVED from that histogram rather than invented. TWO FAMILIES, because they tolerate completely different precision: SURFACES — wash 0.08 / fill 0.12 / solid 0.16 — are large areas where a 4% shift is subtle but visible, so the rungs sit close together; BORDERS — edge 0.25 / line 0.33 / rim 0.42 — are ONE PIXEL wide, where a 5% shift is not perceptible, so they can be coarser and still land. That asymmetry IS the scale and scale.test.ts guards it. THE MIGRATION MOVED 230 SITES AND ALMOST NOTHING MOVED: 82 unchanged, 112 by ≤0.02, 35 by 0.02–0.04, and exactly ONE by more than 0.04 — the worst shift in the whole change is 0.040, mean 0.011. Verified by REPLAY rather than by eye: the migration rule was re-run against HEAD for every touched file and all 196 rewritten lines were found verbatim in the tree. WHAT DELIBERATELY HAS NO RUNG, and this is the finding worth keeping: the measured histogram is CONTINUOUS from 0 to 1, not clustered. Above ~0.45 the values are scrims tuned against the specific content behind them, and inside a LinearGradient every stop is a position on a ramp that has to read as smooth — a ramp needs arbitrary intermediate stops by construction. Neither is a palette choice, so 120 sites keep a raw number and the rule is a RATCHET rather than HARD, with its comment stating that the count should fall only if one of those turns out to be a tint in disguise, NOT by adding rungs until it reaches zero. A token set that covers 71% of its axis honestly beats one that covers 100% by pretending." },
  { id: "tracking-proportional", area: "Core", status: "shipped", title: "Type — tracking names its token; a big figure derives it", detail: "432 raw letterSpacing values → 0, and the rule is HARD in apps/mobile/lib/design-tokens.test.ts. THE ANSWER IS NOT ONE RULE, because the axis is not one problem. (1) SMALL TYPE TAKES AN ABSOLUTE RUNG. The eyebrows live at 10–13dp, a band narrow enough that one dp value works across all of it, and the app has exactly two: tracking.label (0.9) and tracking.caps (1.2). 298 sites already WERE those numbers and now say so; 24 more were stragglers either side (0.4 … 0.8 under, 1.4 / 2 / 3 over) and snapped to the nearer, every shift ≤0.5dp. (2) TITLES TAKE tracking.display (-0.5), and this was the contested call recorded when this entry was still `planned`: two tightenings were in force at the same sizes, the token at -0.019 … -0.031em and a raw -0.3 group at -0.013 … -0.023em, with no arithmetic to pick between them. RESOLVED IN FAVOUR OF THE TOKEN, on the codebase\'s own evidence rather than taste: it holds 51 call sites plus two CORE CONTRACTS (app-header\'s wordmark at 19, hub-masthead\'s title at fs.hero, both guarded by their own tests), and hub-masthead adopted it deliberately over the `-1` that shipped before it. A house value that has already survived one argument is the house value; the raw -0.3 group was the drift. 36 sites moved onto it. (3) BIG FIGURES DERIVE IT — `trackFigure(size)` in packages/core/src/scale.ts, and this is the part no absolute could do. They run 30–68dp, a 2.3× span, where -0.5 is -0.017em at the bottom and -0.007em at the top — nothing at all. Which is exactly why each had hand-multiplied its own value (-1, -1.6, -1.9, -2, -2.4, -2.5) and why `letterSpacing: -1` appeared at 20dp AND at 48dp: one optical intent, twelve spellings, right at none of them. In em they collapse onto -0.035em, so TRACK_FIGURE_EM is that, and trackFigure is `leading(size, role)`\'s twin — the same argument one axis over, for the same reason (an absolute cannot follow a size that moves). It returns -1.6 at fs.stat, which is what the three biggest figures in the app were already drawn at, so the constant is derived from the work rather than imposed on it; nothing moves by more than 0.5dp and the sites that move most were the most clearly wrong (-1 at 44dp was -0.023em against its siblings\' -0.035em). NAMED trackFigure, not track: `track` is already the analytics call in apps/mobile/lib/track. THE BOUNDARY IS THE SCALE\'S OWN DOCUMENTATION — fs.hero (34) is \'mastheads / cover titles\', fs.stat (46) is \'the one hero figure on a screen\' — so titles up to hero take the rung and figures from stat take the function. Four sites sitting at fs.stat under tracking.display moved with it; a 32dp food NAME moved back, being a title that happened to be large. TWO EXEMPTIONS, guarded by name: lib/share.tsx (a card captured to PNG at arbitrary width — its sizes are `width * 0.072`, fractions of a canvas rather than rungs of the app ladder) and the one-time-code fields in login.tsx + mfa-settings.tsx (letterSpacing 8 and 3 is SEMANTIC: it says the characters are discrete digits to read one at a time, and a rung would delete what the spacing is saying). Covered by scale.test.ts (proportionality across the span, and that it returns what fs.stat was already drawn at)." },
  { id: "retired-face-purge", area: "Core", status: "shipped", title: "The old faces are out of the app, including the one still doing layout", detail: "REPORTED AS \"get rid of this Archivo of my app\", and the answer had two halves — one that was only noise and one that was a live defect. THE NOISE: no Archivo or JetBrains Mono binary, package or `fontFamily` had survived the Söhne swap; what survived was their NAMES, 67 mentions across 30 files of live source, and several were false rather than merely stale. globals.css opened with \"Two faces, and that is the identity — Archivo and JetBrains Mono\". `fonts.mono` was documented as \"JetBrains Mono, in two\" while the app loads THREE Söhne Mono cuts. The mobile kit described itself as built on \"Archivo type\". percent-program said `F.mono` IS JetBrainsMono. A comment naming the wrong face is the same class of defect `dead-references.test.ts` exists for: the next session reads it, believes it, and builds on it. All 67 rewritten so the ARGUMENT survives without the name — \"the previous face\", \"the display face\", \"the face it replaced\" — because most of them were making a real point about a face swap and only the proper noun was dead. THE DEFECT IT WAS HIDING, and it would not have been found by grepping for a font name with any other intent: `session-wrapped.ts` carried `CHAR_EM`, a per-glyph advance table \"measured from Archivo Black\", and `fitScale` was still sizing the Wrapped's hero and stat figures with it — a stale MEASUREMENT driving live layout, months after the face it described left the bundle. Nothing failed; the figures just shrank more than they needed to. Measured against the shipped Söhne Halbfett (what `F.black` resolves to since the weight ladder was capped at 600) the two disagree where it matters most: a space is 0.360 against 0.202 (78% too wide), an `m` 0.770 against 0.879 (14% too narrow), and one DEFAULT_EM of 0.682 stood in for ten digits that span 0.402 to 0.639 — a 59% spread the sans cannot even out, since it ships no `tnum`. So every value carrying a space — \"1500 m\", \"10.0 km\", \"2:20 /100m\", which is most of the endurance vocabulary the fitter was ADDED for — measured far too wide. The table moved to theme/face-metrics.ts as `SOHNE_ADVANCE_EM` (the one file allowed to state a font measurement), digits are listed individually, and `ADVANCE_FALLBACK_EM` covers what the face does not carry — `~`, which the extended trial cuts genuinely lack, so an estimate prefix renders in the platform fallback; listed as a known hole rather than discovered as a wrapped tile. THE VISIBLE RESULT: four values pinned in the tests as overflowing now FIT outright — \"1.5 km\" and \"90 min\" in a stat tile, \"1500 m\" and \"10.0 km\" in the hero. They were never too big, they were mismeasured. GUARDED THREE WAYS: face-metrics.test.ts re-reads every advance out of the binary and asserts the table is Halbfett's specifically (Dreiviertelfett fails it, which is the point — if `F.black` moves again this moves with it); scale.test.ts walks apps/ and packages/ and fails on any live source naming a retired face, with `reference/` and `capabilities.ts` exempt as records and this-file exempt because a guard must name what it forbids; and the same test pins `fonts` to the three faces actually loaded, so the rule is a SWAP rather than a ban — retire Söhne one day and the list moves in the same change. core 4199 green, mobile 305, web 235, both typechecks, iOS bundle exports." },
  { id: "typography-measured-rebuild", area: "Core", status: "shipped", title: "Type — the whole system re-derived from the shipped binaries", detail: "THE FINDING, and it explains a complaint nobody could point at: the type system was DESIGNED FOR ARCHIVO + JETBRAINS MONO and had Söhne + Söhne Mono + ITC Garamond swapped underneath it. The swap moved the faces and left every optical constant where it was — the tracking bands were explicitly adopted with the note that their em figures were chosen so the Archivo call sites would not move, which is the right way to MIGRATE a face and the wrong way to finish one. The app has been setting Söhne with Archivo\'s numbers ever since. Measured the seven shipped files with fontTools and rebuilt each axis on what they actually say. NEW `packages/core/src/theme/face-metrics.ts` — the ONE file allowed to state a font measurement: x-heights, cap-heights, ink spans, stems, advances, plus FACE_LIMITS, the four constraints the binaries impose. `face-metrics.test.ts` re-reads them from the binaries in plain JS (OS/2 sxHeight/sCapHeight for the CFF Söhne cuts, whose fields are correct; `glyf` bounding boxes via a cmap-format-4 walk for the TrueType serif, whose fields are NOT) so replacing a font cannot silently invalidate the system built on it. (1) THE LADDER IS GENERATED. 10/11/12/13/14/16/18/22/26/34/46 was an accumulation — consecutive ratios ran 1.077 to 1.353, four one-dp steps at the bottom and two enormous ones at the top, and nothing generated it so nothing could say whether a new rung belonged. It is `TYPE_REF x STEP^n` now: a MAJOR THIRD (1.25) taken in HALF-STEPS (√1.25), seeded at 16 — the size Söhne is fitted for, the one size whose drawn sidebearings need no correction, and therefore the only defensible seed. Granularity follows the eye: consecutive half-steps through the reading band (where rank comes from ink and weight, since at 13 against 14 no size difference can signal rank), FULL steps in the display band (where type is seen before it is read), a step and a HALF to `stat` (which must beat a masthead, not tie with it). caption 12→13, body 13→14, bodyLg 14→16, subtitle 16→18, title 18→20, display 26→28, hero 34→35, stat 46→49; nano, micro and headline do not move. The reading text of a training app gets bigger, which is a REASON rather than a side effect — 13dp is iOS\'s Footnote, and this product is read at arm\'s length by someone out of breath. `RUNG_INDEX` + `promote(role, steps)` walk the ladder in INDICES, and that distinction is load-bearing: `fs.micro x STEP` is 12.3 while the next rung is 12.80 → 13, so promoting a ROUNDED px value is not the same operation and loses the half dp the exact ladder carries. A test pins it. (2) TRACKING IS A CURVE, NOT A BAND TABLE. Two defects beyond carrying Archivo\'s numbers: Söhne is fitted TIGHTER than Archivo (`n` carries 0.144em of sidebearing against ~0.17), so Archivo\'s tightening on top of Söhne\'s own tighter fit over-tightened every heading in the app; and a step table HAS STEPS — 25dp returned -0.015em and 26dp -0.020em, a visible discontinuity mid-ladder that a computed size could land either side of for reasons unrelated to optics. Replaced by `OPTICAL_K x ln(TYPE_REF / size)`: continuous, ZERO AT THE REFERENCE SIZE BY CONSTRUCTION (so the ladder and the curve share one origin), logarithmic because optical size is perceived in ratios exactly like the ladder, anchored on one named constant so it still passes through -0.020em at the old `stat` size — the one value in the old table that was demonstrably right. EVERY MOVE IS A LOOSENING, which is the shape you would predict if the old values had been fitted to a wider face, which the source said they were. The two uppercase voices now COMPOSE caps air with the curve (`0.085em + track(size)`) rather than being flat ems, which leaves the 273 dominant eyebrow sites BYTE-IDENTICAL — the evidence it is the same intent spelled properly. The 48 `label` sites at fs.micro move 0.9 → 1.0, in the direction the model says they always should have. (3) `flush` IS CUT TO THE FIGURE SET\'S INK. 1.00 was the round number nearest the intent. Measured, the mono figure characters span 0.804em (digits +0.729 to -0.011; `/`, which is in `5:12 /km`, +0.732 to -0.072), so a 1.00 box still carried 0.196em of nothing — 9dp at `stat` — which is the same band-of-nothing defect one notch quieter. It is 0.90: the ink plus 0.096em of headroom for React Native\'s baseline placement (RN positions from the font\'s own 1.037em ascent, so a box far below that starts moving the baseline rather than only tightening). The theoretical floor is written down beside it so the next person shaving it knows which number is the design and which is the platform. Safe only because the figure set is CLOSED, which the mono cut guarantees. (4) THE SERIF GOT ITS OWN LEADING, AND ITS RUNG MOVED. `fs.editorial` is inflated 18.6% so the x-heights match — and a leading RATIO multiplies that inflated em, so `snug` was really 1.53x the APPARENT size: body leading on a display-size pull quote, which is precisely the \'reads as a caption for something else\' complaint the token was created to fix, arriving back through the other axis. `lh.editorial` (1.23) is derived from ITC Garamond\'s own 0.9209em ink span x 4/3. The rung itself is 33 (was 30), off the MEASURED 0.4409 x-height rather than the 0.445 literal that had drifted from it; even-rounding was dropped as arbitrary dressing on a derived number. It matches on CAPS too — 20.10 against 20.56 at 28/33 — because Garamond\'s short caps and Söhne\'s tall ones very nearly cancel the compensation, and that coincidence is most of why these two faces can share a screen. The serif takes HALF the optical curve, stated as a factor so it stays visibly a judgement about a second face rather than looking like another measurement. (5) THE WEIGHT LADDER STOPS AT 600 ON THIS GROUND, and this is the largest correction. Light-on-dark IRRADIATES — lit strokes bleed outward into the ground, so every weight reads heavier than it measures, and the standard response is to set one step LIGHTER than on paper. The app had it exactly inverted: Dreiviertelfett (0.16em stem, +78% over Buch) at 298 call sites against 258 for the regular and 81 for the medium — the heaviest cut as the DEFAULT — with 62 of them at fs.body or below, where that is a 2.2dp stroke and the counters of a, e and s close up. Heavy type at reading size is not emphasis, it is mud, and it is most of the answer to why a Söhne/Garamond pairing that should read expensive read cheap. No named style takes `bold` now except `takeover` (the Wrapped\'s full-bleed LIT covers, where irradiation runs the other way and 700 is correct); `hero` dropped to semibold; `subtitle` dropped to medium so the heading band carries a weight step as well as a size one. `F.black` re-points to Halbfett, which collapses it onto `F.bold` — and THE COLLAPSE IS THE POINT: on `ink` there is no weight above 600, so two aliases resolving to one binary says exactly what the rule says. `weightOnGround(w, ground)` is the mechanism for the light direction, one rung up, capped — not a second ladder, which would drift from the first within a week. `F.takeover` is the new alias keeping the 700 binary\'s registration honest. (6) FOUR ROLES THE SYSTEM WAS MISSING: `takeover`, `button`, `buttonSm` (a control is marked out by its container — a filled pill with a 44dp target — so a semibold label on top of that is the inverted weight distribution one component down) and `attribution`, which is SANS on purpose: a serif quote with a serif source reads as one continuous piece of setting, so the source competes with the sentence instead of receding, and the face change IS the demotion. `measureFor(token, chars)` answers max-width in CHARACTERS (66, the centre of the 45-75 band) through Söhne\'s own 0.564em average advance — on a phone every reading rung answers wider than the screen, and that IS the answer. `DESKTOP_PROMOTION` is the desktop scale in full: one step along the ladder, which lands every rung EXACTLY on the next rung, asserted rung by rung — the property a modular scale buys and an accumulated list cannot. ALSO FOUND AND WORTH KEEPING: the shipped Söhne cuts carry NO OPENTYPE FEATURES AT ALL (GSUB empty in all seven), so `tabular-nums` activates nothing and the sans digits stay proportional across eight distinct advances — the system\'s column alignment rests entirely on the mono cut\'s uniform 0.600em advance, which makes \'every measured value is mono\' load-bearing rather than stylistic. `ß` is absent from every Söhne cut, so German is blocked on a fuller licence (see typography-soehne). ITC Garamond\'s OS/2 sxHeight reports 0.220em — not a possible x-height, and exactly half the real one, so anything trusting the table sets the serif at DOUBLE size; the broken field is now asserted so a fixed binary would be noticed. Its figures are hybrid, spanning +0.742 to -0.102, so they are not a consistent height among themselves let alone against a mono column — the metric reason the serif-never-a-figure bar is not negotiable. Web\'s admin table label was 9px, the only text on either client under the system\'s own 10px floor. The full specification — scale tables for both platforms, usage rules, vertical rhythm, the fitness surfaces, dark/light optical adjustments and the face quirks — is `reference/typography-system.html`, regenerated from the code\'s own resolved values so no number in it is hand-typed. core 4193 green, mobile 305 green, both typechecks, iOS bundle exports." },
  { id: "weight-ladder-migration", area: "Core", status: "planned", title: "Weight ladder — migrate the raw heavy-weight call sites onto named styles", detail: "The Aug 2026 rebuild fixed the SYSTEM\'s weight ladder and re-pointed `F.black` to Halbfett, which lands the correction everywhere at once. What it did NOT do is ask, per site, whether the emphasis was wanted at all. The distribution is still heavy-dominant: 286 `F.bold` + 298 `F.black` against 258 `F.reg` and 81 `F.semi`, and a premium type system is mostly regular and medium with sparing heavy accents. Two things to do, and the second is the real one. (1) The 298 ex-`F.black` sites now resolve to the same binary as `F.bold`, so any site that was leaning on the STEP between them has silently lost it — those are the sites to look at first, and the honest fix is a named style (`ty(C, \"hero\")`, `ty(C, \"title\")`) rather than reaching back up the ladder. (2) 62 of them sat at `fs.body` or below. Heavy type at reading size is not emphasis; most of those want `weight.medium` or nothing at all, and deciding that is a per-screen judgement rather than a sweep. Blocked on nothing — it is deliberate, screen-by-screen work, and it wants a rendered proof rather than a grep, which is why it is not bundled into the rebuild." },
  { id: "typography-audit-fixes", area: "Core", status: "shipped", title: "Type — the dead face, the undeclared figure, and the two sizes that were not rungs", detail: "The typography audit found the system strong and named four places where it was not actually in force. All four are closed, on BOTH clients where both have the surface. (1) THE THIRD FACE WAS NEVER SHIPPED. `fonts.condensed` (Archivo Narrow) was declared in the brand tokens and specified in the build brief, and the MOBILE app — the product — never loaded it: app/_layout.tsx calls useFonts with four Archivo weights and two JetBrains Mono weights, and there is no @expo-google-fonts/archivo-narrow in its package.json. Nothing failed, because a declared-but-unloaded family is not an error on either platform (RN falls back to the system face, CSS falls through the stack), so the identity read as three faces and shipped as two for two years. It was NOT unused on web — `cond` had ~30 call sites, every one of them in /admin on the same small uppercase tracked chip — which is why it had to be resolved rather than left: the project rule is that the two admin consoles stay in step, and those chips drew in Archivo Narrow on web against Archivo on the phone, because the phone had no other option. RETIRED, not loaded: the job the condensed face was declared for ('labels / chips') is done by the mono uppercase tracked eyebrow (tracking.label alone holds 216 sites) and always was, so a third face bought a second answer to a settled question plus a webfont download. `cond` collapsed onto `disp` across the 13 admin files, Archivo Narrow left globals.css and core `fontImportUrl`, and theme/tokens.ts records what would have to be true to revisit it (start on mobile: load it, name it in F, name the job neither Archivo nor mono already does). Guarded in scale.test.ts — the declared faces must equal the loaded ones, and the @import may not fetch a family no token names. (2) TABULAR FIGURES NOW HAVE AN OWNER. `font-variant-numeric` was hand-written at ~25 call sites on mobile and at ZERO on web, and neither client's figure PRIMITIVES declared it — so whether a figure held still depended on whoever typed the style. Two costs, and the second is a live defect: a column of stat tiles stops aligning (proportional 1 is narrower than 8), and RollingNumber JITTERS, because each digit is its own box and a units digit rolling 1 → 8 makes its own box wider mid-turn and shoves the digits beside it sideways — the component exists to make one change read as one event and was doing the opposite, on web most of all. The value lives in core scale.ts (`TABULAR_NUMS`, one CSS value, two property names) and both clients spell it once: mobile `TABULAR` in lib/ui.tsx, web `tabular` in lib/ui.tsx. Declared by mobile RollingNumber, AStat, both CountUps (the Wrapped's and the share card's — a figure re-rendered every frame for 900ms is the strongest case there is) and web RollingNumber + Stat. Guarded in apps/web/__tests__/numeric-type.test.ts, which checks BOTH clients together, at the primitives rather than the call sites — a ratchet over the ~25 hand-written sites would have counted the symptom. (3) THE TWO OFF-LADDER CLUSTERS. 87 raw fontSize sites, the two commonest being 17 (17 sites) and 24 (14) — neither a rung. 17 is not a near-miss of anything here, it is SF PRO'S BODY SIZE leaking into an Archivo system, and every site was a card/sheet/section title → fs.subtitle (16), 1dp. 24 went UP to fs.display (26), because the cluster is figures and glyphs — the amount typed into a nutrition row, the weight on the body-progress form, a stat value, a stepper's −, the invite screen's title — and a figure that is the subject of its row belongs above its neighbours, not below (fs.headline is documented as a screen head with no hero, which none of these are). (4) THE 46 CEILING NOW HOLDS SOMETHING. scale.ts said 'a figure larger than 46 is a design smell, not a missing token' and enforced nothing; the app answered with eight figures at 48/48/52/56/56/60/68 — the portion sheet's kcal, the exercise page's hero, the builder's minutes, the food page's energy, the create-food kcal, the volume screen's in-range count, the interval clock — each of them 'the ONE hero figure on a screen', which is the DEFINITION of fs.stat, typed a rung and a half higher by six different people. A 42% spread is six guesses, not a missing rung: all eight are fs.stat, and the hand-tuned absolute line boxes they carried (52 on 48, 56 on 52, 58 on 56, 74 on 68) became one leading(fs.stat, 'tight') — an absolute is only untokenizable while the sizes under it disagree. TWO EXCEPTIONS ARE NAMED rather than merely tolerated: hero.ts's HERO_FIGURE (76), because fs.stat is the ceiling for a figure inside a PAGE and a Wrapped takeover is not inside a page; and GLYPH ART — the watermark emoji bled off a recipe tile or plan cover (78, 96, 118), which carry no font face at all and are pictures sized to a card. The new HARD rule keys on a face being present on the line for exactly that reason, so a fourth watermark needs no exemption. RATCHETS RE-PINNED per the file's own discipline (re-measure every ceiling after any sweep, not just the one you moved): fontSize 87 → 48, lineHeight 48 → 43, borderRadius 162 → 160." },
  { id: "font-face-weight-sweep", area: "Core", status: "shipped", title: "Type — weight is a FACE, never a `fontWeight`", detail: "REPORTED AS: \"in the individual page of breakfast/snacks the font is totally bad and not consistent\". One mistake, repeated 103 times. `F` in apps/mobile/lib/ui.tsx names six SEPARATELY LOADED faces, and expo-google-fonts registers each under its own family name ('JetBrainsMono_400Regular', 'Archivo_700Bold'). So `fontFamily: F.mono` declares a family holding exactly ONE face, and a `fontWeight: '700'` on top of it asks for a weight that family does not hold — the two platforms then disagree, which is the whole symptom: iOS resolves the descriptor back to its real family and picks the nearest loaded face, Android smears a synthetic bold over the regular. Labels that got it right sat beside labels that did not, so the screen read as \"inconsistent\" without being nameable. Nothing was ever gained by it: F.monoBold, F.semi, F.bold and F.black ARE the heavier faces. DONE IN TWO PASSES. (1) The FOOD domain, which was the surface reported and the worst of it — 31 sites across nutrition + its kit, panels, quick-add, pantry, user-recipes, recipe-library, water, copy-day and freshness-sheet, several stacked inside one sheet (the portion editor's ± steppers and both its commit pills, the meal-part CTA), plus the picker's source counts, the facts-panel figures and the Understood kicker. (2) The remaining 72 across 28 files — percent-program, coaches, feed-card, protocol, performance, body-progress, social-kit, wearable-sheet, history-views, coach-rail, workout, leaderboard, hero, kit and the tail. THE RULE APPLIED: the DECLARED FACE WINS. Where a family was already named it stayed and the phantom weight went; only `F.mono` — the sole *regular* of a pair — was promoted to `F.monoBold`, which is what iOS was resolving to anyway, so the sweep is a no-op on the shipping platform and a fix on the other. A weight that VARIES became a varying face (`fontFamily: on ? F.monoBold : F.mono`), which is the shape the readiness/freshness Step twins and six ternary sites took. Text with NO family at all — two coach rows drawing in the platform UI font — was given one. The one case that could not simply be renamed was percent-program's ink ramp: four weights (700/700/500/400) off a family holding one face. It already had a second, finer channel — opacity (1 / 0.92 / 0.72 / 0.55) — which is what actually separated its tiers, so the two heavy tiers took the bold face and opacity kept doing the rest. THE ONE EXEMPTION is components/error-boundary.tsx, and it is a real one: that file is deliberately provider-free ('no theme, i18n, session or app font: any of those may be the thing that failed'), so a crash fallback naming F.* would draw nothing when the font load is what threw — platform font, platform weight, on purpose. GUARDED HARD in apps/mobile/lib/design-tokens.test.ts: any `fontWeight:` outside the error boundary fails the suite, so the rule cannot be re-introduced one call site at a time the way it arrived." },
  { id: "sweep-arrow-emoji-glyphs", area: "Core", status: "shipped", title: "Design sweep — raw arrows + emoji → drawn glyphs", detail: "~275 raw `→` characters (Archivo's subset lacks U+2192 — cta-label.tsx exists precisely for this), plus 🏆/🔥/⚡/🔒/📅/🏃/🏁/📈 emoji and dingbat controls (⠿ ⧉ ⛓ ▾ ✕) across both clients' aurora screens. Replace with CtaLabel / drawn AuroraIcon glyphs (a few new icons needed: trophy, flame, bolt, lock, calendar); add a lint/grep guard so they don't return. Worst files: nutrition ×2, today/home, workout-wrapped. SHIPPED (Aug 2026): UI-chrome emoji replaced with drawn glyphs across both clients (trophy ×11, lock, bolt, calendar-event, arrow-up; Private tab de-emojied; wrapped CTA via CtaLabel); mobile week-rail day glyphs ported to web's exact SVGs. REMAINING BY DESIGN: share-story card content emoji. FINISH-LINE PASS (same sweep): stopwatch+moon glyphs added and the device-ad grid converted on BOTH clients; ~60 CTA/standalone arrows now render through CtaLabel/ArrowGlyph (mobile gained the SVG ArrowGlyph twin); value→value progressions deliberately keep the → as data notation; 2 Text-embedded glyph pairs left (mobile home disclosure, admin security marker). TYPOGRAPHY CONSOLIDATION (Aug 2026, same sweep): 197 sub-pixel font sizes snapped to integer rungs and 625 letterSpacing snaps across both clients — web trackings collapse to .08em/.12em/-.01/-.02/-.03em, mobile to 0.9/1.2/-0.3/-0.5/-1 dp; the uppercase kicker is now exactly two recipes (11/.08em and 10/.12em). Share-story art and deliberate stretched effects excluded." },
  { id: "sweep-radius-spacing-codemod", area: "Core", status: "shipped", title: "Design sweep — radius + spacing consolidation codemod", detail: "~30 distinct border radii and off-scale spacing literals (14 is the single most common mobile padding and is not a space.* rung; 11/13/9/7 add ~160 more uses) collapse to the token set: radius 28 (card) / 16 (field-nested) / 12 (tile) / 999 (pill); spacing snapped to the 4-pt rungs. Mechanical codemod per screen family with visual spot-checks; the audited-screen inventory lives in the panel critique (session Aug 2 2026). SHIPPED (Aug 2026): radii snapped app-wide to the 28/16/12/999 classes and odd off-scale spacing to the 4-pt grid on both clients (flat r22 cards gained the card shadow; profile's 4dp column misalignment fixed; today's 2px head insets dropped). FINISH-LINE PASS (same sweep): the even off-scale band is now empty too — ~400 web + 559 mobile snaps put every aurora padding/gap/margin on {4,8,12,16,20,24,32,40} (micro-nudges ≤10 and structural one-offs excluded by design)." },
  { id: "sweep-icon-stroke-grid", area: "Core", status: "shipped", title: "Design sweep — icon stroke = f(size) grid", detail: "The Aurora icon set renders at 18 distinct strokeWidths and every integer size 11-22; the same Today-hub glyphs are stroked 3.5 on web vs 5 on mobile (43% heavier) at the same 21px. Define one stroke-per-size table in @hybrid/core (e.g. ≤16px → 4.5 viewBox units, ≥20px → 3.5), apply through both renderers, and regenerate the mobile PNGs at the matching weight. Also: draw distinct glyphs for the arrow-up×5 / list-play×3 semantic collisions (statistics/performance/trends/velocity/leaderboard; exercises/video/feed) and retire the duplicate play/list-play + check/check-circle path data. SHIPPED (Aug 2026): auroraIconStroke(size) is the one size→stroke rule consumed by both renderers; mobile AuroraIcon renders true vectors from the shared paths (PNGs remain only for the native tab-bar fallback); hub glyphs 3.5 both clients; check/check-circle + play/list-play are distinct drawings; trophy/flame/bolt added." },
  { id: "sweep-today-hierarchy", area: "Web", status: "shipped", title: "Design sweep — Today hierarchy rebuild", detail: "The panel's biggest structural finding: ~20 top-level modules, the Start CTA below the fold, a 44px done-count and 46px mood word outweighing the primary action, and the two upsell AccessCards as the largest cards on screen. Rebuild to three modules above the fold (session card w/ Start, week strip absorbing WeekVerdict+AlsoToday+streak, one-row Feel), demote the upsells to a single quiet row, delete the greeting/live-dot/wordmark-rule ornaments. Web + mobile in the same change. SHIPPED (Aug 2026): greeting/live-dot/wordmark-rule deleted (hanko kept), the two 220px AccessCards + head collapsed to one quiet DoorRow-idiom ✦ row (funnel routing preserved), done-count 44→26 and mood word 46→26, 🔥→flame + 📅→calendar glyphs, Sheet sub widened to ReactNode; both clients, typechecks green." },
  { id: "sweep-nutrition-hub", area: "Web", status: "shipped", title: "Design sweep — Nutrition hub consolidation", detail: "~37 interactive targets and nine visually identical ink2/r18 rows (4 meal + 5 menu) on one scroll; the 200px calorie ring is the visual hero yet not interactive (and is 104 absolutely-positioned DOM spans on web). Consolidate: ring+macros as one animated SVG canvas (count-up, tap → diary), meal rows as one ledger with a persistent Add action, the 5 menu rows as a single link row. Ring tick weight also drifts 6px web vs 9dp mobile — one geometry. SHIPPED (Aug 2026): ring rebuilt as one SVG track+arc (104 spans → 3 nodes), ring+macros merged into one tappable card routing to the diary (web count-up honours reduced motion; mobile PressScale), menu ladder → one wrapping link row, hub row padding unified 14×16, HUD anchors preserved; both clients." },
  { id: "sweep-mobile-chart-axes", area: "Mobile", status: "shipped", title: "Design sweep — mobile exercise charts get their axes back", detail: "The mobile exercise-page scatter/surface charts dropped ALL gridlines, kg ticks, rep ticks and isoline labels (web keeps them); the body-weight chart is a Recharts line w/ axes on web but a 64dp bar sparkline on mobile. Restore reduced-density axes (kg + rep ticks minimum) on mobile and pick ONE chart identity per dataset across clients. Add page dots to the mobile chart pager (web has arrows; mobile has no affordance that 11 more slides exist). SHIPPED (Aug 2026): scatter regained kg gridline ticks / rep ticks / isoline labels at reduced density; surface regained per-bin rep + W1/mid/last labels; pager segment strip verified at parity (web's edge arrows are a pointer affordance, strip is the shared indicator)." },
  { id: "sweep-plan-cover-hues", area: "Core", status: "shipped", title: "Design sweep — per-goal plan cover hues", detail: "19 goals share 4 accent colours (plans.ts), so five different goal covers render literally the same wash; mobile covers also drop web's radial hotspot layer and accent-tinted chip (hardcoded #edefe8). Widen the goal palette (≥8 hues or a hue-per-goal derivation), add the radial layer + tinted chip on mobile, and align the gradient recipe (web color-mix stops vs mobile alpha stops) so the same plan reads as the same cover on both clients. SHIPPED (Aug 2026): 19 unique hand-authored hues in the muted brand register (originals kept for their most semantic goals); mobile cover gained the radial hotspot layer, 82%-white accent-tinted chip, and web-matched wash stops (covers + goal tiles)." },
  { id: "screen-transitions", area: "Core", status: "shipped", title: "Screen transitions + motion system (Wave 1)", detail: "WAVE 1 SHIPPED on both clients. Before: one 240-260ms fade-and-rise reused for every navigation (web `.aurora-enter` on a keyed wrapper; mobile `useEntrance()`), so a tab switch, a drill-down and a modal looked identical, and on web the outgoing screen was unmounted with no exit half. TOKENS: packages/core/src/motion.ts is now the single source of truth for both clients — SPRINGS, not duration+bezier, for everything positional (slide .34/1.00 -> 403ms, zoom .46/.92 -> 429ms, sheet .38/.86 -> 378ms, plus the already-shipped nav .32/.74 -> 365ms), expressed as SwiftUI response/dampingFraction because a bezier carries no velocity and therefore cannot be interrupted or retargeted, which makes gesture-tracked transitions impossible. `springToCss()` integrates the physics into a CSS linear() easing (with a bezier fallback declared first for engines without linear()); `springToRN()` converts the same parameters to RN's mass/stiffness/damping. Beziers survive only for opacity. Spring durations are solved NUMERICALLY for VISUAL settle (within 0.5% of target and staying there), not from the envelope bound: the analytic bound ignores the critically-damped `1+wt` term and made the first cut of these tokens 521ms (slide) and 641ms (zoom), both over the system's own 450ms ceiling — caught by motion.test.ts, which now guards it. DIRECTION FROM HIERARCHY: `screenTransition(from,to)` derives sibling/push/pop/replace from the bottom-nav order (NAV_ROOT_ORDER, with client-id aliases like log->train, you->profile), so both clients agree what a move means; back is the exact inverse of forward, and two unrelated leaves crossfade rather than inventing a direction. WEB: the shell lazy-loads nearly every screen, so keeping both trees alive for an exit would double-mount chunks — instead `lib/use-screen-transition.ts` wraps setScreen in the View Transitions API (`startViewTransition` + flushSync), which snapshots the outgoing tree in the browser, giving a real paired exit with no structural change; direction is published as data-nav-kind/data-nav-dir on <html> and globals.css picks the keyframes. `.motion-screen` carries `view-transition-name` so ONLY the screen travels (sidebar, header and banners stay put), with the default root cross-fade suppressed. Where the API is unsupported the state update just applies — the old behaviour, no worse. MOBILE: per-route native animation options on the expo-router Stack (rendered natively by react-native-screens 4.25 — previously only 2 of the Stack.Screen entries set any options at all), the tab shell pinned to `animation: none` since it is a root not a push, and `useEntrance()` retimed onto the shared sheet spring. SHEET PARENT-RECEDE on both clients: the presenting screen scales to 0.92 with its corner radius growing to a device radius and dimming, so the sheet sits on a real stack — which is what lets the scrim drop from 0.6 to 0.28. Web drives it via a `data-sheet-open` flag on <html> plus `.motion-recede-host` on the shell root, and Sheet now PORTALS to <body> because the transformed host would otherwise trap a position:fixed modal; mobile drives it through a new root-mounted `lib/sheet-recede.tsx` provider (same shape as NavScrollProvider — a Modal renders in its own native window and cannot reach the shell), reference-counted so a nested sheet does not un-recede early, with the brightness drop drawn as a black wash rather than a `filter` because RN filter support is uneven. REDUCE MOTION is a SUBSTITUTION, never a deletion: every transition cross-dissolves over durations.reduced (150ms) instead of cutting instantly, because an instant cut removes the user's only signal that the screen changed. This required scoping the long-standing global !important reduced-motion backstop in globals.css, which was collapsing the substituted dissolve to 0.001ms and silently reproducing the exact defect. GUARDS: motion.test.ts (springs never exceed the ceiling, slide never overshoots, sheet does, RN conversion round-trips, back inverts forward, reduced-motion duration is non-zero) and apps/web/__tests__/motion-tokens.test.ts, which REGENERATES every curve from core and fails if the pasted globals.css block drifts by so much as 1ms. TESTFLIGHT BUILD 80982819 (v1.0.0) uploaded 2026-07-26 from commit 973bc69 — GitHub Actions run #139, all steps green on a clean macOS runner. That run independently CONFIRMS the reanimated/worklets dependency fix: `pnpm install --frozen-lockfile` and `expo prebuild` both passed with no pre-existing node_modules, which is exactly what a resolution break would have failed. On-device behaviour is now AWAITING SIGN-OFF (internal testing, available immediately, no beta review) — check: sibling nav slides sideways while a drill-down pushes with the parent receding; the exercise-card figure travels into the stats-page hero (the hand-rolled FLIP, the most likely thing to look wrong — an offset landing or an early text reveal means the measurement timing needs adjusting); a sheet scales the screen behind it back with the scrim reading LIGHTER than before; all three repeated with iOS Reduce Motion on should cross-dissolve, never cut; and the edge-swipe back should work on detail screens and be dead on the live workout screen. VERIFIED in headless Chromium against the real globals.css: all five kinds fire the right keyframe pair, exits (160ms) leave faster than entrances (378ms) arrive, and every kind substitutes a 150ms dissolve under OS-level Reduce Motion. react-native-reanimated is now declared explicitly in apps/mobile/package.json (it was already a resolved runtime dependency of @expo/ui and expo-router's drawer, so nothing changed at runtime — it just stops a hoisting change from silently breaking the build). NOTE a pre-existing peer conflict, not introduced here: expo-modules-core wants react-native-worklets ^0.7.4||^0.8.0 while reanimated 4.4.0 requires 0.9.x, and 0.9.1 is what resolves; worklets is deliberately NOT declared so we do not assert a version expo disagrees with. Waves 2 and 3 also landed — see the wave-3 entry for what remains. Deck + working app reproduction: design/screen-transitions-ideas.html." },

  { id: "loading-crossfade-adoption", area: "Core", status: "shipped", title: "Skeleton \u2192 content \u2014 the hand-over at the early-return sites", detail: "SHIPPED (Aug 2026), and the block that stalled it turned out to be the wrong shape of fix. THE BLOCK, as recorded: `LoadSwap` cross-fades a placeholder into the content that replaces it, but the shape almost every loading state is written in \u2014 `if (!data) return <Loading />` \u2014 gives it nothing to fade BETWEEN, because the component returns the placeholder or the content and never both. Closing that was scoped as a body-move per component: extract the body into a child so it can take the data as a prop. For these screens that would also have meant threading their derived values and their mutation handlers through as props, which is why it kept being deferred. THE FIX INSTEAD: LoadSwap already rendered its children only once the data had landed \u2014 making children LAZY (`ReactNode | (() => ReactNode)`, both clients) turns that from observed behaviour into an enforceable contract, and a body that stays in its own scope needs no props at all. The per-site change is then a wrap, not a move. ~33 sites across 29 files: the whole admin console (19 sections), my-social-profile, coaches, leaderboard, history\u2019s three-way empty component, team-compare and team-monitor (whose `body()` was already exactly the lazy shape), the feed, saved, the post page, the followers/following lists and the invite claim. The three-way branches (loading / empty / data) fold the empty state INSIDE the swap, so the hand-over covers whichever lands \u2014 an empty history and a failed fetch now both arrive where the skeleton was. Two specific bugs fixed on the way: audit.tsx\u2019s placeholder was a SIBLING of the list rather than a branch of it, so the log appeared beside a spinner that then vanished; and mobile LoadSwap sat its content in a wrapper that sized to its children, so the leaderboard\u2019s `flex: 1` FlatList would have lost the height it needs to scroll the moment it moved inside \u2014 hence the new `fill` prop. DELIBERATELY LEFT, with reasons rather than as an oversight. (1) session/[id].tsx: it hosts the SHARED-ELEMENT DESTINATION for `sessionHero`, whose FLIP measures the target on mount plus one frame; wrapping it in a fading layer would race that measurement, and the pair is a shipped win not worth risking for a 250ms fade. (2) u/[handle].tsx\u2019s header skeleton and session/[id].tsx\u2019s: both live in their OWN early-returned screen, so the two states never share a position to cross in \u2014 a LoadSwap there would unmount the moment the data arrived and is a fancy spinner, not a hand-over. (3) invite/[token].tsx\u2019s trailing placeholder is the CLAIM in flight \u2014 an in-flight action, which \u00a712 says is what a spinner is actually for. WEB is unchanged and was already right: its four remaining \u2018Loading\u2026\u2019 strings are inline status beside content ALREADY on screen (\u2018more is loading\u2019), which is a different thing from \u2018content is coming\u2019 and correctly reads as a word rather than a shape. With this and mobile-stat-tile\u2019s primitive, the audit\u2019s \u2018twenty of twenty-two fixed everywhere\u2019 note is out of date in the right direction \u2014 see motion-audit-wave4." },
  { id: "fullscreen-cover", area: "Core", status: "shipped", title: "Full-screen cover \u2014 the third spatial claim (entering the live logger)", detail: "SHIPPED on both clients. The app had two honest spatial claims \u2014 a PUSH is deeper in the same tree, a PRESENT is a detour you will finish and leave \u2014 and no third one, so the biggest state change in the product was also its quietest: starting a workout takes the tab bar away, turns the back-swipe off and makes the app a stopwatch with a keyboard, and it arrived as the same right-slide as opening Settings. CORE: `COVER_SCREENS`/`isCover`, and `screenTransition` gains `cover`/`uncover` tested FIRST \u2014 before the detour test and before `back` \u2014 because a mode change is a property of what the destination IS and nothing about where you came from can change it (entering the logger from Nutrition is not a sideways sibling slide that happens to land in a stopwatch). Timed on `springs.zoom` (429ms, the longest in the system): a mode change earns the longest ordinary transition, and this is the audit\u2019s proposed 350ms honoured in the system\u2019s own vocabulary rather than by inventing a duration+bezier for the one transition that most wants to be interruptible. Both halves ride it, INCLUDING the exit \u2014 a cover leaving is the app coming back, not a thing being dismissed. NEW CONSTANTS: motion.coverScale .88 / coverBrightness .55 / coverBlur 8px, all further than the sheet recede\u2019s .92/.72 and guarded as such, because a sheet\u2019s parent is coming back in a moment and a covered screen is not. The BLUR is the part that says \u2018not for you right now\u2019 where a recede alone only says \u2018behind\u2019 \u2014 it is the focus blur screen-transitions-wave3 carried as a separate flourish, and it belongs to the transition. MOBILE: `presentation: \'fullScreenModal\'` + slide_from_bottom, derived from the shared list (MOBILE_COVERS) rather than retyped; `gestureEnabled: false` was already right and now agrees with the presentation. WEB: the rise plus the shell\u2019s own chrome LEAVING with the mode \u2014 the sidebar and the pill bar are unmounted on a covered screen (they persisted through the logger before, which is N5 drift at the one screen where it matters most), the 120px pill-bar clearance goes with them so the reservation cannot become a dead band, and both are lifted into their own view-transition groups scoped to data-nav-kind cover/uncover (the hub-chrome idiom) so they drop away and come back with the cover instead of popping out of existence. THE ONE TRAP, documented at the source and in a test: `log` means DIFFERENT THINGS on the two clients \u2014 web\u2019s `log` is the live logger, mobile\u2019s `log` is the Train launcher TAB and its logger is `workout`. Both ids are listed because a mode must be a mode on both clients, which is exactly why mobile reaches the set through an explicit route list and never by testing a pathname, where `/log` would wrongly read as a mode." },
  { id: "mobile-stat-tile", area: "Core", status: "planned", title: "A shared stat tile on mobile (so its figures can roll too)", detail: "THE PRIMITIVE SHIPS, and the first slice of the sweep with it (Aug 2026). `AStat` is in the mobile kit with web\u2019s exact anatomy and web\u2019s exact rule about which values MOVE: a bare string/number is a figure and rolls through RollingNumber, a caller-composed tree (a unit, an icon) renders verbatim. Guarded by apps/web/__tests__/parity.test.ts so the two tiles cannot drift apart again. SWEPT SO FAR: analytics.tsx, which also had a file-local component NAMED `AStat` with different type (fs.nano label at 1.2 tracking over a 28pt figure, against the kit\u2019s fs.micro at 0.9 over fs.hero) \u2014 a name collision that would have confused the next reader, now a plain grid cell around the shared tile; and admin/_kit.tsx\u2019s `Stat`, which DELEGATES to AStat and so carries its thirty-six call sites across five console sections with no call-site churn. Roughly fifty tiles now roll. ALSO FIXED HERE, on BOTH clients: the sub-line\u2019s tone. It was `startsWith(\"\u2212\") ? RED : LIME`, i.e. everything non-negative was painted in the GOOD accent \u2014 so web rendered `sub={dateStr}` (a DATE) in chartreuse, along with \u2018not enough data\u2019, \u2018ARR $1.2M\u2019 and every portion subtitle. Colour encodes state in this app and a green date encodes nothing. Core `statSubTone` now tones ONLY a sign-led sub (+ \u2212 \u2191 \u2193), which preserves the admin panels\u2019 deliberate minus-marked thresholds (\u2018\u2212below 40\u2019, \u2018\u2212runs dry\u2019) and makes every caption neutral. THE REMAINDER, measured rather than guessed \u2014 a survey found 28 private look-alikes and ~50 inline hand-drawn sites across 29 files, ~85 call sites in total, which is far more than \u2018thirty-one edits\u2019 implied. It is not one sweep, because three structural things block a blind one. (1) THE PROP-NAME SPLIT: `color` (admin) vs `c` (kit) vs `accent` (analytics) for the same thing \u2014 adapters absorb it today, a real sweep should settle on one. (2) THE serifIf FIGURES: ~11 sites draw the figure through `serifIf(scheme, F.black)`, which swaps Archivo for Shippori Mincho on the Kyoto Hour light theme. AStat has no way to express that, so sweeping them blind would silently change how they read on light. Decide whether the tile takes a `serif` opt-in or whether those figures are a different rung. (3) THE FIGURE-BESIDE-LABEL ROW: performance.tsx (twice, near-identical), pantry, feel-prompt, freshness-sheet, wearable-sheet, org, longevity and tactical put the figure to the LEFT of a label column. That is a genuinely different layout from the label-over-figure column AStat draws, not a variant of it. DELIBERATELY NOT SWEPT, with reasons: lib/share.tsx `Stat`/`StoryStat` render into a PNG capture, where a rolling figure is meaningless and the sizes are width-proportional by design; coach-rail.tsx `Stat` is a 13pt mono proof-strip cell, not a display figure; and exercise-widget/exercise-page `heroStyle` are the two ends of a shared-element pair and must stay plain style objects the FLIP can measure. NOTE for whoever continues: the kit\u2019s label is fs.micro/tracking.label (matching web), while the plurality of hand-drawn sites use fs.nano/0.9 \u2014 so the sweep carries a deliberate 1dp label bump toward the web twin, which is the correction, not a regression." },
  { id: "shared-elements-remaining", area: "Core", status: "planned", title: "Shared elements \u2014 the pairs still cutting", detail: "Four participants ship and all four are wired at EVERY entry point: exerciseHero, sessionHero, planCover, and personAvatar \u2014 which now covers all twenty doors to a person\u2019s page (the feed\u2019s live strip and every post header, discover, saved, the leaderboard, notifications, a coach\u2019s client list, the people tabs, and the coaches list), not the one list it shipped with. Reaching them all needed the arm to stop depending on a click target: `armPerson(handle)` on web (every Avatar tags itself with its handle) and `usePersonSource()` on mobile (every Avatar registers its node under the handle and deregisters on unmount), because half those doors are callbacks fired from inside a post card with nothing to walk up from. CALENDAR DAY \u2192 day detail SHIPPED since (motion-audit-wave4, SHARED_ELEMENTS.calendarDay \u2014 the first same-screen pair), and the PR badge \u2192 summary handoff shipped there too (SHARED_ELEMENTS.prBadge). WHAT IS LEFT, in the audit\u2019s own priority order: CHART \u2192 EXPANDED CHART, which is the interesting one and is NOT a clone flight at all \u2014 the axes and gridlines must PERSIST while the frame grows, so it wants the destination to inherit the source\u2019s scales rather than re-render its own, a data change rather than a motion one, and the reason it is listed first despite being the most jarring cut in the analytics screens; FOOD ROW \u2192 food detail (the macro ring); DEVICE PANEL \u2192 full recording (the route line). Separately tracked because it is a different mechanism again: the session accessory expanding into the logger by matched geometry (the Apple Music mini-player moment) \u2014 the system slot is already correct, it just navigates instead of expanding." },
  { id: "screen-transitions-wave3", area: "Core", status: "planned", title: "Screen transitions — Wave 3 remainder", detail: "Waves 1, 2 and most of 3 have SHIPPED (see screen-transitions). WAVE 3 DELIVERED: CONTINUOUS MASTHEAD COMPRESSION on both clients — the big screen title shrinks and the secondary lines recede as a function of scroll offset, not a stepped swap at a threshold. Web gained the shared scroll signal it was missing (lib/use-scroll-collapse.ts), the twin of the mobile NavScrollProvider, published as a REGISTERED CSS custom property (--scroll-collapse, @property <number>) rather than React state: it fires every scroll frame, so re-rendering the shell 60x/sec to move a title would be absurd — CSS does the interpolation and nothing re-renders. The masthead is sticky with a frosted backdrop that fades and blurs in over the same 0->48px range the mobile nav pill already collapses on, so the clients compress in step; mobile reads that same `collapse` value (one signal, two subscribers). Deliberately NOT suppressed under Reduce Motion: this is direct manipulation tracking the scroll position, not an animation, exactly as iOS keeps large titles collapsing. HAPTICS AUDITED: all 14 call sites reviewed; one removed (the exercise-anatomy sheet-open buzz — the only sheet in the app that fired one, so it read as a glitch rather than feedback). The rest kept and justified: every workout haptic is a genuine detent (rest target reached, set banked, drag picked up, reorder committed, pause toggled, timer finished) and the two nav haptics are consistent and a defensible idiom for a custom pill bar rather than a stock iOS tab bar — stripping them would have been applying a rule past the point where it helps. CORRECTION (Aug 2026 motion audit, audit/09): the claim that every site is 'user-gated by prefs.haptics' is NO LONGER TRUE and the count is now 17, not 14 — liquid-seg.tsx:154 and three sites in volume.tsx (216/230/458) fire regardless of the preference, and volume's Toggle uses selectionAsync where a switch wants Impact Light. Tracked for fix in motion-audit-followups. REMAINING. (1) AMBIENT FIELD COUNTER-DRIFT — deferred on a structural blocker, not on effort: web could do it (lg-field is position:fixed at shell level and already survives a screen change), but mobile renders AuroraField PER-SCREEN inside AuroraScreen, behind the Stack's opaque `contentStyle: { backgroundColor: palette.ink }`. A root-level field would be completely occluded, so parity needs every Aurora screen's background made transparent — real visual risk across the whole app for a low-impact flourish, and shipping web-only would create exactly the drift the parity rule exists to prevent. (2) NAV LENS -> FAB MERGE + scrolled-capsule merge. CORRECTION (Aug 2026): the blocker recorded here — 'needs @expo/ui to expose GlassEffectContainer/glassEffectID' — is no longer true, and was already stale. @expo/ui 56 ships GlassEffectContainer, Namespace and the glassEffectId/animation modifiers, and the Today hub dock now uses all of them for real (see swiftui-kit / today-hub-floating-pills): three capsules in one container that fuse at zero gap and flow between frames. So the merge idiom is available and proven at bundle level; what remains is a device to verify it on, and the fact that this particular target no longer exists as described (the hand-built floating nav capsule and its GlassMorphSelector were deleted when the bottom bar became the real system tab bar — see motion.ts). Re-scope before building. (3) LIVE-WORKOUT FOCUS BLUR on entering the logger \u2014 SHIPPED as part of `fullscreen-cover`, where it belongs: the blur is what makes the recede read as a mode change rather than as a sheet, so it is half of that transition and was never a flourish to add beside it. (4) CELEBRATION HANDOFF — SHIPPED (motion-audit-wave4): core livePrLifts badges the record lift's card the moment the record set banks, and on finish the badge flies into the summary's trophy chip (SHARED_ELEMENTS.prBadge), on both clients. (5) INTERRUPTIBLE BACK ON WEB — needs the shell's `{screen === \"x\" && …}` chain extracted into renderScreen(id) so two screens can coexist during a drag. CORRECTION (Aug 2026): the prerequisite recorded here, browser-history integration, SHIPPED in motion-audit-followups — setScreen pushes a real entry and popstate applies the move with the direction the browser travelled. What is left is the two-screens-at-once refactor, a navigation change with regression risk rather than motion work. (6) ZOOM WHERE A CARD BECOMES A SCREEN — SHIPPED for the cover pair (SHARED_ELEMENTS.planCover, motion-audit-followups wave 2): a goal tile grows into the screen it opens on both clients, and mobile's FLIP learned to fly a surface rather than only text to make it possible. The remaining pairs are tracked as shared-elements-remaining. CUT ON REVIEW, not to be re-proposed: staggered list entrance (Material idiom; iOS lists arrive as one unit and staggering delays content) and the specular sweep across the nav glass (glass refracts what is behind it; it does not emit a highlight because you navigated). NATIVE motion still needs a TestFlight build to sign off — it cannot be exercised in this sandbox." },

  { id: "mobile-render-tests", area: "Core", status: "shipped", title: "Mobile render tests — a gate that actually renders", detail: "THE GAP THAT LET BUILD 81628102 SHIP BROKEN. Every tappable surface in the app lost its background, border, radius, width and flexDirection, because PressScale handed a FUNCTION to an Animated.createAnimatedComponent wrapper's `style` prop. React Native's AnimatedProps.js gates on `typeof value === \'object\' && value != null`, so a function skips the branch entirely: AnimatedStyle.from() never runs, the AnimatedValues never join the node graph, and they reach the view as raw objects where numbers belong. Aliasing Pressable -> PressScale in 83 files meant one line took out the whole app. THE POINT IS THAT NOTHING COULD HAVE CAUGHT IT: `tsc` passes because Pressable's own type accepts BOTH a style object and a style function; `expo export` passes because bundling is not rendering; and apps/mobile/vitest.config.ts scopes tests to PURE modules only ('anything that imports react-native is out of scope by construction'), so there is no gate anywhere in the repo that renders a mobile component. Web has real browser verification via Playwright; mobile has none, and the asymmetry is invisible until a render-only defect ships. WHAT TO BUILD: a render-level test setup for apps/mobile — react-test-renderer (or @testing-library/react-native) plus a vitest environment that can load react-native, added through pnpm at the workspace root (NOT npm: running `npm i` inside this pnpm workspace flattened node_modules and removed 887 packages, recoverable only with `rm package-lock.json && pnpm install --frozen-lockfile`). Minimum coverage worth having on day one: mount PressScale with a plain style object, an array, and Pressable's function form, and assert the caller's declarations survive to the rendered tree; mount AuroraScreen and one card-bearing screen and assert non-empty resolved styles. Until that exists, a change to a SHARED UI PRIMITIVE must not be swept across the app in the same change that introduces it — land the primitive, verify it on a device, then sweep. INTERIM GUARD (shipped): apps/mobile/lib/ui.test.ts extracts resolvePressStyle so the invariant is testable, evaluates the real source rather than a copy, reads RN's actual gate condition out of node_modules and runs our value through it, and asserts the JSX call site still passes an array. Verified to FAIL on the exact shipped code and pass on the fix. SHIPPED: apps/mobile/vitest.config.ts now runs TWO projects — `pure` (the old node-environment boundary, unchanged) and `render`, a jsdom project that mounts real components. React Native itself cannot run in node (its source ships as Flow), so `react-native` resolves to `react-native-web`, which renders the same component tree to a DOM the tests assert on; the native edge (blur, gradient, SVG, SwiftUI, haptics, status bar, safe-area, router, secure-store, AsyncStorage) is stubbed in test/stubs, and screens mount under the app's REAL providers via test/render.tsx (theme, language, template, nav-scroll — a gate that stubs our own providers would be testing a tree the app never renders). COVERAGE (18 tests): the day-one list this entry asked for — PressScale with a style OBJECT, an ARRAY and Pressable's FUNCTION form, asserting the caller's background/radius/width/flexDirection survive to the rendered surface; AuroraScreen + ACard + APill asserting non-empty resolved styles and that a caller's radius is honoured — plus the hero's docked sub-rail (see hero-system): its placement contract, its behaviour under a driven scroll, and History's own list-header placement. EVERY GATE WAS FALSIFIED BEFORE BEING KEPT — each was re-run against the defect it claims to catch and observed to fail: the PressScale suite goes red on the exact 81628102 code path (the surface loses its background entirely), and the sub-rail suite goes red on the shipped dock bug (the rail translates 38dp while the hero is still collapsing). A gate that passes both before and after is not a gate. THE BOUNDARY, stated because it decides what to write next: there is no layout engine behind jsdom, so `onLayout`/ResizeObserver never fire and no MEASURED position is reachable — which is its own argument for deriving positions from geometry rather than measuring them (exactly what the sub-rail fix did). It proves the tree we BUILD — what nests inside what, in what order, with which resolved styles — not native rendering. Runs in CI already: the root `pnpm test` fans out through turbo to this package — where the first run went red for a reason worth keeping: jsdom 30 pulls undici 8, whose engines are `>=22.19.0`, and CI runs Node 20, so every render file died with `webidl.util.markAsUncloneable is not a function` while passing locally on Node 22. jsdom is pinned to 29 (undici 7, `>=20.18.1`) and the suite is now verified against a real Node 20 before pushing, not against the engine ranges. Bumping jsdom means checking the transitive undici against CI's Node first — the failure surfaces as a worker that will not start, not as a version error." },
  { id: "motion-audit-followups", area: "Core", status: "shipped", title: "Motion & transition audit — the Apple-level pass (audit/09)", detail: "AUDIT + ALL FIVE TOP FIXES SHIPPED on both clients (audit/09-motion-transition-audit-2026-08.md; live before/after bench published as an artifact). The audit read the SHIPPING code, not the decks: a 9/10 token system with 5/10 adoption — motion.ts (springs over beziers, numerically-solved settle time, direction from hierarchy, Reduce Motion as substitution) is better than most shipping iOS apps and the components largely did not use it. WHAT SHIPPED. (1) BROWSER HISTORY ON WEB — setScreen pushes a real entry and popstate applies the move with the direction the browser TRAVELLED (an index stamped on each entry), so Back is the exact inverse of the navigation that got you there and Forward replays rather than re-inverting. Before, the shell held its whole location in useState and pushed nothing, so Back left the app from any depth. The direction counter lives in ONE place, owned by the shell: a `last` index cached in a closure inside onDeepLinkChange went stale on every forward push and reported the first Back as a Forward (caught by driving real history in a browser, guarded now by __tests__/deep-link.test.ts). (2) PRESS FEEDBACK — RN Pressable has NO default feedback and PressScale sat on 31 of 510 sites; web `.pressable` on 41 of 618. ~93% of tap targets answered a touch with nothing. Mobile: 83 files adopt it with ONE import line each (`PressScale as Pressable`), which required making PressScale a strict drop-in (style-as-function passes through); 4 files keep the raw Pressable deliberately (two full-screen scrims, the swipe row, the lens). Web: 574 buttons across 104 files, and the press now rides the `scale` PROPERTY not `transform`, so it composes with the inline transforms this codebase uses everywhere instead of being overridden by them (verified in a browser). Both: DOWN 120ms / UP 200ms — release is a recovery, not an input. Guarded by __tests__/press-feedback.test.ts so a new screen cannot ship silent buttons. (3) SHEET DRAG-TO-DISMISS + DETENTS — both clients drew iOS\'s 40x4 grab handle and bound NOTHING to it. The panel now tracks the finger 1:1 down, rubber-bands up, and the scrim AND the parent recede interpolate on the SAME drag input (web needed the recede made continuous via a new --sheet-p registered property). Release by velocity projection in core `resolveSheetRelease`, shared so both clients snap identically; a flick moves ONE detent, or dismisses from the smallest. Detents are opt-in (`detents={[\'medium\',\'large\']}`) and single-detent sheets stay CONTENT-SIZED — the first cut gave every sheet a fixed 92vh, which would have left Quick Log and Readiness mostly empty. CORRECTION (Aug 2026, see sheet-elongate): opt-in was the wrong default, and the half of the gesture left unbound was the UPWARD half — every sheet now rests at its measured content height and elongates to full on one drag, with `detents` demoted to an additive stop. Also fixed here: the mobile sheet ran Easing.out(cubic) at the sheet spring\'s DURATION (a different curve wearing the right number) and now runs springToRN(springs.sheet); the web sheet unmounted on setTimeout(160) racing its own transition and now listens for transitionend. (4) SHARED ELEMENTS — SHARED_ELEMENTS gains `sessionHero`: a logged session\'s TITLE travels from its history row into the heading of its own breakdown. The title rather than a figure deliberately — it is literally the same string at both ends, so the travelling element cannot show one value and land on another. The destination DECLINES the pair on a celebration session, whose panel runs its own reveal; the arm then expires and the ordinary push carries the change. (5) LIST MUTATION — zero LayoutAnimation on mobile, zero layout handling on web, so every delete teleported the rows below. Mobile: lib/list-motion.ts wrapping LayoutAnimation on springs.slide. Web: FLIP for inserts/reorders plus collapseAndRemove for deletes — no FLIP needed there because `height` is a layout property, so animating it to zero reflows the rows below every frame and the row leaving and the list healing become ONE animation (verified in a browser). Wired into the set-row deletes on both clients. SMALLER FIXES, all verified: springs.lens joins the token set at response .35/damping .68 (384ms, bouncier) — both clients hard-coded SwiftUI\'s DEFAULT spring at 629ms, 40% past this system\'s own ceiling on the most-tapped control, which escaped the guard because the guard only iterates TOKENS and this was not one; springs.nav renamed springs.press (it was documented as \'THE SHIPPED NAV LENS — global-nav.tsx animates on this\', a file deleted when the bottom bar became the system tab bar); the globals.css token block is now GENERATED by apps/web/scripts/gen-motion-css.mjs instead of pasted; ONE haptics gate per client (lib/haptics.ts) reading prefs.haptics by construction, because gating call sites individually demonstrably failed (4 of 17 fired regardless while the register claimed all were gated), plus Impact Light on switches instead of selection feedback; swipe rows decide on VELOCITY PROJECTION not displacement, rubber-band past the action width, commit a full swipe at 60% and buzz on the destructive commit, with geometry and physics moved to core `swipe` (the two \'twins\' agreed on none of their numbers). CORRECTION to the audit as written: it claimed web\'s three navigator.vibrate sites were all ungated — two were already gated; only the milestone celebration was not. WAVE 2 (Aug 2026) closed the rest of the audit\'s 'worst transitions' list. (A) MODALITY — core `MODAL_SCREENS`/`isDetour` names the screens that are a self-contained TASK rather than a place (Settings, the logger\'s settings, profile-edit, Builder, check-in, the interval timer), listing BOTH clients\' ids because they name some of them differently (web `timer`, mobile `interval-timer`) and a detour that is only a detour on one client is the drift the shared file exists to prevent. `screenTransition` gains `present`/`dismiss`, tested BEFORE `back` on purpose: presentation is a property of the destination, not of the direction travelled, so Forward through history into Settings presents exactly as tapping it did. Mobile reads the list straight into `presentation: \'modal\'` (a real iOS card modal: the parent recedes and stays there, and the card is dismissed by dragging down from anywhere). (B) THE WEB PUSH, which is the other half of the same fix and closes N4: web\'s push WAS the recede-and-rise, i.e. the sheet\'s motion, so the shared token that exists so the clients can\'t disagree described a push only web performed. Web\'s push is now the horizontal travel with the parent parallaxing under (what mobile\'s Stack does natively and the OS edge-swipe reverses), and recede-and-rise is reserved for modality. The recede keyframe also stopped lying about its own number: it scaled to .94 against motion.recedeScale\'s .92, so a presented SCREEN and a presented PANEL receded by different amounts. (C) THE NAV BUTTON agrees with how the screen arrived — `heroNavAction(mode, presented)` dismisses rather than pops, because a card modal wearing a back chevron points at a parent the dismiss gesture doesn\'t reach; resolved from the ROUTE on both clients against the same list, so arrival and exit cannot be set in two files and drift. A presented card stands in HERO.presentedTop for the status-bar inset it does not get. (D) THE PAYWALL (F5) was the last surface bypassing the shared Sheet — a raw Modal route with its own scrim at the flat 0.6, its own 340ms cubic and its own dead grab handle. It is the shared Sheet now (parent recede, coordinated scrim, drag-to-dismiss, elongation), which also ends the last disagreement between the two clients\' paywalls since web had been on the shared Sheet all along. Sheet gains `onClosed`, fired when the exit has FINISHED, so a route-hosted sheet pops its route after the panel has left rather than tearing it down mid-flight. (E) LISTS beyond the set row: the reorder COMMIT travels (mobile animates it inside useDragReorder — the mechanic every reorderable list already shares, so exercise cards, set ledgers and builder blocks all get it from one place), inserts and the remaining deletes travel, and the Exercises page\'s search and sort no longer replace the list wholesale — survivors move, only arrivals fade. The web FLIP changed shape to make the logger wirable at all: it measured a container\'s DIRECT CHILDREN, which cannot describe a list of exercise cards each holding a list of set rows, so it now measures every `[data-list-row]` under one root and skips a row whose measured ANCESTOR also moved (that row is already being carried). SwipeRow marks itself. (F) SKELETONS: @hybrid/core `skeleton` owns the breath (rate, both opacity bounds, the Reduce Motion still), generated into globals.css and read by the mobile Animated loop — the two clients had both landed on 1.4s by coincidence with nothing connecting them. Web gains the primitive it never had (aurora/skeleton.tsx: Skeleton + a Loading shaped exactly like mobile\'s) and `LoadSwap` on both clients makes skeleton → content a HAND-OVER over the new durations.crossfade (250ms — longer than `dissolve` because a crossfade between two states of one box is where the eye compares them) instead of a swap. (G) NUMBERS ROLL (numeric-text.ts, 13 tests): the direction is the VALUE\'s, not the digit\'s (80 → 79 rolls DOWN); compared as numbers so 99 → 100 goes up; aligned from the RIGHT so the new hundreds column is genuinely new rather than a 1 appearing from nowhere; a clock that parses as no number falls back to a positional digit comparison; punctuation never rolls and a figure that changed SHAPE does not roll at all. RollingNumber on both clients, on the rest clock, the builder\'s One Number and the live scoreboard. (H) THE COVER PAIR (§3): SHARED_ELEMENTS.planCover — a goal tile and the screen it opens are the same recipe at two sizes, so the tile GROWS into the poster. Web uses View Transitions (the one pair whose snapshots cross-fade across the whole flight, since a tile becoming a poster changes aspect as well as size) hung on a new `runStackTransition`, because the Plans stack is a navigation the shell knows nothing about. Mobile\'s FLIP now flies a SURFACE as well as text, on the opposite convention: the text clone renders at the destination\'s size and scales down so type is crisp where the eye lands; the surface clone renders the SOURCE\'s own drawing and scales UP, because a tile\'s layout at poster size would keep its 96pt glyph inside a box twice as big. The tile\'s face is extracted so the tile and its clone are ONE drawing. REMAINING (not blockers, tracked as their own follow-ups): loading-crossfade-adoption, fullscreen-cover, shared-elements-remaining. Re-affirmed CUT: staggered list entrance. WAVE 5 (Aug 2026) closed the last open line of the audit's §10 CARDS table — the one row it marked '✅ genuinely good' and then asked for one more thing from: 'add a scale(1.03) + shadow lift on pickup so the dragged card visibly leaves the plane'. It was the only §10/§11 item still outstanding (§10's Tap is PressScale at 0.97/120/200, Expand/Collapse is the plan-matrix accordion, Reorder/Delete came in waves 1-2), and the reason it read as cosmetic is the reason it was worth doing: a picked-up card was drawn at exactly the size and depth of the cards it was passing OVER, so the only thing distinguishing it was that it happened to be moving — the list looked like it was sliding under a hole rather than like something had been lifted out of it. The lift now belongs to `useDragReorder` itself (scale to 1.03 and a shadow to 0.4, one Animated value driving both so a card cannot be caught half-lifted-but-flat, on springs.press — the token for a surface answering a finger), returned as a `rowStyle(group,index)` the rows apply whole. Returning the STYLE rather than leaving it to callers is the actual fix: the audit cited use-drag-reorder as the good mechanic and did not look at what the call sites drew on top of it, and the four surfaces on it had written FOUR different lifts — the builder's block cards with a shadow, its set ledger and the live logger's set rows with `elevation` only (Android; so on the platform this app ships to, two of the four had no depth cue at all), and the live logger's exercise CARDS with a private copy of the whole hook. Putting the exercise cards on the shared hook is what makes this more than a polish pass: that copy predated the hook and duplicated it line for line (same nearest-centre drop, same pickup/commit haptics), so it had silently missed every upgrade the hook received since — including WAVE 2's animated reorder commit, which means the app's LARGEST reorder was the one still teleporting its neighbours into their new slots while the set rows beside it slid. A put-down now settles home on the same spring INSTEAD of snapping flat, but only when the order did not change: a commit reindexes the rows, so `group#from` names a different card the moment onMove lands and holding the lift through a settle would shrink the wrong one — it drops with the commit there, under the layout animation already moving every row. Reduce Motion keeps the lift and drops only its travel (the setting objects to motion, and a card being dragged around by a finger has no business hiding which one is in the hand). Also swept: the profile tile grid, the one 2D reorder and not on the hook (it is a wrapping grid, not a list), already scaled to 1.06 on the same spring but marked depth with `elevation` alone — it casts a real shadow now. Its scale stays native-driven and its shadow static, since a JS-driven animated value in the same style node as a native-driven one is the one combination RN refuses." },
  { id: "motion-audit-wave4", area: "Core", status: "shipped", title: "Motion audit — wave 4: the confusing-navigation remainder + four delights", detail: "SHIPPED on both clients — the still-open items from audit/09 §20's 'most confusing navigation decisions' list, plus four from the 'delight' list. (1) N5, THE SPATIAL MODEL: web's pill nav now stands only on a NAV ROOT (app-shell `barUp` = core navRootRank ≥ 0) — a pushed detail screen or a presented detour covers it, exactly as any pushed screen hides mobile's native tab bar, so the two clients finally agree a detail screen sits ON TOP of the tab, not inside it. Ways back off a detail screen are the hero's own nav button and browser Back — the same pair a pushed screen has on iOS. The 120px pill-bar clearance collapses with the bar (a reservation for chrome that is not on screen is a dead band). (2) NAV_ROOT_ORDER WAS STALE: it still ranked the retired `more` tab and did not rank `messages`, so Today ⇄ Messages — two roots beside each other in the capsule — animated as a drill-down on web while the native bar swapped them as siblings. It now follows the bar (today – nutrition – messages – profile – train, the detached circle last) and motion.test.ts pins Messages as a sibling and `more` as absent. (3) ERROR HAPTICS (§15): haptic.error existed on both clients and fired NOWHERE. toast() gains a kind ('outcome' | 'error') — an error chip reads in the failure voice (red), holds 3600ms against the outcome chip's 1800, announces assertively, and KNOCKS from inside the ToastHost, one gate no call site can forget (the same reasoning that put the prefs gate inside lib/haptics). Wired: web History's archive/delete failures (previously raw alert()s), the web logger's save/network failures (inline banner + knock), mobile session-actions' archive/delete failures, and HealthKit connect/sync failures. Deliberately NOT wired: the mobile finish fallback — a failed server save stashes the workout locally and shows the amber pending-sync note, and a success knock followed by an error knock would report a contradiction. (4) STREAK ROLLS (delight 17): the StreakMark's count renders through RollingNumber on both clients, so the header and done-sheet counts ROLL when a session extends the run (the suffix is prose and never travels). The profile tile and user-page stat stay static deliberately: they mount with their value and never change while watched — mobile-stat-tile tracks that sweep. (5) THE READINESS FACE MORPHS (delight 18): web normalizes every eye/mouth path to ONE command signature (a straight stroke is a quadratic whose control point sits on the line) and transitions CSS `d` + stroke over durations.crossfade — engines that don't animate `d` keep the attribute's instant swap, which is what this replaced. Mobile re-expresses each expression as one PARAMETER SET (signed mouth curvature: + bows down, − bows up, 0 flat; eye character: arc / level / droop) on one subtree and animates between them — a frown becoming a grin flattens through the straight mouth mid-flight, and the bar ⇄ arc crossfade hides the one thing a clipped circle cannot do (flatten to a line). Reduce Motion substitutes the swap on both. (6) CALENDAR DAY → DAY DETAIL (delight 19): SHARED_ELEMENTS.calendarDay, the app's first SAME-SCREEN pair — tapping a day is a `selected` swap the shell never sees. Web arms the tapped cell and runStackTransition moves the name onto the detail card INSIDE the apply (the cell stays mounted, and a name present twice in one snapshot makes the browser silently skip the whole transition); mobile remounts the detail section per selection (a FLIP claim is once-per-mount) and flies the cell's own face into the section's frame. (7) PR BADGE HANDOFF (delight 7, wave-3 item 4): core `livePrLifts` names the lifts that have set a record SO FAR, so a PR badge appears on the exercise card the moment the record set is banked — new on both clients; the audit's 'only the handoff is missing' undersold it, no per-set/per-lift badge existed in the live loggers at all. On finish the heaviest record's badge (prs[0], the one the celebration headlines) FLIES into the summary's new trophy chip (SHARED_ELEMENTS.prBadge). Web's logger → summary swap also became a cross-dissolve on the same runStackTransition — the biggest moment in the app was a hard cut. Mobile arms after the awaited save, immediately before the phase swap, because the arm's 1.2s TTL cannot be trusted across a network round-trip. (8) F7 REMAINDER, the last hand-rolled springs: `springs.pop` is the celebration token — the bounciest spring in the system and the ONE allowed past the 450ms ceiling, with its own guard (≤700ms, per the audit's grid reserving the 500ms tier for celebration) — replacing the two finish pops' friction/tension pairs that were the same feel tuned twice; the profile grid's drag-lift rides springs.press; and History's SwipeCard on BOTH clients — the last swipe surface deciding on displacement with a hand-tuned settle — now decides on core projectSwipe velocity projection, rubber-bands past the reveal width, settles on springs.slide, and ticks Impact Light when the reveal state changes (geometry stays its own: multi-action 88/104pt tiles, not swipe.action). ALSO: the mobile 'every bottom sheet is THE Sheet' guard had been failing on PROSE — the upgrade paywall's docblock mentioned the raw '<Modal' it replaced and the guard greps source text — reworded so the guard is green again. Verified: core 3160 tests, web 183, mobile 69, both clients typecheck. WAVE 6 (Aug 2026) closed §13 (the audit's WORST score, 4/10) and §14 down to their last lines. M1 and G1-G5 were already shipped and GUARDED — press-feedback.test.ts fails a screen that ships silent buttons, and core `swipe` owns the geometry both rows decide on — so what was left was the PER-CONTROL table, which is the same disease one level down: the volume defect was fixed by a sweep, and the individual controls were each still answering a touch in their own hand-rolled way. (1) THE STEPPER, which §13 singles out because in a training app a number changing IS the content. There were TWO hand-rolled ones (the volume model's and the velocity screen's) agreeing on neither geometry nor behaviour, both swapping the old string for the new one in a single frame and buzzing at nothing — the most obvious place in the app where its own `RollingNumber` was missing. `AStepper` in the kit: the figure ROLLS, `haptic.selection()` per step (never `light` — a stepper moves through DISCRETE VALUES, which is what selection feedback is for) and only when the value actually CHANGES, so holding ＋ at the maximum ticks once and goes quiet instead of buzzing against the clamp. Two named size rungs (`row` / `hero`) rather than a free `size`, because a number prop is how the two copies diverged in the first place. The −/＋ are neutral, not lime: a stepper does not GO anywhere, the same reasoning that keeps an expander's count in ash. (2) THE CHECK MARK. `ACheckMark` fills from the CENTRE over 120ms and draws its tick over the last third — the order is the point, since the box filling and the tick arriving in the same frame reads as two pictures swapped rather than one thing being marked. Reduce Motion cross-dissolves the same two states. Wired at the RTP protocol's gates (the one true box) and `haptic.selection()` added at all three checkbox-role controls (protocol gates, the check-in's share-with-coach, device-import's per-workout rows), which had none. (3) THE MENU grows out of its glyph: 0.92 -> 1 scaled from the ANCHOR'S CORNER via transformOrigin (right/top, flipping to right/bottom when the card flips above), plus Impact MEDIUM on open — a menu is a surface PRESENTING itself, the same event class as picking a card up. It used to drop 6dp and fade, i.e. arrive BESIDE the glyph rather than out of it; the corner matters because a menu scaling from its own centre grows in two directions and detaches from what opened it. This is the RN card only — on iOS 26 the menu is the system's SwiftUI `Menu`, which already zoom-morphs from the anchor and plays its own haptic. (4) PINCH, §14's last missing gesture (\"nowhere, and the charts want it\"). `useChartScrub` gains `onZoom`: two fingers is treated as a DIFFERENT gesture, not a scrub with a spare finger (reading under one while the other moves would flicker the readout across the series as the hand closes), and it re-bases after each crossing so a slow continuous spread walks the ladder rung by rung — the audit's own slider rule (selection per detent, never continuously) applied to a gesture with no thumb. The ladder is core's `TREND_WINDOWS` (4/8/13/26/52 weeks) + `stepTrendWindow`, which names the INVERSION once — pinch out = zoom in = FEWER weeks — because that is the easiest thing in a pinch to get backwards; it clamps rather than wrapping, and snaps an off-ladder window to the nearest rung. Wired on the Trends volume sparks, where the window had been the literal `8` at three call sites: a reasonable default no athlete could change. The average label was `avg8w` and would have LIED the moment the window moved, so it is now `avgNw` with {n} (EN/PL/DE). TWO ROWS CLOSED AS DECISIONS, not builds. TOGGLE: the app deliberately uses the NATIVE `Switch` (iOS renders it as Liquid Glass), so its thumb travel and track crossfade are the OS's own and better than a hand-rolled 80/120ms — and UIKit already plays the switch's haptic, so firing `haptic.light()` beside it would double-buzz. Verified no caller fires one today, so the behaviour is at least consistent; the row is satisfied by the platform. SLIDER: there is no slider control in the app (the one `accessibilityRole=\"adjustable\"` is the sheet's grab handle), so the row has nothing to apply to. Its per-detent rule is honoured anyway by the pinch above, which is the closest thing to a continuous control the app has. STILL OPEN and tracked separately: `contentTransition(.numericText())` reaching every figure — `mobile-stat-tile` already measures that sweep. Verified: core 3168 tests, web 130, mobile 71, both clients typecheck, iOS bundle exports. WAVE 7 (Aug 2026) closed §15 and opened §17. §15 WAS ALREADY MOSTLY DONE and the earlier fix held: H1 (4 of 17 sites ungated) and H2 (selection on a switch) are both gone — every haptic in the app routes through lib/haptics’ one gate, there is no raw `Haptics.` call outside it, volume.tsx’s switch fires `light` and liquid-seg’s segmented control fires `selection`. H3 is MOOT: apps/web/components/aurora/logger.tsx, the file with the three ungated navigator.vibrate calls, went with the retired web client. WHAT WAS LEFT of §15 was the MAP, which names nine feedback types against the gate’s seven. `rigid` and `soft` now exist and are wired where the map puts them. RIGID is the HARD STOP, and it is a genuinely different statement from `light`: light says \"that worked\", rigid says \"that is as far as it goes\", and a control answering both the same way cannot tell you it refused. Wired at the end of the swipe row’s rubber-band travel, latched so it fires once rather than every frame the finger spends out there — and the latch covers the full-swipe zone as well as the limit, because that zone lies further out, so returning from it would otherwise re-enter the limit from outside and report a stop the finger never hit. NOT wired at the stepper’s clamp, though a first cut was: at its limits the −/＋ are DISABLED (what UIStepper does, and what accessibilityState should say), so `onPress` never fires and the call was unreachable. Dead code claiming a behaviour is worse than no code, so it went — the audit’s \"value clamped at max\" is answered there by the button disabling, which is the platform’s own way of saying it refused. SOFT is the set bank — the most-repeated haptic in the app, dozens per session, and the one that most decides what the app feels in the hand; the audit asks for it by name as \"a soft landing, not a click\". §17 HAD NO NUMBERS AT ALL. core `states` now carries the vocabulary (emptyOutMs 160 / emptyInMs 200, editDim 0.6, savedHoldMs 800, the ±4px ×3 shake), plus `shakeOffsets()` so both clients shake identically: it starts and ends at REST (a shake ending off-centre has permanently moved the thing it was only meant to draw attention to) and DECAYS (a constant-amplitude shake reads as a broken loop, not an object recoiling). THE BUILD IS THE COMMIT STATE, §17’s highest-frequency row. `APill` gains `state` (idle | saving | saved | error): the IDLE LABEL IS ALWAYS LAID OUT and only its opacity changes, so the pill’s size is a function of its label and never of its state. That is the audit’s defect verbatim — \"the button never resizes, or the layout shifts under the finger\" — and every save in the app had it, because they all swapped the label text (\"Add meal\" → \"Adding…\", different widths) under a finger still resting on the button. Saving also disables (the gate each copy used to remember separately) and announces `busy` to VoiceOver, which none of them did; `saved` ticks and knocks Success; `error` paints red, shakes on shakeOffsets and knocks Error, with Reduce Motion dropping the TRAVEL but never the report. FIFTEEN CALL SITES migrated, which is three times what a first read of the screens suggested — the guard found the rest. lib/commit-state.test.ts fails any button that picks its label text from an in-flight flag, so the next one written the old way fails in CI rather than shipping. Two sibling primitives got the same width-holding treatment rather than being exempted: the admin kit’s `PillBtn` (which already had a spinner while its callers still swapped the label) and social-kit’s `SButton`. STILL OPEN from §17, tracked as `state-change-remainder`: the OFFLINE BAR needs @react-native-community/netinfo, and adding a native module means a new TestFlight build and the whole bundledNativeModules set moving together — deliberately not smuggled into a JS-only change; empty → populated and the editing mode have their tokens here but no surfaces wired yet. FOLLOW-UP FIX, same branch: giving APill a shake wrapper broke the LAYOUT of eleven callers. The press primitive applies its own scale transform LAST in its style array, so an outer transform cannot be merged into it and the wrapper is unavoidable — and the moment it existed, a caller’s `flex: 1` was landing on the inner node while the wrapper, the actual child of the caller’s row, sized to content and refused to stretch. Neither \"all to the wrapper\" nor \"all to the inner\" is right (11 callers pass flex, ~14 pass padding), so core `splitBoxStyle` splits by MEANING: how-I-sit-in-my-parent (flex, alignSelf, margins, width, position) goes out, what-I-look-like stays in, and the inner stretches to fill whatever the wrapper became. Nothing caught this — identical types, clean bundle, green units — so test/kit.render.test.tsx now asserts flex reaches the OUTERMOST node and padding does not, and both assertions were confirmed to fail against the broken version before being kept. Also fixed: a failed commit on an `outline` pill painted red under the outline’s own ash foreground, so the error state now takes the filled treatment whatever the variant was. ALSO in the sweep: nutrition’s three scan buttons had the same resize in the hand-rolled form the `label=` guard cannot see (a ternary inside a `<Text>`), and one of them sits in the SAME ROW as an Add pill that now holds its width — one button steady beside one that jumps is worse than both jumping. Their in-flight state moved into a fixed-size glyph slot (spinner where the icon was) so the word stops changing; the word itself moved to the ACCESSIBLE label, which carries no layout and can say \"Reading label…\" freely, so the translation stays earned rather than deleted. That took the spinner ratchet 19 → 22, raised with the reason written down — all four additions are the permitted kind (inside a button while it works) and each bought a layout fix. AND ONE PRE-EXISTING BUG: hub-masthead’s title claimed in a comment to shrink toward the margin and did not — it leaned on `alignSelf: \"flex-start\"` because \"transformOrigin has no RN equivalent\", which stopped being true at RN 0.76 (this app is on 0.85, and the feed menu already uses it). A scale still pivots about the centre, so the left edge crept in by half the width it lost. The anchor is now declared. Verified: core 3184 tests, web 130, mobile 75, both clients typecheck, iOS bundle exports." },
  { id: "state-change-remainder", area: "Core", status: "planned", title: "State changes — the offline bar, empty → populated, the editing mode", detail: "The rows of audit/09 §17 that wave 7 gave TOKENS but not surfaces. (1) THE OFFLINE BAR is the one with a real blocker: §17 wants a persistent bar that slides down on springs.sheet and SHIFTS THE CONTENT rather than covering it (an offline state is a condition, not a notification), and knowing you are offline needs @react-native-community/netinfo. That is a native module, so it moves with the whole bundledNativeModules set and needs a new TestFlight build — it must not ride along in a JS-only change (see the Expo one-set rule). Deriving it from fetch failures instead was considered and rejected: nothing would say when to CLEAR it. (2) EMPTY → POPULATED: states.emptyOutMs/emptyInMs exist; no empty state crossfades to its content yet, they all still swap. (3) EDITING IS A MODE: states.editDim/editMs exist; no field lifts and dims its siblings yet. All three are token-ready, so each is a wiring job rather than a design one." },
  { id: "onboarding-motion", area: "Mobile", status: "shipped", title: "Onboarding — the first screen joins the motion system", detail: "The motion audit ran four waves across the app and reached the onboarding wizard in NONE of them, so the first screen a new athlete ever sees was the one surface with no vocabulary of its own. The reason is structural and is the interesting part: the screen never asked for the shell. It painted `backgroundColor: ink` on a bare SafeAreaView, so it held no shared component — and every sweep that worked by upgrading the shared ones (PressScale, LoadSwap, RollingNumber, ACard, the sheet, the pill's commit state) passed straight over it. A screen that opts out of the scaffold opts out of everything the scaffold is later taught. WHAT SHIPPED, all mobile (the web onboarding UI is the admin's question EDITOR and is untouched). (1) THE SHELL: it renders AuroraField and runs `useEntrance` — the ambient wash plus the fade+rise every other screen gets from AuroraScreen. It cannot simply BE an AuroraScreen (the progress rail and the CTA row are pinned outside the scroller), so it draws the same two halves itself, which is what the new guard checks. (2) THE STEP TRAVELS. `setIdx` swapped one question for the next in a single frame — the app's most-repeated transition and the one place the product is asking a user to feel progress. `StepSwap` mounts both steps for the length of the exchange: the arriving one comes in from the DIRECTION the wizard moved (forward from the right, Back from the left) on springs.slide, the outgoing one fades and drifts the other way over durations.fast on the exit curve, frozen as a node, absolutely positioned and pointer-events-none so it can neither take a tap nor size the box it is leaving. Reduce Motion substitutes the cross-dissolve. (3) THE PROGRESS RAIL FILLS. Its segments flipped to lime instantly — the one element on screen whose entire job is to report travel was the one element that did not travel. Each segment now wipes in from its leading edge on the same spring, so the bar and the step it describes move on one curve; Reduce Motion fades it instead. (4) THE OPTION ROW STOPS RESHAPING. This component IS the app's documented standard for a wizard row (nutrition-panels cites it by name), and the standard had the defect: the tick was rendered only while active, so picking an option inserted a 22dp glyph at the head of the row and shoved the label sideways under the finger that had just landed on it. The mark is always laid out now, at the TRAILING edge (where iOS puts a table row's check, and where the nutrition twin already had it), and it is the kit's ACheckMark — the box scales from the centre and the tick draws over the last third of that fill. The border, the wash and the label cross-fade on the mark's own 120ms curve so the row and the mark land together; all three interpolate COLOUR, so the row runs on the JS driver, one value in one style node. A pick knocks: `selection` for a single choice, `light` for a multi toggle (a switch flipping), through the one haptics gate. The nutrition twin took ACheckMark in the same change, so the two wizards no longer tick an option with two different glyphs. (5) THE CTA REPORTS. It hand-rolled the label swap audit §17 names — 'Start this plan' became 'Setting up…' — which on this screen did not resize the pill (it is stretched by its row) but did teleport the word, report nothing when the enrol landed, and, being a label rather than a state, had nowhere to report a failure from. APill has held its own width through a commit since wave 2 and carries the tick, the knock and the shake with it. It runs the pill's `state` now: saving → saved (tick + success knock) → route, and for the first time it reports FAILURE. `submitOnboarding` returning false was previously DISCARDED and the app entered as though the plan had enrolled; the pill shakes and knocks Error instead, announces for VoiceOver, and stays put so the press can be repeated — Skip is on the same screen, so a failed enrol reports itself without trapping anyone. (6) THE LOADING STATE: 'Loading…' in place of the question became a step-shaped skeleton (title, help line, three option rows) handed over through LoadSwap. (7) THE PLAN CARD is <ACard>, so the payoff of the whole wizard stops being the only opaque card on an iOS 26 device — struck from the card-surface ratchet and pinned in its CLEARED list. (8) A step is a PAGE: the scroller returns to the top on every move, which the long goal list had been leaving halfway down the next question. DELIBERATELY NOT CHANGED: the route's own presentation. Onboarding stays a funnel step beside welcome/login (slide_from_right, no back-swipe) rather than joining COVER_SCREENS — it is also the app's FIRST screen on a cold start, entered by `replace`, where there is nothing to cover and a rise-from-the-bottom would be a mode change over an empty stack. THE GUARD, because 'we forgot a screen' is exactly the failure a ratchet exists for: design-tokens.test.ts gains two HARD screen rules — every route must REACH the Aurora shell (following its default imports one hop, since half the routes are a five-line re-export of the component that is the real screen), with `index` (a redirect gate that renders nothing) and `upgrade` (a transparent pane for the shared Sheet) exempt in writing; and any screen that draws its own scaffold must draw the ENTRANCE with it, with the live logger exempt because a cover is presented with its own rise. The first rule found a second offender on the day it was written — the coach-invite landing, another flat ink rectangle in the same funnel — which is now an AuroraScreen." },
  { id: "plan-page-cover-redesign", area: "Core", status: "shipped", title: "Plan page — full-bleed sticky cover redesign", detail: "SHIPPED on both clients (design brief + live prototype: design/individual-plan-redesign-ideas.html). THE COVER: the plan detail opens with a full-bleed cover at y=0 (under the status bar; the safe-area inset becomes cover padding) built from the Explore PlanCover recipe — duotone goal-accent wash over #0c0d0c cover ink, ghost discipline glyph, white chip, bottom-anchored 900 title, en-dash meta — via the NEW shared planCoverView() in core (accent/glyph/chip/duration/metaParts/weekBars; unit-tested), so card and hero render one view model. THE COLLAPSE: the cover pins and compresses 1:1 with scroll into a 56dp bar (chip+meta out first, big/compact titles cross-fade, glyph parallax, scrim out, hairline in). Mobile: overlay + counter-translate off one JS Animated scrollY (the NavScrollProvider idiom; PlanCoverScreen in plan-hero.tsx) with snap-to-pole on mid-range release + a prefs.haptics-gated selection detent. Web: position:sticky with negative top + a --hero-collapse custom property published rAF-throttled per frame (the use-scroll-collapse idiom; zero re-renders) + the same snap. NOT suppressed under Reduce Motion (direct manipulation — the masthead-compression stance); only the snap settle degrades to instant. THE BODY: stats hem directly on the ink; maxes as a hairline LEDGER (underline inputs, unit stated once via splitInputsTitle, live inputEcho working-weight echo, and the matrix morphs %->kg as you type since planProgramView already derives it); the week rail sticky beneath the collapsed bar (lime = selection only) — it shipped as a volume WAVEFORM, each chip carrying a bar whose height was that week's real volume, and the bars were DELETED in Aug 2026 (see plan-detail-bars-retired); the rail is now the kit's DockRail/DockChip in `mode` role; programme day cards with display-face 'Day N' headers + right-meta counts, session bands read 'AM — 71 lifts', SILENT empty matrix cells (placeholder glyph removed), radius-28 web<->mobile parity (mobile was 16). THE DOCK: enroll leaves the flow and docks as a pill above the tab bar once the cover collapses (~45%); already-enrolled plans show a quiet status pill from the start; the classic (non-program) detail rides the same scaffold. REMAINING (niceties, not blockers): (1) 'Use my logged 1RMs' ledger prefill from the history engine; (2) the Explore covers still render their own copy of the recipe — port them onto planCoverView; (3) shared-element zoom Explore cover -> detail cover (the wave-3 item — needs Explore to route to the specific plan, not the Plans list); (4) enrolled-state extras: 2dp progress hairline on the collapsed bar, current week auto-centred + completed weeks dimmed in the rail, 'Week N of M' dock label; (5) sticky exercise column when the % matrix scrolls horizontally; (6) the native collapse feel needs a TestFlight pass to sign off (sandbox has no simulator)." },
  { id: "performance-page-merge", area: "Core", status: "shipped", title: "Performance page — Cockpit × Performance merged (one athlete hub)", detail: "The home-tab Cockpit and the analyze-tab Performance screen computed the same objects (computePerformanceState + computeInjuryRisk) and showed the athlete the same HPI twice, one tap apart — Cockpit's state card even deep-linked 'Performance →' for the depth it lacked. They are now ONE page on BOTH clients, and it lives in the ANALYZE group (nav.ts: home stays Today — 'what do I do?'; analyze leads with Performance — 'how am I doing?'); the cockpit id is gone (the web app-shell resolves BOTH screen ids to the merged screen so ⌘K entries, saved deep links and every setScreen('cockpit') caller keep working; mobile keeps /performance as a Redirect to the renamed /(tabs)/performance tab). THE PAGE (aurora/performance.tsx on both clients): living masthead (retitled 'Performance', sub 'Your state, your risk, your season — one read.', chip rail gains an HPI-band pill so the headline number reads before any scroll, and the ACWR pill now rides on HAVING TRAINING DATA, not on having a season — a planless athlete still sees their workload ratio) → Performance State — the CLASSIC card anatomy kept exactly as the Cockpit shipped it (big HPI + 'HPI – band – limiter' caption + sparkline, STR/END/REC as three big-number columns with recovery signed ±N, and today's readiness ring + check-in nudge below the hairline, explained by readinessWhy() — a truth-based line stack computed ONLY from the real log + wearable baseline (honest 'nothing logged yet' baseline copy on an empty log); the old rx.why prescription narrative, which invented a primary lift + starting load for athletes with no history, no longer renders here — it stays with the session pick on the Today flow) → the REAL 14-day trajectory as its own card right beneath (web: recharts, HPI solid lime 2.5px + Readiness DASHED teal — dash-encoded because Kyoto Hour's muted ramp can't separate two solid hues (ΔE≈8, validated), so line style + the legend carry identity, never hue alone; mobile keeps its native bars+marker rendering of the same two series — form first) → injury risk: the exception-driven summary card (calm = slim all-clear row; To Watch grid) with the analyze depth as a 'Tissue detail' DISCLOSURE — web: anterior/posterior body map + per-tissue calibrated probability table + model version; mobile: the phone-width tissue table; the plain-language driver explanations moved inside → This week → Breakdown tabs → Horizon rails (AI-coach door included; the dead 'Performance →' opener dropped) → Goal + Season duo + setup → Return-to-play panel at the bottom (protocols open on the same page as the risk that prompted them). Card headers moved off the mono-kicker+dot pattern onto the SectionHead golden standard (no-decorative-dot-headers). i18n: nav.cockpit removed, w.home.cockpit.commandCenter/-Sub + w.home.today.cockpitTitle + priv-cockpit-t retitled in EN/PL/DE (key NAMES keep the cockpit prefix — they're ids, not copy), new w.analyze.perf.tissueDetail. Casual persona keeps the same locked Teaser, now fronting both former entry points with one upsell." },
  { id: "plan-schedule-quiet-matrix", area: "Core", status: "shipped", title: "Plan schedule table — the quiet matrix (accordion + exercise sheet)", detail: "SHIPPED on both clients (brief: design/plan-schedule-table-redesign-ideas.html; ideas 01+02+04+05+06+07+08+09). The programme day tables were rebuilt in the subtraction vocabulary: (01) the session/group/column header stack collapses to single quiet rule lines — the session marker keeps its semantic colour, the tinted band washes retire, the group label rides the matrix's pinned corner or the accessory column-label line; (02) the per-column load rainbow is replaced by the monochrome INK RAMP (core loadTier: cells gain weight/opacity as %s climb; the single accent marks only the day's heaviest load); (04) PHANTOM COLUMNS die — core percentMatrixView derives columns per group and drops lifts whose loads nobody shares (the lone bodyweight Good Morning) out of the grid into full-width outlier rows (outlierPrescription), preserving authored order around the grid; (05) the exercise column PINS (web: real <table> with sticky row headers + column scope; mobile: name column outside the horizontal ScrollView with fixed row heights) and the scrolled edge fades softly (gradient, hidden at scroll end) instead of cutting; (06) QUIET NOTATION — the parser's leading × is dropped, cells read reps-first with the set multiplier stepped back to ash ('4+1 ×4', a single set is just '4'), tabular numerals throughout (core exposes reps/sets/nl per step alongside the legacy detail string); (07) the day header carried a PULSE — one hairline bar per real prescription, height = load × volume, top-% touches in accent (core dayPulse; aggregated per-lift past 14 bars) — the day-level echo of the week waveform rail; DELETED Aug 2026 with the waveform itself (see plan-detail-bars-retired), core `dayPulse` and `PulseBar` with it; (08) the week is an ACCORDION — one day open at a time (first day by default, reset on week change), closed rows show the day title + a plain-words summary of what the day IS (core dayLeadWords) + volume + a +/− chevron (the pulse used to sit between the two — see 07); rest days are inert rows; all-prose endurance weeks keep the one-card week layout; the expand/collapse runs on the HOUSE MOTION tokens — the body arrives on the sheet spring (mobile Animated.spring via springToRN, web the 0fr→1fr grid-track transition on --e-sheet) and leaves fast on the accelerating exit curve, with Reduce Motion substituting the standard cross-dissolve (height snaps, opacity fades) and collapsed content clipped, untappable and hidden from AT on both clients; (09) every row presses into the EXERCISE SHEET (the shared aurora Sheet on both clients) — each load as % AND kg with its volume in words (stepWords), the note at reading size, the 1RM it's computed from (new ProgramLiftView.oneRm), and RPE explained as reps in reserve (rpeMeaning). NO DATA REMOVED: every %, sets×reps, RPE, note, AM/PM split and lift count survives on the surface or one tap deep. All derivation lives in core beside planProgramView (unit-tested) so the clients cannot drift." },
  { id: "plan-detail-bars-retired", area: "Core", status: "retired", title: "Individual plan — the volume bars (week waveform + day pulse)", detail: "DELETED (Aug 2026), on the request that the bars on the individual plan screen were unnecessary. Two drawings went, both of them the same idea at two scales. (1) THE WEEK WAVEFORM: the sticky week rail under the collapsed cover drew a column per week whose height was that week's volume (core planCoverView.weekBars — NL for strength-percent, item count otherwise), so a plan's wave and taper read as shape. (2) THE DAY PULSE: each collapsed day row in the accordion carried a strip of hairline bars, one per real prescription, height = load × volume with the day's top-% touches in accent (core dayPulse, aggregating per-lift past 14 bars) — described in its own source as the day-level echo of the waveform. REMOVED: the hand-rolled WeekRail and the Pulse component in apps/mobile/components/percent-program.tsx, core `dayPulse` + `PulseBar` + `PlanWeekBar` + `PlanCoverView.weekBars`, and their unit tests. WHAT REPLACED THE RAIL: nothing was added — with the bars gone the week rail is plainly a MODE rail (one week selected, the panel below changes), so it now rides the kit's shared DockRail/DockChip like History's view switcher and the Plans category rail, which also gets it off the last hand-rolled sticky rail on the surface, onto the 44dp hit floor, and to the COVER_GUTTER column its chips sit above. New i18n key w.train.plans.chooseWeek (EN/PL/DE) for the rail's accessible name.", retiredBecause: "Both bars encoded volume as a height nobody could read a figure off, next to the figures themselves: the week's volume is already stated in words beside the Schedule head ('656 lifts this week'), and the day row already carries its own count on the right. Worse, each was normalised against its own peak, so the same wave meant something different on every plan and compared to nothing across plans — the shape said 'this week is taller than that one', which the week numbers say exactly. Revisit only with a figure that is comparable ACROSS plans (a shared scale, or volume against the athlete's own MEV/MRV) and a place to put it that is not already occupied by the number it duplicates." },
  { id: "plans-goal-cover", area: "Core", status: "shipped", title: "Plans — collapsing goal cover on the category screen ('the goal hero')", detail: "Idea 02 of the Plans-section redesign pitch, both clients: the category screen (goal → plan list) no longer opens as a flat heading over lonely cards — it rides the SAME full-bleed collapsing cover scaffold as the plan detail, so every depth of the Plans stack reads as one physical object at a different stage of compression. A new shared goalCoverView(goal) in core (plan-program.ts, unit-tested) derives the goal-level cover from GOAL_TREE alone: the goal's accent wash + ghost glyph, the discipline CATEGORY as the chip ('Strength'), the plan count as the top-right label ('1 PLAN' / '3 PLANS' — properly pluralised; an empty goal says 'COMING SOON', never '0 PLANS'), the goal name as the bottom-anchored display title, the plans' unique loading tags as the meta line, and NO aggregate hem — across a full category, ranges mush into noise ('6–16 weeks', '3–6/wk'); instead EACH plan card carries its OWN rule-topped hem from planHeroView (weeks / sessions / the discipline's volume metric — '656 lifts in wk 1' vs 'exercises in wk 1' vs 'sessions total') under a mono loading-tag kicker ('% OF 1RM'), so a crowded category differentiates plan-by-plan exactly where the athlete is choosing. The scaffolds were generalised rather than duplicated: mobile PlanCoverScreen (plan-hero.tsx) now wraps an exported CoverScreen that takes any CoverSpec — same Animated scrollY collapse into the 56dp glass bar, snap-to-pole detent, haptic, Reduce Motion stance; web PlanHero wraps a CoverHero the same way (position:sticky + --hero-collapse, same snap). PlanList/List render the goal cover with the plan cards beneath; no dock at goal level (nothing to commit to yet). The old goal-screen headers (mobile icon+AHeading, web backLink+h2) are retired. DIFFERENTIATED from the plan cover via a CoverSpec variant — same material, different object: the plan cover is the POSTER (wash from the top-RIGHT, modest ghost glyph, mono meta under the title, blurb below on the ink) while the goal cover is the EMBLEM (the discipline's mark blown up as the cover art — ~218px vs ~150px, 9% vs 7% white, deeper parallax factor 0.66 vs 0.55 — with the duotone wash MIRRORED to the top-LEFT so the light source itself says which level you're on, and the goal blurb rendered ON the cover face, 2-line clamped, in place of the meta line).", },
  { id: "exercise-picker-rooms-az", area: "Core", status: "shipped", title: "Exercise picker — Rooms browse (the A–Z half is retired, see exercise-picker-az-index)", detail: "SUPERSEDED IN PART by exercise-picker-search (Aug 2026): the sheet now opens on a FOCUSED search field, the rooms are a wrapped chip row under it rather than a 25-tile body-mark grid, and the A–Z index is gone (exercise-picker-az-index). What survives is the idea this entry was really about — browse by muscle group, two taps, never a 200-item scroll. Original entry follows. The 'Pick an exercise' surface was redesigned to close the drift with More → Exercises (five standalone concepts in reference/exercise-picker-design-concepts.html; the user chose #02 'Rooms, then Things' + #04 'The Index' with a switchable view). The picker now opens on a GROUPS view — a 2-col grid of pattern/muscle 'rooms' (initials tile tinted by modality or the sport glyph, name, movement count); tapping a room shows only its movements (crumb back to All) — two taps, never a 200-item scroll. A toggle (reusing the Exercises page's Groups/A–Z pill labels) switches to the A–Z INDEX: every movement under display-face letter heads with a right-edge letter rail for one-thumb jumps (web anchors + scrollIntoView; mobile onLayout-captured offsets + scrollTo). Rows share the Exercises-page anatomy — 40px initials tile tinted by kind, sports keep their emoji glyph, and a mono SHAPE HINT on the right (BW / BW+ / assist / secs / m from exerciseProfile) says what the set grid will ask before adding. The old 8px-dot-before-name list and the mono-uppercase category kickers are retired (per the no-decorative-dot-headers + Explore SectionHead rules). Search cuts across every room as one flat list; an unknown name is always a lime custom add. ONE component per client now serves every call-site: mobile ExercisePickerSheet (components/aurora/exercise-picker.tsx) is used by BOTH the Builder and the live logger (which passes its recent lifts as a 'Your lifts' shortcut slab above the rooms; workout.tsx's bespoke FlatList picker + item-flattening memo deleted); web's rewritten ExercisePicker in workout-blocks.tsx serves builder + logger by construction. New i18n w.train.picker.all (EN/PL/DE). Web + mobile at parity." },
  { id: "exercise-picker-search", area: "Core", status: "shipped", title: "Exercise picker — ranked search (add a lift in under three seconds)", detail: "Adding one movement was the slowest thing in the app, and slow in three independent ways. (1) The sheet opened on a WALL: the field was not focused, so the first move was a tap, and behind it sat a 25-tile muscle grid or — one tap away — an A–Z index that mounted all ~310 rows, each with its own drawn mark, in one non-virtualized ScrollView commit. (2) Every KEYSTROKE re-ran everything: the filter was rebuilt outside a memo, up to 60 rows were re-created as raw elements (no component identity, so nothing could bail out), and a spring LayoutAnimation was queued before each character's setState — typing 'deadlift' queued eight. (3) The ANSWER WAS NOT AT THE TOP: `name.includes(q)` ordered results alphabetically-within-section, i.e. by accident — 'curl' put Barbell Curl SEVENTH (behind Reverse Curl, Reverse Wrist Curl, Wrist Curl, Lying Leg Curl, Nordic Curl), 'press' put Overhead Press TWENTIETH of 29, and where the right lift did lead ('deadlift', 'squat') it was luck of the alphabet. It also returned NOTHING for 'rdl' (only Single-Leg RDL, never the Romanian Deadlift it stands for), 'ohp', 'bp', 't2b', 'military press', 'pullups', 'hamstrings' or any typo; nothing for 'bench db' because word order mattered; only Incline Dumbbell Bench Press for 'dumbbell bench', missing DB Bench Press; and nothing for 'trap bar' against Trap-Bar Deadlift, on a hyphen — and pressing return created a lowercase custom 'deadlift' beside the catalog's Deadlift. NOW: packages/core/src/exercise-search.ts is a pure, weighted ranker (buildExerciseIndex + searchExerciseIndex, 54 tests). Bands: exact > whole-word > word-prefix > in-word > muscle/gear > typo; inside a band, words the query did not ask for cost heavily (that is what puts Deadlift above Snatch-Grip Deadlift), a derived PROMINENCE (compound + pattern + implement + base load, plus a small curated canonical list) decides what a bare pattern word means, and the athlete's own logged count lifts the lifts they actually train. Tokens match in any order ('bench db'), synonyms fold DB/Dumbbell and the muscle slang, ~50 curated nicknames plus the DB's 150-odd rename/library ALIASES are searchable forms (so 'rdl', 'ohp', 't2b', 'military press', 'barbell deadlift' all land), run-together spellings work ('benchpress'), and a one-edit typo still finds the lift ('deadlfit', 'sqaut'). Muscle/gear queries answer with work FOR that muscle, not every lift that uses it. ~0.2ms per query over the whole catalog, so there is no debounce and results move with the finger. UI (apps/mobile/components/aurora/exercise-picker.tsx): field AUTOFOCUSED on open (keyboard up before the sheet lands), one virtualized FlatList, rows are a real memo() component outside render, no layout animation on input, and each row carries the GEAR and muscle group under the name (core: exerciseGearLine) — which is what tells eleven deadlifts apart without reading eleven names. Return adds the top match; the custom-add pill appears only for a name the catalog genuinely lacks (core: exerciseNameTaken), so an alias or a case variant can no longer be created twice. Shared kit gains ASearch/AField onSubmit+returnKey. The claims are now GUARDED: apps/mobile/lib/search-surfaces.test.ts scans the source (the design-tokens.test.ts idiom, comments stripped so a docblock quoting the old `name.includes(q)` cannot trip it) and fails if the field loses its autoFocus, the list goes back to a ScrollView, a LayoutAnimation reappears on the keystroke path, a local substring filter creeps into any of the three surfaces, or the sheet stops clamping. Verified: core tests, mobile tests, mobile + web typecheck, web build, iOS bundle export." },
  { id: "sheet-keyboard-fill", area: "Mobile", status: "shipped", title: "A full-height sheet SHORTENS for the keyboard instead of being shoved off the top", detail: "aurora/sheet.tsx wraps the panel in a KeyboardAvoidingView with behavior=padding, which is right for a SHORT sheet (the whole panel rides up and rests above the keys) and wrong for a `fill` one: the panel is laid out at 92% of the screen and the container is now ~60% of it, so a bottom-aligned child that tall overflows off the TOP — grab handle, title and search field with it. Latent for as long as no full-height sheet raised a keyboard by itself; the redesigned exercise picker focuses its field on open, which makes it the default view. Fixed at the source: a `fill` sheet measures the keyboard (useKeyboardHeight, iOS WILL events / Android DID) and clamps the panel to min(0.92 × screen, screen − keyboard), so it ends exactly at the top of the keys — nothing off-screen, every row reachable, and the child still adds no pad of its own (the sheet-pad rule). Inert for every other sheet: no listeners, no height change." },
  { id: "exercise-picker-az-index", area: "Core", status: "retired", title: "Exercise picker — the A–Z index + letter rail", detail: "The picker's second view: every movement under display-face letter heads, with a right-edge letter rail for one-thumb jumps (onLayout-captured offsets + an instant scrollTo). Shipped with exercise-picker-rooms-az.", retiredBecause: "It was a workaround for a search that could not find things, and it cost more than it returned. It mounted all ~310 rows with their drawn marks in a single non-virtualized commit — the heaviest frame in the whole add-an-exercise flow — and its rail sat at right:-6, overlapping the rows it was meant to index. With exercise-picker-search an athlete reaches any lift in three or four characters, including by nickname, alias, muscle or typo; scrolling an alphabetical wall of 310 names is strictly worse than typing 'dead'. Revisit only if search is ever removed, or if a catalog appears whose names an athlete genuinely cannot spell or approximate — browsing by MUSCLE GROUP (the rooms, still there) already covers 'show me the options', which is the real question an index was answering." },
  { id: "builder-one-number", area: "Core", status: "shipped", title: "Builder — 'One Number' redesign (advanced)", detail: "The routine Builder was redesigned to the 'One Number' concept (#02 of reference/builder-design-concepts.html, five Jony-Ive explorations rendered after a live TestFlight critique): the three stat tiles + balance card are replaced by a single display-weight LIVE readout — the routine's estimated minutes in 52-56px mono — that visibly grows as exercises are added (composing feels like loading a bar), with tonnage + moves as one hairline MetaLine and the strength/conditioning/endurance split as a thin 4px modality-coloured bar labelled only for modalities actually present (zeros vanish). ADVANCED EDITING kept and surfaced: per-set TYPE cycling (working/warmup/cooldown/drop badges — tap the set badge), RPE ⇄ RIR — the column header itself is now the mode switch on BOTH clients (persists via the shared rpeAsRir logger pref, setLoggerPref), and exercise reorder ↑/↓ now always available on mobile (open or collapsed, hidden only for a single exercise; web workout-blocks already had it). DISCIPLINE FIXES from the critique: (1) '+ Add set' and '+ Add exercise' demoted from lime fills to the ghost/dashed add affordance — the screen's one lime fill is Save (web workout-blocks add-set inherits this in the logger too); (2) the per-exercise signal metric row now renders ONLY while the card is COLLAPSED on both clients (expanded, the set table + hero already say those numbers — the triple narration is gone; the mobile collapsed-header text summary was dropped for the richer metric row); (3) ghost placeholders are unmistakably ghosts — web .ghost-ph::placeholder (dimmer + italic, carved out from the AA instructional-placeholder rule) on the set inputs, mobile ash@53% on the editor value fields (instructional placeholders like the routine name keep full AA ash); (4) the mobile screen gets 110px bottom clearance so the free-slots note can't slide under the floating tab bar's FAB. Web (aurora/builder.tsx + workout-blocks.tsx signal mode) + mobile (aurora/builder.tsx) at parity." },
  { id: "plans-stack-one-object", area: "Core", status: "shipped", title: "Plans stack — one cover at three compressions", detail: "The Plans stack (library root → goal → plan detail) is now ONE physical object at three levels of compression, on both clients. Levels 2 and 3 already shared the full-bleed collapsing CoverScreen/CoverHero scaffold; the ROOT opened with a plain masthead on the page ink, a 22dp coloured marker in front of each goal name (which also read as the decorative before-heading marker the house rule bans), bare mono category kickers instead of the Explore SectionHead, and '0 plans' / '1 plans' on goals with nothing authored. It now rides the SAME scaffold via a third CoverSpec variant, `library`: emblem-sized ghost glyph like the goal cover, but the wash comes from the RIGHT like the plan cover AND runs at a softer mix (34%/10% web, 7a/2e/08 mobile) so the container never out-shouts the nineteen goal accents on its shelves. Its accent is the THEME's own primary (palette.lime — chartreuse on Aurora, pine on Kyoto Hour) rather than an invented brand colour, because no discipline owns \'Plans\'; its glyph is `◈`, the same geometric family as the goal marks and not one of them (guarded by a test). Shared core: `libraryCoverView(goalCount, categories, labels)` builds the cover, `goalShelves(query)` returns filterGoalGroups with each category ordered READY FIRST (goals with plans, then coming-soon, GOAL_TREE order held inside each half) so what sits past the fold is what is not built yet, and `goalCoverView()` gained a `ready` flag so a goal with no programs dims to 45% and says COMING SOON instead of printing 0. EACH CATEGORY IS A FULL-BLEED SHELF — a SectionHead (display-face title, mono count on the right) over a horizontal rail of goal COVERS (Explore\'s PlanCover recipe at 172×140 tile scale, so tapping one expands into the same poster at screen scale), bleeding under the screen edge per the house rule (mobile marginHorizontal -16 + matching padding; web negative --page-pad-x margins). A category can hold ten or twenty goals without the screen growing down. Making a horizontal rail honest about its tail takes four things, all shipped: the head states the COUNT, the goals are sorted ready-first, a hairline TRACK under each rail shows position (native-driven Animated on mobile, direct node writes on web — no re-render per scroll frame), and the CATEGORY CHIPS dock beneath the collapsed bar and JUMP to a shelf. Those chips now navigate instead of filtering: with every category rendered as its own shelf, narrowing to one would just empty the screen, so the category lever is gone and free-text search remains (still the shared filterGoalGroups). The chips ride the scaffold\'s previously-unused `rail` slot on mobile (CoverScreen gained an optional `scrollApi` handle so a rail can scroll the content beneath it) and a `position: sticky; top: 56` nav on web. The web CoverHero\'s back button is now optional — the Plans root is a top-level screen with nowhere to go back to." },
  { id: "plans-root-enrolled-cover", area: "Core", status: "planned", title: "Plans root — the enrolled season as its own cover", detail: "The redesigned Plans root still shows the current season through the old generic EnrolledCard: a small ink2 card with a mono kicker, which looks nothing like the plan it stands for now that everything around it is a cover. Render it instead with the PLAN cover recipe (the plan\'s own accent, glyph and display face) carrying the week you are on, and let the library sit below it as a second section — so the screen opens on what you are doing before what you could do. Needs a week-of-season number the browse screen does not fetch today (the macrocycle response has planStartedAt; the week has to be derived or added server-side)." },
  { id: "no-decorative-dot-headers", area: "Core", status: "shipped", title: "Section headers — decorative marker dots removed (Explore SectionHead standard)", detail: "Per the new CLAUDE.md rule (a small dot/circle/square before a section label reads as AI slop), every decorative before-text marker was removed and the headers converted to the Explore tab's SectionHead vocabulary — a bold display-face title in chalk (web --font-heading 800/18, mobile serifIf F.black 18) with any meta/action as small mono uppercase on the RIGHT of the same row. Swept: Today's first-run chooser head (lime dot → title + right-side 'Free'), Today's RECOVER & MORE head (ash dot + mono kicker → title), Today's GO FULL head (mono kicker → title; the ✦ premium signifier STAYS — it is semantic, not decorative) — all web (aurora/today.tsx) + mobile (aurora/home.tsx) at parity — and the web More springboard cluster headers (pill-nav.tsx: the per-cluster accent square marker removed, label-only like the mobile More tab; the now-unused GROUP_ACCENT map deleted). Semantic dots are untouched by design: status/live dots, mood dots, chart legend swatches, calendar event markers, notification badges. Remaining screens with kicker-style headers (Cockpit, Sport, admin) fall under the design-system-unification-sweep item." },
  { id: "sheet-elongate", area: "Core", status: "shipped", title: "A sheet elongates on one drag up, and shortens on the way back", detail: "THE REPORT: 'the sheet should have an ability to be elongated on one drag from the bottom to the top, and shortened on the way back.' THE CAUSE: drag-to-dismiss shipped with detents OPT-IN (motion-audit-followups), and almost nothing opted in — so the default sheet had exactly ONE stop. Dragging DOWN worked; dragging UP rubber-banded and fell back. The app drew iOS's grab handle and answered half of the gesture it promises, which reads as broken rather than as 'no'. The reason the default was one stop is that a content-sized panel has nowhere to grow INTO: it is exactly as tall as what it holds. THE FIX, on both clients: the panel is ALWAYS laid out at the `large` height and translated down to its resting stop, so the room above the resting line already exists and the drag just uncovers it. Where it rests is MEASURED — mobile from the sheet ScrollView's content size plus the measured header and the panel's own pads, web from an inner wrapper inside the flexing body via ResizeObserver — so a three-row sheet still looks exactly three rows tall, which is what the first cut of detents got wrong (it gave every sheet a fixed 92vh). The stops come from core `sheetSnaps(maxH, detents, contentH)`: 0 (full) is always one of them, and `detents` is now ADDITIVE — a declared `medium` adds a stop, it never inflates a two-button confirm sheet to half the screen. The scrim and the parent's recede re-range onto the RESTING stop rather than fully-open, or every content-sized sheet would open on a fraction of its scrim. Content that changes under an open sheet (a list loads, a filter narrows) RE-FITS, unless the sheet has been dragged or is being held. Also fixed here, both clients: the mobile PanResponder claimed downward drags only (`g.dy > 6`), so no stop arithmetic could have made the elongation reachable; and a release used whatever velocity was last sampled however long ago, so drag-up-hold-look-release fired on a stale flick and took the sheet away — core `releaseVelocity(v, ageMs)` forgets a sample older than `sheetGesture.stale` (100ms), which matters far more now that holding a sheet part-way is an ordinary thing to do. MOBILE-ONLY BOUNDARY, and it is a real one: only a sheet that owns its scroller can be measured. `scroll={false}` (and `fill`) hands the panel's column to the child, which is typically a ScrollView — RN gives every ScrollView flexGrow 1, so such a child takes the whole panel by construction and there is no shorter height to find. Those sheets are full-height. The short sheets that had passed `scroll={false}` merely to avoid a scroller (confirm, the workout More menu, the RPE cheatsheet, quick-sport's form, four nutrition pickers, the share-story sheet) now use the sheet's own scroller and rest at their content height; the ones that genuinely own a scroller (exercise-anatomy, session-edit, device-match, device-import, the exercise pickers) stay full-height, which suits them. Web has no such split — its Sheet always owns the scroller. VERIFIED in a headless Chromium at 390x844 by driving the real component: opens at 287px of a 776px panel with the scrim at full strength; slow drag up → 776; slow drag down → back to 287 (shortened, not dismissed); flick up → full; flick down from full → the content stop (one stop per flick); flick down from there → dismissed with --sheet-p back to 0; reopen re-measures. Content grown to 8 rows re-fits to 567 and back to 287; a 30-row sheet opens full, scrolls, and rubber-bands on a pull-up; a 100px drag HELD for 250ms releases back to rest instead of dismissing. Guarded by apps/web/__tests__/parity.test.ts ('the sheet's elongation'): both clients must read core's sheetSnaps, lay the panel out at the full height rather than sizing it to content, claim the drag in both directions, and forget a held gesture's speed. Core tests green, both clients typecheck, the iOS bundle exports." },
  { id: "sheet-bottom-pad", area: "Core", status: "shipped", title: "One pad under a sheet's last row (the dead band under every sheet)", detail: "THE REPORT: 'basically every sheet opened has weird margin/padding at the bottom, which creates a huge gap.' THE CAUSE: the pad under a sheet's last row was never OWNED, so it was written at four call sites and stacked. (1) The web More sheet (aurora/pill-nav.tsx) reserved `padding: 20px 20px 110px` — clearance for the floating pill bar, which is z-50 UNDERNEATH that z-60 panel and completely hidden behind it while the sheet is up; the reservation bought nothing and left ~86px of dead panel below Sign out (measured in a headless Chromium at 390×844: panel 675px tall, content ending 110px short of its edge). (2) The two shared Sheet components disagreed: mobile `paddingBottom: insets.bottom + 20` (54dp on any notched iPhone) against web's flat 24 — the home-indicator inset ADDED to the pad instead of being its floor. (3) Children then trailed pads of their own on top: the mobile Sheet's own ScrollView (+4), the exercise-anatomy sheet (+30), the sport picker (+28). (4) The mobile upgrade sheet carried a fourth number, `insets.bottom + 22`. THE FIX: one shared token — `sheetPadBottom(insetBottom = 0)` in packages/core/src/scale.ts, `Math.max(insetBottom, space.xxl)`, MAX not PLUS — read by every sheet surface on both clients: web aurora/sheet.tsx, pill-nav.tsx (110 → 24), exercise-anatomy.tsx, workout-wrapped.tsx (all via CSS `max(24px, env(safe-area-inset-bottom, 0px))`, so an installed PWA gets the home indicator honoured rather than the dead `calc(24px + env(…))` that resolved to 24 anyway since the app sets no viewport-fit=cover); mobile aurora/sheet.tsx + upgrade.tsx. The stacked child pads were removed — first the large ones (mobile Sheet scroller +4, exercise-anatomy +30, quick-sport +28), then the small ones in a second sweep so the rule holds with no exceptions: the six web nutrition sheet bodies (partSheet, portion, quickLog, meal picker, unit picker, goal picker — 6–8px wrappers mobile never had, so stripping them is also a parity fix), its mobile goal-picker twin, the discard/confirm button stacks on both clients (web logger.tsx, mobile confirm.tsx), and quick-start's trailing block margin, which had drifted to different numbers per client (web `: 4` / empty state `16px 2px 8px` / rail `2px 0 8px`; mobile `: 2` / `paddingVertical: 10`) and is now 0 on both. One pad was deliberately KEPT: the web add-product picker's inner `maxHeight: 50vh` scroller (nutrition.tsx), whose 6px is intra-scroller rhythm with a button below it — it never touches the panel edge — the rule is now in CLAUDE.md: the sheet owns the pad, content inside it does not add one. Verified in a headless browser before/after (More sheet 110px → 24px, 86px of content recovered; the shared Sheet still hugs its content at 24px). Core tests 2501 green, web + mobile typecheck clean. CORRECTION (Aug 2026, on-device report): the dead band CAME BACK on mobile, and the one-pad rule was not what failed — the number handed to it was. A sheet is presented in a Modal that covers the bottom bar, so it must not pad for it; but `useSafeAreaInsets()` resolves through React context, not the window the Modal renders in, and a screen inside the iOS 26 native tab bar reports a bottom inset with the BAR FOLDED IN (bar + its bottom accessory + home indicator). `sheetPadBottom` MAXes against that, so every sheet reserved the whole tab bar underneath itself: a dead black band roughly five rows tall that ate the end of the reading area on every sheet in the app (reported against main@e2e4f435, the TestFlight build of 9 Aug, on the Strength-fresh explainer). The rule was written in CLAUDE.md and could not be obeyed, because the input already had the bar in it. THE FIX: apps/mobile/lib/layout.ts `sheetInsetBottom(screenInset)` returns the WINDOW's bottom inset — `initialWindowMetrics`, captured from the root window before any navigator exists, so it is the home indicator and nothing else — as `Math.min(screenInset, windowInset)`, which is a no-op wherever the ambient inset was already the window's. Both mobile sheet surfaces (aurora/sheet.tsx, aurora/upgrade.tsx) now pass their inset through it. `auroraScrollClearance` had the same latent double-count (bar height ADDED to an inset that may already contain the bar — 174pt under every screen's last row instead of 110) and is now `max(bar + windowInset, screenInset) + 12`, correct under either reading and never under-padding. Web is unaffected and was re-measured to confirm it: at 393×844 with long content the panel reaches the screen bottom and the body ends exactly 24px above it, both before and after. Guarded by apps/web/__tests__/parity.test.ts (\"a sheet's bottom pad\"), which scans every mobile file and fails any `sheetPadBottom(...)` whose argument did not go through `sheetInsetBottom` — mutation-tested by re-introducing the raw inset. NEEDS A DEVICE to confirm the band is gone: the inflated-inset diagnosis is reasoned from the report (every sheet, content still scrolls, band far larger than the 24-34pt pad) and cannot be reproduced in this sandbox." },
  { id: "today-first-run-chooser", area: "Core", status: "shipped", title: "Today first-run chooser — 'Three Materials' redesign", detail: "The post-login 'How do you want to start?' state (new user, no plan, no logged history) was redesigned in the Jony-Ive material vocabulary. Five standalone concepts were rendered in reference/start-chooser-design-concepts.html (One Door, Single Slab, Three Materials, Just the Question, One Control) and 'Three Materials' (#03) was chosen: the question becomes an Explore-standard SECTION HEAD (bold display title with 'Free' said ONCE as the mono right-side meta — the three per-row Free badges are gone, per the one-accent discipline on parallel items; originally a dot+kicker, de-dotted same day per the no-decorative-dot-headers rule), and the three free paths wear the Go-Full AccessCard anatomy — corner accent glow, title, growing body, CTA pinned to the bottom in mono uppercase — so the athlete's FIRST decision feels as considered as the paid one. Spectrum-coded: Follow a plan=lime ▤, Build your own=teal ⌗, Just train=sand ↯, with the hue confined to the small glyph + CTA (title/body stay neutral). The floating top-right 'Start →' pill no longer renders in the chooser state on either client (it competed with the choice — each card IS the start; it still serves the athlete AI-prescription state). Copy tightened into card bodies + new chooser*Cta i18n keys (EN/PL/DE); the howStartSub paragraph was retired. LAYOUT FIX (same day, after a live phone screenshot): the chooser renders DIRECTLY on the page — the wrapping today-card (web `card` div / mobile ACard) is skipped in the first-run state, since a box around three cards read as chrome — and the cards are ONE stacked full-width column at natural height on both clients (the web auto-fit grid orphaned the third card 2+1 at phone widths); card surfaces moved to the page-level Go-Full spec (ink2 base, radius 24, padding 20). Web (aurora/today.tsx ChooserCard) + mobile (aurora/home.tsx ChooserCard, LinearGradient corner glow) at parity." },
  { id: "today-living-masthead", area: "Core", status: "shipped", title: "Today masthead — one 'Today' headline that names the viewed day", detail: "The Today/Home opening zone was redesigned (concept 02 of the three-round greeting-zone exploration in reference/greeting-chooser-design-concepts.html + greeting-zone-design-concepts-2/-3.html): the old layout stacked TWO near-equal bold headlines — the greeting (22) and 'How do you want to start?' (18) — four lines apart with a floating 'FREE', so neither won. Now the zone is a MASTHEAD in the App-Store-Today vocabulary: a mono uppercase caption line (viewed-day date left; right slot = 'Free' in the first-run chooser state, or the scrub-distance tag), ONE big display headline (34), and the greeting demoted to a single warm sentence beneath ('Good afternoon, Rafal.'). The first-run chooser's section head is deleted — its three cards need no sentence announcing a choice — and the howStart i18n key is retired (badgeFree moved to the caption line). THE HEADLINE LIVES: it names the VIEWED day, not the screen — scrubbing the plan week rail flips it 'Today' → 'Yesterday'/'Tomorrow' at ±1 → the weekday NAME beyond ±1 (never '2 days ago': clumsy as a headline and badly inflected in PL/DE; the distance rides the caption as a mono tag, daysBack/daysOut). The naming rule is shared core logic (masthead.ts, unit-tested) so the clients cannot drift. Off today the greeting line becomes the teal 'Back to today →' return affordance — same spot every time — which re-anchors both the lifted day scope AND the rail's own selection (new resetToken prop on both week rails). New i18n keys mastToday/mastYesterday/mastTomorrow/backToToday/daysBack/daysOut (EN/PL/DE). Web (aurora/today.tsx) + mobile (aurora/home.tsx) at parity. TRIMMED (Aug 2026, user report): (1) the caption line's 'Free' badge read as a random word floating top-right of the dashboard, so the right slot now carries ONLY the scrub-distance tag (daysBack/daysOut) — badgeFree retired from home.ts (EN/PL/DE); (2) the 'Back to today →' accessory is gone too (the rail's today chip is the way back — tapping it re-anchors the day scope through the same onSelectDay path), which retired the backToToday key, the masthead's accessory slot (no other caller) and the week/logbook rails' resetToken prop, on both clients." },
  { id: "profile-private-tab", area: "Core", status: "shipped", title: "Profile — the Private tab, REMOVED", detail: "RETIRED (Aug 2026). The profile 'You' screen's 4th owner-only tab (Overview / PRs / Activity / 🔒 Private) is DELETED on both clients, along with apps/{web,mobile}/components/aurora/private-tab.tsx and the six i18n keys that only it used (w.account.profile.tab-private, priv-intro, priv-cockpit-t/-s, priv-privacy-t/-s, EN/PL/DE). WHY: it held three rows and two of them were doorways the app already has — the Command-center hero was a link to Performance (which is a nav tab, and the profile's level badge already opens it) and 'Privacy & visibility' was a link to Settings (which the cover banner's edit chip already opens). Stripped of those, the tab carried exactly one thing of its own, Body & progress, and a tab that exists to hold one block is a tab that does nothing. WHERE ITS CONTENT WENT: Body & progress moved to Nutrition → Body (see profile-private-body) — the weigh-in is the input the intake targets are steered by, so the measurement and the thing it feeds now share a screen. The Full upsell the hero carried (FUNNEL.upgradeEntryClick source 'private-cockpit') is not re-created: Today's Go-Full Cockpit card is the same bait at the same funnel stage, on the screen the athlete actually opens daily. Highlight curation was already on the Overview grid (profile-private-hidden) and is untouched. Web + mobile removed in the SAME change." },
  { id: "profile-private-body", area: "Core", status: "shipped", title: "Body & progress — measurements, now in Nutrition", detail: "Dated weight + height + tape measurements (BodyMetric table, owner-only RLS — reference/sql-private-tab.sql) logged through /api/body: the standing-height row (its own field, its own save, because height is a fact you state once and not a thing that trends), the full measurement grid (weight, body fat, waist, chest, arm, thigh, hips, neck, calf — all optional, weight in the athlete's own kg/lb), the weekly BODY REPORT (narrative verdict + ~7-day weight delta + a 7-segment logging-cadence meter), the per-metric trends grid with dependency-free sparklines, and the door through to the progress-photo timeline (its private 'progress' Storage bucket created via reference/sql-progress-photos.sql). All trend maths is in @hybrid/core body-progress.ts, so both clients render the same numbers. FREE for everyone — nothing here is gated. REHOMED (Aug 2026): it used to be a collapsed row inside Profile → Private that opened a slide-up sheet; when that tab was retired (see profile-private-tab) the block moved to NUTRITION → Body on both clients, as the shared aurora/body-progress.tsx panel, and it renders INLINE now — the row-plus-sheet was an idiom for a list of tools and this is no longer in one. It belongs on Nutrition because a weigh-in IS what the intake targets are steered by: /api/body mirrors the weight into the bodyMass Signal that drives maintenance, the trend and every kcal target, so the number and the thing it feeds share a screen instead of sitting two tabs apart. The old kg-only 'Current weight' card that used to head that view is GONE rather than left beside the panel asking for the same number in a second place (with its now-orphaned strings weightProfileSub / noWeightYet / updateWeight); Nutrition's EWMA bodyweight chart rides in the panel's `trend` slot, directly under the report that names the same weight. A save fires the shared refreshBodyweight() AND the host's revalidation, so tonnage, maintenance and the day's targets all move without a reload. Progress photos are reachable ONLY through this panel's door row — that is the reason the block had to be rehomed rather than deleted with the tab. Web + mobile at parity." },
  { id: "profile-private-journal", area: "Core", status: "shipped", title: "Profile Private — private journal", detail: "The Private tab's 'Journal' block is a working private notes editor (JournalEntry table, owner-only RLS) via /api/journal — compose, list newest-first, delete; never exposed on any follower-facing surface. RETIRED as a standalone block: private training reflection moved ONTO the workout itself (the finish screen + history's session note), so there is no separate Journal surface to open — and the Private tab that used to host it is gone entirely (see profile-private-tab). The JournalEntry table and /api/journal still stand behind the session note." },
  { id: "profile-private-hidden", area: "Core", status: "shipped", title: "Profile Overview — highlight curation (Apple-style edit mode)", detail: "Highlight curation lives on the Overview grid as an Apple-home-screen-style EDIT MODE, replacing the old press-and-hold-then-Hide overlay (which hid a tile on the very next tap, with no undo and no reordering). Now: a plain TAP never hides anything; PRESS & HOLD (~450ms) any tile enters edit mode — every tile WIGGLES and grows a corner '–' button. In edit mode you DRAG tiles to REORDER them (the arrangement persists) and tap '–' to hide a tile, which drops it into a 'Hidden — tap to restore' TRAY below the grid (a chip with a + to bring it back). Done / Esc / tap-away leaves edit mode. Each tile carries a stable key — 'pr:<lift>', 'streak', 'sessions', 'tonnage', 'badges'. Reorder is pointer-driven with no DOM reshuffle mid-drag (snapshot each slot at grab, translate neighbours toward the vacated slot, commit once on drop); mobile uses the RN built-ins (PanResponder + Animated) with NO gesture-handler/reanimated dependency, so parity holds without new native deps. Persistence: hidden keys → HiddenHighlight; the arrangement → the new HighlightOrder table (one row per user, ordered key list; owner-only RLS), both via /api/highlights (POST { key, hidden } or { order }). The grid reconciles the saved order against the tiles that currently have data (known keys keep order, newly-earned keys append). Web reorder algorithm verified in headless Chromium; web typecheck + mobile iOS bundle export green; i18n ov-done / ov-restore / ov-edit-hint (EN/PL/DE). Web + mobile at parity. FOLLOW-UPS: (1) the follower-facing renderer should honour the hidden set server-side (service-role) — today the filter runs on the owner's own screen; (2) reorder PERSISTENCE needs the HighlightOrder table — run reference/sql-private-tab.sql section 4 in the Supabase SQL Editor (the API degrades gracefully to a non-persisted, in-session arrangement until then)." },
  { id: "premium-accent-sand", area: "Core", status: "shipped", title: "Premium upsell accent → sand (was violet)", detail: "The colour that ENCOURAGES BUYING FULL was migrated from violet to SAND/amber (#d0cd94 — the brand's `--amber-text` / `C.amber` / accentText.amber) across BOTH clients at parity, so the upgrade cue no longer collides with the lime everyday-action accent and reads as a distinct premium tier. Surfaces recoloured: the Today 'GO FULL' Cockpit/Sport bait cards + section ✦ (web today.tsx / mobile home.tsx); the upgrade paywall sheet — ✦ Full badge, benefit bullets and the primary CTA button, whose ink is now a theme-fixed near-black (#141614) because sand is a LIGHT fill in BOTH themes, unlike the theme-flipping `ink`/`onAccent` (web + mobile upgrade.tsx); the Train AI-coach PremiumHero + locked-routine 'Full' badge; the Nutrition AI-scan bait + locked meal-preset tiles + ✦ Full badges + lock icon + premium-tier chip; the free-user 'AI session' upsell pill (web logger.tsx / mobile app/workout.tsx — mobile brought to web parity: sand only for FREE users, lime for already-unlocked athletes); and the More springboard premium lock badge (web pill-nav.tsx / mobile more.tsx). SCOPE: only buy-premium usages changed — the coach/role + data-series violet usages stayed on the violet token (then recoloured wholesale, see coach-accent-steel). Both typechecks green." },
  { id: "premium-accent-admin", area: "Core", status: "shipped", title: "Admin-configurable premium-CTA colour", detail: "The accent every 'buy Full' CTA wears is now ADMIN-SETTABLE at runtime (no redeploy), not hardcoded sand. Source of truth: packages/core/src/premium-accent.ts — a `theme.premiumAccent` feature flag whose value is a brand-palette key (amber/violet/lime/blue/red) or a custom #hex; `resolvePremiumAccent(value, theme)` returns { fill, text, ink } (ink auto-picked near-black/white for contrast on the fill), with `normalizePremiumAccent` (validates + expands #abc→#aabbcc) and `wcagRating` for the checker. Reuses the existing feature-flag override storage + /api/flags read (no new migration). WEB: globals.css defines `--premium-accent` / `--premium-accent-text` / `--premium-accent-ink` (default → amber); `<PremiumAccentStyle>` (mounted in app-shell) restamps them on <html> from the flag — presets point --premium-accent-text at the theme-aware `--<key>-text` so it still flips light/dark, a custom hex is used verbatim. Every premium surface (Today Go Full cards + ✦ + [note], upgrade sheet badge/benefits/CTA, Train PremiumHero + routine badge, Nutrition scan/locked-tiles/Full-badges/lock/tier-chip, the free-user AI pill, the More upgrade card + springboard lock, the ⌘K hub lock, builder routine-unlock, profile HPI-locked CTA) reads those vars instead of --amber-* — so one setting recolours them all live. MOBILE: the flags store now also holds config VALUES (fetchFlagState) + a `useFlagValue` hook; `usePremiumAccent()` (lib/premium-accent.ts) resolves { fill, text, ink } for the theme, and the same premium surfaces read it instead of C.amber. ADMIN: the Flags panel renders a colour picker for this flag — palette swatches + a custom-hex input + a LIVE WCAG readout (ratio + A/AA/AAA) for the two pairings that matter (accent-as-text on the card, and the auto-ink on a solid button), so a hex that fails contrast is flagged before it ships. Non-premium sand/amber (Plan section, caution states, calendar events, macro chart colours, the free-trial note) is untouched. LATE ADD: the Profile → Private tab's locked-feature '✦ Full' pill (LockedRow on Body & progress + Journal) was the one buy-Full CTA still hardcoded lime — it now reads the premium accent too (web: var(--premium-accent-text) + a premium-tinted border; mobile: usePremiumAccent().text/fill), so the Private-tab upsell matches the Today Go-Full cards. (SUPERSEDED Jul 2026 by the Jony-Ive unification — Body & progress and Journal are now FREE, so those two LockedRows/'✦ Full' pills were removed entirely; the premium accent then dressed the Private tab's Command-center HERO card instead — and as of Aug 2026 that tab is removed too, so the accent's remaining premium surfaces are the ones listed above, led by Today's Go-Full cards; see profile-private-tab.) 9 new core tests; both typechecks + 716 core tests green. NOTE: the flag override persists only once reference/sql-feature-flags.sql is applied in Supabase; until then it runs on the amber default." },
  { id: "coach-accent-steel", area: "Core", status: "shipped", title: "Non-premium violet → steel/slate blue", detail: "After the premium cue moved to sand (premium-accent-sand), the `violet` token was used ONLY by non-premium surfaces, so its VALUE was repurposed in one place — from lavender #c9a9f0 to a steel/slate BLUE — recolouring every remaining usage at once on both clients while keeping all multi-series chart hues distinct. New values (mirrored across tokens.ts colors.violet, palette.ts THEMES.*.accentText.violet, and web globals.css --color-violet/--violet-text for both themes): fill #8296c4; accent-TEXT #8ba0cc on dark / #4c5a78 on light (both clear WCAG AA on the card — guarded by palette.test.ts). This flips, in a single token edit, the coach identity (analytics roster/coach stats, admin COACH role + coach applications, financials coach segment, talent, team compare/monitor, coach-rail, calendar coach-assignment dots), the data-series 5th hue (fat macro, the 'mobility/other' exercise kind, the 5-zone velocity scale, HPI recovery, org ANALYST), the social nav section accent, the Cockpit season/phase progress, and the profile avatar/cover gradients. Hardcoded lavender literals were caught too: periodization Taper/Peak phase colours and the plans.ts VIOLET discipline colour (power/strongman/bodybuilding/tactical/sport). Steel is distinct from the teal `blue` token, so segment charts that show coach next to org (blue) don't collide. Also fixed: semantic.ts ROLE_COLOR.premium 'violet'→'amber' (+test) so the vocabulary reads premium=sand; and a premium lock badge in the web ⌘K hub (app-shell.tsx) that was still on violet was moved to sand. NAMING CAVEAT: the token/AccentKey is still keyed `violet` (renaming it touches ~40 direct call-sites + the type union) — the key is legacy, the value is steel; comments at each source flag this. Both typechecks + core tests (incl. the AA contrast guard) green." },

  // ---- Web ----
  { id: "web-retirement", area: "Web", status: "shipped", title: "Web client retired — HYBRID is a mobile product", detail: "The user-facing web app was officially retired and deleted (Aug 2026): the /app shell, every user screen (Today, Train, Plans, Nutrition, Feed, Statistics, Notifications, Timer, the public Program Efficacy Index page), the aurora web kit and the client-side hooks behind them are gone. What apps/web still ships, deliberately: the ENTIRE /api backend (both the mobile app and the admin panel call it — unchanged), the /admin operator panel (see admin-panel), an admin-only /login (email/password + OAuth + MFA step-up; no sign-up — accounts are created in the mobile app), /auth/callback, the public /privacy + /terms legal pages (App Review requires they resolve), a minimal root landing that points people at the iPhone app, and /invite/[token] as a browser landing that verifies a coach invite and directs the visitor to the app (email match auto-links via /api/me; the app's Coach screen can claim directly). HISTORICAL NOTE: area=Web entries below that describe user-facing surfaces (logger, plans, social feed, coach console, …) are history — those surfaces shipped, then were retired with the web client; their mobile twins are the live product. Web-only niceties that died with the client are tracked in web-only-features-to-port." },
  { id: "web-deploy", area: "Web", status: "shipped", title: "Web on Vercel — API + admin panel", detail: "Next.js App Router, auto-deploys from main. Hosts the /api backend both clients call, the /admin operator panel, admin login, legal pages and the invite landing. The user-facing web app is retired (see web-retirement)." },
  { id: "ui-template-aurora-web", area: "Web", status: "shipped", title: "Aurora template — bespoke per-screen layouts (web + mobile)", detail: "HISTORICAL — read this as the record of a migration that finished. \"When the template is active\" no longer means anything: the classic skin was deleted, and in Aug 2026 the registry that still nominally chose between them went too (see `ui-template-switch`), because a union of one value was gating branches that could not render. Aurora is simply the look. The account of what was rebuilt, below, stands. — Aurora ships a DEDICATED, hand-crafted layout for the large majority of screens on BOTH clients (not just the global skin) — each route renders its Aurora variant when the template is active and the classic screen otherwise, all on the SAME data/engine/API logic so there's no feature loss. Coverage: AUTH/ONBOARDING (Welcome, Login/Register, Onboarding); HOME/ACCOUNT (Home/Today — now at FULL cockpit parity with classic: a horizontally-swipeable Plan-today + AI-coach pager with page dots, the season phase timeline, the reconciled This-week plan with schedule/re-sync + intensity/volume/load×/volume× + the exercise list, accountability, the weekly recap (tap → Statistics), and the Athlete-Twin + injury-risk-by-tissue panel — web reuses the shared ReconciledWeek, mobile mirrors it inline; Settings, Notifications); RECOVERY (Check-in, Nutrition, Progress, Longevity, Connections, Roles); ANALYZE (Statistics, Cockpit, Performance, Volume, Exercises, Trends, Velocity, Running, Force-plate, Video, History, Calendar) — the chart-heavy screens included, reusing recharts on web and the native bar/line primitives on mobile; TRAIN (Plans, Sport, Competition, Periodize, Builder [web + mobile], the Train launcher, and the Logger). IN-PLACE (template-aware radii/pills on the existing component, NOT a bespoke fork — a deliberate, lower-risk skin) rather than dedicated layouts: the mobile LIVE logger (app/workout.tsx — Aurora pills via auroraRadii AND, on Aurora, the ambient violet+lime blob field rendered behind its sticky-timer shell + the completion Summary, via the now-exported AuroraField from the kit, so the logger no longer reads as a flat classic screen in Aurora) + the mobile SESSION-DETAIL review (app/session/[id].tsx — previously always-classic with no template awareness, now wraps its body in AuroraScreen on Aurora / the classic Screen otherwise, with its PR/cardio banners + share CTA rounded to the Aurora pill/card radii) + mobile logger-settings + mobile More hub; the web account Settings (6-tab MFA/billing area); the web ADMIN console (operator-only, /admin, ~8.7k lines across 23 tools — its panel SHELL is template-aware pill nav + rounded chrome, the shared lib/ui --r-* radius tokens under [data-template=\"aurora\"] round the section bodies' cards/inputs/selects, AND the ~73 hardcoded structural borderRadius literals across the 19 tool files were converted to the same --r-card/--r-field tokens so no admin surface stays sharp-cornered next to the rounded shared components in Aurora — pills/dots/bars kept literal); and the multi-scope ANALYTICS hub (athlete/coach/operator dashboards — the scope toggle pills + every Card/ChartFrame/Stat tile round via the shared --r-card var, so the dashboards are Aurora-consistent without 3 risky recharts rewrites). The mobile Builder is a real route (apps/mobile/app/builder.tsx, reached from Train) with classic + Aurora variants. Components live in apps/web/components/aurora/* and apps/mobile/components/aurora/*; COACH surfaces are bespoke on BOTH clients: web AuroraCoach (full roster/invite/consent + ClientDetail programming), AuroraAskCoach (wired into Aurora Today), AuroraUpgrade, AuroraOrg, AuroraTalent, AuroraTactical, AuroraTeamCompare, AuroraTeamMonitor (gated in app-shell); mobile Aurora Coach tab, ai-coach, coach-apply, tactical, talent, upgrade. Icon set (theme/icons.ts) wired across both. BOTTOM NAV (mobile): Aurora's mobile navigation is a bespoke floating pill bar (Today(grid) – Cockpit(chart, athlete-only) – Train(add) – History(calendar) – More(settings)) shown on EVERY screen — not just the main tabs. apps/mobile/components/aurora/global-nav.tsx is rendered once at the ROOT (app/_layout.tsx) and is router-driven (useSegments for the active tab, router.navigate to switch), so it floats over pushed sub-pages (Statistics/Settings/Periodize/…) too; the per-Tabs Aurora bar is retired (Tabs tabBar → null in Aurora) so there's no double bar, and the classic glass bar + 'jump to' command orb render for classic only. It self-hides on auth/funnel + the live-workout screen. Each pill now carries a TEXT LABEL under its glyph (Today/Cockpit/Train/History/More) so the cog-glyphed 'More' tab no longer reads as 'Settings' and the active tab is unmistakable. The home GRID + statistics CHART glyphs are drawn from primitives (no extra asset); the rest use the shared Aurora line-icons. MOBILE AURORA POLISH (home/nav/overlap fixes): (1) the Today Plan-today⇄AI-coach pager aligns cards to the top so a shorter card keeps its natural height instead of stretching to the taller card's height (no more mostly-empty tall Plan-today card); (2) the Today 'Ask your coach' card embeds the AI-coach NOTE inline (AuroraAiCoach `embedded` prop renders just chips+text+Ask-again) instead of mounting the whole AI-coach SCREEN inside the card, which previously double-wrapped a header/back/inner-card; (3) the root Stack disables the back GESTURE on the (tabs) shell so the iOS edge-swipe can't pop the app back to Welcome/Login (which had looked like a sign-out, since welcome→login→replace-to-tabs leaves welcome underneath); (4) the shared Screen + AuroraScreen scroll primitives reserve ~132px bottom padding in Aurora so the floating pill nav never covers the last row (share buttons, sign-out, the Builder CTA on Train); (5) the More hub is reorganised — Settings is PINNED to the top (with the role/entitlement summary) rather than buried at the bottom, and BUILDER is a first-class entry in the More → Plan group (in addition to its existing Train-launcher entry), so composing a routine is reachable from the hub too. MORE-HUB DESIGN PASS (springboard, both clients): the section destinations now render as an iOS-style SPRINGBOARD GRID of rounded glyph tiles (the shared design-kit line icon per nav id via AURORA_NAV_ICONS, label beneath) instead of the old two-up cards with PER-ITEM coloured label text — fixing the 'rainbow text' inconsistency: every tile label is one chalk colour, the lime accent is reserved for the single Unlock-Full card + the active tile. UNLOCK FULL is reshaped from a thin pill into a premium 'membership card' (UPGRADE kicker → title → value line → 'Go Full →' pill, with a soft lime corner-sheen + glow). Mobile: apps/mobile/app/(tabs)/more.tsx (Settings + Athlete-cockpit rows neutralised to chalk with a glyph tile; springboard Tile component). Web parity: the AuroraPillNav 'More' sheet (apps/web/components/aurora/pill-nav.tsx) renders the same springboard tiles + the same Unlock-Full membership card (casual only, → upgrade screen). A SECOND audit pass fixed more of the same classes: (6) KEYBOARD AVOIDANCE — no mobile screen had any, so the keyboard covered low inputs/submit buttons; the shared Screen + AuroraScreen primitives (and the live workout logger) now wrap their scroll body in a KeyboardAvoidingView (iOS padding) with keyboardShouldPersistTaps, fixing every form screen (login/builder/check-in/nutrition/settings/talent/…) at once; (7) the 'builder' nav id was missing from the mobile More hub's MOBILE_NAV_IDS set, so athletes saw 'Builder' wrongly listed under 'More in the web app' even though mobile has a real Builder route — added it; (8) the Aurora Home header SEARCH button was a bare View (looked tappable, did nothing) — wired to the searchable Exercises browser, matching the working bell→Notifications next to it. AURORA DESIGN PASS (both clients, applied to the Today/Home hero): (#1) the greeting is demoted to a single quiet line so the readiness-bearing plan is the hero, not the user's own name; (#2) the AI-coach PROSE renders in the Archivo display sans (mono stays for chips) so a personal note reads like a coach, not a terminal log; (#3) headline numbers are now GLANCEABLE — readiness shows as a dial (mobile: dependency-free tick-ring via the kit `Ring`; web: a conic-gradient ring) and the Athlete-Twin HPI carries a 14-day SPARKLINE (`Spark`, from performanceTrajectory); (#4) colour SEMANTICS are locked in one shared vocabulary (packages/core/src/semantic.ts — role map go/info/premium/caution/danger/neutral + unit-tested readinessRole/hpiRole/riskRole/accountabilityRole/phaseRole), and the Today screens resolve every state colour through it so web+mobile can't drift; (#5) finishing a workout is a felt MOMENT — the mobile summary fires a success haptic (a heavier knock for a PR/first) and springs/fades the hero badge + PR card in; web wins pop-in on the Today recap (globals.css .win-pop, reduced-motion aware); (#6) the floating-nav clearance magic number was centralised in apps/mobile/lib/layout.ts (AURORA_NAV_BAR_HEIGHT + auroraScrollClearance) and the screen primitives derive bottom padding from the real bar height + safe-area inset, so a bar change can't re-hide content. Remaining screens migrate to the shared semantic helpers + Ring/Spark incrementally. DESIGN-FIDELITY AUDIT (vs the reference Aurora design): (A) the classic LEFT-ACCENT STRIPE (a card's coloured left rail — the classic HYBRID signature, used 0× in the reference aurora-preview.html, 21× in the classic HybridApp.jsx) was carried over onto Aurora cards; removed every card-accent stripe across BOTH clients (13 mobile + 24 web), including the conditional/semantic ones — each already conveys its state via a chip or coloured value (private-note chip, tactical GO/NO-GO chips, application-status text), so no information was lost (one blockquote-style indent on the check-in coach reply is kept — it's a quote treatment, not a card rail). (B) SKINNED→BESPOKE pass: (i) mobile logger-settings is now a dedicated components/aurora/logger-settings.tsx (AuroraScreen + ACard + ASegment segmented pills); (ii) the mobile More hub renders inside AuroraScreen; (iii) the mobile LIVE logger (already pill/rounded-card Aurora via auroraRadii) had its last hardcoded sharp corners (custom-exercise input + set-type/superset/work-rest/load-bump toggles) routed through the Aurora field-radius token; (iv) the web account Settings (6-tab MFA/billing — deliberately NOT forked) had its control radii aligned to the web Aurora tokens (--r-field 14, --r-card 24). The web ANALYTICS hub and the ~8.7k-line ADMIN console were CONFIRMED already Aurora-rounded via the shared token system ([data-template=\"aurora\"] sets --r-card:24/--r-field:14 and rounds .rounded-* + inputs; 17/22 admin tool files consume var(--r-card)/var(--r-field), the rest use Tailwind rounded-* which the Aurora overrides round) — a bespoke recharts/operator-tooling fork stays out of scope as the explicitly higher-risk, operator-only path. (C) TOKEN FIDELITY: verified our Aurora against reference/aurora-preview.html — colour tokens, type (Archivo + JetBrains Mono), the pill/segmented/choice/field/back/avatar primitives and the violet+lime blob field all match; the one drift (web corner radii had shrunk to --r-card 24/--r-field 14) was corrected to the reference's 28/16 across globals.css + the 21 hardcoded web card radii + the mobile Card primitive, so card=28/field=16/pill=999 is now uniform on both clients. (D) The reference is a design LANGUAGE + a few hero screens (auth/onboarding/home), so the other ~40 screens are a faithful EXTRAPOLATION of that language (same tokens/primitives/icons), not pixel copies — no foreign design system is used. (E) MODERN BEYOND THE REFERENCE (deliberate): the bottom nav is now LIQUID GLASS (mobile BlurView intensity 28 + translucent film + rim; web backdrop-filter blur(18px) saturate(1.2) over a 62% ink2 tint) — lighter than the classic glass and glassier than the reference's near-solid rgba(20,22,20,.92) bar; plus the readiness dial, HPI sparkline, finish haptics/animation already shipped. (F) FURTHER MODERN pass: (a) MOTION — content fades + rises (~10px) on every screen ENTRY, reduced-motion aware: mobile AuroraScreen + Today home wrap their body in an Animated.View driven by useFocusEffect (replays on pushes AND tab switches), web uses a key={screen} wrapper + .aurora-enter keyframe in the app-shell; (b) DEPTH — a soft low shadow on key cards (mobile ACard + the raw home cards; all 36 web Aurora card surfaces), far lighter than the classic glass shadow; (c) a real BESPOKE Aurora ANALYTICS dashboard for all three scopes (components/aurora/analytics.tsx — AuroraAthleteAnalytics/CoachAnalytics/OperatorAnalytics: accent-dot stat tiles, pill-framed brand-coloured recharts, a rounded roster/PR table), wired into the app-shell analytics screen in Aurora, reusing the exact sessions/roster/admin-stats data + engines (classic kept for the classic template). The bottom nav is also now LIQUID GLASS on both clients (mobile BlurView + film + rim; web backdrop-filter blur). Web production build green. WEB NAV: web keeps the left SIDEBAR at all times — in BOTH templates and on every screen (Aurora rounds it in place via the global skin; it is never replaced by a bottom bar), matching the uploaded HybridWeb sidebar design. In Aurora the app-shell drops its top page-title header (the bespoke Aurora screens render their own header, so this prevents a doubled title) and hides the ⌘K command orb (the sidebar is the nav — no floating circle); Classic keeps both. The theme/lang controls stay top-right in both. Aurora web screens fill a fuller content width (≈1000) rather than a narrow centred column, so they don't look stranded on desktop. Aurora web shows BOTH the sidebar AND a floating bottom pill bar (apps/web/components/aurora/pill-nav.tsx — Today – Cockpit – Train – History – More, More opens the full persona-filtered nav sheet); the ⌘K orb is Classic-only. ICONS: every nav glyph in Aurora comes ONLY from the uploaded design kit (icons1/2/3) — theme/icons.ts AURORA_NAV_ICONS maps EVERY nav id → an AuroraIconName, and 19 kit icons were wired into the shared set (SVG path data + mobile PNGs + the RN require map): bookmark, list-check/play/add, calendar-event, user-circle/add/square, store, globe, swap, village, gps, info, offer, check-circle, download, copy. No unicode/emoji glyphs remain in the Aurora nav; the kit is a generic UI set, so a few fitness/analytics items reuse the nearest icon (e.g. arrow-up for the trend metrics). Reaching a tool from Aurora Today (bell/Your-week) navigates IN-SHELL (keeps the sidebar + bar). Classic keeps its unicode glyphs + sidebar + ⌘K orb unchanged. The three lightweight TOOLS (Statistics, Interval timer, Notifications) are NOT Aurora-only — they ship in BOTH templates (template-aware radii/back glyph) and are first-class nav items (web sidebar + ⌘K via app-shell embedded screens; mobile More → Tools), reachable by the default Classic/casual user; the standalone web routes (/statistics, /timer, /notifications) remain for landing deep-links. iOS bundle export green; web + mobile typecheck clean. AURORA IS THE ONLY template on both clients (DEFAULT_TEMPLATE='aurora', TemplateName='aurora') — it's the main HYBRID look; the Classic skin and its Settings switcher were removed, and the dead classic screen variants described above no longer exist (see ui-templates). Also fixed: the mobile login race where the first sign-in attempt bounced back to /login — the login screens now navigate only once the Supabase session is live in context (a session-gated effect), so the first attempt lands reliably. RUN TRACKING shipped as a real PLACEHOLDER on both clients (nav id 'runtrack', train group, casual-visible): apps/web/components/run-track.tsx + aurora/run-track.tsx and apps/mobile/app/run-track.tsx + components/aurora/run-track.tsx — a live-run surface with a placeholder route map (clearly flagged 'live GPS lands with the native build') plus a working stopwatch + manual distance → derived pace (pacePerKm) that saves a real cardio session via /api/sessions. Reached from the web sidebar, the mobile More hub + command menu. The only piece still pending is the native GPS route MAP itself (needs expo-location + a map lib via an EAS build — can't be built/validated in this sandbox); it drops into this same screen without changing the save logic." },
  { id: "ui-aurora-stats", area: "Core", status: "shipped", title: "Statistics screen (session stats)", detail: "The Figma Statistics screen built for real on BOTH clients off REAL data. @hybrid/core stats.ts sessionBuckets groups logged sessions into Week (7 daily) / Month (5 weekly) / Year (12 monthly) buckets with window totals, active-day count and the peak bucket, unit-tested; empty history → all-zero buckets + honest empty state. Mobile app/statistics.tsx + web components/statistics.tsx render a Week/Month/Year segmented bar chart plus summary cards (active days, HPI, distance, minutes) from sessionBuckets + weeklyRecap + computePerformanceState. Available in BOTH templates (template-aware radii/back glyph, not Aurora-only): web is a first-class nav item (sidebar + ⌘K, embedded in app-shell) AND the /statistics route (landing deep-link); mobile is reached from the Aurora Home readiness hero AND the More → Tools section (so Classic users get it too). iOS bundle export green." },
  { id: "ui-aurora-timer", area: "Core", status: "shipped", title: "Interval timer (work/rest stopwatch)", detail: "A real interval timer — the Figma 'Workout' screen built for real on BOTH clients. @hybrid/core interval.ts is the pure engine (buildIntervalPlan expands rounds×work/rest with a lead-in and drops the trailing rest; locateInterval maps elapsed seconds → active phase + remaining; formatClock mm:ss), unit-tested. Mobile app/interval-timer.tsx + web components/interval-timer.tsx run the identical engine: 10s lead-in, work/rest cycling, round counter, progress ring + session bar, play/pause/reset and rounds/work/rest steppers. Available in BOTH templates (template-aware radii/back glyph, not Aurora-only): web is a first-class nav item 'timer' (sidebar + ⌘K, embedded in app-shell) AND the /timer route (landing deep-link); mobile launches from the Aurora Home (conditioning items → timer, strength → logger) AND the More → Tools section (so Classic users get it too). iOS bundle export green." },
  { id: "ui-aurora-activity", area: "Core", status: "shipped", title: "Notifications / activity feed", detail: "A real notifications feed (not mocked) — @hybrid/core activity.ts buildActivityFeed turns the athlete's REAL logged sessions + coach-assigned workouts into a sorted list (upcoming workouts first, then recent completed within a 14-day window; relativeTime formats 'in 2d' / '1h ago'), unit-tested; empty data → empty feed (honest, no fabricated entries). Mobile app/notifications.tsx and web components/notifications.tsx render it from each client's data. BOTH clients now feed sessions + coach assignments (web fetches /api/assignments, matching mobile — parity, no longer sessions-only). Available in BOTH templates (template-aware radii/back glyph, not Aurora-only): web is a first-class nav item 'notifications' (sidebar + ⌘K, embedded in app-shell) AND the /notifications route (landing deep-link); mobile is reached from the Aurora Home bell AND the More → Tools section (so Classic users get it too). SUPERSEDED IN PLACE by notifications-read-live-feel: activity.ts is still the training half of the feed, but the screens and the bell now read notifications.ts — one merged list (training + social + the open feel reads) with a read state and a live poll, so the badge counts UNREAD rather than the length of the training feed." },
  { id: "notifications-read-live-feel", area: "Core", status: "shipped", title: "The bell means something \u2014 one live list, a read state, and the feel reminder", detail: "Three complaints, one root cause: there was no notifications MODEL. Each client assembled the screen out of two unrelated fetches (the training feed from activity.ts, the social events from /api/social/notifications) and the badge counted a THIRD thing \u2014 the LENGTH of the training feed alone, recomputed independently on Today. (1) NOTHING WAS EVER READ. No read state existed anywhere in the app, so the badge showed 'how much training happened in the last fortnight', a number that cannot go down. A count that never reaches zero is not a badge, it is decoration, and an athlete learns within a week to ignore it. (2) THE LIST WAS A SNAPSHOT. Web fetched once on mount, mobile once per focus, so a kudos landing while you watched the screen needed a reload \u2014 and the badge, computed off a different cache on a different screen, could read 3 over a list of 7. (3) THE ONE NOTIFICATION THE APP ACTUALLY OWES YOU WAS MISSING. feel-schedule.ts has had a clock, a due time, an expiry and msUntilNextRead 'ready for a local notification' since two-read-feel shipped, and nothing ever asked for one. The recovery read is the one that changes training and the one you are guaranteed NOT to be looking at a screen for \u2014 it opens six hours after the session, long after the finish card is gone. The prompt that has to travel to find you was the only prompt with no way to. NOW: packages/core/src/notifications.ts owns the whole thing \u2014 buildNotifications MERGES training, social and the OPEN feel reads into one sorted list, marks each row against a read state, and returns the unread count both clients render. Ordering is urgency then recency (an open feel read, then a request waiting on you, then upcoming, then what already happened), because a purely chronological list buries the two rows that want something. THE READ MODEL is a watermark plus an exception set: a pure watermark cannot express an UPCOMING session (its timestamp is in the future, so any 'read everything up to now' sweep either misses it or pre-reads next week's training), so marking all read stamps seenAt = now and names the future-dated rows explicitly. Bounded to 200 ids \u2014 a read state is not a log. THE FEEL ROWS: the immediate read (title 'How hard was that?', routes to that session's summary) while its 3 h window is open, and the recovery read ('How did you absorb it?', routes to the check-in) from 6 h to the 36 h boundary. Stamped at dueAt rather than now, so '1h ago' is the truth about when the app started waiting. An answered read is dropped by the schedule; a missed one is recorded and never nagged about; a read-but-unanswered one stays in the list without counting \u2014 reminded, not nagged. BOTH CLIENTS, one shape: apps/{web,mobile}/lib/use-notifications.* compose the same engine over react-query (60 s poll + revalidate on window focus / app foreground, and every mutation in the app already invalidates the keys), and apps/{web,mobile}/lib/notif-read.* persist the read state per device (localStorage / AsyncStorage, the logger-prefs contract) behind a useSyncExternalStore subscription \u2014 so marking read empties the badge in the same frame. The Today/Home bell now reads `unread` from that ONE hook instead of computing its own number. The screens gained: a row that is tappable (marks read, then routes), a sweep that marks the visible list read 1.5 s after it opens (not instantly \u2014 a badge that empties before the eye reaches the list has told the athlete nothing; the 'New' markers are frozen for the visit, and cleared on each focus on mobile since an RN screen stays mounted), a MARK ALL READ action in the SectionHead metadata voice, and unread state carried by WEIGHT and full chalk against ash, with a semantic dot on the TRAILING edge so it can never read as a header marker. New i18n keys notif.* (EN/PL/DE) and titleKeys on the engine rows, so the feed localizes for the first time \u2014 which also retired the last 'workout' strings in activity.ts ('Session completed' / 'Upcoming session', the vocabulary guard's rule). 27 tests in notifications.test.ts. DELIVERY is still in-app only: this is the list and the badge, not APNs \u2014 pushing a feel reminder to a locked phone is push-notifications, which stays blocked on Expo push credentials. Cross-device read state is prefs-cross-device-sync. TWO SECTIONS AND TWO GESTURES (Aug 2026): the list was still ONE flat run of rows, and a read row sat exactly where it had always sat \u2014 greyed, but still holding its place. So a fortnight of ordinary training buried the one row that wanted something, and the only way to clear a row you had dealt with was to let it age out. NOW: the screen splits into NEW and SEEN (both clients, one head anatomy \u2014 the Explore SectionHead: display-face title left, meta or head-level control right, no marker; 'Mark all read' moved into New's right slot and the seen COUNT rides Seen's). The line is NOT drawn by `read`, and that is the whole reason splitNotifications lives in the engine rather than as a filter in each client: the screen sweeps itself read a second and a half after it opens, so a split on `read` would tip every row you are still looking at into Seen under your eyes \u2014 the list would reshuffle itself while being read, the exact failure the frozen 'New' markers were introduced to avoid. The line is drawn by the VISIT (`visitNew`, the ref each client already kept, cleared on focus on mobile): rows you arrived to hold their place until you leave and come back. EACH ROW IS A SWIPE ROW, one shared component per client (apps/{web,mobile}/components/swipe-row.tsx, the logger's) rather than a fifth hand-rolled gesture: LEFT deletes, RIGHT puts it back to unread. Both sides obey one grammar \u2014 a short swipe OPENS the action so it can be tapped (and so a keyboard/VoiceOver user reaches it: web renders real buttons, mobile carries `unread`/`delete` as custom accessibility actions in the rotor), a full swipe COMMITS it outright \u2014 and the leading action then settles HOME rather than running off the edge, because it changes the row's state instead of removing it. The physics stay the shared @hybrid/core `swipe` constants, so the two clients cannot drift on the thresholds again. THE READ STATE GREW THE TWO SETS A WATERMARK CANNOT HOLD, and each is an id set because the row is DERIVED \u2014 rebuilt from the sessions and the social feed on every poll, so nothing written onto it survives. `unreadIds` BEATS the watermark: a row put back to unread is older than `seenAt` by definition, so without it the very next sweep would silently undo the athlete's decision. Which forced the sweep to split in two: sweepNotifsRead is the PASSIVE one (the 1.5 s timer) and never clears a hand-set unread \u2014 a timer has no standing to undo a gesture \u2014 while markAllNotifsRead, the button, does, because 'mark all read' that leaves one row bold has not done what it says. The passive sweep also returns the SAME state object when there is nothing to sweep, and both stores skip the write on identity, which is what stops a held-unread row from re-arming the sweep on every 60 s poll forever. `dismissedIds` is a TOMBSTONE: there is no notifications table to delete a row FROM (the list is a projection of training, social events and the feel schedule), so the id is remembered and filtered out of every later build \u2014 before the limit, so deleting one lets the next row up rather than shortening the list. Both sets are bounded at the same 200 as readIds, and normalizeNotifRead fills them in for every device that stored the older two-field shape. New i18n notif.seen / notif.markUnread / notif.swipeHint (EN/PL/DE) \u2014 the hint is the mono aside History already uses over its swipe cards, because a gesture nothing on screen mentions is a gesture nobody finds. 21 further tests in notifications.test.ts." },
  { id: "profile-you", area: "Core", status: "shipped", title: "Profile (\"You\") screen + bottom-nav redesign", detail: "A dedicated athlete PROFILE on BOTH clients — mobile apps/mobile/components/aurora/profile.tsx (the /(tabs)/you route) and web apps/web/components/aurora/profile.tsx (wired into app-shell as the 'profile' screen). Apple-ID / Tesla-account styling on the HYBRID brand tokens: a centered account hero (avatar + edit badge, FULL/FREE tier chip, 'HYBRID ID – email', member-since), a hairline SPEC strip (HPI / Streak / PRs), a premium membership ID CARD (etched, lime sheen, athlete-id hash, no PII), an oversized HPI HERO with a 12-bar trace (performanceTrajectory) + band + pillar delta, a year-of-training HEATMAP, an ACHIEVEMENTS rail, top PERSONAL RECORDS, and athlete MODULE tiles (readiness / body / devices / coach). EVERY metric is REAL, computed in @hybrid/core from logged sessions + recovery signals (computePerformanceState, performanceTrajectory, trainingHeatmap, computeAchievements, lifetimePrCount, longestWeekStreak, streak, topLoadMap (top PRs now rank + read the athlete's HEAVIEST actual lift per movement, not an estimated 1RM — e1RM stays only as the strength-club achievement basis), sessionVolume); empty history degrades to honest empty states (no fabricated numbers). ACHIEVEMENTS are real, tiered, earned/locked milestones — strength club (max e1RM), consecutive-week streak, session count, lifetime tonnage, PRs broken, furthest run — pure + unit-tested (engines/achievements.ts + heatmap helper in engines/calendar.ts, lifetimePrCount in engines/records.ts). NAV REDESIGN (both clients, Aurora): the floating bottom bar is Today – Plans – [Train] – History – More — Cockpit moved into the More hub (still reachable there) and Train became an ELEVATED centre FAB (raised circle + dumbbell glyph). (The bar originally ended in a 'You' profile pill; once the Today-header redesign added a profile avatar, that pill was redundant and was swapped for Plans — see today-header-redesign. Profile is still reached from the header, the web sidebar/More-sheet, and a pinned card atop the mobile More hub.) mobile components/aurora/global-nav.tsx + web components/aurora/pill-nav.tsx. Mobile typecheck + iOS export green; web typecheck + production build green. NAV ENTRY (fix): web now also exposes Profile as a sidebar nav item (NAV_ITEMS, account group, every persona) with an Aurora user-circle glyph + nav.profile i18n (EN/PL/DE), so it's reachable from the desktop rail / ⌘K — previously it was ONLY on the Aurora 'You' pill, so desktop users had no way in ('on web there is no profile'). SOCIAL PASS (both clients, at parity): the profile was reorganized into a social layout — a cover banner, an avatar overlapping it with an EDIT (pencil) icon button (the old 'Edit profile' + 'Share' buttons removed; editing routes to /profile-edit on mobile and the settings surface on web), name + FREE/FULL membership pill (unchanged), bio + a quiet HYBRID-ID line, a Followers/Following/Rank counts row (rank derived from the real leaderboard endpoint), tabs (Overview / PRs / Activity) and a 3-column PUBLIC highlight grid built ONLY from real public stats (top PRs, streak, sessions, tonnage, badges; tiles with no real datum are omitted, never faked). PRIVACY: HPI is now PRIVATE — deliberately absent from the public grid and every follower-facing surface; it lives only in a 'Private – only you' card at the bottom (gated by canSeeHPI: the score for Full, the locked upsell teaser for free). mobile aurora/profile.tsx + web aurora/profile.tsx, still at parity. PARITY/FIDELITY HARDENING: audited 1:1 and against the mock — every new label moved to w.account.profile.* i18n (EN/PL/DE); mobile HPI empty-state now shows unrated/'—' (was a fabricated score) and the HPI trajectory window is aligned to 30d on both; the avatar lime ring, per-tile Overview icons and 96px cover were restored to match the mock. The edit affordance is an icon (the shared 'settings' glyph — a pencil PNG asset is a blocked follow-up), never an Edit/Share button." },
  { id: "today-header-redesign", area: "Core", status: "shipped", title: "Today tab header redesign — profile – HYBRID logo – notifications", detail: "Step 1 of a step-by-step Today-tab redesign. The pre-workout top of Today now opens with a dedicated brand HEADER on BOTH clients (apps/mobile/components/aurora/home.tsx + apps/web/components/aurora/today.tsx), replacing the old quiet greeting + search/bell row: a profile AVATAR on the left (initials on a lime-tinted, theme-aware square with a lime 'live' dot — opens the You/profile screen), the centred HYBRID. wordmark with a short lime accent rule beneath it, and a NOTIFICATIONS bell on the right carrying a live count badge. The badge is REAL — driven by the shared activity feed (buildActivityFeed over the athlete's logged sessions [+ coach assignments on mobile]; capped at 9+), never a fabricated number, and hidden at zero. Squared (radius-14) icon containers are the new Today-header treatment. The in-header Exercises SEARCH shortcut was removed per the redesign brief (Exercises stays reachable from its own route/nav). New shared i18n key w.home.today.profileAria (EN/PL/DE). Five standalone design options were explored in reference/today-header-mockups/ and Option 5 (compact, centred logo + lime accent, count badge) was chosen. BOTTOM-NAV DEDUP (follow-up): putting profile in the header made the bottom-bar 'You' pill redundant, so it was swapped for PLANS on BOTH clients — the bar now reads Today – Plans – [Train] – History – More (Plans beside Today, More on the far edge as the conventional last slot). Plans is casual-visible and the core 'follow a program' loop, previously buried in More. Profile stays reachable everywhere: the Today-header avatar, the web sidebar + More-sheet Account group, and a new pinned PROFILE card at the top of the mobile More hub (avatar initials + name + role → /(tabs)/you) so it isn't stranded on non-Today screens where the header isn't shown. Core tests + mobile typecheck green; web today.tsx/pill-nav typecheck clean (pre-existing Prisma-codegen errors are sandbox-only). MORE STEPS PLANNED — this only redesigns the header + the nav dedup; the rest of the Today tab is refined in subsequent steps. THE LOCKUP (the centring fix, both clients in one change): the header row was `space-between` with the wordmark as its middle child, which centres that child only when both flanks weigh the same. They never did — one 44px avatar tile on the left against the day-streak PILL plus the bell on the right — so the brand rendered ~69px LEFT of the screen's centre on a 390pt device and slid further with every digit the streak gained. Pinning the wordmark to the centre instead was measured and REJECTED: at a 12-day streak on 390pt it drives the wordmark 44px INTO the pill. The row is now a THREE-COLUMN grid with FIXED FLANKS — web `grid-template-columns: 44px 1fr 44px`, mobile fixed 42dp sides with a `flex: 1` centre column — so the wordmark is centred BY CONSTRUCTION, whatever the flanks carry. The streak LEFT THE ROW and became the lockup's second line: a hairline mono caption directly under the wordmark (9.5, .13em, uppercase, terracotta accent-text, flame glyph at 11) tapping through to the same done sheet the pill opened. It therefore survives on all three hub tabs (they render this one header), and it costs NO height — wordmark 19 + caption ~17 still sits inside the 44px/42dp the tiles already set, so the header's height is unchanged. A zero streak renders the wordmark alone. NO new i18n keys: the caption reuses w.home.today.dayStreak, uppercased at the leaf so PL/DE inherit it. Three directions were drawn to true device scale and self-measured in the design artifact (current −69px; the pinned-centre collision; the three candidates at 0px) and the LOCKUP was chosen. This SUPERSEDES the terracotta streak pill in the header row described by today-focus-hierarchy." },
  { id: "exercise-table-bounds", area: "Core", status: "shipped", title: "Trends exercise table \u2014 bounded, folded, and cheap by default", detail: "exerciseTable(sessions, \"all\") used to return EVERY exercise the athlete had ever logged, unbounded, building each row with a per-exercise exerciseDashboard() pass over full history \u2014 the heaviest single call in the app \u2014 and both Trends screens mounted with period 'all' and rendered every row. THREE BOUNDS NOW, all shared: (1) the DEFAULT PERIOD is 8w on both clients \u2014 it matches the two measure bands above the table, and 'all' stays one tap away on the period switch; (2) the ENGINE prefilters by exerciseHistory's lastUsed BEFORE running dashboards, so a movement last touched outside the window costs nothing instead of a full-history pass that the sessions>0 filter then threw away (on 'all' the cutoff is -Infinity and nothing is skipped \u2014 the rows are provably identical, tested); (3) the TABLE FOLDS at EXERCISE_TABLE_FOLD (12, the engine's constant so the clients cannot fold at two depths), ending in the house expander \u2014 chromeless bare \uff0b/\u2212, ash count naming exactly how many rows are folded (a cap is never silent), the endurance-lanes All-sports grammar. Collapsed mount cost is now 12 rows over an 8-week window regardless of history depth. DELIBERATELY NOT DONE: FlatList/FlashList windowing for the expanded state \u2014 the table lives inside the screen's one scroller (and inside the unified Performance page's), where RN forbids nested VirtualizedLists; expanding is an explicit athlete action on a list bounded by their own exercise catalogue (~low hundreds worst case). If a real catalogue makes the expanded render hurt, that restructure belongs to mobile-list-virtualization." },
  { id: "performance-six-surfaces", area: "Core", status: "shipped", title: "Performance rebuilt \u2014 twenty surfaces to six", detail: "An element-level audit of the Performance tab (audit/10-performance-tab-element-audit-2026-08.md, mockups in design/performance-tab-before-after.html) judged 26 elements across 20 card surfaces and found the page had become an archive: Volume and Trends, absorbed whole by `performance-unified`, took roughly 68% of the scroll against 16% for the four blocks that answer 'how am I doing right now', and eleven cards restated a neighbour or belonged on a screen of their own. FOUR DEFECTS FIXED FIRST, all of them places where the page contradicted itself. (1) The headline HPI figure applied the wearable's \u00b115 adjustment while the sparkline beside it and the whole trajectory chart came from performanceTrajectory() \u2192 computeHpi(fatigue) with NO biometrics \u2014 two numbers for the same day, 8px apart; same split on readiness (the ring read prescribeSession's bio+subjective score, the chart's series neither). performanceTrajectory now takes `bio` and applies it to the daysAgo===0 point only (there is no stored wearable history to replay, and today's point is the one the card also prints as a figure), and both clients compute the series ONCE instead of twice. (2) 'Your week' counted a rolling seven days while the sibling hub tab's 'This week' counts Monday\u2192Sunday \u2014 the card is cut and its PRs moved to Today's activity card via a new range-aware prsBetween(), so a month view lists the month's records. (3) HPI is 100\u2212fatigue: it rises on a deload and rises further on a layoff, with a top band called 'peak'. It is now LABELLED for what it measures \u2014 Freshness \u2014 and joined by the half a performance screen was missing: capabilityTrend() (engines/capability.ts), which splits an 8-week window in half and compares each movement's best e1RM (up is better) and best pace (down is better) across the halves, counting only movements present in BOTH halves and never mixing the units. (4) STR/END/REC rendered as three peer figures across two scales; recovery is a \u00b115 additive and now rides the headline as a signed contribution instead of impersonating a third index. THE PAGE, both clients: masthead with one computed VERDICT sentence (stateVerdict(), freshness band + the flagged tissue) replacing a subtitle that described the page rather than the athlete, and two chips instead of four \u2014 the HPI and ACWR pills were previews of a card within reach; YOUR STATE, the one block that grew (300\u2192430px), carrying freshness, the wearable's signed share, the limiter as a display-face SENTENCE with the top driver behind it (the Breakdown card's one unique line), two freshness columns, the capability row, the 14-day plot and readiness untouched; TISSUE; this week's VOLUME hero + a door; SEASON as one card; and five exit rows each carrying a live value. CUT: 'Your week', the Breakdown tabs (three of four panels duplicated the exit rows verbatim from the same runTotals()/velocityProfiles() calls), the band glossary, and all four group markers \u2014 six blocks in a deliberate order need no signposting. THE PLOT IS SHARED GEOMETRY: trajectory-plot.ts (trajectoryPlot / sessionDaysAgo, 11 tests) returns paths, a zoomed-and-STATED domain snapped to tens, and a mark per day that carried training \u2014 a dip you cannot attribute is decoration with axes. It also closed a parity drift: mobile had been approximating the chart with bars and per-day markers while web drew a recharts line, so the same fourteen days had two shapes and only one could be read per point. Both clients now stroke the same paths (react-native-svg was already in the app for the injury mannequin), and recharts is off this page entirely. MOVED, NOT DELETED: Volume and Trends are DESTINATIONS again (nav.ts promotedTo removed; web app-shell renders them directly, the mobile /volume and /trends routes render their screens instead of redirecting to /performance). Volume gained a `compact` prop \u2014 the hero shape, a verdict that names the muscles and their deltas, and one door \u2014 resolved by the SAME landmark code as the full screen so the two can never disagree, and skipping replayLandmarks() and seven weeklyMuscleSets() passes it would never render. The running RTP protocol moved to Today's Recover cluster (RtpPanel on both clients), where an injured athlete meets it on the morning they have to do it; the Tissue card keeps a status line and the door, so the flag and the protocol stay one object. The risk model version moved off the card face into a 'how is this calculated' disclosure. Web's 'Open setup' stopped expanding the whole enrolment flow inline mid-scroll and now routes, matching mobile. ALSO: the casual-persona teaser lost its six decorative leading dots (house rule), the dead `macroReady` prop is gone, and velocityProfiles is computed once rather than twice per render. New i18n in EN/PL/DE (w.home.cockpit.verdict.*, stateTitle, freshness, wearableOf, strengthFresh/enduranceFresh, limiter.*, capability, last14, shownRange, trajectoryKey, weekVolume, inRange, volumeDoor, seasonWeekOf, changeSeason, deeper, trends, askCoach; w.injury.protocolRunning/protocolDay/protocolWhere/howCalculated). Core tests green (2139), web + mobile typecheck green. THE THREE FOLLOW-UPS ARE NOW CLOSED TOO. (a) THE MODEL HAS ITS OWN ROUTE: the landmark fields, the profile form and the two model switches \u2014 around fifty controls that were revealed inside the Volume SCREEN by an 'Edit landmarks' toggle \u2014 are now aurora/volume-model.tsx on both clients (web screen id volume-model, promotedTo volume; mobile /volume-model), reached from Volume's header and from its provenance card's next-gap row. The resolution itself was extracted to a shared hook (lib/use-volume-model.ts, both clients) so the screen and the route read the IDENTICAL landmarks and an edit can never be computed two ways. And because these are model parameters rather than preferences, the route LEADS with the diff: the same profile resolved with and without the athlete's overrides, so every band they have moved is shown as a change from the estimate it replaced ('chest MRV 18 \u2192 22') and can be put back. Editing used to rewrite every band, prescription and verdict above it with no confirmation, no history and no way to tell an edit apart from the model's own estimate. Volume itself lost its `editing` mode entirely \u2014 the inline field grid, the block steppers, the about-you form and the reset button are gone from the read surface. (b) THE BAND GLOSSARY IS CUT: a collapsed card defining MV/MEV/MAV/MRV at the foot of the Volume screen, when every band value on every muscle row is already a control that spotlights that band across the whole list and prints its definition beside the pointer. The interactive version shows rather than tells and arrives when the question is asked; the card was the pre-interactive version left in place. (c) THE TEASER DEMONSTRATES: a casual-persona athlete used to get six identical padlocked rows telling them nothing about what the feature would say about THEM. It now leads with their OWN freshness \u2014 the real figure, from their real log, by the same engine the full page uses \u2014 plus the limiter sentence, and locks the depth behind it. With nothing logged there is no figure and none is invented. NEXT: the trends exercise table is still uncapped and unvirtualised on its own screen \u2014 tracked as exercise-table-bounds. RE-VERIFIED against the register (Aug 2026): all four defects hold in code \u2014 the figure, the plot's last point, the readiness ring (readinessDeficit.kept) and Today's ring (prescribeSession.readiness) are all computeReadiness/computeHpi(fatigue, bio) reads of the same day (the check-in scales only the LOAD, never the score, so no surface disagrees) \u2014 and the freshness rename's last live 'HPI' strings were swept: the tab's own empty state (w.home.cockpit.twinEmpty), the teaser's perfTwin blurb, and the web Connections intro now say freshness in EN/PL/DE (mobile's intro already did), and the dead HPI-era keys (the retired profile hero's hpi-title/hpi-empty/hpi-locked-*, the upgrade athlete-twin row, analyze's twinHpi/emptyBody) are deleted from all three locales. The B2B Teams surfaces (talent/tactical) keep 'HPI' deliberately \u2014 there it is the index's product name pitched to orgs, the same exception the admin Engine Room holds." },
  { id: "state-headline-and-tissue-rail", area: "Core", status: "shipped", title: "Your state headline + the tissue card's footer rail", detail: "TWO LAYOUT DEFECTS, both reported off screenshots. (1) THE FRESHNESS HEADLINE set the metric's NAME and the athlete's READING at identical weight — one mono ash caption reading 'FRESHNESS \u2014 COMPROMISED', the two halves joined by a dash \u2014 so nothing on the line said which of them was the fact, and a second ash line ('Wearable -3 of that') sat under it at a similar size in a different face, giving the block two grey rules and no hierarchy. The band also went UNCOLOURED beside a numeral that was coloured from the same band, splitting one state across two treatments 8px apart. It is now three levels: the label in mono nano, the BAND as a display-face word in the figure's own colour (they are one fact stated twice), and the wearable as quiet provenance below. (2) THE BAND WAS NEVER TRANSLATED \u2014 `hpi.band` is an engine identifier and both clients printed it raw, uppercased by CSS, so the one word that tells an athlete what their score MEANS read 'COMPROMISED' inside an otherwise-Polish line. hpi.ts now exports HPI_BAND_KEY plus a tolerant `hpiBandKey(band: string)` resolver (the coach roster holds an API view type where band is a plain string, so neither client has to cast), with EN/PL/DE words that agree with the feminine \u015awie\u017co\u015b\u0107/Frische. Applied on the Performance card, its casual-persona teaser and the org roster chips, on both clients; the admin Engine Room deliberately keeps the raw identifier, being an operator console. Seven tests pin it: every band has a key, every key resolves in all three languages, PL and DE never echo the English word, and every band word is distinct per language. (3) THE SIGNED WEARABLE printed `-3` \u2014 a hyphen left behind by a template literal, which reads as a dash in a sentence rather than a sign. Both clients share a `signedPoints` helper emitting a real minus (U+2212), and the copy moved from 'Wearable {n} of that' to 'Includes {n} from your wearable' (PL 'W tym {n} od zegarka', DE 'Darin {n} vom Wearable'). (4) THE TISSUE CARD'S FOOTER ended in a right-hugging COLUMN of two controls that do entirely different things: the action row was space-between with an EMPTY left slot whenever a tissue was flagged, so 'Open protocol' sat alone on the right, and 'How is this calculated?' was pinned under it by margin-left:auto (web) / alignSelf:flex-end (mobile), with the model line right-aligned under that. A disclosure about the risk model directly beneath the way into a rehab protocol, at identical weight, with the entire left half of the card empty. Now ONE rail, two ends, kind mapped to side: the quiet disclosures ('All tissues', 'How is this calculated?') group LEFT, the action stays RIGHT in its terracotta, and the model line lands under the control that opened it instead of drifting off the card edge. The rail can no longer collapse into a stack in any state. Core tests green (2335), web + mobile typecheck green, web build and iOS bundle green." },
  { id: "recovery-baseline-excludes-today", area: "Core", status: "shipped", title: "The recovery baseline stops containing today", detail: "The third defect from the wearable-term trace, and the one held back for an explicit product decision. `metric()` compared today's reading against `rollingBaseline`, which takes the most recent 14 readings INCLUDING today \u2014 so the value being judged was part of the normal it was judged against. Self-referential, and the damping runs the wrong way: the more unusual today is, the harder it drags the mean toward itself, so it always reads as LESS unusual than it was. Measured, against an HRV baseline of 50 with a reading of 60: one prior reading scored +4 where the honest figure is +8 (the signal exactly HALVED), two priors +5, three +6, six +7, converging only after roughly a fortnight of history. It distorted hardest precisely when an athlete is newest. It also meant the two entry points disagreed \u2014 the legacy `buildBiometrics` always excluded today \u2014 so identical readings produced different adjustments depending on which table they lived in. NEW: `priorBaseline()` (engines/signals.ts) drops the newest reading and averages the next BIOMETRIC_BASELINE_WINDOW (14), and is what the recovery adjustment now uses on BOTH paths; the legacy path gained the same window cap, which it never had. `rollingBaseline` keeps its meaning ('what has this metric been doing lately') and its doc now says plainly that it is the wrong function for 'is today unusual for me'. THRESHOLD, chosen by the product owner from three costed options: ONE prior reading is enough \u2014 the term counts from the second reading onward rather than waiting for three, so the definition is correct from the earliest possible moment; with no prior at all today is its own baseline, giving a deviation of exactly 0, so a first-ever reading can never move a score it has nothing to be compared against. Six new tests: today never drags its own baseline, the full signal off a single prior, zero adjustment on the first reading, the window bounding out older readings, a drop still reading as a drop, and \u2014 the point of the change \u2014 the two paths returning the identical baseline and the identical adjustment for the same history. Core 2362 green, web + mobile typecheck green, web build and iOS bundle green." },
  { id: "wearable-term-provenance", area: "Core", status: "shipped", title: "The \u00b115 wearable term \u2014 a recency window, and a door", detail: "Reported by an athlete who does not sleep with a device and was being told 'Includes \u22123 from your wearable'. Tracing it found the term had no guards and no explanation. THE BUG: there was NO RECENCY CHECK ANYWHERE in the path. `latest()` returns the newest row of a kind \u2014 ever, with no date filter \u2014 /api/signals returns 500 rows unfiltered, and `toBiometrics` handed that row to the readiness and HPI engines as TODAY's value. One sync months ago therefore pinned a permanent \u00b1N onto the score, printed in the present tense, forever, with a stale baseline freezing the deviation at whatever it was on that last day. engines/signals.ts now exports BIOMETRIC_FRESH_DAYS (7) and signalAgeDays(); a reading past the window (or with an unparseable timestamp \u2014 unknown age is never presented as current) stops counting as today, and when all three are stale `toBiometrics` returns undefined so the term drops to 0 and the line disappears rather than asserting another week's measurement. A SECOND, SEPARATE FABRICATION: the legacy `buildBiometrics` (the Biometric table, which web falls back to when no Signals exist) substituted HARDCODED values for any field the latest entry lacked \u2014 HRV 60ms, resting HR 55bpm, sleep 7.5h \u2014 then compared those invented numbers against the athlete's REAL baseline. An athlete whose true resting HR is 50, on a row that simply had no resting-HR field, was handed an invented 55, a fabricated +10% deviation and \u22124 points of 'recovery' no measurement supports. It now neutralises a missing field exactly as the Signal path always did (today === baseline \u2192 contributes 0), applies the same freshness window, and returns null when the latest entry is stale or carries no usable field. BiometricMetric gained `source`, `ts` and `measured` so provenance survives into the UI. THE DOOR: engines/wearable-explain.ts \u2014 wearableExplain(bio, now?) calls biometricAdjustment (the SAME call the card makes) and returns a row per metric with today's reading, the athlete's own rolling baseline, the signed deviation, the weight (40/40/25), the signed points, the resolved SOURCE LABEL and the reading's age in days, plus the unrounded sum, whether the \u00b115 bound bit, and the distinct sources. The arithmetic was already there \u2014 biometricDeviations() has always been exposed to the ADMIN Engine Room and never to the athlete whose body it describes. `raw` is deliberately full-precision: rounding it for display and again for the total is how a ledger ends up printing '\u221210.5 \u2192 \u221211'. THE CARD LINE NOW NAMES ITS SOURCE: it asserted 'your wearable' whatever wrote the reading, so a hand-typed value was credited to a device the athlete may not own. wearableSourcePhrase() resolves a connector id to its real label ('Apple Watch / Health', 'WHOOP'), a non-connector source to a translated 'your own entries', and two sources at once to 'your recovery signals' \u2014 and the line is a button opening the sheet, with the same \u24d8 affordance as the two freshness columns. New i18n EN/PL/DE (w.home.wearable.*, and w.home.cockpit.wearableOf now takes {source}). 21 new tests: the window's boundary in both directions, one stale metric neutralised without discarding the fresh ones, an unparseable timestamp treated as stale, a months-old sync producing recovery 0, the legacy path's fabrication replaced by a neutral, the clamp, mixed sources, and the total always equalling biometricAdjustment. Core 2356 green, web + mobile typecheck green, web build and iOS bundle green." },
  { id: "design-ratchet-green", area: "Mobile", status: "shipped", title: "The design-system ratchets go green again", detail: "`main` had been failing CI for four consecutive runs since 2026-08-04 on two of apps/mobile/lib/design-tokens.test.ts's own guards, and two PRs were merged through the red. Both are fixed at the source rather than by moving the guard. (1) THE 10dp LEGIBILITY FLOOR is a HARD rule \u2014 `fontSize` below fs.nano is not allowed, because the mono + uppercase + letter-spaced kicker is the least legible combination in the app. The endurance lanes' order control drew its `\u2304` chevron at 9. Raised to fs.nano on mobile AND on web: the type scale is shared (packages/core/src/scale.ts) and a legibility floor is a product rule, not a platform one \u2014 web carries no equivalent test, so its two sites (the shared `kicker` style and the chevron) would have drifted silently. The local `fontSize: 10` override that raised the kicker at one call site is redundant now and is gone. (2) THE CHIP RATCHET counted 9 chip-shaped component declarations against a ceiling of 8. The newcomer was `TodayHubPills`, and it is NOT a chip: it is the docked tab row, and the regex matches on the NAME. Neither the ceiling nor the pattern moved \u2014 the component was RENAMED to `TodayHubDock`, which is what the rest of the codebase already calls it: the capability id is `today-hub-dock`, the hook is `useHubDock`/`hubDockVisible`, and the mobile call site already read `const hubDock = <TodayHubPills \u2026>`. The file moved with it (today-hub-pills.tsx \u2192 today-hub-dock.tsx) on both clients, so nothing is left named for the thing it is not. `APill` (a button \u2014 its own comment calls it the Button union) and `GlassPillRow` (a row) are the same class of false positive and are left alone; only the one over the ceiling needed to move, and renaming it was accurate rather than evasive. Mobile suite 41 green, core 2367 green, both typechecks clean, web build and iOS bundle green." },
  { id: "freshness-explainer", area: "Core", status: "shipped", title: "Strength fresh / Endurance fresh \u2014 a sheet that explains the figure", detail: "The Performance tab's YOUR STATE card printed its two pillar figures as bare numerals under two 9px mono labels \u2014 '62 / STRENGTH FRESH' \u2014 with no door at all: no derivation, no inputs, no statement of what the number refuses to claim. That is the exact condition the readiness block was rebuilt out of (readiness-face-door, readiness-deficit-ring): a figure an athlete cannot audit is a figure they stop trusting the first day it says something inconvenient. Both columns are now DOORS onto a sheet. THE ENGINE: engines/freshness-explain.ts \u2014 freshnessExplain(pillar, log, bio?, weights?) CALLS computeHpi and reads components[pillar] rather than re-deriving it, so the sheet and the column are one value read twice; the ledger's last step IS that value; and score + fatigue is exactly 100 for every log (26 tests). It returns the measured inputs itemised \u2014 the seven tissues heaviest-first for strength, the three energy systems in fixed intensity order for endurance \u2014 each row carrying its share of the pillar's load AND the semantic role it is painted from, following readiness-deficit's ONE PAINT rule so neither client derives a colour (tissue = danger, conditioning = info, the readiness ledger's own vocabulary). TWO HONESTY FLAGS the card could not previously distinguish: `empty` (nothing logged \u2014 a resting baseline, not a measurement) and `noInput` (the log is real but nothing in it feeds THIS pillar \u2014 the everyday case is a pure lifting log, which names no energy system, so 'Endurance fresh' reads 100; that 100 is an absence of input, not an all-clear). Note what noInput is NOT: a running log does not trip the STRENGTH flag, because the catalog attributes Easy Run to quads, so a runner's tissue rows are genuinely measured. The sheet prints the reason instead of listing seven zeros. THE ARITHMETIC is returned as steps so the two clients render identical maths: strength is 'average across the 7 tissues \u2192 100 \u2212 that'; endurance is 'total decayed load \u2192 through the saturating curve (scale 90) \u2192 100 \u2212 that'. THREE ENGINE CONSTANTS were exported so the copy reads off the code rather than restating it: FATIGUE_HALF_LIFE_DAYS (the loop's own decay, previously the literal 2 with a comment beside it), FATIGUE_NORM_FLOOR (the 40 that stops one easy set reading as fully fatigued) and ENDURANCE_SCALE; hpi.ts's local band() is exported as hpiBand() so a pillar figure is banded and coloured by the SAME rule as the headline above it. THE SHEET, identical on both clients (aurora/freshness-sheet.tsx): the figure in its band colour with the roll-up sentence beside it ('this pillar is 55% of the Freshness figure above; the wearable's \u00b115 goes on that headline, never on this number'), WHAT IT IS, the athlete's own inputs as labelled bars, HOW IT'S CALCULATED with the decay and the normalisation stated, THE ARITHMETIC as a ledger ending on the figure at the top, and WHAT IT DOESN'T SAY \u2014 the caveat that freshness rises on a layoff and on an injury that stops you training, pointing at Capability further down the card as the half that measures whether anything is improving. THE AFFORDANCE: the whole column is the hit target (a 9px label beside a 13px glyph is not a phone tap target) with the `info` glyph riding the label row, the same 'explain THIS reading' vocabulary Today's readiness reading uses \u2014 unwrapped, since the glyph is already a ring. New i18n in EN/PL/DE (w.home.fresh.*: explain, sub, whatHead/howHead/ledgerHead/limitHead, rollup, baseline, decay, stepAverage/stepLoad/stepSaturate/stepResult, normFloor, loadNote, colFatigue/colLoad, system.*, and what/how/inputs/limit/noInput per pillar). Core tests green (2328), web + mobile typecheck green." },
  { id: "metric-explainers-remaining", area: "Core", status: "planned", title: "Explainer sheets \u2014 the remaining unexplained figures", detail: "freshness-explainer establishes the pattern (a core explain() that reads the same engine call the surface prints, returning itemised inputs + an auditable ledger + a 'what it does NOT say' caveat, rendered by one sheet per client). readiness-read-explainer is the second (the Recover cluster's readiness reading). The same treatment is owed to every other figure the app states without derivation \u2014 the headline Freshness score, the Capability percent, the injury-risk figure beyond its model-version disclosure, the velocity profile's e1RM and R\u00b2, and the volume landmark bands. Each is its own change: the value is the honest per-metric arithmetic, not a generic 'about this number' modal. RATCHETED (Aug 2026): the Tissue card's four whole-body load figures came off this list \u2014 see whole-body-load-rail." },
  { id: "readiness-read-explainer", area: "Core", status: "shipped", title: "Recover \u2014 the readiness reading explains itself (parity with Performance)", detail: "The Recover cluster's card leads with ONE word at display weight (\"Flat\"), and that word governs the day: it scales the working load of the next session (READINESS_LOAD_FACTOR \u2014 primed \u00d71.05, flat \u00d70.94, wrecked \u00d70.85 and a shed set), it decides whether a second read is wanted, and it is one half of the pair that measures this athlete's own clearance rate. Behind it sat ONE grey sentence, toggled in and out by an \u2139 in a bordered 18px circle \u2014 while every figure on the Performance tab already opened onto its measured inputs, its arithmetic and its caveat (freshness-explainer, wearable-explainer). The same figure treatment, and the same ICON treatment, now ship in Recover. THE ENGINE: readinessReadExplain() (@hybrid/core readiness-read-explain.ts) takes the feeling, the placed read, the day's reads and today's gate, and returns what the sheet lays out \u2014 the lag since training, the lag class, the spentness the answer maps onto, the residual the model still expects to be present at that lag, the recall weight, whether this read is the DECISIVE one, the day's pair verdict when two reads can support one, the gate's own reason, and the constants the copy names (never hard-coded in a string). THE LAW, tested: loadPct and setAdj are the SAME numbers prescribeSession applies \u2014 both read READINESS_LOAD_FACTOR and the wrecked set rule from one place, so the ledger in the sheet and the load the session is given are one value read twice. HONESTY: a reading with no timed read behind it (a legacy check-in row, or the optimistic tap before the refetch lands) prints NO lag and NO residual \u2014 the time since training classifies it and nothing more, because we know when the athlete last trained, not when they answered. THE ICON: the \u2139 is now the Performance affordance exactly \u2014 the glyph bare at 13 riding the row of the figure it explains, no second bordered ring around a glyph that IS a ring \u2014 and it shows whenever there is a reading rather than only on days the context note existed for. Twin sheets on both clients (aurora/readiness-sheet.tsx, mirroring freshness-sheet.tsx block for block: reading \u2192 what it is \u2192 what it's read against \u2192 what it moves \u2192 the pair \u2192 the next reading \u2192 what it doesn't say), 38 new strings in EN/PL/DE (w.home.read.*), 11 new tests." },
  { id: "whole-body-load-rail", area: "Mobile", status: "retired", retiredBecause: "Superseded by whole-body-load-verdict, in the same branch, after a design read that the rail answered the wrong question. It asked how to make four figures readable; the harder question was why there were four. Four \u2139 buttons in a row are four admissions that four figures failed to communicate, and the old grid's real fault was never that it was small \u2014 it was UNACCOMPANIED, four numerals doing the work of a sentence. The rail made the complexity legible instead of removing it. TO REVISIT IT: the rail would be the right shape again if the block ever carried figures that are genuinely INDEPENDENT of one another \u2014 the argument against it here is specifically that strain is literally load \u00d7 monotony and acute is one side of the ACWR ratio, so four tiles were one measurement wearing four hats. It would also come back if the verdict sentence proved untrustworthy on real logs, since the receipts-under-a-sentence layout only works while the sentence is right.", title: "Whole-body load \u2014 a rail of four figures, each with its own door", detail: "REPORTED AS \"we don't even know what these are\", and that was the literal state of it. The Tissue card's whole-body block was a FOUR-UP GRID sharing one card width \u2014 about 85dp per figure \u2014 carrying 0.94 ACWR, 2 546 S-RPE 7D, 1.8 MONOTONY and 4 554 STRAIN as bare numerals under truncated 10px mono labels, with ONE footnote under the whole row that explained only the FIRST of the four. So three of the four shipped with no units, no scale, no derivation and no stated limit: an athlete reading \"4554 STRAIN\" cannot tell whether that is good, bad or merely large, and there was no room in an 85dp column to tell them. TWO CHANGES, and the second is the one that mattered. (1) THE GRID IS A RAIL. Four tiles at 156dp on a snapping horizontal scroller, so each figure gets its full width back and can carry the three things a figure needs: its NAME, its VALUE at 26pt, and a plain-English READ of what that value means (\"Sweet spot\", \"Varied week\", \"Load \u00d7 monotony\"). The full-bleed slider rule does NOT apply \u2014 this rail sits inside a card, the rule's stated exception, so it respects the card's gutter instead of sliding under the screen edge. (2) EVERY TILE IS A DOOR, following the freshness-explainer pattern exactly. engines/load-explain.ts \u2014 loadExplain(metric, LoadState) takes the state the CARD was drawn from rather than the sessions, because an explainer that called computeLoad again could be handed a different `now` and would then narrate a figure that appears nowhere on screen. It returns the formatted value (identical to the tile's, by construction), the unit, the semantic role, the itemised inputs (the seven days behind a weekly total, the four weeks behind a ratio, each with its share of the block's largest row), a ledger whose last line IS the printed figure, the scale where one exists, and the caveat. THE TWO ARBITRARY-UNIT FIGURES GET NO INVENTED BANDS: a strain of 4 554 sits on no published ladder, so `bands` is empty and the role is `neutral` \u2014 drawing a ladder there would be the sheet making a claim the engine never made. ACWR's four stops are read off computeLoad's own banding (one place, or they drift); monotony's three are Foster's convention and are declared as NOT being in the engine, since nothing in the app branches on them. While the ratio is still building the tile says \"Still building\" and lights no stop rather than borrowing a neighbouring band's word. ENGINE ADDITIONS: LoadState now carries `daily` (the seven day-loads that SUM to `acute`), `dailyMean` and `dailySd` (the two figures monotony is the quotient of) \u2014 carried out of computeLoad so the ledger prints the engine's own numbers instead of re-deriving an SD by dividing two rounded fields. semantic.ts gains acwrRole(), which replaces a hand-rolled band\u2192colour map that lived privately in tissue-card.tsx; detraining reads `info`, not `caution`, because coming back off a quiet spell is a fact about the last four weeks and not something to fix today. PAINT: a figure that carries no verdict is drawn in CHALK, not in the neutral accent \u2014 `neutral` resolves to ash, and ash on a 26pt numeral reads as disabled rather than as unbanded. THE AFFORDANCE is the freshness/readiness one: the WHOLE tile is the hit target with the \u2139 riding the label row, drawn bare because the glyph is already a ring. THE SHEET (aurora/load-sheet.tsx) mirrors freshness-sheet.tsx block for block: the figure with its unit spelled out, WHAT IT IS, THE SCALE with your rung marked, WHAT WENT IN as labelled bars, HOW IT'S WORKED OUT as prose plus the ledger, and WHAT IT DOESN'T SAY \u2014 including that ACWR is a contested metric and that the AU figures mean something against your own weeks and nothing against anybody else's. w.injury.acwrNote is DELETED in all three languages: the footnote it was is now the ACWR sheet's own `what` + `limit`, said properly. ~60 new strings in EN/PL/DE, 12 new tests, mobile typecheck green, core suite green. Mobile only \u2014 the web tissue card left with the web client." },
  { id: "whole-body-load-verdict", area: "Mobile", status: "shipped", title: "Whole-body load \u2014 one sentence, four receipts, one door", detail: "THE SECOND PASS, and the one that shipped. whole-body-load-rail was the first: it took the four-up grid of bare numerals, made each figure 26pt on a 156dp snapping rail, and gave each its own \u2139 onto its own explainer sheet. Correct as far as it went, and it went the wrong way. FOUR EXPLAINER BUTTONS IN A ROW ARE FOUR ADMISSIONS that four figures failed to communicate; the old grid\u0027s fault was never that it was SMALL, it was that it was UNACCOMPANIED \u2014 four numerals doing the work of a sentence, with nothing above them saying what they meant together. And the four are not four things: `strain` is LITERALLY acute \u00d7 monotony and `acute` is one side of the ACWR ratio, so the block was one measurement printed four ways. WHAT SHIPPED. The block leads with a SENTENCE in the display face \u2014 \u0027You\u0027re training about as much as usual, but every day is starting to look the same\u0027 \u2014 and the four figures go back to being small underneath it, chromeless, as receipts you can check. They can be small precisely because they are no longer the ones explaining. This is the grammar the card ALREADY speaks: injuryHeadlineKey puts one display-face sentence over the risk axis for exactly the same reason, and the tissue rows above are bare type on the card, so the row of bordered tiles had made one card hold two treatments of the same kind of information. THE ENGINE: loadVerdict(LoadState) in engines/load-explain.ts returns a sentence key plus the two bands it was composed from and the heavier of their two roles, or NULL while the ratio is still building \u2014 the honest answer, not a hedge, since without ~2 weeks there is no \u0027what you normally do\u0027 to compare against and half the sentence would be invented. TWO CLAUSES, NOT FOUR: HOW MUCH (ACWR) and WHAT SHAPE (monotony). The other two are omitted from the sentence not for brevity but because they carry no independent claim. A FULL 4\u00d73 MATRIX OF WHOLE SENTENCES, not two fragments joined by a conjunction: Polish and German inflect the second clause against the first, and a `${a} ${b}` sentence is how an app ends up speaking English grammar in three languages. 12 keys \u00d7 3 languages, paid once, with a test that proves all twelve are reachable, none collide, and every one ends in a full stop (a joined fragment would otherwise slip through looking like copy). monotonyBand() is exported so the verdict and the explainer band the same number in one place. THE SHEET is now ONE door for the whole block, reorganised around THE CLAIM rather than the implementation: the sentence, then the TWO readings that composed it with their scales and your rung marked, then THE WEEK\u0027S SEVEN DAYS ONCE (previously itemised four times \u2014 every figure in the block is a function of them), then all four figures in full with their ledgers and caveats. ALSO: `s-RPE 7d` became `7-day load` \u2014 it was an ACRONYM FOR A THING rather than the thing\u0027s name, and it had exactly one consumer left. ACWR stays, because it IS the metric\u0027s name and an athlete who wants to look it up needs the real term. NOT YET SEEN ON A DEVICE: the sentence\u0027s two-line wrap at fs.subtitle, and whether four receipts fit an ~80dp column in DE/PL, both want a TestFlight build. Core suite green, mobile typecheck green, iOS bundle exports clean. TWO DOORS, NOT ONE (the follow-up, same branch). The first cut of this design put a single door on the block \u2014 the \u2139 opened the whole reading and the four receipts were inert type. That over-corrected. The rail's four \u2139 buttons were rightly criticised because they were the ONLY way in and each one admitted its tile had failed to communicate; a receipt under a sentence that already communicates is a different object \u2014 an affordance, not a confession. And a reader who taps '1.79 MONOTONY' has asked a narrower question than the one the combined sheet answers, so making them scroll past three other figures to reach it reproduces the very failure the combined sheet was built to avoid. Now: the eyebrow row, the \u2139 and the sentence are ONE target opening the whole reading; each receipt is its own target opening THAT figure alone. LoadSheet takes a `focus` prop \u2014 null for the claim-shaped sheet (sentence \u2192 the two readings that composed it \u2192 the week, once \u2192 all four figures), a metric for the five-part per-figure explainer (figure + unit \u2192 what it is \u2192 its scale where one exists \u2192 its OWN inputs \u2192 ledger \u2192 caveat). Both modes share every primitive, so a band ladder or a ledger cannot differ by which door was used. LoadExplain.inputsHeadKey \u2014 dead while the combined sheet was the only consumer, and nearly deleted \u2014 is live again: the focused mode heads its inputs with the figure's own (four weeks for a ratio, seven days for a weekly total). THE EXIT HOLDS THE PAIR: the sheet caches state and focus TOGETHER through the slide-down, because holding the state alone would flip the panel from one figure to all four on its way out. TOUCH: a receipt is a 13pt figure over a 10pt label, about 33dp of type \u2014 under the 44dp floor \u2014 so each carries 6dp of vertical padding and the row cancels it with a matching negative margin, buying touch height at no layout cost (CoachRail's bleed device). Its spoken label is the figure, its band word and the way in, since the layout gives a screen reader no cue that four figures in a row are four buttons." },
  { id: "readiness-face-door", area: "Core", status: "shipped", title: "Readiness — one line on the face, the derivation behind a door", detail: "The Performance tab's readiness block opened with FOUR sentences of prose — ~38 words (47 with the nudge pill) restating four figures the engine had already computed, with every number buried mid-sentence. The word count was the visible fault; the structural one was worse: because the figures lived inside sentences they MOVED from day to day (with no conditioning load the third line vanished and the wearable line jumped up), so an athlete opening it daily could never learn where to look — which is most of the value of a daily metric. THE BLOCK NOW LEADS WITH ONE LINE. `readinessVerdict(log, bio)` (engines/performance-state.ts) returns the face — a kind (`limiter` / `clear` / `empty`), an i18n KEY rather than a baked English sentence (this is the one line most athletes will ever read, so it speaks Polish and German too), the tissue to substitute, the deficit (100 minus the score — arithmetic, not attribution), and how many reasons sit behind the door. The four sentences move INTO the door, unedited: `readinessReasons()` is `readinessWhy()` minus its score line, which the ring beside it already draws at full size. Nothing left the product, only the default view. AGREEMENT IS STRUCTURAL, NOT EDITORIAL: the face and the derivation share one limiter threshold (LIMITER_FATIGUE), so the card can never name a limiter its own lines don't; and the door's count IS the length of the array it opens onto, so it can never promise three reasons and reveal two. Plurals are the engine's to own (`readinessReasonsKey` — Polish needs three forms), so the clients can't pick differently for the same number, and the door's own label is picked by the engine too (`doorKey`): 'Where the 33 went' is the honest question while something is missing, but at a clean 100 nothing went anywhere and the wearable can still have a line worth reading, so it asks 'How today was read' rather than pointing at a zero. On an empty log there is no door at all — no reasons sit behind it, and the face says so. Both clients ship it in this change (web `<button aria-expanded>`, mobile `Pressable` with `accessibilityState.expanded`, matching the tissue card's disclosure idiom). New copy in EN/PL/DE (w.home.readiness.verdictLimiter/verdictClear/verdictEmpty, door, reasonsOne/Few/Many). 13 new tests. Design study: design/readiness-why-seven-ideas.html (seven concepts) and design/readiness-ring-door-spec.html (the chosen composite). This is step one of that spec — the door, which needs no engine attribution; the deficit ring that replaces the drawn ledger is tracked as readiness-deficit-ring." },
  { id: "nutrition-training-join", area: "Core", status: "shipped", title: "The nutrition \u2192 training join (energy availability + protein in the engines)", detail: "THE COLUMN THAT WAS TYPED AND NEVER READ. The Signal ontology has carried energyIntake / protein / carbs since the nutrition engine shipped, and adaptiveTargets has estimated this athlete\u2019s MAINTENANCE from them for just as long \u2014 and nothing downstream read either one. The whole nutrition column computed targets, drew rings, and stopped, so an athlete four days into an aggressive cut scored exactly the readiness and exactly the volume ceilings of one eating at maintenance. The product\u2019s central claim is that it owns training, recovery AND nutrition in one model; that was true of the screens and false of the engines. This closes it. ENGINES/FUEL.TS (32 tests) is the join. energyBalance() takes the rolling mean of COMPLETED days\u2019 intake over FUEL_WINDOW_DAYS (14) and measures it against estimateMaintenance() fitted over FUEL_MAINTENANCE_DAYS (28); fuelAdjustment() ramps the shortfall into a readiness cost: severity = clamp((shortfall \u2212 FUEL_DEADBAND_PCT) / (FUEL_SATURATION_PCT \u2212 FUEL_DEADBAND_PCT), 0, 1), points = \u2212round(FUEL_PENALTY_MAX \u00d7 severity). NEVER POSITIVE: a deficit costs points, a surplus earns nothing, because \u2018ate over maintenance therefore more recovered\u2019 is a bonus for overfeeding and an athlete would learn to farm it inside a week. That makes it the EXACT MIRROR of the heat prior \u2014 heat needs no cost kind on the readiness ring because a credit takes no arc, and fuel needs one ALWAYS, because a cost that cannot be drawn is a cost the sum law catches. IT DOES NOT STAND DOWN FOR A WEARABLE, and the asymmetry with heat is deliberate rather than an oversight: heat and HRV are two accounts of ONE NIGHT, so a measurement beats a prior; energy availability and HRV are different quantities on different timescales \u2014 an athlete three weeks into a controlled cut can wake with a textbook HRV and still be unable to absorb the volume they absorbed in a surplus. FUEL_PENALTY_MAX is 6 against the wearable\u2019s \u00b115, so a self-report still cannot out-vote a measurement. THE GATES ARE THE HONEST PART, and each one is a specific failure it prevents. TODAY IS EXCLUDED \u2014 a day in progress is a day with dinner still ahead of it, so counting it would report a deficit every morning that resolves itself by bedtime, worst at the hour the athlete most often opens the app. FUEL_MIN_DAY_KCAL (800) treats a thin day as a GAP IN THE RECORD rather than a fast: logging breakfast and getting on with your life is 380 kcal against a 2,700 maintenance, an 86% deficit that never happened. FUEL_MIN_DAYS (5 of 14) makes a thin log ABSENT rather than small, because its failure mode is not noise but systematic understatement that would read as a crash diet. FUEL_DEADBAND_PCT (10%) is the UNDER-REPORTING allowance, not a tolerance for under-eating \u2014 self-reported intake runs 10\u201320% low in any free-living population, so a paper deficit inside it is more plausibly the logging than the athlete. KNOWN CIRCULARITY, stated rather than hidden: the maintenance estimate is itself partly fitted to logged intake (maintenance \u2248 avgIntake \u2212 \u0394kg\u00b77700/days) and the two windows overlap, which pulls maintenance TOWARD recent intake and SHRINKS the measured gap \u2014 so the term understates a deficit and can never overstate one, which is the right direction of error for something that takes points off an athlete. The windows are different lengths precisely so recent eating is read against a longer-run expenditure level rather than against itself, and that is also where this beats the scale: on day four of a cut the bodyweight trend is still water and the diary already knows. READINESS. computeReadiness gains a fourth term (fuelAdj, clamped \u2264 0 at the engine as well as at the source, so no caller can turn it into a credit); Readiness.fuelAdj joins bioAdj/heatAdj; readinessDeficit gains a `fuel` COST KIND between conditioning and the wearable \u2014 between what the athlete DID and what was READ about them, which is where it belongs, since it is the first cause on that ring from a different column of the app. The sum law is gated across every credit 0\u2013\u22126. readinessVerdict gains a `fuel` FACE (w.home.readiness.verdictFuel, \u2018You\u2019ve been eating under maintenance.\u2019) on the same MEANINGFUL_COST bar the other non-tissue causes clear \u2014 without it the ring would draw a fuel arc under a sentence naming a tissue, and the athlete would go on believing they trained too hard when they in fact ate too little. readinessFacts gains factFuel, and it takes the fuel read WHOLE rather than its points, unlike heatAdj: the arc already carries what the deficit cost, and the figure the line needs \u2014 how far under maintenance they have been eating \u2014 lives on the read. prescribeSession takes opts.fuelAdj, so the join moves prescribed load and not just a number on a card. performanceTrajectory applies it to daysAgo === 0 only, like bio and heat, because there is no stored history of what the window read a week ago. VOLUME CEILINGS. The old rule was \u2018the scale, always\u2019: the scale is the outcome and the food log is the estimate, so an athlete losing weight IS in a deficit whatever the diary says. That reasoning is correct and it was still the wrong rule, because the scale answers LATE and sometimes never \u2014 bodyweightTrend needs three weigh-ins spanning a fortnight, water masks the first fortnight of any cut, and an athlete who never steps on a scale gets no answer at all. So the LOG LEADS and the SCALE BACKS IT UP: measuredProfile prefers energyStateFromIntake and falls back to energyBalanceFromBodyweight, recording which in `nutritionBasis` \u2014 the two are different claims with different lags and \u2018measured\u2019 without \u2018measured how\u2019 cannot be argued with. The scale did not lose its argument, it lost its monopoly, and it still catches the athlete whose logging is fiction, because a log too thin to clear the gates IS the fallback firing. The intake bands are ASYMMETRIC (\u221210% deficit, +5% surplus) for the under-reporting reason: a logged surplus is more likely real than a logged deficit, so the two directions do not carry the same evidence. PROTEIN is a SECOND factor rather than a nudge to the first: energy decides whether there is fuel to repair with, protein decides whether there is material to repair FROM, and an athlete eating at maintenance on 0.9 g/kg is short of the second and fine on the first. PROTEIN_RECOVERY_TIERS \u2014 1.00 / 0.98 / 0.94 at \u22651.6, \u22651.2, below 1.2 g/kg \u2014 with NO BONUS TIER, because the dose-response flattens around 1.6 g/kg and paying a multiplier for 3 g/kg would be inventing an effect. It has only ONE path (there is no outcome measure: the scale cannot say how much of a kilogram was muscle), it is gated on its OWN sufficiency flag rather than on the energy read\u2019s (protein needs no maintenance estimate, so an athlete whose maintenance is unknown still has a readable protein average), and it is EXCLUDED FROM LANDMARK CONFIDENCE for exactly the reason heat is \u2014 every athlete eats protein, but only one who logs food has told us, and counting it would permanently cap everyone who doesn\u2019t log for declining a practice. Energy availability IS counted there, and that is not an inconsistency: it is answerable from the bodyweight trend alone, so an athlete who never opens the tracker can still supply it. sanitizeVolumeProfile deliberately does not accept proteinGPerKg, so it can never be persisted as typed. DATA PATH. Nutrition rows are what CROWD the unfiltered signal window \u2014 one logged food writes up to eight of them \u2014 so the stream that evicts everyone else is the one stream that cannot afford to be evicted by its own volume: fetchNutritionSignals / useNutritionSignalsQuery on mobile and a per-kind query in athleteInputs on the server, both asking for energyIntake + protein + bodyMass (bodyMass rides along because maintenance and the protein-per-kg figure both read it, and two windows could disagree). revalidate.recovery names the new cache entry, so logging dinner refreshes the readiness figure instead of leaving it computed as though the athlete hadn\u2019t eaten \u2014 the same defect the heat row shipped with. CLIENTS: Today (home.tsx) and the Performance readiness block compute fuelAdjustment ONCE and hand the identical read to prescribeSession, readinessDeficit, readinessVerdict and readinessFacts \u2014 four surfaces, one reading of the fortnight \u2014 with `today` as a memo dependency so an app left open across midnight stops excluding yesterday as \u2018today\u2019. useVolumeModel feeds the same rows to measuredProfile. Copy in EN/PL/DE (costFuel, factFuel, verdictFuel). ADMIN ENGINE ROOM: ENGINE_FORMULAS gains readiness-fuel and landmark-nutrition, the readiness expression interpolates + fuelAdj, the readiness group names engines/fuel.ts; deriveReadiness prints the rolling intake, the severity ramp, the adjustment AND the insufficiency line with its reason \u2014 a term that silently reads \u22120 is indistinguishable from one never computed, and here the distinction is the whole feature, because \u2018this athlete logs nothing\u2019 and \u2018this athlete eats at maintenance\u2019 are opposite findings that render identically on every athlete-facing surface; EngineTrace carries the whole FuelAdjustment; whatIfFuel() joins whatIfLog/whatIfBio/whatIfHeat with an \u2018Eating \u2014 % of maintenance\u2019 slider and an \u2018Aggressive cut (75% of maintenance)\u2019 scenario preset. THE WHAT-IF IS A PERCENTAGE RATHER THAN SYNTHESISED ROWS, unlike whatIfHeat, and the difference is forced by what the two terms are made of: a sauna is two numbers at one instant so faking one is honest, while a fortnight of eating is a hundred rows against a maintenance estimate fitted to them \u2014 fabricating that would simulate the ESTIMATOR, not the athlete. Everything on the panel the operator did not move stays the athlete\u2019s real read. New FuelPanel renders the substituted arithmetic and renders DIFFERENTLY for insufficient vs at-maintenance. sampleNutritionSignals() gives the sample athlete a real-shaped diary \u2014 two weeks at 2,900 then two at 2,100 against a falling scale \u2014 because a flat log at ANY level reads as maintenance by construction and fourteen identical days would have made the panel look broken. NOT APPLICABLE: the mobile admin console has no Engine Room section, so there is no two-sided parity to hold. STILL A JUDGEMENT CALL, not a finding: FUEL_PENALTY_MAX (6), the deadband, the saturation point and the three protein tiers are priors drawn from published work on other people, exactly as every heat constant is. They are exported constants the Engine Room renders live, so retuning any of them is one line \u2014 and the honest replacement is the same one heat is waiting for: this athlete\u2019s own measured response, once the data network has labelled enough of it." },
  { id: "readiness-conditioning", area: "Core", status: "shipped", title: "Readiness counts conditioning load (engine fix)", detail: "A MODELLING BUG, found by building the deficit ring on top of a sum law that refuses to draw a cause the score does not have. Readiness was `clamp(35..98, round(100 − 0.7 × avg(muscle fatigue) + bioAdj))` — tissue fatigue and the wearable, nothing else. Conditioning work doses `fatigue.systems`, never `fatigue.muscles`, so a week of hard running left the muscle average near zero and readiness near 98: an athlete could run themselves into the ground and the number that PRESCRIBES their training would not notice. HPI had always counted it (its endurance pillar); readiness, the number that actually moves load, did not. THE FIX: `readiness = clamp(READINESS_FLOOR..READINESS_CEILING, round(100 − MUSCLE_SLOPE × avgMuscleFatigue − ENDURANCE_SLOPE × enduranceFatigue + bioAdj))`, with ENDURANCE_SLOPE = 0.35 — deliberately HALF the tissue slope, because conditioning load clears faster and limits the next session less than local tissue damage. At saturation it can take 35 points; one hard threshold session (≈45) costs about 16; an athlete with no conditioning in their log scores exactly what they did before, so nothing changed for pure lifters. `enduranceFatigue` moved from hpi.ts to fatigue.ts (readiness cannot import hpi — hpi imports the wearable adjustment from readiness) and is re-exported, so every existing caller still resolves. THE CONSEQUENCES, all shipped in the same change: the deficit ring gains a third cause in the blue it uses everywhere else; the card's face can now say the engine is the limiter (`verdictEngine`) or that recovery is (`verdictRecovery`), chosen from the LARGEST arc the ring draws so the sentence and the ring can never name different causes; the drawer's conditioning line appears whenever the load has actually cost a point (CONDITIONING_VOICE = 10) instead of at a threshold of its own, and says that it counts. ADMIN, IN THE SAME CHANGE (the console is the engine's own witness): ENGINE_FORMULAS carries the real expression built from the live constants, plus a new `readiness-deficit` card documenting the attribution law; `deriveReadiness` gained the energy-system and both-terms steps — it had been printing `100 − 0.7 × avg` as the base while the live engine used two terms, a derivation that no longer reached its own result, which is worse than no derivation because it is trusted; `EngineTrace` carries the athlete's deficit split; and the Engine Room renders it as one bar with a visible SUM-LAW check beside it. Drift guards updated to read the exported constants rather than repeat the literals, so the console cannot silently fall behind the engine again. OPEN QUESTION, deliberately left open: 0.35 is a defensible first slope, not a fitted one — it should be validated against real outcomes once the data network has labelled enough of them." },
  { id: "readiness-deficit-ring", area: "Core", status: "shipped", title: "Readiness — the deficit ring (the number accounts for itself)", detail: "Steps two and three of design/readiness-ring-door-spec.html, on top of the shipped face-and-door. The readiness ring stopped gauging what is KEPT and now accounts for the whole 100: a run of ticks for the score, then one run per cause, each in its cause's own semantic colour. The explanation and the number are one object rather than a caption on one, and the drawer opens onto a LEDGER of the same points in the same order and colours (baseline 100, each cost, the total) above the sentences. THE SUM LAW is the gate: `readinessDeficit()` (engines/readiness-deficit.ts) returns `{ kept, deficit, costs, bioAdj, clamped }` where kept plus every cost equals EXACTLY 100, apportioned by largest remainder so whole points can't miss their own total. Nothing may fall off the edge — anything unattributable lands in a named cost. `readinessRingSegments/Ticks()` hand both clients ONE geometry (the way trajectoryPlot already does), enforcing a fixed drawing order (kept → tissue → wearable → ceiling, never re-sorted by value, or the card stops being learnable) and a minimum arc of one tick paid for by the longest run, so a −1 nudge is visible without being mistaken for a big one. 52 tests. TWO FINDINGS THE LAW FORCED OUT, both of which changed the design: (1) CONDITIONING DID NOT MOVE READINESS, AND NOW IT DOES. The ring tried to give conditioning an arc and the arithmetic said its cost was zero: readiness was `100 − 0.7 × avg(muscle fatigue) + wearable` and nothing else, while conditioning doses `fatigue.systems`, never `fatigue.muscles` — so a hard endurance week left the muscle average near zero and the score near its ceiling. An athlete could run themselves into the ground and the number that prescribes their training would not notice. That was a modelling bug the card surfaced, not a layout question, and it is fixed in the engine — see `readiness-conditioning`. Conditioning now takes a real arc, in the blue it has everywhere else. (2) PER-MUSCLE ARCS READ WORSE THAN ONE TISSUE ARC. Readiness averages all seven tissues, so a first cut split that term per muscle — and on any normal week the fatigue spreads, which made the ring's biggest arc an anonymous 'other tissue' LARGER than the tissue actually named (glutes 10, other 27, measured). The tissue term is now ONE cost named by whichever tissue carries most of it — the same tissue the card's face calls the limiter, so the ring and the sentence above it can't tell two different stories. A positive wearable nudge takes no arc: it doesn't cost anything, it makes the whole deficit smaller, so each cost is 'this cause's share of TODAY's deficit' — the only reading under which the parts sum to the whole. Both clients render it through the SAME tick ring they already used (web's local Ring + the mobile kit's, each gaining an optional `tickColors`), so the deficit arrives in the shape the athlete knows rather than as a new instrument. New copy in EN/PL/DE (w.home.readiness.baseline/total/costTissue/costWearable/costCeiling)." },
  { id: "readiness-ring-paint", area: "Core", status: "shipped", title: "Readiness — the ring's paint, and the block at full width", detail: "TWO FAULTS IN ONE BLOCK, reported off a screenshot at a score of 53 — 'that yellow thing is -3 but on the ring it is half the ring'. THE PAINT WAS LYING. The ledger was arithmetically sound (100 - 30 - 14 - 3 = 53, guaranteed by the sum law) and the geometry was right too — the wearable really did get 1 tick of 32. But the kept run wore `readinessRole(score)` and the wearable cost is fixed at `caution`, and at 40-59 BOTH RESOLVE TO SAND, so the ledger's 8px swatch — the only bridge between a row and an arc — pointed at the seventeen ticks the score had kept. The collision is structural, not a one-off: the kept band overlaps a cause in EVERY band but the top one (60-79 with conditioning's teal, under 40 with tissue's terracotta); only 80+ was honest, and only because no cause is chartreuse. The spec had anticipated hue collision and answered it with 'weight, not hue, says which part of the ring is yours' — sound for a shape you stare at, but the moment the LEDGER shipped, hue stopped being decoration and became a lookup key. THE FIX, chosen from four rendered options (design/readiness-ring-colour-and-layout.html — kept-in-chalk, kept-dimmed, deficit-only, two-rings, each shown across a teal/sand/terracotta day): the kept run KEEPS its band hue, so severity still reads before any number, and the WEIGHT separates them — kept draws at `KEPT_ARC_ALPHA` (0.3), every cause at full strength, so the causes sit in front of the run they were taken out of. CRUCIALLY THE RULE MOVED INTO CORE. Both clients derived the kept arc's colour inline and both derived the same collision; a `RingSegment` now carries the role it is drawn from (the BAND's role, not a neutral) plus a `dim` flag, and one `segPaint()` per client is the only place a colour is resolved — ring, proportional bar and ledger swatch all read it. THE LAYOUT, all of it at parity: the ring + kicker + face are ONE masthead row; the check-in nudge gets its own line (inside the old right-hand column it had ~24 characters and broke mid-sentence); and the door, a new PROPORTIONAL BAR and the ledger all span the full card with values flush right — the breakdown used to be trapped in a column starting a third of the way across, so its numbers never reached the card edge and its rows sat narrower than the prose beneath them. The bar reads the SAME segments the arcs do (it cannot disagree with them, and it inherits the minimum-arc rule that keeps a -3 visible), and the ledger's total row now carries the kept swatch so every colour on the ring is named by a row. THE PROSE IS GONE. Three sentences restated the three rows above them, in English-only prose. What they carried that the rows CAN'T is now `readinessFacts()` — i18n keys plus figures for the limiting tissue's own fatigue (the row says the term cost 30; only this says the tissue reads 81/100), the energy-system load, and the wearable's signed nudge. That last is the case that matters: a POSITIVE nudge takes no arc and no row (it shrinks every other share instead), so without it a wearable that was read and did move the score would show nothing anywhere on the card. CONSEQUENCE THE CUT FORCED: the door's count was `readinessReasons().length` — the sentences — and now counts the ledger's rows, because that is what opening it reveals. Both were 3 on a typical day, which is exactly how a door promising three of something no longer there ships unnoticed; the old drift test pinned the count to the prose and failed, which is how it should go. New copy in EN/PL/DE (barKept/barSpent/provFrom/factTissue/factLoad/factWearable). 11 new tests: the paint law sweeps every score 1-98 and asserts no two runs of one ring ever share a role-and-weight pair — and inside 40-59 asserts that the kept run and the wearable's DO share a role, which is precisely why the hold-back is not optional. OPEN, deliberately not fixed here: in KYOTO HOUR the cost hues themselves crowd — sand darkens to #875427 and terracotta to #a3442f, a brown and a vermilion that are closer to each other than their dark-theme twins. That is a collision between two CAUSES, which no choice of kept-arc paint addresses; tracked as readiness-light-cost-hues (now fixed — see that entry). ALSO FIXED HERE, a latent geometry bug the new tests' own fixtures surfaced: the kept-starvation guard in readinessRingSegments lifted a starved kept run to one tick WITHOUT PAYING for it, and the coverage correction then took the extra tick off the LAST run — the smallest. At a kept of 1 that drove the wearable's run to count 0 and `from` 32, past the end of a 32-tick ring: a legend row pointing at an arc that is not drawn, which is precisely the lie the minimum-arc rule exists to prevent. The guard now pays from the LONGEST run (the same idiom the minimum-arc rule uses), the coverage correction can never take a run below its one guaranteed tick and re-seats every `from` after the run it moved, and a regression test sweeps kept 1-3 across 12/24/32/60 ticks. Not reachable through readinessDeficit today — READINESS_FLOOR clamps the score at 35 — but readinessRingSegments is exported and takes any deficit, and the floor is a tunable constant." },
  { id: "accent-text-channel", area: "Core", status: "shipped", title: "Accent colours are drawn in the TEXT channel, app-wide", detail: "THE SWEEP the readiness fix uncovered. Every brand accent exists twice — a FILL (--color-red), tuned to sit UNDER something, and a TEXT tone (--red-text), tuned to be legible on the card in whichever theme is showing. lib/ui.tsx has mapped between them since the light theme shipped, but only for the RAW HEX constants (txt(RED)). The Aurora screens use a different idiom — C('red') → var(--color-red) — which that mapping never saw, so 78 call sites across 32 files painted GLYPHS in the fill. Measured against each theme's own card: light amber #d0cd94 is 1.57:1 (sand on washi paper — near-invisible), light violet 2.83:1, light red 3.26:1 (every role=alert error message on the light theme), dark blue 3.59:1. The type scale here runs 10-14px, so WCAG's large-text exemption covers exactly NONE of them; lime was the only accent that passed on both grounds. FIXED: lib/ui.tsx gains `accentText(name)` beside `roleText(role)` — same channel, for the many call sites that name an accent directly rather than deriving one from a semantic role (an error is red because it is an error, not because something computed `danger`). 75 sites repointed across 32 files; mobile's three raw `color: C.amber` error texts (device-import, device-match, session-edit) went through txt() for parity. THREE FALSE POSITIVES were caught and are the interesting part: in both builders, a `color` property on a data object was the bar-segment BACKGROUND, with a sibling `textColor` for the label. The property was renamed `fill` on both clients rather than exempted — a value used as a background should not be called `color` in a codebase where that word now means the text channel. GUARDED by apps/web/__tests__/accent-text-channel.test.ts: no `color:` in components/, app/ or lib/ may resolve to an accent fill, in either the C('x') or the var(--color-x) spelling, with doc-comment lines skipped so the rule can quote the pattern it forbids. Backgrounds, borders, bar segments and body-map areas still take C(...)/roleVar — the test only ever looks at the `color` property. A reviewer cannot hold this distinction across 32 files; the test can." },
  { id: "readiness-light-cost-hues", area: "Core", status: "shipped", title: "Kyoto Hour — the cost hues, and the colour a legend is actually drawn in", detail: "FOUND while fixing the ring's kept-arc collision (readiness-ring-paint) and fixed here, along with a bigger fault it uncovered. (1) THE COST HUES CROWDED. On dark the three costs are terracotta / teal / sand, plainly three marks. In Kyoto Hour the same roles darkened to #a3442f vermilion and #875427 brown — ΔE 13.2 apart, both comfortably AA against the paper, and still the same warm mark at the ledger's 8px swatch. The light `amber` accent-text is now a deep OCHRE #885c00: ΔE 22.1 from vermilion and 22.5 from sage, in line with the dark set's own floor (its worst cost pair is 28.4). Mirrored across THEMES.light.accentText.amber and globals.css --amber-text. (2) THE REAL FAULT WAS THE CHANNEL. Mobile has had `txt()` since the palette split — it maps an accent FILL to the AA-guarded text tone for the current theme — and WEB HAD NO EQUIVALENT. `roleVar()` returns the fill, which is tuned to sit UNDER something, and Kyoto Hour deliberately leaves --color-amber as the pale sand #d0cd94. Used as a text colour on washi paper that is 1.57:1 — roughly a quarter of AA. Every caution-band figure on the light theme was printed in near-invisible ink: the readiness ledger's wearable row and its −3, an elevated tissue's driver label, and the HPI headline at 46px (a compromised athlete's own score). Even on DARK the fill was the wrong channel — the teal fill #3c787e as text on the card is 3.2:1, under AA, which is exactly why accentText lifts it to #6cb6bd. lib/ui.tsx now exports `roleText()` (web's twin of mobile's txt(), ash passed through since the muted token is already a text colour) plus `tint()`, and the rule is stated where both live: a background, a bar or a body-map area takes `roleVar`; glyphs, a legend swatch or a ring's ticks take `roleText`. Repointed: the readiness ledger (swatch + value + ring ticks + the new bar), the HPI headline on both the full card and the Teaser, the tissue card's driver label, Today's readiness ring, and the admin Engine Room's DeficitBar — which also gets the kept run's hold-back, since its own comment claims it mirrors the athlete's ring and it was reproducing the exact collision the card had. GUARDED: contrast.ts gains `labOf()` + `deltaE2000()` (CIEDE2000) and DISTINCT_ROLE_DE, and palette.test.ts now measures the cost roles against EACH OTHER per theme, not just against the ground — the half of accessibility contrast cannot express, and the half that was missing when this shipped. Verified the guard bites: reinstating #875427 fails `light: danger vs caution`. The readiness ring's kept arc is deliberately exempt — it wears its band's hue, which DOES collide, and is separated by weight instead (KEPT_ARC_ALPHA), which is the whole design of that ring. NOTE for a future pass: in light, pine (#3c4f43, the kept arc at 80+) and sage (#4f5c3a, conditioning) are ΔE 9.6 — they co-occur on the ring but only ever as dimmed-vs-full, so the weight rule already separates them; if the kept arc is ever drawn at full strength again, that pair has to be retuned first." },
  { id: "heat-recovery", area: "Core", status: "shipped", title: "Sauna / heat exposure as a recovery input", detail: "BUILT. Design write-up in audit/11-sauna-heat-recovery-design-2026-08.md, rendered as design/sauna-heat-recovery-spec.html. NO DEVICE WILL EVER MEASURE THIS \u2014 a watch does not know you sat in a sauna and nothing in the room reports to us \u2014 so the entire input is two numbers the athlete TYPES: how long, and how hot. That fact decides the architecture. THE ENGINE (engines/heat.ts, 30 tests). heatIntensity(T) = clamp((T \u2212 HEAT_FLOOR_C) / (HEAT_REF_C \u2212 HEAT_FLOOR_C), 0, HEAT_INTENSITY_MAX): a 45 \u00b0C floor so a hot shower earns nothing, an 80 \u00b0C reference where an equivalent minute IS a minute, a 1.6\u00d7 clamp reached at 101 \u00b0C. Duration alone is not the dose \u2014 22 min at 90 \u00b0C is +2 and the SAME 22 min at 55 \u00b0C is +1, and that gap is the entire reason temperature is collected. equivMin \u2192 dose = HEAT_CREDIT_MAX \u00d7 (1 \u2212 e^(\u2212equivMin / HEAT_TAU_MIN)) \u00d7 decay 0.5^(hoursSince / HEAT_HALF_LIFE_H) over a 48 h window. THE SUPPRESSION RULE IS THE DESIGN: biometricAdjustment ALREADY credits improved sleep and parasympathetic tone at \u00b115 from the athlete's own baseline, so a flat bonus on top counts one night twice \u2014 and on a bad dose it credits the sauna while the wearable correctly debits it. Heat is a PRIOR, the wearable is a MEASUREMENT, and with any fresh reading the credit is exactly 0 with `suppressed: true` so the surface STATES the rule rather than silently rounding to nothing. Heat is the one recovery input that will never graduate out of the self-report tier, which is why this is permanent rather than a stopgap. NEVER NEGATIVE by construction, so readinessDeficit needs no cost kind for it \u2014 a positive credit takes no arc exactly as a positive bioAdj takes none, and the sum law is gated by tests across every credit 0\u20133. STORAGE, NO MIGRATION: two Signals per sitting (`sauna` minutes + `saunaTemp` \u00b0C) sharing an EXACT ts, regrouped by heatSittings() on (ts, source) with both ids so a delete removes the pair \u2014 the same idiom derivedFoodEntries already uses for the eight rows one logged food writes. Signal.kind is a String column and /api/signals allow-lists from SIGNAL_KINDS, so nothing needed a schema change. A sitting with no temperature row is read at the reference and MARKED assumed rather than presented as measured. HEAT_TEMP_BOUNDS (40\u2013120 \u00b0C) and HEAT_MINUTES_BOUNDS (1\u2013180) are REJECTED at the API rather than clamped \u2014 900 is a typo and a typo should bounce. units.ts gains TempUnit with \u00b0C canonical and the display unit derived from the weight unit (lb \u2192 \u00b0F), mirroring heightUnitFor. THE CHRONIC CHANNEL, the better-evidenced half: HEAT_RECOVERY_TIERS in landmark-profile.ts \u2014 1.00 / 1.02 / 1.04 at <2, 2\u20133, 4+ sittings a week, derived from the signals by heatWeeklyFrequency() and never asked, with a HEAT_SESSION_MIN_EQUIV floor of 10 equivalent minutes so a token five minutes in a cool cabin is not a fourth session. Counted in SITTINGS because sessions-per-week is what the cohort evidence is expressed in. Capped at 4% because the adaptation is documented and its transfer to weekly-set tolerance is inferred. It does NOT stand down for a wearable \u2014 four weeks has no single night for two terms to argue about. NOTE: personalizeLandmarks' confidence divisor moved 9 \u2192 10 because `heat` is an eighth supplied field; a fully-supplied profile still reads 1.0. ADMIN ENGINE ROOM, all nine touch points: ENGINE_FORMULAS gains heat-intensity / readiness-heat / landmark-heat and the readiness expression itself now interpolates + heatAdj; the readiness formula group names engines/heat.ts as a source; deriveReadiness() prints the sittings, the intensity conversion, the dose, the decay AND the suppression line with the wearable's own timestamp \u2014 a term that silently reads +0 is indistinguishable from one never computed, and the rule is the whole design; computeEngineTrace() takes heatSignals + now and carries the whole HeatAdjustment on the trace; whatIfHeat() joins whatIfLog/whatIfBio with three sliders (minutes, \u00b0C showing the live intensity multiplier, hours ago) and a 'No wearable, sauna last night' scenario preset \u2014 which has to be wearable-free or the panel correctly reads +0 and the slider looks broken; readinessFacts() gains factHeat for the same reason factWearable exists; performanceTrajectory applies it to the daysAgo===0 point only, like bio; prescription.ts takes opts.heatAdj. New HeatPanel renders the substituted arithmetic (equivalent minutes, dose, hours since, decay, product) and renders DIFFERENTLY for suppressed vs absent. athleteInputs() queries the two kinds ON THEIR OWN rather than slicing the unfiltered 200-row window \u2014 a recovery input must not be evictable by the food log (see signals-read-window-crowding). MOBILE (aurora/heat-sheet.tsx). The first cut was built out of SYSTEM CONTROLS called directly \u2014 GlassSegment for both preset rows, two NativeSteppers, NativeDateField \u2014 each returning null off-iOS behind a hand-drawn floor. That is no longer what ships: see heat-sheet-redesign, which rebuilt it on the kit after three of those leaves turned out to lay out wrong on the device. It shows the EQUIVALENT MINUTES live as you type, so the model is met as a claim you can argue with rather than as a result. Saving below the floor is blocked with a stated reason instead of storing something that would score nothing. aurora/heat-row.tsx sits in Today's Recover cluster beside the check-in and RtpPanel, showing the week's sittings and equivalent minutes (the chronic channel's own input, visible without opening anything) behind a bare \uff0b \u2014 it grows the log in place, so no ring. Copy in EN/PL/DE. REVIEW PASS, and it caught five real defects worth recording. (1) THE CHRONIC CHANNEL WAS NOT CONNECTED \u2014 HEAT_RECOVERY_TIERS existed and nothing ever populated AthleteVolumeProfile.heat, so the multiplier was dead code. It is now a MEASURED field (measuredProfile / withMeasured / measuredFields in landmark-context.ts) fed by a per-kind useHeatSignalsQuery, which is where it always belonged: there is no form for it and there is not going to be one. sanitizeVolumeProfile deliberately does NOT list it, so it can never be persisted as typed. (2) THE ACUTE CREDIT REACHED THE ADMIN CONSOLE AND NOT THE ATHLETE \u2014 readinessFacts gained factHeat but no client passed a heatAdj, so the athlete's own ring, ledger and prescription still scored as if the sauna had not happened. Today (home.tsx) and the Performance readiness block now compute heatAdjustment ONCE against the same `bio` the rest of the block reads and hand the identical number to prescribeSession, readinessDeficit and readinessFacts \u2014 four surfaces, one reading of the day. (3) LANDMARK CONFIDENCE WAS TAXED BY AN OPTIONAL PRACTICE: adding heat to the `supplied` list moved the divisor 9 \u2192 10 and silently dropped every existing fully-supplied athlete from 1.0 to 0.9, and permanently capped anyone who has never sat in a sauna. Heat is excluded from confidence entirely now \u2014 it moves the multiplier, it does not judge how well we know them \u2014 with the divisor reverted and a regression test on both halves. (4) SAVING A SITTING DID NOT REFRESH THE SCORE: HeatRow held a private fetch, so the row updated and the readiness figure behind it did not. It reads the shared cache entry and revalidate.heat invalidates it. (5) TWO SHEET BUGS: stepping off a preset left the segmented control highlighting a value the athlete had not chosen (it now passes the real value, so SwiftUI shows no selection when nothing matches \u2014 which is the truth), and `when` was captured once at mount, so a sheet opened in the evening offered to record the sitting at whenever Today first rendered \u2014 stale on the one field the decay and the phase-4 matching both read. Also added: WhatIf.noWearable, because the three biometric overrides can only RETUNE a reading and never remove one, so on an athlete who owns a watch there was no way to see the prior fire at all \u2014 and the 'No wearable, sauna last night' preset was therefore promising something it could not do. THREE MODALITIES, THREE RAMPS (Aug 2026, on the answer to the open question below). Air temperature means something DIFFERENT in each, and ONE ramp calibrated on dry sauna scored the other two at roughly nothing \u2014 45 \u00b0C is exactly the dry FLOOR, so a steam room was worth exactly zero. HEAT_PROTOCOLS now gives each modality its own floor and reference, every pair answering the same question: what air temperature, in THIS modality, loads an athlete the way 80 \u00b0C of dry sauna does? SAUNA 45\u219280 \u00b0C (the anchor \u2014 dry air evaporates sweat freely, which is precisely why it has to be that hot); STEAM 35\u219248 \u00b0C (~100% humidity blocks evaporative cooling, so far less air temperature does the same work \u2014 45 \u00b0C goes 0.00 \u2192 0.77); INFRARED 35\u219260 \u00b0C (radiant panels heat tissue directly, so the air thermometer under-reads by construction \u2014 55 \u00b0C goes 0.29 \u2192 0.80). The protocol rides in Signal.source, so STILL NO MIGRATION and no backfill: 'manual' alone is a dry sauna, which is exactly what every row written before this looks like, and provenance stays the PREFIX ('manual:steam') so a reader after a row's origin need not learn a protocol vocabulary. A sitting with no temperature is read at ITS OWN protocol's reference \u2014 assuming 80 \u00b0C for a steam room would be assuming a room nobody has. STILL A CALIBRATION, NOT A MEASUREMENT, and the narrowed caveat is worth keeping: a steam room at 60% humidity and one at 100% are different doses and this cannot tell them apart. The API keeps ONE flat 40\u2013120 \u00b0C plausibility gate across all three \u2014 a typo guard, not a claim about modalities, since the per-protocol clamp already stops an implausible entry earning an implausible dose. The mobile sheet asks WHICH KIND first because it decides what the temperature below it MEANS, and switching modality re-seats the temperature at that modality's reference rather than carrying a stale one across. (The per-modality temperature PRESETS \u2014 sauna 70/80/90/100, steam 40/45/50, infrared 45/55/60 \u2014 went with heat-sheet-redesign: a continuous value does not want a preset rail beside a stepper. The per-modality REFERENCE they were derived from is still what the sheet seats on and what its derivation line quotes.) The Engine Room's intensity card renders all three ramps from the live table and the what-if gained a protocol picker. 54 heat tests. SETTLED: all three modalities are in use, so the protocol tag shipped; 40\u2013120 \u00b0C is the accepted input range; HEAT_CREDIT_MAX stays at 3 and the MRV ceiling at 1.04 \u2014 deliberately timid, and heat-clearance replaces the first with the athlete's own measured delta rather than a bigger guess. STILL A JUDGEMENT CALL, not a finding: the three floor/reference pairs. They are exported constants the Engine Room renders live, so retuning is one line. NOT BUILT, tracked separately: nothing \u2014 phases 1\u20134 and the post-session Wrapped entry all shipped. NOT APPLICABLE: the mobile admin console has no Engine Room section at all, so there is no two-sided parity to hold here. OUT-OF-ORDER LOGGING (Aug 2026). Nothing in this app is logged in the order it happened: a watch recording is imported whenever the phone next syncs — the 13:00 workout read at 21:00 — while the sauna after it was typed the moment it ended. The ENGINES always survived that, because every figure here reads event time (Signal.ts, Session.completedAt) and never write time. The ENTRY POINTS did not, in two ways, and both are fixed. (1) THE DUPLICATE. The Wrapped's 'finish in the sauna?' prompt was an unconditional invitation, and the sheet behind it opens defaulted to the session's own end — so an athlete who logged the sitting at 14:00 and imported the workout at 21:00 was invited to write a SECOND sitting an hour off the first, which heatAdjustment sums inside one 48 h window and heatWeeklyFrequency counts as two sittings toward the MRV tier, with nothing on any screen saying so. Both entry points now ask the record first, through one primitive rather than each inventing a window: heatSittingsBetween(), with heatAlreadyLogged() (HEAT_DUPLICATE_MIN, 90 min) behind the sheet's save and heatAfterSession() (HEAT_EDGE_GRACE_MIN before the end, HEAT_AFTER_SESSION_H after) behind the prompt. The prompt now REPORTS what is already logged instead of asking as though nothing were; the sheet WARNS and names the clashing sitting, opens the recent list so it can be deleted, and lets a second press through — an athlete who really sat twice is not wrong. (2) THE MIS-BUCKETED PAIR, and this one was worse than a lost sample. pairReads tagged heat with `t > end`, but the sitting's instant is TYPED and the session end is MEASURED by a watch that gets stopped whenever the athlete remembers, so a sauna taken straight after training routinely lands minutes BEFORE the end it followed — and that pair did not drop, it went into the WITHOUT-heat bucket, putting the treatment inside the control and biasing saunaClearance's delta toward 'heat does nothing'. The window now opens HEAT_EDGE_GRACE_MIN (30 min) before the recorded end, CLAMPED TO THE SESSION'S OWN START — on an hour-long session the grace is comfortably mid-session and absorbs only the artefact it is for, but on a 15-minute run it would otherwise reach back past the start and relabel a PRE-workout sauna as post-session heat, which is a different sitting rather than a clock error. 13 new tests, copy in EN/PL/DE. KNOWN LIMIT, stated rather than papered over: the duplicate guard reads the shared heat cache, so a save made before that query has ever resolved is unchecked — both entry points mount the query on the screen that reaches them, so the window is narrow, and a duplicate that does get through is still deletable from the sheet's recent list." },
  { id: "heat-sheet-redesign", area: "Mobile", status: "shipped", title: "Heat \u2014 the log sheet rebuilt on the kit, then reduced", detail: "THE SHEET WORKED AND DID NOT LOOK LIKE THE APP. Design write-up rendered as an artifact (three live states: the shipped build, a kit-consistent rebuild, and the reduction pass that shipped). It was the one surface built almost entirely out of native SwiftUI leaves called directly rather than through the kit, and that cost it THREE LAYOUT DEFECTS, not just a house-style mismatch. A `Host matchContents` lays out at its own intrinsic size, so each NativeStepper wrapped in a bordered `space-between` row ESCAPED that row: the \u00b1 capsule floated clear of the field\u0027s top edge and the value fell to the bottom-left of an empty ~108dp rectangle \u2014 twice, plus the same box around NativeDateField. quick-sport.tsx had the identical leaf in a BARE row and sat correctly, which is what named the cause. FOUR MORE, each a decision the app had already made once: the segments were tinted `C.amber` where the kit\u0027s one segmented control tints with the app accent, and their off-iOS fallback was a private static `PresetRow` where every other screen gets `LiquidSeg`; each value carried TWO controls (a preset segment and a stepper, both writing the same number) with the segment holding NO selection at all once the stepper moved the value off the preset grid; the first field label carried no top gap, so it collided with the sheet\u0027s subtitle; and the commit was a hand-rolled sand pill (mono caps, paddingVertical 15, opacity 0.45 when blocked) beside APill\u0027s width-holding commit states. WHAT SHIPPED IS THE REDUCTION, not the like-for-like rebuild. The rebuild fixed how each control was DRAWN and never asked whether it should be there: seven controls, five uppercase kickers and ~790dp of scroll to record that you sat in the same sauna you sit in every week, at the temperature it always runs at, just now. `AScrubField` (new, in the kit) is the answer to the doubling: the FIGURE IS THE CONTROL \u2014 fs.hero, a horizontal drag across it walks the value one step per 14dp of travel with `haptic.selection()` per detent (the audit\u0027s slider rule, applied to a gesture with no thumb), and bare 44dp \u2212/\uff0b land an exact number. It carries `accessibilityRole=\"adjustable\"` with increment/decrement actions, which is the one thing a hand-drawn \u00b1 normally loses against the platform\u0027s stepper. The preset rows went, and with them five labels that were naming what the values already said \u2014 \"HOW LONG\" above \"20 min\" is the interface talking about itself. ALSO: the dose lost its box (a border and a fill do not make a number important, scale does) and its derivation line now speaks ONLY when the multiplier is not 1.00, since at the modality\u0027s own reference a minute IS a minute and saying so is noise wearing the voice of an explanation; WHEN is one word (\"Now\") that reveals the system picker on tap, because the back-dated sitting is the minority case and was wearing a fully-dressed labelled field; RECENT moved behind a bare \uff0b-with-ash-count expander (ADrawer) \u2014 it is a correction path, not a third of the resting sheet; and Save is APill. The panel rests without scrolling now, which is the only measurement that matters for a log you open in a towel. THE ROW ON TODAY (heat-row.tsx) took the same pass: ACard\u0027s own CARD_PAD instead of a bespoke 13/16 that made it sit shorter than the check-in card clustered above it, the week\u0027s two figures composed as FIGURES rather than joined into one sentence by `.replace`, and the trailing mark DRAWN \u2014 a new bare `plus` glyph in AURORA_ICON_PATHS, on auroraIconStroke like everything else, replacing the fullwidth \uff0b text character whose weight and centring belonged to whichever mono face was loaded. `add` stays the CIRCLED variant, because a ring means it opens something. THREE NEW KEYS in EN/PL/DE (equivWord, counts, now) plus two for the row (rowWeek, rowEquiv); every other string was reused. NOTHING IN engines/heat.ts, the API or the stored shape moved \u2014 this is a view change. THE ONE SEAM LEFT CLOSED ITSELF MID-BRANCH. The redesign shipped with the kind segment still drawing in the SYSTEM face on iOS \u2014 raised as a planned item, because moving it is a house-level decision about every ASegment in the product rather than a Heat one. liquid-glass-segments then made it for the whole app from the other direction: the native Picker sized its host from the SwiftUI content ONCE at mount, before the labels were translated, so on device it drew outside its own box. The dispatch is deleted and ASegment IS LiquidSeg everywhere. Heat inherits it by going through the kit \u2014 which is the argument for the kit, made by the calendar: the surface that called the native leaf directly had to be fixed by hand, and the one routed through ASegment was already fixed. THE ROW, AGAIN (Aug 2026) \u2014 reported as \"the Heat section looks inconsistent with the rest of Today\", and it was, in three ways the earlier pass fixed the padding of and never looked past. (1) IT EMITTED NO TOP GAP. Every block on Today owns its own leading space \u2014 the plan hero, the done floor, the check-in card and RtpPanel take 16, the week verdict 20, the chromeless doors 14 \u2014 and this one took none, so its card butted FLUSH against the check-in card clustered directly above it: two hairline borders touching, the only place on the screen where that happens. The gap sits on the PressScale rather than on the ACard inside it, so it is layout outside the pressed surface instead of dead space that scales with the tap. (2) ITS TITLE WAS THE ONE CARD TITLE ON TODAY IN BOOK WEIGHT (F.reg/14) \u2014 the check-in beside it heads at F.bold/16/-0.3, the protocol at F.black/20/-0.3, the doors at F.bold/14. \"Heat\" now heads exactly as \"Readiness\" does, which is the only defensible answer for two cards in the same cluster. (3) THE BARE \uff0b WAS AMBER, the single tinted glyph in the Recover cluster and the only expander mark in the product not in ash \u2014 the Other-sports tail, the endurance block's All-sports control and this feature's OWN sheet (\"bare \uff0b/\u2212 in ash\") all draw it in ash, because a hue on a control mark says \"go\" and an expander goes nowhere. Also tokenised the clearance card's three magic numbers (7 \u2192 space.xs, 11 \u2192 space.md, lineHeight 18 \u2192 leading(fs.caption), which is the same 18). A view change only \u2014 nothing in engines/heat.ts, the API, the stored shape or the copy moved. THE FOURTH INCONSISTENCY WAS NOT HEAT'S, and it is fixed in the same branch \u2014 see today-card-material: Today mixed card MATERIALS on iOS 26, because three of its cards hand-rolled ACard's box instead of importing it, and Heat was on the kit's side of that split. Making Heat solid would have cemented the copy; the three copies were deleted instead." },
  { id: "today-card-material", area: "Mobile", status: "shipped", title: "Today \u2014 one card material (the three hand-rolled ACards deleted)", detail: "FOUND WHILE FIXING THE HEAT ROW (see heat-sheet-redesign), and the bigger half of the same complaint. Today drew its cards on TWO MATERIALS on an iOS 26 device: the plan hero and both Heat cards imported ACard, which drops a native SwiftUI Liquid Glass layer behind transparent RN content, while the check-in card (FeelingCard), the DONE TODAY wrapper (both in aurora/home.tsx) and the week verdict (aurora/week-verdict.tsx) spelled ACard's base style out by hand \u2014 borderWidth 1 + C.line + RADIUS.card + CARD_PAD + C.ink2 + cardShadow() \u2014 and a literal View stays SOLID INK2 forever. So the Recover cluster read check-in (solid) \u2192 Heat (glass) one gap apart, and Today's largest card sat opaque between two glass ones. THE COPY WAS BYTE-FOR-BYTE, which is what makes this the kit's own argument rather than a taste call: the hand-rolls named every value ACard already owns and could still never reproduce the one thing that matters on iOS 26, because the material is not a style property \u2014 it is a native layer the component mounts. ACard's own header calls this out ('234 cards across two materials, decided by which file the developer happened to import from'), names glass as Today's material, and gives `solid` as the opt-out for data-dense read surfaces. None of these three is one. THE FIX IS A DELETION: three `<View style={{\u2026}}>` \u2192 `<ACard>`, and `cardShadow` fell out of both files' imports as dead. Only the week verdict passes any style at all, and only its padding, which is genuinely its own: the compartment below supplies the bottom pad while the drawer is open, so the card gives its own up rather than fencing the panel in (Yoga resolves the edge-specific padding over ACard's `padding` shorthand, which is why all three edges are passed). Its literal `borderRadius: 28` went with it \u2014 the same number RADIUS.card holds, written twice. THEN THE OTHER THREE, on the same branch, which is what actually finishes the screen. (a) THE PROTOCOL CARD (aurora/protocol.tsx) was the last geometry in the Recover cluster: an unfilled hairline box at radius 20 with 16/16/12 padding, sitting between two ACards at radius 28 and a flat 20, and \u2014 having never carried a fill \u2014 the only one on the screen with no material at all. Its COLOUR is why it stayed hand-drawn and also why it could stop: `accent` draws exactly the left rail it wanted (lime once cleared, blue while the ladder runs), so it keeps the one thing that made it look unlike a panel of the day's figures and gives up the four values that differed only because nobody had asked. The stage spine below already reads the same `accent`, so the rail and the ladder now agree by construction. Its bottom pad goes 12 \u2192 20, the cluster's number rather than the bespoke one. (b) APressCard (new, in the kit) closes the gap that made hand-rolling look reasonable in the first place: ACard is a `View`, so a card that had to be TAPPABLE had nothing to come from, and ChooserCard + StructureCard each drew ACard's box from memory. Wrapping ACard in a PressScale was never the answer \u2014 the scale would run on a wrapper that is not the surface, so the shadow and the glass would sit still while the box inside them shrank. APressCard IS the PressScale, wearing ACard's surface (same fill, hairline, RADIUS.card, CARD_PAD, cardShadow, the same `solid` opt-out, the same GlassSurface). It also owns THE CORNER GLOW \u2014 two absolutely-filled layers under the content, drawn identically in both cards, and the reason `overflow: hidden` is mandatory rather than remembered: a caller who passes a hue and forgets to clip gets a rectangle of colour outside a rounded card. Passing the hue is the whole API, and THE CLIP RIDES WITH IT: `overflow: hidden` sets the layer's masksToBounds on iOS, which clips the SHADOW as well as the glow \u2014 so applying it unconditionally (the first cut did) handed every caller a `cardShadow()` that could never render, a primitive promising ACard's surface while guaranteeing one part of it was dead. A glowing card now trades the shadow for the clip, which is exactly what both chooser cards already did by drawing neither; a plain one keeps ACard's depth. ORDER MATTERS UNDER GLASS: glass down first as the surface, glow painted ON it, content on top \u2014 a glow UNDER the glass would be a coloured card seen through frosting rather than a tint in the material. Both callers keep only what is genuinely theirs: ChooserCard passes nothing (its `padding: 20` WAS CARD_PAD, spelled as a number), StructureCard passes `width` and a 16 pad, because a 72%-wide rail card cannot spend 20 a side. (c) A GUARD so none of this can recur \u2014 see card-surface-guard. SO TODAY IS NOW ONE MATERIAL, every card on it from the kit. Mobile + web typecheck green, iOS export bundles, 3158 core + 79 mobile + 139 web tests pass. The change cannot be verified visually in this sandbox \u2014 no simulator \u2014 so the material itself lands on the next TestFlight build; the JS is exercised by the export. THE ONE THING TO LOOK AT ON DEVICE: the two chooser cards were solid ink2 and are now glass, and their corner glow is a 5% accent wash designed against a solid ground. It should still read (the glow paints on the material, not under it), but if it muddies, `solid` on APressCard is a one-word revert at each call site. THEN THE FIRST TWO OFF THE RATCHET, worked straight off card-surface-guard's list: percent-program (3) and history-views (3), the two densest files on it \u2014 25 \u2192 19. PERCENT-PROGRAM: the 'How it progresses' card becomes ACard + `cardStack` (its radius was the literal 28 and its run gap the literal 12; both are tokens). The all-prose WEEK card and the DAY card are ROW-LIST cards \u2014 their rows run to the card's own edges and carry their own 16 inset plus the hairlines between them \u2014 so both take `padding: 0` and keep the clip, which is the only thing about either that was ever bespoke. The day card stays ACard rather than APressCard deliberately: what presses is the HEADER ROW, which collapses back to a header when the day opens, and making the card the target would put the whole expanded day's tables inside the press. One of the three had also hoisted the box into a `const card` and spread it \u2014 the copy one step further along, where shared values make it look deliberate. HISTORY-VIEWS: the session card (agenda + timeline) is the textbook APressCard \u2014 a bare `Pressable` wearing ACard's box, which is precisely the shape that had nowhere to come from, and it gains an accessible NAME in the move (it carried neither role nor label, so VoiceOver read the card's whole text as one run and never announced it as a button). The week card keeps the ONE value that is genuinely its own, the lime-tinted hairline on the current week. And StatsView's `cardStyle` const \u2014 ACard's base style hoisted and spread into two different shapes \u2014 is deleted, with the chart card and the three Mini tiles each passing only what is theirs. The Mini tiles keep their mono figures rather than moving to AStat: that would swap the face and add rolling, which is a redesign, not a material fix (tracked as mobile-stat-tile). ONE MORE THING FOR THE DEVICE PASS: History's session list is UNBOUNDED and not virtualized (see mobile-list-virtualization), so it now mounts one native glass host per card. Nothing here can measure that. If the list scrolls badly on the next build, `solid` on APressCard is the same one-word answer \u2014 and a data-dense list is exactly the case ACard's own note reserves it for. THEN THE NUTRITION SURFACE, 19 \u2192 16 \u2014 and it is the clearest statement of the whole problem: nutrition.tsx ALREADY RENDERED EIGHT REAL ACARDS beside its two hand-rolls, so on iOS 26 the material split was not between screens, it was INSIDE ONE SCREEN. Eight glass panels and a solid macro strip among them. The MACRO STRIP (a food's P/C/F row) keeps two values that are genuinely its own \u2014 `flexDirection: row` and a 6dp side pad, because three flex columns centre their own text and 20 a side would only squeeze them \u2014 and gives up the fill, the hairline and the radius. The VERIFIED-SOURCES rail card becomes APressCard passing only its rail width and its compact 16 pad (the tail beside it was already the shared RailTail, which is what made the card the odd one out in its own rail). PANTRY's hero had nothing of its own at all: the radius was the literal 28 that RADIUS.card holds and the pad was already CARD_PAD, so the entire box was the kit spelled out, and only its leading gap survives. NOT TOUCHED, and worth saying because it is one line below the macro strip: the FROM THE PACK panel draws at radius 16, not the card radius, so the guard does not claim it and neither did this pass \u2014 a nested panel inside a card is a different object, and the kit's own concentric-radius note is what governs it." },
  { id: "card-surface-guard", area: "Core", status: "shipped", title: "A card comes from the kit \u2014 the hand-rolled-surface ratchet", detail: "THE RULE: a card is a component, never a style object. apps/web/__tests__/card-surface.test.ts scans every mobile .tsx and fails on a style object that pairs a card RADIUS (`RADIUS.card`, or the literal 28 it holds) with a card FILL (`\u2026ink2` / `\u2026card`) \u2014 the two properties that only ever mean THIS IS A CARD, and the only two that did NOT vary across the copies it was written for (padding, border and shadow all did). WHY IT EXISTS: today-card-material found three hand-drawn ACards on ONE screen and nothing had flagged any of them. `tsc` is perfectly happy with a hand-drawn box, and the copies were byte-for-byte correct \u2014 they simply could never mount the native Liquid Glass layer, because the material is not a style property. They were found by reading the screen beside a bug report about something else, which is not a process. This repo already fails builds on the same class of drift (card-foot on a hand-rolled footer rule, feed-typography on a Text with a size and no face, screen-gutter on a bleed that does not name its container); card SURFACES had no such rule. IT IS A RATCHET, and that is the design rather than a compromise. 25 hand-rolls remain across 20 files, on screens this branch had no business rewriting, so RATCHET lists them BY FILE WITH COUNTS. That makes the list do three jobs at once: the four Today files are pinned at zero and cannot regress; a NEW hand-roll anywhere fails immediately; and fixing one FAILS UNTIL IT IS STRUCK OFF \u2014 the reverse check matters as much as the forward one, since a list that silently over-permits stops ratcheting. The number can only go down, and it IS the live backlog for design-system-unification-sweep's card work. Same idiom as expo-alignment.test.ts's DELIBERATE map: an exception may exist, but only in writing. IMPLEMENTATION NOTES worth keeping. It matches a card HOISTED INTO A CONST on any of the five properties such an object plausibly opens with, not just `backgroundColor` \u2014 the two files that did hoist one were caught only because theirs happened to open on that word, which is luck rather than a rule. It BRACE-MATCHES style objects rather than scanning lines \u2014 a line-based scan would have missed the week-verdict shape, which spelled its box across three lines. It blanks comments while preserving offsets, so line numbers stay true and the prose documenting the rule (this entry, the test's own header, ACard's) cannot trip it. It skips any object pinning BOTH a numeric width and height: a 56\u00d756 box at radius 28 is an avatar, and a card never fixes its own height \u2014 that exclusion went in after the detector flagged the settings avatar ring. And it carries two SELF-CHECKS, because a regex that matches less than it should reads exactly like a clean codebase: one asserts the file list is non-trivial, the other feeds the detector ACard's own base style and requires it to fire. Verified end-to-end by injecting a hand-rolled card into heat-row.tsx and confirming both the sweep and that file's own assertion went red. It also pins APressCard's existence and shape (a PressScale, offering GlassSurface): if that primitive goes away the two chooser cards are hand-rolled again within a week, because the gap it fills is exactly what caused them. TURNS SO FAR: 25 \u2192 19 (percent-program + history-views) \u2192 16 (nutrition + pantry), each worked straight off the list rather than found by reading \u2014 see today-card-material. THE REVERSE CHECK EARNED ITS KEEP ON THE FIRST TURN: clearing those two files made the suite FAIL \u2014 'down to 0 (listed as 3) \u2014 good. Now lower it in RATCHET so the win is locked in' \u2014 which is precisely the moment a one-directional allowlist would have gone on permitting three hand-rolls in a file that no longer had any. Cleared files move to the CLEARED list beside Today's four, where a second assertion also checks they still render a kit card at all (zero hand-rolls is what an empty file looks like too). 11 assertions." },
  { id: "day-card-state-motion", area: "Mobile", status: "shipped", title: "The day card travels between its states", detail: "REPORTED AS: \"remember about these elements having motion/transitions while operating different scenarios.\" They had none. Both week rails replace EVERYTHING under their hairline when a chip is tapped — a receipt with its figures, an empty block with three actions, a declared rest day — and those are different HEIGHTS, so the card snapped between them and the whole screen below jumped with it. The rest declaration is the largest of the three: three actions become none and the card loses most of its height. This is the exact class of bug lib/list-motion.ts was written about — correct state, passing tests, a happy typechecker, and an app that feels cheap in a way nobody can point at — and it is the one class no gate here can catch by RUNNING the code, only by watching a device. NO NEW MECHANISM: `useListMotion()` from lib/list-motion.ts, the shared springs.slide curve through the same springToRN both clients use, armed immediately before the commit, honouring Reduce Motion (where the correct substitution for a layout change is no motion at all — there is no position left to cross-dissolve). Three sites: the logbook rail's day chip, the plan week rail's day chip (the two rails are ONE object in two modes, so a transition on one and a teleport on the other is precisely the drift the shared shape exists to prevent), and the rest declaration. THE REST COMMIT ALSO MOVED. It redrew from setRestDay's returned set, which put an AsyncStorage round-trip between the finger and the first pixel — and would have armed the animation around whatever commit happened to land during the await, since configureNext decorates the NEXT layout whenever it arrives. The day key is already on the LogbookDay, so the next state is computed at the call site: animate, apply, persist best-effort, reconcile on the next read. setRestDay still returns the set, with its doc now saying who that is for. GUARDED as text, in list-motion.test.ts beside the logger's set list, on the CALL SHAPE rather than on the import — an unused hook is the failure mode — plus a negative assertion that the store's answer is not piped back into state. WHAT IS NOT ANIMATED, deliberately: the done floor growing when a sauna is saved elsewhere. That commit is a query revalidation with no tap behind it, so there is no call site to arm without straddling an await; it needs the same compute-locally treatment the rest declaration just got, and is left until something asks for it. PLATFORM: LayoutAnimation is ON for iOS + Fabric and OFF for Android + Fabric in the installed RN 0.85.3 (the reasoning is in list-motion.ts's header). The product ships to the App Store, so this is a note, not a blocker. The standing rule is now in CLAUDE.md so the next session does not rebuild a teleport." },
  { id: "action-vocabulary-nouns", area: "Core", status: "shipped", title: "The logbook day's actions are nouns — Gym, Sport, Rest", detail: "REPORTED AS: \"Log a session should be renamed to Gym / Log a sport should be renamed to Sport / we should get space for a Rest button.\" The three are one change, and the third is the reason for the first two. AActionPair wraps, and three VERB PHRASES do not share a 320pt measure — 'Log a session', 'Log a sport' and any rest label would have taken two rows, which is the arrangement that component exists to avoid (it replaced a full-bleed glowing pill sitting forty pixels above a dashed tile). Nouns fit on one baseline and lose nothing: the row sits under a card that has just said the day is empty, so 'Gym' has no other reading, and the verb was carrying a job the context had already done. w.home.today.alsoTodayLogFirst 'Log a session' → 'Gym' (PL 'Siłownia', DE 'Gym'); alsoTodayLogSport and alsoTodayLogSportMore both → 'Sport' in all three languages; w.home.logbook.sportOn 'Log a sport on {day}' → 'Sport on {day}'. THE TWO SPORT KEYS NOW SAY THE SAME WORD, and both are kept rather than collapsed: alsoTodayCopy (plan-schedule.ts) still picks between them on doneCount, its contract and its tests are unchanged, and the day the row wants 'Sport' vs 'Another sport' back the distinction is still there to say it. THE SHEET AND THE CHOOSER FOLLOWED, on review. They were held back on the reasoning that a sheet title is a heading and the chooser's siblings are 'Follow a plan' / 'Build your own', and the user overruled it — correctly, because the split left the SAME destination named two ways one tap apart ('Sport' on the row, 'Log a sport' on the sheet it opens), which is the exact confusion log-sport-vs-workout-clarity was written to end. w.home.quickSport.title → 'Sport' in all three languages; w.home.today.chooserLogTitle 'Log a session' → 'Gym' (PL 'Siłownia', DE 'Gym'). The subs under both are unchanged and still read — 'Sport' over 'A run, a swim, a match — it still counts', 'Gym' over 'One session, right now — no plan needed.' — and the chooser card keeps its VERB cta ('Log'), which is a button on a card rather than a name for the thing. This unwinds half of log-sport-vs-workout-clarity, which is amended in place rather than retired: its argument (the sport log and the structured logger must read apart) is untouched and is exactly why the two nouns are DIFFERENT nouns. The design-tokens ratchet that pins one action pair per day state is unmoved at 2 — see rest-day-declared for why the third state deliberately carries none." },
  { id: "rest-day-declared", area: "Mobile", status: "shipped", title: "Rest — the plan-less athlete declaring a day off", detail: "A logbook day had exactly two states: it held training, or it was empty. Those are not the same as \"I rested\". An EMPTY day is the app NOT KNOWING, and it draws the invitation to log something — every day, at an athlete who has decided today is for recovering, with no way to answer it. The plan rail has always had a rest day because a PROGRAM can prescribe one; the plan-less athlete had no vocabulary for the same fact. Rest is now the third action on the empty day (see action-vocabulary-nouns, which is what made room for it) and the third state of the day card. DECLARED, NOT INFERRED: this is the second training-INTENT signal the app collects, after day-band-prefs' \"not today\" — everything else it knows is what already happened. THE DRAWING is the plan rail's own rest day (moon, w.home.rail.restDay) so one fact has one shape on both rails, centred like the empty block it replaces, and NO ACTION PAIR — the only day state without one. A day the athlete has just called a rest day must not still be offering the two ways to train it, and the retraction pill that shipped in the first cut was worse than nothing there: it was the single thing on the card at full button weight, so the loudest element on a settled day was the offer to unsettle it. THE BLOCK IS THE RETRACTION instead — it takes the tap, with the undo spoken in its accessibility hint rather than drawn as a control — because a declaration that cannot be taken back is a mis-tap that traps the day until midnight. w.home.logbook.restUndo survives as that hint, which is the one place it is still worth saying. Logging anyway is never blocked — a session that lands on the day moves the card to the receipt branch, which never reads the flag; the rail also ignores a declaration on a day that holds training, so the work is always the answer. STORED ON THE DEVICE (apps/mobile/lib/rest-days.ts, AsyncStorage), same reasoning as day-band-prefs: it is a statement about a day, not a record of work, and it moves no figure any engine computes. Unlike a rejection it is NOT scoped to today — \"Saturday was a rest day\" is still true next week and the rail scrolls back four — so the store is a SET of local day keys, pruned to 90 days so it cannot grow without bound, with a +1 upper bound so a declaration written just before local midnight is not erased. THE LIMITS ARE REAL AND STATED: a rest day does not reach the web admin panel, does not survive a reinstall, and feeds NOTHING — the streak still breaks, the MRV week still counts it as untrained, the day band still speaks. Making it an input is a different change and needs a different store; see the planned rest-day-signal. Copy is new in EN/PL/DE (w.home.logbook.rest, .restOn, .restBody, .restUndo); the title reuses w.home.rail.restDay rather than spelling 'Rest day' twice. 5 render cases in test/logbook-rest.render.test.tsx, including the block being a button: with the pill gone, reversibility rests entirely on that, which is exactly the kind of affordance a refactor drops silently. The design-tokens ratchet counting one action pair per day state went 2 → 3 → back to 2, and now records WHY this state is excluded rather than only what the number is." },
  { id: "rest-day-signal", area: "Core", status: "planned", title: "A declared rest day as an ENGINE input, not a note", detail: "rest-day-declared stores the declaration on the device because it changes no figure. The moment it should change one it has to move: a streak that a declared rest day does not break, an MRV week that counts it as planned recovery rather than as a day the athlete failed to train, a day band that stops proposing work on a day already called. All three need the declaration to be on the RECORD — a Signal, written through /api, readable by the engines and by the admin panel — because an engine reading AsyncStorage is an engine that answers differently on a reinstall. Deferred rather than done: the storage decision is the whole change, and shipping the button first is what tells us whether the declaration is used enough to be worth a column." },
  { id: "heat-day-log", area: "Mobile", status: "shipped", title: "The sauna in Today's log — under the session it followed", detail: "REPORTED AS: \"sauna should be displayed in your training in today, in the appropriate place — if you did gym and swimming and the sauna was after the gym, it goes below the gym.\" It was not displayed there at all. A sitting was readable in exactly TWO places in the product: the week's count on Today's Heat row (\"2 this week, 44 equiv min\") and the sheet's Recent list. Neither says WHEN in the day it happened, so an athlete who lifted at 09:00, sat in the sauna at 10:20 and swam at 16:00 opened the one surface that names the day's work — the done floor — and saw two workouts with nothing between them. The engines had already scored the sitting (the readiness credit, the MRV tier, the clearance split); the RECORD of the afternoon did not contain it. PLACEMENT IS CORE'S, not the view's: `heatDayRows(sessions, signals, { day, order })` in engines/heat.ts returns the day as ONE ordered list of `{ kind: \"session\" }` / `{ kind: \"heat\", under }` rows, hanging each sitting off the session it followed. It reads the WINDOW ALREADY IN USE — HEAT_EDGE_GRACE_MIN before the recorded end, clamped to the session's own start, through HEAT_AFTER_SESSION_H after it — which is `heatAfterSession`'s window, so the day's list and the Wrapped's post-session prompt cannot disagree about which workout a sauna came after. Inventing a second window here is exactly how they would have. A sitting hangs off ONE session only (nearest end wins, the same tie-break the prompt uses), so a sauna between two workouts is never printed twice; a session can hold SEVERAL, because two rounds ninety minutes apart are two sittings and both followed that workout. An UNATTACHED sitting (a rest-day sauna, one taken hours from any session) sorts among the sessions by its own instant rather than being dumped at the end. Only the VIEWED calendar day's sittings are placed — a 00:30 sauna after a 23:00 session is the next day's first event, and a row appearing on both days is one sitting counted twice by eye. `sessions` is consumed in the order given and the anchors sort by event time in `order`, defaulting to \"desc\" because that is what sessionsOnDay returns. THE ROW (aurora/heat-accent.tsx) is an ACCENT, and three things say so at once: it INDENTS under its parent (a standalone sitting does NOT — an indent under a session it did not follow would be a claim); its glyph is BARE where a session's sits on a 40dp filled tile, because a sitting must not wear a workout's chrome; and it is AMBER, heat's hue everywhere else it appears, against the lime of a plan session and the blue of one logged off-plan. It is deliberately THE SAME SIZE as the rows around it — title at the session title's rung, meta at the session meta's, same vertical padding — so the list keeps one rhythm and the sauna is subordinate by position and hue rather than shrunk into a footnote. Figures are COMPOSED with a gap (\"20 min\" / \"90 °C\"), never joined by a separator; an ASSUMED temperature is not printed at all, since the engine's own reference standing in as a measurement is the false precision `assumedTemp` exists to mark. IT IS IN BOTH VIEWS OF THE DAY: the done floor (aurora/done-floor.tsx, which takes `heat` + `dayTs` and renders nothing new when they are absent) and the Done-today sheet it expands into — a day that names the sauna in one and not the other is a day disagreeing with itself. The sheet variant reads the host's own glyph column (30, not the floor's 40) and takes its hairline, so titles start on one edge. THE SEAM'S COUNT STILL COUNTS SESSIONS: \"3 done today\" on a day with two workouts and a sauna would be claiming a workout that never happened, and that number is what the athlete checks their week against. NOT INTERACTIVE, deliberately: a sitting is corrected and deleted in the Heat sheet's Recent list, the one surface that can also fix the minutes and the temperature, and a tap target here would be a second, weaker path to it. NO NEW COPY — the protocol names (w.recovery.heat.protocol.*) and \"min\" were already translated in EN/PL/DE. 9 new core tests covering the reported case, both orders, the standalone placement, the one-session-only tie-break, two rounds under one session, the shared window at both edges, the short-session clamp, the day boundary, and the untouched no-heat path. Nothing in the engine, the API or the stored shape moved. WEB: not applicable — the done floor is a mobile surface and the web client was retired in Aug 2026. THE LOG SHEET THEN LOST ITS HISTORY, and the delete moved with the reading. `RECENT` (heat-sheet.tsx) listed the last three sittings behind a bare-＋ expander with a × on each row, and it existed for one reason: this sheet was the ONLY surface in the product that had ever heard of a sitting. Once the day's log carries them that list is a worse copy of a better surface — three rows with no day and no session against them, behind an expander, in a sheet whose job is to WRITE. A log sheet that also lists the log is two screens under one title. It is deleted, along with `showRecent`, the `remove` handler and the `heatSittings` read behind it; `useHeatSignalsQuery` stays, read now only by the duplicate guard. THE CAPABILITY DID NOT GO WITH IT, and this is the part a naive removal would have got wrong: that × was the only way to correct a fat-fingered 90-minute entry anywhere in the app, and the out-of-order duplicate guard explicitly opened the list so the clash could be deleted one row down. Both are answered on the ROW instead. A sauna accent row now SWIPES to delete behind the app's own confirm sheet (never Alert.alert — the design-token test bans the system alert), exactly as the session rows around it do, taking BOTH Signal rows via `HeatSitting.ids` — removing only the minutes would leave a temperature nobody sat in, and `heatSittings` drops a lone `saunaTemp`, so the row would vanish from the screen while a dead signal stayed on the record. It invalidates the SHARED heat cache entry, not a private fetch: the readiness ring and the volume model read the same rows, so a sitting removed here has to move the score behind it. The gesture is on the FLOOR only — the Done-today sheet renders the same rows without `onDelete`, because a sheet is not where a destructive gesture belongs and the floor behind it has the same row. The duplicate warning no longer promises a list underneath it: it names the day, the minutes and the temperature, so the athlete recognises the entry rather than taking the app's word for it, and the entry itself is on Today under the session it followed. COPY: `w.recovery.heat.recent` and `.remove` are gone in EN/PL/DE; the line reuses the protocol names and `min`, which were already translated. TWO CORRECTIONS ON REVIEW, both reverting a call this branch got wrong. (1) THE LINE IS SMALLER, which is what the brief said. \"Mostly as an accent\" shipped first as SAME-SIZE — session-rung title over a mono meta line, subordinate only by indent and hue — on the literal reading of \"the same sized as main workouts\". That was the wrong half of an ambiguous sentence: at equal size an accent is not an accent, it is a third workout that happens to be amber. It is ONE line now (modality and both figures on a single baseline at fs.caption, glyph at fs.bodyLg, paddingVertical space.xxs) at roughly half a session row's height. The glyph keeps the tile's COLUMN WIDTH so every title in the list still starts on one edge. `flexShrink` is on the modality and not on the figures: at a large Dynamic Type setting the word is what can afford to truncate. (2) THE DELETE IS NOT ON THE LINE. It was put there when Recent went, on the argument that the place you notice a wrong entry is the place you read it — and that argument does not survive the size change: a destructive gesture on a half-height accent inside a list whose other rows delete a WORKOUT is a mis-swipe waiting to happen, and it would be the only place in the app where two rows of different rank in one list both destroy something. `SwipeRow`, the confirm and the `deleteTitle`/`deleteBody` copy are all gone with it. SO THERE IS CURRENTLY NO DELETE PATH FOR A SITTING ANYWHERE, and that is stated rather than hidden: `deleteHeat` in apps/mobile/lib/api.ts is kept and has no caller, waiting for the surface that will own it. Until then a mis-typed sitting can only be corrected by logging over it, which the engine sums — the duplicate guard warns about exactly that case and lets it through. That is a known, accepted gap, not an oversight; see the planned entry heat-entry-correction. FOLLOW-UP (Aug 2026, user-reported): \"Sauna should be displayed even if there was no workouts.\" It was not. A sitting reached the day's log through the done floor, and the floor read a day with no SESSIONS as an empty day. WHERE IT ACTUALLY VANISHED was LOGBOOK mode — the plan-less athlete, and the reported case: `LogbookDay.logged` counts sessions and only sessions, so the rail took an unlogged day down the empty branch and never mounted the floor at all. The sitting appeared on no surface that names the day, while the engines had already scored it into readiness, the MRV tier and the clearance split. The count-based plan hero had a second, narrower version of the same hole: its card was gated on `!!sched || sessions.length > 0`, so a first run with a sauna and no session ever logged hid it too. On the plan WEEK rail the sitting did render — that branch mounts the floor on every day state — but under the empty day's invitation (\"a match, a run, a swim — it lands here\"), which is a card telling the athlete nothing had landed here directly above the line saying something had. Core was never at fault — `heatDayRows([], signals, …)` has always returned the standalone sitting — the READING was. THREE PLACES, one rule: a day is empty when it holds no entries, not when it holds no workouts. (1) done-floor.tsx gained `emptyCaption` and an `entries`-based verdict: the seam's count still counts SESSIONS (a sauna is not a workout and \"1 done today\" would claim one), so a sauna-only day gets NO seam — no count, no arrow, and no invitation, because an invitation printed directly above the line saying something already landed here denies the line under it. The floor returns null outright when it has nothing to state and nothing to offer, rather than contributing its 22px seam gap. (2) logbook-rail.tsx now mounts the floor on the EMPTY day too, with home.tsx setting `emptyCaption={!logbookMode}` — that branch already is the invitation drawn whole (symbol, headline, sentence, both log actions), so the floor stays silent there on a truly empty day and draws the sauna on a day holding only that. (3) home.tsx's no-rail branch (the count-based plan hero, which keeps the floor in a card of its own) takes `doneEntries.length > 0` into its gate, since a session list cannot see a sitting. The plan WEEK rail needed no mounting change: it already renders the floor on every day state, rest days included — only its seam changed. 1 new core test (a day whose only entry is a sauna) + a new mobile render gate (test/done-floor.render.test.tsx, 5 cases: the sitting listed, the invitation dropped beside it, the invitation kept on a day holding nothing, null on a host that already said it, and the sauna still drawn there). The test harness grew ConfirmProvider, which the floor's rows require unconditionally. No engine, API, schema or copy change — no new strings at all. SECOND FOLLOW-UP (Aug 2026, user-reported): \"even if no training logged, sauna shouldn't be displayed below the buttons, but above.\" It shipped for one release UNDER the logbook day's action pair, which put a statement of what the athlete DID beneath two offers of what they could do — the card answering its own question after asking it, and the buttons no longer the last thing on it. The floor is now the FIRST child of the empty block, above the pair, and the same order holds in the declared-rest state beside it (rest-day-declared). test/logbook-rest.render.test.tsx asserts it on DOM order in both states rather than by eye: both nodes render either way, and only the sequence carries the mistake." },
  { id: "heat-entry-correction", area: "Mobile", status: "planned", title: "Somewhere to correct or delete a logged sitting", detail: "THE GAP IS DELIBERATE AND IT IS REAL. The log sheet's RECENT list carried the only delete for a sitting in the product; it was removed when the day's log started showing sittings (heat-day-log), because a log sheet that also lists the log is two screens under one title. The delete was briefly moved onto the day's accent line and then taken off again: the line is half-height by design, and a destructive swipe on it sits inside a list whose other rows delete a WORKOUT — two ranks, one gesture, one mis-swipe. SO TODAY A MIS-TYPED SITTING CANNOT BE REMOVED. It can only be logged over, which `heatAdjustment` sums inside one 48 h window and `heatWeeklyFrequency` counts as two sittings toward the MRV tier — the exact failure the duplicate guard warns about and then, correctly, allows through. The blast radius is bounded (the acute credit decays over 48 h; the chronic tier averages over four weeks) but it is not nothing, and it is silent. `deleteHeat(ids)` in apps/mobile/lib/api.ts is kept with no caller, waiting for this. WHAT IT NEEDS TO BE: a surface where a sitting is the SUBJECT rather than an accent beside a workout — the natural candidates are a heat history behind Today's Heat row (which already owns the week's figures and is the one card whose subject IS heat) or a day sheet reached from the accent line itself. Either way the correction should be able to fix the minutes and the temperature, not only delete: most wrong entries are a fat-fingered number, and a delete-and-relog moves the timestamp, which the decay and the pair matching both read. Both Signal rows travel together via `HeatSitting.ids` — removing only the minutes leaves a temperature nobody sat in, and `heatSittings` drops a lone `saunaTemp`, so the row would vanish from the screen while a dead signal stayed on the record." },
  { id: "heat-clearance", area: "Core", status: "shipped", title: "Heat \u2014 replace the prior with the athlete's own measurement", detail: "THE PAYOFF PHASE OF heat-recovery, and the version worth having. Every constant in engines/heat.ts is a prior drawn from published research on OTHER people; recovery-pairs.ts already measures THIS athlete's clearance by matching the immediate post-session read to a later recovery read, with a rigorous exclusion (a second session inside the gap contaminates the pair, so the pair is dropped). Each surviving RecoveryPair now carries `heat` \u2014 true when a sitting fell INSIDE its gap (session end \u2192 recovery read), counting only sittings clearing HEAT_SESSION_MIN_EQUIV so a token five minutes in a cool cabin cannot relabel a pair it could not plausibly have changed. `heat` is left UNDEFINED when no heat log was supplied at all, because absent is not the same as false. saunaClearance(sessions, recovery, heatSignals) returns { withHeat, withoutHeat, delta, confidence, withSamples, withoutSamples }: both sides must clear MIN_RECOVERY_PAIRS INDEPENDENTLY, and until they do it returns a flat zero-confidence result rather than a direction it cannot support \u2014 the same standard the estimator it is built on already holds itself to (the clearance estimate is allowed to be slow to arrive; it is not allowed to be wrong). Realistically silent for four to six weeks. WHY A DIFFERENCE OF INDICES rather than a ratio: both sides are already ratios against the same population curve, so subtracting leaves a number in the units the rest of the clearance model speaks, and a caller compares `delta` against the SAME \u00b10.15 band CLEARANCE_FAST/CLEARANCE_SLOW already bracket 1.0 with. THE READ SURFACE is in Today's Recover cluster under the heat row and renders NOTHING until confidence clears zero \u2014 that silence is the feature. Its verdict reads the DELTA, not either side's absolute index: the question is 'does heat help ME', and an athlete who clears slowly overall can still clear meaningfully faster after a sauna, which reading the absolute would have called 'no difference'. Copy in EN/PL/DE. The check-in\u2192RecoveryReport mapping was extracted to lib/use-recovery-reports.ts rather than written a second time \u2014 a day is EVERY read it carries, not one value, and duplicating readReports/placeReads would have been the drift. 5 tests covering the pair tagging, the undefined-vs-false distinction, the token-sitting floor, the silence below the floor, and the direction once both sides have evidence. IT IS IN THE ADMIN ENGINE ROOM TOO (Aug 2026), which closed the last known limitation on this feature. The console could not compute the split because athleteInputs carried sessions but not check-in reports \u2014 and the mapping from stored check-ins to RecoveryReports lived only in a mobile React hook, so the server had no way to reach it. That mapping moved into core as `recoveryReports()` and both sides now call it: writing it twice is precisely how the phone and the console would come to disagree about one athlete, which is the thing an operator surface exists to rule out. THREE THINGS THE MOVE SURFACED. (a) athleteInputs never mapped `fatigue` or `feelLoggedAt`, so every server-side pair would have silently fallen back to a check-in taken hours later and the console's clearance would have disagreed with the athlete's own \u2014 both now mapped, alongside the `feel` and `device` fields the same function already documents dropping as a hazard. (b) A pair needs SIX fields from a session, so shipping thirty sessions of blocks to answer that is a support-read carrying far more of an athlete's training than the question needs; `ClearanceSession` names the requirement as a type and the route sends exactly that projection. (c) VERIFIED RATHER THAN ASSUMED: `sessionEnds` can only FILTER reports, never re-shape them \u2014 placeReads computes a lag, context and reading, and readReports then emits only { date, energy, loggedAt }. The natural assumption is the opposite, so it is now guarded by a test. THE PANEL is the one on this screen allowed to say NOTHING, and it says nothing LOUDLY: below the pair floor it prints how many pairs each side actually has, because 'not enough evidence yet' and 'measured, no difference' are conclusions an operator must never confuse and a blank space would let them. It reads the REAL inputs rather than the what-if's \u2014 a slider that scales recent training cannot retroactively change what an athlete reported six weeks ago, and letting the simulation touch it would let an operator appear to move a measurement." },
  { id: "hook-order-guard", area: "Mobile", status: "shipped", title: "Hooks above the early return — the crash the drawer shipped", detail: "THE BUG, from a TestFlight report: tapping the profile avatar took the app down with 'Rendered more hooks than during the previous render' at updateMemo → AuroraSideMenu → AppHeader. The drawer is MOUNTED on every tab root and bails with `if (!open) return null` while shut, and global-search's two useMemos (the cross-app index and its results) had been added BELOW that line. So the component rendered 16 hooks closed and 18 open; React matches hooks by call order and nothing else, so the render where the count changed threw. Every shut render was fine, which is exactly why it reached a tester: the fault needs the OPEN render to be the second one, and the drawer's whole job is to start shut. THE FIX is the only one available — both memos moved above the bail-out, guarded by `everOpen` rather than `open` so the index is still never built for a drawer nobody has touched (which is the reason they were written low in the first place). THE SECOND INSTANCE is why this is a capability and not a commit: the scan written to confirm the fix found the identical mistake, unshipped, in app/workout.tsx — `useSafeAreaInsets()` under `if (phase === 'done' && summary) return <Summary…>`, three lines below a comment stating that hooks must stay above the early return. Finishing a session would have dropped a hook from the render. The rule was known, written down at the crash site, and broken anyway, because nothing but a human eye was checking. THE GUARD: there is no ESLint in this repo to carry react-hooks/rules-of-hooks, so it is a source scan in the idiom of the guards beside it (card-surface, expo-alignment, app-header) — packages/core/src/hook-order.ts reads every .tsx on the phone as TEXT, blanks comments and string bodies while preserving offsets (an unbalanced brace inside a literal would desync the depth count and silently blind the rest of the file), counts brace depth, and fails on a `use…()` call in a component's DEPTH-1 body below a depth-1 `return`. Only the top-level body counts: a hook inside a nested callback is a different and legal thing, and JSX in the returned tree is deeper still. It reads the guard clause in BOTH spellings — the one-liner and the block form, whose return sits a level deeper; the block form was missed by the first draft and caught by the detector's own fixtures, which is the argument for the detector living in its own importable module with tests rather than buried inside the test that consumes it. Six of those fixtures pin the shapes it must NOT flag (a hoisted component, a nested callback, a provider whose return precedes an exported hook below it, a brace inside a string), because a detector that matches less than it should reads exactly like a clean codebase. Verified by running it against the pre-fix tree, where it names all three offending lines. TWO TREES RUN IT, from ONE detector in core (pure text analysis, no React and nothing to execute, which is what lets it sit beside the engines; its fixtures live in core beside it). apps/mobile/lib/hook-order.test.ts sweeps the phone and regression-locks the drawer's memos above the bail-out; apps/web/__tests__/hook-order.test.ts sweeps the admin panel. THE WEB HALF IS NOT A CONTRADICTION OF THE MOBILE-FIRST RULE: the retired thing is the user-facing web CLIENT, and apps/web did not go with it — it still hosts the backend and ~23 admin components that operators use daily, plus the login, the legal pages and the two landings. Those render on a client and can carry this exact fault; a retired user-facing client is not an unguarded codebase. The web half is deliberately the plain sweep with no ceremony, because the admin panel has none of this crash's history and dressing it up would pretend the two surfaces matter equally. BOTH SWEEPS ASSERT AN EMPTY LIST, which is also what a walker that stopped finding files would produce, so each opens by pinning that it read a real tree (a file count and two named files). Verified by injecting a violation into an admin component and watching the web guard name all seven stranded hooks with file, line and the offending return. Wired into the vitest `pure` project on mobile and the existing web test job, so both run in CI. AND THE TREE IS CHECKED TOO, because a static scan proves a shape and not a behaviour: test/side-menu.render.test.tsx mounts the drawer SHUT — the state a tab root really mounts it in — then opens it, which is the only sequence in which the fault is visible at all. Run against the pre-fix file it fails with the tester's exact string, 'Rendered more hooks than during the previous render'; against the fix it passes. Its third case mounts the drawer ALREADY OPEN and passes even on the broken code, which is not a redundant assertion but the explanation for how this reached TestFlight: a developer who opens the drawer on a screen they are already editing never renders the closed state first, and so never sees it. Reaching the drawer needed two stubs at the native edge (expo-file-system, expo-sharing — the settings module the drawer imports for its search routes pulls in the data export) and QueryProvider added to test/render.tsx, since the search index reads the exercise catalog. A SEPARATE ONE-OFF AUDIT, kept out of the repo because the shipped guard subsumes what it found, swept both clients for the OTHER conditional-hook shapes this guard cannot see — hooks inside if/for/while blocks, and hooks in a ternary or short-circuit — by classifying every brace as FUNCTION/CONTROL/OTHER and flagging any hook with a CONTROL scope between it and its nearest function. Zero findings, and validated on a probe file carrying all four shapes so the zero means something." },
  { id: "signals-read-window-crowding", area: "Backend", status: "shipped", title: "The signal stream was crowded out by the food log", detail: "FIXED. Mobile's fetchSignals() called GET /api/signals with NO kind filter, and that route returns the 500 newest rows across ALL kinds. Every logged food writes up to EIGHT Signal rows (FOOD_KINDS \u2014 the four macros plus the satFat/sugar/fiber/salt label panel), so five foods a day is 40 rows/day and the window covered roughly TWELVE DAYS for a diligent nutrition logger, with hydration on top. priorBaseline() then read its BIOMETRIC_BASELINE_WINDOW of HRV and sleep values out of that truncated stream \u2014 so the \u00b115 wearable term was quietly degraded for exactly the athletes who use the app most, and SILENTLY: today's reading still survives the seven-day freshness gate, so the term kept printing against a baseline built from fewer samples than it asks for. THE FIX: /api/signals?kind= now accepts a COMMA-SEPARATED LIST (validated against SIGNAL_KINDS, so an unknown kind is dropped rather than becoming an unfiltered read), and every surface that resolves toBiometrics reads a dedicated fetchRecoverySignals() \u2014 hrv, restingHr, sleep, sleepScore, bodyMass \u2014 on its own cache key (qk.recoverySignals, invalidated by revalidate.recovery). Today, Performance (both call sites) and the statistics screen are repointed; the Nutrition screen keeps the unfiltered read, which is correct there because those rows ARE the stream it wants. The admin athleteInputs() query was already split per-kind for heat in the same spirit. A recovery input can no longer be evicted by an unrelated one." },
  { id: "performance-unified", area: "Core", status: "shipped", title: "Performance, Volume and Trends are ONE page (SUPERSEDED by performance-six-surfaces)", detail: "SUPERSEDED \u2014 the merge preserved everything and appended two whole screens to one scroll; Volume and Trends are their own destinations again and Performance keeps a volume hero plus doors. See performance-six-surfaces. HISTORICAL RECORD: The hub's Performance tab is a single scroll on BOTH clients (apps/web/components/aurora/performance.tsx + apps/mobile/components/aurora/performance.tsx), not three sub-views behind a chip rail. THE REDUNDANCY THAT FORCED IT: Volume and Trends were drawing the SAME engines twice. Both called volumeStatus() to render a per-muscle breakdown — Volume as landmark rails, Trends as a chip row — and both called volumeAdvice() to print the same add-volume / ease-off prescription, so an athlete could read 'add volume to chest' on one tab and see it again as a chip on another, with no way to tell whether they were two findings or one. Trends also carried a muscle PICKER purely to feed its focus-muscle chart, which meant selecting a muscle in two different places on the same subject. WHAT MERGED, IN ORDER: (1) the command centre unchanged — context rail, Performance State/HPI + components + readiness nudge, the 14-day HPI/readiness trajectory chart, injury risk with things-to-watch (ACWR, s-RPE, monotony, strain) + the tissue body-map disclosure + driver explanations, this week's recap & PRs, the discipline breakdown; (2) VOLUME entire — hero (muscles in range + the week as a seven-column shape + verdict), the block ramp, both prescription cards, the per-muscle rails with their band scales, gloss, targets and inline landmark editing, the provenance/profile card and the MV/MEV/MAV/MRV glossary; (3) TREND — weekly sets and weekly tonnage over eight weeks, then the sortable exercise-analytics table with its period toggle; (4) the horizon rails, goal + season, and return-to-play. WHAT WAS DEDUPLICATED, NOT DROPPED: Trends' muscle-chip breakdown is gone (Volume's rails say the same thing with the landmarks attached), its advice line is gone (the two prescription cards say it with magnitudes), and its focus-muscle 8-week chart MOVED onto the muscle row it describes — expanding a muscle in Volume now shows that muscle's own eight-week history, so '18 sets' and 'and it has been climbing for a month' are one thought instead of two screens. Nothing else was removed. PARITY GAP CLOSED ON THE WAY: mobile Trends never drew the weekly TONNAGE series web has always had — it does now. MECHANICS: volume.tsx and trends.tsx each take a `unified` prop — on web it demotes their <h1> to a section head; on mobile it ALSO returns the sections bare (no AuroraScreen), since the host page owns the scroller, safe area and pull-to-refresh, and nesting would have stacked two ScrollViews. Both still render standalone off their own props/queries, and useSessionsRead is literally toRead(useSessionsQuery(...)) so the three composed screens share one react-query entry rather than fetching three times. NAV: the volume + trends nav items are promotedTo \"performance\" (the same mechanism statistics\u2192history uses) so menus stop offering the same content twice, while the ids stay routable — web resolves screen \"volume\"/\"trends\" to the unified page, and the mobile /volume and /trends routes <Redirect> to /performance, so saved links and the More hub's map still land somewhere real. NO RESTYLE RULE: the merge is STRUCTURAL — not one stylesheet, theme token or palette value was touched, and no section was redrawn. Each merged block renders exactly as it did as its own screen: Volume and Trends keep their display-size titles verbatim (only the HEADING LEVEL changes, h1 to h2, so the unified page doesn't carry three h1s — identical CSS via createElement), the mobile sections use the page's existing 14dp rhythm rather than a new spacing value, and the relocated per-muscle 8-week chart is drawn in vocabulary that already existed at its destination (mobile lifts the old focus-chart geometry verbatim — 4 + n/max*48, radius 3, blue with 66 on older weeks; web reuses BlockCard's ramp idiom in the same file — ink track, radius 7, .95 current / .32 the rest — since Volume imports no chart library and a recharts frame with axes would have looked foreign inside a Volume card). A moved element must not become a restyled one. Core tests + web/mobile typecheck + web build + iOS bundle export green." },
  { id: "today-hub-tabs", area: "Core", status: "shipped", title: "Today is a hub — Dashboard / Performance / Feed pills", detail: "Today is no longer one screen; it is the athlete's HOME HUB. A three-pill segmented control sits directly under the profile header and ABOVE the calendar/week rail on BOTH clients (apps/web/components/aurora/today.tsx + apps/mobile/components/aurora/home.tsx), switching between: (1) DASHBOARD — the daily guided loop exactly as it was (masthead, week/logbook rail, plan hero, feel, this week, coaches); (2) PERFORMANCE — the analytical layer, all of it, behind a SECONDARY chip rail carrying the three existing screens unchanged: Performance (the command centre: HPI, injury risk, season, modules), Volume and Trends; (3) FEED — the social surface (posts, PR cards, kudos, comments), live today and the seat for the wider social product still to come. The registry (ids, order, labels) is shared in @hybrid/core today-tabs.ts (TODAY_TABS, PERFORMANCE_VIEWS, normalizeTodayTab, normalizePerformanceView; unit-tested) so the two clients cannot drift, and the labels REUSE the existing shared nav.* strings rather than minting a second copy of 'Dashboard'/'Performance'/'Volume'/'Trends'/'Feed' — zero new i18n keys. TWO LEVELS, TWO TREATMENTS: the top row is a primary segmented control (three equal full-width segments in one capsule, active filled chartreuse); the Performance tab's own switcher is the small mono chip rail History already established, so the hierarchy stays readable instead of pills-under-pills. The tab is deliberately NOT persisted (Today's job is 'what do I do today?', so every visit opens on the daily loop); the Performance sub-view is remembered for the session. NO SCREEN WAS FORKED: each tab renders the same component its own route/screen renders. Web embeds them as lazy dynamic imports (an athlete who only opens the daily loop never pays for recharts or the feed) and threads the shell's SAFE-CACHE gates (sessionsReady/macroReady/macroSettled) into Performance so it can still tell 'no history' from 'not asked yet'. Mobile keeps each screen owning its own scroller, pull-to-refresh and safe area via a new AuroraScreen `top` slot that renders the hub header + pills above the screen body (nesting them in Today's ScrollView would have doubled both); the mobile feed moved from the app/feed.tsx route into components/feed-view.tsx (route re-exports it) so the hub can host the same FlatList-virtualized feed, and the embedded screens hide their back button since there is nothing to go back to. Performance/Volume/Trends/Feed all remain reachable as their own destinations from nav — the hub is an additional entry point, not a replacement. GLYPHS, NOT WORDS (follow-up): the three pills carry marks rather than their labels — 'Dashboard', 'Performance' and 'Feed' are three very different lengths, so as text in three equal segments they read as a control out of alignment with itself, and no amount of centring fixes it (it gets worse in PL/DE). HUB_GLYPHS in theme/icons.ts adds three purpose-built 72x72 monoline marks in the SAME stroke box and weight as the design-kit icons — a bento (the daily loop's own shape: hero card plus the smaller cards under it), three rising bars on a baseline, and two figures — drawn as true vectors on both clients (inline <svg> on web, react-native-svg on mobile), so no PNG asset is needed. This follows the NUTRITION_GLYPHS precedent exactly: the kit is a generic UI set with no chart or community mark (its own comment says so), and an EMOJI would be the one place in the app that breaks the monoline voice while rendering as a different picture on every platform. The word is not lost — it stays each tab's ACCESSIBLE NAME (aria-label on web, accessibilityLabel on mobile) and the web tooltip, so screen readers, voice control and translation are unaffected and the i18n keys stay referenced. ONE ROW, NOT TWO: the Performance tab briefly carried its own chip rail (Performance / Volume / Trends); those three are now a single page (see performance-unified), so the rail had nothing left to switch between and was removed. THE HUB MOVE (follow-up fix): switching pills used to be a broken patchwork — web hard-cut the content; mobile replayed the WHOLE-SCREEN entrance over the supposedly-stable chrome on Dashboard→Performance, hard-cut on the other switches, and the lens pill teleported (with a one-frame vanish) because the tree swap remounts the LiquidSeg before it can fly. Now the switch is ONE motion on both clients, per the shared-element rule (the flying lens owns the move; the content behind it cross-dissolves; the chrome holds perfectly still): web runs a scoped View Transition (runHubTransition in lib/use-screen-transition.ts publishes data-nav-kind='hub'; globals.css lifts .motion-hub-chrome into its own still group and dissolves the rest over --d-dissolve/--e-fade, Reduce Motion inherits the --d-reduced substitution; guarded in __tests__/motion-tokens.test.ts); mobile suppresses the whole-screen entrance in AuroraScreen's hub mode and dissolves only the content below the `top` slot (lib/ui useHubDissolve/HubDissolve — also wired into FeedView's virtualized list and the Dashboard body's return trip, with the wrapper's offset threaded into the sticky-rail geometry), while LiquidSeg gains a `flightKey` FLIGHT MEMORY so the remounting control takes off from the slot its predecessor held and the lens still flies glassy across the change. ON-DEVICE FIX (frame-by-frame from a TestFlight screen recording): the first build showed (a) a one-frame chrome jump — a freshly mounted native SafeAreaView applies its inset a frame late, so hub shells (AuroraScreen hub mode, classic Screen's new `hubTab` prop, home's dashboard shell) now pad with the provider's synchronously-correct insets instead — and (b) the dissolve never visibly ran: mounting a hub tab blocks the JS thread longer than the fade, so a JS-driver timing snapped to its end on its first tick. The dissolve now runs on the NATIVE driver, started from the wrapper's onLayout (the view provably exists, dodging the Fabric strand race that forces useEntrance onto the JS driver). GLYPHS, NOT WORDS — EXCEPT INSIDE THE SYSTEM'S OWN CONTROL (Aug 2026): on iOS 26 this switcher is now a real SwiftUI segmented Picker (see swiftui-kit, device round 3) and there it carries the three WORDS. The glyph rule is not repealed, it is scoped: 'three labels of three different lengths never centre as a set' is a fact about the track WE draw, and the system control answers it itself by laying the words on exact thirds and centring each in its own segment. The marks were deliberately not re-expressed as SF Symbols to keep the native segment glyph-only — each was drawn to be UNLIKE its system counterpart (the bento is explicitly not the plain 2×2 'all apps' grid; the kit has no chart glyph), and swapping them in would have drawn the same three marks two ways while the floating dock kept drawing the originals. So: words in the native segment, HUB_GLYPHS everywhere else (Android, iOS < 26, the reduced-motion dock, web), the same accessible name in both — and the registry, order and labels are still the one shared source in today-tabs.ts, with no new i18n key." },
  { id: "checkin-on-today", area: "Core", status: "shipped", title: "Guided daily check-in runs ON Today (no trip to More)", detail: "The daily check-in has FIVE cards (Energy, Sleep, Soreness, Mood, then the details/submit card), but Today's 'How ready do you feel?' offered only the single one-tap face row — the rest lived on a separate screen (web More \u2192 Daily check-in, mobile /checkin), so 'log more' meant leaving the homepage. The whole ritual now happens on Today. The one-tap face IS step 1 (it writes Energy); tapping the 'Log the full picture' nudge underneath EXPANDS the very same guided wizard inline inside the feeling card, opening on step 2 (Sleep) and walking Soreness \u2192 Mood \u2192 details, then submitting and collapsing back to the card. IMPLEMENTATION: both wizards (web components/aurora/checkins.tsx, mobile components/aurora/checkin.tsx) gained the same three props — `embedded` (drops the screen chrome: the h1/back header AND the card shell, so it reads as one surface rather than a card boxed in a card), `startStep` (the question to open on, which also becomes the floor the Back button cannot reverse under, so an embedded flow can't walk into a step its host already answered), and `onDone` (fires on a successful submit so the host collapses + refetches). ONE component serves both entry points — the standalone More/'/checkin' screen renders it with the defaults, so the two can never drift. The wizard already prefills from TODAY's row, so the expansion REFINES the one-tap value instead of resetting to neutral 3s, and the same-day upsert (see checkin-relog-cooldown) means it never dead-ends on a 429. CORRECTNESS: re-tapping a readiness face while the flow is open COLLAPSES it — the open wizard's prefill was read from the previous row, so submitting it would write the superseded values back; re-opening re-reads the row. Web lazy-loads the wizard (next/dynamic, ssr:false) so its weight only lands when an athlete actually expands it. New i18n w.recovery.readiness.logMoreOpenSub (EN/PL/DE) swaps the nudge subtitle while expanded, and the chevron rotates. Web + mobile at parity." },
  { id: "checkins", area: "Backend", status: "shipped", title: "Daily check-in (personal + optionally shared)", detail: "A DAILY training-readiness log (bodyweight, energy/sleep/soreness/mood 1–5, self-reported adherence, free-text note). By default it is for the athlete's OWN personal use; a PAID athlete may opt to SHARE a given check-in with their coach, who then reads + replies (the online-coaching loop). Checkin model gains sharedWithCoach (default false); /api/checkins POST honours sharedWithCoach only when entitlement==='paid' (free users are silently coerced to private). The coach read (/api/coach/links/[id]/checkins) and reply (/api/checkins/[id] PATCH) are gated to sharedWithCoach=true AND an ACTIVE CoachLink; the RLS coach-read/reply policies carry the same sharedWithCoach predicate. Client UI on web (Daily check-in screen + share-with-coach toggle, paid-gated) + mobile (check-in route + share toggle) — both now a GUIDED, one-question-per-card flow (Energy → Sleep → Soreness → Mood, each a big reactive readiness face on a 1–5 scale, then a final details/submit card), replacing the old wall of number pills; the shared step model + value→face mapping live in packages/core/src/checkin-flow.ts so the two clients can't drift, and the copy is day-framed (fixing the prior 'this week' mislabel). NOTE: run reference/sql-history-checkin-anon.sql to add Checkin.sharedWithCoach + update the coach RLS policies (and reference/sql-checkin.sql first if the table is brand new). The home feeling card also reads back today's last logged feeling + a re-log cooldown chip (see checkin-relog-cooldown)." },
  { id: "checkin-relog-cooldown", area: "Backend", status: "shipped", title: "6h re-log cooldown (enforced) + same-day refine", detail: "The product rule 'log a feeling ≥1×/day, re-log ≤1×/6h' is enforced, not just displayed — but REFINING the same day's check-in is exempt. Server: /api/checkins POST first looks for an existing check-in on the SAME (UTC-calendar) day as the submitted weekOf; if one exists it UPDATES that row (the coarse one-tap readiness is replaced by the detailed guided values — no duplicate, no cooldown), and only a check-in for a NEW day is rejected with 429 { retryAfterMs } when it lands within CHECKIN_COOLDOWN_MS (6h) of the athlete's most recent one (keyed on the server-side createdAt, so the cap holds regardless of client or client-supplied weekOf). This makes the Today 'log the full picture' nudge coherent: after the one-tap readiness pick, the guided full check-in refines today's row instead of dead-ending on the cooldown. Clients: the home feeling card (FeelingCard in today.tsx / home.tsx) disables the four faces while cooling and shows today's last logged feeling + a 'next in Xh Ym' chip (from the shared checkinCooldownRemainingMs), and — once today's feeling is set — surfaces a 'Log the full picture' nudge (w.recovery.readiness.logMore/logMoreSub, plus logMoreOpenSub while open) that EXPANDS THE GUIDED CHECK-IN IN PLACE — see checkin-on-today; the guided full check-in surfaces any 429 as a cooldown message (web inline error, mobile Alert) via createCheckin's { ok, cooldownMs } result. The 6h window + remaining-time helper live in packages/core/src/checkin-flow.ts (unit-tested), so server and both clients agree." },
  { id: "checkin-saved-state", area: "Core", status: "shipped", title: "A saved check-in READS as saved (muted answers + Edit)", detail: "Re-opening a day whose check-in was already complete looked exactly like a form that had never been sent: live 1-5 tiles under a 'Submit check-in' button, with no way to tell whether the answers had landed short of pressing Submit again. The wizard now distinguishes ANSWERED from ANSWERED-AND-STORED. `storedMetrics` is what the server holds, stored efforts come from each session's own `feel`, and `dirty` marks anything touched since the last successful write; `locked = hydrated && allAnswered && !dirty && (storedAll || submittedOnce) && !editing`. The submittedOnce clause matters because the refetch that would prove a just-completed write lands a beat after the write itself. HYDRATION IS PART OF THE CONTRACT: the flow renders NOTHING about the day until the stored check-in has arrived. It used to render off empty state, so an athlete at 4/4 opened the sheet onto 'Today's read - 0 / 4 answered', four dashes and a Submit button, which corrected itself a moment later — a blank form is not a neutral placeholder, it is a wrong answer. Web derives storedMetrics from the shared cache with useMemo rather than syncing it in from an effect (as state it lagged the first render that saw the cache by one frame, which was exactly long enough to draw the wrong button); mobile sets `hydrated` in a `.finally` so it always flips AFTER the values it gates. LOCKED, the flow is a READ-BACK: the scale/effort tiles are disabled, the lime selection drops to a neutral outline and the unpicked options fade to 35%; the 'Today's read' summary tiles are muted to 45% and stop being buttons, because left at full colour and still tapping through to their question they read as 'edit any of this whenever' — which is what Edit is for. The details footer is [Edit] [Done] — Edit lives there and ONLY there, beside Done, where the athlete already is when they decide the read is wrong. A banner states 'Check-in saved' as text, no control of its own. A write made while editing confirms as 'Check-in updated' rather than 'Check-in logged', and the CTA reads 'Save changes'. TODAY'S FACE ROW LOCKS TOO: the four readiness faces on the Today card stayed tappable over a finished day, so the committed answer could be overwritten by a stray thumb while the flow below was busy muting that same answer; they now mute and go inert once the day is complete (TODAY only — a past day has no follow-up sheet and that row is the only way to back-log it). THE OPTIONAL FIELDS ARE GONE: weight has a real home in the body log (one canonical weigh-in, not a second buried in a check-in), adherence is countable off the plan rather than worth asking for, and three blank boxes under a finished check-in made the card look unfinished when it was not. Share-with-coach stays — it is an entitlement control, not an optional data field. Both clients, i18n EN/PL/DE." },
  { id: "today-cockpit-ia", area: "Core", status: "shipped", title: "Today ⇄ Cockpit split — daily loop vs command center (no redundancy)", detail: "Today and the Cockpit no longer duplicate each other. TODAY (apps/web/components/aurora/today.tsx + apps/mobile/components/aurora/home.tsx) is now a GUIDED DAILY LOOP, walked top-to-bottom with section kickers: a time-of-day greeting + streak chip → TRAIN (today's session ⇄ AI-coach pager + quick sport log) → RECOVER – FEEL (a slim on-track/accountability strip + the check-in & nutrition SQUARE widgets) → PLAN (the reconciled This-week plan, or a follow-a-plan nudge for casual) → CONNECT (the coach rail + the friends' Feed strip). The strategic/analytical layer was MOVED OFF Today onto the COCKPIT (apps/web/components/aurora/cockpit.tsx + apps/mobile/components/aurora/cockpit.tsx, athlete/Full command center): the Performance Twin (HPI) now carries the 14-day sparkline + a readiness dial, and the injury-risk-by-tissue panel and the weekly recap ('Your week') moved there too — so HPI/readiness, injury risk, the season timeline and the recap live in exactly ONE place. The Today 'sell Full' upsell card and the free season-brief were dropped (the Cockpit is the paid hub; the AI-coach card + browse-plans still pitch upgrade). SQUARE WIDGETS: the check-in + nutrition quick-actions are now two iPhone-home-screen-style SQUARES side by side (apps/web/components/aurora/today-quick.tsx + apps/mobile/components/aurora/today-quick.tsx) — each shows a glanceable state (have you checked in today? today's kcal vs adaptive target + macros, read from /api/checkins + /api/signals via todayNutrition/adaptiveTargets) and OPENS THE FULL Check-in / Nutrition screen on tap (logging lives there, so the widgets stay small). Web + mobile at parity; new i18n keys under w.home.today.* + w.home.today.w.* (EN/PL/DE); accountability stays Today-only, the recovery analytics stay Cockpit-only. Manual macro entry only — the food DB + barcode scan remain the blocked nutrition-fooddb layer. PREMIUM-TODAY FIX: a premium (athlete) user WITHOUT an enrolled plan no longer gets a fabricated AI 'Back Squat + Assault Bike' hero on Today — everyone with logged history but no plan now routes into LOGBOOK MODE (their real last-7-days calendar rail + the Follow-a-plan / Build / Just-train chooser), so the home hero is always the plan/calendar or a path to one, and the readiness-driven daily prescription stays on the analytical Cockpit (the removed AI branch also took its awkward top-right Start+readiness cluster with it). COCKPIT VOICE: the Cockpit header is now a LIVING MASTHEAD in Today's editorial idiom (mono season caption + one oversized 'Cockpit' headline + a warm sub) and the w.home.cockpit.* copy was de-slopped — dropped 'command center', the A→B→C arrow chains, and the feature-list-as-sentence blurbs — so it reads as Today's sibling (EN/PL/DE)." },
  { id: "compliance", area: "Core", status: "shipped", title: "Planned-vs-actual compliance", detail: "compliance.ts: weekly planned-vs-actual adherence from logged sessions — this week's completion %, status (on-plan/under/over), a compliant-week streak (≥80% of target), and a 4-week history. Plus dailyChecklist (habits.ts) for the consumer daily 'rings' (trained / nutrition / check-in today, derived — no new storage). Pure + unit-tested. Surfaced on the Check-in screen and the Fitness GPS home." },
  { id: "persona-shape", area: "Core", status: "shipped", title: "Persona-aware app shape (nav by who you are)", detail: "The app shapes itself to the customer instead of showing one flat 28-item menu to everyone. @hybrid/core/nav.ts defines a nested Persona model — casual (Average Joe: a frictionless logger + share loop), athlete (the data/technique athlete: casual PLUS depth — periodize, sport, velocity, running, performance, video, connections), coach (athlete PLUS the console — roster, squad, team compare, org, tactical), admin (everything); casual ⊂ athlete ⊂ coach ⊂ admin. Each NAV_ITEM carries a minPersona; resolvePersona(role, clientChoice, entitlement) derives it from the auth role, a CLIENT's mode choice, and the account's billing entitlement, and navForPersona/navVisibleTo filter the surface (pure + unit-tested). Both clients carry it. The choice persists per-device (mobile lib/persona.ts on AsyncStorage; web lib/persona.ts on localStorage — each a useSyncExternalStore store + a usePersona hook that reads the session role) and is reversible (mobile: a Simple/Full toggle in More AND in Settings → Subscription/Mode; web: a Mode toggle in Settings; both client-only — coach/admin come from the role, and Full is entitlement-gated so an unpaid tap on Full routes to /upgrade). SET at onboarding: a 'How do you want to use HYBRID?' fork (Just track / Train for a goal) on both the mobile and web onboarding. SURFACED: the mobile More hub + CommandMenu tiles, and the web sidebar nav + ⌘K hub, all filter through navForPersona(persona); and the HOME shapes itself too — the mobile Today + web Today gate the athlete-depth cards (plan setup, This week reconciled plan, Future Self projection, Athlete Twin/HPI) behind the athlete persona, so a casual user gets the lean Start→route→on-track→share home while an athlete/coach gets the full cockpit. So a casual user no longer sees Coach/Squad/Velocity/Periodize anywhere; an athlete gets the depth; a coach gets the console. DEFAULT POLICY (the curated 'done deal', fully admin-overridable in Access control): CASUAL = the lean loop PLUS the free pre-built plan library (today, log, history, calendar, nutrition, progress, check-in, plans, settings — browse, enroll & FOLLOW a pre-built plan AS WRITTEN is free, and Today renders the enrolled plan's session for the day to a casual user (a single 'Unlock Full to auto-adjust loads to your recovery' strip is the only upsell, plus a 'Follow a plan — free' nudge when none is enrolled); what's PAID is the adaptive layer — periodizing the season + readiness-adjusted auto-progression + the Twin/analytics cockpit; onboarding is a flow, not a nav item); ATHLETE adds the cockpit + all depth/analytics (periodize, sport, competition, performance, velocity, running, force-plate, video, longevity, talent, connections, builder — the old Dashboard is retired; its surfaces live on Today); COACH adds the console (coach, squad, team-compare, org, tactical); ADMIN all. The casual set is pinned by a nav unit test so it can't silently drift. MOBILE PARITY OF ACCESS: the mobile app has fewer deep screens than web, so the More hub also shows a 'More in the web app' section listing exactly the features the user's persona allows that live on web only (with an 'Open the web app' link) — so access is never silently invisible on mobile. ACCESS TIERS (revised): the three modes are now ENTITLEMENT-GATED, not free self-select. (1) SIMPLE (casual) = free, the default. (2) FULL (athlete) = PAID: ClientPersona is casual|athlete only, and resolvePersona returns 'athlete' only when the client chose Full AND carries a paid Entitlement ('free'|'paid', read from the auth session.user_metadata on both clients). An unpaid client who taps Full hits the Upgrade/paywall screen (mobile app/upgrade.tsx; web account-settings note) instead of switching — the real purchase flow (Stripe / App-Store IAP) is still BLOCKED (see the 'full-billing' capability + its sub-items). COACHED (read-only): a client linked to an ACTIVE coach stays casual (free) but additionally gets a READ-ONLY view of everything the coach assigned (plans, programs, diet) via the useHasActiveCoach flag — a coach link NEVER grants Full; to edit/add or self-program the client makes their own paid upgrade (so the seat can't be used to obtain Pro for free). (3) COACH = VERIFIED only: coach is no longer a self-serve ClientPersona — the coach surface comes solely from a verified COACH role. A client applies via 'Become a coach' (mobile app/coach-apply.tsx; web Settings) → POST /api/coach/apply → an admin reviews the queue in Governance and approves, promoting the role to COACH (audited; mirrors the AccessRequest pattern — see the 'coach-verification' capability). Coach data APIs remain relationship-gated (squad/roster/compare filter by the caller's OWN CoachLinks — 'enforced by relationship, not role label'). INCOMING invites are still surfaced to EVERY persona — a CoachInviteBanner in the web app-shell + a Today invite card on mobile (fetch /api/coach/links asClient PENDING → Accept/Decline PATCH) — so any client can complete a mutual-consent link. See reference/roadmap.md + persona-gap-matrix.md." },
  { id: "coach-assign-plans", area: "Backend", status: "shipped", title: "Coach assigns plans to clients + client groups (bulk)", detail: "A coach builds/picks a plan and pushes it straight into a client's account — and into a whole GROUP of clients at once. PER-CLIENT: /api/coach/links/[id]/macrocycle now also accepts an optional named planId (on top of goal), so an active coach persists a full periodized season into the client's account; their Today/Periodize reflect it. GROUPS (the solo-coach version of segmentation, NOT the enterprise Org Graph): a lightweight CoachGroup model (coach-owned, holds client user ids; prisma + reference/sql-coach-groups.sql) with /api/coach/groups (GET/POST), /api/coach/groups/[id] (PATCH rename/set-members, DELETE) and /api/coach/groups/[id]/assign-plan (POST goal[,planId] → buildMacrocycle fanned out to every member who is still an ACTIVE client, via createMany). Membership is re-validated against ACTIVE CoachLinks on every write/assign, so a group can never grant access to a non-consented client. Every group endpoint SOFT-DEGRADES to 'not enabled yet' (503 / unavailable flag) until the table exists, so the deploy is safe in any order. COACHED CLIENTS ARE READ-ONLY (not Full): a client with an ACTIVE coach STAYS on the free/casual tier and gets a READ-ONLY view of what the coach assigned (plans, programs, diet) — surfaced via the useHasActiveCoach flag on both clients. resolvePersona NO LONGER elevates a coached client to athlete (the old hasActiveCoach→athlete path was removed); editing/adding and the adaptive engine remain a paid (athlete) upgrade, so a coach link can never grant Pro for free. A coached client who wants to self-program upgrades like any other consumer. UI: web Coach screen gets a 'Client groups' section (create/rename/delete, toggle members, assign a plan by goal to the whole group); mobile parity via a shared components/coach-groups.tsx rendered in BOTH the classic and Aurora coach tabs. Packaging: client groups + bulk assignment sit in the Coach PRO seat ($79); the full Org Graph (roles/medical/team subtrees) stays Business. Write routes are body-limit guarded (readJsonLimited) and the 'Client groups' section is admin-gateable via the coach.groups feature flag (web + mobile). NOTE: run reference/sql-coach-groups.sql in the Supabase SQL Editor to persist groups (and reference/sql-coach-macrocycle.sql for the coach-write macrocycle RLS if not already applied)." },
  { id: "plan-program-today", area: "Core", status: "shipped", title: "Discipline-shaped programs drive 'Your plan today'", detail: "Enrolling in a discipline-shaped PlanProgram (Soviet OWL %-block, 6-day PPL, 5K running, Saturday kettlebell circuit, 12-week kettlebell) now DRIVES the daily 'Your plan today' card — the enrolled plan's real session, not the engine's fallback prescription. planProgramToday(planId, sessionsLogged, maxes?) in plan-day.ts resolves the program via programFor(), flattens its weeks→days across the whole program (skipping pure REST days; AM/PM sessions kept and prefixed), and picks the current day by how many sessions the athlete has logged (cycling at the end — the same model as the legacy planToday). It returns BOTH a display view (rows: name + the SAME formatted prescription the Plans-library card shows, reusing planProgramView — %×reps×sets–kg, '4×8 – 80 kg – @9', or the prose run — plus any complex/tempo note) AND a prefill (blocks): each PlanLift %-ramp expands step-by-step into strength sets with kg DERIVED from the athlete's 1RM when a `maxes` map is supplied (else blank loads to fill live, via stepKg); structured hypertrophy/scheme entries → strength blocks; prose endurance/conditioning entries → a named conditioning block. Wired into BOTH Today cards (web today.tsx + mobile home.tsx render plan.rows + plan.day incl. a Week-N prefix for multi-week plans + a rest/competition kindLabel) and the live-logger 'Start' (web onStart(plan.blocks); mobile source=plan → planProgramToday(...).blocks in app/workout.tsx) — so a CASUAL (free) enrolled user sees and starts their real plan day (following a pre-made plan as written is free), which also fixes 'I can't tell whether I'm enrolled'. Pure + unit-tested (plan-day.test.ts: resolves an enrolled program, advances/cycles by sessions logged, derives kg from maxes). MAXES PERSISTENCE (shipped): the athlete's 1RMs entered on the Plans 'fill in your numbers' panel now persist on-device (a flat athlete-level `{ inputKey: 1RM }` map keyed by ProgramInput.key, shared across plans) via a small reactive store on each client — web lib/plan-maxes.ts (localStorage + usePlanMaxes/setPlanMax) and mobile lib/plan-maxes.ts (AsyncStorage-hydrated, mirrors the persona store). The Plans panel seeds each input from the store and writes on change; Today (web today.tsx + mobile home.tsx) reads it via usePlanMaxes and passes it to planProgramToday, and the live-logger source=plan prefill reads it via readPlanMaxes — so once you enter your maxes, 'Your plan today' and the prefilled session show REAL working kg (derived off the %-ramp), not just the %. CROSS-DEVICE SYNC (shipped, SQL-gated): the maxes also persist on the account — a JSONB User.planMaxes column (reference/sql-plan-maxes.sql) read/written via GET+PUT /api/plan-maxes using RAW queries (so it soft-degrades to on-device-only before the column is migrated). Each client hydrates once on first use (merging the server's values over local — server wins per key) and writes the whole map back on every change, and both clear it on sign-out (resetPlanMaxes in session.tsx, mirroring resetPersona) so a shared device never leaks one athlete's maxes. So maxes entered on one device appear on the others once the SQL is applied. LIFT DISCLOSURE (Jul 2026): the Today card's lift list leads with the first lift CLEAR and frosts the next THREE behind a 'Show all N lifts →' reveal — a REAL blur (CSS `filter: blur` + a gradient veil on web today.tsx; an expo-blur BlurView + expo-linear-gradient veil on mobile home.tsx), replacing the old bare ▾ text toggle — so the card teases 'there's more' at a fixed height; expanding lifts the frost and unfolds the remaining rows, collapsing via a quiet 'Hide lifts'. On a plan the primary START action is a full-width button anchored at the BOTTOM of the card (below the [note]); the top row then carries only the readiness dial (athlete), while other Today states keep the compact top-right Start. Both clients at parity; reuses the existing showAllLifts/liftsWord/hideLifts i18n and introduces no middot separator (honours the separator-purge rule in design-system-unification). Explored + verified as reference/glass-card-frosted.html (CSS-only checkbox prototype). REMAINING: NL/intensity-zone day targets on the card are still to surface. See reference/plan-model-redesign.md." },
  { id: "plan-page-hero", area: "Core", status: "shipped", title: "Plan detail — 'The Columns' hero (gradient panel + editorial stats)", detail: "The discipline-shaped plan detail page (the PercentDetail/PercentProgram renderer used by every PlanProgram) opens with a designed hero instead of a small title over a pipe-y meta line — the winning concept ('The Columns') from design/plan-page-redesign-ideas.html. Anatomy, web + mobile at parity: a rounded GRADIENT PANEL in the accent tint (web CSS radial+linear color-mix over ink2, mobile expo-linear-gradient over ink2 — both derive from the theme lime token so the Kyoto-Hour light theme gets a pine/sage wash automatically) carrying a square back chip + the plan's loading tag as a mono label (right), a CHALK-filled goal chip (auto-inverts per theme), and the plan name set big (900/clamp 30–38 web; F.black 33 mobile); then THREE RULE-TOPPED STAT COLUMNS (duration, frequency, and the discipline's own volume metric) and a one-line BLURB. All content comes from ONE shared view model — planHeroView(plan, program) in @hybrid/core plan-program.ts (unit-tested): stats are DERIVED, never authored (weeks + sessions from the plan meta; the third column is week-1 NL for strength-percent ('656 lifts in wk 1' for the Soviet block), week-1 exercise count for hypertrophy/conditioning, total sessions for endurance which has no comparable count; the 'in wk 1' suffix drops on single-week plans), the nav label is the plan's tag ('% of 1RM'), and the blurb is the first sentence of the plan description. The old header MetaLine (weeks │ ×/wk │ tag │ peak-note) is superseded by the columns on this page; the maxes panel, week selector, ProgramDays, progression card and enroll CTA below are unchanged. The CLASSIC (non-program) plan Detail is migrated too: the hero is a shared per-client component (web PlanHero in aurora/plans.tsx; mobile components/plan-hero.tsx) used by BOTH detail renderers, planHeroView's program param is optional (no program → the volume column falls back to total sessions), and the classic header's level fact moved into the info-card grid as a new Level card (i18n w.train.plans.level, en/pl/de) so nothing from the old meta line is lost." },
  { id: "coach-program-builder", area: "Backend", status: "shipped", title: "Coach-authored multi-week program builder", detail: "Type 3 of the plan model (see plans-lib): a coach composes their OWN multi-week program — weeks → days → exercises (name + sets×reps + RPE) — and assigns it to a client or a whole group, which MATERIALIZES it into dated Assignments (week i, day j → start + (i*7 + j) days) on the athlete's account, surfaced via the existing 'Assigned by your coach' Today card + calendar. CoachProgram model (coach-owned; weeks stored as JSON PlanSample[][], reusing the core plan shape so planDayToBlocks() turns each day into session blocks) + reference/sql-coach-programs.sql. APIs: /api/coach/programs (GET/POST), /api/coach/programs/[id] (PATCH/DELETE), /api/coach/programs/[id]/assign (POST {linkId|groupId, startDate} → createMany of the materialized assignments; targets re-validated as ACTIVE links). apps/web/lib/coach-program.ts holds the shared sanitizeProgramWeeks (clamps ≤26 wk / ≤7 days / ≤20 items) + programAssignments materializer. UI: a 'Programs' section in the Coach screen on BOTH clients — web components/coach-programs.tsx and mobile components/coach-programs.tsx (rendered in the classic AND Aurora coach tabs, on the template-aware Card) — list/create/edit (nested week/day/exercise editor)/delete + an assign control (client or group + start date). Every endpoint soft-degrades to 'not enabled yet' until the SQL runs; write routes are body-limit guarded (readJsonLimited) and the 'Programs' section is admin-gateable via the coach.programs feature flag (web + mobile). NOTE: run reference/sql-coach-programs.sql in the Supabase SQL Editor." },
  { id: "coach-verification", area: "Backend", status: "shipped", title: "Verified-coach application (coach is no longer self-serve)", detail: "Coaching others is now gated on a verified COACH role instead of a free persona toggle. A client applies via 'Become a coach' (mobile app/coach-apply.tsx → applyForCoach; web account-settings) → POST /api/coach/apply (own-row upsert, rate-limited, body-capped; rejects existing coach/admin) writes a CoachApplication (pending). An admin reviews the queue in Governance (components/admin/coach-applications.tsx) and approves/denies via PATCH /api/admin/coach-applications/[id] — approve promotes the user's role to COACH in one transaction and audits it (coach.approve); deny audits coach.deny. Mirrors the AccessRequest pattern (admin guard, audit, RLS). NOTE: run reference/sql-coach-applications.sql in the Supabase SQL Editor to create the CoachApplication table; until then the apply/queue routes soft-degrade to an 'aren't enabled yet' message so the deploy is safe in any order." },
  { id: "coach-assign-diet", area: "Backend", status: "shipped", title: "Coach-assigned diet (macro targets, read-only for the client)", detail: "The nutrition analogue of an assigned training plan: a coach sets a client's daily macro targets (kcal / protein / carbs / fat + note) and the client VIEWS them READ-ONLY on their Nutrition screen, above their own free macro logging (editing their own adaptive targets stays the paid upgrade). CoachDiet model (coach-owned, one per coach↔client; prisma + reference/sql-coach-diet.sql) with @@unique([coachId, clientId]). APIs: GET/POST /api/coach/links/[id]/diet (CoachLink-gated: coach owns + ACTIVE; upsert, clamped/body-limited) and GET /api/nutrition/assigned (the client reads the diet assigned by an ACTIVE coach — ending the link withdraws it). UI: a shared CoachDiet editor (web components/coach-diet.tsx + mobile components/coach-diet.tsx) in the client-detail view of the classic AND Aurora coach screens; the client-side read-only band on the web + mobile Nutrition screens. Soft-degrades to 'not enabled yet' until reference/sql-coach-diet.sql is run. NOTE: v1 is macro targets; a full structured meal plan is a later expansion." },
  { id: "coach-client-invite", area: "Backend", status: "shipped", title: "Coach-led client onboarding (invite by link / QR / email)", detail: "A coach can add a client who isn't on HYBRID yet. ONE CoachInvite (prisma + reference/sql-coach-invites.sql) backs all delivery methods: a copyable invite LINK, a QR code the client scans, or an EMAIL (stored for auto-match). The client CLAIMS it on first sign-up — by opening the link/QR (web app/invite/[token]/page.tsx; mobile app/invite/[token].tsx) OR an email auto-match in /api/me (claimPendingCoachInvites mirrors the org-invite claimPendingInvites) — which creates an ACTIVE CoachLink (the act of claiming IS the consent; decided 2026-06-19). The claimed client lands on the FREE plan with read-only access to what the coach assigns (see persona-shape: coached = casual + read-only). APIs: POST/GET /api/coach/invite (create/list, coach-only, roster-capped, body-limited), GET/DELETE /api/coach/invite/[token] (public-by-token info for the claim page / coach revoke), POST /api/coach/invite/[token]/claim (signed-in claim → ACTIVE link, single-use, expiry-checked). If the email already belongs to a HYBRID user it falls back to the existing-user PENDING-link path. A stashed token is also auto-claimed post-login (web CoachInviteBanner; mobile session provider). UI: shared CoachInvite component on BOTH clients (web components/coach-invite.tsx — QR via the qrcode lib's toDataURL; mobile components/coach-invite.tsx — QR drawn as a View matrix from qrcode's module data, no native dep so the iOS export stays green; Share API for the link) rendered in the classic AND Aurora coach screens. Tokens are random (18-byte hex), single-use, 30-day expiry. NOTE: run reference/sql-coach-invites.sql in the Supabase SQL Editor; until then every invite endpoint soft-degrades to 'not enabled yet'. Native deep-link app-open (QR → app rather than web) lands with the EAS build (see coach-invite-sms/mobile-preview); today the QR encodes the web URL which works now." },
  { id: "coach-invite-sms", area: "Integrations", status: "blocked", title: "Coach invite — automated SMS/phone delivery", detail: "The invite model already stores a client phone; v1 delivery is the QR + copyable link (the coach sends it via their own channel) plus email auto-match. Automatically TEXTING the invite link needs a third-party SMS provider.", blockedBy: "An SMS provider account + credentials (e.g. Twilio ACCOUNT_SID/AUTH_TOKEN + a sending number, or Vonage) in the server env, then a send step in POST /api/coach/invite when a phone is supplied. Carries per-message cost." },
  { id: "coach-invite-email", area: "Integrations", status: "shipped", title: "Coach invite — automated email delivery", detail: "When a coach supplies an email on a pre-account invite, POST /api/coach/invite now SENDS the invite link directly via the email system (sendEmail, transactional) — the coach no longer has to copy/share the link by hand (the link/QR are still returned as a fallback, and the email still auto-matches on sign-up). Best-effort: the response carries emailSent so the UI can confirm. Runs on the same Resend config as email-marketing; if email isn't configured it silently falls back to the manual link (no failure). The SMS analogue is still blocked (see coach-invite-sms)." },

  { id: "coach-verified", area: "Backend", status: "shipped", title: "Verified-coach tick", detail: "A coach whose credentials an admin has vetted carries a verified badge — User.coachVerified (prisma + reference/sql-coach-verified.sql). Set automatically to true when an admin APPROVES a coach application (apps/web/app/api/admin/coach-applications/[id]) and togglable on the admin Users → user record (the user PATCH route + the drawer's 'Verified coach' checkbox, shown for COACHes; audited). Surfaced as a '✓' next to the coach's name wherever a CLIENT sees them, on BOTH clients in BOTH templates: web classic + Aurora coach screens ('Your coach' + incoming coaching requests) and the web CoachInviteBanner; mobile classic + Aurora coach tabs. The flag rides on the coach object returned by GET /api/coach/links (and /api/me for the coach themselves). Soft-degrades until reference/sql-coach-verified.sql is run (the column simply reads false). NOTE: run reference/sql-coach-verified.sql in the Supabase SQL Editor." },

  { id: "admin-user-plan", area: "Backend", status: "shipped", title: "Admin sets a user's plan (Free ⇄ Premium)", detail: "An admin can change any account's entitlement between free and paid from Admin → Users → the user record (a Plan select in the drawer, plus a Free/Premium add-user default and a Plan column + chip in the table). PATCH /api/admin/users/[id] routes the change through setEntitlement (apps/web/lib/billing.ts) so it updates the DB entitlement AND mirrors 'paid'/'free' into the Supabase auth user_metadata — both clients unlock/lock Full from their session, exactly like a real purchase — and fires the `upgraded` lifecycle email automation when moving to paid. The change is audited (before/after). The auth-metadata mirror needs SUPABASE_SERVICE_ROLE_KEY to propagate to the clients (the DB always updates regardless — see entitlement-mirror)." },

  { id: "admin-user-delete-wipe", area: "Backend", status: "shipped", title: "Delete user — full record wipe (+ auth account)", detail: "Admin → Users → Delete now wipes EVERYTHING the account touches, not just the headline tables. DELETE /api/admin/users/[id] cascades sessions/macrocycles/biometrics/signals/checkins/rtp/videos/events/risk/talent/connections/memberships/templates/assignments/coachNotes/coachLinks AND the previously-missed coach-owned artefacts (coachGroups, coachPrograms, coachInvites incl. claimed, coachDiets as coach AND client), the coachApplication, and the user's email footprint (enrollments + the EmailMessage ledger — the EmailSuppression opt-out row is deliberately KEPT to honour an unsubscribe/bounce permanently per CAN-SPAM/GDPR) — then the User row. It ALSO now deletes the Supabase AUTH user via the service-role admin client (previously the code claimed to but didn't, leaving an orphaned login), best-effort + reported in the response/audit (authDeleted: deleted|skipped|failed). The same last-admin / self-delete lockouts and the type-the-email confirm remain. CONFIRM UX (Jul 2026): the destructive confirm is now a designed in-app modal (alertdialog) on web — it names the account, spells out the cascade, gates the button behind typing the email back, and shows a LIVE spinner while the request is in flight so 'Deleting…' never reads as stalled (mobile mirrors this with a native destructive Alert + a spinner on the busy button). The old bare window.prompt is gone. Auth deletion degrades to a no-op without SUPABASE_SERVICE_ROLE_KEY (the DB wipe is authoritative)." },

  { id: "admin-user-create", area: "Backend", status: "blocked", title: "Admin creates a user (real loginable account)", detail: "Admin → Users → '+ Add user' provisions a brand-new account: a modal takes email, name, role, plan (free/premium), language, an optional initial password and a 'send verification/welcome email' toggle. POST /api/admin/users creates a REAL Supabase auth user via the service-role admin API (so the person can actually sign in — confirmed immediately if a password is set, otherwise a magic-link verification email is sent), mirrors it into the User row in the same flow (cleaning up the auth user if the DB write fails), enrolls them into any active `signup` email sequence, and audits it. Code-complete; degrades to a clear 503 until the service-role key is configured.", blockedBy: "SUPABASE_SERVICE_ROLE_KEY is now set in the deployment — the only thing left is to confirm a live create (add a user with a test email) to flip this to shipped. STANDING DEPENDENCY (don't forget): account creation needs the Supabase service-role key, and the welcome/verification email rides on Resend (see email-marketing); keep both set in every environment." },

  { id: "email-marketing", area: "Integrations", status: "retired", title: "Email — marketing & lifecycle automation", detail: "DELETED (Aug 2026) — the MARKETING half only. Was: audience segments (all/free/paid/coaches/clients/admins), lifecycle triggers (signup/inactive/trial_ending/upgraded/coach_approved/manual), campaigns with resumable batched fan-out, multi-step sequences with per-step delays, enrollment, an unsubscribe HMAC + suppression list, an hourly cron worker and an admin Email & marketing console. Removed: all of it — the admin console, /api/admin/email/*, /api/cron/email, /api/email/unsubscribe, every enrollInTrigger call site, and the EmailCampaign / EmailSequence / EmailSequenceStep / EmailEnrollment / EmailSuppression tables (drop script: reference/sql-strategy-cuts-2026-08.sql). KEPT AND STILL WORKING: transactional mail — sendEmail() over the Resend HTTP API, account verification, the coach-invite email (see `coach-invite-email`), the merge-tag renderer in core, and the EmailMessage deliverability ledger.", retiredBecause: "We built Customer.io. Buy Customer.io (or Loops, or Resend Broadcasts) when there is an audience to send to — a lifecycle platform with zero users is pure maintenance surface." },
  { id: "email-batch-send", area: "Integrations", status: "retired", title: "Email — batch/chunked sending for large audiences", detail: "NEVER BUILT, now moot. It was the scale story for campaign + sequence fan-out (Resend's Batch API, a concurrency limiter, resumable send progress). Campaigns and sequences were themselves cut (see `email-marketing`), and transactional mail is one send per event — there is nothing left to batch.", retiredBecause: "Its parent capability was retired. Bulk sending is a bought problem now, not a built one." },
  { id: "full-billing", area: "Backend", status: "blocked", title: "Paid entitlement / subscription billing (consumer Full upgrade)", detail: "The Full (athlete) experience is a PAID tier — resolvePersona only grants the athlete persona when the account carries a paid Entitlement, read from auth session.user_metadata.entitlement on both clients (default 'free'). The ENTIRE flow is now code-complete — only external credentials remain, tracked as the three sub-items below (full-billing-stripe, entitlement-mirror, full-billing-iap). WEB (Stripe): apps/web/lib/billing.ts (lazy Stripe client, entitlement helpers) + routes POST /api/billing/checkout (hosted Checkout subscription), POST /api/billing/portal (manage/cancel), GET /api/billing/status, and POST /api/billing/webhook (signature-verified; checkout.session.completed + customer.subscription.* → setEntitlement). account-settings shows an 'Upgrade to Full' button (free) and 'Manage subscription' (paid). MOBILE: app/upgrade.tsx 'Subscribe' button → startCheckout() opens the hosted Checkout; POST /api/billing/iap/verify is built on the App Store Server API (StoreKit 2) — JWS-signature-verified transaction lookup, product/expiry checked — for the native IAP path. PROPAGATION: setEntitlement writes the DB User (entitlement/stripeCustomerId/subscriptionStatus columns — reference/sql-billing.sql) AND mirrors entitlement into Supabase auth user_metadata via a service-role admin client (lib/supabase/admin.ts), so both clients read it from their session (web auto-refreshes on /app?upgraded=1). Every entry point degrades to a 'not configured' 503 until the keys are set, so the deploy is safe meanwhile. DEVICE CHOICE NEVER OUTRANKS THE ACCOUNT: the Simple/Full mode choice is stored per-device (localStorage / AsyncStorage) while the entitlement lives on the account, so both persona stores now pipe the stored choice through core's effectiveClientChoice() before resolvePersona — a 'casual' recorded while the account was still FREE (the onboarding answer) is dropped the moment the account is paid. Without it, anyone who became paid other than by tapping upgrade in that very app instance (bought on the web then opened the phone, restored an IAP, or was granted Full by an admin) kept the free surface on every device holding the old answer — Cockpit/Performance, HPI and Recipes locked behind the teaser with 'Switch to Full' as the only escape. A Simple mode chosen DELIBERATELY while paid (Settings → Mode) is recorded as such and still honoured. NOT to be confused with 'billing' (coach/SMB monetization — a separate revenue layer).", blockedBy: "Three external dependencies, each tracked as its own capability below: full-billing-stripe, entitlement-mirror, full-billing-iap. Plus run reference/sql-billing.sql in the Supabase SQL Editor. See .env.example." },
  { id: "full-billing-stripe", area: "Backend", status: "blocked", title: "Full upgrade — Stripe keys + webhook (web checkout)", detail: "The web Stripe path is built (apps/web/lib/billing.ts + /api/billing/checkout, /portal, /status, /webhook) and powers the 'Upgrade to Full' / 'Manage subscription' buttons in account-settings. It degrades to a 503 'not configured' until the keys land, so deploying now is safe.", blockedBy: "A Stripe account, then set in the server env: STRIPE_SECRET_KEY, STRIPE_PRICE_FULL (the Full subscription Price id), STRIPE_WEBHOOK_SECRET, and NEXT_PUBLIC_APP_URL (redirect origin). Create a Stripe webhook pointed at POST /api/billing/webhook subscribed to checkout.session.completed + customer.subscription.* . See .env.example." },
  { id: "entitlement-mirror", area: "Backend", status: "blocked", title: "Full upgrade — service-role key to push 'paid' to the clients", detail: "On a successful purchase (or an admin plan change — see admin-user-plan), setEntitlement updates the DB AND mirrors the entitlement into Supabase auth APP_METADATA via the service-role admin client (apps/web/lib/supabase/admin.ts patchUserAppMetadata) so both clients read 'paid' straight from their session. SECURITY (2026-07 audit, H-4): the mirror moved from user_metadata to app_metadata — user_metadata is writable by the end user via supabase.auth.updateUser({ data }), so mirroring a paywall flag there was a self-upgrade trap; app_metadata is service-role-only. Clients read entitlement from app_metadata first (legacy user_metadata fallback), and the DB column from /api/me is the authoritative override. Without the key the DB still updates, but a user wouldn't see Full unlock until they next re-authenticate. SUPABASE_SERVICE_ROLE_KEY is now set in the deployment, so the mirror is live (already exercised by admin-driven plan changes); this stays surfaced only because it's part of the broader full-billing flow that still needs Stripe (see full-billing-stripe).", blockedBy: "SUPABASE_SERVICE_ROLE_KEY in the server env (Supabase → Settings → API → service_role) — now set. Server-only — never exposed to the browser." },
  { id: "full-billing-iap", area: "Mobile", status: "blocked", title: "Full upgrade — Apple In-App Purchase (iOS)", detail: "POST /api/billing/iap/verify is built on the modern App Store Server API (StoreKit 2), migrated off the deprecated verifyReceipt/shared-secret path. The native client posts a transactionId; apps/web/lib/apple-iap.ts authenticates to Apple with an App Store Connect API key (.p8, JWT-signed by @apple/app-store-server-library), fetches the signed transaction and CRYPTOGRAPHICALLY VERIFIES its JWS signature chain back to an Apple root CA before pinning the product id + unexpired window and calling setEntitlement('paid'). Config is env-driven + lazy, root certs loaded from APPLE_ROOT_CERTS_DIR (apps/web/lib/apple-root-certs, see its README), so the route degrades to a 503 'not configured' until everything is set — deploy-safe. The Apple .p8 sandbox key is in hand (Key ID RQTCHVF25S) and Apple's public root CA certs (G3/G2/Inc) are committed under apps/web/lib/apple-root-certs. The NATIVE IAP CLIENT IS NOW BUILT: react-native-iap v15 (Nitro/StoreKit 2) + its Expo config plugin, apps/mobile/lib/iap.ts (purchaseFull → requestPurchase subs → on success POSTs the transactionId to /api/billing/iap/verify → finishTransaction), wired into app/upgrade.tsx (iOS uses native IAP, web/Android fall back to hosted Stripe Checkout) and the session refreshes so the mirrored 'paid' entitlement unlocks Full immediately. Mobile typecheck + iOS bundle export both green. APP-STORE COMPLIANCE HARDENING (2026-07 audit): the paywall + IAP now meet Apple's auto-renewable-subscription requirements — (1) a LAUNCH-TIME transaction listener (startIap in app/_layout.tsx) verifies + grants + finishes any transaction that completes with no purchase UI open (interrupted / Ask-to-Buy / renewal / another-device), so paid-but-not-granted states can't happen; (2) a 'Restore purchases' button on the paywall (restorePurchases → getAvailablePurchases → server verify); (3) the paywall shows the LOCALIZED StoreKit price (fetchFullPrice → fetchProducts) instead of a hardcoded $9.99; (4) Terms + Privacy links on the paywall (shared LegalLinks); (5) 'Manage subscription' opens Apple's native sheet (showManageSubscriptionsIOS) on iOS instead of looping back to the paywall. The remaining work is all external/operational, not code.", blockedBy: "(1) Set the server env (see .env.example): APPLE_IAP_PRIVATE_KEY (the .p8 contents), APPLE_IAP_KEY_ID, APPLE_IAP_ISSUER_ID, APPLE_IAP_BUNDLE_ID, APPLE_IAP_PRODUCT_FULL (+ APPLE_IAP_APP_APPLE_ID for Production). (2) Create the auto-renewable 'Full' subscription product in App Store Connect (id must match APPLE_IAP_PRODUCT_FULL / EXPO_PUBLIC_IAP_PRODUCT_FULL). (3) An EAS build to run the native module on-device (needs an Expo token) — react-native-iap can't run in Expo Go or the web bundle. Apple Developer account is now OBTAINED (no longer a blocker); first sandbox purchase is the live test." },
  { id: "account-settings-pro", area: "Web", status: "shipped", title: "Professional account settings (General / Security / Privacy / …)", detail: "SHIPPED v1 — the web Settings (components/account-settings.tsx) is now a professional TABBED account area (General – Security – Coaching & access – Privacy & data) instead of one flat stack, preserving every existing flow. GENERAL: a new Account identity card (name/email + role + Free/Full-paid + provider badges, sign out) and a new Preferences card with REAL working controls — Appearance (dark/light via use-theme), Language (EN/PL/DE via useLang) and the Workout-logger Simple/Detailed toggle (logger-settings) — plus the existing Mode + billing (upgrade/manage). SECURITY: the existing MFA settings + an honest password/sessions note (provider-aware). COACHING & ACCESS: the existing Become-a-coach application + Request-a-feature. v2 FILLED THE GAPS (all SQL-free, on the user's own Supabase auth — no admin/backend tables): a 6-tab area (General – Notifications – Privacy – Security – Coaching & access – Data). GENERAL now has an EDITABLE PROFILE (display name → user_metadata, change email → auth.updateUser) on top of identity + the real Appearance/Language/Logger toggles + Mode/billing. NOTIFICATIONS + PRIVACY are real persisted preference sections (weekly recap / coach messages / check-in reminders / product updates; share-detail-with-coach / discoverable-in-talent / analytics-opt-out) stored in auth user_metadata so they sync across the user's devices (microcopy is honest: saved + honoured as channels roll out). SECURITY: MFA + change-password (auth.updateUser) + 'Sign out everywhere' (global signOut). DATA: 'Download my data (JSON)' (GET /api/account/export — read-only, gathers every user-owned table best-effort) above the Danger-zone reset. Everything degrades cleanly in demo mode (no Supabase). MOBILE PARITY (shipped): the mobile Settings — BOTH the classic (app/settings.tsx) and Aurora (components/aurora/settings.tsx) variants — now carry the same sections: editable profile (display name + change email), Notifications + Privacy preference toggles, change-password + Sign-out-everywhere (Security), and Download-my-data (the export JSON is fetched with the Bearer token, written to a cache file via expo-file-system and opened through the expo-sharing sheet). The notification/privacy row defs + keys are shared in @hybrid/core (account.ts — ACCOUNT_NOTIF_ROWS/ACCOUNT_PRIVACY_ROWS + defaults) and consumed by BOTH clients, and the mobile logic lives in a shared lib/account.ts hook used by both variants, so web ⇄ mobile can't drift. NEXT (deferred): active-session/device LISTING (needs the service-role admin API), cross-device sync of the per-device logger prefs, and Connections (Apple Health/wearables — the standing EAS/Apple-account blocker). ORIGINAL PLAN: Consolidate today's scattered settings into a proper, sectioned Account area on BOTH clients — web Settings is currently a single ~350-line stack (persona Mode, Become-a-coach, billing Upgrade/Manage, a Danger zone, Sign out) plus a separate MFA component, with language + theme up in the header; mobile has app/settings.tsx. Reorganize into clear sections and FILL THE GAPS: GENERAL/PROFILE (display name, avatar, email, units kg/lb, language, appearance/theme, timezone); ACCOUNT (change email, change password, mode/persona, manage subscription — billing already built); SECURITY (password, two-factor — mfa.ts/account/mfa.tsx already built — plus active sessions/devices + sign-out-everywhere + login history, which are NEW); PRIVACY (profile visibility, what a linked coach can see, analytics/marketing opt-out, consents — NEW); NOTIFICATIONS (push + email prefs: rest alerts, weekly recap, coach messages, check-in reminders — NEW, pairs with the notification infra); DATA (export my data / GDPR, delete account — Danger zone + an export hook already exist); COACHING & ACCESS (become a coach, manage coach links, request feature access — all already built, just re-homed); CONNECTIONS (wearables / Apple Health — future, gated on the wearables work). Reuses the existing pieces (MFA, billing, persona toggle, request-access, danger zone, language, theme) and ABSORBS the configurable-logger settings (logger-settings) as its own section. Shared section definitions + defaults in @hybrid/core so web + mobile stay in lockstep; new prefs persist per-device first then sync via a User-prefs column. Likely split into a few PRs (the shell + reorg first, then each new section). v3 REDESIGN (Instagram-style hub, both clients): the tabbed/flat layout is replaced by a categorised accordion HUB — a profile header (avatar + name + role/Free–Full badges + email) over four labelled groups (How you use HYBRID – Who can see & reach you – Login & billing – Your data & support), each a glass list of icon rows that EXPAND inline to their controls. The information architecture (group order, the 10 categories — account/preferences/logger/notifications/privacy/coaching/security/subscription/data/danger — and each row's icon/title/subtitle) is a SINGLE SOURCE OF TRUTH in @hybrid/core settings-nav.ts (SETTINGS_GROUPS / SETTINGS_CATEGORIES), so web (components/account-settings.tsx) and mobile (components/aurora/settings.tsx) render the same hierarchy and can't drift. Every existing flow is preserved and re-homed into a row (profile edit, appearance/template/language, logger detail+units+volume, notification/privacy toggles, become-a-coach + request-access, MFA + password + sign-out-everywhere, Simple/Full mode + Stripe billing, data export, danger-zone reset). On web each row expands inline (accordion); on mobile most expand inline (LayoutAnimation) and logger/coaching/subscription navigate to their existing dedicated screens (/logger-settings, /coach-apply, /upgrade). Built on the real Aurora glass tokens + the shared Aurora icon set (AuroraIcon) on both clients. Mockup explorations live in reference/settings-mockups/. Classic mobile (app/settings.tsx) keeps its existing layout (Aurora is the default skin on both clients)." },
  { id: "profile-settings-redesign", area: "Core", status: "shipped", title: "Instagram-grade profile & settings polish (both clients)", detail: "First tranches of the profile/settings redesign, shipped web+mobile in lockstep: (1) a dedicated 'edit' pencil glyph — a shared @hybrid/core SVG path + a matching 48×48 mobile PNG (assets/icons/edit.png) — on the profile-head edit-profile button so it's no longer the settings gear (the two read as the same action next to the avatar). (2) The 'Security' settings category renamed to 'Password & security' (Instagram's wording) via one change in the shared settings-nav. (3) The Password & security screen rebuilt from a single password box into two Instagram-style groups: 'Login & recovery' (MFA on web + change-password with a LIVE password-strength meter + a Connected-account line) and 'Security checks' ('Where you're logged in' → a This-device indicator + Sign-out-everywhere). Password strength is a shared, unit-tested core helper (passwordStrength — 0–4 score + weak/fair/good/strong, gates the app 8-char minimum) consumed identically by both clients; new i18n keys in EN/PL/DE. Verified: core tests (686) + web/mobile typecheck green. Remaining redesign MOVES are still to build: the unified single 'Edit profile' screen (merging Account & profile + Public profile), avatar upload (blocked on the social-avatar-upload Storage bucket), the live 'what followers see' preview, inline per-field validation, the Settings-header completeness ring, and the full rich treatment on every other tab (Preferences / Logger / Notifications / Privacy / Coaching / Subscription / Data). Mockup: design pitch published as an artifact. (4) HEADLINE MOVE — the unified single 'Edit profile' screen: 'Account & profile' and 'Public profile' are MERGED into one settings category ('Edit profile') on both clients — the separate 'social' category is removed from the shared settings-nav, and the profile-head pencil + Settings both open the same surface. It stacks the public profile (avatar, handle, display name, bio, visibility) over the account identity (name, email). AVATAR now has a real preview + one-tap branded GRADIENT PRESETS (six tiny PNG data-URIs in @hybrid/core AVATAR_PRESETS that persist in avatarUrl and render through the existing <img>/<Image> paths on both clients — no renderer change, no Storage bucket), plus an honest disabled 'Upload photo (soon)' affordance. Verified additionally: web prod build + mobile iOS bundle export green. (5) Live 'what followers see' PREVIEW card atop the editor (avatar / name / @handle / bio / visibility badge, re-renders as you type) + INLINE VALIDATION on both clients: a debounced handle-availability check (green ✓ available / red ✕ taken, using the /api/social/profile/:handle 404-vs-profile signal, with a 'this is your handle' case), instant format-rule feedback, and a live bio character counter (n/280). (6) MOVE 5 — the Settings header is now a launchpad: a completeness RING around the avatar (web = an SVG arc; mobile = a lime ring + a proportional bar, since RN has no inline SVG here) showing the % + an 'Add a photo & a bio' nudge, driven by a shared, unit-tested profileCompleteness() helper (name / handle / display name / bio / photo). The whole header is tappable → Edit profile, with quick-action chips (Edit profile – Share — native share sheet / clipboard of the @handle). (7) The per-tab TREATMENT pass (both clients): Preferences theme-swatch cards; Notifications + Privacy grouped sub-headers (via a shared PrefRowDef `group`); the Data tab's 'what's included' export checklist; the Subscription tab's Full-benefits list (FULL_BENEFITS, shared with the upgrade sheet so they can't drift); and the Coaching 'how it works' 3-step intro (web + the mobile coach-apply screen). Logger already carried its full toggle set. (8) ROW-DRILL — Edit profile is now the exact Instagram tap-a-row layout: a live preview + the avatar/presets on top, then a row list (Name / Username / Display name / Bio / Email / Visibility) where each row opens a FOCUSED single-field editor with its own validation (live handle availability, bio counter) and Save; name/email persist via Supabase auth (threaded into the shared SocialProfileEdit), the rest via the social API. The redesign is feature-complete; the only deferred piece is the real photo UPLOAD (blocked on the avatars Storage bucket — social-avatar-upload), with gradient presets shipping as the interim. Verified across every tranche: core tests + web/mobile typecheck + web prod build + mobile iOS bundle export." },
  { id: "persona-access", area: "Web", status: "shipped", title: "Admin access control — who sees what (per-persona nav)", detail: "An admin tool to choose what each persona sees, so retail users aren't overloaded yet anyone can be granted depth on request. Admin → Governance → 'Access control' (components/admin/access.tsx) is a per-nav-item matrix: the admin sets the MINIMUM persona each feature is visible from (Casual ⊂ Athlete ⊂ Coach ⊂ Admin) — drop Velocity/Analytics to Casual to give an average-Joe the stats, or raise an item to hide it. Pure core layer: a sparse PersonaAccess map ({ navId: minPersona }) overrides the code minPersona defaults; navForPersona/navVisibleTo take an optional override and sanitizePersonaAccess validates the payload (only known ids → valid personas), all unit-tested (404 core tests). STORAGE reuses the feature-flags infra with NO new table — the map is the value of a new registry flag access.personaNav, written via the existing admin flags POST (admin-gated, rate-limited, body-capped, AUDITED as flag.upsert) and read via /api/flags. CONSUMED on BOTH clients: the web sidebar + ⌘K hub filter through navForPersona(persona, …, access); mobile reads it (lib/access.ts store + useNavAccess hook over /api/flags) and applies it to the More hub + command menu + the cockpit entry. Changes take effect on the next client load — no deploy. Persistence needs the FeatureFlag table (reference/sql-feature-flags.sql); until applied it runs on code defaults and the screen says so. The Access control screen also carries the read-only RBAC role model (Client/Coach/Admin data-access permission matrix), moved here from the retired user-facing 'Roles & access' screen (the per-feature matrix below is now the only entitlement surface — the admin Financials console that held a static 'what each plan gets' table was retired in Aug 2026). REMOVED (Jul 2026): the earlier PER-USER 'individual access' layer — admin-granted per-person FeatureGrants + the user-facing 'Request a feature' loop (AccessRequest) — was deleted end-to-end (web + mobile UI, /api/access-requests + /api/admin/access-requests routes, the FeatureGrant/AccessRequest Prisma models, and the /api/flags grant-fold). Access is now purely the per-persona matrix; there is no longer any one-off unlock-for-one-person path. NOTE: an earlier 'per-feature casual hidden/locked bait' axis was also REMOVED — scattering padlocks both undersold Full (looked like one screen) and cluttered the lean casual app; the freemium upsell is a single Unlock Full page (see 'full-upgrade-page'). The matrix is min-persona only." },
  { id: "athlete-cockpit", area: "Mobile", status: "shipped", title: "Athlete cockpit (the depth, organized)", detail: "Phase 2: a single mobile screen (app/cockpit.tsx) that assembles the data-athlete's scattered depth into one coherent path — Goal & season (the enrolled macrocycle's current phase/week, or a 'set up a plan' prompt) → Today's route (the velocity-aware prescription + readiness, Start) → Performance (the Athlete Twin: HPI score/band/limiter + pillar split + the top state driver) → Sport S&C (the saved sport + level) → Velocity & technique → Endurance (whole-history run totals). Each section shows a LIVE snapshot computed off the athlete's real sessions/signals (prescribeSession, computePerformanceState, runTotals, toBiometrics) and links out to the deep screen, so the athlete stops hunting through a flat menu for their cockpit. Surfaced ONLY to the athlete/coach personas (see persona-shape): a persistent Cockpit tab in the mobile bottom bar (athlete-gated via navVisibleTo, hidden for casual — the mobile parallel of the web sidebar's Home → Cockpit item) + a featured card atop the More hub; casual users never see it. WEB PARITY shipped too — a 'cockpit' nav item (nav.ts, minPersona athlete) + components/cockpit.tsx render the same six live sections (jumping to the deep web screens: Periodize/Log/Performance/Sport/Velocity/Running), wired into the app-shell. Honest empty states until there's training to compute from. The only remaining piece is the still-blocked real input streams it would feed on (Apple HealthKit recovery sync + camera pose-capture for the technique engine — see wearables / vbt-capture / video-intel, all gated on an EAS build + native modules/credentials)." },
  { id: "home-two-tab", area: "Web", status: "shipped", title: "Home consolidated to two tabs (Today + Cockpit)", detail: "The home nav group is two tabs now, not four. The old Dashboard (a web-only mirror that ran on a hardcoded demo macrocycle) is RETIRED — its two unique surfaces moved onto Today and now run on the athlete's REAL enrolled season: the macrocycle phase timeline (Base→Build→Peak→Taper, current phase/week highlighted) and the injury-risk-by-tissue panel (folded into the Athlete Twin card via computeInjuryRisk). 'Get started' (onboarding) is no longer a standalone tab either — see the onboarding capability (folded into Cockpit's 'Goal & season' as a 'Set up / change plan' flow; first-run still routes new accounts in). nav.ts drops both dashboard + onboarding from NAV_ITEMS (the casual lean-set unit test updated to match), and the web app-shell/screens.tsx drop the DashboardMirror screen. MOBILE PARITY: the mobile Today gained the identical season phase-timeline bar + injury-risk-by-tissue panel; Cockpit is a persistent bottom-bar tab (app/(tabs)/cockpit.tsx) for the athlete persona so the two-tab home reads the same on both clients, and the old redundant Today cockpit promo card was dropped; mobile's More keeps the onboarding tile (special-cased now that it's not a nav item). FREEMIUM FUNNEL (revised — see 'full-upgrade-page'): a casual (free) user keeps a CLEAN nav with NO scattered padlocks and NO locked Cockpit tab; the entire paid toolkit is sold on ONE 'Unlock Full' page reached from a single value-labeled entry, so the upgrade's full value is clear without cluttering the lean app." },
  { id: "funnel-analytics", area: "Core", status: "blocked", title: "Upgrade funnel instrumentation", detail: "The freemium funnel is instrumented end-to-end at the call-sites: shared event names in core (analytics.ts FUNNEL — upgrade_entry_click / upgrade_page_view / upgrade_cta_click), fired through a provider-agnostic track() shim on both clients (apps/web/lib/track.ts + apps/mobile/lib/track.ts) from the Unlock Full entries (web sidebar; mobile Today + More), the bundle page view, and the CTA. The shim is a safe no-op (dev-logs only) until an analytics provider is wired — do that in ONE place and every call-site reports for free. So conversion is measurable the moment a provider lands.", blockedBy: "Pick an analytics provider (PostHog / Segment / GA4 / Amplitude) and set its key, then forward `track()` to it (and ideally join with the billing purchase event for end-to-end conversion)." },
  { id: "full-upgrade-page", area: "Web", status: "shipped", title: "Unlock Full — one bundle upgrade page (clean freemium funnel)", detail: "The freemium upsell is a SINGLE value-labeled surface, not scattered per-feature padlocks (which both undersold Full as 'one screen' and cluttered the lean casual app). A casual (free) user keeps a clean nav; one pinned 'Unlock Full' entry (web sidebar; mobile a Today card) opens an Upgrade page that sells the WHOLE toolkit — a flagship Cockpit card + grouped bundle (Train smarter / Your performance / Sport & technique / Endurance & body), a '12+ pro tools' framing and the CTA. Web: components/upgrade.tsx (screen 'upgrade', nav.upgrade i18n), CTA hits /api/billing/checkout (or flips a paid-but-Simple user straight to Full). Mobile: app/upgrade.tsx reuses the existing IAP/Stripe subscribe flow with the same bundle content. The Cockpit teaser remains only as a deep-link fallback. Casual no longer sees a locked Cockpit tab on either client. OPEN FOLLOW-UPS (deliberately deferred): (1) PRICING/TRIAL — the page shows no fixed price yet ('pricing shown at checkout', CTA 'Upgrade to Full'); set the real monthly price + any free-trial copy once the Stripe/IAP products are live (see full-billing-stripe / full-billing-iap for the products + keys). (2) ANALYTICS — the funnel is instrumented but reports nowhere until a provider is wired (see funnel-analytics).", },
  { id: "onboarding", area: "Web", status: "shipped", title: "Guided onboarding → first plan", detail: "A guided intake (persona, goal, experience, days/week, equipment) → a personalized first plan via recommendPlan (onboarding.ts, pure + tested). The main-goal options are sourced directly from the plan library's goals (GOAL_TREE) so the two never drift; the chosen goal maps straight onto its node and picks the plan whose weekly frequency fits — a 3-day lifter never gets the 6-day split they'd quit. Enrolls the macrocycle on completion. When the chosen goal has no plans yet (current state — the library is empty), the picker shows a 'plans coming soon' card with a Continue-to-the-app action instead of dead-ending. NO LONGER A STANDALONE TAB — folded into Cockpit's 'Goal & season' as an inline 'Set up / change plan' expander on web, a routed setup screen (reached from Cockpit + More + the Home banner) on mobile; first-run still routes a brand-new account straight into it. DYNAMIC, ADMIN-EDITABLE QUESTIONS (admin → Onboarding): the questionnaire is DATA, not hard-coded UI. @hybrid/core DEFAULT_ONBOARDING_QUESTIONS holds the five built-ins (persona/goal/experience/days/equipment) as the seed + fallback; the OnboardingQuestion table stores the admin's edits — reword, reorder, enable/disable, and ADD/REMOVE custom questions (single/multi/number/text). Engine-relevant answers feed the recommendation via an `engineKey` (recommendFromAnswers); custom questions are saved to the profile and ignored by plan matching, so the engine never breaks. Both clients (classic + aurora, web + mobile) fetch /api/onboarding/questions and render every kind generically; admin mutations are ADMIN-gated + audited; reference/sql-onboarding.sql. SERVER-SIDE GATING (the fix for 'I never got onboarding'): completion is the OnboardingState table (its own table off the User hot path; surfaced as `onboardedAt` on /api/me, falling back to 'already has a plan' so existing users aren't nagged). A client who hasn't onboarded is routed into the questionnaire on entry — web app-shell + mobile index gate — and POST /api/onboarding saves the answer map + marks onboarded + enrolls in one call. This replaces the fragile localStorage 'pendingOnboarding' flag (set only at signup, lost across the email-confirm round-trip / device changes / the sign-in path), so onboarding reliably appears once and survives those. A same-device local 'onboarded' flag is the fallback before the migration is applied. The pre-registration GUEST flow is deliberately UNCHANGED — a non-registered user still goes welcome → train → finish → share with no persona/onboarding prompt; onboarding only appears AFTER they create an account. The persona fork (see persona-shape) leads the menu. The single biggest 30-day-retention lever." },
  { id: "first-run-tour", area: "Core", status: "shipped", title: "First-run guided tutorial", detail: "A guided coach-marks tour shown ONCE to a fresh account right after onboarding, on BOTH clients — web apps/web/components/tour.tsx is a spotlight overlay that highlights real `data-tour` anchors (the Today plan card + the Plans/Profile/Settings nav items) and tracks them on scroll/resize; mobile apps/mobile/components/tour.tsx is a stepped overlay mirroring the same copy/flow. Steps: your day on Today → follow a plan → your Profile → configure your account in Settings. GUEST-FIRST ORDERING: if the account was created right after logging a GUEST workout, that just-saved workout lands first — the tour steps aside for one app open (flushGuestSessions sets `hybrid.cameFromGuest` when it syncs ≥1 session; the home screen defers the tour that open, then shows it the next). Triggered via `hybrid.pendingTour` (set at signup on mobile / on onboarding completion on web) and never re-nags once `hybrid.tourSeen` is set." },
  { id: "today-first-session", area: "Core", status: "shipped", title: "First-session chooser", detail: "A brand-new, not-yet-enrolled user's Today plan card offers THREE explicit paths instead of a single Start button: Follow a plan (free → Plans library), Build your own (free → the Builder; a free account keeps up to 2 saved templates, see workout-routines), and Log a one-time workout (free → empty logger). Implemented on both clients AND both templates: web components/today.tsx + components/aurora/today.tsx, mobile components/aurora/home.tsx. Replaces the old single 'Start your first session' prompt. COLD-START FLASH FIX (Jul 2026): the chooser is the fallback shown when no plan resolves, so on a hard refresh (web) / cold app open (mobile) an ALREADY-ENROLLED athlete briefly saw 'How do you want to start?' before their plan loaded — the plan-card decision only gated on the sessions fetch, not the slower enrollment (macrocycle) fetch. Now the plan hero holds a skeleton until BOTH sessions and enrollment settle: web useMacrocycle exposes loading (isPending||isFetching), app-shell ORs it into Today's loading prop, and the aurora/today loading skeleton is ordered ahead of the AI-prescription and chooser fallbacks (plus the top Start pill is held back while loading); mobile aurora/home gates the same hero on a new initialLoad flag (true until the first Promise.all load settles). So the chooser only appears once we actually know the athlete isn't enrolled — never as a load-time flash." },
  { id: "today-upsell", area: "Core", status: "shipped", title: "Today premium upsell (Performance State)", detail: "Free users get a 'See your Performance State' card on Today that sells the Full intelligence layer (HPI + readiness + injury-risk fused into one live state, plus adaptive loads, periodization, velocity tracking, analytics and the AI coach), and an enrolled free user additionally gets a season-brief upgrade CTA. Both clients/templates, fired through the same FUNNEL track() events (source: today-perfstate / today-season / today-build) so the upsell is measurable the moment an analytics provider lands (see funnel-analytics). AI-PRESCRIPTION GATING (Jul 2026): the AI-prescribed session (prescribeSession — 'the moat') is now PREMIUM (paid) ONLY, everywhere it was entered: Today's 'Your plan today' card (web today.tsx + mobile home.tsx now render the readiness ring + rx.blocks + rx.why ONLY when isAthlete && (hasData||phase) — a casual/guest instead sees the honest enroll/build/log chooser, never the fabricated 'Back Squat + Assault Bike, readiness 98' session presented as theirs), the Train launcher AI card (mobile train.tsx), the live-logger empty-state AI button (web aurora/logger.tsx + mobile workout.tsx loadPrescribed + the source=ai deep-link handler). A non-premium tap is FUNNELLED not fulfilled: a GUEST (no account) is sent to register (/login?mode=signup), a free logged-in user to the upgrade paywall (/upgrade), tracked as FUNNEL.upgradeEntryClick (source: workout-ai / train-ai / logger-ai). This closes the leak where a guest could pull a fabricated readiness-driven session for free — the intelligence layer is the paid moat. The hardcoded English AI-card copy ('Smart starter session' + blurb) was also moved to i18n (train.aiEmptyTitle/Blurb, train.aiLockedTitle/Blurb, train.premium; EN/PL/DE). Enrolled users (incl. casual/free — following a pre-made plan is free) now see their REAL enrolled plan day on the card (see plan-program-today), so 'Your plan today' is only the AI prescription when premium AND not on a named plan." },
  { id: "periodization-access", area: "Core", status: "shipped", title: "Periodization is a Full feature", detail: "The full Periodize screen is paid. Free (casual) users are NEVER routed there — the post-enroll redirect lands them back on Today (not Periodize), and Periodize stays out of their nav (minPersona athlete) — and instead get a read-only SEASON BRIEF on Today: the goal, current phase and the phase timeline, with an 'Unlock full periodization' CTA. Defense-in-depth: a casual who still reaches the Periodize screen is shown the upgrade page. Web app-shell + today.tsx/aurora-today; mobile aurora/home.tsx (mobile never force-redirected after enroll)." },
  { id: "nav-category-hub", area: "Core", status: "shipped", title: "Navigation — category hub (drill-in) on small screens", detail: "The full nav (40 destinations across the 7 canonical groups home/train/analyze/recovery/social/teams/account) was an unreadable single scroll on mobile. Redesigned as a CATEGORY HUB (concept 4 of a 5-concept study): the surface shows the SEVEN areas as cards — each with a Spectrum-coloured glyph, a live preview of its first few screens and a count — and tapping one drills into just that area's list; a back control returns. No feature was removed; only the door changed. Built on the SHARED canonical IA so web + mobile can't drift: both derive the areas from @hybrid/core groupedNav(navForPersona(persona, …, access)) (persona + admin-access filtered). MOBILE: apps/mobile/app/(tabs)/more.tsx is the hub — identity/Settings/mode-toggle/Unlock-Full/Admin pinned above the seven area cards, each area drilling to its screens (id→route via a local HREF map; screens with no mobile route show a 'WEB' tag and open the web app, so granted access is never invisible). WEB PARITY (responsive): the app-shell sidebar keeps every group EXPANDED on the desktop rail (there's room), but its mobile off-canvas DRAWER now drills the same way (area cards → one area's items → back) — so the two clients share the information architecture and each renders it right for its viewport. onboarding (a re-runnable setup flow, not a nav item) is injected into Train. Derived from reference/hybrid-nav-redesign.html (the 5-concept pitch). SEARCH-FIRST PASS (both clients, at parity): the More hub gained a SEARCH field at the top of the springboard that filters the launcher tiles by (localized) label and drops clusters left empty, with a total-tool count in the placeholder and a no-match empty state — mobile app/(tabs)/more.tsx, and on web BOTH More surfaces match (the app-shell mobile drawer AND the AuroraPillNav bottom-sheet, apps/web/components/aurora/pill-nav.tsx). Locking/gating still derives from groupedNavWithLocks (locked tiles show a lime 🔒 and route to upgrade); the identity card, Unlock-Full membership card and Sign out are unchanged. PARITY/FIDELITY HARDENING: the 3 springboard surfaces (mobile tab, web app-shell drawer, web pill-nav sheet) audited 1:1 and against the mock — unified to a 4-col grid with counted headers, a lime lock badge (was a 🔒 emoji on the sheet), per-cluster accent-tinted chips, an identity card + Sign out on the sheet, one `more-${id}` tracking token and one empty-state string; mobile now applies the same nav feature-flag gate (new useFlags() in apps/mobile/lib/flags.ts) and localizes the Unlock-Full copy (more.upgradeKicker/Blurb/goFull). SUPERSEDED (Aug 2026) by `side-menu`: the springboard is no longer a BOTTOM-BAR TAB on either client. The mobile More tab (app/(tabs)/more.tsx) and the web pill-nav More sheet were both deleted, as was the web app-shell's mobile off-canvas drawer (which nothing had been able to open since the mobile header was retired). The tool grid itself survives, as the 'All tools' expander inside the side menu behind the Today header's avatar — same groupedNavWithLocks source, same persona/flag gating, same locked-visible premium items and same `sidemenu-${id}` upgrade tracking (renamed from `more-${id}`), rendered as compact grouped ROWS rather than a 4-column tile grid because a 300dp drawer is not a springboard. The mobile id→route map moved out of the deleted screen into apps/mobile/lib/nav-href.ts, which is what the web ↔ mobile parity guard now reads." },
  { id: "side-menu", area: "Core", status: "shipped", title: "Side menu — the drawer behind the profile avatar", detail: "Tapping the avatar in the top-left of the Today hub header slides a menu in from the LEFT edge (the idiom X uses) instead of jumping straight to Profile. Registry in @hybrid/core side-menu.ts (SIDE_MENU_PRIMARY / SIDE_MENU_FOOTER / SIDE_MENU_SCREEN_IDS / SIDE_MENU_WIDTH), so both clients render the same rows in the same order with the same targets: PRIMARY (large rows) Profile – History – Dashboard – Performance – Feed – Nutrition; FOOTER (smaller, whitespace-separated, no divider rule) Connections – Settings and privacy – Help center, then Sign out. A row target is either a `screen` (a canonical nav id) or a `hub` (one of Today's three views), and the hub rows switch the hub IN PLACE — the drawer lives inside Today, so Dashboard/Performance/Feed here and the glyph pills above the calendar are one control. The hub rows draw the hub's own HUB_GLYPHS mark, so the drawer and the pill row can't mark the same view two ways. Between the two lists sits the ALL TOOLS expander: the full persona-filtered nav (groupedNavWithLocks, nav.<id> flag-gated, premium items locked-visible and routing to the paywall) minus the rows the drawer already names — this is where the retired More springboard went, and it is what keeps every screen reachable. It GROWS IN PLACE behind a bare ＋/− in ash with an ash count, never a ringed arrow, per the exit-grammar rule. Also carried: the identity header (avatar, name, ROLE – FREE/FULL), the Unlock-Full accent card for casual users, the Admin console row for operators, and Sign out. WEB (components/aurora/side-menu.tsx) portals to <body> because the app shell is a transformed surface (.motion-recede-host) that would trap a position:fixed panel, uses lib/use-dialog for the focus trap + Escape + focus restore, locks body scroll, and enters on the shared slide spring (.motion-side-menu in globals.css, cross-dissolve under Reduce Motion). MOBILE (components/aurora/side-menu.tsx) is a Modal so it draws over the native tab bar, with an Animated translateX panel and a scrim fading off the same driver; exempted in the design-tokens Modal guard and the press-feedback guard with reasons (it is a left-anchored full-height drawer, not a bottom sheet, and its raw Pressable is the scrim). Guarded by packages/core/src/side-menu.test.ts (row order, targets resolve to real nav ids / hub tabs, icon XOR hub glyph, labels resolve in EN/PL/DE, and only the two deliberate bottom-bar repeats)." },
  { id: "direct-messages", area: "Core", status: "planned", title: "Direct messages", detail: "IN PROGRESS — the feature is being built, and the bar slot is held on that basis. Messages is the bottom bar's third destination (it took the slot the More springboard held — see @hybrid/core nav-bar.ts) and the SCREEN IS STILL A PLACEHOLDER that says so: apps/mobile/app/(tabs)/messages.tsx states that direct messages are coming and that nothing is hidden behind the tab. Deliberately no sample threads, no fabricated unread count and no badge on the tab — the placeholder must not fake the feature, and that constraint holds until real conversations land. THE STANDING OBJECTION, kept in view rather than dismissed: the Aug 2026 audit cut this tab, and its reasoning was sound for the state it found. The bar has four positions, the product has four jobs (log training, log food, see state, follow a plan), and a quarter of the scarcest surface the app owns was spent on a room with nothing in it — the screen's honesty was never the problem, the placement was. The removal shipped and was then REVERTED, deliberately, because the answer to that objection is to build the feature rather than to reclaim the slot: a tab is earned by having something behind it, and this one is now being given something. If the build stalls, the audit's finding is still the correct one and the slot should go back. STILL TO BUILD: a thread/message model in prisma/schema.prisma (participants, read receipts), the API (list threads, fetch a thread, send, mark read), realtime or polled delivery, an unread count that the bar's badge and the notifications feed can share, composing from a profile / a coach link / a feed post, and moderation + blocking (which the existing social blocklist should feed)." },
  { id: "help-center", area: "Core", status: "shipped", title: "Help center", detail: "The side menu's last footer row, on both clients: apps/web/components/aurora/help-center.tsx and apps/mobile/app/help.tsx, rendering the shared rows from @hybrid/core help.ts (HELP_ROWS + SUPPORT_EMAIL + supportMailto), so the help offered can't drift between the clients. Four rows, and every one of them does something that already exists: REPLAY THE GUIDED TOUR (clears hybrid.tourSeen and arms the first-run walkthrough — web sets the shell's tour state and navigates to Today, mobile sets hybrid.pendingTour, which is the marker Today already looks for; the row confirms in place rather than leaving, since what it arms happens on the next screen), CONTACT SUPPORT (a mailto to contact@hybriddomain.xyz, a real Google Workspace mailbox — the row shows the address as its subtitle so nobody has to open a mail client to find out where it goes), REQUEST A FEATURE (into Settings → Coaching & access, the existing request-access flow) and PRIVACY & TERMS (the published /privacy and /terms pages). Deliberately NO article index and no search field: there are no articles, and a search box over nothing is the worst kind of placeholder. `help` is a real nav id in @hybrid/core nav.ts with a route on both clients, so the web ↔ mobile parity guard covers it. WHEN hybrid.app IS ACQUIRED: change SUPPORT_EMAIL in help.ts to support@hybrid.app and both clients follow." },
  { id: "nav-sliding-indicator", area: "Core", status: "shipped", title: "Bottom-nav sliding selection indicator (liquid pill)", detail: "The floating Aurora bottom-nav highlight now BEHAVES like Instagram's tab bar (the requested feature): instead of a static chalk pill that instantly pops onto whichever tab is active, a SINGLE shared indicator SLIDES between the four flat slots (Today · Explore · More · Profile) and STRETCHES mid-travel (a scaleX squash-and-settle) for the liquid feel, springing into its new position. Built once on BOTH clients at parity: WEB apps/web/components/aurora/pill-nav.tsx — one absolutely-positioned chalk span inside the pill, measured against each flat button (offsetLeft/offsetWidth via refs), animated with a CSS spring transition (cubic-bezier overshoot) on transform + a brief scaleX on move; recomputed on resize; the per-button background was removed so the glyph just tints ink over the shared pill. MOBILE apps/mobile/components/aurora/global-nav.tsx — an Animated.View (RN core Animated, no reanimated dep) positioned from each item's onLayout geometry, driven by Animated.spring(translateX) + an Animated.sequence(scaleX 1→1.28→1) + opacity, native-driver. The centre Train FAB is EXCLUDED (it keeps its own lime glow) so the indicator fades out (opacity→0) while Train is active. Both clients snap without animation on first paint and honour reduced motion (web prefers-reduced-motion; mobile AccessibilityInfo.isReduceMotionEnabled + reduceMotionChanged). Layout, icons, labels, blur/film/rim and the FAB are otherwise unchanged. Web build + iOS bundle export + both typechecks green. LIQUID GLASS + TAP-LAG (Jul 2026, from live TestFlight feedback): (1) the moving highlight is now REAL Liquid Glass on iOS — a native SwiftUI glassEffect lens (@expo/ui RoundedRectangle + glassEffect, the same material as the kit's GlassSurface) rendered on Platform.OS==='ios' (independent of the opt-in SwiftUI-kit toggle, so it's visible to evaluate; degrades gracefully on iOS<26 with a faint floor underneath). Android keeps the opaque chalk pill (which still scaleX-stretches; the glass gets its liquid quality from the material, so it only springs). Over the translucent lens the active glyph stays bright (chalk) instead of inverting to ink, so it can't read as a dark-on-glass smudge. (2) TAP-LAG FIX — selection is now OPTIMISTIC: a local selectedSeg updates the instant a tab is pressed and drives BOTH the pill position and the icon tint, then reconciles to the real route (useSegments) once it settles. Previously the tapped icon inverted to its dark on-pill colour the moment the route flipped — which on a heavy screen lagged ~1s behind the press — so you saw a black icon before the pill slid in. Now the pill leads the navigation. PARITY/PLATFORM: true native Liquid Glass is iOS-only (it's the blocked swiftui-kit material); web keeps its CSS backdrop-filter frost + chalk indicator (web in-shell nav is synchronous, so it has no tap-lag to fix) and Android keeps the chalk pill — a platform capability gap, not a feature gap. Native glass can't be visually verified in the sandbox (no Xcode/simulator); typecheck + iOS export are green and it validates on TestFlight. CROSS-FADE (from live feedback: sliding the glass 'lagged when moving between tabs'): sliding a single native glassEffect lens across the bar re-samples its backdrop every frame and janks, so the indicator no longer travels — each of the four slots now owns its OWN lens and selection CROSS-FADES (opacity only, ~170ms; nothing translates), which stays smooth because a fixed-position glass view doesn't re-sample while it fades. The optimistic-selection tap-lag fix is unchanged. Trade-off (superseded below): the highlight faded in place instead of gliding. NATIVE GLASS MORPH (from feedback: 'don't lose the Apple travel motion, but it can't be laggy'): iOS now renders the selection as a SINGLE native Liquid Glass lens that MORPHS between slots — Apple's matched-geometry travel via @expo/ui GlassEffectContainer + Namespace + glassEffectId (the four slot cells live in one container; only the selected cell renders the glass RoundedRectangle, tagged with a stable glassEffectId, so when selection moves SwiftUI fluidly morphs the glass across). This is smooth because the morph runs natively in SwiftUI — unlike the earlier JS-driven translate, which re-sampled the glass every frame and janked (the reported lag). Component: GlassMorphSelector in global-nav.tsx, a Host overlaying the inner row BEHIND the RN glyphs, laid out with concrete SwiftUI frames (four equal slots of (rowW−64)/4 + a fixed 64pt centre gap, measured via the row's onLayout) to align with the icons. Deliberately does NOT use withAnimation/worklets (which throw if react-native-worklets isn't wired) so there is no crash path — worst case the glass snaps instead of gliding. The glyphs, Train FAB and taps stay RN, so the bar still works if the native layer doesn't render. Android keeps the per-slot chalk cross-fade. Unverifiable in the sandbox (no Xcode/simulator) — typecheck + iOS export green; the morph itself is validated on TestFlight and may need device iteration. TRAVEL ANIMATION (grounded in a @expo/ui native-Swift audit, after two builds SNAPPED): the matched-geometry approach (GlassEffectContainer + glassEffectId, glass conditionally rendered only in the selected cell) does NOT animate — an ancestor `.animation(_:value:)` can't animate a child that INSERTS/REMOVES (source + destination never co-exist to morph between), so it cut. The pattern that DOES animate (per @expo/ui's own ChartView: `.animation(.easeInOut, value: props.data.count)`) is a PERSISTING view whose animatable property changes under `.animation(value:)`. So the selector is now a SINGLE persistent glass RoundedRectangle, centred, moved by an animated SwiftUI `.offset(x:)` bound to selectedIndex with `.animation(Animation.spring, selectedIndex)` — the offset animates natively in SwiftUI (smooth; unlike the very first attempt, which drove translateX from JS over the bridge and janked). No GlassEffectContainer/Namespace/glassEffectId, no worklets. Offsets are from row centre: ∓(1.5·slotW+32), ∓(0.5·slotW+32), slotW=(rowW−64)/4. Still native-only, validated on TestFlight. INSTAGRAM-AUDIT PASS (Jul 2026, from a frame-by-frame comparison of a real Instagram recording vs ours — 60fps centroid tracking showed our travel rendered smoothly but still READ laggy): the tells were (a) the active-glyph tint flipped INSTANTLY on press while the lens arrived ~250ms later (the eye reads 'tap registered… now watch a slow bubble catch up'), (b) a constant-size lens with no stretch/overshoot reads floaty at any duration, (c) selecting Train UNMOUNTED the lens (return null) so leaving/re-entering a side tab popped — the exact SwiftUI can't-animate-a-remount trap documented above. Fixes on BOTH clients at parity: (1) ICON-SYNC — the active tint is now a crossfade timed to the lens ARRIVAL (incoming glyph fades in delayed ~90-100ms to land with the lens, outgoing fades back immediately; mobile: per-slot Animated iconOp overlay icons, web: a delayed-transition ink overlay in PillButton); (2) STRETCH — the lens elongates toward the target mid-travel scaled by distance then settles (mobile: two native poses — frame width springs up then back, each keyed by a monotonic tick under .animation(value:); web: distance-scaled scaleX min(1+dist/240, 1.9) for 150ms); (3) snappier spring with arrival energy — mobile response 0.32/damping 0.74 (was 0.38/0.82), web .34s cubic-bezier(.32,1.36,.44,1) (was .44s); (4) NEVER UNMOUNTS — Train now fades the lens via animated opacity in place on both clients, so the next selection travels/fades instead of popping; (5) the lens widened 46→56pt toward IG's proportions (reads as 'the selection', not a token behind the glyph). Both honour reduced motion (snap, no stretch). Web + mobile typecheck and the iOS bundle export green; feel needs TestFlight validation as ever." },
  { id: "nav-ios26-tab-bar", area: "Core", status: "shipped", title: "Bottom nav — Liquid Glass capsule + detached Train (SUPERSEDED by nav-hig-anatomy)", detail: "HISTORICAL. This shipped the Aurora bottom nav as a Liquid Glass capsule of four tabs (Today, Explore, More, Profile) with Train as a DETACHED bar-height lime circle beside it, on both clients, and unified the nav glyph weight to 4 viewBox units after a live-preview note that the icons read too thick (it also fixed a real parity drift: mobile stroked the bar at the kit default 6 while web stroked it at a 2.6 hairline, so the 'same' bar never matched). The glyph-weight and parity work still stands. TWO CLAIMS IN IT WERE WRONG, corrected by nav-hig-anatomy below: (1) the detached circle was described as 'Apple's split tab bar (tab group + standalone accessory, like Search in the system apps)' — in iOS 26 a circle separated beside the tab bar is the SEARCH ROLE, which morphs into a search field on tap, so a training CTA parked there reads as search; (2) putting Train there at all runs against the guidance that a tab bar carries navigation — 'avoid placing screen-specific actions in the tab bar'. Kept as a record of what was built and why it changed." },
  { id: "nav-hig-anatomy", area: "Core", status: "shipped", title: "Bottom nav — five tabs in one capsule + session accessory (SUPERSEDED by nav-split-action)", detail: "PARTLY HISTORICAL: nav-split-action (Aug 2026) returned Train to a detached action circle at the owner's direction, so the five-in-one-capsule anatomy below no longer ships; the session accessory, the shared nav-bar.ts contract and all the material work still stand. The bottom nav rebuilt to Apple's actual tab-bar anatomy on BOTH clients, replacing the split capsule-plus-detached-circle of nav-ios26-tab-bar. ANATOMY: five tabs — Today, Explore, Train, More, Profile — inside a single Liquid Glass capsule (five is Apple's iPhone ceiling). Train is a TAB, not a detached action: it always opened the Train launcher, i.e. a destination, and 'avoid placing screen-specific actions in the tab bar' rules out the button reading. The separated-circle slot beside the bar is left EMPTY because that geometry is the iOS 26 search role (it morphs into a search field), so it now stays reserved for real search if HYBRID ever adds it. Persistent session state moved to the tab-bar ACCESSORY above the capsule (Apple's mini-player slot, the system home for players and active orders): when a workout draft exists, a glass strip shows a lime status dot, the draft title, a live elapsed clock and Resume, standing down on the Train launcher and the live logger where it would only repeat the screen. SHARED CONTRACT: packages/core/src/nav-bar.ts is new and holds the tab table, geometry and material constants plus formatSessionElapsed, so the two clients can no longer hard-code divergent copies (they previously did, which is how the glyph weights drifted). MATERIAL: the body went from a 40% ink film under blur(24) — the frosted recipe from iOS 7-18, opaque enough that nothing behind survived — to a ~6% film under blur(14) saturate(1.8) brightness(1.08), with the identity moved to the RIM: a specular arc bright at the top-left and gone by 40% around, a refraction band whose last 9px sample a magnified saturated backdrop so content visibly bends at the edge, and a dark bottom lip that makes the glass read as thick rather than printed. The selection lens is now LIFTED — thinner glass than the capsule carrying it (clearer + brighter), with its own drop shadow and a specular top arc — and travels with stretch along the axis plus counter-squash across it. The active glyph takes the brand tint rather than chalk (iOS moves the selected item to a true tint and a filled symbol; this kit is line icons only per the project rule, so tint carries it). WEB: the material lives in globals.css as .aurora-navglass with @supports and prefers-reduced-transparency fallbacks to a solid bar, so nav is never an invisible surface. MOBILE: iOS 26 keeps the real native SwiftUI glassEffect capsule and the travelling native glass lens (GlassMorphSelector, whose slot maths was hard-coded to four cells and is now derived from the tab table — promoting Train is exactly the change that would have silently mis-seated the lens); Android and iOS < 26 get the same improved fallback numbers as web. Verified here: core tests, both typechecks and the iOS bundle export are green. The native glass itself still needs TestFlight — no Xcode/simulator in the sandbox." },
  { id: "nav-native-tabs", area: "Core", status: "shipped", title: "Bottom nav — the real system tab bar (native UITabBarController)", detail: "The mobile bottom nav IS the system tab bar now: app/(tabs)/_layout.tsx uses expo-router's native tabs (expo-router/unstable-native-tabs, already present in SDK 56 — no new dependency and no config plugin), which is UITabBarController on iOS and BottomNavigationView on Android. Liquid Glass on iOS 26 is therefore INHERITED rather than reconstructed, along with everything the custom bar could only approximate: the material's real refraction and adaptive contrast, the system selection treatment, tabBarMinimizeBehavior (set to onScrollDown), the scroll-edge transition, Dynamic Type, Reduce Transparency, Increase Contrast, double-tap-to-pop-to-root, and sidebarAdaptable for iPad/macOS. DELETED: components/aurora/global-nav.tsx entirely, including GlassMorphSelector (the ~110-line workaround for SwiftUI matched-geometry not animating across insert/remove) and the hand-built collapse ramp with its mini hysteresis — all of it is system behaviour now. The session strip moved to components/aurora/session-accessory.tsx inside NativeTabs.BottomAccessory, with the draft and the clock in a module-level store because the accessory's children are rendered TWICE (once per placement, regular and inline) and per-component state would let the two copies disagree the moment the bar minimized. FOUR ACCEPTED TRADE-OFFS, all of them deliberate and all of them the platform's rules winning: (1) the bar no longer shows on pushed sub-pages (Statistics, Settings, Periodize, …) — a native tab bar is hidden by a stack pushed above it, and the old bar was root-mounted specifically to defeat that; (2) tab glyphs are SF Symbols (house, globe, dumbbell, square.grid.2x2, person.crop.circle, each with its .fill selected variant) rather than the Aurora design-kit line icons, because native tab bars take SF Symbols or image resources and never React components — the kit PNGs are passed as `src` so Android keeps the house style; (3) every route that is not one of the five tabs MOVED OUT of app/(tabs) into the root stack (performance, history, plans, sport, velocity, endurance, coach; explore moved in), because a native tab bar renders every route in its directory and marking one hidden makes it un-navigable — verified in expo-router's own NativeBottomTabsNavigator, which filters on options.hidden and throws if a hidden route is focused; (4) the Classic template's command orb moved from the tabs layout up to the root layout, since the tabs layout is now purely NativeTabs. VERIFIED HERE: mobile typecheck (which also validates every SF Symbol name against expo-router's SFSymbols union, so a typo is a build error rather than a blank tab), the iOS bundle export, and the core suite. NOT verifiable here and gated on the next TestFlight build: the bar's actual appearance, and the bottom clearance in lib/layout.ts, which is now sized for the floating iOS 26 bar rather than measured against it (it errs high on purpose — over-padding is a gap, under-padding is a hidden row)." },
  { id: "nav-split-action", area: "Core", status: "shipped", title: "Bottom nav — the split bar: four places in the capsule, the verb in a detached circle", detail: "The bar's anatomy changed from five tabs in one capsule (nav-hig-anatomy) to a SPLIT bar, at the owner's direction: the capsule carries the four PLACES — Today, Nutrition, Messages, Profile — and TRAIN, the app's one VERB, rides beside it as a detached circle of the same glass. Inside the capsule Train was one grey glyph among five, indistinguishable from the places around it; as the detached circle it is the one thing on the bar that ACTS. THE ACTION IS CONTEXTUAL: @hybrid/core nav-bar.ts now holds AURORA_NAV_ACTIONS (train + post) and auroraNavAction(surface) — Train everywhere, ADD POST while the feed is the visible surface (the feed screen, or Today's Feed hub tab — the shell learns the hub tab via Today's new onHubTab callback). Everything stays in the EXISTING language: the circle's material is the capsule's own .aurora-navglass (never a solid lime fill — that is the retired nav-ios26 look), its glyphs are the kit's own (the shared inline dumbbell; the `list-add` compose mark for Add post), and the accent lives in the GLYPH — lime, the 'go' colour, per the accent grammar. THE TRADE, made deliberately and recorded in nav-bar.ts so it is never re-litigated by accident: a circle detached beside an iOS 26 tab bar is the platform's SEARCH role, which is exactly why nav-hig-anatomy vacated the slot. HYBRID spends it on the verb anyway — it has no cross-app search to put there, the circle never wears a magnifier, and it morphs per surface, none of which a search field does. WEB (pill-nav.tsx): the capsule shrinks to four slots, the ActionCircle sits actionGap (10, in AURORA_NAV_GEOMETRY) beside it at the capsule's full height (slotH + 2·padV — 56, 48 mini, so it shrinks in step with the scroll-mini bar), the verb morphs with a glyph crossfade plus a small spring on the incoming glyph (reduced-motion honoured), and Add post hands focus to the feed's always-open composer (the hybrid:compose event) rather than opening a second editor. The Train launcher and logger now light NO capsule slot — the lens fades out in place, the same behaviour every off-bar screen already had. MOBILE (native tabs, (tabs)/_layout.tsx): the four places render from the same AURORA_NAV_TABS table, and the Train trigger takes role=\"search\" — the one detached geometry iOS 26 offers — with the dumbbell SF symbol and Train label applied over the system item (react-native-screens sets a custom icon/title over a systemItem when given one). Android and iOS < 26 degrade to a plain trailing fifth tab, correct for platforms with no detached slot. VERIFY ON DEVICE with the next TestFlight build: that the search-role circle wears the dumbbell + 'Train' (not the system magnifier/'Search') — the sandbox cannot render the native bar. The morph gap on mobile is recorded as nav-action-morph-mobile. Guarded by nav-bar.test.ts (capsule = the four places, train excluded, action table + per-surface resolution)." },
  { id: "nav-action-morph-mobile", area: "Mobile", status: "shipped", title: "Mobile nav \u2014 the detached circle morphs per surface", detail: "THE WALL FELL, AND THE RECORD WAS STALE. This entry said the native bar could not honour the contract's contextual action \u2014 'a native trigger is a ROUTE, not a button, and expo-router's unstable native tabs expose no tabPress intercept to repurpose the press'. The first half is still true; the second is not, in the installed version: NativeTabs.Trigger now takes `listeners.tabPress`, and a trigger marked `disabled` STILL EMITS that press while skipping navigation (types.d.ts: 'the navigator emits tabPress but skips navigation'). A disabled trigger plus a press listener is exactly a button. WHAT SHIPPED: the action is assembled from whatever the visible surface asks for. nav-bar.ts gained a THIRD action (search \u2014 'Find') and, more importantly, a `kind` on every action: `route` means the circle GOES somewhere and is an ordinary trigger; `screen` means it ACTS on the surface in front of you and is a disabled trigger whose press the surface handles. apps/mobile/lib/nav-surface.ts is the channel \u2014 a module store (not a context: a provider would have to sit above the tabs layout, and every screen would re-render for one trigger) where a screen publishes its surface id AND the handler for the press, both cleared on unmount so a screen cannot leave a stale verb or a dead ref in the bar. The route `name` stays `log` in every state, because `name` identifies the trigger to the navigator and swapping it would rename a route under a mounted navigator. FIRST USER: the add-to-meal picker (nav-action-picker-search). REMAINING: the FEED's Add post morph is now a small follow-on rather than a platform limitation \u2014 it needs the Today hub to publish 'feed' as its surface and the composer to expose a focus handler, and the same mechanism carries it. See also feed-composer-retire, which was BLOCKED on this constraint and no longer is." },
  { id: "nav-action-picker-search", area: "Mobile", status: "shipped", title: "The add-to-meal picker's own verb in the detached circle", detail: "ASKED FOR FROM A SCREENSHOT of Apple Podcasts' split bar \u2014 a glass capsule with a detached circular Search beside it \u2014 with the note that it should apply to the nutrition meal pages (Snacks, Breakfast, \u2026) and nowhere else. HYBRID's bar was already that anatomy and already the system's own Liquid Glass (nav-native-tabs), so nothing had to be drawn: the only question was what the circle carries. On the picker it now carries the MAGNIFIER and the label 'Find'; everywhere else it is the dumbbell, unchanged. WHY THIS DOES NOT REOPEN THE SPENT-SLOT TRADE (nav-split-action, global-search): the recorded trade is that the detached slot is never spent on a cross-app search we do not have. This is not a search destination \u2014 it is the surface's own verb, in exactly the sense Add post is the feed's, and it is resolved FROM A SURFACE so it can never become the app-wide default (guarded in nav-bar.test.ts). WHAT THE PRESS DOES: opens search \u2014 scrolls the picker home, reveals the field and focuses it, with a light haptic. THE FIELD IS NO LONGER A PERMANENT BAND, and that followed directly from the circle: once the bar carries this screen's Find, a search field pinned under the day header is the same question asked twice ten inches apart. So search is a STATE the circle opens (the pattern the pantry already uses \u2014 `searchOpen` there, with a hero accessory as the toggle instead of the bar), and the resting screen is what the athlete came for: what the day owes, the four sources, the list. Leaving search clears the query, because a stale query behind a closed field would leave the list showing results with nothing on screen explaining why; leaving the picker leaves search with it. TWO CONSEQUENCES HANDLED RATHER THAN ACCEPTED: (a) the keyboard covers the bottom bar, so the control that OPENED the field cannot be the one that closes it \u2014 the field carries a plain-word Cancel (no bordered box, per the exit rule) reachable while typing; (b) the BARCODE SCANNER was the field's trailing glyph, which was right while the field was always on screen and wrong the moment it went behind a toggle \u2014 scanning a label is a primary way to add a food, so it moved to the head's one trailing slot, which this screen had left empty and which is always there. Built on the morph mechanism in nav-action-morph-mobile; the field takes an `inputRef` and AuroraScreen's plain path now forwards a `scrollRef` (the hero path deliberately does not \u2014 that scroller has a collapse track to respect). NOT VERIFIED ON DEVICE: the disabled-trigger + tabPress path is exercised by typecheck and the iOS bundle export only, and the sandbox cannot run a simulator." },
  { id: "feed-composer-open", area: "Core", status: "shipped", title: "Feed — the always-open composer (the X compose box)", detail: "The feed's composer no longer hides behind a collapsed one-line pill that expanded into a bordered card on tap — that tap was a door in front of an empty page, and the expanded card wrapped the input in chrome the stream around it doesn't wear. On BOTH clients (web social-feed.tsx, mobile feed-view.tsx) the box is now ALWAYS OPEN at the head of the stream in the X compose grammar: my avatar beside a bare auto-growing field at heading size (the placeholder IS the invitation), one accent glyph row beneath it — the attach-my-latest-PR mark in X's toolbar position, filling when armed — with the Share pill on its right, DISABLED until there is text or an attached PR; no fill, no border, no radius, no Cancel and no open/close state, the whole block running edge to edge between two hairlines (it bleeds under the screen gutter exactly like the timeline rows, so its hairline and the stream's are the same width). Two parallel builds of this idea merged here: the compose-box grammar with the avatar and auto-grow field, and the nav handoff — the pill nav's Add post circle (nav-split-action) doesn't open a second editor, it fires the hybrid:compose event and the composer scrolls itself into view and takes the caret, so there is exactly one place a post is written. No autoFocus on mount — an input that grabbed the keyboard on every feed open would make the stream unreadable." },
  { id: "global-search", area: "Core", status: "shipped", title: "Cross-app search — mobile has no search entry point", detail: "SHIPPED (Aug 2026) — in the SIDE MENU, which is the honest home for it: the drawer is already the list of everywhere you can go, so making it searchable rather than only scrollable is the smallest true version of the feature, and it needs no new route, no nav-bar change and no parity-guard churn. An ASearch sits between the identity block and the primary list; while a query is present the results REPLACE the drawer's own lists (stacking them would bury the answer under a screenful of navigation). Corpus + routing in packages/core/src/global-search.ts (19 tests): reachable SCREENS (persona + access filtered, locked ones shown with a lock and routed to the paywall), SETTINGS (title + subtitle + the keywords that already powered matchSettings), MOVEMENTS and SPORTS (straight off buildExerciseIndex, so every nickname, rename breadcrumb, muscle term and prominence weight comes with them), PLANS (goals + named plans, each carrying its goal so a result lands on the plan and not the library root), RECIPES and HELP. Ranked by the SHARED engine (ranked-search), so 'hamstrings' reaches the Romanian Deadlift, '2fa' reaches Password & security, 'rdl' still works and a typo still lands — none of it re-implemented. Results are grouped by kind (groupGlobalResults), groups ordered by their own best hit, because reading a mixed list means re-deciding what each row IS on every line. Three deep links were added so a result arrives at the THING rather than its screen: /plans?goal=&plan=, /nutrition?recipe= and /settings?cat= (a category that lives on its own screen — Edit profile, Session logger, Coaching — is routed there directly via the exported SETTINGS_ROUTES, so a result never lands on a page that only bounces the athlete onward). The index is built on the drawer's FIRST OPEN and kept, never on a Today render for a drawer nobody touched. THAT DEFERRAL SHIPPED A CRASH, fixed Aug 2026: both memos were written BELOW the drawer's `if (!open) return null`, so opening the drawer rendered two more hooks than closing it and React threw on the first tap of the avatar (see hook-order-guard). They now sit ABOVE the bail-out and key off `everOpen` instead, which defers the work exactly as intended without making the hook list conditional. Emits SEARCH.global through track(). The tab-bar slot stays spent on Train — that trade and its test guard are unchanged. STILL NOT COVERED: logged sessions and people (both need a server query, not a local index) — The motion audit's §4 flagged the reserved iOS 26 search slot beside the tab bar: 'correct to reserve it; it now needs filling.' The product answered the SLOT question differently — it is deliberately SPENT on the Train action (the split bottom bar detaches the app's one verb into the circle via role=\"search\", and the circle never wears a magnifier; see (tabs)/_layout.tsx's own reasoning), so a future search does NOT live there and the audit item is closed as a decision, not a build. What remains true: web has ⌘K and per-screen search fields; mobile has per-screen search (Exercises, the food pickers, Discover) and NO cross-app search anywhere. If cross-app search is built it needs its own surface on BOTH clients — most plausibly grown out of Discover's search or the side menu — and it should search the things athletes actually re-find: exercises, logged sessions, foods, people, plans." },
  { id: "search-one-ranker", area: "Core", status: "shipped", title: "One ranked-search engine, every search field", detail: "The exercise picker's ranker was written for one field and, the moment it worked, every other search in the app wanted it and had none of it. Extracted to packages/core/src/ranked-search.ts (20 tests, deliberately exercised on a NON-exercise pool — if the engine only behaves when its entries are lifts it is copied, not shared): the bands (exact > whole word > word prefix > in-word > what-it-is > typo), the adjustments (words you didn't ask for, prominence, what you actually use), the synonym vocabulary, the typo slack and the tie-breaks all live there in ONE weights block, pinned by a test that no adjustment may carry a result across a band boundary. exercise-search.ts is now an ADAPTER — it decides what a movement answers to, what it trains and how canonical it is, and knows nothing about weighting; its 54 tests pass unchanged. Second caller: the home QUICK-SPORT picker, which had its own `name.includes(q)` (so 'swiming' or a reversed 'bike ride' found nothing there while the picker two screens away handled both) and now ranks, weighted by the sports this athlete actually logs. Third caller: global-search. The drift this prevents is the point — five fields once drew five different tails; five searches would have grown five different vocabularies." },
  { id: "search-vocabulary", area: "Mobile", status: "shipped", title: "Search misses — the nickname backlog, written by the people who use it", detail: "The exercise search ships ~50 hand-curated nicknames ('rdl', 'ohp', 't2b', 'farmers walk'). Every one is a GUESS about gym slang, some are wrong, and the gaps are invisible: a query that finds nothing looks exactly like a query for something that does not exist, and the athlete quietly types a custom name instead — which splits their own history under a spelling nobody chose. Now recorded at the two moments the vocabulary demonstrably failed: a real query (≥3 chars, filed on sheet CLOSE rather than per keystroke so prefixes never reach the list) that matched nothing, and — weighted 3× higher — a query a custom movement was created from. packages/core/src/search-misses.ts is the pure reducer (18 tests): normalize, aggregate by query, weight, sort, cap at 50, and survive any corrupt stored value. apps/mobile/lib/search-misses.ts persists it per device (AsyncStorage, the logger-prefs/exercise-favourites pattern) and also emits SEARCH.miss / SEARCH.customAdd through track(). Read in the MOBILE ADMIN console on the Exercise library screen — where an operator already is when they would act on it — as 'Searched, not found', each row either a nickname to add or a movement the catalog lacks. No web twin by construction: the web console cannot read a phone's storage, so a panel there would be permanently empty rather than merely sparse." },
  { id: "search-vocabulary-sync", area: "Backend", status: "planned", title: "Search misses — aggregate across athletes", detail: "The miss backlog is device-local, so it is one athlete's gaps rather than the fleet's — useful for an operator testing on their own phone, and not yet the product backlog it should be. Needs a table (query, reason, count, first/last seen, no athlete id — the words are the finding, not who typed them), an endpoint to post a batch on app background, and an admin view that ranks by distinct-athlete count rather than raw hits so one person's typo cannot look like a trend. The client half is done: recording, weighting, capping and the local read are all shipped (search-vocabulary), and SEARCH.miss / SEARCH.customAdd already fire through track()." },
  { id: "exercise-picker-queue", area: "Mobile", status: "shipped", title: "Add the whole warm-up at once — the picker's queue", detail: "The picker closed on every pick, so building a six-movement session meant opening it six times: with search this fast, the sheet's own open/close animation had become the slowest thing left in the flow. Now a MULTI mode that keeps it open. Entered two ways — a head-level SELECT control in the right slot of every section heading, INCLUDING the search results (which gained a head, and with it the result count the view never had; the Explore SectionHead grammar, small mono uppercase, on the right of the same row, never a second band of chrome), or a LONG PRESS on any row, which starts the queue from that movement wherever you are. The control CANCELS when multi mode is on — the queue commits from the bar, and a control labelled 'Done' that silently discarded the selection was a promise the bar was already keeping. In multi mode a tap toggles instead of adding-and-closing, the row's right slot shows a checked ring in place of its shape hint (so a single-pick sheet is unchanged and nothing shifts sideways when the queue starts), and a bar on the panel — above the sheet's own bottom pad, never scrolling away under the movement you are about to add — carries the count and 'Add N'. The set arrives in the order it was chosen, in ONE state commit and ONE layout animation (workout.tsx addExercises, builder addMany), so six lifts land together instead of six sheets opening and closing. A queued CUSTOM movement resolves its kind the way a single custom add does rather than being dropped by a catalog-only lookup — queue five, get five. The single-tap path is untouched and still two taps. Offered only when the caller passes onPickMany — a control must never promise something the screen behind it cannot do." },
  { id: "nav-collapse-on-scroll", area: "Core", status: "shipped", title: "Bottom-nav shrink-on-scroll (full at top, compact when scrolled)", detail: "The floating Aurora bottom nav now SHRINKS as you scroll (the requested Instagram behaviour): full size at the very top, scaling down smoothly to a compact pill as the active surface scrolls, and re-expanding when you scroll back up or land on a new screen. Continuous over the first 48px of scroll (not a hard snap). Built at web<->mobile parity, honouring reduced motion, and it scales the WHOLE bar — the centre Train FAB + the sliding selection indicator are children of the scaled element, so everything shrinks together. WEB apps/web/components/aurora/pill-nav.tsx — a passive window-scroll listener (the shell scrolls the document, no inner overflow container) drives an imperative per-rAF transform: scale on the bar (transform isn't in its React-managed style, so re-renders never clobber it); recomputed on screen change so a short screen re-expands. MOBILE — RN has no global scroll event, so a shared signal was added: apps/mobile/lib/nav-scroll.tsx (NavScrollProvider mounted once at the root in app/_layout.tsx) exposes a `collapse` Animated.Value + an `onScroll` handler; the scroll surfaces publish their offset by spreading useNavScrollProps() — wired into BOTH shared screen primitives (the Aurora AuroraScreen AND the classic lib/ui Screen, since Aurora is the only template so classic-Screen screens like Coaches/Admin still sit under the pill), plus every bespoke scroller that opts its parent out of the primitive's ScrollView (scroll={false}): the Today/home ScrollView, and the FlatLists in the social Feed, workout History and the Leaderboard. Horizontal rails/pagers/chip rows and modal scrollers are deliberately NOT wired (a sideways swipe or a sheet must not collapse the bar). apps/mobile/components/aurora/global-nav.tsx consumes `collapse` and interpolates it to scale/translateY/opacity on the pill wrapper, resetting to full on every route change (so a new screen always starts expanded). Both typechecks, the web build, the iOS bundle export and the 804 core tests stay green. ICON-ONLY WHEN SMALL: past the collapse threshold the bar now also DROPS ITS LABELS — the small bar is glyphs alone, matching the native minimized tab bar (the requested behaviour). Each slot's label row animates its HEIGHT (not display) to 0 while fading out, so the slot morphs smoothly from 60x46 to 44x34; the selection lens, the capsule and the detached Train circle (58 -> 46) tighten with it, and the residual scale was eased back (web 0.84 -> 0.94, mobile 0.82 -> 0.94) so the geometry does the shrinking and the bar never scales down to an untappable size. Accessibility is unaffected: the visible label is decoration over an aria-label / accessibilityLabel that is always present, so screen readers announce the same names in both states. Because it is a LAYOUT change rather than a continuous ramp, it flips on a hysteresis-guarded boolean (on at 0.6 of the collapse ramp, off at 0.25) so it cannot flicker at the threshold. WEB — CSS transitions on the slot/lens/circle box, the mini flag derived in the same per-rAF scroll pass that sets the scale; the sliding indicator re-places on the flag (and now only fires its travel-stretch on a genuine slot change, so resizing for mini does not read as a hop). MOBILE — a JS-driven miniAnim timing value (width/height/radius are not native-driver props) fed by a listener on the shared collapse value; the fallback chalk pill splits into two nested views so the native-driven opacity crossfade and the JS-driven radius never share a style object, and on iOS 26 the native Liquid Glass lens resizes with the slots via the existing .animation(value:) tick. Reduced motion keeps the bar full-size (no collapse ramp, so no mini state) on both clients." },
  { id: "named-plan-today", area: "Backend", status: "shipped", title: "Named-plan 'Your plan today'", detail: "When a user enrols in a REAL uploaded plan, that plan's exact day (planToday → PLAN_DETAIL) drives the Today card instead of the engine's algorithmic prescription. Both clients SEND the enrolled planId on enroll — web components/plans.tsx + aurora/plans.tsx, mobile aurora/plans.tsx, and the onboarding enroll — and the backend persists it on macrocycle.planId; the macrocycle.planId column has been migrated in Supabase (reference/sql-macrocycle-planid.sql), so the POST /api/macrocycles handler keeps the planId and useMacrocycle surfaces it to Today on both clients. Previously NO client sent planId AND the column was unmigrated, so Today always fell back to the generic prescription ('Back Squat + Assault Bike, readiness 98')." },
  { id: "perfstate-naming", area: "Core", status: "shipped", title: "'Performance State' naming (Athlete Twin retired)", detail: "The fused current-state object is called 'Performance State' everywhere user-facing — the legacy 'Athlete Twin' / 'Athlete Hybrid' / 'digital twin' wording is fully retired across both clients and all EN/PL/DE i18n string VALUES (DE: 'Leistungszustand', PL: 'Stan formy'). The engine file is engines/performance-state.ts (exports computePerformanceState / PerformanceState / performanceTrajectory — unchanged); i18n KEY identifiers were intentionally left as-is to avoid churn." },
  { id: "squad-monitor", area: "Web", status: "shipped", title: "Squad monitor (RAG + ACWR)", detail: "The screen a team coach opens every morning: one row per ACTIVE athlete with readiness (red/amber/green), training-load ACWR + band, acute load, injury-risk band + flagged tissue, HPI and last session — sortable worst-first, with a squad summary strip (low-readiness / ACWR / injury flag counts). New session-load engine (load.ts: sRPE load → rolling 7d:28d ACWR + monotony/strain, unit-tested) surfaces the load math; /api/coach/squad computes everything server-side from each consented client's real sessions + signals (CoachLink-gated)." },
  { id: "forceplate-ingest", area: "Integrations", status: "retired", title: "Force-plate / jump-test CSV ingest", detail: "DELETED (Aug 2026). Was: forceplate.ts parsing a Hawkin/ForceDecks-style CSV (wide or long) into jump height / asymmetry / body mass Signals, a web import screen (file/paste, recognized-vs-skipped preview, one-click import) with a jump-height trend chart, and mobile parity. Removed: packages/core/src/engines/forceplate.ts, its tests, the `forceplate` nav destination and both Force plate screens. KEPT: the jumpHeight / asymmetry Signal kinds and the generic /api/signals ingest — the ontology is fine, it was the CSV importer and its screen that had no user.", retiredBecause: "Roughly 5,000 institutions worldwide own force plates and every one of them already runs Hawkin or VALD software. A CSV importer does not displace the software that ships with the hardware." },
  { id: "composition", area: "Core", status: "shipped", title: "Bodyweight trend (smoothed)", detail: "composition.ts: EWMA-smoothed bodyweight + fitted weekly rate (kg/wk) from bodyMass signals (the weekly check-in weigh-in writes one), so the UI shows the trend instead of demotivating daily noise. Pure + unit-tested. Charted on the Nutrition screen on BOTH clients — web via a recharts line (raw + smoothed), mobile via the native bar-trend idiom (no react-native-svg) on the smoothed series; both append a weightChangeKg/28d suffix to the maintenance line." },
  { id: "auto-segmentation", area: "Web", status: "shipped", title: "Coach auto-segmentation (triage)", detail: "segment.ts: athleteSegment buckets each athlete (needs-attention / dormant / new / on-track) from the squad signals the monitor already has — readiness, ACWR band, injury flag, days-since-last, session count. The Squad monitor filters + counts by segment and tags each row, turning a 100-athlete roster into a 'who needs me today' list. Pure + unit-tested." },
  { id: "coach-tags", area: "Web", status: "shipped", title: "Manual coach roster tags", detail: "Coach-authored labels on each CoachLink (e.g. powerlifting, remote, comp-prep) edited as chips in the client detail; the Squad monitor shows them per row and filters by tag, alongside the auto-segments. CoachLink.tags text[] (reference/sql-coach-tags.sql adds the column); GET + 'tags' PATCH action on /api/coach/links/[id], surfaced via /api/coach/squad. NOTE: run reference/sql-coach-tags.sql in Supabase. MOBILE PARITY: the Aurora coach console's client detail (apps/mobile/components/aurora/coach.tsx) reached parity with web's ClientDetail — roster tag chips (add/remove via the same 'tags' PATCH), per-client template ASSIGNMENT (pick a saved template + date → POST /api/coach/links/[id]/assignments), GENERATE PERIODIZED WEEK (enroll a macro via POST …/macrocycle, then buildTrainingWeek → bulk-assign the week off the client's real cadence/logs), and WEEKLY CHECK-INS with a coach reply (GET …/checkins + PATCH /api/checkins/[id]) all now ship on mobile too, using the same endpoints and the mobile fetch helpers in apps/mobile/lib/api.ts." },
  { id: "progress-photos", area: "Web", status: "shipped", title: "Progress photos", detail: "Body-recomposition photo timeline in a PRIVATE Supabase Storage bucket under progress/{auth.uid()}/… with owner-folder RLS, so each user only ever sees their own. Listed straight from storage (no app table to keep in sync), displayed via short-lived signed URLs; upload + delete from the web Progress screen. reference/sql-progress-photos.sql creates the bucket + policies. Web screen uploads/deletes; mobile screen (expo-image-picker) captures from camera or library to the same bucket — the phone is the natural capture surface. NOTE: run that SQL in Supabase; the storage upload path is untestable from this sandbox (Supabase host blocked), and the mobile native module needs an EAS build to run on-device (blocked on the Apple Developer account + Expo token)." },
  { id: "workout-builder", area: "Web", status: "shipped", title: "Workout builder + template library + scheduler", detail: "Build a workout (strength/conditioning blocks, sets×reps×load×RPE×velocity) with reorder (↑/↓), duplicate, and add/remove, then save it as a reusable WorkoutTemplate. A coach assigns a template to a rostered athlete on a date (CoachLink-gated); blocks are SNAPSHOTTED at assignment time so later template edits never change a workout already assigned. The athlete sees assignments on the mobile Home ('Assigned by your coach' → Start/done) and the web Calendar (violet markers + Mark-done); the coach sees status (assigned/completed/skipped) in the client detail. WorkoutTemplate + Assignment models + reference/sql-workout-builder.sql (own-rows + active-coach RLS); /api/templates, /api/templates/[id], /api/assignments, /api/assignments/[id], /api/coach/links/[id]/assignments. NOTE: run reference/sql-workout-builder.sql in Supabase to create the tables. ACCESS: composing your own routine in the Builder is FREE for every persona (the `builder` nav item is no longer minPersona-gated) — this fixed a web↔mobile parity gap where mobile surfaced the Builder to free clients (More hub + Train launcher) but web hid it from casual; Today's first-session 'Build your own' chooser is now a free path into the Builder (no ✦ Full badge), and the Builder was dropped from the Full upgrade bundles. A FREE user can also SAVE up to 2 templates (FREE_TEMPLATE_LIMIT, see workout-routines); an unlimited template library — plus adaptive plans / periodization / auto-progression — is the PAID layer, not the act of building a workout." },
  { id: "workout-routines", area: "Mobile", status: "shipped", title: "Save workouts as routines (self-serve, load-and-go)", detail: "The athlete-facing routine loop, closed on top of the existing WorkoutTemplate infra (model + /api/templates own-rows GET/POST + [id] DELETE). MOBILE: the Train launcher (app/(tabs)/log.tsx) now lists 'Your routines' below empty/AI/repeat-last — tap one to start (router.push('/workout?source=template&templateId=…'), which the live-workout screen loads via fetchRoutines→blocksToExercises); and the finish Summary has a 'Save as routine' action (name → createRoutine) for non-guest users. WEB: both loggers (Aurora components/aurora/logger.tsx + classic components/logger.tsx) show a 'Your routines' quick-start (tap to load a saved template's blocks into the editor). 'Save as routine' has MOVED off the live logging surface to the finish screen — a shared SaveRoutineCard (components/save-routine-card.tsx) renders a collapsed pill → inline name field → saved confirmation, so a workout is named only when it's actually being saved as a reusable routine (nobody names a plain workout). New mobile api helpers fetchRoutines/createRoutine/deleteRoutine. Reuses the shared SessionBlock shape so routines and logged sessions share one editor and can't drift; routines need an account (guests see none). Localized EN/PL/DE (train.routines, summary.saveRoutine/routineSaved, w.train.logger.saveAsRoutine/saveRoutineHint/routineNamePh). NEXT: a dedicated 'My routines' manage screen (rename/delete/reorder) and pre-building a routine from scratch (today you save one from a finished/edited workout); the coach-assign path (workout-builder) is unchanged. FREEMIUM GATE (Jul 2026, supersedes the earlier all-paid gate): a FREE (casual) user can now SAVE up to 2 reusable routines — the Builder itself is free for everyone; only a LIBRARY BEYOND 2 templates is Full. One source of truth in packages/core/src/access.ts: FREE_TEMPLATE_LIMIT = 2 + count-aware `canSaveRoutine(persona, savedCount)` (Full personas unlimited, casual true while savedCount < limit, fails closed on unknown personas; unit-tested). The API enforces it server-side: POST /api/templates counts the free client's WorkoutTemplate rows and 403s (code upgrade_required) only AT the limit — entitlement still read from the DB row, never client metadata. UI on BOTH clients + BOTH save surfaces: the Builder save button (web components/aurora/builder.tsx off templates.length, mobile components/aurora/builder.tsx off routines.length) shows a '{used} of {limit} free template slots used' hint (w.train.builder.freeSlots) for free users under the limit and flips to the ✦ upsell card (FUNNEL.upgradeEntryClick source 'builder-save') only at the limit; the finish-Summary SaveRoutineCard (web components/save-routine-card.tsx, mobile app/workout.tsx SaveRoutine, source 'save-routine') fetches the saved count for free users and upsells only at the limit (mobile's api createRoutine is status-aware — {ok, status} — so a stale count's 403 maps straight to the upsell, never a confusing error, and the mobile builder hook's save messages are localized w.train.builder templateSaved/signInSave/saveError/networkError instead of hardcoded English). The Train launchers' routines row is no longer a Premium-badged upsell for free users — everyone sees their saved routines (or the build-first prompt into the free Builder). Upsell copy is limit-aware ('Routine limit reached — Free includes 2 saved routines…', w.train.logger.routineFullTitle/Blurb + summary.routineFullTitle/Blurb, EN/PL/DE); the admin ENTITLEMENT_MATRIX row (Template builder) reads free '2 saved templates' / paid 'Unlimited', and FULL_BENEFITS gained 'Unlimited saved routines'." },
  { id: "today-quick-start", area: "Core", status: "shipped", title: "Today — Quick start a saved routine (favourites + rediscover sheet)", detail: "A fourth 'Train your way' path on Today/Home, beside Follow a plan / Build your own / Just log: a violet ⚡ 'Quick start' card that opens a bottom sheet to re-launch a routine the user already owns (WorkoutTemplate) in one tap — no re-onboarding through the builder every day. The card is ALWAYS present alongside the other three paths; with no saved routines the sheet shows a build-first empty state ('No saved routines yet' + a Build a new routine action), so it's a prompt rather than a dead door. When the user is ON A PLAN (where Today shows the plan hero instead of the four 'Train your way' cards) the sheet stays reachable via a quiet '⚡ Quick start a saved routine' secondary link under the plan card's Start button (w.home.today.quickStartLink), so a saved routine is one tap away without leaving the plan. The sheet (shared aurora/quick-start.tsx on each client, driven off the existing Sheet primitive) shows FAVOURITES as a snap rail on top (respecting the sheet's own padding — bleed is only for screen-level rails), then a shuffle-able 'Rediscover' list of the remaining routines; each card/row starts the session immediately (web onStart(blocks, name) → the shell logger; mobile router.push('/workout?source=template&templateId=…'), the launch path already shipped in workout-routines) and carries a ★ toggle to favourite/unfavourite. An honest one-line meta per routine comes from the shared pure helper routineSummary(blocks) in packages/core/src/engines/session.ts (move count + summed minutes ONLY from cardio/conditioning blocks that carry them — a pure gym routine shows just the move count, never a fabricated duration). A 'Build a new routine' footer routes into the free Builder. Mirrored web (components/aurora/today.tsx + quick-start.tsx) + mobile (components/aurora/home.tsx + quick-start.tsx). i18n w.home.today.chooserQuick* + w.home.quickStart.* + w.home.logbook.slimQuickSub (EN/PL/DE). The favourite FLAG's server persistence is tracked separately as `routine-favourite` (blocked on the reference SQL) — until it runs, starring soft-degrades (the launch + sheet work regardless, and favourites simply don't persist)." },
  { id: "routine-favourite", area: "Data", status: "shipped", title: "Routine favourite flag (persistent star)", detail: "A `favourite Boolean @default(false)` column on WorkoutTemplate powers the Quick-start sheet's Favourites rail (see today-quick-start). Following the plan-maxes soft-guard idiom: the column lives in prisma/schema.prisma but is READ via a guarded raw query merged onto the existing GET /api/templates list (missing column → every routine reads favourite:false, never an error) and TOGGLED via a new guarded PATCH /api/templates/[id] ({ favourite } → $executeRaw UPDATE, soft-degrading to 503 before the column exists). The DELETE handler's owner check now selects only ownerId so it can't touch the new column pre-migration. Mobile api gains fetchRoutines' `favourite` field + favouriteRoutine(id, favourite). The favourite column is APPLIED in production (sql-pending.sql run, Jul 2026) — starring now persists." },
  { id: "today-confirmation-loop", area: "Core", status: "shipped", title: "Today confirmation loop + quick-access (free-version UX pass)", detail: "A pass on the free-version Today + navigation, mirrored web (components/aurora/today.tsx) + mobile (components/aurora/home.tsx) via shared core helpers (packages/core/src/engines/session.ts: sessionsOnDay/trainedToday/sessionShape/sessionCardioTotals). (1) DONE TODAY — every session logged today (a finished prescribed session OR a quick sport log) now surfaces on Today with a ✓ and a sport-adaptive one-line meta (distance–time for cardio, tonnage for lifts), tapping through to its detail; this is the missing acknowledgement that a workout counted (previously Today only ever prompted 'Start'). (2) SPORT-ADAPTIVE SESSION SUMMARY — the session detail + history-list cards read per-sport: a run/match has no 'volume', so cardio sessions show Duration–Distance–Pace (web components/session-detail.tsx + aurora/history.tsx, mobile app/session/[id].tsx + components/aurora/history.tsx). (3) SHARE ANY SESSION — the branded story-card share (previously strength-only / finish-screen-only) is now on EVERY logged session's detail (web + mobile). (4) COCKPIT + SPORT quick-access on Today — the Cockpit (command center) and Sport prescription are reachable straight from Today; free users get an upgrade nudge in the SAME tile (Cockpit is Full; sport-specific prescriptions are Full, logging any sport stays free via the quick-log carousel), FUNNEL sources today-cockpit / today-sport. (5) CALENDAR widget — a swipeable calendar card sits beside the week done-strip (this-week dates, today ringed, logged days lime-filled) for easy day-by-day access → the full Calendar. (6) CONNECT as two labelled sliders — the Today Connect section is a 'Feed' heading over a left/right horizontal rail of feed cards (FeedPreview `horizontal`), then a 'Coaches' heading over the CoachRail, each with an Explore action through to the social surface (revised from the earlier swipe-tabs per feedback: both rails are visible + independently scrollable, not hidden behind a tab). BOTTOM NAV changed to Today – Explore – [Train] – More – Profile on both clients (web pill-nav.tsx, mobile global-nav.tsx): Explore opens the social/discovery Feed, Profile is a bar slot again; Plans/History/Cockpit live in the More sheet. (The Explore slot has since been replaced by NUTRITION and the coach rail moved onto Today — see nav-nutrition-tab.) i18n nav.explore + w.home.today.kDone/doneView/cockpit*/sport*/cal*/connect* + session.duration/distance/pace (EN/PL/DE). (7) LOCKED PREMIUM LIST — the upgrade page (web components/aurora/upgrade.tsx + mobile components/aurora/upgrade.tsx) shows a free viewer the WHOLE Full toolkit with an explicit 🔒 on every feature (the Cockpit flagship + each bundle item); a paid-Simple viewer sees ✓. i18n w.account.upgrade.locked (EN/PL/DE). (8) LOCKED-VISIBLE NAV — reversing the old 'clean nav, no scattered padlocks' freemium rule: a FREE user now SEES every Full (athlete-tier) destination in the nav with a 🔒 badge instead of it being hidden, so the whole toolkit (Cockpit, Analytics, Periodize, Velocity, …) is discoverable; tapping a locked item routes to the upgrade screen (FUNNEL.upgradeEntryClick, source `more-<id>`/`sidebar-<id>`). Coach/admin-only tools stay HIDDEN for a casual user (role-gated, not purchasable). Driven by new core helpers packages/core/src/nav.ts `navForPersonaWithLocks` / `groupedNavWithLocks` (returning {item, locked}), consumed by the mobile More hub (app/(tabs)/more.tsx), the web pill-nav More sheet (components/aurora/pill-nav.tsx) AND the web sidebar (components/app-shell.tsx). Guarded by nav.test.ts (casual sees cockpit locked + coach hidden; athlete sees cockpit unlocked). (9) PLAN → CALENDAR link — the Today PLAN section kicker carries a direct 'Open calendar →' action to the full month Calendar (the swipeable calendar widget was easy to miss behind the horizontal swipe), on both clients via an optional Kicker `action`." },
  { id: "paid-is-full-by-default", area: "Core", status: "shipped", title: "Paid clients are Full by default (Simple/Full toggle retired)", detail: "resolvePersona (packages/core/src/nav.ts) now resolves a PAID client to the athlete (Full) surface BY DEFAULT — paying for Full no longer requires flipping a separate mode toggle. They only fall back to casual if they've EXPLICITLY chosen Simple (clientChoice === 'casual'). Free clients stay casual regardless of choice; coach/admin still outrank. This let the redundant two-tab 'Simple / Full' selector at the top of the mobile More hub (app/(tabs)/more.tsx) be removed — it duplicated the Unlock-Full card for free users and was unnecessary for paid users. The web persona pref in account Settings is unchanged. Guarded by the updated nav.test.ts." },
  { id: "calendar", area: "Web", status: "shipped", title: "Training calendar (web + mobile)", detail: "A month calendar (calendar.ts: sessionsByDay + monthMatrix + loadIntensity, pure + unit-tested) that paints each day by training load (sRPE heat) and marks session count AND assigned workouts (violet), with prev/next/today navigation and a day-detail panel (session volume/load/blocks/readiness and assignments with Mark-done). Reads logged sessions + /api/assignments. The competition-event layer was removed from both clients with `competition-intel`. The shared engine is reused by the native mobile Calendar screen (Home → Calendar)." },
  { id: "fitness-gps", area: "Web", status: "shipped", title: "Fitness GPS home (Today)", detail: "Consumer-first landing that fuses the engines into one screen: today's route (velocity-aware prescription + readiness + why), an 'On track?' card (Accountability Engine band + risk + streak + habit strength + the right intervention), a Future Self projection (12-week strength curve + goal ETA + probability), and the Twin/HPI. The default web screen and the mobile Home tab. Reframes existing intelligence for a beginner — 'what to do today, why, and how far to the destination.'" },
  { id: "week-verdict", area: "Core", status: "shipped", title: "This week — the verdict card (Statistics + Analytics, merged onto Today)", detail: "Statistics and Analytics were two destinations answering the same question at two depths, on two different gates (statistics free for every persona, analytics athlete-only). They are now ONE block on Today, sitting directly under Readiness because it is the same kind of thing: a verdict with its working-out shown. packages/core/src/week-verdict.ts owns every decision — it sums tonnage / session count / training time over the last 7 days, builds a FOUR-WEEK baseline from the four weeks before it, and names the metric whose signed move from that baseline is largest. TWO HONESTY RULES, in the done-receipt tradition: NO BASELINE, NO VERDICT (fewer than MIN_BASELINE_WEEKS = 2 of the prior four weeks carrying training returns `cold` — the figures render with no claim over them, because a percentage off one week of history is a coin flip wearing a number), and QUIET IS A REAL ANSWER (nothing past VERDICT_THRESHOLD_PCT = 15 returns `flat` and names no metric — a card that finds something wrong every week is a card people stop reading). An EMPTY prior week counts INTO the baseline rather than being dropped: a fortnight off genuinely is part of your average, and excluding it would make every return from a layoff read as a personal best. The card never disappears — a block that comes and goes is worse than one that is sometimes quiet. RENDERING: the sentence takes a {m} template so the metric name can be bolded inside it without string concatenation (EN/PL/DE), the signed delta and the NAMED metric's column both carry the SEMANTIC colour (terracotta down, chartreuse up, ash flat — never the brand accent, so a bad week cannot read as a highlight), and the named metric sorts FIRST in the figure row so the claim and its number are visibly the same thing. FOURTH COLUMN: distance (km) joins tonnage / session count / training time, but ONLY when the athlete has endurance in the window or in its baseline — a pure lifter never carries an empty column, and a runner who took this week off still sees theirs (the week they most need to look at is exactly the week a value-gated column would vanish). It arrived when the Endurance block's own cross-sport totals strip was retired into this card, so Today states the week ONCE; at four columns the figures step down a size and the divider gutter tightens. Distance is read off the same cardio block field the lanes read, so the two cannot disagree about a kilometre. Twin components on both clients (aurora/week-verdict.tsx), mounted under the Readiness card. NAV: both leave More, the sidebar, the pill-nav and ⌘K via promotedTo, which keeps their routes, icons, i18n labels and persona gates live (the mechanism Endurance uses) — `analytics` promotedTo \"today\" and `statistics` promotedTo \"history\", because Statistics turned out to duplicate History rather than this card (see history-calendar-views: it is now History's TREND view). Two rows under the card are the doors back to that depth: History → every session, the week chapters and the trend chart, and — for athletes only — Analytics → /analytics (per-lift loads and trends, plus the coach/operator scopes for roles that hold them), so nothing is stranded. SUPERSEDED IN PART (Aug 2026) by activity-breakdown: the window is no longer a fixed rolling seven days and the totals are no longer lifting-shaped — the card carries a date filter and every figure opens into its own sessions. week-verdict.ts now takes an ActivityRange and compares it against the preceding windows of the same length (four for a week, three for a month, two years for a YTD), and its totals come from activity-window.ts so the figure and the list behind it are one arithmetic. ONE ORDER, ONE VALUE GRAMMAR (Aug 2026): the four columns render in core\u2019s VERDICT_METRICS order \u2014 Tonnage, Sessions, Hours, Distance \u2014 always. The row used to re-sort itself with the SENTENCE\u2019s metric pulled to the front, so the columns rearranged every time the week\u2019s story changed and no figure was twice in the same place; the subject is already marked by the bold word in the lead, the column\u2019s tone and the delta beside it, none of which cost the layout a position. The breakdown sheet\u2019s per-session meta line follows the same order (it was four hand-written branches with four different sequences, so it contradicted the columns it hangs under and itself), dropping only the metric the row\u2019s own value already carries and reading distance in the GROUP\u2019s unit so a 600 m swim can\u2019t say \u201c0.6 km\u201d on one line and \u201c600 m\u201d on the next. And every column now shares one grammar: the LABEL names the metric, the FIGURE carries its unit. Distance printed bare (\u201c25\u201d) under a label that was itself the unit (\u201cKM\u201d) \u2014 the only one of the four split that way, and unfixable in the label because tonnage\u2019s unit is the athlete\u2019s (t or lb) and has to travel with the number \u2014 so w.home.week.lDistance became Distance / Dystans / Distanz and the figure reads \u201c25 km\u201d. Precision is unchanged: km still rounds through distance.ts. Pinned by apps/mobile/test/week-verdict.render.test.tsx, which mounts the card on a deliberately DISTANCE-led week (the one case the old sort acted on) and asserts the order and the units. 17 engine tests." },
  { id: "figure-order", area: "Core", status: "shipped", title: "One reading order for every training figure (figure-order.ts)", detail: "Fixing the Progress card\u2019s column order (see week-verdict) surfaced the general case: the app printed the same handful of numbers in SIX different sequences. The done receipt led with duration, the profile tiles with the session count, the share card with time, the leaderboard put distance third, the endurance section put it second, and Wrapped had five orders of its own \u2014 one per discipline. Nothing chose any of them; each row was simply in the order it happened to be typed. A row of figures is found by POSITION before it is read at all, so packages/core/src/figure-order.ts is now the single answer: FIGURE_ORDER (tonnage \u2192 sets \u2192 reps \u2192 rounds \u2192 sessions \u2192 active days \u2192 streak \u2192 hours \u2192 distance \u2192 pace \u2192 elevation \u2192 kcal \u2192 hr \u2192 effort \u2192 PRs), plus figureRank() and a STABLE orderFigures() that sorts a row without touching which figures are in it. It EXTENDS ACTIVITY_METRICS rather than competing with it \u2014 the four Progress totals keep their relative order and everything else attaches to whichever of the four it is a facet of (sets and reps are tonnage\u2019s grain, active days and the streak are the session count\u2019s, pace and climb are the distance\u2019s) \u2014 and a unit test asserts exactly that, so the two can never drift apart. Aliases (volume/duration/minutes/efforts/km/climb/energy) rank as their canonical figure, so no surface had to rename its own stat keys to comply; an unknown key sorts LAST instead of throwing, so a sport\u2019s own measure still renders. TWO THINGS IT IS DELIBERATELY NOT. It is not a SELECTION rule \u2014 which figures a surface shows stays that surface\u2019s business (a swim has no sets, Wrapped still picks four tiles by discipline priority), and those lists now feed through orderFigures() so the choice and the sequence stop being the same line of code. It is not a PRIORITY rule either \u2014 doneReceiptHero and sessionHeadline still choose the ONE figure that earns display size (tonnage, else distance, else duration), which is a claim about what the session was ABOUT, not a listing order. That distinction bit once, in this very change: Wrapped\u2019s hero falls back to the top of the discipline\u2019s priority list when every other branch misses, and it read that fallback off `basics[0]` \u2014 so sorting `basics` silently redefined \u201cthe most important figure we have\u201d as \u201cwhichever figure sorts first\u201d. Reachable in 23 configurations (an endurance session whose only extras are climb and energy; a mixed circuit with bodyweight sets and no clock, whose hero would have flipped from \u201c3 km\u201d to \u201c2 sets\u201d). The selection now keeps its own array \u2014 `chosen`, in priority order, feeding the hero \u2014 while `basics` is the laid-out row, and session-wrapped.test.ts pins both halves against that circuit. CONVERTED: doneReceiptStats, feed-card sessionStats + FeedStatKey + FEED_STAT_LABEL_KEY, feed-workout feedWorkoutStats (the extras are sorted IN beside their own kind rather than appended, so an opened post is the card with more of it instead of a second layout), session-wrapped\u2019s basics tiles, ENDURANCE_METRICS (which did not even match the EnduranceTotals shape above it), SportTotal, social\u2019s LEADERBOARD_METRICS + compareAthletes + ProfileStats, WeeklyRecap, WeekChapter.totals, workoutShareCaption, and on mobile the analytics/profile/trends/coach/history/history-views/statistics rows, the two workout share cards, the recap share card and its text, and the public profile tiles. Tests updated where they pinned an old sequence (done-receipt, feed-workout, sport-page, workout-caption); figure-order.test.ts pins the order itself." },
  { id: "activity-breakdown", area: "Core", status: "shipped", title: "Today's summary card — date filter + figures that open (total activity)", detail: "The \"This week\" card stated one rolling seven-day window of mostly-lifting totals and stopped there. Three things were wrong with that, and this fixes all three. (1) THIS WEEK WASN'T THIS WEEK: a rolling now−7d window on a Thursday reports last Friday under a label claiming the current week. It is MONDAY → SUNDAY now (localMondayMs — the same week History, the plan schedule and every calendar surface already use). (2) THE TOTALS WEREN'T TOTAL: training time counted only sessions with a completedAt, so a tennis match logged as 90 minutes on a block contributed NOTHING to the week's hours. A session's minutes are now the greater of its wall-clock elapsed time and the sum of its blocks' own minutes (a watch import carries 44 minutes on a session with no stopwatch; a two-hour gym day holds one logged 20-minute finisher), block minutes are attributed to their own sport, and whatever wall-clock is LEFT OVER belongs to the lifting — or, in a session with no lifting, to its largest activity — so every group sums back to exactly the figure the card shows. (3) THE FIGURES HAD NO RECEIPTS: \"41.6 km\" is four sports in a trench coat — 39 km of running, 600 m in the pool, the rest across tennis and squash. CORE: activity-window.ts owns the period model (resolveActivityRange: week | d7 | d30 | ytd | m:YYYY-MM, with `through` capping a live period at the end of today and `days` counting only elapsed ones), activityBaselineWindows (the preceding windows of the same length, TRUNCATED to the current period's elapsed length so three days of this week are compared against the first three days of the four before it, not against four finished weeks), activityMonths (this month back to the oldest session, empty months INCLUDED — a blank March is a fact about the training year), and activitySummary, which returns per-metric GROUPS + the sessions under them. Grouping is by SPORT, not by tag: every racket/team/combat block carries the same `sport` discipline, so they group by NAME (Tennis and Squash stay apart instead of collapsing into one lane called \"Sport\"), and a group keeps its own natural distance unit so 600 m of swimming never has to read as 0.6 km inside a km total. BOTH CLIENTS (parity): aurora/week-verdict.tsx on web + mobile. The filter is the iOS segmented-control idiom — a track, one thumb that TRAVELS between segments (CSS transform on web, Animated.spring on mobile), the label it lands on taking the foreground — with a fifth segment opening a grouped-list sheet of individual months; the choice persists per device (localStorage / AsyncStorage). Every figure column is a button: pressing one expands a drawer beneath the row with a caret that SLIDES to point at the column it belongs to (a 0fr→1fr grid row on web, a measured Animated height on mobile), carrying a share bar, one tappable row per activity (tap to narrow the list) and the sessions themselves — each opening the logged session. 16 breakdown tests + 17 verdict tests. i18n EN/PL/DE. BUG FIXED AFTER THE FIRST SHIP: the mobile drawer never opened — its measured height was taken in flow inside a box clipped to zero, which measures zero. It now runs the kit's shared `ADrawer`; see volume-detail-drawer for the trap and the fix. SELECTION TOOK THE COLOUR (Aug 2026), and paid for three container edges with it. The figure row's tone used to mark the metric the SENTENCE named and nothing else, so it never moved when a column was opened — pressing Hours left the chartreuse sitting on Distance, and the press itself registered only as an `ink`-on-`ink2` fill invisible on a phone in daylight. Because pressing was invisible, the drawer had to shout: a second bordered, 16px-radius card inside the 28px card, with a caret travelling between columns to point back at whichever figure opened it — four devices saying one thing. Now the OPEN column carries the tone, drawn from its OWN move via core's new `figureDirection` / `figureDeltaPct` (same VERDICT_THRESHOLD_PCT the sentence uses, so a move too small for a claim is too small for a hue): a week where hours fell reads terracotta on Hours inside a card headlining a distance rise, and a column inside the threshold reads chalk rather than being coloured for a move it did not make. Selection also gets a NON-HUE channel — a 2px rail under the column, since a flat column is toned chalk and colour vision is not universal — plus a 9% wash of its own tone in place of the old fill. AT REST (nothing open) the named metric still holds the tone, so the resting card is unchanged and the fix costs nothing on first paint. With the lit column doing the pointing, the caret, the drawer's border and the drawer's radius all went: the panel is the card's own LOWER COMPARTMENT, bled to the card's edges and taking its bottom corners (27 = 28 less the border), with the card surrendering its bottom padding while open. The drawer header's session count moved down to the \"Sessions\" rule that introduces the sessions, and its place is taken by that column's own comparison (\"−20% vs 6.4\", w.home.act.vsBase, EN/PL/DE) — the working-out for the colour, beside the colour. Mocked up first as design/activity-card-active-column.html (three options per defect, live and pressable); both clients in the same change. 5 new engine tests. THE BREAKDOWN BECAME A SHEET (Aug 2026) — the third and last shape it has taken: card-in-a-card, then the card's lower compartment, now a bottom sheet. Un-carding it fixed the nested chrome but not the two things underneath it. (1) IT PUSHED THE SCREEN DOWN: groups, a share bar, five session rows and a \"show all\" is several hundred points of content unfolding in the middle of Today, so Records, the exercise rail and the whole Endurance cluster were shoved off the fold by a press meant to answer ONE figure — reading the answer meant scrolling, and closing it left you somewhere else. (2) IT WAS A MODAL WEARING A DISCLOSURE: everything inside it is a detour off the week (narrow to a sport, expand the list, open a session), and the only dismissal was pressing the same column a second time, three hundred points up by then. It now opens the shared aurora/sheet.tsx — the same component this very card's period picker opens ten points above, so the two controls on one card present identically — which means it takes the drag, the scrim and the tap-out every other sheet on Today takes, and moves nothing on the screen behind it. NO `detents`: the sheet rests at its CONTENT height, so a five-row breakdown is a short sheet and \"show all\" elongates it rather than jumping. It is titled with the pressed column's own label, and its first row RESTATES that column's total in that column's tone with that column's comparison beside it — the card is behind the scrim now, so the figure being decomposed has to travel with the decomposition; the panel head kept only its \"where it came from\" label, the comparison having ridden up to the figure it explains. A session opened from inside the sheet waits for the panel to LEAVE before the route is pushed (a Modal is its own native window, so navigating under one raises the session screen behind the sheet) — the same ordering aurora/upgrade.tsx uses for its route pop. The lit column stays, no longer pointing (a sheet is unambiguous about what opened it) but still what the screen looks like the instant the finger lands. MOBILE ONLY: the web twin was retired with the web client (Aug 2026), so aurora/week-verdict.tsx lives on mobile alone. Pinned by apps/mobile/test/week-verdict.render.test.tsx, which presses a column and asserts the breakdown renders OUTSIDE the card's subtree and never inside it — a panel that comes back in-tree is the drawer growing back. THE ROW MARKS THE PERIOD'S TWO ENDS (Aug 2026), which is the third and last owner of this card's colour channel — and the first one that says what the row is for. `metric` is ONE slot, so the sentence can only ever name the biggest move: a week headlining \"+50% training time\" put the chartreuse on Hours and rendered the distance that fell from 4 km to 1 km in the styling of \"nothing happened\", which is the single biggest fact about that week going unsaid on the one row that holds it. Selection owning the colour (the previous pass) fixed an invisible press but made the card's only hue a function of what the finger was doing, and at rest it fell straight back to the sentence's one slot. Core's `activityVerdict` now also returns `best` and `worst` — the figure furthest ABOVE its baseline and the one furthest BELOW it, ranked separately under the same machinery the sentence uses: same VERDICT_BASELINE_FLOOR + prior-window coverage gate applied per direction (a qualified riser beats an ungated one, so the 0.1 km four-week mean that yields \"+5900%\" cannot take the brightest mark on the card off a tonnage with four weeks behind it), and the same VERDICT_METRICS tie-break, so two clients can never rank one period two ways. `metric` is by construction the larger of the same two moves, so the bold word in the lead is always sitting on one of the two lit columns and the card can never point three ways at once. The mobile row tones `best` chartreuse and `worst` terracotta AT REST, before anything is pressed, with the columns between them carrying no tone at all — and the two channels are now kept apart: the MARK is about the period and does not move (tone on the label and figure, plus that end's own signed percentage under it — the working-out for the colour, and the non-hue channel the pair needs since chartreuse-vs-terracotta reads as one grey to a red-green athlete, spoken as \"biggest rise/drop this period\" in the a11y label, EN/PL/DE), while SELECTION is about the finger and travels (the 9% wash and the 2px rail, with an untoned column washing chalk so a middle column still registers the press). `figureDirection` stays exactly where it earns its keep — the breakdown sheet, which carries a figure behind a scrim with the row it was ranked in off-screen, so it colours by that figure's own move; it agrees with the ranking by construction, so an end's column and its sheet always read one hue. 7 new engine tests + 3 render tests (both ends marked, the middle columns not, and each end's percentage printed). THE MAROON WASH IS RETIRED (Aug 2026) — the fall is FOREGROUND, exactly like the rise, and the row carries no fill at rest on any column. The wash made one column a SURFACE while the other three were type on the card, so the row read as three figures and one filled box, and the box was what the eye found first whether or not the slip was the week's story. The asymmetry argument below was wrong about its axis: hue and sign already separate a rise from a fall, and weight is not a third thing the pair needed. The background is now SELECTION's channel alone on every column alike — transparent at rest, a 9% tint of the column's own tone under a finger, chalk at 6% for an untoned one — which also deletes the `maroonLit` \"lifts rather than lightens\" special case that only existed because the two channels were sharing the background. `colors.maroon` / `colors.maroonLit` went with it (the palette now carries NO composited surface colours: a background is a named surface or a withAlpha() tint, never a hand-derived third kind), as did their three contrast guards, replaced by one guard asserting no wash token creeps back in. The three render tests that pinned the wash became three that pin its absence: every column unfilled at rest, the fall still marked in the foreground, and a press filling exactly one column in that column's own tone. WHAT IT REPLACED, kept for the argument: THE TWO MARKS ARE NOT SYMMETRIC (Aug 2026): the fall sat in a MAROON WASH — core `colors.maroon` / `colors.maroonLit`, the palette's only wash, added for this one column. Toned text alone gave the rise and the fall the same visual weight, and equal weight is the one thing the pair must not have: a rise is a bright figure glowing off the card, a fall is a dark stain the figure sits in, and the slip is the half of the week that asks for a decision. The values are COMPOSITED, not alphas — the column only ever sits on `card`, so stating the result is exact where a wash-over-alpha drifts the moment the surface beneath it changes — and the wash LIFTS to `maroonLit` when its column is opened rather than taking the tone-alpha the other columns use, which would have made the fall go PALER under a finger and read as the mark being lifted off. Three new guards in theme/palette.test.ts: terracotta (what is printed ON the wash) clears AA at BOTH levels (5.8:1 and 4.8:1) so a wash can never darken until it swallows its own figure, and the wash stays under 1.6:1 against the card so it reads as a stain on the column rather than as a second card drawn inside the first — the exact reading the breakdown drawer was un-carded to end. 3 further render tests pin the maroon to the worst column, its absence from the rise and the flat columns, and the lift under a press. THE MARKS GOT A BAR OF THEIR OWN (Aug 2026) — VERDICT_END_THRESHOLD_PCT = 5, against the sentence's VERDICT_THRESHOLD_PCT = 15. Ranking the two ends separately did nothing for the week that was actually reported: +31% hours, +18% tonnage, −7% sessions, −9% distance. Every faller sat under the sentence's 15% bar, so `worst` came back null and the ONE measure that went backwards rendered in the same ash as the two that held — the exact failure `best`/`worst` were added to end, reintroduced through the threshold. The two bars answer different questions: a SENTENCE makes a claim about the period and a claim needs a move worth stating, while a MARK only says which end of this row a figure is, and the far end is the far end at 9% as much as at 40%. Not zero, because a row where something is always the worst has stopped meaning anything: five points is where a figure stops being round-off and starts being a direction. The pools are now built at the mark's bar and filtered again at the sentence's, so the ends rank over a SUPERSET of the metrics allowed to claim the lead — plus one override, the winner taking its own end whatever pool it came from, since a sentence won out of the fallbacks while a small qualified move sat in the same direction would have put the bold word in the lead on one column and the tone on another (it can only ever hand a mark to a figure the sentence has already named, so the brightest-mark-on-the-least-trusted-figure case stays shut). `figureDirection` moved to the mark's bar with them — the breakdown sheet has to open in the hue of the column that opened it. `enduranceDirection` deliberately did NOT: it tones the endurance section's LEAD, which is a sentence with no row to be an end of. 4 new engine tests + 2 render tests (the reported week's −9% distance taking the drop mark and the maroon, its single-digit percentage printed, the sentence still on the rise, and a −2% noise week still marking neither end). THE ROW BECAME A GRID (Aug 2026) — the card was reported as visually broken and it was: on a hybrid athlete's week \"DISTANCE\" wrapped mid-word to \"DISTA / NCE\" and \"9.18 km\" wrapped to \"9.18 / km\", so two of the four figures sat a line below the other two and the receipts read as debris. The wrap was the only defect you could see and the least of the three. (1) THE ROW OF FOUR NEVER FITTED. A 390dp screen leaves the card 326dp; four cells with an 8dp gutter are 75dp wide, ~65dp inside their padding, and \"6h 52min\" is eight glyphs of JetBrains Mono (0.6em advance), so fitting it would take a 13dp figure — the app's BODY size, for the largest figures on Today's largest card. `figSize = wide ? 17 : fs.heading` was a layout apologising for itself: it spent a rung of legibility whenever a fourth metric appeared and wrapped anyway. The figures are TWO UP now (50% cells, wrapping, row-major so VERDICT_METRICS order and therefore position constancy are untouched), which gives each 139dp of type for a 96dp figure at the full 20dp — 40% of headroom, enough for Dynamic Type at 1.4x, for German, and for any formatter that grows a character. One figure size, every cell, every period; the shrink is deleted. A three-metric lifter gets two cells and a third alone on the second row, left-aligned under the first. (2) THE HEADLINE FIGURE IS GONE, which is the deletion the card most needed. The lead's corner drew the named metric's percentage at 23dp and the cell for that same metric drew the identical percentage again below it — ALWAYS, not sometimes, since `metric` is by construction one of the two ends. Every marked week stated one number twice, in two sizes, in one hue, on a diagonal. The cell's copy survives because it carries more (it says WHICH measure moved by sitting on it, where the corner needed the sentence to say so); the header keeps what the grid cannot — the interpretation and the axis — and the grid keeps every number. The lead moved up a rung to fs.subtitle on snug leading, which the freed width pays for. (3) THE CEILING WAS GUARDING ONE SLOT OUT OF THREE. VERDICT_PCT_CEILING was written as a rule about the lead because the lead was the only place a percentage was drawn large; it was never the only place one was drawn. On the very week the ceiling exists for, the card carried an honest \"0.1 km -> 6.8 km\" in the lead, a raw \"+6700%\" in the cell three lines under it, and the same \"+6700%\" again on page two. Core now exposes `pctPastCeiling` (the predicate, on the number) with `figureShowsStep` and `verdictShowsStep` as its two askers: a receipt cell past the ceiling prints the step, and a comparison row PROMOTES its real difference into the figure slot (\"+6.7 km\") rather than restating a step its own PREV/NOW landmarks already are. TONE MARKS THE VALUE, NEVER THE SCAFFOLDING: an end used to tint its label, its figure and its mark, so a marked week carried six tinted elements out of about fourteen and read as two coloured boxes beside two plain columns. Labels are ash on every cell now; what stays lit is the figure and its signed move. The label is also a NAME rather than a kicker — display face, sentence case, untracked — which is both the narrower setting (mono uppercase at tracking.label is the widest way this app can set eight characters, the mechanical cause of \"DISTA / NCE\") and the grammar page two of this same card had already settled on (measure-row's MeasureLine: \"it names a thing, and things get names, not labels\"). Figures gained TABULAR numerals and trackFigure tracking, the mark slot is reserved on every cell so both grid rows share a baseline, and the row's hand-typed 3/5/7/12/16 rhythm went onto the `space` ladder. 6 new engine tests + 4 render tests (the headline percentage counted at exactly one per page, the sentence and its axis kept, no four-digit percentage anywhere on either page, and the promoted difference on the comparison row). THE HERO FIGURE CAME BACK AS TYPE (Aug 2026), which is the follow-up the corner's deletion earned. Deleting it removed a duplicate and, with it, the only thing on the card readable at arm's length; restoring the corner would restore the duplication with it, so the SIZE goes where the number already is. The cell for the metric the sentence names is drawn a ladder rung up (fs.display, stepping to fs.headline then fs.heading) IN ITS OWN POSITION, so the card carries one hero figure and the grid keeps the constant order it was rebuilt for — a render test pins both halves (exactly one cell promoted, and it is the sentence's; a cold card promotes nothing, because no metric was named). The rung is DERIVED, not picked: new core `fitMonoFigure` + `MONO_ADVANCE_EM` (scale.ts) ask the one multiplication a monospaced face makes possible — characters x size x 0.6em — against the cell's MEASURED type width (pageW / 2 - CELL_INSET, 155dp on a 390dp screen, confirmed against the rendered cell rather than derived and trusted: the first spelling dropped an inset) and the athlete's LIVE text scale (useWindowDimensions, not PixelRatio.getFontScale, which answers with whatever the scale was at mount). So a six-glyph tonnage gets the full 26, an eleven-glyph year-to-date span steps to 22, and the same figure at 1.4x Dynamic Type steps rather than ellipsising — a hero figure that has to be truncated was never a hero figure. Every figure shares ONE line box (the promoted rung's leading), so a 20dp neighbour and a 26dp hero sit on one baseline instead of a few points apart, which would have reintroduced the raggedness the grid replaced. 7 new engine tests on the fitter, including the assertion the four-column row never had: that the PLAIN rung clears the cell at the multiplier its text is capped to. THE PAGER TRAVELS WITH THE FINGER (same pass). The card is the height of the page it is on (~200dp against ~390dp) and that height changed on `onMomentumScrollEnd` — so the entire drag ran at page one's height with page two arriving CLIPPED at a border that had not moved, and then, once the finger was long gone and deceleration had finished, the card played a 220ms LayoutAnimation and grew. Two motions in sequence, neither of them the one the finger was making. Height is now an `Animated.Value` interpolation off the scroll offset itself (Animated.ScrollView + Animated.event, scrollEventThrottle 16, useNativeDriver false because layout is not native-drivable), so the card is 40% grown at 40% across, page two arrives at the height it will rest at, and an ABANDONED swipe travels back rather than snapping. The page dots ride the same offset: the leaving pill contracts as the arriving one stretches and the chartreuse crosses between them, instead of popping 7dp to 20dp after the fact under a finger that had already let go. Every interpolation is memoised — `interpolate()` registers a node on its parent value per call, the trap lib/ui's useEntrance documents. The LayoutAnimation is deleted; `page` survives as what the card reports about ITSELF (assistive technology, and how the swipe hint retires), and a dead `pager` ref went with it." },
  { id: "activity-axis-previous-period", area: "Core", status: "shipped", title: "The card measures from the PREVIOUS period \u2014 the rolling mean becomes a landmark", detail: "THE DIAGNOSIS, reported plainly: pick \"last 7 days\" (the 10th to the 16th) and you are asking about the 7 days before it (the 3rd to the 9th). The card answered with a FOUR-WEEK AVERAGE. The label said \"four-week average\" and was therefore not lying \u2014 but honest labelling does not rescue an answer to a question nobody asked, and the filter is the question. Every percentage on the card is now measured from `priors[0]`, the immediately preceding window of the same length: last 7 days against the 7 before, June against May, this week against last week. `VerdictFigure` gained `previous` (the AXIS) beside `baseline` (which keeps its name and its arithmetic but is no longer what anything is measured FROM). THE MEAN SURVIVES AS A LANDMARK, and that is what makes the change safe rather than merely simpler. `comparisonAverageMark()` places it on the SAME signed half-track the bar is drawn on, and the comparison page renders it as a second notch \u2014 precisely the move MEV and MRV make either side of where you are on a muscle\u2019s rail. \"Up 22% on last week\" and \"still under your normal\" are two different facts about one week; a row that draws both can be read either way round without swiping anywhere. The notch is absent when the mean IS the previous period (it would land under the axis it duplicates) or when the move is under VERDICT_END_THRESHOLD_PCT. The landmark scale went from two cells to THREE \u2014 PREV / NOW / AVG, exactly as MEV / MAV / MRV are three, and every cell keeps its slot with an em dash when it has nothing to say so the columns line up down the list. WHAT IT COST, stated because it was a real trade the change was made with eyes open: the headline now rests on ONE window rather than a mean of four, so a deload or a holiday week swings it where the mean absorbed it \u2014 the exact thing the original \"a verdict computed against one week of history is a coin flip wearing a percentage\" rule was protecting. The second notch is what pays for it: the sentence is about last week, and the rail still shows whether last week was normal. THE COLD GATE became the plainest form of the old one: cold exactly when the PREVIOUS period carried no training, because a percentage against a week nobody trained is not a small number, it is not a number. MIN_BASELINE_PERIODS no longer gates it (an empty window still counts INTO the mean \u2014 a fortnight off is part of your average). The per-metric gate simplified from a pair (a floor on the mean plus a four-window coverage count) to one floor on `previous`: against a single window the floor says the whole thing. EVERY LABEL WAS REWRITTEN in EN/PL/DE, because the old ones now describe the wrong window: the lead is \"Your {m} is up on the week before\" rather than \"the highest it\u2019s been in four weeks\", the working-out is \"Previous 7 days {b}\", the comparison head is \"vs previous 7 days\" / \"vs previous month\" / \"vs last year\", and the cold copy says there is nothing to measure against yet rather than promising comparisons after a few weeks. THE ENDURANCE SECTION MOVED WITH IT, and had to: it sits on the SAME screen, reads the SAME range filter, and its minutes are a strict SLICE of the verdict card's hours column \u2014 so a screen where the totals card said \"up on last week\" and the endurance card said \"up on your average\" would be one screen giving two answers about one window. `EnduranceWindow` gained `previous` beside its mean on the same terms, `enduranceDeltaPct` divides by it, and the section's working-out reads \"on the period before\" in all three languages. Two tests pin the pair together, one of them asserting the two modules return the IDENTICAL percentage for an endurance-only window. TWO THINGS WENT with the old axis: `MIN_BASELINE_PERIODS` is DELETED (it answered \"how many preceding windows must carry training\", a real question against a mean of four and a collapsed one against a single window \u2014 `cold` asks it directly now, and a constant nobody reads is a constant that gets re-derived wrong; a tombstone comment holds its place), and every header that described the old comparison was rewritten rather than left to rot \u2014 week-verdict's two honesty rules, the threshold's worked example, the baseline floor's, and activity-window's note on what the preceding windows are FOR. 8 new engine tests pin the axis, the landmark's placement and side, its absence when it duplicates the axis, the cold case, and the two cards' agreement; the render tests pin the three-cell scale." },
  { id: "activity-compare-page", area: "Core", status: "shipped", title: "The activity card's second page \u2014 every metric against its own average", detail: "THE DIAGNOSIS: the figure row can mark only TWO of its four metrics. `best` and `worst` are the period's two ENDS, and a row of four totals has no room to argue about the middle \u2014 so a week of +31% hours, +18% tonnage, \u22127% sessions and \u22129% distance states four figures and explains two of them. The other two comparisons were computed on every render and thrown away. The card is a PAGER now (the app's existing idiom: a horizontal `pagingEnabled` + `snapToInterval` + `decelerationRate=\"fast\"` ScrollView, exactly as workout-wrapped.tsx carries its slides, with that screen's dot indicator under it \u2014 7dp dots, the active one a 20dp chartreuse pill \u2014 reused rather than re-drawn, because five rails once drew five different tails). Page one is the verdict, untouched. Page two is aurora/activity-compare.tsx: one row per metric, every one of them, against its own baseline. IT IS A PAGE, NOT A SHEET, and that is the whole shape of it: a sheet is for a DETOUR (narrow to a sport, open a session, come back) which is why the breakdown is one, but the comparison is not a detour \u2014 it is the same four figures said the other way, so it belongs beside them, at the same level, reachable without leaving the screen. The sheet stays one layer down for what a single figure is MADE of, and a row press on page two raises exactly the sheet its column raises on page one: two ways in, one destination, or the second page is a dead end wearing a chart. THE CHART: ONE AXIS, and the axis is the athlete's own average \u2014 not zero. Bar length is per cent of that average, because per cent is the only unit tonnage (kg), a session COUNT, hours (minutes) and distance (km) can share; a shared VALUE axis across those four is not one axis, it is four pretending. Paired now-against-average bars were rejected for the same reason plus the count: eight marks in a card that size to say what one diverging bar says, when the DIFFERENCE is the subject. The scale is FIXED (COMPARE_SCALE_PCT = 50), not fitted to the week's biggest mover \u2014 fitting draws the identical picture for a +4% week and a +40% one, so length stops carrying magnitude at a glance; past the scale the bar PINS and the figure keeps counting, the same honesty the card already applies past VERDICT_PCT_CEILING. COLOUR STILL MARKS ONLY THE TWO ENDS, which is the rule a chart most wants to break: every rise going chartreuse here would light tonnage's +18% on page two and leave it ash on page one, the same week read two ways one drag apart, and direction is already carried by which side of the axis the bar sits on \u2014 a channel this page has and the figure row does not. NO TRACK BEHIND THE BAR: only the axis is drawn, because only the axis is information. A rail behind every bar is four bars of chrome on a card whose columns already separate by whitespace, the same reasoning that retired the column dividers, the GroupMark hairline and the drawer's border. ONE COMPONENT, TWO SCREENS \u2014 aurora/measure-row.tsx. The comparison row was first built as a hand-rolled COPY of Volume's `MuscleRow`, which is the drift the shared-component rule exists to end (five rails once drew five different tails because each sized its own). The row is now three shared pieces both screens read: `MeasureLine` (name, figure, context on ONE baseline at ONE size), `MeasureTrack` (the rail shell \u2014 an 11dp clipped track on `ink`) and `MeasureScale` (the landmark row, N equal columns, quiet label loud figure, cells pressable only where a caller supplies onPress). THREE PIECES AND NOT ONE COMPONENT, because the callers wrap them differently and a single component would have grown a prop for each: Volume puts the press target around the line and rail only (its landmark cells are their own controls and a drawer of prose follows), while the comparison page presses the whole row through to the breakdown sheet. What is deliberately NOT shared is the geometry INSIDE the rail \u2014 Volume normalises sets onto a landmark scale (`railX`) and draws a band, a ceiling tint, two notches and a target caret; the comparison page maps a signed percentage onto a centre axis and draws one bar. The FIELD is common, the marks in it are not. A HARD guard in design-tokens.test.ts pins it: the 11dp-on-ink rail may not be re-typed outside measure-row.tsx, and both screens must actually import it. The page's head is the kit's `ASection` rather than a seventh hand-rolled \"title left, mono meta right\" (the section-head ratchet caught that one), and `MeasureLine` is named LINE rather than Head so the same ratchet does not have to spend a rung absorbing a component that is not a section head. CONSISTENT WITH \"BY MUSCLE\" (the row was re-invented, then replaced): the comparison row is now Volume's `MuscleRow` grammar lifted whole \u2014 that list is this app's answer to exactly this shape of question (a measure, where it sits, and the landmarks it sits between) and it had already settled every decision this page was re-litigating from scratch. The LABEL is the display face in chalk (F.semi at fs.note), not a mono uppercase kicker: it names a thing, and things get names. The FIGURE is F.monoBold at the SAME size on the same baseline, toned only for the two ends \u2014 a name and its headline figure sharing one line at one size is what makes a list of them scan. The third slot on that line is mono caption ash carrying the context the figure needs (\"Target 14\" over there, the difference in real units here). The bar is a TRACK: a filled rail on `ink` with the axis notched into it, which REVERSES an earlier pass here that drew a bare bar and argued the track was chrome \u2014 the track is not chrome, it is the field the mark is measured in, and this app had already decided that. Under it the figures pin left in equal columns, AVG / NOW where MEV / MAV / MRV sit, quiet label and loud figure, so values align down the whole list instead of floating at two indents. The HEAD is that section's too: display-face title (\"By metric\", mirroring \"By muscle\") left, mono uppercase meta right naming the axis. Page two came DOWN to ~390dp doing it. READABILITY PASS (the build before it failed this): every line but the percentage sat at nano or micro in ash \u2014 the label, both values, the difference \u2014 which is the styling of a META row under a session, and it made the one line carrying what an athlete actually did the least legible thing in the row. The STEP now reads in chalk at body size (it is the training, not a footnote to it), the percentage moved up to fs.heading, the label and the head to fs.micro, and the bar from 8dp to 10dp so a mark under a 20dp figure stops reading as a hairline. The gap BETWEEN rows went to 24dp against 10dp inside one: at 16-against-10 the separation barely exceeded the joins and four rows read as twelve loose lines, and the grouping has to come from whitespace because this card does not draw rules between things. THE ROW'S GRAMMAR: the signed per cent takes the BIG slot (page one already carries the totals; this page exists to say which way they went), and the foot states the move as a STEP \u2014 \"5h 14min \u2192 6h 52min\" \u2014 the card's own idiom, the same arrow the verdict prints when a percentage past the ceiling stops being a measurement, so two figures ride one line and neither needs a \"was\" label; the difference in REAL units sits right, because a per cent alone never says whether 31% is an hour or a minute and the real figure is the one an athlete acts on. THE HEAD names the AXIS and nothing else (`comparisonHeadKey`, which tracks the range the way `verdictWhyKey` does so a month's chart cannot read as if drawn against four weeks) \u2014 the WINDOW is deliberately absent, since the section head above the card already carries it and printing it twice is the redundancy the Progress cluster's sweep exists to catch. THE CARD TAKES THE HEIGHT OF THE PAGE IT IS ON, eased under the athlete's own drag (LayoutAnimation, which the platform no-ops under Reduce Motion): page one is ~157dp of content and page two ~390dp, so a pager fixed to the taller page would leave two hundred points of dead card under the figure row, on the page an athlete sees first, every time. THE FILTER STAYS OUTSIDE the pager, above the card: both pages describe the same window, so a control that changed on page two would be a second period hiding behind a swipe. THE FULL-BLEED RULE DOES NOT APPLY \u2014 screen-level rails must slide under the physical screen edge, but this pager is inside a CARD, the rule's stated exception, so its pages respect the card's padding. DISCOVERABILITY: the card's existing one-time hint now teaches the SWIPE rather than the column press \u2014 the dots say there is more but not what, and the second page is the only thing on this card an athlete cannot find by looking at it (a column is a large figure that obviously presses). It retires on the first swipe OR the first column opened, under the key it always used. Core owns the model: `activityComparison(v)` returns value, baseline, signed per cent, absolute difference and which end (if either) per metric, in VERDICT_METRICS order \u2014 a chart that re-sorted itself would be the sorted-columns mistake the card already made once and fixed \u2014 and `comparisonBar()` maps a move onto the signed half-track, returning NULL on a cold card so the rows keep their figures and draw no bars rather than measure against a baseline the card has already said it does not trust. 8 new engine tests + 6 render tests. i18n EN/PL/DE, including the a11y row sentence, which carries the percentage, both figures and the difference because a screen reader gets neither the bar's direction nor its length." },
  { id: "activity-records-figures", area: "Core", status: "retired", retiredBecause: "The BLOCK survives and is better; this TREATMENT of it is gone — see records-quote-path-ledger. Three faults, and the edge dissolve described below is the one that reads as a bug on a phone: it filled two LinearGradients with `C.ink` while the block sits on the SCREEN, and the screen is not ink (AuroraField lays a 14% lime bloom, a 16% Muskmelon and a 10% blue glow over it), so it deleted the field for 24dp and left a black band with a hard inner edge instead of a dissolve — and it was permanently on, because the viewport width was only ever written by the scroll handler and the first paint compared the content against a viewport of 0. A real soft edge has to mask ALPHA (a MaskedView, not a dependency this app carries); ink over ink is the version that is cheap and looks broken. Second, Today already scrolls sideways in five places and a sixth rail of the same cells was not new information. Third, and the reason a redraw rather than a repair: the figure could not answer the question it raised — a load alone cannot tell `70 × 9 → 70 × 10` (six weeks grinding a rep out) from `65 × 8 → 70 × 10` (a first plate at 70), both print '70 kg', and 'more reps' underneath is a caption, not a path. To revisit the rail specifically: it would need a genuine alpha mask and a reason for a horizontal scroll that a vertical ledger cannot serve. `strengthPrProof` (engines/session.ts) was built for that caption and now has no caller; it is left in core, tested, rather than deleted, since it is the structured form of the summary's own `strengthPrDelta`.", title: "The period's records — figures with proof, and a rail past two", detail: "The records block that activity-breakdown moved onto Today's activity card (from Performance's cut 'Your week') shipped as a list, and read as a receipt: FOUR hairlines around two 12px rows — a section rule, a rule under the header and one above every record including the first — fencing content that already sits under a card. Three faults, all treatment rather than data (design/prs-block-redesign-ideas.html studies ten candidates, from pure subtraction to deleting the block; this ships #2 carrying #3's proof and #1's subtraction). (1) THE RULES OUTNUMBERED THE CONTENT: whitespace separates two items perfectly well, so the three decorative hairlines are gone and only the section divider — which separates the figures from what follows — remains. (2) A RECORD WAS TYPED LIKE A LIST ITEM, at the same size as the label beside it. It is now a FIGURE: the load at fs.display, the largest figure in the card, with the lift as a mono kicker above it, in the grammar the card already uses for its own numbers. (3) THE NUMBER STOOD WITH NO PROOF — 90 kg is only an achievement against the 82.5 it beat. New core helper strengthPrProof (engines/session.ts) returns the same three shapes strengthPrDelta does (climb / first / more reps) but STRUCTURED, because the caption prints in two colour channels — 'from 82.5' in ash and only the gain in the accent — which one joined string cannot express; values come back BARE since the unit is on the figure above them, formatted through fmtWeight/splitFigure so lb rounds as lb and the subtraction can't print a float artifact. THE RAIL: at one or two records the cells sit still (a single record takes the full width; two split it) — no rail, no fade, nothing to drag, because a rail that cannot move is worse than no rail. At THREE the same cells become a horizontal rail. A cell is half the content column, the same width the two-up grid gives it, so going from two records to three resizes nothing: the third simply appears past the right edge, and that peek is the whole affordance (no arrows, no dot row, no 'swipe' label). The block sits DIRECTLY ON THE SCREEN — it is a sibling of the card, not a child — so the rail is FULL-BLEED per the house rule: negative margins of the screen gutter with matching internal padding, so cards slide under the physical screen edge (web margin/padding on --page-pad-x; mobile marginHorizontal -16 with contentContainerStyle paddingHorizontal 16). THE EDGE DISSOLVE IS A STATUS, NOT DECORATION: 24px of fade on the right only while records remain, on the left only once scrolled past the first, and none at all on an edge with nothing behind it — web writes --pr-fade-l/-r from the scroll offset into a mask-image (declared @property so the fade eases rather than snapping; .pr-rail in globals.css, which also kills the horizontal scrollbar the global rule would otherwise leave sitting exactly where the fade belongs), mobile animates two screen-coloured LinearGradient overlays' opacity on the native driver — the coach-rail idiom, so no MaskedView dependency. Snap is proximity on web and decelerationRate='fast' on mobile: each platform's existing rail feel, a flick landing cleanly without forcing. Capped at eight records with a final 'Show all {n}' cell (the existing key) that expands the rail in place, so a year view can't become an endless drag; the cap resets when the period changes. PRESS-THROUGH: PrHit gained optional sessionId + at, stamped by newPrsInSession, so a record opens the session that set it — the same promise the figures above it make ('open a figure for the sessions behind it'). When a lift PRs twice in one window the kept hit's id follows the heaviest, not the one it replaced. The kicker now NAMES THE WINDOW ('Records this week' / 'in the last 30 days' / 'this year' / 'Records – July 2026'), since the card is period-aware and a month's records must not read as this week's news; the month case interpolates the localized month name rather than an inflected phrase, because 'in July' declines in Polish and a nominative month in that slot would be wrong. Silence at zero records is unchanged. Rail is focusable with a visible focus ring and a group label naming the window and the count on web; reduced motion drops the fade's easing (the global catch-all) without dropping the fade, which is the status. Twin components on both clients (aurora/week-verdict.tsx), i18n EN/PL/DE (w.home.act.prsWeek/prsD7/prsD30/prsYtd/prsMonth, prFrom, prFirst, prReps, prOpen). 8 new core tests (six on the proof helper, two on the session stamp, both verified to fail against the old behaviour); core suite green at 2273, both typechecks clean." },
  { id: "records-quote-path-ledger", area: "Core", status: "shipped", title: "Records — a quote, a delta that never estimates, and the path each record moved between", detail: "The redesign of activity-records-figures (retired above), in three moves. (1) THE QUOTE. The period's biggest move leads the block: the load at fs.stat in CHALK with the reps beside it as a quiet multiplier, the pair under it, and the delta in chartreuse. The accent moved OFF the figure and onto the change — a rep record headlines a load that stood still, so a lime figure there was claiming to be the thing that moved, and the level is context while the change is the news. (2) THE LEDGER. Every other record is one line — lift, pair, delta — tabular mono in a fixed column so four records compare straight down the page, which is the value of the block and exactly what a rail could never do. No horizontal scroll, so no bleed, no dissolve, and the black edge is gone by construction rather than by fixing a gradient. Capped at four rows with the bare `＋ Show all {n}` expander in ash (it GROWS in place, so no ring and no accent, per the exit grammar — the old 'Show all' was a bare mono label in a rail slot, which is neither shape the grammar allows). (3) THE PATH, which is the substance. Every record prints the PAIR it moved between — `70 × 9 → 70 × 10` — because a delta says how far and never from where, and on a bench press that is most of the story. Colour AND SIZE carry it with no legend: the origin small and ash, the arrival a rung larger and chalk, the delta chartreuse. `×` and `→` are the same glyphs in EN/PL/DE, so the busiest line in the block needs no translation and no unit conversion; a first-ever lift takes an em dash for its origin so it uses the same shape as everything else. NEW CORE (engines/records.ts, RECORDS AS A PATH): `blockTopLoadSet` / `blockBestE1rmSet` return `{load, reps}` and the old number helpers now DERIVE from them, so a figure and a path can never come from different sets; `PrRecord` carries the axis, both points, the delta and `gainPct`; `prRecordsInSession` + `prRecordsBetween` (recap.ts) are the block's view of `newPrsInSession` / `prsBetween`, off the same detection pass. THE AXIS SPLIT REPAIRS A SEAM THAT WAS ALREADY THERE: a `PrHit` fuses `topLoad` from the heaviest set with `e1rm` from the best-estimated one, and on the day you bench 80 × 1 and 70 × 10 those are two DIFFERENT sets — one record assembled from two, invisible while only the load was printed and a contradiction the moment a pair is drawn beside a delta. They are two achievements (the heaviest thing lifted, the best set done), so they file two rows; the lift is named once and the second row leaves the name column empty the way a ledger omits a repeated key (visually only — the spoken label still names it). A set that is both files ONE row, on the load axis. `newPrsInSession` KEEPS its one-hit-per-lift contract, because the celebration, the feed line, the history badge and the live counter all count records with it. THE DELTA NEVER ESTIMATES: the load went up so it names the load, only the reps went up so it names the reps, nothing came before so `New` — an estimated 1RM decides whether a strength record happened and is never the figure shown. RANKING is the percent on the coordinate each row prints (ties to the heavier load, a debut last), so the block ranks by the same number it shows and the quote cannot headline one record while the table argues for another. NO CHART: an eight-session strip was drawn and cut, because on a record the newest bar is the maximum by definition — one possible shape, every time; a lift's real trajectory is on its exercise page (`topLoadTrendWithPRs`), which is where a row should lead. i18n EN/PL/DE (prRepDelta/prRepDeltaOne/prNew/prAxisLoad/prAxisStrength/prPath — two rep keys in English for the plural, one invariant abbreviation in PL/DE rather than a plural rule for a two-character column). 14 new core tests (engines/pr-records.test.ts) covering the axis split, the 80 × 1 / 70 × 10 day, the same-set collapse, the delta rules including a heavier load on the strength axis, ranking and the window; core green at 3842, mobile suite green at 228 with the withAlpha floor re-pinned 107 → 105 for the two deleted gradients. MOBILE ONLY, and the header comment no longer claims a web twin: the web client was retired in Aug 2026." },
  { id: "today-progress-endurance-split", area: "Core", status: "shipped", title: "Today's retrospective, split — PROGRESS and ENDURANCE as two sections", detail: "Today's retrospective was ONE cluster called Progress carrying five blocks of two different subjects: the verdict card, its records, the exercises rail, the endurance lanes and the other-sports tiles. A runner scrolled past a strength verdict, a strength records rail and a strength-favourites rail to reach their own sport, under a single headline claiming all of it was 'Progress'; endurance itself had no summary at all — five lanes of per-discipline depth with nothing above them saying what the disciplines came to, how many times you went out, or whether any of it was more than usual. The lanes answered 'how is my running going'; nothing answered 'how is my endurance going'. NOW TWO SECTIONS. PROGRESS = (a) This week (the verdict card + the cluster's date filter), (b) RECORDS, (c) EXERCISES — coarse to fine. ENDURANCE = (a) This week (its OWN summary card and its OWN filter), (b…) one lane per discipline, (x) Other sports. THE ENDURANCE OPENER IS A SENTENCE (core endurance-window.ts `enduranceLead`, aurora/endurance-summary.tsx on both clients): 'Four sports, mostly running.' with one mono line under it — '5.4 h, +12% on your average'. It took two cuts to get there, and both failures were the same failure: saying something the screen was already saying. (1) A CARD — three figures, a hairline, a 'what it was made of' header, a share bar and a row per discipline, about 300px of section opening before the first lane, where every row was a discipline with a WHOLE RAIL immediately below it carrying eight weeks of volume, a pace trend, zones and its last effort. A table of contents for a list already in view, pushing the thing it indexed off the screen. (2) A STRIP of the same three figures — better, but the figures WERE the redundancy: only endurance and sport groups ever carry distance, so this section's kilometres are the verdict card's KM column to the decimal one screen apart, and its efforts sit beside that card's SESSIONS count reading as a contradiction of it. Only TIME was new, and one honest figure does not need three columns and a rule. So the opener states the one thing nothing else on Today can — WHAT the endurance was made of, how many sports and which carried them — and hands the arithmetic back to the card above and the lanes below. Three sentence shapes, each true of the week it describes (nothing logged; one sport, which is a fact rather than a degenerate case; several, split at ENDURANCE_LEAD_MAJORITY on whether the leader carried half the time — 'led by' is safe at any count, since the leader is the largest slice by definition). The working-out carries the section's own minutes against its own baseline, the same comparison the verdict card makes at the same VERDICT_THRESHOLD_PCT, and only the DELTA takes the semantic tone: colouring the whole line would give a 12% week the weight of a headline. No baseline → whyCold, which carries no delta at all, because 'nothing to move from' must never render as 0%. The block has NO FILTER of its own either — it reads the screen's period (TODAY_RANGE_STORE_KEY, which the verdict card's control writes), so a second five-segment control ten lines below the first, always agreeing, is not drawn; it names the window instead, since a total with no period is not a total. The per-slice breakdown and the totals stay in the core model regardless: they are what the window IS, they carry rules worth keeping proven (a brick session counts once; shares run on TIME, not distance, so a timed sport is never silently dropped), and the Endurance screen is the obvious next reader. A model is complete; a client picks from it. This is NOT the cross-sport strip week-verdict retired: that one was removed because two totals cards on a screen counting different populations under near-identical labels is a misreading waiting to happen, and what has changed is the heading — these figures sit under a cluster headline reading ENDURANCE, so the section scopes them. They are also a SLICE, never a second opinion: enduranceWindow calls the exact activitySummary the verdict card renders and keeps the `endurance` + `sport` groups (the progress-parentage reconciliation rule). EFFORTS ARE SESSIONS, not blocks — a brick session is one time you went out — so the figure cannot disagree with the verdict's session count. No drawer: the per-discipline breakdown this card owes is standing in the lanes directly beneath it, and the mix list is the index to them. THE SEAM (aurora/section-seam.tsx, both clients) marks the chapter change: ONE rule, full-bleed to the true screen edge like the rails, fading to nothing at both ends so it reads as a horizon rather than as a container edge, and belonging to neither section. It is deliberately not the divider the GroupMark study retired — that one was a hairline ATTACHED to a label; this one has no label and carries nothing. Web draws it as an `<hr>` (a thematic break is exactly what it is, so the structure is free for a screen reader); mobile as a 1dp LinearGradient. THE FILTER IS NOW SHARED (core activity-range-view.ts + aurora/range-filter.tsx on both clients). Two sections mean two controls, and each client already held its own copy of the segment list, the short-label map, the lit-segment arithmetic and the span rule — four copies would have followed, which is how 'last 30 days' ends up meaning two windows on one screen. Core owns the shape, the clients own only locale month names and storage. ONE PERIOD FOR THE SCREEN: both sections pass core's TODAY_RANGE_STORE_KEY, so they are the same filter shown twice rather than two filters. The first cut gave each its own key on the reasoning that a control belongs to the card it sits above; what that produced was a screen where 'Last 30 days' could mean two windows at once, with the second control a scroll away and nothing on screen admitting the disagreement. The clients hold the choice in a module store the hook subscribes to (useSyncExternalStore) rather than in component state, because a shared storage KEY alone would leave two mounted controls out of step until the next launch — the worse half of the bug. The key stays a PARAMETER, so a future block that genuinely is its own scope can be independent by saying so. RECORDS became a block of its own too (aurora/period-records.tsx): same rail, same edge dissolve, same eight-record cap, but headed like its neighbours — display-face 'Records' with the WINDOW as mono meta on the right (a Records block with no period would read as all-time) and the count joining it from three up. It still takes its window from the Progress filter, since a PR belongs to the period it happened in. The old window-naming strings (w.home.act.prsWeek/prsD7/prsD30/prsYtd/prsMonth) retire with the kicker they were written for. The endurance lanes render HEADLESS under the new GroupMark — the block printing 'Endurance' again would be the title twice in 60px — and their ORDER CHIP is hoisted onto that headline, through a new `right` slot on GroupMark. That slot is the Explore SectionHead grammar applied at cluster altitude: a head-level control belongs beside the title, on the same row. The chip had been left on an orphan right-aligned row floating between the section's opener and its first lane, attached to neither, and a control that orders the whole section belongs at the section's altitude. The screen owns the state now (useLaneOrder, counting through activeDisciplines rather than rebuilding the lanes) and the block takes `order` as a read-only prop; hidden when only one lane exists, since sorting a list of one is a control that does nothing. The Endurance SCREEN is unchanged — uncontrolled and headless, it still renders the chip itself, because there its hero is the headline. The DATE filter is untouched by this: it stays the segmented control under the This-week head. Other sports moved its parentage denominator from the whole week's hours to the endurance section's time (progressParentage gained `enduranceMinutes`): under a card reading '3.2 h', a lifting-inclusive '5.2 h' beside it read as that card's total and contradicted it. The archive + deep-analysis doors now sit after BOTH sections — they are the way out of everything above them, and one exit after all the breakdowns is the wave-3 rule. The whole Endurance section is ABSENT for a pure lifter — no heading, no empty card, no column of zeroes — gated on the history (hasEnduranceHistory) rather than on the window, so a runner who took this week off still finds their section where they left it with the card saying the week was quiet. i18n EN/PL/DE (w.home.act.recordsTitle, w.home.endw.*). 22 new core tests; core suite green at 2627, both typechecks clean, iOS bundle exports." },
  { id: "web-dashboards", area: "Web", status: "shipped", title: "Analytics dashboards — BOTH clients (web + mobile)", detail: "The 3-scope Analytics dashboard (Athlete/Coach/Operator) ships on web AND mobile, per the parity rule. HISTORY, because this drifted twice: it was first retired from web in favour of a 'get the app' pointer while mobile had NO Analytics screen — so the dashboard had no live surface on EITHER client, and the mobile springboard (which treats a nav id with no route as web-only) opened the browser, which pointed back at the phone. Both halves are now real screens: web app-shell mounts aurora/analytics.tsx (recharts) and mobile has app/analytics.tsx + components/aurora/analytics.tsx (dependency-free bars/meters). SHARED ACCESS MODEL: nav.ts owns analyticsScopesFor(role) / resolveAnalyticsScope() / analyticsScopeLabelKey() / analyticsScopePrivacyKey(), so both clients derive scope availability from the AUTH ROLE (never the self-serve persona), land on the highest scope the role holds, fall back to the athlete scope on a mid-session demotion, and render the SAME per-scope privacy note above the charts. A coach now gets the athlete scope too (a coach trains too — matching the documented persona nesting; the old web build locked a coach to the roster scope alone). Scope tabs are hidden when the role holds only one. 6 scope unit tests + an i18n test asserting every scope label and privacy note resolves in EN/PL/DE." },
  { id: "warmup-sets", area: "Core", status: "shipped", title: "Mark sets as warm-up / cool-down", detail: "A per-set role on StrengthSet — working (default, absent) / warmup / cooldown — added as an optional additive field, mirroring how drop?/rest?/vel? already ride the shared SessionBlock shape so every pre-existing session stays valid. UI: the leftmost set badge is a single SET-TYPE CYCLER — tap to walk working → warm-up (amber W) → cool-down (blue C) → drop (lime ↓) → working — folding the role enum and the drop flag into one mutually-exclusive choice (a set is exactly one of these); '+ warm-up' / '+ cool-down' / '+ drop set' add-buttons add a pre-flagged set (so cool-down has a one-tap button now, not just the badge cycle). One source of truth in @hybrid/core (setType / cycleSetType / setTypeBadge, generic over both clients' differing set shapes) drives the web shared editor (workout-blocks.tsx → Logger + Builder), the mobile live logger (app/workout.tsx) and the read-back badges + ' – warm-up/cool-down/drop' tags on both session-detail screens. ENGINE EFFECTS (the real reason it matters): warm-up/cool-down sets are EXCLUDED from working-set volume (volumeByMuscle), session tonnage (sessionVolume), e1RM (blockBestE1rm → so PR detection + e1rm trends), and the training-log hardSets/topRpe — so a heavy ramp single can't fake a PR and warm-ups don't inflate the weekly hard-set counts that will feed the volume landmarks (MV/MEV/MAV/MRV, see volume-landmarks). Drop sets remain WORKING sets (they count). Warm-up ramp sets are KEPT for load–velocity profiling (velocity.ts reads set.vel directly — useful sub-maximal points). DESIGN DECISION: warm-ups/cool-downs are excluded from volume by DEFAULT, but this is now USER-CONFIGURABLE — a 'Count warm-ups in volume' toggle (logger-settings → countWarmupsInVolume, default off, RP-style) flows a setsForVolume(block, includeWarmups) option through sessionVolume / volumeByMuscle / weeklySetsByMuscle / volumeStatus / weeklyVolumeTrend / exerciseDashboard so the Volume, Trends and per-exercise screens honour it; PRs &amp; e1RM always stay warm-up-excluded regardless. Pure helpers unit-tested (isWorkingSet/workingSets/setType/cycleSetType/setTypeBadge + the excluded-from-volume/e1RM cases); localized EN/PL/DE." },
  { id: "manual-sport-session", area: "Core", status: "shipped", title: "Manual sport-session logging (Olympic sports)", detail: "Log a sport session by HAND — no wearable / connected gear required (complements the blocked `wearables` sync, which is an OPTIONAL accelerator, never the only way in). @hybrid/core olympic-sports.ts is THE single sport database for the whole app (unified Jul 2026 — it now also backs the S&C transfer engine; see sport-engine): OLYMPIC_SPORTS catalogs 60+ sports/disciplines (mostly Summer & Winter Olympic, plus a few the app prescribes for — BJJ, Squash), each with an icon, a category (Athletics/Aquatics/Cycling/Combat/Racket/Team/Gymnastics/Target/Outdoor/Strength/Winter/Multisport) and the PARAMETERS it actually tracks — every sport carries duration (minutes); the endurance sports (Running, Swimming, Cycling, Track Cycling, Mountain Biking, Rowing, Canoe Sprint, Race Walking, Triathlon, Cross-Country Skiing, Biathlon, Speed Skating, Short Track) add distance (km) and a derived pace. The prescribable sports additionally carry an `sc` (S&C) block that sports.ts projects into its engine view — one list, no drift. Helpers olympicSport (case-insensitive lookup), sportTracksDistance, olympicSportsByCategory + OLYMPIC_SPORT_NAMES; pure + unit-tested. A picked sport is logged as a normal CARDIO activity block named after the sport, so pace (pacePerKm), cardio PRs (distance/pace), history, the weekly recap and the training log all read it with ZERO special-casing — no new block kind, no migration. UI parity on BOTH clients via the shared model: web adds a '+ Sport' button + a categorized sport picker to the shared workout editor (workout-blocks.tsx → Logger + Builder); mobile adds a grouped, searchable 'Sports' section to the live-logger exercise picker (app/workout.tsx). The activity editor shows duration ONLY for known timed sports (Tennis, Judo, Gymnastics, …) and the distance + minutes → pace grid for distance sports + generic/custom cardio (timedSportOnly = a KNOWN Olympic sport that doesn't track distance, so a typed-in 'Run' keeps its distance field). PER-SPORT DISTANCE UNITS: distance is shown/entered in each sport's natural unit (metres for Swimming/Rowing/Canoe Sprint, km for the rest) and pace by its split (Swimming /100m, Rowing & Canoe Sprint /500m, everything else /km) — but STORAGE stays kilometres, so the shared pace/PR/recap math is always single-unit. olympic-sports.ts owns the conversion (sportDistanceUnit / displaySportDistance / parseSportDistance / formatSportDistance / sportPacePerMeters) and session.ts the sport-aware pace (cardioPace / formatSportPace); cardioSummary, both loggers' distance inputs + pace lines, and the cardio-PR celebration lines (web screens.tsx + mobile workout/session-detail) all render per-effort distance/pace in the sport unit, while AGGREGATE totals (weekly recap distance, the Running screen, cockpit km) correctly stay in km. ENTRY POINTS (all three logging surfaces + a home widget, both clients): (1) LOG SESSION — the sport picker is in the shared workout editor (web workout-blocks.tsx → Logger + Builder) and the mobile live-logger picker (app/workout.tsx). (2) BUILDER — web inherits the picker via the shared editor; mobile builder (app/builder.tsx + components/aurora/builder.tsx) now has its own categorized Sports section + sport-aware distance units (use-routine-builder.ts converts display↔km). (3) SPORT TAB — a '+ Log a {sport} session' button opens the logger pre-seeded with that sport (web app-shell onLogSession→pendingBlocks; mobile /workout?source=sport&sport=…). (4) TODAY/HOME QUICK-LOG WIDGET — a 'Log a sport session' card on the dashboard (web components/quick-sport.tsx in Today + Aurora Today; mobile components/quick-sport.tsx in the classic Home + Aurora Home) where the athlete picks a sport, enters time (+ distance for distance sports) and taps Log to save a real session straight to /api/sessions (mobile falls back to a guest/on-device stash) WITHOUT leaving home — 'back from a run → log it → done'. The sport selector is a RECENT/SUGGESTED shortlist (suggestedSports: the athlete's most-recently-logged Olympic sports newest-first, topped up with defaults) shown as one-tap chips, with a 'More' expander revealing the full categorized catalog — so the common case is a single tap. Picker + button copy localized EN/PL/DE; distance-unit labels (dist m/km) localized too. Pure helpers unit-tested." },
  { id: "reorder-sets-exercises", area: "Core", status: "shipped", title: "Reorder sets & exercises (arrows + drag-to-reorder)", detail: "Two pure helpers in @hybrid/core/session — moveItem(arr,index,dir) (one slot up/down, clamped at the ends) and moveItemTo(arr,from,to) (slide an item to any index, for drag-and-drop) — are the single source of truth both clients share, so set + exercise reordering can't drift. UI parity: reordering is HOLD-AND-DRAG everywhere — the ↑/↓ single-step arrow buttons are GONE from set rows and exercise/block cards on both clients (drag is the gesture people actually use; the arrows were dead weight). SETS — every strength set row has a ⠿ grip: web drags the row onto another row (HTML5 drag, constrained to the same block) in the shared editor (Logger + Builder), mobile lifts the row via a shared useDragReorder hook (lib/use-drag-reorder.ts — onLayout-measured rows, nearest-centre drop, keyed by group so a drag can never cross into another exercise's ledger) in BOTH the live logger and the Builder set ledger; the grip itself is the shared components/drag-handle.tsx (PanResponder, no gesture-handler dep). EXERCISES/BLOCKS — drag via the ⠿ grip on each card (the mobile Builder's block cards gained the grip when their arrows were removed): web uses native HTML5 drag (draggable handle → the card is a drop target → moveItemTo by uid), mobile uses the SAME useDragReorder hook as the set rows, driven by the same PanResponder-based DragHandle (no react-native-gesture-handler/reanimated dependency — consistent with SwipeRow): it measures every card via onLayout and on release drops it next to whichever card's centre is nearest (robust across variable card heights). CORRECTED Aug 2026: the live logger's exercise cards did NOT share the hook — they carried a private line-for-line copy of it written before the hook existed, described here as if it were the shared one, and the register said 'lifts only the dragged card (Animated translateY + elevation)' because that is exactly what the copy did. Folding it onto the hook (see motion-audit-followups wave 5) is what gave the exercise reorder the animated COMMIT its own set rows had had since wave 2, plus the pickup lift. The web Log Session screen now passes `reorder` (it previously had no reorder controls at all — only the Builder did), so logging and template-building share the same reorder + grip-handle affordances. moveItem/moveItemTo are unit-tested (swap/clamp/no-op + arbitrary-index slide, no mutation)." },
  { id: "logger-settings", area: "Core", status: "shipped", title: "Configurable workout logger (simple ↔ detailed + automation prefs)", detail: "The hardcoded logger behavior is now a per-device preferences layer. logger-prefs.ts in @hybrid/core is the single source of truth: LoggerPrefs + DEFAULT_LOGGER_PREFS + normalizeLoggerPrefs (type-safe merge over defaults, restSeconds clamped to REST_SECONDS_CHOICES) — pure + 4 unit tests. v1 settings: detailed (Simple↔Detailed — hides the RPE column on both clients), velocity (the M/S bar-velocity column for VBT logging — its OWN toggle, OFF by default, so the column only appears for athletes who train with a velocity device; gates the column in the mobile live logger and the shared web editor/Logger/Builder alike), countIn (5→1→GO overlay on/off), restTimer (auto rest countdown on/off — switchable from Settings AND on demand mid-workout via a ⏱ pill in both live loggers' header row, writing the same persisted pref; turning it off mid-rest also dismisses the running countdown) + restSeconds default (60/90/120/180), carryOver (pre-fill a new set from the previous), keepAwake, haptics, and countWarmupsInVolume (whether warm-up/cool-down sets count toward Volume/Trends/per-exercise volume — default off; see warmup-sets). MOBILE: a Logger-settings screen (app/logger-settings.tsx, reached from Settings) writes a useSyncExternalStore store (lib/logger-prefs.ts on AsyncStorage); the live workout (app/workout.tsx) honours every toggle — count-in, keep-awake, all haptics, carry-over, the RPE column, and the rest timer (enabled + default duration). WEB: lib/logger-prefs.ts (localStorage store) + a Simple/Detailed toggle on the Logger, which passes a new `detailed` prop into the shared WorkoutBlocks editor (Builder stays detailed). Localized EN/PL/DE (loggerPrefs.*). Defaults match today's behavior so nothing changes until opted into. NEXT (the fuller list, deferred): auto-approve/auto-advance, RPE-vs-RIR, units (kg/lb with real conversion), rest sound/background notification toggles, per-exercise rest, plate calculator + quick-increments, auto warm-up generator, default start view, and cross-device sync via a User-prefs column. This settings surface will re-home into account-settings-pro as its 'Logger' section." },
  { id: "units-pref", area: "Core", status: "shipped", title: "Weight units (kg / lb)", detail: "A user weight-unit preference (LoggerPrefs.units, default kg). Storage stays canonical KILOGRAMS everywhere — units.ts converts only at the display/input boundary (kgToUnit/unitToKg, displayLoad/storeLoad for editable load inputs, fmtWeight, fmtTonnage) so the engines, DB and cross-device sync never care about the unit. Pure + 4 unit tests. Wired through EVERY weight surface on both clients: the loggers' load column (input + header label), the per-exercise dashboard (stats + e1RM chart), the Trends exercise table + weekly-tonnage chart, session detail (per-set load, block e1RM, volume, muscle tonnage, e1RM trend, PR lines), the web analytics dashboard, AND the branded share cards + share text (WorkoutShareCard/RecapShareCard, the post-workout summary and the weekly recap). Also covered by this work: the logger flow toggles autoAdvance / defaultStart / rpeAsRir (RIR = 10−RPE via rpeRirSwap, stored as RPE). Toggle in mobile Logger settings + web account Preferences; localized EN/PL/DE (summary.volumeMoved, loggerPrefs.units/defaultStart/autoAdvance/rpeAsRir)." },
  { id: "distance-two-decimals", area: "Core", status: "shipped", title: "Distances read to two decimals — one rule, every surface", detail: "A watch records 5.54921352352 km and the app stores every digit of it (the device is the source of truth, and derived pace has to divide by the exact figure). What reached the screen was a mess of two conventions: some surfaces printed the raw measurement, most rounded to a single tenth — so the same run read '10.2 km' on the history row, '10.23 km' on the device panel beside it, and '10.20593123 km' on the import sheet. distance.ts is now the ONE place a kilometre figure is rounded: roundKm (a NUMBER at two decimals, so a flat 10 km never grows a '.00'), kmValue, fmtKm and kmOrMeters (metres below a kilometre, because a 34 m pool swim reads as nothing in km). Two decimals is 10 m — the step the watch's own summary shows, fine enough that a 5.55 km loop is not flattened to 5.5. Wired through every km surface on BOTH clients: the session meta line, done-receipt rail stats, history rows + headlines, weekly recap and the statistics screens, the activity/week-verdict card and its per-group figures, sport pages (figures, week series, week average, the sports list), run totals + weekly mileage, endurance lanes and levels, exercise analytics, achievements, the social leaderboard/compare, the feed stat row, the device import sheet, the live run track, and the AI coach's prompt context. Sub-kilometre and metre-unit sports keep their own rules (displaySportDistance); km/h speeds and per-km paces are unchanged. 4 unit tests on the helper, plus the engine tests that pinned the old tenths." },
  { id: "web-logger", area: "Web", status: "shipped", title: "Session logger + history", detail: "Engine-prescribed or manual sessions saved to the DB; History lists real sessions. CARDIO and CONDITIONING are now SEPARATE block kinds (alongside strength): a `cardio` block (distance/minutes → derived pace — runs/rows/rides) is distinct from a `conditioning` block (format/work/rest/rounds/minutes — metcons/intervals/EMOMs), each with its own '+ Cardio' / '+ Conditioning' button, field set and badge colour. RPE is captured on STRENGTH sets only — cardio and conditioning don't ask for it. SessionBlock is a 3-way union (StrengthBlock | CardioBlock | ConditioningBlock); a pure `migrateBlocks` upgrades sessions logged before the split (a conditioning block carrying a distance and no interval shape → cardio) and is applied at every read boundary (/api/sessions GET + the server engine reads), so old runs render as cardio with zero data loss. Adding a conditioning/cardio block no longer pre-fills phantom minutes/RPE. The block editor (strength sets×load×reps×RPE×velocity, cardio distance/min, conditioning format/work/rest/rounds/min) is ONE shared component (components/workout-blocks.tsx) the Logger and the template Builder both render, so the two can't drift — both pull the same exercise catalog (built-ins + the admin exercise library via useExercises) and both keep the set-row grid from overflowing (1fr inputs get minWidth:0). The AI 'Use prescribed' path keeps the prescribed interval (work/rest seconds × rounds) intact AND derives editable minutes, so prescribed conditioning is no longer blank. On an AEROBIC day the engine prescribes a STEADY RUN with a real distance + goal pace read off the athlete's own logged runs (easyRunTarget: their median easy pace + typical distance, scaled by today's readiness; a gentle 5 km @ 6:30/km default before any run history) — distance/minutes flow into the logger so its derived pace matches the target. CARDIO: conditioning blocks take a distance (km) and the shared conditioningSummary/pacePerKm helpers DERIVE pace (min/km) from distance + minutes (never stored, so it can't disagree) — surfaced in History + session detail. SUPERSETS & DROP SETS: strength blocks can be grouped into supersets — tap 'superset ↑' to join the exercise above and the shared supersetLabels helper labels the group A1/A2/A3… (stable group key on StrengthBlock.group, so a group survives reordering and holds 3+ moves; the pure toggleSuperset helper does the join/leave + singleton cleanup). Any set can be flagged a drop set (a lighter, no-rest continuation; '+ drop set' adds one pre-flagged) via StrengthSet.drop. Both are additive optional fields on the shared SessionBlock shape so existing sessions/engines are untouched. An RPE cheatsheet (the shared @hybrid/core RPE_SCALE + RIR explainer) opens from the rpe column / a 'What's RPE?' button. LIVE LOGGER (full parity with the mobile train-funnel): the web Aurora logger is now a LIVE logger, not a fill-then-save form. apps/web/lib/use-workout-timer.ts (useWorkoutTimer + mmss) drives a get-ready COUNT-IN overlay (5→1→GO, gated by the shared `countIn` pref) then a sticky elapsed stopwatch with a PAUSE/hold (freezes the clock + any running rest, shifting both so neither jumps); the saved startedAt is the real clock start (at GO), not the Save instant. The shared WorkoutBlocks editor gained a `live` mode (used by the logger, not the Builder): a ✓-to-BANK column per strength set + a 'last time' reference per lift (lastStrengthByLift + blockSummary). Banking a set starts a REST COUNTDOWN (default from the `restSeconds` pref, 60/90/120/180 presets, over-rest readout, a navigator.vibrate buzz at the target — the web analog of the mobile haptic/notification) and records the rest taken on StrengthSet.rest; banking into a drop set or mid-superset skips the rest (you keep moving), and auto-advance appends a fresh set — matching mobile exactly. KEEP-AWAKE via the Wake Lock API (re-acquired on tab refocus), and DRAFT auto-save/resume via apps/web/lib/workout-draft.ts (localStorage, debounced; restored post-mount so SSR hydration stays intact, resuming the original clock) — so a refresh/crash never costs the session. The classic (non-Aurora) logger keeps the lighter count-in + elapsed clock. Mobile (app/workout.tsx) already had all of this; this brings web up to it. FINISH FLOW + ADD-SET REDESIGN: logging is now name-free and ends in a single primary 'Finish workout' button (no more 'Save session' / inline 'Save as routine' pair). The session AUTO-TITLES itself via the shared defaultSessionTitle() (time-of-day, identical on web + mobile) — there's no title input while logging, because nobody names a plain workout. Finishing IS the save and lands on the finish/summary screen, where naming actually happens: a shared SaveRoutineCard (Save as routine, with the only name field) plus an OPTIONAL ✎ rename of the session (web SessionRename + mobile SummaryRename → PATCH /api/sessions/[id], which now accepts a `title`; createSession/the web POST surface the new row id). Adding sets is no longer a row of five equal buttons — one primary '+ Add set' with warm-up / ramp / cool-down / drop tucked into a compact 'Special ▾' menu (web in workout-blocks.tsx, mobile in app/workout.tsx); the set badge still re-types a set with a tap. The classic web logger gets the same flow via a minimal finish panel. ENTRY POINTS: the Aurora bottom-pill Train FAB and the Today 'Start' both open the logger and the timer goes off immediately — the Train FAB opens an EMPTY session, while Today's plan card prefills the enrolled plan's day (planDayToBlocks). MOBILE parity: the Aurora Train FAB (components/aurora/global-nav.tsx) now drops straight into the live /workout?source=empty (timer + count-in) instead of the launcher hub, and the Aurora Today plan-card CTA follows the enrolled plan (source=plan) before falling back to AI/empty — both already ran the mobile live clock." },
  { id: "builder-signal-redesign", area: "Core", status: "shipped", title: "Builder redesign — the signal board (per-set control, modality-aware fields, live session math)", detail: "The Builder on BOTH clients is now a SIGNAL BOARD: every metric on screen is a live projection of the editable prescription, never a stored label. CORE (engines/session-signal.ts, pure + unit-tested): sessionSignal(blocks) → estimated duration, working tonnage and an integer strength ⇄ conditioning ⇄ endurance time split (sums to 100); estimateBlockMinutes (strength = working sets × (planned rest + ~45 s) + ~1 min per warm-up/cool-down; cardio = entered minutes; conditioning = minutes else rounds × (work+rest)); strengthBlockStats (scheme / top load / working tonnage); blockSignalSummary (collapsed one-liner, en-dash separators per the no-middot rule); the exercise-profile model (see exercise-profile-model) decides WHICH fields a cardio activity shows, so a squat can never show pace and a swim can never show incline on either client. SCHEMA (additive, optional): StrengthBlock.restSec (PLANNED rest between working sets — distinct from StrengthSet.rest, the live logger's measured rest), CardioBlock.incline / stroke / zone. PER-SET CONTROL: the mobile Builder previously flattened an exercise to 'N sets × one reps/load'; it now edits the REAL SessionBlock[] shape — every set its own row (load × reps × RPE/RIR each, honouring the rpeAsRir + kg/lb logger prefs via rpeRirSwap/displayLoad), set-role badges cycling working → warm-up → cool-down → drop (shared cycleSetType), add-set carry-over, and a planned-rest stepper (15 s steps, 15–600 s clamp) — what you save is exactly what the logger runs. WEB: the shared WorkoutBlocks editor gained a `signal` prop (Builder only — Logger untouched): collapsible block cards (header keeps the grip/name/summary; arrows + superset controls tuck away when collapsed) with an always-visible METRIC ROW derived per modality (strength: set×reps / top load / tonnage / est min; cardio: distance / pace / minutes; conditioning: format / rounds / est min), the rest stepper, and the new cardio extras fields; the Builder screen adds the SESSION PULSE strip (est time, fmtTonnage, moves, balance bar in lime/violet/teal). MOBILE (components/aurora/builder.tsx + lib/use-routine-builder.ts rewrite): the same signal cards, pulse strip, per-set ledger and modality fields, same core math. DESLOP: modality is encoded in the kind tag + the accent on the key metric — no left border rails anywhere, no middot separators (spaced en dash where a string separator is unavoidable). i18n: w.train.signal.* + new blocks keys (restBetween/inclinePct/stroke/zone/setCol/expand/collapse) in EN/PL/DE. SET DELETION (mobile): the Builder's per-row '−' remove button is gone — a set row is deleted by SWIPING it left (the live logger's SwipeRow, now a shared component at components/swipe-row.tsx with a host-card background prop), so the ledger reads clean; web keeps its explicit per-row remove button (a swipe gesture is a touch idiom, pointer surfaces want the visible control). RPE ⇄ RIR EVERYWHERE: the tap-the-column-header mode switch the Builder and web editor already had is now also on the mobile LIVE logger's RPE/RIR column head — one gesture on every set-editing surface, all writing the same rpeAsRir pref. BUILDER↔LOGGER PARITY (mobile Builder caught up to the other three surfaces): the M/S bar-velocity column (gated on the same velocity pref, off by default), drag-to-reorder set rows (see reorder-sets-exercises), and the 'Special ▾' add-set menu (warm-up / auto warm-up ramp / cool-down / drop, with the ramp's ~40/60/80% logic shared via core warmupRamp) — all reusing the live logger's i18n keys and visual vocabulary. NEXT: feed restSec into the live logger's rest-timer default per exercise, and surface the pulse on the routine cards in Train." },
  { id: "exercise-database", area: "Core", status: "shipped", title: "Exercise database — every gym exercise as first-class data", detail: "exercise-db.ts: a comprehensive built-in catalog (150+ exercises, integrity-tested) where EVERY gym exercise carries its own property sheet — fine-grained muscles (20 muscles, primary/secondary), movement pattern (squat/hinge/lunge/push-h/push-v/pull-h/pull-v/olympic/carry/core/isolation/plyo), equipment (barbell/dumbbell/kettlebell/machine/cable/bodyweight/trap-bar/ez-bar/smith/sled/medball/landmine…), mechanics (compound/isolation), unilateral flag, LOAD MODE (external / bodyweight — plain Pull-Up, Triceps Dip, Pistol Squat take NO load entry, the column disappears / bodyweight-plus — the Weighted Pull-Up, Weighted Triceps Dip, Weighted Chin-Up variants read BW + added / assisted), MEASURE (reps / time for planks+holds / distance for carries+sleds) and a baseLoad anchor. Covers Chest, Back, Shoulders, Traps & Forearms, Quads & Glutes, Hamstrings & Glutes, Calves, Biceps, Triceps, Abs & Core, Olympic & Power (full snatch/clean/jerk family + KB ballistics) and Carries & Conditioning. Query API: gymExercise (case-insensitive), gymExercisesByMuscle (primaries first), gymExercisesByEquipment, gymExercisesByCategory. ENGINE BRIDGE: each entry derives an engine Movement (fine muscles mapped onto the 7 fatigue groups) and folds into MOVEMENTS — so fatigue attribution, volume-by-muscle, landmarks, records, prescription and BOTH clients' pickers cover the whole catalog with zero per-engine changes; the 18 original hand-tuned entries still override their DB twins so pinned engine behaviour is unchanged, and the sport S&C engine now prescribes real starting loads for DB movements it used to treat as bodyweight. PICKERS: exercisesByCategory folds the DB's muscle-group headings in by default (admin-library categories still override; LIBRARY_CATEGORY_ORDER gained Olympic & Power + Carries & Conditioning); the web editor's ad-hoc BASE_CATALOG is gone. EDITORS READ THE DB: exerciseProfile now returns a per-lift StrengthProfile (measure/loadMode/unilateral/equipment) and the set editors adapt on web + mobile Builder + mobile live logger — a Plank's set column reads seconds, a Farmer Carry reads metres, a Pull-Up's load placeholder reads +kg (BW+added), an Assisted Pull-Up reads −kg. i18n: w.train.blocks.secs EN/PL/DE. DIPS split by target muscle (Jul 2026): 'Triceps Dip' (upright torso) is triceps-primary under Triceps, 'Chest Dip' (forward lean) is chest-primary under Chest, each with a weighted variant ('Weighted Triceps Dip' / 'Weighted Chest Dip', bodyweight-plus) and 'Bench Dip' kept under Triceps — so tonnage and volume-by-muscle attribute the right muscle. RENAMED (Jul 2026): the triceps variant was originally just 'Dip', ambiguous against 'Chest Dip'; it's now explicitly 'Triceps Dip' / 'Weighted Triceps Dip', with GYM_ALIASES bridging the old names so already-logged sessions still resolve and canonicalize (see exercise-name-canonicalization). All four count bodyweight toward tonnage via effectiveSetLoadKg (see bodyweight-tonnage): 10 bodyweight dips at 70 kg BW = 700 kg, the weighted variants add the entered plate. NEXT: aliases per DB entry (e.g. 'Kettlebell Swing' → 'KB Swing'), per-exercise coaching cues + video hooks (exercise-video-library), unilateral '/side' rendering in schemes, and admin-library editing OF built-ins (override a DB entry from the CMS)." },
  { id: "bodyweight-tonnage", area: "Core", status: "shipped", title: "Bodyweight-aware tonnage (10 BW pull-ups at 70 kg = 700 kg of work)", detail: "effectiveSetLoadKg (engines/session.ts) is the canonical per-set effective load, resolved from the exercise DB's load mode: external = the entered kg; bodyweight = the athlete's bodyweight (10 pull-ups at 70 kg BW = 700 kg); bodyweight-plus = BW + the entered added weight (+10 kg → 800 kg total); assisted = BW − the entered assistance. Sets measured in seconds or metres (planks, carries) are NEVER tonnage — multiplying a hold's seconds by a load isn't work done. sessionVolume, sessionSignal, strengthBlockStats and liveSessionStats all take an optional bodyweightKg and stay exactly backward-compatible without it (unknown bodyweight degrades to entered-load math, so guests and pre-weight users see no change). SOURCE: the dated /api/body history (Profile → Private → Body & progress) via bodyweight.ts — bodyweightLookup(points) resolves the weight the athlete WAS at each session's date (sessions older than the log use the earliest measurement; no date → current weight), surfaced by useBodyweightLookup()/useBodyweight() hooks on each client (one shared fetch per page). e1RM AND PRs are bodyweight-aware too: blockBestE1rm ranks bodyweight/BW-plus/assisted lifts on their EFFECTIVE load, and newPrsInSession/prsForSession/bestE1rmMap/bestE1rmByLift/e1rmSeries compare with the SAME dated basis on both sides, so a +15 kg weighted pull-up correctly beats a +10 kg one from a lighter era. WIRED EVERYWHERE the athlete's own data renders: the Builder pulse + signal cards, both live loggers (live stats, finish volume, finish PRs, fun-fact), session detail (stats, per-block e1RM, share card, e1RM trend), history rows, calendar day chips + sessionsByDay, Today/Home done-today meta, analytics (lifetime volume, PR list, volume series), Trends (weeklyVolumeTrend + exerciseTable), per-exercise dashboards, profile (lifetime tonnage, this-week, achievements, PR tiles), statistics + cockpit weekly recaps — on BOTH clients. Unit-tested: the 700/800 kg canonical example, holds/carries excluded, dated lookup semantics, and a BW+ PR comparing on effective load. NUDGE (Jul 2026): a bodyweight lift under-counts (a plain one reads 0) until a weight is on file, so BOTH live loggers show an inline 'Set your bodyweight to count this' card the moment the board has a bodyweight-dependent lift and no weight is known — core needsBodyweight(blocks, bw) + isBodyweightDependent(name) decide when (rep-counted bodyweight/bodyweight-plus/assisted only; holds/carries/external excluded). The card takes a weight in the athlete's unit, POSTs /api/body (web fetch; mobile lib/api.logBodyweight) and calls the new refreshBodyweight() to invalidate the shared bodyweight cache and re-fetch in every mounted hook, so the live volume recomputes and the card self-dismisses without a reload — no navigating away to Profile. i18n EN/PL/DE (w.train.logger.bwNudge* / live.bwNudge*). MUSCLE BREAKDOWN (Jul 2026): volumeByMuscle is now bodyweight-aware too (optional bodyweightKg → effectiveSetLoadKg, with the same holds/carries guard as sessionVolume so a plank never gains bodyweight × seconds) — so the finish-summary muscle slide, session detail's Muscle Focus, the Wrapped share cards, session-wrapped facts and the weekly recap's top-muscle all count dips/pull-ups on both clients instead of reading 0; every call site passes the athlete's weight at that session's date. DELIBERATELY NOT WIRED (viewer's weight would be the WRONG body): coach views of an athlete's sessions, the social feed/compare/leaderboards (other athletes), and the server API summaries (roster, ai-coach prompt, social posts) — each needs the SUBJECT athlete's BodyMetric server-side; recorded here so it isn't forgotten." },
  { id: "dumbbell-tonnage", area: "Core", status: "shipped", title: "Dumbbell tonnage counts both bells (24 kg × 10 = 480 kg, not 240)", detail: "A bilateral dumbbell lift is performed with TWO dumbbells, one per hand, each carrying the entered per-bell load — so a rep moves twice the number on the bell. loadUnitCount(name) (exercise-db.ts) returns 2 for a bilateral dumbbell exercise, 1 for everything else (barbell, machine, cable, a single kettlebell). UNILATERAL dumbbell work splits by which limb is one-sided: single-ARM upper-body work (a one-arm DB row, a concentration curl) holds one bell → 1, but single-LEG lower-body work (a Bulgarian split squat, a walking lunge, a single-leg RDL — squat/hinge/lunge patterns) is unilateral at the LEG while both hands still hold a bell → 2 (previously these read half: 100 kg × 1 counted 100, not 200). This factor scales VOLUME/tonnage ONLY and is applied at every tonnage site — sessionVolume (engines/session.ts), the Builder signal's strengthBlockStats (session-signal.ts), volumeByMuscle (records.ts), the per-exercise dashboard volume (exercise.ts), weeklyVolumeTrend (analytics.ts), and exercise-analytics v2's weekly tonnage + tonnage surface + block-vs-last comparison (via a tonnageKg field on each flattened LiftSet) — so the same doubling flows through lifetime tonnage, calendar/history/recap volume, achievements, fun-facts and the profile stats on BOTH clients with no client math. DELIBERATELY NOT DOUBLED: e1RM, rep-maxes and PR detection (blockBestE1rm/newPrsInSession/bestE1rmByLift and the analytics rep-max matrix / e1RM isolines / intensity zones) stay PER-BELL — a dumbbell 1RM is quoted per bell, and a cross-lift 1RM board must not rank dumbbell work on a doubled number — so loadUnitCount lives APART from effectiveSetLoadKg and is never folded into it. Unit-tested: the 24×10 = 480 canonical example, a unilateral DB lift staying single, and a barbell lift unchanged. GUIDANCE: both loggers (web workout-blocks + mobile Builder + mobile live logger, at parity) show a per-dumbbell hint under any bilateral dumbbell lift — 'Enter one dumbbell's weight — volume counts both.' (i18n w.train.blocks.dbPerHint, EN/PL/DE) — so the athlete enters one bell and understands the doubled tonnage. CUSTOM LIFTS: a free-text movement the catalog doesn't know (the picker's '+ …' custom add) never carries an equipment tag, so loadUnitCount falls back to the NAME — anything reading as a bilateral dumbbell move ('Dumbbell Thruster', 'DB Snatch') counts two bells, while single-arm / concentration phrasing stays one — so a bespoke DB lift no longer under-counts its tonnage by half. Because the doubling lives in the shared loadUnitCount, the per-dumbbell hint and every tonnage site pick it up on BOTH clients with no extra wiring. IMPLEMENT COUNT: a GymExercise can carry an explicit `implements: 1 | 2` that overrides the heuristic for a hold it can't read — a two-hands-ONE-implement lift (Goblet Squat, overhead dumbbell extension) gripped bilaterally on a single bell, where 100 kg × 10 is 1 000 kg, not 2 000. So loadUnitCount resolves in order: explicit implements → non-dumbbell = 1 → bilateral or single-LEG dumbbell (both hands loaded) = 2 → single-ARM dumbbell = 1; the custom-name fallback mirrors it (single-arm / goblet / pullover phrasing = 1, else a named dumbbell move = 2). Because tonnage is computed on read (never stored — Session.blocks holds the raw sets), correcting the count reflows through all historical sessions on both clients with no data migration." },
  { id: "exercise-name-canonicalization", area: "Core", status: "shipped", title: "Renaming an exercise propagates to every summary (logged-name canonicalization)", detail: "Renaming an exercise now reflows through EVERY summary, history row and analytic — not just future logs — with no data migration (the sandbox can't touch the DB). Root cause it fixes: a logged Session stores the exercise as a plain string, so renaming the catalog entry (e.g. the barbell 'Incline Bench Press' → the dumbbell 'Incline Dumbbell Bench Press') left already-logged sessions displaying the OLD name. THE LAYER: (1) a built-in rename registry GYM_ALIASES (exercise-db.ts) maps an old catalog name → its current name; gymExercise()/loadUnitCount() now resolve through it, so a historical 'Incline Bench Press' set gets the dumbbell profile + correct two-bell tonnage. (2) exerciseNameAliasMap(library) (engines/movements.ts) folds those built-in breadcrumbs with the admin library's per-entry aliases (alias → primary name) into one name→canonical map; canonicalExerciseName(name, map) resolves a name, following a rename CHAIN (A→B→C) with cycle-guarding. (3) canonicalizeBlockNames(blocks, map) rewrites each block's display name; migrateBlocks() now applies it (built-in map by default) at EVERY read boundary it already guarded — so the old name heals to the new one everywhere migrateBlocks runs (server /api/sessions GET + coach/roster/compare/squad, ai-coach, social feed, mobile guest sessions). (4) /api/sessions GET passes the FULL map (built-in + the cached published library's aliases) so an ADMIN rename (which keeps the old name as an alias) propagates to the athlete's own summaries on BOTH clients (mobile reads the same endpoint). Only the display name changes — sets/e1RM/PRs are untouched; idempotent + pure. Unit-tested (movements + session + exercise-db): the incline heal, admin-alias folding, custom-wins-over-builtin, rename-chain + cycle guard, and migrateBlocks canonicalization. Ships with the Incline Bench Press breadcrumb seeded." },

  { id: "body-progress-dashboard", area: "Core", status: "shipped", title: "Body & progress — weekly report + trends dashboard", detail: "Profile → Private → Body & progress, rebuilt from a two-field (weight + waist) box into a weekly body report over a trends grid, on BOTH clients. body-progress.ts is the shared source of truth: metricTrends() collapses the dated /api/body history into per-metric trends (latest, delta vs previous, direction, an up-to-8-point series); weeklyReport() derives the latest weight, its ~7-day delta, logging cadence (distinct days in the last 7) and a narrative verdict that reads the weight move together with waist/body-fat direction (down + leaner tape → 'lean'; up + bigger tape → 'build'); sparkHeights() normalises a series into dependency-free column-bar heights (mobile has no react-native-svg, so both clients render the sparkline with Views/divs — identical maths). The logger now captures all nine BodyMetric fields the API already stored but the UI hid (weight, body-fat, neck, chest, waist, hips, thigh, arm, calf); the trends grid surfaces whichever the athlete tracks. First-run state invites the first measurement with placeholder tiles. No API/schema change — GET /api/body already returned the full history. i18n EN/PL/DE. Pure trend maths is unit-tested. REDESIGN (Jul 2026): the block no longer sits as an always-expanded panel on the Private tab. It is now a COMPACT card in the same nutrition-row idiom as Today's rows (icon tile + title + a live subline that leads with the latest weight then the tracked-metric count, joined by a spaced en dash + a chevron) — the whole surface is tappable. Tapping opens the shared slide-up Sheet, which LEADS with the full log-measurement form (all nine BodyMetric inputs), exactly like 'Add a meal' opens from the Nutrition row; the weekly report hero, the per-metric trends grid and the progress-photos link now live INSIDE that sheet beneath the form. The old separate 'Log' pill and the standalone EmptyBody first-run panel were removed (the form is the first thing in the sheet). Web + mobile at parity (PrivateTab.BodyBlock on each). REHOMED (Aug 2026): the collapsed row + slide-up sheet are GONE with the Private tab that held them (see profile-private-tab). The report hero, the height row, the log form and the trends grid now render INLINE on Nutrition → Body, out of the shared aurora/body-progress.tsx panel — same maths, same components, one screen instead of a row that opens a sheet. See profile-private-body." },
  { id: "session-notes", area: "Core", status: "shipped", title: "Private per-workout note (mood + tags)", detail: "Private post-workout reflection attached to each logged Session, on BOTH clients — replacing the standalone Journal (which overlapped session notes + the daily recovery check-in note). The finish screen reveals an optional note: free text + a quick mood tap (1..4, rough → strong) + context tags (tough/easy/pr/niggle/form/deload/tired/motivated), PATCHed onto the just-saved session next to the existing rename affordance; the note (mood dot + text + tags) then renders on the owner's History cards. Shared primitives in notes.ts (MOODS, SUGGESTED_TAGS, sanitizeNote/Mood/Tags, hasNote) — pure, unit-tested. Schema adds Session.note (String?) + mood (Int?) + tags (String[]). OWNER-ONLY: note/mood/tags are kept out of the LoggedSession DTO and every non-owner serializer; the one raw-row endpoint (coach reads a client's sessions) now uses an explicit select that excludes them. The standalone Journal block was removed from Profile → Private; its JournalEntry model + /api/journal are left dormant (not dropped) so any existing notes are preserved. i18n EN/PL/DE. Session.note/mood/tags are APPLIED in production (sql-pending.sql run, Jul 2026)." },
  { id: "exercise-profile-model", area: "Core", status: "shipped", title: "Exercise-profile model — every exercise declares its OWN values & units", detail: "One declarative resolver (exercise-profile.ts: exerciseProfile(name) → { kind, fields[], pace, distanceUnit, paceLabel }) answers 'which fields does THIS exercise get' for every movement and sport, on both clients: running gets pace + distance (km) + time + elevation gain; swimming paces per /100 m with metre distances and a stroke field; rowing/canoe pace per /500 m; treadmill work swaps elevation for incline (%); tennis/judo/team sports are time-only; every cardio effort carries an HR zone; strength prescribes per-set load × reps × effort + planned rest; conditioning keeps format/work/rest/rounds. Resolution order: the Olympic-sports catalog wins (a catalog sport ALWAYS logs as cardio — the keyword heuristic only matches whole words, 'Run' but not 'Running'), then inferBlockKind + name heuristics for custom entries (treadmill/incline-walk → incline; swim strokes → stroke; run/trail/hike/ruck/cycling/skiing → elevation, suppressed when incline applies). Replaces the ad-hoc cardioExtras regex helper. Schema: CardioBlock.elevation (m) joins incline/stroke/zone — all optional + additive. CONSUMED BY: the web shared editor (workout-blocks.tsx — Builder AND live Logger), the mobile Builder (CardioEditor), and the mobile LIVE logger (app/workout.tsx: WExercise carries incline/stroke/elevation/zone, the cardio card renders the profile's fields, buildBlocks persists them, itemsFromBlocks restores them — with ?? '' guards so a pre-upgrade draft resumes safely). Unit-tested (exercise-profile.test.ts). i18n: w.train.blocks.elevation EN/PL/DE. NEXT: per-exercise strength profiles (tempo, unilateral/per-side, bodyweight+load), and surfacing stroke/incline/elevation in history + session detail summaries." },
  { id: "exercise-dashboard", area: "Core", status: "shipped", title: "Per-exercise dashboard (open a lift, see all its progress)", detail: "A dedicated dashboard for ONE movement — its whole progress history on web + mobile. exercise.ts: the new pure per-movement aggregator exerciseDashboard(sessions, name, period) with date-WINDOWING (periodCutoff: 8 weeks / 6 months / 1 year / all) + exerciseKind detection, returning a discriminated union — STRENGTH: best e1RM (window + all-time), working sets, tonnage, sessions/frequency, heaviest load, the best set, and the windowed e1RM trend (e1rmSeries); CARDIO: runs, distance, longest, best pace + a windowed pace trend (paceSeries/runStats). Reuses the canonical session helpers (warm-ups already excluded, so working-set counts are honest) so it can't drift; 6 unit tests. SURFACED as a new 'Exercises' screen (analyze group — FREE FOR ALL personas: the minPersona gate was removed so casual/free users get per-exercise progress too; it's a universal hook, not paid depth) on BOTH clients — web components/exercises.tsx (searchable exercise list + per-period charts via recharts) and mobile app/exercises.tsx (search + chips + dependency-free trend bars). A period selector (8wk/6mo/1yr/all) re-slices every stat. Honest empty states. Nav entry localized EN/PL/DE (screen body English for v1). DEEP-LINKED: every strength/cardio exercise name in the session-detail breakdown (web components/screens.tsx + mobile app/session/[id].tsx) is now tappable — straight to that movement's dashboard (web focuses the Exercises screen via app-shell's openExercise/exerciseFocus; mobile router.push('/exercises', { name })) — so you drill in from where you're already looking, in addition to the Trends table and the Exercises search. NEXT: the same deep-link from the live exercise picker, and the velocity profile inline when bar speed is logged." },
  { id: "exercise-analytics-v2", area: "Core", status: "shipped", title: "Exercise dashboard v2 — advanced per-movement analytics", detail: "The (free-for-all) Exercises screen upgraded from 4 tiles + one trend line to a full analytics surface, on BOTH clients from ONE set of pure aggregators (engines/exercise-analytics.ts, 14 unit tests, all computed from LoggedSession so it works offline for free users). Shipped cards — strength: 1) heaviest-lift trend with PR-marked sessions (topLoadTrendWithPRs — the actual top weight is the headline, e1RM is a secondary stat; PRs judged against ALL history, not just the window), 2) rep-max matrix (best-ever 1–10RM grid with 8-week freshness shading + never-attempted cells as session ideas), 3) load×reps map (every working set as a dot, dashed Epley equal-e1RM isolines, recent sets highlighted), 4) weekly tonnage stacked by effort (hard = RPE ≥ 8; no-RPE counts as base), 5) intensity distribution (%e1RM zones vs the ROLLING all-time best so old sets grade honestly), 6) isometric 3D tonnage landscape (12 weeks × rep-range bins); cardio: 9) best-pace-per-distance-band curve (all-time vs last 8 weeks) + last-runs-vs-own-average delta bars (the honest stand-in for km splits, which aren't logged); both kinds: 8) per-movement consistency (26-week heat calendar reusing trainingHeatmap + week streak / per-week / longest gap), 10) this-block-vs-previous compare (8-week windows: delta-chipped metric tiles + week-aligned weekly overlay). Web renders recharts + custom SVG (aurora/exercises.tsx), mobile mirrors with react-native-svg (aurora/exercise-charts.tsx). Chart colors: brand accents for single-series, the CVD-validated deep chartreuse/sand pair (#84a01e/#bd871e) for stacked series. Localized EN/PL/DE. Designed in reference/exercise-analytics-designs.html; concept 7 (rankings) intentionally NOT built — see exercise-analytics-rankings." },
  { id: "today-exercise-widget", area: "Core", status: "shipped", title: "Today Exercises widget + individual exercise page (variant B)", detail: "The free-for-all Exercises surface pulled onto the HOMEPAGE as an iPhone-style widget rail, plus a dedicated per-movement page — on BOTH clients from ONE core module (exercise-widget.ts, 12 unit tests). WIDGET: exerciseWidgetCards auto-picks favourites so different purposes lead (most-trained strength lift, cardio move and conditioning move of the last 8 weeks, explicit favourites honoured first — the favourites param is ready for a user-managed list), each card carrying an 8-week headline (strength = the ACTUAL HEAVIEST lift, topLoadSeries, not estimated 1RM — e1RM is a secondary stat / best pace / strength-fallback tonnage / weekly MINUTES for conditioning + minutes-only cardio — ConditioningBlock has no per-set loads, so duration IS its volume, derived from the interval format when unlogged), a stock-ticker delta vs the PREVIOUS 8 weeks (pace: faster = improving) and a full-bleed sparkline — the 'chart is the card' reduced design (variant B) chosen over the badge-heavy variant A in review; prototypes for both in reference/exercises-widget-preview{,-ive}.html. Rail hides until history exists; end card + All link open the Exercises tab. PAGE: exercisePageModel returns hero-per-slide stats (strength: heaviest-lift PR trend (topLoadTrendWithPRs — actual top weight, e1RM demoted to a secondary substat), weekly tonnage, %e1RM zones, 26-wk consistency; cardio: pace trend, pace curve, runs-vs-average deltas, consistency — each gated on real data, minutes-only cardio falls back to weekly minutes; conditioning: weekly minutes, consistency) rendered as a no-boxes screen — one big number crossfading with the swiped chart, hairline segment indicator, quiet ALL STATS expansion, typographic substats row, 8W/6M/1Y/All underline periods. Web: aurora/exercise-widget.tsx + exercise-page.tsx (recharts), new 'exercise' app-shell screen (back → Today). Mobile: same-named components (react-native-svg), /exercise?name=… route, rail on aurora/home.tsx. Localized EN/PL/DE. NEXT: user-managed favourites (persisted), and PR push notifications off the widget deltas." },
  { id: "exercise-page-unification", area: "Core", status: "shipped", title: "One canonical exercise page (Exercises screen becomes a picker)", detail: "Fixes the split where an individual exercise opened TWO different screens depending on entry point: the Today favourites widget led to the variant-B exercise page, while More → Exercises / Trends / History / session detail led to the older inline Exercises dashboard. The exercise page is now the golden standard — the ONLY individual-exercise screen — and it absorbed the dashboard's full data/chart set AS SLIDES in the variant-B pager (per review: no boxed card stack — one hero number crossfading with one full-bleed chart, matching the Ive-reduced design). exercisePageModel gained the deep-dive slides, all gated on real data: repMax (best-ever 1–10RM grid, hero = heaviest rep-max), loadReps (load×reps cloud + e1RM isolines, hero = working sets plotted), surface (12-week isometric tonnage landscape, hero = peak week×rep-bin tonnage), compare (this 8-week block vs the previous — weekly overlay + delta-marked metric tiles, hero = block volume/distance with a ticker delta), and the consistency slide upgraded to the 26-week heat calendar + streak/per-week/gap/active-day stats (exerciseConsistency detail); the cardio paceCurve slide now draws the recent-vs-all-time band curve. Slide order: strength heaviest-lift (topLoadTrendWithPRs — the actual top weight is the headline; e1RM stays secondary in the substats + zones/isolines) → tonnage → zones → repMax → loadReps → surface → compare → consistency; cardio pace → curve → deltas → compare → consistency (unit-tested). Best set + velocity profile render as quiet typography over a hairline under the substats — no boxes anywhere. The interim web aurora/exercise-analytics.tsx and mobile aurora/exercise-charts.tsx card files were deleted; both clients render the new slides natively (recharts/SVG on web, react-native-svg on mobile) from the ONE core model. The Exercises screen is now a pure movement PICKER (search + kind/count/last-used rows) routing to the page: web app-shell's single openExercisePage (Today widget, picker, Trends, History/session detail) remembers the return screen so back goes to wherever you came from; mobile /exercises rows push /exercise?name=…, legacy /exercises?name deep links redirect to the page, and the Trends + session-detail deep links were retargeted. Same @hybrid/core aggregators throughout." },
  { id: "exercise-anatomy-animation", area: "Core", status: "shipped", title: "Exercise page — 'How it's done' animation + muscle activation + body-map", detail: "Every gym lift's exercise page now has a 'How it's done' surface, on BOTH clients from ONE pure core module (exercise-anatomy.ts, unit-tested). THREE parts, all derived from the exercise DB so all ~190 lifts are covered with zero per-exercise authoring: (1) a LOOPING SCHEMATIC ANIMATION of the movement — a side-profile stick figure whose 7-joint skeleton ping-pongs through 2–3 hand-tuned keyframes per movement ARCHETYPE (squat/lunge/hinge/hipThrust/pressH/pressV/dip/pullH/pullV/curl/extension/raise/calf/plank/crunch/hangingLeg/twist/jump/carry/olympic/generic; exerciseArchetype() resolves pattern-first, refined by category/name so a curl, a lateral raise and a triceps pushdown each get the right motion), with the loaded implement drawn per equipment (barbell/dumbbell/kettlebell/bodyweight/fixed) and a faint dotted trace of the bar path; skeletonAt() interpolates a triangle-wave eased phase so web + mobile render the exact same motion, and both respect prefers-reduced-motion (a held mid-rep pose). This is the unblocked, offline, zero-asset answer to 'show the movement' while the hand-drawn demo library stays blocked (exercise-video-library). (2) MUSCLE ACTIVATION — the DB's primary/secondary muscles (order = importance) become a ranked list with a share-of-effort PERCENTAGE per muscle (primaries take the bulk weighted by order, secondaries split the remainder; muscleActivation() always sums to 100) and a High/Moderate/Low tier, each with an anatomical label ('Pectoralis major (chest)') and a bar — the 'main driver … triceps ~25%' breakdown, computed not typed. (3) STABILIZERS + step-by-step form CUES + a plain-language EMPHASIS line (e.g. bench: 'the shoulder joint contributes ~75% of the force, triceps finish the lockout ~25%'), keyed off the archetype. PRESENTED AS A BOTTOM SHEET: a compact 'How it's done' PILL sits under the exercise name; tapping it opens a slide-up sheet (web = scrim + dialog with Esc/scroll-lock, mobile = RN Modal animationType slide, both bottom-anchored with a grabber + close) holding the animation + muscles + stabilizers + cues. This keeps the page a clean STATS view for the many athletes who already know the lift; the animation loops only while the sheet is open (an `active` prop gates the rAF), and on mobile a light haptic fires on open (gated on the user's haptics preference). Renders on web (aurora/exercise-anatomy.tsx) and mobile (aurora/exercise-anatomy.tsx); the pill sits directly under the exercise name above the analytics and skips silently for cardio sports + custom names the DB doesn't know. Localized EN/PL/DE (w.analyze.exp.anatomy.*). ANIMATION SEPARATED FOR SWAP: the movement demo is deliberately isolated from the muscles/cues so it can be replaced by hand-drawn art without touching the anatomy content. Core exercise-animation.ts owns the movement taxonomy (exerciseArchetype), the skeleton + keyframes + skeletonAt, and nothing else — the drawn art and the single registry it lands in live in exercise-media.ts (see exercise-media-layer), which resolves what a surface actually shows and falls back to this figure per-lift until that lift is drawn. Each client's exercise-animation.tsx renders the procedural figure only; the anatomy sheet embeds exercise-media.tsx, so shipping sketches changes neither. FRONT/BACK BODY-MAP (Jul 2026): the side-profile motion is now complemented by an anatomical BODY-MAP that shows WHERE the effort lands — a second pure-shared core module (body-map.ts, unit-tested) of schematic geometry: two mannequin figures (front + back, so posterior-chain lifts light their hamstrings/glutes/lats/erectors — a single view would miss half the body), each muscle a simple polygon region in the same 0-100 box as the skeleton (bilateral muscles carry a mirrored left+right shape; each of the 20 DB muscles is assigned to EXACTLY ONE figure so it never appears twice). Every region GLOWS in proportion to that lift's share of effort — intensity = the muscle's pct ÷ the top mover's pct (0..1), computed by muscleGlows() from the SAME muscleActivation() data as the ranked bars, so the picture and the numbers can't drift: primary movers bright, secondary dimmer, untargeted faint (one hue, opacity carries the signal). exerciseBodyMap(name) returns the ranked glow + a muscle→intensity map, or null for a name the DB doesn't know. Rendered on web (aurora/body-map.tsx) and mobile (aurora/body-map.tsx) at parity, embedded in the 'How it's done' sheet directly under the 'Muscles worked' header — the VISUAL first, then the ranked bars — as two side-by-side figures (Front/Back labels) with a legend (Primary/Secondary/Not targeted) and a caption that names the top mover by default and swaps to whichever muscle you hover (web) / tap (mobile); theme-aware (Aurora dark ⇄ Kyoto light, drawn in the lime accent). New i18n w.analyze.exp.anatomy.map.* (front/back/primary/secondary/off, EN/PL/DE). Verified by headless-Chromium render across bench/deadlift/curl/pull-up/press/squat. BODY-MAP SEPARATED FOR SWAP (like the animation): the schematic mannequin is deliberately the zero-asset DEFAULT, ready to be replaced by professional anatomical illustration without touching the muscle/cues section. exerciseBodyMap(name) returns kind:'schematic'|'sketch' — the SAME discriminated-swap idiom as exerciseAnimation — carrying the always-present schematic `figures` plus an optional `sketch` (SketchBodyArt: base front/back illustration + per-muscle highlight overlays). A single SKETCH_BODY_ART holder (EMPTY today; the body art is GLOBAL — the illustration doesn't change per lift, only which muscles glow — so unlike the per-exercise SKETCH_MEDIA registry it's one art set, not a name→asset map) is the single swap point: set it and every lift flips to kind:'sketch', with the SAME intensity data driving the overlay opacities. Each client's body-map.tsx switches on kind (schematic renderer live; sketch renderer — base image + intensity-composited per-muscle overlays — wired but dormant). NEXT: populate SKETCH_MEDIA (exercise-media.ts) + SKETCH_BODY_ART once the commissioned art exists (exercise-video-library)." },
  { id: "exercises-smart-browse", area: "Core", status: "shipped", title: "Exercises picker — smart sort, hybrid buckets, Aurora-pass design", detail: "The Exercises screen (the movement picker) rebuilt from the raw last-used dump into the reviewed 'Aurora pass' design, on BOTH clients from ONE pure core module (engines/exercise-browse.ts, unit-tested). SMART ORDER: a time-decayed use score (Σ 0.5^(daysAgo/14) over the last 90 days) so the ranking tracks the CURRENT training block, not lifetime totals — switch from a year of back work to legs and the leg lifts top the list within a week; a recency-tier floor (used this week → this month → older) guarantees this week's rotation can never sink below ancient history. HYBRID BUCKETS: seven one-word sections (Olympic, Legs, Posterior, Push, Pull, Core, Engine — merging the muscle-group headings with the movement patterns; pattern is the tiebreaker so a deadlift filed under Back lands in Posterior, sled work in Engine, Olympic & Power plyo in Olympic; keyword fallback buckets free-typed names like 'Clean Extension' → Olympic, with the snatch/clean/jerk family beating the lift words they contain). SIGNALS: staple (trained ≥3 of the last 4 weeks → lime monogram tile), stale (established lift untouched ≥14 d → amber recency ghost), in-rotation (≤14 d). UI (web aurora/exercises.tsx + mobile aurora/exercises.tsx, assembled from shipped parts): Explore's hero + search, Smart/Groups/A–Z sort pills (lime fill active), a 'This block' band wearing Profile's cover gradient (violet→lime wash + lime corner glow) with in-rotation/this-week stats, Explore SectionHead-style bucket headers (18px black + mono count), plan-card row cards with monogram tiles and a single mono recency ghost ('5 d' / 'today') — counts/dates/kind live on the exercise page, not the list. Groups pill = fixed bucket order; Smart = buckets ordered by best-ranked entry; A–Z = flat alphabetical; search filters flat. Localized EN/PL/DE (w.analyze.ex.*). Designed in-session (8 sorting concepts → combined buckets → Ive reduction → Aurora tokens); rationale preserved in the session's preview HTML." },
  { id: "exercise-rail-pinning", area: "Core", status: "shipped", title: "Exercises rail — pin the movements you're chasing (＋ adds, See all displays)", detail: "The Exercises rail's trailing tile was a dashed '＋ All exercises & favourites' that NAVIGATED to the exercises list — a plus that added nothing, and a list with no way to change what the rail showed. The rail's tail now carries TWO doors: (1) '＋ Add an exercise' opens a pin sheet and the chosen movement is a card in this rail before the sheet closes; (2) the shared RailTail 'See all' just DISPLAYS the exercises list, touching no pins (the one-exit rule keeps both at the end of the rail, where the swipe ends). PINS: exercise-favourites.ts is the shared source of truth — key, cap (8), case-insensitive identity, a normalizer for corrupt/older stored values, and toggleExerciseFavourite, which at the cap returns the list UNCHANGED rather than silently unpinning an earlier choice (the UI dims locked stars and says why). Each client persists per-device with the logger-prefs idiom (web localStorage + useSyncExternalStore, mobile AsyncStorage) — cross-device sync rides prefs-cross-device-sync with the rest of the per-device preferences. CORE: exerciseWidgetCards takes the pins first, in pin order, and its ceiling now LIFTS to the number pinned (max=3 is only the size of the auto-fill GUESS) — pinning a fourth movement that then failed to appear would read as the pin not working. A pin with no logged history is still dropped: there is no 8-week chart to draw, which is also why the sheet only offers movements from exerciseBrowse(sessions) rather than the whole catalog. THE SHEET (aurora/exercise-favourites-sheet.tsx, both clients): search, pins first (visible + removable), then the exercises screen's Smart order, rows wearing that screen's anatomy (40px monogram tile, recency ghost, ★/☆). The EXERCISES SCREEN itself gained the same star per row — the row is now two controls, the body opening the movement's page and the ★ pinning it — so the rail is editable from the one place that lists everything. i18n w.home.exw.add*/pin/unpin/pinned/yourMovements EN/PL/DE (the old allCard string retired). Web + mobile at parity; core pin logic + the widget's pin ceiling unit-tested." },
  { id: "exercise-analytics-rankings", area: "Core", status: "planned", title: "Exercise analytics — rankings (standards, percentiles, club board)", detail: "Concept 7 of the exercise-analytics designs (reference/exercise-analytics-designs.html), explicitly excluded from the v2 build by user decision. Would add: strength-standards ladder (beginner→elite by bodyweight multiple), percentile vs bodyweight class, age/sex-graded running percentile, and a club leaderboard (bodyweight-normalised, opt-in). Standards + percentile tables can ship as static core data; the club board needs the social schema (see social-schema)." },
  { id: "training-analytics-hub", area: "Core", status: "shipped", title: "Training analytics hub (muscle breakdown, volume trends, exercise analytics)", detail: "A 'Trends' destination that sits ABOVE the per-exercise dashboards (exercise-dashboard) and ties the logging-quality ideas together — distinct from the web-dashboards capability (the client/coach/admin SCOPE overview); this is the deep training view. analytics.ts (pure + 3 unit tests): weeklyVolumeTrend (rolling 8-week WORKING-set count + tonnage — warm-ups excluded via warmup-sets) and exerciseTable (one row per movement trained in the period with frequency, heaviest actual lift (topWeight — e1RM demoted to secondary), volume and an improvement trend ▲/▼/→ off the top weight, sorted by volume, built on exerciseDashboard). The screen composes three sections: (1) VOLUME TRENDS — weekly working-sets + tonnage bars; (2) MUSCLE BREAKDOWN — this week's per-muscle sets coloured by landmark zone (volumeStatus/volumeAdvice from volume-landmarks) with a link to the Volume screen for detail; (3) EXERCISE ANALYTICS TABLE — sortable, an 8wk/6mo/1yr/all period selector, each row DRILLING INTO that movement's per-exercise dashboard. Web components/trends.tsx (recharts; row → focuses the Exercises screen via a shared exerciseFocus state in app-shell) + mobile app/trends.tsx (dependency-free bars; row → router.push('/exercises', { name }) which the Exercises route preselects from the param). Nav entry 'trends' (analyze group, athlete) localized EN/PL/DE; screen body English for v1. Honest empty states. NEXT: per-muscle volume trend lines (not just overall) and sortable column headers." },
  { id: "trends-sheet", area: "Core", status: "shipped", title: "Trends — one sheet, figures first (card redesign)", detail: "The Trends screen's three floating cards (weekly sets, weekly tonnage, exercise analytics) are now ONE sheet divided by hairlines — idea 7 of design/trend-cards-redesign-ideas.html, picked from seven treatments. What it fixes, in the order the critique found it: (1) THE FIGURE LEADS. The week's number was the smallest text on the card (10px mono, bottom-right corner) under 80dp of chart; it is now the band's subject at fs.display with its unit beside it and its eight-week average under it. (2) TRUE ZERO. Every empty week used to be floored at a 6px stub, so 'trained nothing' and 'trained a little' drew identically and the floor read as a real baseline. The new sparkline (packages/core/src/spark.ts, `sparkline()` — ONE geometry function, unit-tested, consumed by web <svg> and mobile react-native-svg so the two lines cannot drift) anchors the scale at zero and sits an empty week ON the baseline. (3) THE TWINS. Sets and tonnage were the same eight weeks drawn twice in two hues; they are now two bands of one object. (4) ONE MEANING PER COLOUR. Chartreuse marks only the current week and the live selection — exercise names return to chalk, and the period switch loses its filled pill for an underline that moves. (5) THE ARROW BECOMES A FIGURE. `ExerciseTableRow.change` carries the SIGNED delta behind `trend` (strength: kg on the top load; cardio: seconds per km on pace, where a negative number is faster), rendered by the shared `fmtRowChange()` as '+5 kg' / '−12 s/km' with a true minus, colour still taken from `trend` because the sign alone can't say 'improved'. The column is sortable like the rest. (6) SENTENCE-CASE LABELS, no mono capitals and no separator in a title — mono is kept for data, where tabular figures earn it. Sorting keeps all five keys (the header's second line mirrors the row's second line: freq – volume). New i18n setsMeasure/tonnageMeasure/avg8w/perExercise/colChange (EN/PL/DE). Web recharts BarCharts retired from this screen. Web + mobile identical in the same change." },
  { id: "web-session-detail", area: "Web", status: "shipped", title: "Session detail (web)", detail: "History rows are now clickable (with a 🏆 PR chip) and open a full session breakdown matching mobile: minutes/sets/tonnage stats, a PR banner (pr-detection), a muscle-focus tonnage chart (volumeByMuscle), the heaviest lift's e1RM trend (recharts LineChart over e1rmSeries), and — for runs — a first-class PACE trend (paceSeries over the session's headline cardio move, recharts LineChart with a reversed axis so faster trends up, m:ss/km ticks), plus a per-exercise table with each set + block est-1RM, PR-badged. Pure client state in the History screen — no new route." },
  { id: "web-plans-sport", area: "Web", status: "shipped", title: "Plans + Sport screens", detail: "Render the shared plan library and sport engine. The Plans landing is the library cover over one full-bleed shelf per category (see plans-stack-one-object); free-text SEARCH over the shared, unit-tested filterGoalGroups(query) in @hybrid/core (matches goal name/blurb, drops empty groups) is what narrows it, so the two clients narrow identically and can't drift, and an empty result shows a 'no goals match' line. The category chips no longer filter — with every category already on screen as its own shelf, narrowing to one emptied the screen, so they dock under the collapsed cover bar and JUMP to a shelf instead. The Sport screen remembers the athlete's sport, level and per-sport performance markers (persisted client-side on web; sport + level on mobile), so it reflects them rather than opening on a default selection." },
  { id: "web-periodize", area: "Web", status: "shipped", title: "Periodize", detail: "Renders a real macrocycle (phase timeline + load/recovery microcycles). Now on mobile too (app/periodize.tsx — same phase timeline + per-phase microcycle bars off the enrolled season), reachable from the More hub + the cockpit." },
  { id: "web-running", area: "Web", status: "shipped", title: "Running analytics (folded into the Endurance hub)", detail: "Running analytics — now the 'running' discipline INSIDE the Endurance hub (the standalone Running screen + its nav item were removed on both clients to avoid duplicating what the hub already shows) — read the logged cardio: a pure, unit-tested running engine (running.ts — runTotals, runStats per move with best pace + longest, weeklyMileage over the last 8 weeks, paceEffortSplit, pacedRunMoves) drives headline totals (runs / distance / time / easy-%), a weekly-mileage chart (recharts bars on web, dependency-free bars on mobile), a pace trend per move (paceSeries, reversed axis / faster-is-taller bars), an easy/steady/hard PACE-ZONE bar — intensity DERIVED FROM PACE with no manual input (no RPE, no HR, nothing typed): per move, anchored to the athlete's best (fastest) pace (hard ≤6% off best, easy ≥16% slower), and a tightly-clustered move (<8% spread) reads as steady rather than guessing; the zoning reads DEVICE TRUTH — both the pace and the minute-weighting come from cardioSeconds, not from display-rounded minutes, since a 7:52 effort typed as '8 min' sits 1.7% off its own pace and that is enough to push an effort across the 6%/16% line and contradict the pace shown beside it (runStats' best pace moved to the same second-accurate anchor in the same change, so the by-move table and the zone card can never disagree about one effort); the bar auto-hides until there's enough pace data — and a by-movement table (runs/km/longest/best pace). Honest empty state until a run with distance + minutes is logged. These now render inside the Endurance hub (aurora/endurance.tsx, both clients) when 'running' is the selected discipline — the old aurora/running.tsx screens + the /(tabs)/running route + the 'running' nav item were deleted. DISCIPLINE-TAGGED CARDIO: every logged sport lands in a cardio block (a pool session, tennis, a ride, a row all share the same shape), so a CardioBlock now carries a coarse `discipline` tag (running/swimming/cycling/rowing/skiing/walking/sport/other — session.ts cardioDiscipline()). It's stamped at log time by the sport/run loggers (quick-sport, sport, run-track on BOTH clients) and backfilled from the name on read by migrateBlocks, so consumers read a stable tag instead of re-guessing. The Running screen filters via runningSessions() (discipline === 'running') so a swim or tennis session never counts as a 'run'; the Cockpit's Endurance summary filters via enduranceSessions() (drops discipline === 'sport' — tennis/football/etc — but keeps swims/rides/rows/generic cardio); the AI coach keeps its mileage/pace/easy-hard math run-coherent and reports other endurance (swim/ride/row) as a separate cross-training count. Both clients feed the SAME filters, so they can't drift." },
  { id: "endurance-hub", area: "Core", status: "shipped", title: "Endurance hub — per-discipline analytics (swim/bike/row/ski), BOTH clients", detail: "The Running screen was runs-only, so swims, rides, rows and skis had no analytics home — the Endurance hub gives each endurance discipline the same analytics the Running screen gave running, keyed off the CardioBlock `discipline` tag. CORE: endurance.ts — activeDisciplines(sessions) (disciplines with logged cardio, most efforts first; excludes 'sport' — tennis/football aren't endurance), DISCIPLINE_META (per-discipline label key + emoji + pace-vs-speed mode + pacePer + unit), formatDisciplinePace()/disciplinePaceUnit() (labels a canonical sec/km rate as that discipline's pace — /km run, /100m swim, /500m row — or km/h SPEED for cycling), and disciplineSessions() in running.ts. 6 unit tests. BOTH CLIENTS (parity): mobile app/(tabs)/endurance.tsx + components/aurora/endurance.tsx, and web components/aurora/endurance.tsx mounted at the 'endurance' screen — same sections in the same order (discipline picker, totals, weekly volume, effort split, pace trend, by-move table), same core helpers, same empty states; only the rendering differs (recharts on web, dependency-free bars on mobile). Every pace/best value is labelled in the selected discipline's own unit. PARITY HISTORY: the standalone Running screen was removed from both clients and folded in as the 'running' discipline, but the web hub was initially NOT built — web got a 'get the app' pointer instead, which broke the parity rule and left the web Cockpit's Endurance module linking at nothing. The web hub now exists and the Cockpit links to it again on both clients. REMAINING (Phase 2): cycling speed refinements + elevation, swim stroke/zone breakdowns, per-discipline PRs (formatCardioPr), and per-effort distance in the discipline's natural unit (MVP shows km everywhere; only pace switches unit)." },
  { id: "web-velocity", area: "Web", status: "shipped", title: "Velocity (VBT) screen", detail: "Velocity screen on the shared vbt-engine: pick a lift → estimated 1RM from velocity, the load–velocity profile chart (measured sets + fitted line crossing the MVT), the velocity-zone reference, recent sets with bar speed + zone, and the autoregulated 'AI load' recommender (drag a target bar speed → suggested kg + %1RM). The live logger captures bar speed per set via the exercise detail sheet (live-logger-exercise-sheet, both clients); the Builder grid keeps its m/s column behind the velocity pref. With no bar speed logged the screen shows an honest empty state (no sample data) on web + mobile." },
  { id: "team-compare", area: "Web", status: "shipped", title: "Team compare (group coaching)", detail: "A coach lines up their whole roster side by side on any lift — best e1RM, the velocity-based 1RM, best bar speed, total volume and reps — as a ranked bar chart + table, switchable by exercise and metric. /api/coach/compare computes every stat server-side from each ACTIVE client's real sessions (consent-gated by CoachLink, only the coach's own athletes). Empty-state guides coaches with no roster yet." },
  { id: "branded-select", area: "Web", status: "shipped", title: "Branded dropdown control", detail: "A single Select component in apps/web/lib/ui.tsx replaces every native <select> across the app (language switcher, RTP, coach assign). Strips the OS chrome (appearance:none), draws a brand chevron, paints the control with @hybrid/core tokens (ink2/line/chalk + lime focus ring) and supports a rounded `pill` variant for the header language switch — so dropdowns stop looking like raw browser widgets and match the dark identity. Note: the open option list on macOS is still OS-rendered (unstylable by the web platform)." },

  { id: "liquid-glass", area: "Web", status: "shipped", title: "Liquid Glass material + command menu + light mode", detail: "A reusable Liquid Glass surface layer in apps/web (globals.css `.liquid-glass` + `.lg-thin/.lg-thick/.lg-vibrant` variants, exposed as the `<Glass>` primitive; `<Card>` is glass BY DEFAULT (with a `glass={false}` opt-out + a `variant`), so EVERY consumer + admin screen renders on glass, Stat/ChartFrame included). Both the consumer app shell AND the admin console carry the ambient field, and the consumer sidebar is collapsible to an icon-rail (persisted). Authentic glass — `@property`-animated tokens (blur/opacity/rim-angle/glow/lift interpolate), backdrop-filter blur+saturate, an inline-SVG visionOS grain, a conic rim light masked to a 1.5px ring, and a layered depth shadow stack — not gradient transparency. A fixed ambient field of slow-drifting accent blobs (lime/blue/violet) sits behind the app shell so the glass has real content to refract. Drives a central ⌘K/Ctrl-K command menu in app-shell.tsx: a floating orb blooms a control-center hub (scrim blurs the app behind) whose tiles mirror the sidebar nav (same NAV_GROUPS + per-item flag gating) and jump screens. LIGHT MODE: a header toggle (lib/use-theme.ts, persisted, sets `<html data-theme>`) flips the brand surface/text tokens — the surface + primary-text constants in lib/ui.tsx resolve through CSS variables, while LINE/ASH/accents stay raw hex because recharts feeds them to SVG presentation attributes (where var() can't resolve); three chart spots that used a themed token were repointed to chart-safe colours. Contrast-audited to WCAG AA+ in both themes via a theme-aware `--accent-text` token (lime on dark, #4c6606 on light). Honours prefers-reduced-motion (and prefers-reduced-transparency → drops blur/grain/field to solid surfaces) and falls back to a solid surface where backdrop-filter is unsupported. WCAG AA is guarded in CI by a pure contrast test in @hybrid/core (contrast.ts + theme/palette.ts THEMES, asserted in theme/palette.test.ts) so a token edit can't silently regress either theme. Nav is sourced from one canonical map in @hybrid/core (nav.ts NAV_ITEMS) shared by the web sidebar + ⌘K hub and the mobile command sheet. Standalone previews: reference/liquid-glass/system.html + before-after.html. KNOWN GAPS: deep inline-styled screens are not yet contrast-tuned for light mode (muted ASH text runs ~2.7:1 on light); theme is applied client-side so a light-mode user sees a brief dark flash on first paint (no SSR cookie yet)." },
  { id: "design-scale", area: "Core", status: "shipped", title: "Shared type & spacing scale (anti-drift)", detail: "A single source of truth for sizing in @hybrid/core (scale.ts): a named type ladder `fs` (nano 10 – micro 11 – caption 12 – body 13 – bodyLg 14 – note 15 – subtitle 16 – title 18 – heading 20 – display 26) and a spacing scale `space` (xxs 4 → huge 40), consumed by BOTH clients. WHY: before this, every fontSize/gap was an inline magic number duplicated across the web (apps/web) and mobile (apps/mobile) trees with no shared definition — exactly how the admin console drifted ~1px smaller than the consumer app (same widths, smaller text → it read miniature). The data showed web and mobile already use the SAME integer ladder (they differed only in WHICH rungs each screen picked), so ONE map serves both — and is the strongest guarantee against re-drift; if a genuine per-platform need appears, scale.ts splits into WEB_FS/MOBILE_FS without touching call sites. WIRING: both lib/ui files re-export `fs`/`space` and point their shared primitives at it (web: Card/Select/Stat/Chip/ChartFrame/tip; mobile: Card/GlassCard/Mono/Kicker/Chip/Button). ADOPTION: all ~21 web + 19 mobile admin section components were migrated off inline numbers onto the tokens — an EXACT-VALUE migration (each fs.<role>/space.<token> equals the literal it replaced), so zero visual change; a handful of bespoke one-off sizes (half-pixel values like 12.5/13.5, and rare hero numbers >26) are intentionally left raw. APP-WIDE: the same exact-value migration was then rolled across the ENTIRE consumer app on both clients (~91 web + ~86 mobile screen/component files — classic + Aurora, app-shell included), so the whole product now sizes off the shared rungs. Still raw by design: responsive ternaries (e.g. `fontSize: isMobile ? 22 : 30`) and half-pixel values — these are conditional/bespoke and frequently use sizes outside the body ladder, so snapping them would change pixels. Guards web+mobile parity (CLAUDE.md): the size vocabulary is now shared, not hand-mirrored." },
  { id: "theme-discipline", area: "Core", status: "shipped", title: "Disciplined themes — Aurora (dark) + Japandi (light)", detail: "Replaced the multi-accent 'AI-slop' look with two disciplined themes, picked from a 10-concept exploration (reference/today-cockpit-design-concepts/). THE DIAGNOSIS: the UI competed FIVE accents at once (lime + blue + violet + amber + red on near-black) — that rainbow, not the lime, read as inconsistent/generated. THE FIX is one accent + a neutral ramp + type/grid hierarchy. TWO THEMES, both in @hybrid/core theme/palette.ts (the single source mirrored by web globals.css [data-theme] + mobile lib/theme.tsx): (1) `dark` = AURORA — a refined neutral charcoal ramp (card #151715, line #242724) with lime as the SINGLE accent and red kept strictly for risk/injury; (2) `light` = JAPANDI — warm oat-paper surfaces (ink #efe9df, card #fcfaf4), clay/sage darkened accentText, calm low contrast. Japandi REPLACES the old cool light theme (the theme set is now exactly these two — labelled 'Aurora' / 'Japandi' in web Preferences + mobile Settings appearance, mobile keeps the System auto-switch). All Japandi text/accent pairs clear WCAG AA on its card (guarded by theme/palette.test.ts). ACCENT DISCIPLINE applied to TODAY + COCKPIT on BOTH clients (apps/{web,mobile}/components/aurora/{today,home,cockpit,today-quick}): every decorative blue/violet/amber chrome (section-header dots/kickers, the AI-coach + upsell accents, the amber streak pill, the violet plan-meta, the check-in heart, the freemium teaser dots) collapses to a single lime accent over neutral ash kickers; the cockpit Section/teaser dropped their per-section `color` prop entirely. DATA COLOURS ARE KEPT (the 'keep data colours' decision): readiness/HPI/risk band colours (the shared semantic.ts role map), the nutrition over-budget red bar, and the reconciled-week sport-vs-strength coding all stay — colour there encodes state, not decoration. Core tests + web/mobile typecheck green (web Prisma-codegen errors are sandbox-only). (LIGHT THEME SINCE EVOLVED: the Japandi palette described here went through Clay & Sage on Oat — see japandi-clay-sage — and is now KYOTO HOUR, washi/pine/vermilion with the picker labelled 'Aurora' / 'Kyoto Hour'; see kyoto-hour-light-theme. The two-theme discipline itself is unchanged.) (LIGHT THEME REMOVED Aug 2026: the app is dark-only now — AURORA is the one theme and the single-accent discipline is unchanged; see light-theme-removed.)" },
  { id: "theme-discipline-rollout", area: "Core", status: "shipped", title: "Single-accent discipline rolled out app-wide", detail: "Rolled the single-accent discipline (see theme-discipline) across the whole CONSUMER + COACH + ANALYZE product, in committed batches. RULE: decorative blue/violet/amber chrome → one lime accent + neutral ash kickers; errors → semantic red; KEEP data/state colours (semantic role map, load/RPE + velocity zones, sport-source, macro P/C/F, chart series + axes + legends, rating stars/medals/PR trophy, status states, bio-age delta / weight-rate / volume guidance / MEV-MAV-MRV landmarks / asymmetry / flags). DONE (web + mobile parity): Today, Cockpit, ReconciledWeek (+ mobile twin), live logger, quick-sport; PLAN/TRAIN (plans, sport, builder, competition, train); RECOVERY/HISTORY (checkins, nutrition, longevity, history, calendar); TODAY-EMBEDDED (ai-coach, coach-rail, feed-preview) + Upgrade; COACH (coach, coach-apply, coaches, coach-invite + banners, org, tactical, talent, coach-groups, coach-programs); SOCIAL (feed, leaderboard, social-ui/social-kit, discover); ACCOUNT/NAV (account-settings, app-shell, mobile settings; pill-nav/more springboard already chalk; login/mfa/announcement kept their role/warning semantics); ANALYZE/CHART (performance, trends, volume, exercises, velocity, video, forceplate, analytics, statistics) — chrome only, chart series kept. ADMIN (operator-only) — REVIEWED and found already disciplined: it is a coherent SINGLE-accent surface where AMBER is the deliberate operator identity (shell wordmark, nav-active, section labels) — not a rainbow. Its blue/violet/amber are almost entirely SEMANTIC LEGENDS, kept: the role map (ADMIN=amber / COACH=violet / CLIENT=lime), the verified-coach blue tick, email/agent status maps (scheduled/sending/sent/failed), and recharts series. Only a few purely-decorative metadata chips (a 'joined {date}', a scheduled-time, an email-trigger label) were neutralised to ash. NOTE: admin keeps amber as its operator accent on purpose (signals operator mode, and amber is warm-harmonious in both Aurora and Japandi); unifying admin onto the brand lime/clay accent would be a separate product decision + a large raw-hex codemod, deliberately not done. Net: everything an athlete, coach, or operator sees is now disciplined — consumer/coach = lime (Aurora) / clay (Japandi) single accent, admin = amber operator accent, all with semantic role/status/chart legends preserved." },
  { id: "japandi-identity", area: "Core", status: "shipped", title: "Japandi identity — clay accent + serif display", detail: "Japandi now matches its approved concept (reference/today-cockpit-design-concepts/04-japandi): a warm CLAY/terracotta accent and a Fraunces SERIF display over a clean sans body — not the muted olive-lime first shipped. ACCENT: the `lime` token (which drives every accent fill) becomes clay #a5573c in the light theme only — web `[data-theme=light]` overrides --color-lime/--lime-text/--accent-text, core THEMES.light.accentText.lime = #a5573c, mobile paletteFor() sets palette.lime = #a5573c. #a5573c is the concept's #b06a4f darkened just enough to clear WCAG AA (4.9 as a fill under light text, 5.0 as text on the cream card); on-accent text flips to light paper (--on-accent / mobile onAccent) so labels read on the clay. Aurora (dark) is unchanged (raw bright lime, dark on-accent). SERIF (web): Fraunces is loaded; a new --font-heading var (Archivo in Aurora, Fraunces in Japandi) is applied to the hero headings on Today + Cockpit (greeting, plan/session titles, AI-coach title, command-center, HPI + Stat numbers, nutrition kcal, reconciled-week values); body/prose stays on the sans --font-display and labels stay mono — the concept's serif-display/sans-body split. AMBIENT FIELD was already retinted warm for Japandi. Contrast guarded by palette.test.ts (658 green). See japandi-serif-mobile for the one remaining gap. (SUPERSEDED Jul 2026 by kyoto-hour-light-theme: the light accent is now PINE #44584c — clay is retired — and the light display face is Shippori Mincho, not Fraunces. The per-theme --font-heading / accent-fill ARCHITECTURE built here still carries the new values.) (REMOVED Aug 2026 with the whole light theme — see light-theme-removed.)" },
  { id: "japandi-serif-mobile", area: "Mobile", status: "shipped", title: "Japandi serif display on mobile (Fraunces)", detail: "Mobile now matches web's Japandi serif character. @expo-google-fonts/fraunces added (installed with --ignore-scripts to skip the blocked @prisma/engines postinstall); Fraunces_500Medium/600SemiBold/700Bold loaded in apps/mobile/app/_layout.tsx useFonts(); F.serifMed/serifSemi/serifBold added to apps/mobile/lib/ui.tsx with a `serifIf(scheme, archivo)` helper (returns Fraunces in light/Japandi, the given Archivo weight in dark/Aurora — the mobile twin of web's --font-heading). Applied to: the shared AHeading primitive (so EVERY screen title goes serif in Japandi), and the hero headings/numbers on Today (greeting, plan/session/AI-coach titles, reconciled-week Metric values, nutrition kcal) + Cockpit (goal, blocks, HPI score, Stat values). Body stays Archivo sans, labels stay JetBrains mono — the concept's serif-display/sans-body split. Verified: mobile typecheck clean + `expo export --platform ios` bundles green (Fraunces module resolves + Metro bundles the weights). On-DEVICE visual still wants an EAS build to confirm rendering (no simulator in this sandbox), same bar as the rest of the mobile Aurora kit. (SUPERSEDED Jul 2026 by kyoto-hour-light-theme: serifIf() now returns the Shippori Mincho cuts in light and @expo-google-fonts/fraunces was replaced by @expo-google-fonts/shippori-mincho — the serifIf/useFonts WIRING described here is unchanged, only the face swapped.) (REMOVED Aug 2026 with the whole light theme: serifIf() and the Shippori Mincho fonts were deleted; headings read the Archivo faces directly. See light-theme-removed.)" },
  { id: "liquid-glass-mobile", area: "Mobile", status: "shipped", title: "Liquid Glass on mobile (expo-blur)", detail: "The web Liquid Glass material ported to React Native (apps/mobile/components/liquid-glass.tsx): a `GlassCard` on an expo-blur BlurView base with a brand tint film, a top rim-highlight, a soft border and a depth shadow (grain is omitted — no perf-free RN noise primitive); a frosted `GlassTabBarBackground` behind the bottom tab bar; and a central `CommandMenu` — a floating glass orb (FAB) that blooms a blurred control-center sheet of nav tiles (the mobile analog of the web ⌘K hub), animated with core Animated springs (no Reanimated, so no extra native babel config). Wired globally in app/(tabs)/_layout.tsx. The `GlassCard` primitive now lives in lib/ui (alongside `Card`), and `Card` renders on GlassCard BY DEFAULT (with a `glass={false}` solid opt-out), so EVERY mobile screen sits on glass — parity with web where `<Card>` is glass by default. The web ambient drifting-blob field is ported too: a `GlassField` (lib/ui) of slow-drifting lime/blue/violet blobs (faked with stacked low-opacity concentric circles — no SVG/gradient dep — and core Animated loops) mounts once behind every `Screen` so the glass has real content to refract, matching the web `.lg-field`. LIGHT/DARK: a mobile theme system (lib/theme.tsx — ThemeProvider + useTheme, palette from the shared @hybrid/core THEMES) follows the OS appearance by default with a System/Light/Dark override in Settings; the shared primitives (lib/ui Screen/Card/Mono/Kicker/Chip/Button), the glass surfaces (GlassCard/tab bar/command sheet) and the status bar all follow it. The command sheet is built from the shared nav map (nav.ts). Shares the @hybrid/core palette so it stays in lockstep with web. Every screen follows the theme: surfaces/text via a per-component palette shadow and accent TEXT wrapped in txt() so it stays WCAG AA on light (parity with web). iOS bundle export verified; on-DEVICE rendering of the native blur + the themed screens still needs an EAS build to verify visually (blocked on the Apple Developer account + Expo token — see mobile-preview)." },

  // ---- Mobile ----
  { id: "mobile-app", area: "Mobile", status: "shipped", title: "Native app (code)", detail: "Expo app: Supabase auth + a funnel-first tab navigator (Today / Train / History / More) on the same core and the same /api backend. The dense screens (Plans, Sport, Velocity, Coach) plus the pushed consumer/coaching routes — Nutrition, Check-in, Calendar, Progress photos (camera/library capture), Onboarding — now live behind the More hub so the gym path stays uncluttered. The Today home (Fitness GPS) leads with a one-tap Start and the Accountability/Future-Self/Twin cards + 'Assigned by your coach' workouts. iOS bundle verified (incl. the expo-image-picker native module); on-device run needs an EAS build (see mobile-preview). BOOTS WITHOUT A KEY: when EXPO_PUBLIC_SUPABASE_ANON_KEY is unset (e.g. a fresh Simulator run), lib/supabase.ts hands createClient a harmless placeholder instead of an empty string so the client can't throw 'supabaseKey is required.' at startup — the app opens logged-out (isSupabaseConfigured() stays false, gating every auth/storage call) and the login screen shows a 'set the key' hint rather than crashing." },
  { id: "train-funnel", area: "Mobile", status: "shipped", title: "Train funnel — start → log → finish → share → analyze", detail: "The deliberate path for the everyday athlete: ease in the gym, depth at home. (1) START is one tap — a hero button on Today and a Train launcher (empty / AI-prescribed / repeat-last) both open a focused live-workout screen. (2) LOG is frictionless: a fresh workout opens with a 5→1→GO get-ready COUNT-IN overlay (the elapsed clock only starts at GO, so the time reflects real training, not setup; skipped when resuming an in-progress draft), then a running elapsed clock, add exercises from the MOVEMENTS map or free-type, big set rows (kg/reps/RPE) with previous-set carry-over, a ✓ to bank a set, and an auto rest timer — no menus, big tap targets. A set row can be SWIPED LEFT to delete one added by accident (built on Animated + PanResponder — only claims horizontal drags so inputs/scroll still work; long-pressing the set number also deletes). A PAUSE/HOLD button next to Finish freezes the elapsed clock and any running rest (shifting both forward on resume so neither jumps); the header shows a PAUSED state. The SCREEN STAYS AWAKE while logging (expo-keep-awake — no dimming mid-set), exercises can be REORDERED in place (↑/↓ on each card, matching the web builder), and every strength exercise shows a 'LAST TIME' reference (the most recent prior session's sets for that lift via the shared lastStrengthByLift helper, computed once per session — not re-sorted on the per-second timer re-render) so progressive overload has a target to beat. CARDIO is its own exercise kind (separate from interval CONDITIONING): logs distance (km) + minutes with a live derived pace (min/km via the shared pacePerKm), while conditioning logs minutes; neither asks for RPE (strength-only); the picker classifies a typed/library move via inferBlockKind (catalog + keyword). A set can be tapped to a DROP SET (lighter no-rest continuation, or '+ drop set') and a strength exercise joined into a SUPERSET with the exercise BELOW it — the '⛓ superset' control sits on the UPPER card so even the FIRST exercise can start a superset (labelled A1/A2/A3… via the shared supersetLabels) — both ride the same SessionBlock fields the web logger writes (StrengthSet.drop / StrengthBlock.group). A 'What's RPE?' sheet shows the shared RPE_SCALE + reps-in-reserve explainer so a beginner can self-rate. (3) FINISH saves the real Session via /api/sessions (or the device, in guest mode — see guest-first-workout) and shows a completion summary (duration, sets, tonnage, today's est-1RM bests). A 🏆 PR celebration fires the moment a lift beats its prior best (see pr-detection). (4) SHARE renders a branded PNG card to the native share sheet (see share-card). (5) ANALYZE: the summary funnels into History → a per-session detail screen (see session-detail) for the at-home deep dive (and the web goes deeper still). All flow copy is localized EN/PL/DE. app/workout.tsx + the rebuilt Train tab; the old form-heavy Log tab is replaced." },
  { id: "guest-first-workout", area: "Mobile", status: "shipped", title: "Guest mode — train before you sign up", detail: "Removes the cold-start wall: a brand-new user lands on a welcome screen and can START THEIR FIRST WORKOUT with no account. Guest sessions are persisted on-device (AsyncStorage, lib/guest.ts) and the full live-workout + summary + share flow works offline. The completion screen then offers 'Save & track progress → create a free account'; on sign-in, flushGuestSessions() pushes every locally-stored workout up to /api/sessions so nothing is lost and it appears in History + on the web. Unauthed routing now goes welcome → (guest workout | login?mode=signup) instead of a hard login gate." },
  { id: "share-card", area: "Mobile", status: "shipped", title: "Shareable workout card (image)", detail: "The post-workout summary and the session-detail screen render a branded HYBRID stat card (title, minutes/sets/tonnage, est-1RM bests with 🏆 PR markers) and capture that exact view to a PNG via react-native-view-shot, then open the native share sheet through expo-sharing — an image made for social. A reusable lib/share.tsx (WorkoutShareCard + shareWorkout) backs both. Degrades gracefully to a formatted text share (RN Share) when image capture or the share sheet is unavailable. Capture/share are JS-driven but the underlying modules are native, so on-device behavior is verified by an EAS build (see mobile-preview). WEB PARITY + a 9:16 STORY card now ship too — see empty-log-share-stats." },
  { id: "empty-log-share-stats", area: "Core", status: "shipped", title: "Empty-log section — live in-session stats + 9:16 story share (web + mobile)", detail: "The 'log a workout from scratch' loop is now a first-class, parity-matched section on BOTH clients with live stats and social share. ENTRY: the center bottom-nav button already opens an EMPTY live session (mobile /workout?source=empty; web app-shell screen='log' → AuroraLogger with no seeded blocks); added an explicit 'Empty log' tile to the mobile More hub (app/(tabs)/more.tsx) to match the web 'More' sheet, which already lists the casual 'Log session' nav item. LIVE STATS: a new pure helper @hybrid/core live-stats.ts — liveSessionStats(blocks, prior) returns running exercises / logged working-sets / tonnage (sessionVolume) / strength + cardio PR counts (newPrsInSession/newCardioPrsInSession), counting a set only once reps/load is typed; pure + 5 unit tests. It drives a live SCOREBOARD strip that appears the moment the first set lands — web components/aurora/logger.tsx and mobile app/workout.tsx render the same exercises/sets/volume cells plus a 🏆 PR badge, so the in-gym screen shows the session adding up in real time (same numbers the finish summary uses). SHARE: web previously had NO workout share at all (parity gap) — apps/web/lib/workout-share.ts now draws a branded 9:16 (1080×1920) Instagram-story PNG on a <canvas> (no new dependency; wordmark + lime glow, minutes/sets/volume, today's est-1RM bests with PR markers) and hands it to the Web Share API (navigator.share with files), degrading to a PNG download then a text/clipboard share; the web finish screen gains a 'Share to story' CTA (glowing on a PR/first-ever). Mobile adds a tall WorkoutStoryCard (9:16) to lib/share.tsx captured from an off-screen render via the existing react-native-view-shot path, with a 'Share to story' button on the summary alongside the original square card. All copy localized EN/PL/DE (live.*, summary.shareStory, emptyLog.*, w.train.logger.live*/share*). Web canvas share verified by typecheck/build; mobile capture is native, verified on an EAS build (see mobile-preview)." },
  { id: "session-detail", area: "Mobile", status: "shipped", title: "Session detail — the 'analyze at home' screen", detail: "The explicit counterpart to the in-gym ease: tap any History row to open app/session/[id].tsx — duration, tonnage and set count up top, a 🏆 PR banner (pr-detection vs prior history), a muscle-focus card (volumeByMuscle bars — what the session actually trained), then a full per-exercise breakdown (every set's kg×reps×RPE×velocity and the block's est-1RM, PR-badged) with a dependency-free e1RM trend sparkline per lift (e1rmSeries over the whole history) AND a pace-trend sparkline per cardio move (paceSeries; faster = taller, delta shown as time saved/lost per km), plus the branded share card so an old win can be re-shared. History rows now show a PR chip and a 'tap for the full breakdown' hint. Conditioning blocks read back their full interval via the shared core conditioningSummary/blockSummary helpers (format – rounds×work/rest s – minutes – RPE) — one formatter the web History + mobile History/detail all use so they can't drift. Localized EN/PL/DE." },
  { id: "exercise-picker", area: "Mobile", status: "shipped", title: "Fast exercise picker (search + your lifts)", detail: "Adding an exercise mid-workout is now one tap: the picker opens with a search box (autofocused) and a 'Your lifts' row of the athlete's own most-recently-used movements (exerciseHistory, incl. custom names they've typed before), above the movement Library. Typing filters both lists live and offers '+ \"<what you typed>\"' to add a brand-new exercise, preserving strength/conditioning kind from history. Localized EN/PL/DE." },
  { id: "mobile-exercise-library", area: "Mobile", status: "shipped", title: "Mobile picker consumes the admin exercise library (+ alias dedup + muscle-group grouping)", detail: "PARITY with web's cms-exercise-library — the admin-authored custom catalog is now pickable on the phone. A mobile useExercises() twin (lib/queries.ts, TanStack Query + Bearer fetch of /api/exercises via lib/api.ts fetchCustomExercises) folds the library over the built-in MOVEMENTS with mergeMovements and returns the SAME shape web does: movements (resolution map, alias keys kept), catalog (pickable names from catalogNames), aliases (aliasNames — superseded lifts hidden), and categoryByName (categoriesByName). Both mobile pickers — the live logger (app/workout.tsx) and the AURORA routine builder (components/aurora/builder.tsx) — now call exercisesByCategory(MOVEMENTS, catalog, categoryByName), filter aliased names, and render muscle-group section headers, so a custom 'Barbell Bench Press' (aliasing built-in 'Bench Press') shows once, under its 'Chest' heading, identically on web + mobile. Degrades to the built-ins alone when signed-out / the API is unavailable. Verified: mobile typecheck + iOS bundle export; live rows need the deployed API + a seeded table (reference/sql-exercise-seed.sql)." },
  { id: "rest-haptics", area: "Mobile", status: "shipped", title: "Rest COUNTDOWN + set haptics + saved rest", detail: "In-gym ergonomics on the live workout: banking a set (tap ✓) fires a light haptic (expo-haptics) and starts a rest COUNTDOWN — it targets a default 90s (or a tapped 60/90/120/180s preset) and ticks DOWN to zero showing the time LEFT, then flips to a lime 'Rest done – +mm:ss' over-rest readout with a success-buzz the instant the target is hit — an eyes-off cue so you can rest heads-down between sets (tapping the active preset clears the target back to a plain count-up stopwatch). A prominent '■ Stop' button ends the rest manually at any time (e.g. when you're choosing the next exercise). BACKGROUND ALERT: a local notification (expo-notifications) is scheduled for when the countdown ends and fires with sound even if the app is pocketed / the phone is locked (permission requested best-effort; kept in sync as the rest starts/changes/stops, cancelled on Stop) — so the cue lands whether the phone's in your hand or not. SAVED AS DATA: the rest taken before each set (the live gap between banking the previous set and this one) is recorded on StrengthSet.rest (seconds) and surfaced per-set in the session-detail breakdown — an optional field, so web-editor / imported sessions are unaffected. NO-REST-AWARE: banking a set that flows straight into a DROP SET or the next exercise of a SUPERSET does NOT start the rest clock (you keep moving) — the rest only kicks in after the last drop / the last superset exercise, matching how the structures are actually trained. A one-time 'how logging works' coach tip (what ✓ does, the rest countdown + presets, how to superset) shows on the live screen until the athlete banks their first-ever set or dismisses it (persisted in AsyncStorage). Haptics no-op gracefully where unsupported; the device feedback itself is verified by an EAS build (see mobile-preview)." },
  { id: "weekly-recap-ui", area: "Mobile", status: "shipped", title: "'Your week' recap + share (web + mobile)", detail: "The weekly-recap engine surfaced on both Today screens: a 'Your week' card (sessions, tonnage, sets, active days, top muscle, PRs, and the delta vs last week). On mobile it's a branded card captured to a PNG via the shared share infra — 'Share your week' to social — with a nudge when the week's still empty. On web it's a span-2 panel on the consumer home. Mobile copy localized EN/PL/DE." },
  { id: "coach-week-view", area: "Web", status: "shipped", title: "Coach sees the athlete's week", detail: "The same weeklyRecap an athlete sees on their own Today now headlines the coach's client-detail view (web Coach screen + mobile Coach tab): sessions, tonnage, sets, active days, top muscle, the week's PRs, and the delta vs last week — computed from the client's real sessions the coach already reads (CoachLink-gated). One source of truth for both sides of the relationship, so a check-in starts from the same numbers." },
  { id: "workout-resume", area: "Mobile", status: "shipped", title: "Resume an interrupted workout", detail: "The live workout auto-saves an in-progress draft to the device (lib/draft.ts, debounced) as you log, so a crash, a dead battery or an accidental back-swipe never costs the session. Today's hero and the Train launcher detect the draft and offer Resume (with an exercise count + 'in progress'); the launcher also exposes Start-fresh (drops the draft) and Discard. The draft is cleared on finish. Plain AsyncStorage — no backend needed." },
  { id: "workout-minimize", area: "Core", status: "shipped", title: "Minimize a live workout into the nav accessory", detail: "The live logger had two ways out and BOTH ended the session: 'Cancel' — which actually just called router.back() and discarded nothing, a mislabel whose string was even borrowed from the coach namespace (w.teams.coach.cancel) — and 'Finish'. Nothing said 'put this away and carry on', even though the accessory strip above the nav pill (see nav-native-tabs) was already built and already watching the persisted draft. The header now reads MINIMIZE / clock / ⋯ + Finish on BOTH clients. Minimize is a chevron-down glyph (already in the kit, so no new icon was needed) pointing at where the session goes: mobile app/workout.tsx leaves the route and the session reappears in NativeTabs.BottomAccessory; web components/aurora/logger.tsx calls a new onMinimize prop that app-shell routes to Today, where pill-nav's SessionAccessory picks the localStorage draft up. Nothing is lost either way — the draft is persisted on every change. DISCARD, the one irreversible exit, is deliberately NOT a peer: it is a row inside the ⋯ menu, behind a confirm (mobile useConfirm's promise-based sheet; web a matching Sheet with Cancel first and the destructive action second, so the button under a resting thumb is never the irreversible one). This was the chosen shape out of four studied: two adjacent unlabelled glyphs, one reversible and one not, in the corner a thumb rests on is the arrangement that loses sessions — and ✕ was rejected outright for discard because on iOS it means 'close this' (dismiss, nothing lost), which is exactly what the chevron beside it would do; ✕ already means close-this-card in three places in the same logger. Both loggers latch a `discarded` ref that gates the debounced draft save, so a write scheduled in the 500ms before a discard cannot land AFTER clearDraft() and resurrect the session the athlete just confirmed away. Localized EN/PL/DE (workout.minimize / moreOptions / discardSession / discardTitle / discardBody, reusing train.discard for the confirm button). Verified here: both typechecks, the web build, the iOS bundle export, and the 2025 core tests — including the web↔mobile copy-parity guard and the vocabulary rule that calls the object a session. FOLLOW-UP (Aug 2026): the accessory used to hover above the nav pill on EVERY screen as an empty glass bar, whether or not a workout was minimized. Rendering null inside it was not enough — UIKit builds the accessory (capsule, height, the lot) from the mere presence of the <NativeTabs.BottomAccessory> SLOT, so an empty child is still a bar. The tab layout now subscribes to the same draft store (useSessionDraft) and mounts the slot only while a draft exists, which also moves the route-change re-read up to the layout — the accessory is unmounted exactly when a new draft would need to be noticed. The CLOCK moved with it, into the store: the accessory renders twice (regular + inline placement) and each copy used to run its own interval, so one figure was driven by two timers a second apart, each waking every subscriber. The draft now owns the tick — started when a draft appears, cleared when one goes — and the layout subscribes to the draft ALONE rather than to the ticking snapshot, so a running session no longer rebuilds the tab bar's children once a second. apps/mobile/lib/nav-accessory.test.ts ratchets all of it as source text (the render project aliases expo-router wholesale, native tabs included, so the layout cannot be mounted there)." },
  { id: "workout-minimize-lossless", area: "Core", status: "planned", title: "Minimize restores the POSITION, not just the session", detail: "What shipped in workout-minimize is content-lossless but not position-lossless: the draft restores the exercises and the clock, not the focused set, the scroll offset or an open keypad — because the live state lives INSIDE the logger screen. That is also why the mobile workout route still sets gestureEnabled:false, whose comment records the reason exactly: swiping out mid-set loses the live session's context. Hoisting the running session into a module-level store read through useSyncExternalStore — the pattern components/aurora/session-accessory.tsx already uses for its own draft and clock, for the same reason: two surfaces must read one truth without drifting — would make the route a VIEW of the session rather than its owner, and restore would return the athlete to set 2 of 5 with the field still focused. It also unblocks three things that are dishonest without it: drag-down-to-minimize (the back-swipe can only be handed back once leaving is provably free), a Log-set control in the accessory strip itself so a straight-set block can be finished without restoring, and rest-driven auto-minimize. Refactor-shaped, with no visible feature of its own, and it touches the most load-bearing screen in the app." },
  { id: "offline-sync", area: "Mobile", status: "shipped", title: "Never-lose-a-workout offline sync", detail: "Finishing a workout while offline (or on a server hiccup) no longer fails: the session is stashed on the device using the same store guests use, the summary shows a 'saved on this device — will sync' note, and the SessionProvider flushes the backlog to /api/sessions on the next sign-in AND every time the app returns to foreground (likely back online) via AppState. So the gym's dead signal can't eat a PR." },
  { id: "mobile-ci", area: "Mobile", status: "shipped", title: "Mobile CI — typecheck + iOS bundle export", detail: "CI (.github/workflows/ci.yml) runs `pnpm typecheck` across core/web/mobile AND now exports the iOS bundle (`pnpm --filter @hybrid/mobile export:ios` → `expo export --platform ios`) on every PR/push to main. The export proves the Expo app actually bundles — Metro resolution, native-module + asset registration — which a type-only check can't see, and needs no simulator (the same headless check called out in CLAUDE.md). This is the build gate the web-only Vercel deploy doesn't cover." },
  { id: "ios-simulator-guide", area: "Web", status: "shipped", title: "iOS Simulator runbook (admin)", detail: "Admin → Governance → Guidance → the 'simulator' guide (switcher): a beginner-friendly, explain-it-like-I'm-a-kid step-by-step guide for opening the managed-Expo mobile app on a pretend iPhone (Apple's iOS Simulator). CONSOLIDATED: this was a standalone 'iOS simulator' Governance tab; it's now documentation-as-DATA in @hybrid/core (guidance.ts SIMULATOR_GUIDE) rendered by the shared Guidance screen (apps/web/components/admin/guidance.tsx) alongside the deploy + accounts guides — one operator-runbook hub, single source of truth, mobile-admin-ready. The GuideBlock model gained a copy-able `cmd` block (the command boxes keep their Copy button) and a `matrix` cheat-sheet block to carry this content faithfully. Opens with a plain-words glossary (Mac, Terminal, 'type a command', Xcode, Simulator, the app code) so no jargon is unexplained, then ONE continuous numbered walkthrough (steps 1–8, each box copy-pasteable with a Copy button) that goes from nothing to the app running via the quick Expo Go path — get Xcode from the App Store, download an iPhone runtime in Xcode → Settings → Components, install node + pnpm, `git clone` + `cd` + `pnpm install` (each line explained in kid terms), install Watchman, `pnpm --filter @hybrid/mobile dev`, press `i` to pop up the pretend phone, then tap around — ending in a friendly ✓ Done note (repeat only steps 6–7 thereafter; an admin supplies EXPO_PUBLIC_SUPABASE_ANON_KEY to enable sign-in). IMPORTANT: Expo Go now throws a red 'Native module is null, cannot access legacy storage' (AsyncStorage) screen for this app because it uses native modules Expo Go can't load, so the native dev build (steps 9–10: CocoaPods + `npx expo run:ios`) is flagged as the RECOMMENDED / most-reliable path — it fixes that red screen and is what makes login/camera/save-photo actually work. A 'cloud robot' EAS build (`eas build --profile preview --platform ios`) is the third option when local building is too slow. Plus a 'which one do I pick' cheat sheet and a plain-language 'if something looks wrong' section whose top entries are the real snags from a live walkthrough: the AsyncStorage red screen (→ run the dev build), a 'Verifying iOS … simruntime' that looks frozen for 5–20 min (one-time, be patient), harmless CHHapticPattern / 'CA Event app launch measurement' log noise on the Simulator (safe to ignore), a `git pull` blocked by local changes (→ `git checkout -- <file>` then pull), and running `pnpm install` from the wrong folder (must be inside `hybrid/`). Also has an 'After we change the app — get the latest & reload' card: leave Expo running, `git pull` in a new tab, reload with Cmd+R / `r` (Fast Refresh often auto-updates), and when a full restart vs reload is needed (new deps / native code) — plus that website-only changes don't touch the Simulator app, and how to check your branch (`git branch --show-current` / `git status`). Troubleshooting also covers the zsh '#'-comment trap (a note pasted after a command runs as args → ERR_PNPM_ADDING_TO_ROOT) and the reload-key mixup (Cmd+T = new Terminal tab, NOT reload; use Cmd+R in the Simulator or `r` in Expo). Reference docs only; the sandbox can't boot a Simulator (no macOS, Supabase host blocked) so these run on the user's Mac." },
  { id: "mobile-store-assets", area: "Mobile", status: "shipped", title: "App Store submission assets", detail: "The standalone HYBRID app is now store-ready (no Claude branding anywhere — it ships as `app.hybrid.mobile`). Branded icon set generated on-brand (lime H on ink #0c0d0c) at apps/mobile/assets/ — icon.png (1024² flattened, no alpha per Apple's rule), Android adaptiveIcon foreground, expo-splash-screen image, and a web favicon — all wired into app.json. App version bumped 0.0.0 → 1.0.0 with ios.buildNumber 1. The remaining gap to a live submission is purely credentials (see mobile-preview), not the app." },
  { id: "mobile-preview", area: "Mobile", status: "shipped", title: "On-device build (internal distribution)", detail: "ACHIEVED: the app now builds via EAS and installs on a REAL iPhone through an internal-distribution build — `npx eas build --platform ios --profile device` (apps/mobile/eas.json `device` profile = internal, simulator:false), installed over the build's QR / expo.dev → Builds. Apple is authenticated with an App Store Connect API KEY that has the ADMIN role (export EXPO_ASC_API_KEY_PATH / EXPO_ASC_KEY_ID / EXPO_ASC_ISSUER_ID — Admin is required, not App Manager, so EAS can manage certs/profiles, register the device, and sync capabilities) because the Apple ID's only second factor is a hardware security key, which EAS/Fastlane's interactive login can't complete; the public Supabase anon key is defaulted in the app so it signs in without extra EAS env. The Apple Developer + Expo accounts now exist (Apple team T4WC9M8Y3S). The full runbook + the stale-checkout and security-key traps are documented in the admin Guidance deploy guide (sandbox-build section). REMAINING for a public launch: a TestFlight / App Store submission (`eas build --profile production` + `eas submit` + Apple review) — tracked in the deploy guide's status section. BUILD METHOD (chosen): NO-CLONE CLOUD — the user wants to keep the sud-italia experience (no local clone, no Mac), so the documented primary path is a cloud EAS build triggered from the browser: .github/workflows/mobile-release.yml runs `eas build --platform ios --profile production --auto-submit` on an ubuntu runner (the compile + signing happen on Expo's CLOUD builders; the runner just kicks it off and waits), invoked via GitHub → Actions → Run workflow or a `mobile-v*` tag. Needs five repo secrets: EXPO_TOKEN, EXPO_PUBLIC_SUPABASE_ANON_KEY, APPLE_ASC_API_KEY_P8 (the raw .p8 file contents pasted as-is — written to apps/mobile/asc-key.p8 at runtime via printf; *.p8 is git-ignored. Earlier base64 variant was dropped because pasting the raw PEM into a base64 secret failed `base64 --decode` with 'invalid input'), APPLE_ASC_KEY_ID, APPLE_ASC_ISSUER_ID (injected into eas.json submit.production.ios at runtime via jq so the IDs stay out of git). ONE-TIME CREDENTIAL BOOTSTRAP — NOW DONE (2026-06-26): a `--non-interactive` CI build REUSES iOS signing credentials but will NOT create them the first time (fails 'Distribution Certificate is not validated for non-interactive builds / Credentials are not set up' — eas-cli #3202). The bootstrap was completed live by running interactively once on the user's Mac (`npx eas build --profile production --platform ios` with EXPO_ASC_API_KEY_PATH/KEY_ID/ISSUER_ID + EXPO_APPLE_TEAM_ID=T4WC9M8Y3S exported so Apple auth goes via the ASC key, not the hardware-key-2FA-blocked interactive login). Result stored on EAS: Apple Distribution Certificate (serial 25DD967C9657247A2AF99EE677FA33A, exp 2027-06-16) + App Store Provisioning Profile (portal id G6SM2S47CK, active) for com.hybriddomain.xyz. So from now on the no-clone GitHub Actions button reuses these hands-free. KEY ID is ZRWGCS2PUS (the .p8 file is AuthKey_ZRWGCS2PUS.p8; issuer 654187d8-9306-4753-97f6-c33797648364) — NOTE the RQTCHVF25S id in older notes was a DIFFERENT/old key, and the GitHub secret APPLE_ASC_KEY_ID + APPLE_ASC_API_KEY_P8 must be updated to the ZRWGCS2PUS key or the automated build 401s. REMAINING blocker to an actual TestFlight binary: the account's monthly FREE EAS Build quota is exhausted (resets ~2026-07-01) — so either wait for the reset then click the GitHub button, upgrade the EAS plan, or do a fully-local build. LOCAL BUILD STATUS (tried live on the user's M1 Max, Xcode 26.5 / CocoaPods 1.16.2 / Fastlane installed): `eas build --local` now runs all the way through pnpm install + expo-doctor + pod-equivalent and into code-signing, but fails at the PREPARE_CREDENTIALS phase with 'Distribution certificate with fingerprint … hasn't been imported successfully' — a known flaky bug in eas-cli-local-build-plugin's throwaway-keychain import on recent macOS, NOT a project/credential problem (creds are valid; the cloud build will use the same ones fine). So local-via-eas is a rabbit hole; the reliable local alternative is `pnpm prebuild:ios` then Xcode Archive → Distribute → TestFlight (Xcode manages the keychain natively). Net: the clean path to the first TestFlight binary is the no-clone cloud GitHub button once the free quota resets (~Jul 1), or an EAS plan upgrade to build immediately. SHIPPED (2026-06-26): a real TestFlight build IS now live — uploaded via local Xcode Archive → Distribute → TestFlight (1.0.0, build 2, com.hybriddomain.xyz), installed and running on device. PIPELINE PIVOT (user requirement: NO Expo build service / 'nothing to do with dev expo'; and they ship sud-italia via GitHub, not Codemagic-the-service): .github/workflows/mobile-release.yml builds on a GitHub macOS runner using codemagic-cli-tools (open-source Apple-signing helper, NOT codemagic.io) + `expo prebuild` as local code-gen only (no Expo account/token). Signing is AUTOMATIC — `app-store-connect fetch-signing-files --create --certificate-key` creates/reuses the Apple Distribution cert + App Store profile from the ASC API key, so NO Keychain export and NO .p12 (the earlier Fastlane/.p12 approach was dropped because it forced manual cert export). Sets an always-increasing CFBundleVersion via agvtool — whole SECONDS since 2024-01-01 UTC (~78M; monotonic with the clock, always above the last upload, no API call or stored state, so a re-run/retry can't reuse a build number), then uploads via `app-store-connect publish --testflight`. (The earlier `get-latest-testflight-build-number` query was returning empty → `$((0+1))` → build '1' → a repeated 409 'bundle version must be higher than 2'; the self-contained timestamp removes that dependency. Matches apps/mobile/app.config.js, which covers the local Xcode-archive path the same way.) Free + UNLIMITED (public repo, no EAS quota). Secrets: APPLE_ASC_API_KEY_P8 / APPLE_ASC_KEY_ID (ZRWGCS2PUS) / APPLE_ASC_ISSUER_ID / EXPO_PUBLIC_SUPABASE_ANON_KEY + APPLE_CERT_PRIVATE_KEY (a one-time `openssl genrsa 2048` key — no Keychain, no .p12). EXPO_TOKEN unused. CAVEAT: first run may hit Apple's 2-Distribution-cert cap (EAS + Xcode certs already exist) → revoke the old EAS 'iPhone Distribution' cert at developer.apple.com once, then re-run. The fundamental truth conveyed to the user: ANY CI iOS signing needs the signing key available to the runner once (Apple's design) — sud-italia had this set up once and forgotten; this is the lightest form of it. Also: the OTA/eas-update config still points at Expo's u.expo.dev servers — if the user wants ZERO Expo at runtime too, drop expo-updates and rely on the (free, unlimited) CI build for every change. Equivalent no-clone alternative noted in the runbook: the EAS GitHub integration (connect repo at expo.dev, click Build on the dashboard). TRADE-OFF: a cloud build consumes the EAS Build free-tier quota (that's the price of no Mac) — but it's only run for real native builds/releases; JS changes ship free via OTA (eas-update), and the monthly quota resets. OPTIONAL local/Xcode path (free + unlimited, but needs a clone + a Mac) stays documented as a secondary option: CNG → `pnpm prebuild:ios` → `pnpm run:ios` or Xcode Archive, or `pnpm build:local` for a signed .ipa. Convenience scripts (prebuild:ios / run:ios / build:local) + EXPO_PUBLIC_SUPABASE_ANON_KEY in .env.example support it." },
  { id: "eas-update", area: "Mobile", status: "shipped", title: "OTA updates (EAS Update) — ship JS without a new build", detail: "Wires up over-the-air updates so the common case (JS/TS changes: screens, logic, styling) ships to an already-installed build for FREE instead of burning an EAS Build credit — the fix for the 'ran out of free builds' wall, since a TestFlight build and a dev build consume the SAME cloud-build quota and only a real build can carry NATIVE changes. Done in code: expo-updates (~56.0.19) added; app.json gains updates.url = https://u.expo.dev/<projectId> with runtimeVersion.policy='fingerprint' (the fingerprint auto-invalidates OTA when native deps change, so a JS update is never delivered to an incompatible binary — safer than 'appVersion' given the native modules react-native-iap / @expo/ui / expo-notifications); eas.json build profiles each carry a `channel` (development/preview/production; the `device` on-device test build and `production`/TestFlight both track the `production` channel). Mobile typecheck + iOS bundle export both green. OPERATE: after a JS change, `cd apps/mobile && pnpm update --message \"…\"` (= `eas update --branch production`) pushes the OTA; the installed app picks it up on next launch. You still do a full `eas build` only when native code/deps change. Web parity: web already ships every change instantly via the Vercel auto-deploy on main, so OTA is the mobile equivalent — no web work needed. NOTE: publishing an update requires Expo auth (interactive `eas login`, or an EXPO_TOKEN robot secret for CI) — the config is committed and inert until then; a one-time CI workflow to auto-publish OTA on push to main is an easy follow-up once EXPO_TOKEN is set as a repo secret. THE FINGERPRINT WAS NEVER STABLE, so none of the above could ever have worked (Aug 2026, found while answering whether a JS-only crash fix could ship without a TestFlight build). runtimeVersion.policy='fingerprint' hashes the EVALUATED Expo config, and apps/mobile/app.config.js derived ios.buildNumber and android.versionCode from Date.now() — so the project had a different runtime version every second: `328037069e…` and `be4d948001…` from the same commit two seconds apart. A binary is stamped with the fingerprint computed when it was BUILT and accepts only updates published under that same runtime version, so an update published from this config carried a fingerprint no installed build could ever have had. It would publish, report success, and reach no phone. HOW MUCH THIS ACTUALLY COST IS UNKNOWN and the honest answer is probably nothing yet: publishing needs the Expo auth this entry already records as never set up, so nobody had likely reached the failure. It was a trap armed to spring the first time anybody relied on OTA — and one that fails by reporting success, which is the worst way for a release path to fail. FIXED by taking the clock out of the config entirely: app.config.js no longer injects a build number unless APP_BUILD_NUMBER is set explicitly (a fixed integer is deterministic, so it is safe to hash), and the increasing CFBundleVersion comes from where it was ALREADY coming from on the only pipeline that ships — the release workflow's agvtool step, which runs after prebuild and was doing this independently the whole time. The config's copy was redundant on that path and did nothing but poison the fingerprint; the 409 protection is untouched. Verified: three consecutive evaluations now give `52c100eaa5…`, and main vs the fix branch hash identically with the build number pinned, which is how the JS-only crash change was confirmed to carry no native delta. GUARDED by apps/mobile/lib/build-number.test.ts, which EVALUATES app.config.js twice with the system clock moved a day and compares, and separately fails on any Date.now/new Date/Date.UTC/hrtime in the file — the mechanism, not just a value that happened to move. Both cases were verified to FAIL against the old config before the fix landed. WHAT IS STILL NEEDED before an OTA can actually be delivered: (1) one TestFlight build from this config, to put a binary in the field carrying the stable runtime version — updates cannot reach any build made before it; (2) the Expo auth above. Android is unaffected today (no Play pipeline); when one is added it must stamp versionCode in the pipeline, not in the config, or the clock goes straight back into the fingerprint." },
  { id: "push-notifications", area: "Mobile", status: "blocked", title: "Push notifications \u2014 the retention bell (three, and only three)", detail: "THE RETENTION LOOP GETS ITS BELL. The in-app bell (notifications-read-live-feel) is a PROJECTION: it can only tell you something while you are already holding the phone with HYBRID open, so everything it surfaces is something you came back for on your own. That makes it a good list and a bad retention loop \u2014 and it left the one prompt that has to TRAVEL to find you, the morning readiness read at 07:00, with no way to. NOW: three notifications ship, and packages/core/src/push.ts is the whole vocabulary \u2014 (1) the morning readiness nudge, (2) your coach assigned you a session or a program, (3) somebody asks you to co-sign a lift they claim. Deliberately three. Streaks, weekly recaps, product news and 'you haven't trained in 3 days' are each a message about US wanting attention rather than about something that happened to YOU, and mixing those in with the three above is the fastest way to teach an athlete to swipe the whole channel away. Nothing else ships until these three have earned it, and a core test pins PUSH_KINDS at exactly three so a fourth is a decision rather than a diff. STRAIGHT TO APPLE, no Expo push service and no EAS project (apps/web/lib/apns.ts): a provider-token JWT (ES256 over an App Store Connect .p8) and an HTTP/2 POST is the entire protocol, so the alternative would put a third party between this app and every athlete's lock screen, need push credentials uploaded to Expo, and hand token ownership to the account the whole TestFlight pipeline exists to avoid needing. The one subtlety that silently breaks push is handled: a device token is only valid on the host that ISSUED it (a TestFlight build gets a production token, a development-signed build a sandbox one, nothing in the token says which, and the wrong host answers 400 BadDeviceToken \u2014 indistinguishable from a dead token), so a send with no recorded environment tries production, retries sandbox once, and writes the answer back to the device row. THE BUILD PLUGIN THAT STRIPPED THE ENTITLEMENT IS GONE. app.json used to end its plugin list with a config plugin that DELETED aps-environment, because an App ID without the Push Notifications capability produces a profile without it and the archive fails. app.json now declares expo-notifications with mode=production, and the release workflow ENFORCES the capability on the App ID the same way it already did for HealthKit (generalized into one ensure_capability helper) and deletes App Store profiles predating either entitlement \u2014 so a fresh profile is minted with push in it, in seconds, with no portal visit. THE DEVICE ROW (PushDevice, prisma/schema.prisma + reference/sql-push-device.sql) is the other half of NotificationState: that one records what you have SEEN, this one records where something can be SENT. It carries more than a token, each field for a reason \u2014 the IANA timezone (the nudge aims at the ATHLETE's 07:00; 07:00 UTC is 02:00 in Los Angeles, and one notification at two in the morning spends the permission for good), the locale (iOS renders a push from what the SERVER sent, so the server picks the language \u2014 all three, EN/PL/DE), a MIRROR of the three account switches (the sender needs them in a WHERE clause and cannot afford an auth-metadata round trip per recipient; user_metadata stays the source of truth and every launch re-mirrors), the APNs environment, and the nudge's own bookkeeping. Registration is POST/DELETE /api/notifications/devices, beside the read-state route, and the mobile client re-registers on EVERY launch and on every token rotation \u2014 'register once at the grant' is exactly how a push channel goes dead with nobody noticing, since a token changes on a restore, a reinstall and at Apple's discretion. THE NUDGE'S CLOCK is pure and tested (core nudgeDecision, 8 tests): a three-hour window from 07:00 local so a missed cron run doesn't cost the day, one nudge per LOCAL day enforced by a stamp rather than by the hour, never for a day already checked in, nothing at all for an account with no training or check-in history (a readiness prompt on day zero is the worst possible first push), and it GIVES UP after seven unanswered mornings \u2014 a daily prompt to somebody who has stopped answering is how an app gets its notifications switched off wholesale, and once off the channel is gone for the athletes who would have answered. One check-in resets the count. Delivery is an hourly job (/api/cron/push, CRON_SECRET like the agent jobs) because there is no single server hour at which it is 07:00 anywhere. It is scheduled from GITHUB ACTIONS (.github/workflows/push-nudge.yml), not from vercel.json, and that was a production incident rather than a preference: Vercel validates cron schedules at DEPLOY time against the plan, the Hobby plan caps a cron at once per day, and the hourly entry this feature shipped with did not merely fail to fire \u2014 it failed every deployment instantly, before any build ran. Vercel was green through PR #458 and red on #459 (which added it), #460 and #461, all with the same error link. Actions schedules are free and unlimited on a public repo, the same reason the TestFlight pipeline lives there. Over-firing is safe: the endpoint is idempotent per device per LOCAL day (it stamps lastNudgeAt and skips a device whose local day already got one), so a delayed, doubled or hand-fired run cannot double-send. If the account ever moves to a plan allowing sub-daily crons, the workflow can be deleted and the one entry put back. EVERY NOTIFICATION DEEP-LINKS TO THE SURFACE IT NAMES \u2014 the nudge to /checkin mid-question, an assignment to /calendar where it is dated, a co-sign request to /feed where CosignInbox renders it with its buttons \u2014 and the client validates the route against core's closed list before navigating, because a payload is network input and router.push on an unvalidated string is somebody else's navigation. THE PERMISSION IS ASKED FOR ONCE, AND NOT AT LAUNCH: a prompt on first open, before the app has shown what it would notify you about, spends the single iOS ask for nothing. It is asked in Settings, and once automatically the moment a check-in is SAVED \u2014 the athlete has just done the exact thing the morning nudge exists to bring them back for, so 'shall I remind you tomorrow?' is a question they can answer. Settings gained a master switch (permission + this phone's registration) above the three per-kind switches, and it can say BLOCKED: iOS never shows the prompt twice, so a switch that silently flicks back would be a lie \u2014 the row sends you to the Settings app instead. TWO SWITCHES WERE RETIRED WITH THIS, not left in place: the account's notification rows promised a weekly recap and product news, neither of which the app has ever been able to send (there was no delivery channel at all). A switch that governs nothing is the same lie as a badge that cannot reach zero, so ACCOUNT_NOTIF_ROWS is now exactly the three notifications that exist, and a core test asserts the three PUSH_PREF_KEYs and the three rows are the same set in both directions. NO ICON BADGE, deliberately: a badge the app never clears is the exact failure the bell's read state was built to end; wiring it to the unread count is a real feature and not one of the three. Android is out of scope (FCM is a separate key and sender) and every surface gates on pushSupported(). WHAT THE COACH NOTIFICATION IS, EXACTLY: an ASSIGNMENT (one session, or one per athlete for a whole program \u2014 never one per materialized session, which for a twelve-week program would be dozens of pushes and the end of the channel). A coach's REPLY to a check-in is deliberately NOT sent, and the reason is worth writing down: the athlete has nowhere to read it. Checkin.coachReply is written by the coach's own screen and rendered only there \u2014 no athlete-facing surface shows it on either client \u2014 so a notification about it would deep-link to nothing, breaking the one rule every notification here keeps. Tracked as coach-reply-athlete-view; the push is a two-line addition the day that screen exists. ONE MORE THING THIS FIXED ON THE WAY: setNotificationHandler is GLOBAL and last-writer-wins, and there were two \u2014 the rest timer's at the top of the workout screen and none for remote push. Which rule was live depended on module load order, i.e. on which screen the athlete opened first. One handler now, branching on remote-vs-local (a push belongs in Notification Centre; a rest cue does not). Tests: 25 new (17 core on the three kinds, the copy in three languages, payload coercion and the nudge clock; 8 web on the ES256 provider token \u2014 the 64-byte r||s signature JOSE requires, where node's DER default answers 403 and looks exactly like a wrong key \u2014 plus the timezone/DST clock and the day label). Core, web and mobile suites green; all three typechecks clean.", blockedBy: "Two things, both human, neither of them code: (1) CREATE THE APNs KEY \u2014 App Store Connect \u2192 Users and Access \u2192 Integrations \u2192 Keys \u2192 Apple Push Notifications service (APNs), download the .p8 ONCE, then set APNS_KEY_P8, APNS_KEY_ID and APNS_TEAM_ID in the Vercel env (see .env.example; APNS_BUNDLE_ID defaults to com.hybriddomain.xyz and APNS_ENVIRONMENT should stay unset so the sender resolves the host itself). Until they are set every send is a recorded no-op \u2014 the app registers device tokens and nothing arrives, which the register route reports as configured:false rather than hiding. Also set CRON_SECRET if it isn't already, or the hourly nudge job returns 503. (2) RUN reference/sql-push-device.sql in the Supabase SQL Editor (the sandbox cannot reach the DB), then confirm the round trip on the next TestFlight build: allow notifications, check a device row appears with a timezone and a locale, and watch the three arrive \u2014 a co-sign request is the easiest to trigger on demand, the morning nudge needs a 07:00. The aps-environment entitlement itself is no longer blocked on anything: the release workflow enables the Push Notifications capability from the API key and deletes profiles that predate it." },
  { id: "coach-reply-athlete-view", area: "Mobile", status: "planned", title: "The coach's reply to a check-in, where the ATHLETE can read it", detail: "A coach can reply to a check-in (Checkin.coachReply + repliedAt, written from the coach's roster screen) and the athlete has NOWHERE TO READ IT. The column has existed since the coach layer shipped; the reply renders only on the coach's own view of that athlete's check-ins, so the reply is written into a database and never delivered. Found while wiring push-notifications: the audit asked for 'a coach's message or assignment' as one of the three notifications, and the message half could not ship, because every notification must deep-link to the surface it names and there is no surface. NEEDED: the reply on the athlete's own check-in history (the recovery surface that already lists their days), unread-marked so it reaches the bell's list like any other event \\u2014 then the coach notification kind covers replies as well as assignments, which is two lines in the checkins/[id] route." },

  // ---- Auth ----
  { id: "account-reset", area: "Backend", status: "shipped", title: "Reset account (wipe my data)", detail: "A Settings → Danger zone control that erases ALL of the signed-in user's data while keeping their login: POST /api/account/reset (confirm token required) now delegates to the SHARED wipeUserData() helper (apps/web/lib/account-wipe.ts) so it covers EVERY user-owned table — including the private-tab tables (BodyMetric, JournalEntry, HiddenHighlight, HighlightOrder), PlanDayOverride/OnboardingState, and the full social graph (SocialProfile, Post, Kudos, Comment, Follow, Block, CoachProfile, ProgramEnrollment, CoachReview) that the old inline list SILENTLY LEFT BEHIND — plus sessions/macrocycles/biometrics/signals/check-ins/RTP/videos/events/risk/talent/connections/memberships/templates/assignments/coach links+notes, strictly user-scoped. Progress-photo Storage is cleared via the service-role client (works for web cookie AND mobile Bearer). UI on web (Settings screen, type-RESET to arm) and mobile (More → Settings; also clears on-device guest/draft caches). Localized EN/PL/DE." },
  { id: "account-self-delete", area: "Backend", status: "shipped", title: "Delete my account (self-serve — App Store 5.1.1(v) + GDPR)", detail: "Apple requires an in-app way to DELETE (not just deactivate) an account for any app that supports sign-up, and GDPR Art. 17 requires self-serve erasure — previously HYBRID had only 'reset' (which keeps the login) and admin-only deletion, a hard App Store blocker. Now DELETE /api/account (auth-scoped, rate-limited, body { confirm: 'DELETE' }) hard-deletes ALL of the user's data via the shared wipeUserData() helper (+ email footprint, keeping the EmailSuppression opt-out), removes their progress photos, deletes the User row, then deletes the Supabase AUTH identity via the service-role admin client so the login can't be reused. UI is a dedicated 'Delete my account' danger-zone block (type DELETE to arm) on BOTH clients — web components/account-settings.tsx (redirects to /login after) and mobile components/aurora/settings.tsx (clears guest/draft caches + signs out). The wipe helper is the SINGLE source of truth shared by reset, self-delete and admin-delete so coverage can't drift. Localized EN/PL/DE (settings.delete* keys). Auth deletion degrades to a no-op without SUPABASE_SERVICE_ROLE_KEY (the DB delete is authoritative)." },
  { id: "admin-panel", area: "Web", status: "shipped", title: "Admin console (institutional admin panel)", detail: "A dedicated, WordPress-style operator console at /admin (and ready for an admin.hybrid.app subdomain — a host rewrite in middleware.ts maps it onto the same route group). Gated server-side: app/admin/layout.tsx resolves the DB user and redirects any non-ADMIN before a byte of admin UI/data is sent — the client shell is only chrome. Every /api/admin/* route starts with the shared requireAdmin() guard (lib/admin.ts). Sections: Overview (live KPIs — users/MAU/sessions/coaches/orgs/links, 12-week signup-vs-session growth, role/lang/plan splits), Users (searchable + paginated directory with a detail drawer and role/language editor — with rails: can't self-demote or remove the last admin), Orgs & coaching (the org + coach↔client link graph, management metadata only), Moderation (the trust & safety review queue — see moderation-queue), a full CONTENT/CMS group — Announcements (broadcast, cms-announcements), Exercise library (cms-exercise-library), Media library (cms-media-library), Localization (cms-localization), Feature flags (cms-feature-flags) — Capabilities & data (the registry + Data Network refit), Audit log, and System (DB health/round-trip, deployment + versions, env-var PRESENCE only — never secrets). INSTITUTIONAL GUARANTEES: full RBAC + an append-only audit trail — every privileged mutation writes an AdminAudit row (actor, action, target, before/after, IP, timestamp), surfaced in the Audit log. Privacy-preserving by design: admins see aggregates + management metadata + activity COUNTS, never another user's raw private training rows; impersonation is deliberately excluded. NOTE: run reference/sql-admin-audit.sql in Supabase to create the AdminAudit table (RLS-locked to the server) — until then privileged actions still work but aren't recorded, and the panel flags it. WEB IS NOW RESPONSIVE: the console works on phones/tablets — the sidebar collapses to a hamburger drawer and every section reflows (grids stack, wide tables become horizontally scrollable). TYPE SCALE NORMALIZED TO THE CLIENT APP: the section bodies were authored data-dense (clustered at 10–12px) and read miniature next to the consumer app (which bases at 13–14px), even though the shells share identical widths/padding (admin's main column is actually wider). The admin section type scale was lifted to sit on top of the client's (web: a uniform +1px on every fontSize ≤ 13, preserving each screen's internal hierarchy; cramped table rows loosened by +2px vertical padding) so the two surfaces feel like one product. Mobile is calibrated separately: there the admin already shared the client's 11px body base, so only the excess ≤10px 'tiny' text was lifted (+1) — a blanket bump would have overshot. The fix is now ENFORCED against future drift by a shared scale (see design-scale): every admin section on BOTH clients references named rungs (fs.body, space.lg) from @hybrid/core instead of inline magic numbers, so the two surfaces can no longer silently diverge. MOBILE PARITY: the SAME console now ships on the Expo app (apps/mobile/app/admin/* + components/admin/*) — admin-gated route group, a section springboard, and all 19 sections hitting the same /api/admin/* backend with a Bearer token; reachable from the More tab (admin-only). See mobile-admin for the handful of native parity gaps." },
  { id: "mobile-admin", area: "Mobile", status: "shipped", title: "Mobile admin console (full 19-section parity)", detail: "The institutional admin console (admin-panel) ported to the Expo app so operators can run the platform from a phone — satisfying the web↔mobile parity rule for the operator surface (it was previously web-only). ARCHITECTURE: an expo-router route group apps/mobile/app/admin/* gated by app/admin/_layout.tsx (redirects a signed-out → /login and a non-admin → the app tabs; the REAL boundary stays server-side — every /api/admin/* call runs requireAdmin and 401/403s a non-admin regardless of the client). A shared lib/admin-api.ts wraps the SAME backend the web console uses (adminGet/adminSend, Supabase access token as a Bearer header — the CSRF middleware exempts Bearer). app/admin/index.tsx is a grouped section springboard; app/admin/[section].tsx is the dynamic host that wraps each body in the standard Screen + header. The section registry (components/admin/sections.ts) mirrors the web nav one-for-one. ALL sections are implemented natively in components/admin/* (Overview, Users, Coaching, Moderation, Agent HQ, AI agents, Announcements, Exercise library, Media library, Localization, Feature flags, Capabilities & data, Access control + coach applications, Security, Audit log, Guest workouts, System, Guidance) reusing a shared RN admin kit (components/admin/_kit.tsx) + lib/ui primitives, hitting the identical endpoints + payload shapes as their web twins (read-the-web-component parity), with the same loading / unavailable-SQL / error / empty states and the same optimistic-mutation-then-resync discipline (deletes behind Alert confirms). Entry point: an admin-only 'Admin console' card on the More tab. NATIVE PARITY GAPS (recorded, not lost): (1) charts — recharts is web-only, so dashboards render the same DATA as native stat tiles + label/bar rows; (2) agent RUN uses the non-streaming POST /[id]/run instead of the SSE /stream endpoint (RN fetch can't readily consume a ReadableStream) — same final {output,steps,usage}, no live token streaming; (3) media UPLOAD of new bytes + clipboard copy and CSV/PDF export stay web-only for now (manage/edit/delete/reveal-URL work on mobile); (4) datetime entry for scheduled announcements is plain ISO text (no native picker yet); (5) the per-feature persona-access editor and KPI logging are present, but the coach seat-mix sliders / KPI trend line chart are simplified. Verified: mobile typecheck clean + the iOS bundle exports (the CI mobile gate)." },
  { id: "admin-guidance", area: "Web", status: "shipped", title: "Admin Guidance — deploy/ops runbooks", detail: "A Guidance tab in the admin console (Governance → Guidance, apps/web/components/admin/guidance.tsx) that explains, in beginner-plain language, how HYBRID actually ships: a 'big picture' of web (auto-deploy to Vercel on main) vs mobile (Expo → EAS → TestFlight → App Store); a glossary of every moving part (Expo, expo.dev, Expo Go, the Apple Developer Program, App Store Connect, EAS Build/Submit/Update, TestFlight, what the terminal is for); a step-by-step App Store runbook; how to UPDATE after merging a PR (note: EAS Update OTA is NOT wired up yet, so every mobile change currently needs a fresh EAS Build; native changes additionally need Submit + Apple review; web/backend ride the Vercel deploy); a SANDBOX-BUILD runbook for internal on-device testing (build from the merged branch — EAS bundles your local committed code, not GitHub, so `git pull` first; the `device` internal-distribution profile + QR install; and the App Store Connect API-key workaround when hardware-key 2FA blocks Apple login); Vercel-vs-own-VPS hosting trade-offs; and a 'where we stand' section (the Apple Developer + Expo blockers are now cleared and the app has run on a real device; the public App Store submission is the remaining step). The content is documentation-as-DATA in @hybrid/core (guidance.ts — GUIDES/DEPLOY_GUIDE/SIMULATOR_GUIDE, typed GuideBlock/GuideSection, the block set now including copy-able `cmd` boxes + a `matrix` cheat-sheet) so the copy is the single source of truth and a future mobile admin renders the same words. The tab carries THREE guides (a switcher in the TOC): the deploy/ops runbook above, an iOS SIMULATOR runbook (the beginner copy-paste guide to running the app on Apple's Simulator — merged in from a former standalone 'iOS simulator' tab so all operator runbooks live under one Guidance hub; see ios-simulator-guide), and an ACCOUNTS & IDENTITY map — the source-of-truth inventory of every account HYBRID runs on (domain hybriddomain.xyz on GoDaddy, mail contact@hybriddomain.xyz on Google Workspace, GitHub/Vercel/Supabase, and the still-to-create Apple Developer Organization + Expo accounts), the 'which email owns what' decision (currently open, with a business-mailbox recommendation), the LLC → D-U-N-S → Apple-Organization path, a DECIDED domain & email architecture (landing on hybrid.app, app./admin. subdomains, no-reply@/marketing@/support@ unified onto hybrid.app once acquired — with an open cross-subdomain-session question), and the GoDaddy-DNS → Vercel wiring (keeping Workspace MX intact). hybrid.app remains the intended production domain; hybriddomain.xyz is the working placeholder. The runbook copy is documentation-as-DATA in @hybrid/core, and the mobile admin console now renders these SAME guides natively (the Guidance section, apps/mobile/components/admin/guidance.tsx), so web and mobile operators read identical words — see mobile-admin." },
  { id: "security-posture", area: "Web", status: "shipped", title: "Security posture + control registry (admin Security tab)", detail: "A live security dashboard in the admin console (Governance → Security) and the test suite that backs it. packages/core/security.ts is the single source of truth: shared, unit-tested primitives (normalizeRole, isValidLanguage, clampPage/clampPageSize, evaluateRoleChange — the lockout/escalation rule, redactSensitive) that the admin API actually uses, plus a registry of ~30 SECURITY_CONTROLS across Authentication / Authorization / Data protection / Audit / Input validation / Transport & headers / Secrets / Privacy / Supply chain / Abuse, each with severity + an honest status: pass (green — implemented AND covered by a test), todo (red — the remaining work, e.g. a strict nonce-based script-src CSP), or manual (amber — done in code, needs an out-of-repo step like applying RLS SQL in Supabase). Hardening since the first cut closed most of the reds: per-IP fixed-window RATE LIMITING (tested limiter applied to the AI coach + admin mutations, 429 + Retry-After), request BODY-SIZE caps (413), DEPENDENCY CVE SCANNING (Dependabot + a CI audit that fails on high/critical), and a CSP BASELINE (object-src 'none', base-uri/form-action 'self', frame-ancestors 'none', upgrade-insecure-requests). A further batch added CSRF PROTECTION (middleware rejects cross-origin cookie-auth mutations via a tested Origin/Referer check; mobile Bearer calls exempt), SUPPORT-READ AUDITING (an admin opening an individual's record writes a user.view audit entry), AES-256-GCM TOKEN ENCRYPTION at rest for wearable OAuth tokens (lib/crypto.ts, round-trip + tamper tested; activates on TOKEN_ENCRYPTION_KEY), an RFC-9116 /.well-known/security.txt disclosure contact, and TOTP MULTI-FACTOR AUTH end-to-end (enroll/verify/remove in account settings + a sign-in step-up that's a no-op until a factor is enrolled, so it can't lock anyone out; stepUpRequired + code validation unit-tested). and a STRICT NONCE-BASED CSP (per-request nonce in middleware → script-src 'self' 'nonce' 'strict-dynamic'; pages render dynamically so the nonce injects, verified against the running production server — every Next script incl. the inline RSC stream carries the matching nonce and chunks load via strict-dynamic). With that, every code-side control is green. Deploy/Supabase setup status: the AdminAudit SQL and TOKEN_ENCRYPTION_KEY are applied. ⚠️ DEFERRED: per-user MFA (TOTP) enrollment is intentionally on hold until the production domain is registered — enroll + verify the challenge flow in the browser then (Supabase TOTP is on by default; no code change needed). RLS reference SQL should also be confirmed applied in Supabase. The /security tab renders a posture score (share of controls green), a critical-open warning, LIVE runtime probes (HTTPS, DB reachable, audit table present, auth configured) from /api/admin/security, and every control grouped by category with its evidence. GUARANTEED HONEST: 226 core tests (incl. 22 security tests) + a web static-scan suite (vitest) assert the green controls actually hold — every API route authenticates, every /api/admin route requires ADMIN, OAuth tokens are never selected into responses, no dangerouslySetInnerHTML, security headers present, no committed secrets / secret logging — so a regression that weakens a control turns CI red. Hardening shipped alongside: baseline security response headers in next.config (HSTS, X-Frame-Options DENY, nosniff, Referrer-Policy, Permissions-Policy, frame-ancestors 'none'); the admin role path now routes through the tested core guards; pagination is clamped against resource-exhaustion (fixed a real bug where an absent pageSize fell to the minimum instead of the default). Both web test + the mobile iOS-bundle export run in CI." },
  { id: "cms-announcements", area: "Web", status: "shipped", title: "CMS — announcements / broadcast", detail: "The first content-management surface in the admin console (Content → Announcements), turning the operator panel into a CMS: an admin authors a message (title + body, level info/success/warning, audience all/coaches/clients) and drives it through a draft → published → archived lifecycle with optional scheduled publishAt + auto-expiry expiresAt and a 'pin as banner' flag — no code change or deploy needed to talk to the app. Announcement model + reference/sql-announcement.sql (RLS: any signed-in user reads only PUBLISHED rows; all mutations are server-only). Admin CRUD at /api/admin/announcements (+ /[id]) — ADMIN-gated, rate-limited, body-capped, and every create/update/delete writes an AdminAudit row. The read side /api/announcements returns only the published, in-window, audience-matched rows for the caller; pinned ones render as a per-device-dismissible banner atop the web app shell. The reusable pattern (model + RLS + admin CRUD + audited mutations + in-app surface) the rest of the CMS collections build on. NOTE: run reference/sql-announcement.sql in Supabase to create the table." },
  { id: "cms-exercise-library", area: "Web", status: "shipped", title: "CMS — exercise library (admin-managed catalog)", detail: "The second CMS content type (Content → Exercise library): a DB-backed, admin-authored exercise catalog that is a SUPERSET of the core Movement shape — engine-critical fields (pattern, muscles, baseLoad, system, validated against the same core enums) PLUS authored content (category, equipment, aliases, description, coaching cues, demo video URL — the slot the blocked exercise-video-library plugs into). Lifecycle: draft → published → archived. ARCHITECTURE: the 9 built-in movements stay in @hybrid/core so the engines stay PURE and the catalog works with an empty DB; a new pure, unit-tested mergeMovements(builtins, custom[]) folds the library over the built-ins by name (a custom entry overrides a built-in; aliases resolve to the same Movement but never clobber a real entry) into the one Record<string,Movement> the engines + pickers already consume — no engine signature changes. Name is the engine key; on rename the old name is auto-kept as an alias so prior logged sessions still resolve. Exercise model + reference/sql-exercise.sql (RLS: signed-in users read only PUBLISHED rows; mutations server-only). Admin CRUD at /api/admin/exercises (+ /[id]) — ADMIN-gated, rate-limited, body-capped, audited (P2002 → 409 on duplicate name). Read side /api/exercises (published) feeds a useExercises() hook that merges client-side; wired into the web Builder's exercise picker so an authored exercise immediately autocompletes across the app. PICKER DEDUP (aliases resolve but aren't pickable): mergeMovements keeps every alias key for engine RESOLUTION (an old logged session under an alias still attributes), but the two new pure helpers catalogNames(builtins, custom) + aliasNames(custom) drive DISPLAY — the pickable catalog is the primary names only, with any name a custom entry aliases removed. So a descriptive custom lift (e.g. 'Barbell Bench Press') that aliases the generic built-in 'Bench Press' shows ONCE under its own name while 'Bench Press' is hidden from the picker + datalist (workout-blocks.tsx filters both the pattern buckets and BASE_CATALOG through the alias set) yet still resolves for prior logs. reference/exercise-catalog.md is the deduplicated source-of-truth list the library seeds from, and reference/sql-exercise-seed.sql is the idempotent 209-row INSERT that populates the table from it (pattern/muscles best-effort-mapped to the coarse engine enums; isolation groups the enum lacks — biceps/calves/abs/forearms — left empty). MUSCLE-GROUP GROUPING: exercisesByCategory now takes a categoryByName map (categoriesByName helper) so library exercises group under their muscle-group heading (Chest, Back, …, LIBRARY_CATEGORY_ORDER) instead of the catch-all 'Other'; built-ins still bucket by movement pattern. MOBILE PARITY: shipped — the mobile pickers now consume this same library (see mobile-exercise-library). NOTE: run reference/sql-exercise.sql THEN reference/sql-exercise-seed.sql in the Supabase SQL Editor — the sandbox can't reach Supabase, so nothing appears in the picker until those run. EDIT ANY EXERCISE (built-ins included): the admin directory now lists the CODE built-ins alongside the custom DB rows (one A–Z list; GET /api/admin/exercises folds builtinExerciseRefs() under the custom rows, a same-name custom row hiding its built-in twin). A built-in is a virtual row (source 'builtin', synthetic id) with an 'Edit / override' action: it opens the editor pre-filled from the built-in's engine fields (pattern kept via LIBRARY_PATTERNS — the CMS allow-list is now core's full pattern taxonomy so a built-in override round-trips its pattern losslessly; muscles/category/equipment carried over) and SAVING creates a custom override (POST) that supersedes it by name. Renaming during an override seeds the original built-in name into aliases automatically (the PATCH route already does this for a custom rename), so prior logs resolve AND every summary canonicalizes to the new name (see exercise-name-canonicalization). If the Exercise table doesn't exist yet the built-ins still LIST (read-only) under a non-blocking banner instead of the old blocking 'not initialized' screen. Editing is a WEB admin surface (mobile consumes the result via the shared catalog + server-canonicalized sessions — no mobile editor by design, same scope as mobile-exercise-library)." },
  { id: "cms-media-library", area: "Web", status: "shipped", title: "CMS — media library (shared asset catalog)", detail: "The third CMS content type (Content → Media library): an admin-managed catalog of shared assets (exercise demo clips, announcement imagery) that turns scattered URLs into a managed library. Bytes live in a PUBLIC Supabase Storage bucket 'media' (reusing the progress-photos storage pattern) with admin-only writes — a new public.is_admin() RLS helper gates insert/update/delete on storage.objects while keeping public read, so the stable CDN URL can be embedded anywhere. A MediaAsset catalog table (title/alt/kind/tags/size + lifecycle) is the searchable index + the URL other content points at; reference/sql-media-library.sql creates the bucket, is_admin(), the storage policies, and the table (RLS: signed-in read published, mutations server-only). Admin uploads client-side (like progress photos), then POSTs the row to /api/admin/media (+ /[id]) — ADMIN-gated, rate-limited, body-capped, audited; DELETE removes the row and best-effort removes the bytes (admin auth carries through is_admin()). Read side /api/media returns published assets. The admin screen is a grid with image/video previews, draft/publish/archive, tag/title editing, and a one-click Copy-URL that drops straight into an Exercise's demo-video field (cms-exercise-library) — connecting the CMS types. NOTE: run reference/sql-media-library.sql in Supabase; the actual upload/delete round-trip is untestable from this sandbox (Supabase host blocked), same as progress-photos. MOBILE PARITY: the mobile admin console (apps/mobile/components/admin/media.tsx) manages, edits, status-cycles, deletes existing assets and reveals the public URL, but UPLOADING new bytes is out of v1 scope on mobile — new clips/images are uploaded from the web console (the RN Storage upload + a clipboard copy aren't wired yet)." },
  { id: "cms-localization", area: "Web", status: "shipped", title: "CMS — localization manager (edit EN/PL/DE in-app)", detail: "The fourth CMS content type (Content → Localization): a non-engineer can fix copy or fill a missing PL/DE string from the admin UI — no code edit, no deploy. ARCHITECTURE mirrors the exercise library: the shipped TRANSLATIONS stay in @hybrid/core as the baseline (the app always renders offline-first on them), and a SPARSE Translation table holds only the (lang,key) rows an admin edited/added. A new pure, unit-tested makeTWithOverrides(lang, overrides) layers the overrides over the baseline (override active-lang → shipped active-lang → override en → shipped en → key); makeT is unchanged so nothing else breaks. allTranslationKeys()/baselineString() expose the catalog to the editor. Translation model + reference/sql-translation.sql (RLS: signed-in read all — UI copy isn't secret; mutations server-only). Admin CRUD at /api/admin/translations — ADMIN-gated, rate-limited, body-capped, audited; POST upserts and an empty value REVERTS to baseline. Read side /api/translations groups overrides by language and is wired into BOTH clients' LanguageProvider (web fetch + mobile Bearer via fetchTranslationOverrides), so an edit reaches web AND the phone. The editor is a searchable, group-filterable key table (overridden / missing-translation filters, per-cell revert, live key + override counts). NOTE: run reference/sql-translation.sql in Supabase to create the table." },
  { id: "cms-feature-flags", area: "Web", status: "shipped", title: "CMS — feature flags / runtime config", detail: "The fifth CMS content type (Content → Feature flags): toggle app capabilities at runtime, no redeploy. ARCHITECTURE matches the other CMS layers — the KNOWN flags + defaults live in a FEATURE_FLAGS registry in @hybrid/core (so the app runs on defaults with an empty DB, and only a flag that code actually reads can gate anything), while a sparse FeatureFlag table stores per-flag OVERRIDES (enabled / audience / optional JSON value). Pure, unit-tested evaluator: evaluateFlags/evaluateFlag + flagAudienceMatches (admins always match; coaches/clients scoped) + flagValues. FeatureFlag model + reference/sql-feature-flags.sql (RLS: signed-in read all; mutations server-only). Admin CRUD at /api/admin/flags (+ /[key] reset) — ADMIN-gated, rate-limited, body-capped, audited; only registry keys are accepted, an empty override row resets to default. Read side /api/flags evaluates the registry against the caller's role and returns a boolean map (+ values); a fail-OPEN useFlags() hook consumes it. WIRED end-to-end: the web app shell gates the announcement banner behind app.announcements and hides nav items (Nutrition/Talent/Longevity/Tactical) behind nav.* flags — flip a toggle in the console and the feature appears/disappears on the next load. The editor lists every registry flag with an on/off switch, audience scope, override/default badge, and per-flag reset. NOTE: run reference/sql-feature-flags.sql in Supabase so toggles persist (until then the app runs on registry defaults)." },
  { id: "moderation-queue", area: "Web", status: "shipped", title: "Moderation queue (trust & safety)", detail: "The sixth admin surface (Platform → Moderation): a review workflow over user-generated content. Any signed-in user can flag a social profile, a comment or a post (→ /api/reports, rate-limited, dup-collapsed); a denormalized Report model feeds the queue, and the admin console lists open reports with Take down / Dismiss / Mark resolved. It used to have a SECOND feeder — an approval gate on discoverable talent profiles, with its own tab — which went with the Talent Graph in the Aug 2026 strategy cuts; one feeder does not need a tab bar, so the switcher came out of both clients too. Every decision is ADMIN-gated + audited (moderation.profile.*, moderation.report.*). Report model + TalentProfile columns + reference/sql-moderation.sql (RLS: a user inserts only their own report; reads/resolutions are server/admin-only). NOTE: run reference/sql-moderation.sql in Supabase to add the columns + table." },
  { id: "auth-email", area: "Auth", status: "shipped", title: "Email auth + roles", detail: "Supabase email sign-up/in on web + mobile. Role model CLIENT/COACH/ADMIN; admin can toggle all dashboards. SIGN-OUT RESILIENCE FIX (Jul 2026): the primary 'Sign out' now sticks even offline. Two defects were fixed on BOTH clients — (1) the plain sign-out used the DEFAULT scope, which is GLOBAL, so it unexpectedly revoked every device AND was redundant with the explicit 'Sign out everywhere' button; it now uses scope:'local' (this device only), leaving 'everywhere' as the sole global revoke. (2) supabase.auth.signOut() only removes the persisted session when its network revoke SUCCEEDS (auth-js returns early on any non-401/403/404 error, e.g. offline/timeout/5xx), so a failed revoke left the token behind — `sb-*` cookie (web) / `sb-*` AsyncStorage key (mobile) — and getUser()/getSession() silently signed the user back in on the next launch ('logged out but still logged in'). The `hybrid.*` state wipe never touched those `sb-*` keys. Now clearClientState() force-clears the Supabase auth token regardless of the revoke result — web session.tsx expires every `sb-*` cookie, mobile session.tsx drops every `sb-*` AsyncStorage key — and mobile also nulls the React session directly (the auth listener only fires SIGNED_OUT on a successful revoke). So sign-out tears down locally every time. Web=web/lib/session.tsx, mobile=mobile/lib/session.tsx. Plus MIDDLEWARE SESSION REFRESH (web): the edge middleware previously did only CSRF/CSP and never rotated the Supabase auth cookie — Server Components can't write cookies, so an access token that expired between visits left SSR rendering from a stale session until the browser client refreshed it. middleware.ts now runs the standard @supabase/ssr refresh (createServerClient + getUser()) with the rotated cookies written onto BOTH the downstream request (this render sees them) and the response (the browser persists them); gated on an `sb-*` cookie being present so logged-out traffic never pays the auth round-trip, and best-effort so a refresh hiccup never blocks a page." },
  { id: "auth-social", area: "Auth", status: "blocked", title: "Apple + Google sign-in", detail: "Code paths exist (buttons + OAuth callback).", blockedBy: "Enable providers in Supabase: Google Cloud OAuth client; Apple needs the Apple Developer Program + Services ID." },

  // ---- Backend / Data ----
  { id: "db", area: "Data", status: "shipped", title: "Database (Prisma + Supabase)", detail: "RLS-enabled models — core: User, Session (+ its queryable projections SessionSet / SessionStream / SessionLap — see session-sets-table and session-streams), Macrocycle, Biometric, Plan, Signal; coaching: CoachLink, CoachNote, Checkin, WorkoutTemplate, Assignment; performance: RtpProtocol, RiskOutcome, ModelFit, Connection; governance: AdminAudit; CMS: Announcement, Exercise, MediaAsset, Translation, FeatureFlag; trust & safety: Report. Each feature ships its own migration in reference/*.sql (table + policies) since the sandbox can't reach the Supabase host. The 2026-08 strategy cuts dropped twelve tables (Organization/Team/Membership/OrgInvite, TalentProfile, VideoAnalysis, Event, and the five email-marketing tables). Two paths, because production's migration bookkeeping is not yet reconciled (see prisma/MIGRATIONS.md): a fresh environment gets the drop from the Prisma migration 20260812000000_strategy_cuts; production gets it by running reference/sql-strategy-cuts-2026-08.sql in the Supabase SQL Editor." },
  { id: "api", area: "Backend", status: "shipped", title: "Shared API", detail: "One Next.js backend (app/api/*) both clients call — every route is user-scoped and accepts web cookies OR mobile Bearer tokens. Surface spans /api/me, /api/sessions, /api/signals, /api/macrocycles, /api/checkins, /api/templates, /api/assignments, /api/rtp, /api/state, /api/connect/[provider], /api/datanet, the coach routes (/api/coach/squad, /api/coach/compare, /api/coach/links/[id]/{sessions,notes,checkins,assignments}), the CMS read routes (/api/announcements, /api/exercises, /api/media, /api/translations, /api/flags), user reporting (/api/reports), and the ADMIN-gated console routes (/api/admin/{stats,users,announcements,exercises,media,translations,flags,moderation,…})." },
  { id: "rls-policies", area: "Backend", status: "shipped", title: "Row-level security policies", detail: "Base policies in reference/rls-policies.sql (own-rows for Session/Macrocycle/Biometric/Signal, coach read via ACTIVE CoachLink, private CoachNotes hidden from clients, Plan readable by signed-in users). Each later feature adds its own own-rows + active-coach policies in its migration file (sql-checkin, sql-workout-builder, sql-coach-tags, sql-progress-photos storage RLS, sql-rtp, sql-coach-macrocycle, etc.). Defense-in-depth atop the API-layer enforcement." },

  // ---- AI / Integrations ----
  { id: "ai-coach", area: "AI", status: "shipped", title: "AI coach (server-side Anthropic)", detail: "/api/ai-coach builds context from your real sessions + signal-derived recovery and grounds the note in the Athlete Twin — HPI + its limiting pillar, ranked state drivers, and tissue-level injury flags. It now also feeds the coach the ENDURANCE side and EFFORT trend: cardio mileage (total + last-4-weeks via the running engine), recent pace, the easy-vs-hard balance (paceEffortSplit — derived from pace, not RPE), and the athlete's average logged strength RPE across recent efforts — and the system prompt weighs both sides of hybrid training (flagging an off easy/hard balance, or a grind when RPE sits ≥8.5). Calls Claude server-side (claude-opus-4-8, adaptive thinking); falls back to a Twin-aware engine note (state summary + risk warning) if the key is ever unset. ANTHROPIC_API_KEY is SET in Vercel, so the LLM coach is live (never called from a client). This same key powers the AI agent org (see agent-org)." },
  { id: "device-full-capture", area: "Integrations", status: "shipped", title: "Everything the device holds, in the database — not a 30-day slice of three metrics", detail: "SHIPPED. TWO GAPS, both of them 'the data was already on the phone and we never asked'. (1) STREAM BACKFILL: streams only landed for matches made after the feature shipped, so every workout matched or imported before it had a summary and no trace. The trace is usually still in Apple Health — the session stores the store's own uuid — so `backfillWorkoutStreams` walks the athlete's matched history, skips what is already stored and fetches the rest, 12 sessions per foreground, sequentially. THE WORK-LIST IS ITS OWN ENDPOINT (GET /api/sessions/recordings — id, uuid, provider, a `streamed` skip flag and a `fingerprint` of the stored read), and that is a FIX rather than a convenience: both device passes originally built their list from `fetchSessions`, which is the History list and returns the FIFTY most recent sessions, so 'walks the whole matched history' held only for an athlete with fewer than fifty workouts — everyone else had an invisible ceiling past which older recordings were never repaired and older traces were never going to be fetched. The index is uncapped because it carries four small fields per row instead of a whole workout, and the fingerprint (core `deviceFingerprint`) lets the repair pass spot an unchanged read without the stored blob crossing the wire. A GPS track is thousands of fixes to read and tens of KB to upload; firing them in parallel off a phone on a train is how a sync ends up worse than no sync. It is a one-time catch-up that goes quiet once the backlog clears. (2) THE RELAY READ THREE METRICS over THIRTY DAYS. Everything else Apple Health held stayed on the device, and an athlete who had worn a watch for five years handed us a month of it. Now: SEVENTEEN metrics (`HEALTHKIT_SIGNAL_TYPES` in core, the one table both ends read so the phone cannot ask for something the server cannot store) — HRV, resting HR, sleep, respiratory rate, blood oxygen, sleeping wrist temperature, walking HR, heart-rate recovery, VO2 max, steps, active + basal energy, exercise minutes, stand time, and the body mass / fat / lean mass a smart scale writes. Eleven new SignalKinds, each with its own plausibility bounds. THREE THINGS EACH READ HAS TO GET RIGHT, and each has cost this codebase a metric before: the UNIT is requested explicitly (HealthKit's default for respiratory rate is count/SECOND — the honest-looking read stores 0.25 breaths/min and the bound refuses it, a metric that vanishes with no error anywhere); the AGGREGATION is SUM for cumulative types and MEAN for point readings (averaging a day of step samples reports about forty steps — `dailySum` is new beside `dailyMean`); and PERCENTS are detected rather than assumed, since Apple's percent unit is a 0..1 fraction but a third-party source may write 98, and blindly multiplying gives 9 800. THE HISTORY WALK: every sync relays the recent 30 days for freshness AND walks one 180-day chunk further back, up to a ten-year floor, stopping early when two consecutive chunks come back empty (that is the start of this person's data, not a bug). Resumable by construction — the cursor is on the device, a failed chunk is NOT advanced past so the next foreground retries it rather than leaving a hole nobody can see, and every batch is idempotent server-side because Signal is unique on (user, kind, ts, source). A full ten-year backfill completes in about ten foregrounds without any one sync feeling like a download. BOUNDED at both ends: the relay body is capped at 4 MB (it was unbounded) and `parseHealthKit` at 20 000 samples. The Connections page reports what each pass did and says plainly when older history is still importing, so 'why is it still syncing?' has an answer on screen. 4 new tests." },
  { id: "plausibility-guards", area: "Core", status: "shipped", title: "Idiot-proofing — a logged number has to be one a body could produce", detail: "SHIPPED (packages/core/src/plausibility.ts + every write path + both loggers). THE HOLE: almost nothing checked what a figure MEANT. The session CREATE route stored `blocks` verbatim — the comment said it holds \"what our own logger built\", which was never a guarantee for an HTTP endpoint — so a 70 000 kg bench press or a 5 200 km swim landed in the document and from there into tonnage, e1RM, mileage, pace, training load, ACWR, readiness, the records ladder and the k-anonymous cohort norms the data network publishes. One typo moves an athlete's entire history and nothing downstream can tell it from a fact. Where bounds DID exist they were sport-blind (0.001–1000 km for every discipline, so a 900 km swim passed), one-sided (bodyweight capped at 500 and floored at zero — and a near-zero bodyweight silently ZEROES the effective load of every pull-up, dip and assisted rep ever logged), or absent (a 1RM under 100 000 kg, and every Signal kind except sauna accepted any finite number straight into a rolling baseline). TWO TIERS, and collapsing them is the mistake this design exists to avoid: REFUSE = physically impossible or a unit slip wearing a plausible face, never stored; CHECK = improbable but real (an elite lifter, a 24-hour race, a 200 kg athlete), asked about and stored. A single threshold either refuses a real 500 kg leg press or waves 70 000 through. CONTEXT DECIDES: a load is judged against the IMPLEMENT (120 kg is ordinary on a barbell, worth a question on a dumbbell, refused on a kettlebell; a weighted pull-up's field holds the BELT, not the athlete), a rep count against what the field actually holds (reps, or the seconds of a hold, or the metres of a carry), a distance against the DISCIPLINE (5 200 km is the unit slip that matters most, because pool distances are quoted in metres everywhere else in the product), and every ceiling sits above the world record so the bound can only ever catch a typo. THE COMPOUND CHECKS EARN THEIR KEEP, because most real mistakes are a plausible PAIR that cannot both be true: `checkEffort` catches 10 km against 5 minutes on speed — and its soft tier catches what the hard one structurally cannot, since a 100 m sprint legitimately averages 10 m/s so the refusal must sit at sprint speed, leaving a 10 km logged at world-record-beating pace to the world-record-pace band; `checkSet` catches 60 kg × 40 reps on the implied one-rep max. NOTHING IS EVER CLAMPED — 1 500 kg is no more true than 70 000, and an invented figure is worse than an absent one because nothing can tell it was invented. An impossible FIELD is dropped and the workout is KEPT (losing a finished session over a slipped finger punishes the athlete far harder than losing the number); a malformed SHAPE is still a 400. WIRED: sanitizeSessionBlocks now enforces the load/reps STRINGS it previously only length-capped, and is the one definition of a storable workout used by POST /api/sessions (which validated nothing at all), the edit PATCH, /api/anon-sessions, /api/templates, the device-import write, and the SessionSet projection — that last one is what keeps a pre-guard 70 000 kg bench out of the cross-athlete aggregates, as a read-time filter that leaves the athlete\'s document untouched (a routine is re-logged, so a bad figure there is re-logged with it); plus /api/body, /api/biometrics, /api/plan-maxes (a typed max is an INSTRUCTION — it is multiplied into every working set a percent plan writes), /api/signals, the connector relay (an unattended source nobody reads on the way in, whose readings become baselines) and `foodLogSignals` in core — the one builder that decides what a logged food means, so a per-100 g panel filed as per-serving cannot turn one entry into a 400 000 kcal day. CLIENT-SIDE, BOTH TIERS ARE WIRED. The REFUSAL stops the keystroke the way a maxLength does — the digit does not appear — on EVERY logging surface — the live logger, the routine builder and the edit-workout sheet — with a toast naming the bound in the FIELD\'S OWN unit (a lb user is told 3 306 lb, a pool swimmer 100 000 m). The CHECK tier is a quiet amber line under the figure, and it NEVER BLOCKS: a strong athlete would meet a modal every session, and a warning that interrupts is one people learn to dismiss without reading. It says WHAT is odd rather than shrugging — `inspectSet`/`inspectEffort` return a reason beside the verdict and CONCERN_KEY maps it to one sentence both clients share (EN/PL/DE), so the athlete reads 'that pace beats the world record — check the distance and the time' rather than 'check this'. The reason separates a single odd FIELD from an odd PAIR, which are different problems with different fixes: 450 kg reads as unusually heavy, while 200 kg × 60 reps reads as a big implied one-rep max, because neither of those figures is odd alone. On a collapsed set row the row\'s own figures turn amber instead of gaining a badge — it is already three columns wide on a small phone, and tapping it re-opens the set where the full sentence waits. `checkSet`/`checkEffort` survive as thin wrappers returning the verdict alone, so no write path moved. BOTH HALVES ARE SHARED RATHER THAN COPIED (mobile lib/field-guard.ts + aurora/concern-line.tsx): three screens were about to hold three copies of the same toast string and the same amber line, and the drift would have been invisible — a bound tightened in core would keep announcing the old number from whichever screen was forgotten. THE EDIT SHEET IS THE SUBTLE ONE: its draft is in DISPLAY units (the athlete's own weight unit, the sport's own distance unit) where the bounds are written in kg and km, so every comparison and every message converts first — and the implied-max rule judges in kilograms, or every heavy set logged in pounds would read as unusual. It is also where a slipped finger is likeliest, since the athlete is retyping a figure they already got wrong once, onto a session that already carries PRs, a feel report and possibly a device match. 44 tests." },
  { id: "session-sets-table", area: "Data", status: "shipped", title: "Sets are rows — the per-set fact table", detail: "SHIPPED (prisma SessionSet + core/session-facts.ts + reference/sql-session-sets-streams.sql). THE PROBLEM, in one sentence: a logged set lived only inside Session.blocks, a jsonb document, and Postgres cannot index a value it cannot see — so there was no per-set grain, no per-set index, and every cross-athlete analytic was a full-session scan in the lambda under a `take:` cap. The cap is the part that matters: it does not fail, it TRUNCATES, so the answer comes back looking complete and is quietly the answer for whatever subset happened to fit. The moat data existed and was not in a shape anything could ask a question of. Fixed while the table is empty, which is the only cheap time to do it — at a million users this is a re-architecture performed under load. THE GRAIN: one row per strength SET, one row per timed effort (a continuous effort has no sets to split, so setIndex is 0 and (blockIndex, setIndex) keys every kind alike). THE ROW: identity (sessionId, userId, performedAt, archived — all DENORMALISED, because a warehouse table that needs a join for its most common filter is not a warehouse table), what was trained (exercise, movement pattern, muscle groups, cardio discipline), what was done (role, drop, reps, load, RPE, VBT velocity, ROM, measured rest / distance, seconds, pace, elevation, watts, zone, rounds), and what is DERIVED (effective load, tonnage, e1RM, pace). Seven indexes, three of them cross-athlete: (exercise, performedAt), (movement, performedAt), (discipline, performedAt). IT IS A PROJECTION, NOT A SECOND TRUTH — the document stays the source of truth and these rows are rebuilt from it, which is what makes them safe: rewritten on every session write (create, block edit, device attach/unlink, import), delete-then-insert in a transaction so a reader never sees a half-written session, and rebuildable in batches from POST /api/admin/backfill/session-sets. Failure is NEVER fatal to the write it follows — a projection error is logged and swallowed, because losing an athlete's finished workout to repair a derived table is a trade that is never close. THREE RULES the projection keeps, all in core so an aggregate and a screen cannot drift: (1) THE DEVICE WINS — the projection runs deviceTrueSession() first, so a matched effort stores the measured duration/distance/climb/power, and `measured` marks the row so provenance is filterable; a warehouse fed the typed numbers would disagree with every screen in the app. (2) DERIVED FIGURES USE THE ENGINES' OWN HELPERS (effectiveSetLoadKg, loadUnitCount, e1rm, cardioSeconds) rather than a second implementation in SQL. (3) THE ROW REMEMBERS WHAT IT ASSUMED — bodyweight-dependent lifts resolve against the athlete's weight AT THAT DATE and store it, so a later weigh-in makes old rows STALE rather than wrong and the backfill can find them. PROVEN by a real consumer: GET /api/admin/analytics/sets answers most-trained lifts, movement-pattern balance, endurance modality split and the platform's RPE distribution as GROUP BYs with no rows pulled into the lambda, no cap, and k-anonymity (≥5 athletes) on every group. RLS is owner-only. 16 tests." },
  { id: "session-streams", area: "Data", status: "shipped", title: "Streams — the recording, not the summary of it", detail: "SHIPPED (prisma SessionStream + SessionLap + core/session-streams.ts + POST/GET /api/sessions/[id]/streams + the HealthKit read). THE PROBLEM: Session.device carried a workout SUMMARY only — duration, distance, kcal, avg/max HR — and summary-level Apple Health data is a commodity every app with the entitlement can read. The heart-rate time-series, the GPS route, the laps and the splits (the data WHOOP and Garmin actually own) never landed here at all, and cannot be recovered afterwards: only the phone can read a health store, and only while the recording is still in it. STORAGE, and the shape is the decision: a stream is ONE ROW PER (session, kind) holding parallel offsets/values arrays — offsets are whole SECONDS from an anchor, not timestamps — with the aggregates (sampleCount, durationSec, min/max/avg) lifted into their own columns so the common questions never touch the arrays. A row per SAMPLE is the obvious answer and the wrong one: an hour of 1 Hz heart rate is 3 600 rows, so an athlete-year is millions and a million athletes is a table nobody can afford to index. Postgres arrays keep it queryable (unnest) without paying that. Seven kinds: hr, power, cadence, speed, altitude, distance (CUMULATIVE km — the series everything else is derived from) and route (latitude in `values`, longitude in `valuesB`). Capped at STREAM_MAX_SAMPLES = 3000 by DOWNSAMPLING ACROSS THE WHOLE SPAN, never by truncating — rates averaged per bucket, positions and cumulatives taking the bucket's last real sample; a truncated stream would turn a six-hour ride into a forty-minute one and every derived figure would be wrong in a way nothing downstream could detect. LAPS DO GET A ROW EACH, because laps are tens per workout rather than thousands: the device's own laps and multi-sport segments, the SPLITS derived from the distance series (interpolated at the boundary, not snapped to the nearest sample — at 3:30/km a 5 s sample is 23 m, which is five seconds of split error), and `best` rows — the FASTEST WINDOW inside the effort covering each catalog distance, found by a two-pointer sweep and STORED so 'my fastest 5 km' is an indexed lookup rather than a scan over every recording. HR ZONE TIME is derived on READ, never stored, because its denominator moves: the max is the highest the athlete has ever been MEASURED at (one indexed aggregate over the stream table's own max column — a query this table exists to make possible), not 220-minus-age, which is a population average with no relationship to any individual. THE CLIENT READ (apps/mobile/lib/healthkit.ts): per-workout quantity series behind a workout filter, the route via getWorkoutRoutes(), lap markers off the workout's events, and the cumulative distance series INTEGRATED from the fixes (HealthKit stores locations, not distance) — skipping fixes worse than 50 m horizontal accuracy, because integrating a GPS jump into the next street adds distance the athlete never ran and would make every split and every best effort flatteringly fast. Uploaded after a hand match AND after every device import, fire-and-forget: the session is already saved, and a failed upload must never look like a failed match. Sanitisation drops bad samples rather than clamping them (a clamped 500 bpm becomes a plausible 260 that poisons an average) and rejects a backwards fix in a cumulative series (one of those makes the next split negative and the one after twice as fast). Provider-neutral by construction — nothing HealthKit-specific crosses into core, so a Garmin or WHOOP connector fills the same shape. RLS is owner-only with NO coach read, unlike Session itself: a route stream is the athlete's home address written down three thousand times, and a coach who should see a workout has not thereby been shown where it started. 32 tests." },
  { id: "biometrics-manual", area: "Data", status: "shipped", title: "Readiness check-in (manual biometrics)", detail: "Enter HRV / resting HR / sleep; persisted to the Biometric table and fed into the readiness engine (today vs. rolling baseline). Web dashboard." },
  { id: "vbt-capture", area: "Integrations", status: "blocked", title: "Live bar-velocity & trajectory capture", detail: "The whole VBT analytics stack (vbt-engine + Velocity screen + team-compare) is live and consumes velocity from any source. What's missing is the on-device CAPTURE that produces real per-rep velocity and the sagittal bar-path/trajectory: either a bar-mounted IMU sensor over BLE or a phone-camera CV pipeline (the pose engine that would have fed it went with `video-intel`, so this now starts from a blank page). Until then, velocity is entered manually per set (m/s) in the Logger's exercise detail sheet and still drives the full profile + 1RM + autoregulation. Per-rep trajectory plots are stubbed in the UI.", blockedBy: "A bar-sensor BLE SDK (e.g. an Enode/Vitruve-class device) or a native camera pose-capture module in the EAS build to emit per-rep velocity + bar path." },
  { id: "financials-model", area: "Web", status: "retired", title: "Financials & unit-economics console", detail: "DELETED (Aug 2026). Was: Admin → Business → Financials — the business-model and cost-structure surface over a pure, unit-tested economics engine (REVENUE_STREAMS, pricing tiers, COST_DRIVERS, DEFAULT_ASSUMPTIONS, computeEconomics), with a live unit-economics calculator, revenue-by-stream and MRR/cash charts, per-segment economics, a SaaS health scorecard and a 12-month forecast. Removed: packages/core/src/economics.ts (+ test), the Financials screen on web AND mobile, and the Business group in both admin consoles. The static 'what each plan includes' table went with it — the LIVE entitlement surface is the per-feature persona matrix in Admin → Governance → Access control, driven by nav.ts minPersona, which is the one that actually gates anything.", retiredBecause: "It modelled the unit economics of a business with zero revenue, using assumptions no customer had tested. Every number in it was an input we chose. Rebuild it from Stripe's numbers once cards are being charged — by then the model will be arithmetic over facts rather than a spreadsheet of hopes." },
  { id: "billing", area: "Backend", status: "blocked", title: "Billing & payments (coach marketplace)", detail: "Payment rails for the coach MARKETPLACE — the client pays the coach for a paid program and HYBRID takes a platform fee on the transaction (see social-paid-coaching). NOT seat fees: coach tooling is free, permanently (see coach-tooling-free) — the Jul 2026 strategy review reframed coaches from a revenue line into the acquisition channel, so there is no 'HYBRID charges the coach for tools' path to build. The old seat-fee tiers are gone entirely — the Financials model that held them as a shelved counterfactual was retired in Aug 2026. What's missing here is the live charge path for client→coach checkout; it can be built behind a provider abstraction but can't be wired or tested without provider keys.", blockedBy: "A Stripe account + STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET (and Connect onboarding for coach payouts) in the server env." },
  { id: "coach-tooling-free", area: "Core", status: "shipped", title: "Coach tooling is free — coaches are the acquisition channel", detail: "The Jul 2026 strategy review's call, now the recorded product policy: one coach brings 30–200 athletes at zero CAC, so taxing the channel with seat fees is strictly worse than growing it. What this means in code: the verified COACH role unlocks everything (access.ts whitelists 'coach'; coach nav is role-gated, never paywalled), roster/invites/program builder/groups/squad monitor/check-in replies carry no entitlement checks on either client, and the invite route's roster cap is an anti-abuse guardrail (200) rather than a billing tier — the 'enforce 10/40/150 when billing lands' TODO is deleted. The pricing stance: coach tooling is $0 forever, revenue later as a marketplace platform fee on coach-sold programs (social-paid-coaching). The old seat tiers were dropped along with the Financials model that modelled them (retired Aug 2026). The only gate on coaching is the verification queue (coach-verification), which is quality control, not pricing." },
  { id: "exercise-implement-marks", area: "Core", status: "shipped", title: "Exercise tiles — the implement mark (the initials are retired)", detail: "Every 40px tile that names a lift used to hold the lift's INITIALS, which was the worst of both worlds: it repeated what the row already says, in a form that read as noise, and it collided constantly — in the Chest room alone 'Cable Chest Press' and 'Cable Crossover' were both CC, 'Decline Barbell Bench Press' and 'Decline Bench Press' were both DB, and 'Incline Dumbbell Bench Press' and 'Incline DB Press' were both ID. A mark should carry what the NAME DOESN'T, and inside a muscle group (where most picking happens) the name already says the movement — what varies is the GEAR. So the tile now draws the IMPLEMENT: a barbell, a pair of bells, a kettlebell, a cable handle, a weight stack, a figure for bodyweight, a band, a sled, a med ball, and one neutral mark for a custom name the DB doesn't know. A column of them sorts by eye. NEW CORE MODULE exercise-marks.ts, built on the sport-marks.ts idiom: stroke paths in the same 72-unit box as theme/icons.ts, stroked at the shared auroraIconStroke(size) weight by one renderer per client (components/aurora/exercise-mark.tsx, web + mobile), so the marks sit at the same optical weight as the rest of the Aurora icon set on both. THE MARK NAMES THE KIND, NOT THE INSTANCE — a Smith/EZ/trap/landmine bar is 'a barbell' at 24px, so the barbell family collapses to one drawing rather than four near-duplicates to keep in step; two lifts on the same gear SHOULD look alike (a fact about the pair, not a collision). exerciseMarkPaths(name) is ALWAYS non-empty so a tile can't render blank: a gym lift draws its implement, a catalog sport reuses the mark its own sport page signs with, anything else gets the neutral mark. WIRED as the fallback inside the media tile (exerciseMedia's 'thumb' variant), so every surface shows the hand-drawn demo once that lift is drawn and the implement mark until then: both exercise pickers, the More → Exercises browse list, and — new — the BLOCK CARD HEADERS in the builder and the live logger on both clients, where the mark replaces the redundant mono 'STRENGTH' word (the tint carries modality exactly as that word's colour did; the kind stays on the element as its accessible label). Sports keep their catalog glyph. 6 unit tests pin the gear→mark mapping, the family collapse, the always-non-empty guarantee, and the degenerate single-arc circle that silently renders nothing. FOLLOW-UP: the initials outlived the tiles that drew them. `exerciseInitials` and ExerciseBrowseEntry.initials stayed in core after this change and were still being computed for every browse entry a year later, with the last reader — the pin-exercises sheet — only converted when the square-avatar sweep reached it. Both are now deleted. The lesson is the field, not the function: retiring a DRAWING leaves the data behind it looking harmless, and the next tile finds it." },
  { id: "exercise-room-marks", area: "Core", status: "shipped", title: "Picker rooms — the body they train, not their initials", detail: "The lift rows took implement marks (exercise-implement-marks); the picker's ROOMS grid — the muscle-group tiles you tap before the list — kept initials, which were worse here than on the rows: the helper takes the first letter of each word and truncates to two, so 'Chest' and 'Calves' were both 'C', 'Back' and 'Biceps' both 'B', and 'Quads & Glutes' / 'Abs & Core' / 'Olympic & Power' came out as 'Q&' / 'A&' / 'O&' with a dangling ampersand. A room is a muscle group, not a lift: it has no implement, so its mark is the BODY, with the muscles that room trains lit on it. NEW roomBodyMark(names) in body-map.ts reuses everything the How-it's-done sheet already draws — the schematic mannequin outline, the per-muscle polygon regions, and MUSCLE_SIDE — and is driven by the ROOM'S OWN EXERCISE LIST rather than a hand-typed category→muscles table, so it cannot drift from the catalog and works for every room: the built-ins' pattern buckets, the admin library's muscle groups, and whatever an admin adds later. Each lift's prime movers count by their order of importance, assisting muscles at a fraction; the totals pick the FIGURE (one, not two — at tile size a front/back pair would be ~12px each, and the side carrying more of the room's work is the one an athlete pictures: Chest → front, Back/Triceps → back) and set each muscle's glow as its share of the room's top mover. A 0.25 floor keeps the mark legible — a dozen faintly-lit regions read as a smudge, not as a group (guarded by a test that caps the lit count and asserts every lit muscle lives on the figure being drawn). Rendered at 32px inside the 40px tile by one component per client (components/aurora/body-mark.tsx, web + mobile): silhouette + head in the hairline tone, regions filled in the modality accent at 0.35 + 0.65 × intensity, labelled with the room's top mover for screen readers. Sports rooms keep their catalog glyph; a room holding no lift the DB knows returns null and falls back to its initials, so nothing can render an unlit body. 3 unit tests." },
  { id: "exercise-media-layer", area: "Core", status: "shipped", title: "Exercise demo media — the resolver, the placeholder and the swap seam", detail: "The ONE answer to 'what picture do we show for this lift', written once in core (exercise-media.ts) and rendered by BOTH clients (components/aurora/exercise-media.tsx, web + mobile). exerciseMedia(name) ALWAYS returns a resolution — never null — with `asset` set once art exists and `fallback` carrying the procedural stick-figure demo to draw until then, so a surface can't accidentally render nothing. Four drawn shapes: still (one drawing), loop (keyframes cross-faded, OR one self-animating WebP/GIF — a single entry in `frames`), clip (mp4/webm, played inline on web), and link (a hosted page an admin pasted — poster + open-out rather than a broken embed). Resolution order: the admin library row for the lift (Exercise.videoUrl / thumbUrl, published via setExerciseMediaCatalog from both clients' catalog fetchers — /api/exercises now returns thumbUrl too) → SKETCH_MEDIA under the exact name → SKETCH_MEDIA under the lift's ARCHETYPE (one drawing of 'a squat' stands in for every squat, status 'pattern') → the placeholder. WIRED: the How-it's-done sheet's demo on both clients now renders through it (the framed hero, tagged 'Sketch pending' / 'Pattern sketch' in en/pl/de so the stand-in reads as deliberate), and every tile that names a lift — both exercise pickers, the More → Exercises browse list, and the builder + live-logger block-card headers, on both clients — renders through the 'thumb' variant, which shows the drawing once it exists and the lift's IMPLEMENT MARK until then (see exercise-implement-marks), so those surfaces upgrade to the art the moment it lands. COMMISSIONING: mediaSlot(name) is the stable filename contract ('Back Squat' → back-squat), and sketchShotList() / sketchCoverage() / sketchBrief() turn the exercise DB into the illustrator's brief (every undrawn lift with its slot, archetype, equipment and measure, grouped by archetype so one sitting covers a whole pattern) — surfaced in the admin console's Content → Media on BOTH clients (coverage bar + pending-by-pattern chips + copy/share the brief). Shipping the real art is then: drop the files, call registerSketchMedia with the manifest — no client change. 12 unit tests pin the resolution order, the library URL→shape mapping, the slot rules and the coverage maths." },
  { id: "exercise-video-library", area: "Web", status: "blocked", title: "Exercise demo library — the hand-drawn art itself", detail: "Per-exercise demos + coaching cues a beginner needs for confidence and a coach needs to stop re-explaining. The PLUMBING is done and shipped (see exercise-media-layer): the resolver, the four media shapes, the registry, the admin-library overlay, both clients' renderers, the picker thumbs and the commissioning brief. Every lift currently resolves to the procedural stick-figure placeholder. What's left is the ART — the demos are to be HAND-SKETCHED, one drawing per lift (241 in the built-in DB, spread across 20 movement archetypes), which is a commission, not a code change.", blockedBy: "The hand-drawn sketches themselves (an illustrator working through sketchBrief()'s shot list), plus hosting for the delivered files — the admin media library + Supabase Storage bucket already covers hosting. Register a delivery with registerSketchMedia(), or point a lift at an uploaded URL from the exercise CMS." },
  { id: "exercise-media-video-mobile", area: "Mobile", status: "planned", title: "Inline video playback for exercise demos on mobile", detail: "Web plays an mp4/webm demo clip inline in the How-it's-done sheet; mobile shows its poster and opens the clip out instead, because the app carries no native video player. Hand-drawn demos are expected to ship as stills and animated loops (which Image already renders natively on both clients), so this only bites if a real video clip is ever registered for a lift. Closing it means adding expo-video and a native rebuild — deliberately deferred rather than taking a native dependency for a format the art isn't using." },
  { id: "nutrition-fooddb", area: "Data", status: "shipped", title: "Food database search + barcode lookup (Open Food Facts)", detail: "The frictionless food-search path on top of nutrition logging is now LIVE on Open Food Facts — the free, open, no-key food database. Core (@hybrid/core/nutrition-off.ts, pure + unit-tested) normalizes OFF's loose product JSON to one OffFood shape (name, brand, serving, kcal + P/C/F, preferring per-serving nutriments and falling back to per-100 g) and owns the endpoint URL builders (offSearchUrl text search, offBarcodeUrl by EAN/UPC, isLikelyBarcode). A thin auth-gated server proxy /api/nutrition/search (GET ?q= text OR ?barcode= — a numeric query auto-routes to barcode lookup) adds the required User-Agent, bounds the upstream call (8 s AbortController), and returns normalized foods (graceful empty → the client falls back to manual entry). The 'My foods' sub-screen on BOTH clients is now SEARCH-FIRST: one box (text or barcode) → debounced results as rows (brand · serving · macros) each with LOG-to-today (writes the same energyIntake/protein/carbs/fat signals, source 'off') or a bookmark SAVE-to-library (POST /api/nutrition/products, respects the free FREE_PRODUCT_LIMIT cap → upgrade). Below sits the user's saved foods + a redesigned manual builder (labelled colour-coded macro tiles + a live 4·4·9 kcal readout) for anything the DB lacks. PORTION & QUANTITY: logging any item — a search hit, a saved food, or a saved meal — opens a shared editor sheet (the app Sheet, web + mobile) with a serving × quantity stepper (0.5 steps + a typed value); the calorie total and every macro RECOMPUTE LIVE as you dial, and the scaled amounts are what get written (a search hit can also be Saved to the library from the same sheet). This is the tracker-standard portion control the flat form lacked. Web + mobile at parity; i18n EN/PL/DE. Live CAMERA barcode scanning (point-and-scan) is a separate native follow-up — see nutrition-barcode-camera. TWO-TIER (Jul 2026): the same search box now returns HYBRID Verified items ABOVE the community results (see nutrition-verified-foods), and every hit carries the full label panel — saturates, sugars, fibre, salt (see nutrition-label-panel)." },
  { id: "wearables", area: "Integrations", status: "blocked", title: "Wearable sync (WHOOP / Oura / Garmin / Apple)", detail: "Connector layer built: core registry + normalizers (parseWhoop/parseOura/parseHealthKit → Signal[], unit-tested), prisma Connection model + reference/sql-connection-table.sql, OAuth start/callback + sync routes (/api/connect/[provider]), /api/connections, and a Connections hub screen (mobile parity: app/connections/). Each provider writes into the Signal ontology without touching the engines. OAuth providers show 'setup pending' until creds are set. Apple's native half is now built — tracked separately in apple-healthkit.", blockedBy: "Per-provider OAuth credentials in Vercel env (e.g. WHOOP_CLIENT_ID/SECRET, OURA_CLIENT_ID/SECRET) + NEXT_PUBLIC_SITE_URL." },
  { id: "connections-provider-pages", area: "Integrations", status: "shipped", title: "Connections — a dedicated page per provider", detail: "Every provider in the Connections hub opens its OWN page: web uses the same focus-page pattern as the exercise page (components/aurora/connection-page.tsx inside the connections screen), mobile a real route (app/connections/[provider].tsx). The hub is now a pure directory (status chip + provides + Open); connect/sync/disconnect actions and a recent-data list (latest Signal rows from that source) live on the provider page. Built as new files beside the hub so the change is trivially revertable. EN/PL strings." },
  { id: "apple-healthkit", area: "Mobile", status: "blocked", title: "Apple Watch / Health native sync (HealthKit)", detail: "The apple connector's native half, isolated in apps/mobile/lib/healthkit.ts + the Apple provider page: @kingstinct/react-native-healthkit (Nitro, new-arch — the first pick, react-native-health, compiled after patching but its legacy NativeModule never registers on RN 0.85, which removed the legacy architecture), permission request, 30-day HRV (queried in ms, daily mean) / resting-HR / nightly-sleep-hours reads, relayed to the EXISTING /api/connect/apple/sync (parseHealthKit → Signal rows, deduped by the userId+kind+ts+source index) — plus a new /api/connect/apple/register that records the Connection row so the hub shows a real status, and lastSyncAt now stamps for apple syncs too. Degrades gracefully wherever the native module is absent (Expo Go / Android show the install hint). Signing self-heals in the mobile-release workflow (enables the HealthKit App ID capability + deletes stale provisioning profiles via the App Store Connect API); plugins/with-plain-healthkit.js keeps the entitlements to plain HealthKit only.", blockedBy: "Verify on-device with the next TestFlight build: Connections → Apple Watch / Health should offer Connect Apple Health (not the install hint); grant the 3 read permissions → Sync now → Signal rows appear with source=apple. Then flip this to shipped." },
  { id: "device-workout-match", area: "Mobile", status: "blocked", title: "Match a logged workout with the device's recording (Apple Watch)", detail: "Open any workout summary and pull the watch's read of that EXACT session onto the row: the hero panel carries a 'Match the workout from your watch' button → a sheet reads the HealthKit workouts around the session (queryWorkoutSamples ± the shared 24 h window — wide on purpose, quick-logged sports are stamped at log time, not play time), ranks them in core (rankDeviceWorkouts: time proximity + duration similarity, best match tagged), and one tap PATCHes the frozen DeviceWorkout onto Session.device — everything the recording carries: uuid, activity label resolved on-device, interval, measured duration/kcal/distance, avg+max+min HR, steps, elevation ascended, swim strokes, flights climbed, average METs, indoor flag, weather temperature, source. The picker's best candidate renders as THE card (lime-washed, tagged, its own match affordance) so the choice reads at a glance — server-sanitised (sanitizeDeviceWorkout) and matchedAt server-stamped. THE MEASUREMENT WINS, INCLUDING IN THE ENGINES. Once a session is matched, the device's read IS the session everywhere — never just on the summary. Session-level: done-receipt takes its duration, distance and climb; energy.ts takes its kcal (basis 'device', and the figure drops the '~' because it is no longer an estimate); the sRPE load model scales to the measured minutes so load ÷ minutes stays the mean RPE; the nutrition fuel bump eats for the measured burn; the wrist's avg HR joins the summary tiles and its max HR the premium facts. Block-level: core/device-truth.ts projects the recording onto the session's activity block (`deviceTrueSession`), and every engine that walks blocks applies it at its entry — toTrainingLog (so tissue fatigue, injury risk, performance state and readiness all read measured minutes/distance), running/endurance totals + weekly mileage + pace splits, cardio PRs, per-exercise dashboards and widgets, the fitness-level 5 km-equivalent, weekly recaps and the social distance comparison, the History row headline and the session-detail breakdown. Attribution is deliberately conservative: exactly ONE timed block takes the recording; zero (a pure lifting session) or two or more leaves the blocks alone rather than splitting one interval by guesswork. Only measured quantities are projected — RPE, zone, stroke and the exercise name stay the athlete's. The server-side engine inputs (athlete-state, ai-coach, coach squad, social) now select Session.device for the same reason the `feel` column is selected there: dropping it would make the server reason about a different athlete than the app shows. What was typed in is the SECONDARY reading, kept on one surface: the 'Your device's read' panel on BOTH clients, 'You logged' in one column and the device's measurements in the other (both read with ignoreDevice so the same number can't print twice), with re-match + unlink (unlink works on web too). That panel speaks the ACTIVITY's own units so it can sit next to the watch face without translation — a pool swim reads '510 m' and '3:52 /100m' (not '0.51 km' at '38:36 /km'), rowing splits per 500 m, everything else per km — and the pace takes the recording's second-accurate `durationSec` rather than the whole minutes every engine reads, because 19:41 rounded to 20 min would print 3:55 against the watch's 3:52 — and the DURATION row shows that same measured time on the device's own clock ('19:41', '1:34:12' past the hour) beside the typed '20 min', since printing '20 min' in both columns invents an agreement neither side claimed. The app column's DERIVED figures (the modelled kcal, the pace computed from two typed numbers) wear the '~'; what the athlete typed does not, because a self-report is not a model. The bridge's unit conversion moved to apps/mobile/lib/health-quantities.ts — pure, no react-native import — and is pinned by apps/mobile's FIRST unit tests (health-quantities.test.ts, vitest), which assert the literal unit strings the native serializers emit ('meters' from a workout total vs 'm' from a statistic, 's', 'kcal', 'count', 'degC'). An unrecognised unit makes a metric vanish rather than throw, so that contract is now the thing under test. And because the stored read is a SNAPSHOT, a fix to what the native bridge understands would otherwise only reach future matches — so the Apple Health sync carries a repair pass (refreshMatchedWorkouts): it asks HealthKit for exactly the uuids already on the athlete's sessions (one query, no date window), re-normalizes each, and re-PATCHes only the rows whose read actually changed, leaving recordings the watch no longer holds matched rather than silently unlinking them. The device is also NAMED properly: the native read resolves the HKDevice name (then the source's true name via toJSON, then the product-type family), because this library hands the source back as a Nitro hybrid object whose own `name` is the class name — which is why the first build displayed the literal 'SourceProxy'. core's deviceSourceLabel/isDeviceName filter that junk on read as well as on write, so rows matched before the fix render as 'Apple Watch' rather than the bridge's class. Connect/permissions ride inside the sheet: the workout/HR/active-energy read types are asked on first open (and folded into the Apple Health connect for new users), and re-opening the sheet IS the re-sync. Shape, ranking, comparison rows and API sanitisation live in core/session-device.ts (pure, unit-tested); i18n EN/PL/DE. NATIVE CONSTRAINT (permanent, recorded per the parity rule): only the phone can read HealthKit, so the MATCH action is iOS-only — web renders the matched panel, can unlink, and points matching at the iPhone app. AND THE PICKER NOW KNOWS WHAT SPORT IT IS LOOKING AT. Ranking was decided on the CLOCK alone — interval proximity plus a duration nudge — so a Tennis recording scored exactly as well as the ride you actually logged in that hour, arrived lime-washed and badged 'Best match' with the big Match button on it, and the unattended import would have welded it onto the row. device-activity.ts is now the ONE place either half decides what an activity IS: a name resolves to a catalog sport, a coarse cardio modality or resistance training, and two identities agree when they share a name or share a MODALITY — so a session logged 'Bike intervals' recognises a recording labelled 'Cycling', 'Marathon' and 'Race Walking' both recognise plain 'Running', and walking folds into running outright (same feet, and the log and the watch disagree freely about which word applies to a ruck, a hike, or a jog that spent half its minutes walking). The one modality that does NOT unify is 'sport', which is not a modality at all but the catch-all for a catalog sport tracking no distance: Tennis and Table Tennis are both 'sport', so two different timed sports never fall through to it and stay different. The verdict is THREE-VALUED on purpose — a watch label is often vaguer than the log ('Other', 'Mixed Cardio') and a title is often free text ('Morning session'), so 'can't tell' is a real answer and is treated exactly like agreement; refusing a match we merely failed to understand would be worse than the bug. Only a genuine contradiction costs anything: the picker multiplies the score by 0.35 (a multiplier, not a subtraction, so ordering WITHIN the wrong-sport set is preserved — if tennis is all the watch has, tennis still leads), never lets it be THE card, and labels it 'Not the sport you logged' in amber (EN/PL/DE), while the unattended attach in planDeviceImport refuses it outright and lets the recording land as its own session instead. Unit-tested in device-activity.test.ts.", blockedBy: "Session.device is APPLIED in production (sql-session-device.sql run in the Supabase SQL Editor, Jul 2026) — the DB side is done. Matching itself is VERIFIED on-device (Jul 2026, TestFlight): a manually logged tennis session matched its watch recording. What's left is one more TestFlight pass on this change: after matching, the summary must headline the device's duration/kcal (94 min, 677 kcal — not the typed 90 min, ~860 kcal) and the chip + comparison header must read as the device signature (see device-signature-mark) rather than 'SourceProxy' or the retired ⌚ emoji. ALSO on that pass: a matched POOL SWIM must now show DISTANCE and PACE in the device column (they read '—' in the Jul 2026 build — the bridge serializes a workout's totalDistance with the literal unit string 'meters' while the converter only accepted 'm', so every measured distance was dropped, taking the derived pace with it). The reference recording is 510 m in 19:41 → the panel must read '510 m' and '3:52 /100m', matching the watch's own summary exactly. And because Session.device is a snapshot frozen at match time, the SESSIONS ALREADY MATCHED must be repaired by the same pass: Connections → Apple Health → Sync now re-reads every stored uuid straight from HealthKit and re-PATCHes the ones whose read changed, reporting the count ('N matched workouts re-read from your watch') — confirm an old swim gains its distance + pace without the athlete re-matching it by hand. Also on that pass, the WRAPPED panels must now agree with that comparison table rather than reading a rounded version of it — the same swim's hero and stat strip must show 510 m and 3:52 /100m (they showed 500 m at 4:00 /100m, and a swim pace PR headlined '39:13 /km'); see session-wrapped-measured-precision. Then flip to shipped." },
  { id: "device-import", area: "Mobile", status: "blocked", title: "Import a workout the device already recorded (auto-sync)", detail: "The other direction from device-workout-match: the training STARTS on the wrist and the app pulls it in, with every value the recording carries and nothing typed. Every log surface leads with it — the quick-log carousel on BOTH clients opens with an 'Import from your watch' bar above the sport grid, because if the watch already recorded it there is nothing to type. The mobile sheet reads the last 14 days (queryRecentDeviceWorkouts, the same normalizer as the match read), plans them in core (planDeviceImport) and shows what a tap will DO before it does it: a new recording adds a session, one that IS a session the athlete already logged joins that row instead of duplicating it, and ones already in the log sit greyed at the bottom as proof of what synced. Rows are individually excludable — the watch calls a six-minute walk to the shop a workout. THE DOUBLE-COUNT PROBLEM is the whole design: dedupe is decided SERVER-side against the database (POST /api/sessions/import-device runs the same shared plan), so an auto-sync that retries on a flaky network, or a second phone syncing at the same moment, cannot write the same session twice — recordings are matched on the health store's own uuid, and the attach write is guarded on the row still being unmatched. Pairing is one-to-one and greedy on closeness, and it is deliberately conservative: an interval-logged session absorbs a recording only on ≥50% overlap, a point-logged one (the quick-log sheet stamps startedAt == completedAt hours after the game) needs BOTH a stamp within 3 h and a duration agreeing within 30% — anything short of that imports as its own session, because a duplicate the athlete can delete is recoverable and a recording welded onto the wrong session is not. Imported sessions land at the TRAINING's time, not the sync's, titled by the catalog sport the activity maps to (HealthKit's ambiguous enums — hockey, skatingSports — keep the device's own wording rather than guess), carrying a stamped discipline and the recording on Session.device, so THE MEASUREMENT WINS downstream exactly as for a hand-matched session. AUTOMATIC is the point of the ask: loggerPrefs.deviceAutoImport (off by default, on both clients' logger settings and inside the sheet) makes every foreground pull new workouts in silently — throttled to 30 min, never opening an unprompted permission sheet, silent on failure. Pure + unit-tested in core/device-import.ts; i18n EN/PL/DE. NATIVE CONSTRAINT (recorded per the parity rule): a health store is native, so the READ is iOS-only — web ships the same entry point, the same auto-import switch and the receipt (every session carrying a device read, with its measured figures), and says plainly that the pull happens on the phone. PROVIDERS: the sheet ends with a 'Where it reads from' strip driven by DEVICE_IMPORT_PROVIDERS — Apple 'Ready', Garmin 'Not connected yet' — so an athlete hunting for their Garmin gets an answer instead of importing nothing and guessing why. Garmin is a real placeholder, not a stub: its logo is now traced into device-marks.ts (wordmark lockup + the delta as the compact glyph, ® dropped for legibility per the Apple precedent), deviceSourceLabel already names it, and a Garmin DeviceWorkout plans, blocks and imports through exactly the same code as an Apple one (unit-tested) — the only missing piece is a reader. Nothing downstream branches on the provider, so the day the credentials land, no planning code changes. NO PHANTOM 'YOU LOGGED': an import-created session's block is the recording, copied — so the summary's device panel must not read those figures back as what the athlete typed. deviceImportedSession (core, unit-tested) recognises the echo — the session's interval is the recording's to the millisecond AND the block figures are the recording's, unedited — and both clients then render the panel single-column (measured only, with its own lead + footer copy, EN/PL/DE). Decided from shape, not a stored flag, so sessions imported before the fix heal too, and an athlete's genuine edit to a figure brings the logged column back. AND THE IMPORT NOW ENDS IN THE ONE QUESTION THE WATCH CANNOT ANSWER. A recording carries every figure of a session except how hard it FELT — the value session load, ACWR and every risk read are built from (session-feel.ts) — so an imported row landed unrated, and the only place to answer was the Wrapped's panel, six full-screen panels down a summary the athlete has to go and open. Nobody does that, so in practice every imported session stayed weightless in the models. Three changes fix it, all sharing the one existing instrument (feel-prompt.tsx): (1) the import route now returns `landed` — the rows it created or joined, with the DEVICE's moving time as their trusted minutes and a `rated` flag — and the mobile sheet finishes on a rate phase instead of closing, one prompt per session, the way out reading 'Not now' until an answer lands and 'Done' after; (2) both clients' done floor gives an unrated row a bare lime 'Rate' that opens the new shared feel-sheet.tsx (the same prompt with the session's name over it), which also catches the SILENT auto-import path where there is no sheet to end on; (3) the web import panel's receipt offers the same word on any unrated row, so the phone-only READ does not make rating phone-only too. `isRated` / `unratedSessions` in feel-schedule.ts define 'unrated' once for both clients — deliberately a different question from `hasImmediateRead`, which asks whether the read the SCHEDULE wanted was taken in its window; a late answer misses that and still fills the load model's hole. WHY THE SHEET SOMETIMES READ EMPTY WHILE THE MATCH PICKER DID NOT — three ways the import read was strictly weaker than the per-session match read, all fixed, and all of them looked random from the outside because each depended on what happened to be in the fortnight. (1) ONE BAD RECORDING COST THE WHOLE LIST: readWorkouts wrapped the entire normalize loop in a single try/catch, so a proxy that threw mid-read (a date the bridge could not hand over — new Date(junk).toISOString() throws — a statistic rejecting synchronously, an object the store had released) returned null for EVERYTHING, and the sheet showed its error line. The import reads 14 days where the match sheet reads one day either side of a single session, so the import met the bad recording many times more often. The query failing is now the only thing that means unreachable; every recording past it degrades on its own, dates go through an isoOrNull that returns null instead of throwing, and refreshMatchedWorkouts (which sweeps the athlete's WHOLE matched history, so it was even more exposed) got the same treatment. (2) THE WINDOW ENDED AT NOW: the import read [now − 14 d, now] while the match read reaches DEVICE_MATCH_WINDOW_H in BOTH directions, so a recording stamped even seconds ahead of the phone's clock — the paired watch's clock is not the phone's — fell outside the import's window and inside the picker's. The import window now runs a day past now; a recording cannot be too new to import. (3) SHORT RECORDINGS WERE INVISIBLE, NOT DECLINED: planDeviceImport dropped everything under DEVICE_IMPORT_MIN_MIN before the sheet ever saw it, so a brief swim or sprint session read as 'your watch recorded nothing' — while the match picker offered that same recording. The floor now belongs to the UNATTENDED path only (planDeviceImport takes minMinutes, defaulting to it, and the server keeps the default so auto-import still never writes a walk to the shop); the sheet plans with minMinutes 0 and renders the brief ones switched OFF, saying why, one tap from being included.", blockedBy: "Verify on-device with the next TestFlight build: the quick-log's 'Import from your watch' bar should list the last fortnight of Apple Watch workouts with the right plan per row, importing should create/attach exactly once (re-open the sheet — everything should read 'Already in your log'), an imported session's summary panel should show the measured column ALONE (no 'You logged' column of echoed figures), and with auto-import on a fresh watch workout should appear on Home without a tap. Everything else — the plan, the server route, the web half — is done and testable in CI. Garmin is listed but deliberately inert: it needs the OAuth credentials tracked under `wearables`, not new import code. And confirm the empty state is now honest: a sub-5-minute recording should be LISTED and switched off (saying it is too brief), not absent, and a workout the summary's match picker can see must never be missing from the sheet. FIRST DEVICE REPORT (Aug 2026): the app EXITED on the import — a process abort, not a JS error, so nothing reached the error boundary and nothing was left to read. The import worked on the build before #449, which narrows the cause to exactly two things that commit added to this path and nothing else: the read behind the sheet (readWorkout, workoutDistanceKm, queryRecentDeviceWorkouts) is byte-identical to the working build. NEW ON THE TAP: a SECOND requestAuthorization (requestStreamReadAuth) fired straight after the first, so one tap raised a system permission sheet while the previous one was still dismissing, over two of the app's own stacked modals. NEW ON THE IMPORT: uploadLandedStreams, which reaches HKWorkoutRouteQuery for the first time in this app's history. Three things went in for it and all three need the device to confirm. (1) `@kingstinct/react-native-healthkit` is now PATCHED (patches/, gated by lib/healthkit-patch.test.ts): its `getRouteLocations` ran `try!` over a CheckedContinuation, so a route the store would not hand over — iCloud not downloaded, a locked store, a second callback from a route still being written — called fatalError() and took the process, on the stream read the import fires the moment sessions land. (2) The tap path asks for the WORKOUT types and nothing else — byte-identical to the request that shipped when the import worked — and the stream types are asked for at the point of use, inside readWorkoutStreams, after the sessions have landed. That is deliberately mechanism-agnostic: whether the abort was two system sheets overlapping or the route type in the list itself, neither can be reached from the tap now, and both live inside the one span the watchdog switches off for good. Pinned by lib/healthkit-auth.test.ts. (3) No device read runs until a sheet has actually put those types to the store (`hybrid.healthkit.askedTypes`, which records HOW FAR the ask got) — querying a type the app never requested is the library's own documented crash, and the two unattended paths (auto-import on foreground, the sync's stream backfill) could not ask. Auto-import and the sync's repair pass therefore no-op until the athlete has opened the import or match sheet once — safer than the pre-existing behaviour, which queried workout types that an athlete who connected before this feature had never been asked for. If it STILL exits, the import sheet now names the span it died in (lib/healthkit-watchdog.ts writes what is in flight to disk and reads it back at the next launch) — that line is the report to send. SECOND DEVICE REPORT (Aug 2026, on the build above): the sheet opened, the fortnight listed, the import LANDED — the sessions were in the log at the next launch — and the app went as it finished. That is the first hard fact about this crash: moving the stream half off the tap moved the crash with it, so the fatal call is inside the stream half itself (the ask for the route + cycling types, or a read behind it), not in the two permission sheets overlapping. It also means the import itself, and every read the tap makes, are clean. So the trace stops being something the app does to an athlete on the tail of an import. (1) NOTHING ASKS FROM A READ PATH: requestStreamReadAuth is called from ONE control — the import sheet's 'Full recordings' row, which says what it fetches — and never from readWorkoutStreams (healthkit-auth.test.ts fails if that returns). (2) NOTHING UNATTENDED GOES FIRST: the sync's backfill and the auto-import's stream upload wait for `hybrid.healthkit.streamsProven`, set by the first trace read that RETURNS in front of the athlete who asked for it; a crash in front of somebody who just tapped 'fetch' is a feature that didn't work, the same crash on a foreground sync is the app closing for no nameable reason. (3) THE GATE IS THE STORE'S ANSWER, NOT OURS: every read now checks getRequestStatusForAuthorization for exactly the types it is about to query (storeHasAsked) instead of a local flag written after calling requestAuthorization — the bridge silently drops identifiers it cannot build, so the two claims come apart, and the gap is the library's documented crash. The askedTypes flag is gone. (4) THE SPAN NAMES ONE CALL: 'streams' covered a permission sheet, a sample query and a route query, so a marker naming it named three suspects; it is now stream-auth / stream-read / stream-route, the markers are a stack (nested spans no longer clear each other), a fault quarantines only its own call, and a fault left by the previous build under the old name implicates all three so the new build cannot cheerfully re-run what already killed it. (5) LESS SURFACE: cycling power and cadence are queried for cycling recordings only — a lift was being asked for its power meter, on the one path the app has twice disappeared inside. What the next build has to answer: import (expect the sessions to land with no trace and no second permission sheet), then tap 'Full recordings' — if THAT is where it goes, the marker at the next launch names stream-auth, stream-read or stream-route, and that is the whole diagnosis. NOTE FOR THE TESTER: the phone that met the last crash carries its marker, so the trace row will read 'Try anyway' rather than 'Fetch' — implicated spans are skipped until asked for by name, and that tap is the ask (it clears the trace spans' record only). Also closed with it: connectHealthKit no longer includes the series types (one function names them now), and matching a workout no longer uploads a trace until one read has returned on this phone — so the FIRST encounter with the suspect call can only happen under that one row. THIRD DEVICE REPORT (Aug 2026, on the build above): the app closes on the import again — reported as 'it crashes when I click import from device', which does not distinguish the tap that OPENS the sheet from the tap that imports, and after two builds of moving spans around that ambiguity is itself the finding: the watchdog was supposed to have removed it, and it could not, because the FIRST native call the tap makes was the one call in the bridge with no span on it. Two defects, one in the gate the last build introduced. (1) THE GATE READ 'ALL ASKED' OUT OF 'NOTHING RESOLVED'. storeHasAsked believed getRequestStatusForAuthorization === unnecessary meant every type in the set had been put to the store. It does not: the bridge's objectTypesFromArray swallows every identifier it cannot build (four of the nine distance identifiers in WORKOUT_READ_TYPES are iOS 18 additions, so the set reaching HealthKit is routinely smaller than the one handed over) and getRequestStatusForAuthorization then SHORT-CIRCUITS on an empty set, warning 'Both toRead and toShare are empty' and returning unnecessary. So unnecessary meant either 'all asked' or 'none resolved', the gate answered 'safe to read' for types the store had never heard of, and the next line queried them — the library's own documented crash, reached THROUGH the guard written to prevent it. This is the exact hole the last build diagnosed in the flag it replaced; the replacement inherited it one layer down. Fixed by probing what the bridge can build first (areObjectTypesAvailableAsync) and asking the status only about that subset — the resolvable SUBSET, not all-or-nothing, because requiring the whole list to resolve would take the import out on every iOS the newest distance identifiers predate. An empty subset now reads as 'not asked' and refuses the read. (2) THE GATE ITSELF WAS UNNAMED, AND IT RUNS FIRST. storeHasAsked called native bare, so a process that died inside it left an empty in-flight marker: no fault, no fault line on the sheet, nothing at the next launch — which is why two builds of markers taught nothing about a tap whose first act was this call. It is now the 'auth-status' span. (3) AND THE TAP WAS STILL TOUCHING THE SERIES TYPES. The sheet's open called streamReadGranted(), which is getRequestStatusForAuthorization against HKWorkoutRouteTypeIdentifier and the two cycling types — not a permission sheet, so every rule the last build pinned let it through, but a native call against the series types on the exact tap being reported. It was also unnecessary: 'proven' is only ever written by a trace read that came back, and that read is behind the grant, so proven implies granted and an unproven phone cannot be in the state the sheet is asking about. streamReadState reads proven (one AsyncStorage get) and reaches the store only when it is already true. Net effect: every native call the import tap can make is now a named span, and the read gate can no longer be satisfied by a set the bridge dropped. All four new invariants are pinned in lib/healthkit-auth.test.ts. HOW REACHABLE IS (1), HONESTLY: for the empty-set short-circuit to fire, EVERY identifier in the set would have to fail to resolve, and HKWorkoutTypeIdentifier and HKQuantityTypeIdentifierHeartRate resolve on every iPhone — so on this athlete's device the trap is almost certainly NOT reachable, and (1) is therefore probably not the crash. It is a real defect on the crash path and it is closed; it is not a diagnosis, and calling it one would be the third time this bug has been declared fixed from a plausible reading of the source. The honest state: the reachable change is (3), which removes a native call against the series types from the tap, and the change that will actually END this is that every native call the tap can make is now a named span. (4) AND THE WITNESS ITSELF WAS WRONG, which is the reason the last two builds' markers were worth so little. nativeSpan left its frame with stack.pop(), which removes whatever frame is LAST — so two overlapping chains (the sheet's gate while the auto-import's foreground pull is still reading) had the first to finish delete the OTHER's frame. The marker then named a call that had already returned while the one still running had none: the original single-key bug the stack was built to fix, in a subtler form, and it sends the next build to harden an innocent call. Each span removes its own frame by identity now. And the watchdog is TESTED — lib/healthkit-watchdog.test.ts, 14 cases, in the pure project: nothing in it touches a native module (a stack, a JSON blob, one AsyncStorage key), so unlike everything it watches it can be RUN. The concurrency case was verified to FAIL against pop() before the fix went in, reporting 'auth-status' as in flight while 'workouts' was the call still running. WHAT THE NEXT BUILD HAS TO ANSWER: if the import works, say so — but the cause will be (1) or (3) and the marker cannot tell us which, so do not close this entry on it. If it STILL closes, the marker at the next launch names the call, 'auth-status' is now among the names it can say, and a concurrent span can no longer erase the frame that matters. That line is the report to send, and it is the first one that can be trusted." },
  { id: "device-signature-mark", area: "Core", status: "shipped", title: "Device signature — the matched device's own mark", detail: "A matched session no longer signs itself with a ⌚ emoji — which renders as a different object on every platform, has no equivalent for a strap or a head unit, and cannot carry a manufacturer's logo. core/device-marks.ts holds the artwork as shape data normalised to a 100-unit-tall viewBox: the Apple Watch lockup and the Apple mark alone, plus Garmin’s wordmark + its delta glyph, and HYBRID’s own watch/band silhouettes for the connectors whose logos we don’t ship yet, mapped provider → drawing by deviceMarkFor() beside the deviceSourceLabel() that already resolves the device’s NAME. EVERY LOGO READS AT THE SAME SIZE, which the first cut got wrong: scaling each drawing so its BOUNDING BOX fills the box is the obvious move and it is incorrect, because the boxes hold different things — Apple's is bounded by the apple (36% above its letters), Garmin's by the delta (90% above), so at one requested height Apple's letterforms came out 1.39x Garmin's, one logo visibly bigger than the other in the same row. Drawings are now normalised on what the EYE measures: a wordmark on its CAP HEIGHT (DEVICE_MARK_CAP), with the cap band parked at the same y in every wordmark (DEVICE_MARK_CAP_TOP) so logos share a baseline as well as a size and the ascenders live in headroom above; a glyph on DEVICE_MARK_GLYPH, the geometric mean of its ink area's square root and its bounding height — neither alone works, since a triangle and an apple of equal HEIGHT don't read as equal size while a flat band and a tall watch of equal INK don't either. Each art carries its `kind` and `optical`, and the suite fails if a new logo is added by eye instead of by measurement. Two forms: the lockup wherever the device is being named (Wrapped hero chip, the device panel’s measured column header, the match sheet title), the compact mark wherever it is merely being counted (a candidate card, the match CTA). THE ONE RULE, and the reason the module exists: a manufacturer’s logo reproduces in solid black or solid white and nothing else — no accent, no gradient, no opacity, no stretch. So neither renderer takes a `color` prop; callers name the SURFACE and the ink comes from DEVICE_MARK_INK (the brand’s chalk and ink, unit-tested to contain no accent and to meet AA on the ground it names), and size is height-only so the width follows the artwork and the aspect ratio survives any layout. The knock-on is a colour grammar worth keeping: on a matched session WHITE means ‘the device said so’ (the lockup, the words on its line, the measured column) and lime means ‘HYBRID said so’ — which is why the hero chip and the device panel’s measured figures moved off lime. Copy reads ‘Measured on’ + the lockup, a fragment the artwork finishes, so the device is never named twice. Renderers: apps/web/components/aurora/device-mark.tsx (inline SVG) and apps/mobile/components/aurora/device-mark.tsx (react-native-svg) drawing the SAME path data, so both clients are identical at every size; a connector with no artwork falls back to naming the device in text." },
  { id: "device-signature-placements", area: "Core", status: "planned", title: "Device signature — the remaining surfaces", detail: "The mark ships on the Wrapped hero, the device panel and the match sheet (device-signature-mark). Four surfaces still show a matched session with nothing saying so: the SHARE CARD (a footer receipt — lockup then the timestamp, the only surface that leaves the phone, so it uses the product name and never the athlete’s personal device name), the LOGBOOK + HISTORY rows and the WEEK RAIL + CALENDAR (the compact mark leading a measured row, answering ‘which of these numbers do I trust’ without opening anything), and CONNECTIONS (the connected state showing the lockup and the last read instead of a generic row). Two further directions from the same pitch: per-stat PROVENANCE (the mark in the corner of each measured figure, opposite the ‘~’ estimate sign — needs WrappedBasic.estimate to become a three-way basis of measured/modelled/logged) and the HANDOFF motion on match confirm (the mark travelling from the chosen candidate into the hero chip, the chip then beating at the measured avg HR). Anything on a themed surface passes on=‘light’ so the mark inks black there — the Wrapped panels are dark whatever the theme does." },

  { id: "weekly-pace-device-truth", area: "Core", status: "shipped", title: "Weekly pace off the device's seconds", detail: "The last derived rate still computed from display-rounded minutes. paceEffortSplit, runStats and paceSeries all anchor on cardioSeconds; weeklyMileage() summed whole `minutes` per 7-day bucket and both week-pace sites divided by those — endurance-lanes' weekPace() (the lane's pace TREND) and sportPageModel's week series — so a trend could read seconds-per-km off the very efforts whose own paces were printed beside it, and off the zone card measured from the same buckets. WeekMileage now carries `seconds` (summed from cardioSeconds, the watch's where it recorded one) ALONGSIDE `minutes`: the bars and the time totals keep drawing whole minutes, only the DIVISION moved. Both week-pace sites read `seconds`. Rule of thumb the field docs now state: anything that divides by a week reads `seconds`, anything that displays a week reads `minutes`. Tests: a two-effort week whose typed minutes give 314 s/km and whose recording gives 308, and a two-week sport-page trend that reads 240/234 off minutes and 236/236 off the watch." },


  // ---- North-star roadmap (see reference/north-star-strategy.md) ----
  // Year 1 — System of Record
  { id: "athlete-twin", area: "Data", status: "shipped", title: "Athlete Digital Twin + Performance State", detail: "computePerformanceState fuses HPI + readiness + fatigue into one object WITH ranked 'why did it move' drivers (load + recovery attribution) and a one-line summary, fed by the persisted Signal ontology. Dedicated web Performance screen: HPI cockpit + 14-day HPI/readiness trajectory (performanceTrajectory replays the log) + tissue body map. A MOBILE Performance screen too (app/performance.tsx — HPI cockpit + pillar bars, a dependency-free 14-day HPI trajectory, and the tissue-level injury-risk table), reachable from the More hub + the cockpit. Also on the dashboard, mobile Home, and grounds the AI coach. Next depth: nightly materialization/caching." },
  { id: "injury-risk", area: "AI", status: "shipped", title: "Tissue-level injury risk + return-to-play", detail: "computeInjuryRisk scores every tissue 0..100 (per-tissue ACWR + absolute load + recovery suppression) AND maps it through a versioned logistic calibration to an injury probability (RISK_MODEL_VERSION; documented prior, refit when the data network has labeled outcomes). Surfaced as a body map + per-tissue table (risk, P(injury), ACWR, drivers). Return-to-play rails (rtp.ts): 5 gated stages (acute→…→return-to-perform), the engine blocks advancement until every gate is met; RtpProtocol model + /api/rtp + a checklist panel. The calibration is a LIVE refit loop: labeled outcomes captured (RiskOutcome — positives recorded when an RTP protocol opens, plus club records fed in), refitCalibration re-fits the logistic, persisted (ModelFit) and applied everywhere once ≥30 samples exist; admin Refit on the Data Network screen, with negative-sample snapshots (/api/datanet/snapshot) so the logistic trains on both classes. RTP is auditable: every gate attestation, advance, and override (reason-required, force-advance) is appended to an immutable audit log with who + role + timestamp. WEB↔MOBILE PARITY: the mobile Aurora Performance screen now matches web — the per-tissue table carries the ACWR + top-driver columns, the trajectory plots BOTH HPI (bars) and Readiness (per-day markers) via native primitives (no chart lib), and the full gated RtP panel (tissue picker → open → gate checklist → advance/override + audit trail) ships on mobile against the same /api/rtp. The one remaining web-only piece is the anterior/posterior tissue BODY-MAP SVG (deferred on mobile — needs react-native-svg, a dep the app deliberately avoids). PLAIN-LANGUAGE GUIDANCE: the per-tissue driver is no longer an opaque English template string — computeInjuryRisk now tags each RiskDriver with a machine-readable `kind` (spike/load/detrain/recovery) plus the ACWR behind it, and both clients resolve it through localized keys (RISK_DRIVER_LABEL_KEY / RISK_DRIVER_EXPLAIN_KEY → w.injury.* EN/PL/DE). The Cockpit injury card carries a collapsible 'What's raising this?' panel that explains each active driver in plain language with what-to-do copy (e.g. a 'workload spike' = last-7-days load well above your 4-week weekly average, ACWR past ~1.3), plus a one-line ACWR gloss under 'Things to watch'; the Analyze per-tissue table (web + mobile) shows the same localized driver label and the ACWR note. BUGFIX — the degenerate 4.00 ACWR: the per-tissue table reported ACWR 4.00 on EVERY tissue for any athlete whose logged sessions all sat inside the 7-day acute window. That is arithmetic, not a spike — for such a log chronic₂₈ equals acute₇, so acute / (chronic/4) = 4.00 exactly, the formula's ceiling, identical on every tissue regardless of what was actually trained. Worse, it handed each tissue the full 55-point 'workload spike' driver, so a brand-new athlete's first week read as HIGH risk everywhere. computeInjuryRisk now gates enoughHistory the way computeLoad already did: the log must span ≥14 days (MIN_HISTORY_DAYS) AND the tissue must carry load OUTSIDE the acute window (tissueLoadWindow gained a `from` bound) — the second half matters on its own, since a tissue trained for the first time this week has no baseline however long the athlete's overall history is. With no baseline the ACWR reports a neutral 1, both clients' tables show '—', and BOTH ratio-derived drivers (spike and detraining) are held at 0; absolute tissue load and recovery suppression still score, so a new athlete gets a real risk number, just not a fabricated one. The same leak is closed on the coach console: /api/coach/squad sent load.acwr unguarded, so a squad row read '4' beneath an 'insufficient' band — it now sends 0, which both team monitors already render as '—'. This also cleans the personalization evidence set (injury-personalization): maxAcwrAt / acwrEventsFromHistory were replaying those 4.00 phantoms as 'tolerated spikes' and pushing the learned onset toward its 1.6 cap. EXPLAINED, NOT JUST BLANKED: a bare '—' where a number used to sit reads as broken data, so computeInjuryRisk now returns `awaitingBaseline` (tissues trained in the last 7 days that have no chronic base yet — a tissue simply not trained is NOT listed, so a seasoned athlete never sees the note), plus `historyDays` / `minHistoryDays`. Both clients render a plain-language panel under the per-tissue table when it is non-empty (w.injury.acwrPending / acwrPendingBody, EN/PL/DE): why the dash is there, that a number would be arithmetic (every area would read 4.00), roughly two weeks until it resolves, and that the risk score itself is still real (tissue load + recovery, minus the ratio term). The engine owns the decision, so web and mobile can't drift. The coach console gets the counterpart: the team monitors (web + mobile) add a second note under the ACWR legend, shown only when a row actually carries the 'insufficient' band (w.teams.monitor.acwrInsufficient). ONE CARD — THE TISSUE MERGE (Aug 2026): injury risk and return-to-play were two sibling cards stating the same subject twice — a risk summary that showed no tissue, and an always-open seven-chip form with a chartreuse fill for an event that happens twice a year. They are now ONE card (aurora/tissue-card.tsx, web + mobile), whose SHAPE is the signal. The engine defines overall risk as Math.max of the per-tissue scores, so the headline number IS one of the tissues — which lets the card draw a single object at two zoom levels: collapsed, one 0..100 axis carrying a tick per tissue (the heaviest tick being the overall) with band zones at 30/50/70 and a dashed FLAG LINE at 50 where a tissue joins the worklist; opened, the same tissues as ROWS on the SAME scale, each with its own risk, calibrated probability and ACWR, the flag line running straight down the block. That replaced the 5-column table which sat behind the disclosure at minWidth 420 inside a horizontal scroller (a spreadsheet you dragged sideways on a phone); the driver now prints only on flagged rows instead of being a column of dashes. The geometry is shared — tissue-axis.ts (tissueAxis / RISK_ZONES / FLAG_THRESHOLD / injuryHeadlineKey, 18 tests) — so neither client rounds its own tick positions and both speak the headline sentence from one key. Rows are a disclosure only while calm: a flagged tissue opens them itself. RTP is no longer a card but this card's deep end — the tissue chips appear only AFTER 'Log an injury' is pressed, and an open protocol (gates, advance, reason-required override, audit trail — all unchanged) takes over the card's body. Kept on the card face in every state: per-tissue risk, P. injury and ACWR, plus the model version, which keeps its footer line. Two honesty fixes fell out of the merge: the per-tissue ratio and the whole-body ACWR from computeLoad are different numbers, so the watch tiles are now labelled 'whole-body, not per tissue'; and the awaiting-baseline explanation sits directly under the rows where the dash appears. New copy in EN/PL/DE (w.injury.tissue / line.clear|elevated|high / allTissues / wholeBody / logInjury / pickArea, w.rtp.*); the console-cased 'RETURN-TO-PLAY – GATED PROTOCOLS' heading, the 'Things to watch' label over its own fallback sentence, and the 'No active protocols' empty state are retired. THE PROTOCOL, REDESIGNED (Aug 2026): the return-to-play surface was the last part of this capability still shaped like a form, and it is now rebuilt on both clients (aurora/protocol.tsx, web + mobile; presentation logic in @hybrid/core rtp-view.ts, geometry in injury-body.ts). ASKING: \"which area is hurt?\" was a ragged wrap of seven word-chips with one PRE-SELECTED — a question that arrived already answered, in our vocabulary rather than the athlete's (\"Posterior\"), under a chartreuse GO fill on the worst news an athlete ever files. It is now a SHEET showing a body you touch (see injury-body-map-primary), nothing pre-selected, the choice read back in words plus an anatomical locator underneath, and a neutral chalk primary that stays inert until the question has an answer. A SECOND REAL QUESTION was added — when it started (today / this week / longer ago, resolving through `injuryDateFor`) — because the API stamped every injury as happening the moment the protocol was opened, so day one was a fiction and the day count that follows from it was wrong. LIVING WITH ONE: a stage pill, a progress bar and a column of raw browser checkboxes were three drawings of one fact, none of which said where in the journey you stood or how long you had been in it. The protocol is now a LADDER of all six stages on one spine — the rail through the stage marks IS the progress bar. Stages behind you carry the date you entered them, recovered from the audit trail we were already keeping (and marked `forced` when they were entered by override rather than met); the current rung is the only one that opens, carrying a plain-language line about what the stage is FOR, its gates, and — only when every gate is met — the one action that leaves it. When gates remain, the honest line (\"2 gates to go\") sits where the button was, with Override as a quiet text control rather than a red pill shouting beside a lime one. Abandon had an API action and NO UI, so a protocol opened by mistake was permanent; there is now a two-step \"Close this protocol\" that keeps the record in the trail. The audit trail folds away behind a disclosure and is laid out as a timeline rather than joined strings. LOCALIZATION: every stage name and every gate criterion was a hardcoded English string from engines/rtp.ts printed straight into a Polish or German UI — they now resolve through RTP_STAGE_KEY / RTP_STAGE_SUB_KEY / RTP_GATE_KEY / RTP_ACTION_KEY (EN/PL/DE), and the seven areas through INJURY_AREA_KEY, which also renames them everywhere on the card so the rows, the axis tooltips and the protocol header stop calling the same tissue two different things. Colour discipline: the accent fill is now reserved for advancing and for being cleared — the two things that are genuinely good. 44 new tests across injury-body + rtp-view. Next: org-role gating of overrides (medical-only)." },
  { id: "admin-engine-room", area: "AI", status: "shipped", title: "Admin Engine Room — live engine transparency console", detail: "A dedicated admin section (AI → Engine room, apps/web/components/admin/engine-room.tsx) that exposes the FULL math behind the intelligence stack. It runs the real @hybrid/core engines client-side (they're pure) on either the built-in sample athlete or any real athlete's stored inputs — served by GET /api/admin/engine (admin-gated, and the support-read writes a user.engine.view audit row; returns the TrainingLog + Biometrics + live calibration so no math is duplicated server-side). Shows: headline HPI/readiness/injury-risk/p(injury) with semantic band colors; the ranked state drivers; a 14-day HPI+readiness trajectory chart with hover tooltips; the per-tissue injury table (fatigue, ACWR, the four risk components as a stacked bar — spike/load/detrain/recovery — score, calibrated probability, band); the live logistic calibration curve with the athlete's position marked and the active ModelFit version chip; an HPI weight-profile switcher (hybrid/strength/endurance); and a WHAT-IF SIMULATOR — sliders for last-7-day training scale and today's HRV/resting-HR/sleep that transform the inputs (whatIfLog/whatIfBio in @hybrid/core engine-room.ts, unit-tested) and re-run the whole stack live, with deltas vs actual. The FORMULA SHEET renders ENGINE_FORMULAS — every formula the stack runs (fatigue decay/dose/normalization, endurance saturation, bioAdj, readiness, HPI blend, sRPE, ACWR, the four injury components, band gates, the logistic calibration, and — added with the volume work — the VOLUME LANDMARK stack, the BLOCK VOLUME RAMP and FEEL TIMING) as DATA with constants + meaning, drift-guarded by tests that recompute the live engines from the documented expressions. The three newer groups are: `landmarks` (the four-layer resolution order population → profile → observed → manual; the stimulus multiplier on MV/MEV and the compounded recovery multiplier on MRV with every factor's live value; the MAV band re-derived at its proportional position; and the observed ceiling correction with its ±35% bound and two-week evidence gate), `volumeBlock` (this week's target = mev + (top − mev) × ramp, with the deload at MV), and `feelTiming` (the acute-fatigue residual curve, the timing-adjusted session cost with its strain threshold, the recall discount, and the check-in soreness-polarity conversion that documents the freshness trap in the open). Kept current with the engines as they change — the pooled recovery reading (`recovery-pooled`: soreness and energy folded onto one spentness scale, lag-adjusted, one threshold) was added when the one-card merge replaced the two raw thresholds, and a `fitnessLevel` group (level-from-relative-strength with the live entry ratios, plus the influence-weighted profile-completeness score) and `landmark-frame` (body mass read against the frame carrying it) followed the training-level work. Their constants are rendered FROM the live exports rather than hand-copied, so the sheet cannot drift by construction; the behavioural guards — that the documented expression reproduces what the engine actually computes — are mutation-tested (flipping the deload target from MV to MEV, or round to floor in the landmark scaling, each fails a named test). UNDERSTAND & COMPARE (Jul 2026, second pass): the console now goes beyond formulas to full TRACEABILITY. (1) DERIVATIONS — packages/core engines/derivation.ts builds step-by-step SUBSTITUTED arithmetic (the athlete's actual numbers plugged into each formula): deriveReadiness (per-muscle fatigue → average → base → each wearable metric's deviation × weight → clamp → final), deriveHpi (S/E/R, the weighted blend, and the limiter-gap comparison), deriveTissueRisk (acute + chronic windows → ACWR → each of the four components with real values → band → σ substitution), all ending in EXACTLY the live engine's output (drift-guard tests per derivation; biometricDeviations + tissueLoadWindow were exported from the engines so derivations reuse the same primitives). Rendered as collapsible 'How is this calculated' panels for readiness + HPI and a per-tissue picker under the injury table; derivations follow the active what-if so the math always explains the numbers on screen. (2) FEELING LAB — the one-tap check-in made inspectable: the rating→feeling thresholds (FEELING_THRESHOLDS, drift-guarded against feelingFromRating), and feelingImpacts() runs the REAL prescribeSession once per feeling so the operator sees in kg and sets what primed/good/flat/wrecked does to today's session for this athlete (×1.05 / ×1.0 / ×0.94 / ×0.85, wrecked sheds a set), with the honest note that the feeling scales the PRESCRIPTION only — it never touches HPI/readiness/risk. (3) SCENARIOS — the what-if simulator gained named scenarios: one-click presets (Crashed recovery, Eased week 50%, Overreached 150% — biometric presets are relative to the athlete's own baseline) plus save-current-sliders, all compared in one table (HPI/readiness/risk/p/flagged with deltas vs actual). (4) COMPARE ATHLETES — a side-by-side card (second athlete picker against the same audited /api/admin/engine feed): actual states only, each computed with that athlete's own inputs, calibration and personal onset — metric table, a deterministic 'biggest divergence' line naming the HPI pillar that separates them, and each side's ranked drivers, answering 'why does one athlete read better'. (5) EFFORT MODEL — a card for the per-athlete learning loop behind 'how did that feel?' (see effort-model): the learned effort bias on a bounded scale against the population prior, the rated-session count, the leave-one-out error beside the unpersonalised engine's baseline with an honest verdict either way, and the 'same work over time' fitness trend. Mobile parity for this admin section is tracked as its own planned entry (mobile-engine-room) — the shared math already lives in @hybrid/core so the native section is a render-only port." },
  { id: "admin-engine-explain", area: "AI", status: "shipped", title: "Engine Room — grounded LLM explanation layer", detail: "An 'Explain this athlete' card in the admin Engine Room backed by POST /api/admin/engine/explain (admin-gated, rate-limited 12/min, body-size capped, per-user reads audited as user.engine.explain). The server RECOMPUTES the full engine state itself — sample athlete or a real athlete's stored inputs, with the sanitized what-if transforms applied via the shared whatIfLog/whatIfBio — GROUNDED ON THE EFFORT MODEL TOO: the prompt carries a line naming how many sessions the athlete has rated, whether they report the work harder or easier than the log implies, the held-out error against the unpersonalised baseline, and the same-work-over-time trend. It has to: the log those numbers come from is personalTrainingLog, so a reported effort moves ACWR hard (the same month of training reads 1.43 or 0.40 depending only on what the athlete said it cost them), and a narrative blind to that input would confidently attribute an elevated risk to something else. Zero ratings says exactly that, rather than 'they report what the log implies' — which would be a claim about an athlete who has said nothing. The line is in the DETERMINISTIC path as well, since that is what an operator actually reads until ANTHROPIC_API_KEY is configured. So the LLM is grounded exclusively on structured engine output (HPI components + limiter, readiness + bioAdj, per-tissue risk with ACWR and per-driver point contributions, calibrated p(injury), model version), never on client-supplied numbers and never free to invent data. Claude (claude-opus-5, adaptive thinking, cached system prompt) narrates what the numbers mean and what a coach should do, contrasting simulated-vs-actual when a what-if is active. HONEST DEGRADATION: without ANTHROPIC_API_KEY (or on an API error) the endpoint returns the engines' own deterministic explanation (state summary + readinessWhy + flagged tissues) labelled 'engine fallback' with a note — same pattern as the AI coach — so the card is useful today and upgrades the moment the key is set (the same key the AI coach + nutrition scan wait on). GROUNDED ON THE FULL SCORE (Aug 2026, with nutrition-training-join). The endpoint built its trace WITHOUT the heat rows and without a fuel read, so it computed heatAdj and fuelAdj at zero while the Engine Room panel beside it passed both — the console and its own explanation printed different readiness numbers for the same athlete (71 against 67 on the sample), and because the narrative is grounded only on what it is handed, it attributed the missing points to the causes it could see. That is the exact failure the effort-model grounding line above exists to prevent, reintroduced by a term added to the score and not to the prompt. Fixed on three levels: the trace now takes the same heatSignals + fuel read the panel does (and the REAL athlete's, from athleteInputs, not the sample's); the prompt names every adjustment (wearable, heat, fuel) plus the deficit split that sums to 100; and both terms get grounding lines in the DETERMINISTIC fallback as well, since that is what an operator reads until the key is set. THE ABSENCE IS DESCRIBED AS AN ABSENCE: a fuel term of zero has two opposite causes — eats at maintenance, or logs nothing usable — which render identically on every athlete-facing surface, so an unmeasurable read is stated as NOT MEASURED with its reason and an explicit instruction not to call the athlete's nutrition adequate. Same treatment for a heat credit suppressed by a wearable, which is the rule working rather than an athlete who never saunas. SECOND DRIFT FIXED IN THE SAME PASS: the explain request forwarded four of the panel's ten what-if fields, so an operator simulating a cut, a sauna or no-wearable got an explanation of the untransformed athlete captioned 'this is the live state' — the caption being wrong is worse than the transform being absent. All ten are forwarded, sanitized and counted toward whatIfActive now. Guarded by apps/web/__tests__/engine-explain-parity.test.ts (28 assertions), which scrapes the panel's own WHATIF_OFF field list so it cannot fall behind a slider someone adds, and which was verified to FAIL on both original defects before being kept." },
  { id: "injury-body-map-primary", area: "AI", status: "shipped", title: "Injury card — the body IS the subject (one figure, both clients)", detail: "SHIPPED on BOTH clients, as part of the protocol redesign (see injury-risk). All four parts landed. (1) A REAL SILHOUETTE: the card's nine rounded rectangles are gone. The Tissue card and the injury picker now draw the SAME schematic mannequin the exercise body-map has always used — @hybrid/core injury-body.ts derives the seven engine areas from the existing per-muscle polygons (quads; glutes; posterior = hamstrings + calves; back = traps + upper-back + lats + lower-back; chest; shoulders = front + side delts; triceps), so anatomy authored once is drawn everywhere and cannot drift. Untracked parts of the body stay faint, which makes what you can choose legible without a word of instruction. (2) TOUCH TARGETS: solved by resolution rather than by inflating shapes into each other — `nearestInjuryArea` measures a touch against each shape's centre, so the whole limb is live (a touch anywhere down the back of the arm reads as triceps) while a touch near nothing tracked resolves to NOTHING rather than a wrong answer. `injuryTouchPoint` does the letterbox-correct box→viewBox conversion in core, because mapping linearly across the rendered box mis-reads every tap near the edges — exactly where the small areas live. Both are pure and tested. (3) THE FLAG AND THE PROTOCOL ARE ONE OBJECT: touching an area on the card's risk figure opens the injury sheet with that area already answered; the word-chips are gone entirely. (4) THE MOBILE TWIN: mobile draws the identical figure via react-native-svg (already a dependency), closing a parity gap that had stood since the merge — mobile had NO body map at all. The card's chassis is unchanged: the figure sits in the opened state where the rows already are." },
  { id: "injury-personalization", area: "AI", status: "shipped", title: "Per-athlete online learning — personal ACWR spike onset", detail: "The injury engine's spike component is now personalized per athlete. packages/core engines/personal.ts: derivePersonalization() learns a PERSONAL spike onset (bounded 1.1..1.6 around the population prior 1.3) with the same shrinkage idiom the data network uses for cohort norms — spikes above the prior tolerated WITHOUT injury raise it toward what the athlete demonstrated, recorded injuries lower it (an injury at low ACWR is strong evidence of fragility), a pseudo-observation prior weight (8) keeps thin evidence near the prior, and uninformative events (no spike, no injury) carry no signal. Evidence comes from the athlete's OWN engine: acwrEventsFromHistory()/maxAcwrAt() replay the training log at each labeled RiskOutcome's date and pair the peak per-tissue ACWR with the outcome — so the flywheel that refits the population calibration (data-network) also personalizes the individual threshold. computeInjuryRisk gained an optional spikeOnset opt (spike = ramp(ACWR, onset, onset+0.9) × 55; default exactly reproduces the population 1.3/2.2 behavior — regression-tested), threaded through computeEngineTrace. Server: GET /api/admin/engine derives it from the user's RiskOutcome rows and returns it as `personal`; /api/admin/engine/explain uses the same onset so the LLM narrative matches the on-screen numbers. UI: an Engine Room 'Personal model' card shows the onset scale (prior vs personal markers), evidence counts (tolerated spikes / injuries / n), and the spike formula in effect; the per-tissue table and what-if simulator compute with the personal onset. 13 unit tests. NEXT: surface the personal onset in the consumer clients' injury explanations (today it's admin-only), and per-tissue onsets once outcome rows reliably carry the tissue." },
  { id: "volume-profile-server-sync", area: "Core", status: "shipped", title: "The questionnaire follows the athlete, not the device", detail: "The answers the volume/readiness model is built from (training age, age, SEX, body mass, height, sleep, stress, energy intake, sessions per week) lived in each device's own storage. Two consequences, both shipped-fixed Aug 2026. (1) They did not follow the athlete between devices, so one person got two different volume models, each labelled 'estimated for you'. (2) The SERVER could not read them — which mattered most for `sex`, because every strength and endurance threshold in the app is published for a male athlete and shifted from there, so the public level badge scored her against the men's bar at the same moment her own card scored her correctly. THE FIX: a `questionnaire` JSONB column on User (reference/sql-questionnaire.sql + prisma/migrations/20260817120000_questionnaire), /api/questionnaire GET/PUT validated by the SAME `sanitizeVolumeProfile` the clients save through (one definition of a legal answer, so a bound cannot drift between phone and server), and apps/mobile/lib/questionnaire.ts mirroring it on-device. THE MERGE RULE, since two devices can disagree: the account wins WHOLESALE whenever it holds anything, and a device only seeds it when it is empty. That is deliberate over a per-field merge, which cannot tell 'never knew' from 'deleted' and therefore resurrects answers the athlete removed. The seed case is the migration path for athletes who answered before the column existed. Everything soft-degrades: signed out, offline or un-migrated leaves the on-device answers untouched." },
  { id: "questionnaire", area: "Mobile", status: "shipped", title: "The volume model becomes THE QUESTIONNAIRE", detail: "The screen was named after the machine, and it showed: one flat stack of four identical cards holding a landmark-override diff, a profile form of six 30%-wide numeric boxes and three pill rows, a table of THIRTY-FIVE TextInputs, and the periodization switches — about a dozen questions about a person at exactly the same weight as the engine's internals, with nothing saying which was which. AND ONE OF THE QUESTIONS DID NOT WORK: `sanitizeVolumeProfile` never named `sex`, so the toggle was tapped, stored, silently discarded on the way to storage and read back blank — the control never even lit. REBUILT as packages/core/src/questionnaire.ts (the questions as DATA — four sections named for what they ask ABOUT: your body, your training, your recovery, your fuel) plus apps/mobile/components/aurora/questionnaire.tsx. One question per block with its own control; the justification prose shows ONLY while a question is unanswered, so the form gets shorter as you fill it in; an unanswered number shows NO figure (a seeded 80 kg presented as the athlete's own would be a fabricated measurement the model then explains its ceiling with); measured answers say they were measured; 'What your answers changed' puts the earned multipliers on the same screen, so the form is visibly a cause; and the landmark table + switches sit behind one disclosure as the model rather than the person. `trainingYears` is now ASKABLE — it was sanitized, stored and read by `effectiveExperience` but had no field for four months. THE PADDING GUARD: questionnaire.test.ts asserts every question names a factor that actually fires, and round-trips every asked answer through the sanitizer, so neither a decorative question nor another silently-dropped field can land. Route renamed /volume-model → /questionnaire." },
  { id: "questionnaire-decay", area: "Core", status: "shipped", title: "The answers that go stale, and the measurements that do not", detail: "Two decay defects and one gap, found by asking what happens to the questionnaire when the athlete CHANGES. (1) BODY MASS OUTRANKED THE SCALE, FOREVER. `withMeasured` gives a typed answer priority over a measured one, which is right for a standing claim (training age, sex, typical sleep) and wrong for the one field that is a READING WITH A DATE: a figure typed once outranked every subsequent weigh-in for the life of the account, so the volume model, the recovery multiplier and every strength standard kept quoting the number from the day the athlete installed the app. Made worse by the setup change that shipped the same week, which guaranteed every new athlete had one. The rule now INVERTS for body mass alone — the newest weigh-in wins, a typed figure is the fallback for someone who has never weighed in — and body mass has left the questionnaire store entirely: setup writes a dated weigh-in (`bodyMassFromAnswers` + logWeighInNow), and the form's own row appends one too, debounced so a drag from 80 to 74 leaves one reading rather than the trail it crossed, and superseding only the row this device wrote earlier the same day. One store, already read by tonnage, e1RM, the maintenance fit and the energy read. (2) AGE NEVER HAD A BIRTHDAY, AND THEN THE FIX STILL DERIVED IT. `ageYears: 30` typed at setup is 30 in 2031, while the recovery factor moves 1.2% a year — a full multiplier step of silent drift. The first fix asked for an age and computed `birthYear` from it, which was better and still a derivation: `currentYear − age` is right only once the birthday has passed, so it carried a ±1-year error — the same size as the factor's own yearly step, which is to say the correction could be as large as the thing it corrected. The question ASKS FOR THE DATE now: a new `birth` onboarding kind whose answer is a YYYY-MM string (one answer-map slot, no new value type in the admin-editable schema), rendered as one block with a year scrub and twelve month marks — one question, not two steps. `birthMonth` is optional: a profile written before it was asked keeps the honest ±1 reading rather than having a month invented. `effectiveAgeYears` subtracts a year when the birthday month has not arrived. AND THE SAME HAND-COPIED-LIST TRAP AGAIN: `normalizeOnboardingQuestion` held a duplicate of the KIND union too, and unlike the engine-key case an unrecognised kind makes it return null — dropping the whole question from every client's wizard. Both derive from ONBOARDING_QUESTION_KINDS now. (3) BODY FAT NOW SHARPENS THE MASS FACTOR. The penalty exists because more tissue means more to repair, and fat mass does not need repairing — a 100 kg athlete at 10% has 90 kg of contractile tissue to recover, at 30% they have 70, and the flat rule charged them identically. `frameAdjustedMassKg` reads LEAN mass where BodyMetric.bodyFatPct exists, against `REFERENCE_BODY_FAT` so the term cancels at the reference and an athlete AT it reads exactly as before. Sex-neutral in construction, because the landmark maths is forbidden from reading sex. It retires the comment that said the app 'cannot see composition' — it can, because the athlete types it into their own body log. The tape measurements (neck, chest, waist, hips, thigh, arm, calf) stay display-only: waist as a third path into energy availability would be inventing an effect on top of two that already work." },
  { id: "questionnaire-placement", area: "Mobile", status: "shipped", title: "The questionnaire is asked at setup, and lives on Profile", detail: "Rebuilding the questionnaire left it unfindable: two doors, both deep inside Performance (a sheet reached through a drawer on the Volume card, and the foot of the monthly story), and NOT in the side menu — its nav entry is promotedTo performance. A screen holding a person's body, training age and recovery could only be reached by someone already looking for it. FOUR MOVES. (1) SETUP ASKS. The intake now carries sex, age and body mass beside the training age and frequency it already asked — 38% more of the estimate, three taps, at the one moment an athlete expects to be asked about themselves. A new athlete reaches Today at 76% known instead of 0%. None of the three ships a `defaultValue`, deliberately: the client seeds every answer from its default, so a default would write '80 kg' for anyone who stepped past the screen — a fabricated measurement the model then explains its own ceiling with. (2) PROFILE IS THE HOME. `AboutYouLead` sits directly under `LearnedLead`: what the app WORKED OUT about you, then what you TOLD it, adjacent, because neither is complete alone. It leads with the gap ('Next — body mass'), not the score. (3) THE GUESS IS ITS OWN DOOR. The Volume card's provenance row now reads the state of the numbers: while they are still mostly a textbook's it SAYS so and goes straight to the questionnaire; once the profile is answered it goes back to opening the working. No list row, no menu entry — the athlete notices the app is guessing about them and touches the guess. A 'Go deeper' row was considered and REJECTED: that card holds five doors, and a sixth is the failure the subtraction pass already named. (4) THE CHORE METER IS CUT. The screen opened with the completeness figure at fs.stat — 46dp of '0%' as the first thing on a page about yourself, grading the athlete on arrival in the app's own units. The lead is the sentence now; the figure annotates the track it belongs to at reading size. (5) AND THE 'ANSWER' BUTTON WENT WITH IT, one round later. The first cut kept an unanswered number behind a button, so the field appeared only once asked for — one tap, on twelve questions, on screens whose entire purpose is to be answered. The rule had been read one level too high: 'do not display a number the athlete did not give' constrains the VALUE, not the CONTROL. AScrubField now takes `unset`: the field is present and live from the first frame, reads as a dash, and the first drag or press answers it from the seed. A press of ＋ or − on an empty field lands ON the seed rather than one step above it — the first press means 'start here', not 'one up from a number I cannot see'. Same control answered and unanswered, so nothing is added to or taken from the row as its state changes." },
  { id: "profile-lead-rail", area: "Mobile", status: "shipped", title: "The You tab's three leads read inline, as one rail", detail: "The tab opened with THREE stacked full-width cards — what the app learned about you, what you told it, and (while it is incomplete) the set-up-your-profile nudge — and between them they ran ~326dp before the cover banner even began — 41% of the visible content area on a 6.7-inch screen, and the whole of it on a 4.7-inch one, where the athlete scrolled to reach their own face. The rail runs ~121dp for the same three cards. Stacking also asserted a ranking none of the three has over the other two; they are three parallel invitations into three different screens, not a sequence. They lay INLINE now, as a snapping left/right rail (apps/mobile/components/aurora/lead-rail.tsx): one card holds the eye at a time, the next peeks by 28dp so the row announces itself as scrollable without a caption saying so, and the identity block is back where a profile starts. FULL-BLEED per the screen-level slider rule — negative margins the width of AuroraScreen's 12dp gutter with matching internal padding, the exercise-widget rail's idiom — so a card slides under the true screen edge instead of clipping at the content column. The rail STRETCHES its cards to the tallest of them, so the three sit on one baseline top and bottom whatever their content; with the nudge hidden it closes to two, and at one card it stops being a rail at all (a snap grid with a single stop is a scroll view that cannot scroll, and the peek would be an empty promise). This supersedes the placement half of questionnaire-placement — AboutYouLead is BESIDE LearnedLead now, not under it — and nothing about the argument for the pair changes: what the app worked out about you next to what you told it, neither complete alone. AND THEN THE ROW SHOWED WHAT STACKING HAD HIDDEN. Three cards a screen apart can disagree quietly; three cards in one row cannot, and these disagreed in nine places. The two model cards were near-copies that had already drifted: one tracked its 18dp figure with `tracking.display` (the flat -0.5 house TITLE tightening) and the other with `trackFigure` — same size, two rules — and NEITHER took the tight line box every other figure in the app gets, so the smallest hero figure was the one place the figure rules did not reach. The profile nudge was a third shape entirely: its title a rung larger at fs.subtitle, its body in the SYSTEM font because it never named a family, no kicker where the other two open with one, no accessible name at all, its own hand-drawn card chrome instead of APressCard (so no shadow, and free to drift further), a 16 inset against their CARD_PAD 20, and a 44dp icon tile that pushed its text column 56dp right of theirs — three kickers, three titles and three left edges in three places, in a row whose whole job is to read as one set. They all draw through ONE `LeadCard` now (kicker, title, figure+unit+delta, body, meta, arrow), which is the project's own rule about shared components applied to the thing that had just proved it. TWO DIFFERENCES SURVIVE BECAUSE THEY MEAN SOMETHING: the FIGURE is optional, since the third card carries a sentence rather than a number and inventing one for it would be fabricating a measurement; and the ACCENT is optional, the lime rim and wash marking the one card that asks the athlete to DO something. The icon tile went with the rest — a filled tile in front of one card's title and not the other two's is the marker-before-a-label this app retired everywhere else, and the lime rim plus the lime arrow already say everything the tile did. New i18n key `w.account.profile.setup-kicker` (EN/PL/DE) gives that card the eyebrow the skeleton requires." },
  { id: "intake-questionnaire-parity", area: "Mobile", status: "shipped", title: "Setup and the questionnaire ask the same question the same way", detail: "The setup wizard and the questionnaire screen ask for the same two things — a quantity and a date of birth — and were drawing them from two near-identical LOCAL copies, plus a duplicated month list. That is the exact shape of the three bugs already fixed on this branch (a duplicated engine-key list, a duplicated question-kind list): copies agree right up until one of them is edited, and these agreed only because both were edited in lockstep. There is now ONE of each, in the mobile kit: `ANumberField` (segmented when the whole range fits, scrubbed when it does not, and always scrubbed while unanswered — a segmented control cannot express 'unanswered', it always lights one option) and `ABirthField` (a year scrub plus twelve month marks, inert until a year exists). `MONTH_KEYS` moved to core beside the questionnaire schema. The invariants are asserted once, on the controls (kit.render), rather than once per screen, which would be the duplication again one level up. TWO REAL DIVERGENCES FELL OUT OF THE MERGE. (1) THE WIZARD INVENTED A MONTH: it stored `formatBirth(year, month ?? 1)`, so a year scrubbed without a month was written down as JANUARY — in the one place the athlete cannot see it happen, and against the model's own design, where `birthMonth` is optional precisely so a year alone keeps the honest ±1 reading. `parseBirth`/`formatBirth` carry the year-only form now. (2) A SEED IS NOT AN ANSWER: the client seeds the answer map from every question's `defaultValue` (the plan recommender needs a complete set, and a choice step has to open with its default shown as picked), so stepping past the experience step wrote 'beginner' onto the PROFILE, where it moved the landmark factors and then read back on the questionnaire as answered. The body questions ship without defaults for exactly this reason — that was only half the guarantee while `experience` and `days` still carried a value nobody chose. The wizard tracks which keys were TOUCHED now; the full map still goes to the server and to the recommender, and only the profile is held to what was actually given." },
  { id: "onboarding-answers-reach-the-engines", area: "Core", status: "shipped", title: "Setup's answers reach the engines at all", detail: "THREE MORE DROPPED FIELDS, found while placing the questionnaire, and the worst of the set. The intake's answers went to the server and stopped: `use-volume-model` read training age and weekly frequency from `hybrid.experience` / `hybrid.daysPerWeek`, and Today's `home.tsx` read training age and equipment from `hybrid.experience` / `hybrid.equipment` — device keys NOTHING on either client has ever written. `git log -S` finds no writer in the entire history, so these were read-only from birth. Consequence: `prescribeSession` has always run with `experience: undefined` and `equipment: undefined`, for every athlete, INCLUDING the ones who had just answered both during setup. Same defect as the dropped `sex` field, three more times. FIXED by giving the answers one destination that is actually read: `questionnaireFromAnswers` (core) projects the intake onto the questionnaire through the same `sanitizeVolumeProfile` every other writer uses, `finishOnboarding` writes it, and the dead fallbacks are deleted rather than left looking like they work. Equipment keeps a device key — it decides which movements may be prescribed, not how much you recover — but something writes it now. ROOT CAUSE OF THE SILENCE: `normalizeOnboardingQuestion` held a hand-copied duplicate of the `OnboardingEngineKey` union, and it is a validator that does not REJECT an unknown key, it sets it to undefined. The three new questions would have shipped answered and connected to nothing. Both now derive from ONBOARDING_ENGINE_KEYS, with a test asserting every default question keeps its engine key through the normalizer." },
  { id: "per-pattern-training-age", area: "AI", status: "planned", title: "Per-movement-pattern training age (scoped MEV/MRV scaling)", detail: "Today fitness-level.ts estimates ONE global level from the athlete's best lift or run, and landmark-profile.ts applies one experience scalar to every muscle — so an elite bench presser who took up squatting last month gets advanced-scaled (×1.15) leg landmarks until the observed layer corrects them. The blast radius is bounded (stated experience always wins, the multiplier is ±15%, blocks start at MEV, and landmark-adapt corrects per muscle within two qualifying weeks) but the right shape is a training age per movement pattern / muscle group: the estimator already holds per-benchmark-lift evidence (squat/hinge/press ratios, run pace), so scope the stimulus/recovery experience factors to the patterns each muscle is trained through, with the global estimate as the fallback for untested patterns. Surface any per-pattern disagreement the same way resolveExperience does today — shown, never silently applied." },
  { id: "clearance-superposition", area: "AI", status: "planned", title: "Clearance pairs under double sessions (superposition residual model)", detail: "recovery-pairs.ts drops a clearance pair when a second session ends inside its gap — correctly conservative, because the later read measures the SUM of both sessions' residuals and attributing it to one would fit a confidently wrong personal rate. Cost: a two-a-day athlete's FIRST session of the day never pairs (the last session of the day still pairs with the next-morning check-in, so they are not starved outright — the truly starved case is 3+/day). Fix is a modelling extension, not a threshold tweak: compute the expected residual as the superposition of expectedResidual(h) over ALL sessions that ended before the read, weighted by each session's immediate cost, so contaminated reads become usable instead of dropped. Keep the identifiability discipline from feel-timing.ts — normalise against the population curve, never fit a personal time constant from a single pair." },
  { id: "mobile-engine-room", area: "Mobile", status: "planned", title: "Engine Room section in the mobile admin console", detail: "Port the web admin Engine Room (admin-engine-room) to apps/mobile/components/admin/ like the other 19 admin sections: sample/real athlete picker against GET /api/admin/engine, headline HPI/readiness/risk/p(injury), per-tissue table with the component split, what-if sliders (the whatIfLog/whatIfBio transforms are already shared in @hybrid/core), and the ENGINE_FORMULAS formula sheet. Charts use native primitives (bars/markers) like the mobile Performance screen — no chart lib." },
  { id: "ai-copilot", area: "AI", status: "planned", title: "AI Coach Copilot (grounded + agentic)", detail: "Upgrade of /api/ai-coach into an agent grounded in the Twin + the club's own methodology: drafts plans as editable objects, explains with citations to the athlete's data, proposes (never auto-commits) clinical changes. Human-in-the-loop + full audit." },
  { id: "agent-org", area: "AI", status: "shipped", title: "AI agent org — dynamic config, control & execution", detail: "WEB OPERATOR PANEL ONLY, since Aug 2026 — the mobile admin console's Agent HQ and AI agents sections are deleted (see `mobile-agent-console`), because an agent-operations console has no business being compiled into the consumer app every athlete downloads, role gate or not. Everything below is unchanged and lives at /admin. — An admin-controlled executive team (CEO/CFO/CMO/COO + custom roles) defined as DATA, not hard-coded prompts. agents.ts in @hybrid/core (pure + unit-tested) holds the role PRESETS and the dynamic builders: buildSystemPrompt(def) assembles the live system prompt from editable fields (mandate, responsibilities, KPIs, guardrails, escalation, tone, collaborators, tools) so changing a KPI rewrites what the agent runs on; buildAgentConfig(def) maps a definition onto the Claude Managed Agents shape; coordinatedAgents/delegateToolName/resolveEffort wire delegation + clamp effort to each model. AgentConfig model + reference/sql-agents.sql (server-only RLS — deny-by-default). CONTROL PLANE: admin CRUD at /api/admin/agents (+ /[id]) — ADMIN-gated, rate-limited, body-capped, audited; create-from-preset, edit every field, pause/resume (status), delete. EXECUTION PLANE: /api/admin/agents/[id]/run executes an active agent on a task via the Messages API using its generated prompt (apps/web/lib/agent-runtime.ts) — an EXECUTIVE runs an agentic loop that exposes its reports as delegate_to_<role> tools and, when it delegates, the orchestrator runs that functional agent (its own prompt/model) and feeds the result back: a real coordinator→functional hierarchy on plain tool use (turn- and token-bounded), audited (agent.run). TWO RUNTIMES (selectable per agent): 'messages' — stateless Messages-API run with the coordinator→functional delegation above (apps/web/lib/agent-runtime.ts); 'managed' — a Claude Managed Agents session with a durable MEMORY STORE mounted so the agent remembers across runs (apps/web/lib/managed-runtime.ts, lazily creates+reuses the managed agent + memory store, ids persisted on the row). STREAMING: /api/admin/agents/[id]/stream sends live progress over SSE (text deltas + delegation boundaries); /run is the non-streaming JSON twin. RUN HISTORY: every run is persisted to AgentRun (reference/sql-agent-runs.sql); read per-agent via /api/admin/agents/[id]/runs AND across the org via /api/admin/agent-runs (surfaced in the Agent HQ → Reports tab, status-filterable). SCHEDULED RUNS: AgentSchedule (reference/sql-agent-schedules.sql) holds standing tasks per agent on a cadence (hourly/daily/weekly); a Vercel Cron (apps/web/vercel.json) hits /api/cron/agents — auth'd by CRON_SECRET, NOT admin — which runs every due schedule on an active agent, records the run (ranBy 'scheduler') and rolls nextRunAt forward; managed per-agent in the editor's Schedules section. Admin console: a command-center DASHBOARD (AI → Agent HQ, /api/admin/agents/overview) — Command center (headline stats incl. 7-day DOLLAR COST via core costUsd/MODEL_PRICING, an org chart with live status, a 7-day activity chart, a recent-activity feed, upcoming scheduled work, and a 'needs attention' count), SCORECARDS (per-agent: success-rate vs a 90% target, runs/cost 7d, last run, and KPI TARGET-VS-ACTUAL — KPIs carry an optional numeric target, admins log numeric actuals inline (AgentKpiMeasurement), shown as % of target + a trend sparkline + an expandable actual-vs-target trend chart over time with a target reference line), a WORK board (drag-to-assign an agent onto a dropzone + quick run + queued/recent columns), an INBOX (PERSISTENT, dismissible notifications — AgentNotification, written on run failure, mark one/all read — plus schedules that can't fire, with an unread badge), and a REPORTS tab (the org-wide run feed, exportable to CSV via /api/admin/agent-runs/export or print-to-PDF). Actual 30-day agent run spend (tokens × model list price via core costUsd) is reported in Agent HQ's own cost report; it used to also feed the admin Financials console, which was retired in Aug 2026. The builder screen (AI → AI agents): roster with status/model/effort, a full editor (KPI/responsibility/guardrail list editors, model+effort+authority+runtime selects, tool toggles, Schedules), a LIVE system-prompt preview, a Run panel that STREAMS the agent's output + delegation trace + token usage live, and a per-agent History view. Live runs need ANTHROPIC_API_KEY server-side (the same key the ai-coach uses); scheduled runs additionally need CRON_SECRET; the spend-causing actions (run/stream/schedule) go through requireAgentOperator — restrict them to specific admins by setting AGENT_OPERATOR_EMAILS (comma-separated) in the server env (unset = every admin is an operator, back-compatible). SPEND GOVERNANCE: per-agent APPROVAL GATES (approvalThresholdUsd — a run whose estimated cost ≥ the threshold is held as an AgentApproval and must be approved by a DIFFERENT operator, who triggers the execution; Approvals tab in HQ with a pending-count badge) and per-agent BUDGET CAPS (budgetUsd7d — when trailing-7-day spend reaches the cap the agent is auto-paused + a notification raised; enforced on every manual and scheduled run). DAILY DIGEST: a Vercel Cron (/api/cron/agent-digest, CRON_SECRET) posts a 24h run summary (count, success rate, $ spend, top agents, failures) to Slack via SLACK_WEBHOOK_URL; previewable + send-now in the HQ Command center (/api/admin/agents/digest). MONTHLY COST REPORT: per-agent monthly spend — current + previous month in the HQ Command center with CSV download (/api/admin/agents/cost-report) and a Vercel Cron (/api/cron/agent-monthly, 1st of month) that Slacks the prior month. PER-AGENT TIMELINE: a unified activity feed in the agent editor (config edits from the audit trail + runs + approvals, /api/admin/agents/[id]/timeline). SLACK INTERACTIVE APPROVALS: approval requests post to Slack with Approve/Deny buttons; the callback (/api/slack/interactivity) verifies the Slack request signature (SLACK_SIGNING_SECRET), executes the decision (idempotent on retry), and replaces the message — needs a Slack app with interactivity pointed at that URL (web Approvals enforce the two-person rule; Slack records the Slack approver). without them the endpoints return an honest 'unconfigured'/'not configured' note and the rest of the screen still works. The managed runtime additionally needs the managed-agents beta (can't be exercised from this sandbox — Supabase + the API are out of reach here); reference/agent-managed-smoke-test.md documents the deployed verification. NOTE: run reference/sql-agents.sql, sql-agent-runs.sql, sql-agent-schedules.sql, sql-agent-kpi-notifications.sql, then sql-agent-approvals.sql in Supabase. Optional env: SLACK_WEBHOOK_URL (digest/report/approval delivery), SLACK_SIGNING_SECRET (Slack interactive approvals), AGENT_OPERATOR_EMAILS (restrict run/spend)." },
  { id: "agent-scheduled-runs", area: "AI", status: "blocked", title: "Scheduled agent runs (cron)", detail: "The scheduling layer of the agent org (see agent-org) is BUILT: AgentSchedule (hourly/daily/weekly per agent), the editor's Schedules section, and /api/cron/agents which runs every due schedule on an active agent, records it (ranBy 'scheduler') and rolls nextRunAt forward. A Vercel Cron in apps/web/vercel.json fires it (default daily 08:00 UTC). Manual + streamed runs are already LIVE (ANTHROPIC_API_KEY is set — see ai-coach); only the SCHEDULED path is gated on the cron credential.", blockedBy: "Set CRON_SECRET in the Vercel env (Vercel Cron sends it to /api/cron/agents as a Bearer token). Until then the endpoint returns 503 'cron not configured' and schedules never fire." },
  { id: "agent-slack-approvals", area: "AI", status: "blocked", title: "Slack interactive approvals", detail: "Approval requests post to Slack with Approve/Deny buttons (postSlackApproval); the callback /api/slack/interactivity verifies the Slack request signature (verifySlackSignature) and runs the shared decideApproval (idempotent on retry / double-click). Code is COMPLETE and typechecks. KNOWN LIMITATIONS — (1) couldn't be exercised from the build sandbox (no live Slack app / network); verify the round-trip once configured. (2) Approve executes the held run SYNCHRONOUSLY, which can exceed Slack's ~3s ack window — the run still completes and the message is replaced with the outcome, but Slack may briefly show a timeout; it's idempotent so no double-run. The clean fix is ack-immediately-then-run via next/server after() or a queue. (3) The two-person rule (a DIFFERENT operator must approve) is enforced only for WEB approvals, where the operator's email is known; the Slack path records the Slack approver but does not block self-approval — mapping Slack user IDs → operator emails is needed to extend it. The web Approvals tab is the fully-governed path; Slack is a convenience.", blockedBy: "A Slack app with Interactivity enabled, request URL pointed at /api/slack/interactivity, plus SLACK_SIGNING_SECRET and SLACK_WEBHOOK_URL in the server env. Optional hardening: after()/queue for the 3s ack, and a Slack-user→operator map for the two-person rule over Slack." },
  // Year 2 — Risk & Teams
  { id: "org-graph", area: "Backend", status: "retired", title: "Team Operating System (Org Graph)", detail: "DELETED (Aug 2026). Was: Organization + a self-referential Team hierarchy + Membership with roles (OWNER/DIRECTOR/COACH/MEDICAL/ANALYST/ATHLETE), access scoped by role x data sensitivity AND by team subtree (org.ts, unit-tested), email invites with a pending OrgInvite claimed on first sign-in, /api/org/* and an Organization screen on both clients. Removed: packages/core/src/org.ts (+ test), /api/org/*, /api/admin/orgs, both Organization screens, the org half of the admin Directory, the Organizations admin stat, claimPendingInvites(), and the Organization/Team/Membership/OrgInvite tables (drop script: reference/sql-strategy-cuts-2026-08.sql). Return-to-play lost its org medical/director staff path — the athlete and their active coach remain. The coach console (roster, squad monitor, team compare, groups, programs) is UNTOUCHED: that is the B2B2C acquisition channel and it stays.", retiredBecause: "Pre-revenue enterprise multi-tenancy. TeamBuildr wins that market on athletic-director relationships and procurement cycles, not features; no college program switches mid-season for a better org chart. Revisit after 100k consumers, or spin it out." },
  // Year 3 — Intelligence & Video
  { id: "video-intel", area: "AI", status: "retired", title: "Video intelligence (markerless motion analysis)", detail: "DELETED (Aug 2026). Was: video.ts consuming pose-keypoint frames into joint angles, rep counts, L/R asymmetry, depth and a 0-100 technique score with flags, emitting an asymmetry Signal into the Twin; a VideoAnalysis model, /api/video and a Video screen on both clients, with 10 unit tests. It never had a capture path, so it never analysed a real recording. Removed: engines/video.ts (+ test), /api/video, both Video screens, the `video` nav destination and the VideoAnalysis table (drop script: reference/sql-strategy-cuts-2026-08.sql). UNRELATED and untouched: the exercise demo/media library, which also uses the word 'video'.", retiredBecause: "Real ML cost, real accuracy risk, and the credible players (Perch, Output, Uplift, Onform) are hardware-adjacent. Buy or integrate, never build. The one form of CV worth revisiting is plate-counting for ATTESTATION (see `verified-record`) — not form coaching, which is a graveyard." },
  { id: "competition-intel", area: "AI", status: "retired", title: "Competition intelligence + peaking optimizer", detail: "DELETED (Aug 2026). Was: peaking.ts — optimizeForEvent back-solving the macrocycle to the weeks available and projecting a fitness-fatigue-form curve (Banister impulse-response) so the form peak lands ON the event; an Event model, /api/events, a Competition screen on both clients, event markers + an event layer on the Calendar, and 5 unit tests. Removed: engines/peaking.ts (+ test), /api/events, both Competition screens, the `competition` nav destination, the calendar's event layer, and the Event table (drop script: reference/sql-strategy-cuts-2026-08.sql).", retiredBecause: "Serves maybe 0.1% of users. Beautiful engineering, no market. Note this is NOT the same as race-result integration for the hybrid/Hyrox tribe (a result landing in the log next to the block that produced it) — that is a growth bet the strategy review actively recommends, and it starts from a blank page, not from this optimizer." },
  // Year 4 — Network & Talent
  { id: "talent-graph", area: "Data", status: "retired", title: "Talent Graph + benchmarks", detail: "DELETED (Aug 2026). Was: a TalentProfile model + /api/talent (own report) + /api/talent/search (consent-gated discovery, ranked by percentile or potential) + a Talent screen on both clients + a talent-profile moderation queue + the 'Discoverable in Talent' privacy switch. Removed: all of the above, plus talentReport()/TalentReport from benchmarks.ts, the nav.talent flag and the TalentProfile table (drop script: reference/sql-strategy-cuts-2026-08.sql). KEPT: the norm math in benchmarks.ts (normalCdf / cohortNorm / benchmarkMetric / developmentFraction) — it is consumed by the fitness-level engine and datanet, neither of which was cut. CONSEQUENCE: Wrapped's 'where you stand' percentile is gone too (its only cohort source was the talent profile), and datanet now honestly reports zero cohort observations instead of aggregating a synthetic one.", retiredBecause: "Recruiting is a relationship market with entrenched incumbents, and a talent graph is meaningless without the verified record (see `verified-record`). WITH that record it builds itself later — so this is a sequencing cut, not a permanent no." },
  // ---- Year 5 — vertical expansion ----
  { id: "tactical-vertical", area: "AI", status: "retired", title: "Tactical / SOF readiness", detail: "DELETED (Aug 2026). Was: tactical.ts — a Deployment Readiness Index fusing the Twin (HPI + injury availability) with occupational capacity into a duty status (ready/qualified/limited/non-deployable) with a hard medical gate, plus a unit-level go/no-go rollup, a Tactical screen on both clients and 6 unit tests. Removed: packages/core/src/tactical.ts (+ test), the `tactical` nav destination, the nav.tactical flag, both Tactical screens. NOTE the CONSUMER 'Tactical & Military' training goal in the plan library STAYS — ruck/carry/work-capacity programming for the hybrid tribe is not the SOF vertical, and r/tacticalbarbell is one of the watering holes the growth plan targets.", retiredBecause: "Zero consumer users, an 18-36 month government sales cycle and FedRAMP-class compliance a pre-revenue team cannot carry. It is a different company. Revisit only if a unit signs something first." },

  { id: "longevity-vertical", area: "AI", status: "retired", title: "Performance medicine / longevity", detail: "DELETED (Aug 2026). Was: longevity.ts estimating biological age vs chronological from the Twin's recovery markers (resting HR, HRV, VO₂, sleep) against documented marker priors, plus a healthspan score and flags; a Longevity screen prefilled from the Signal ontology, the `longevity` nav destination, the nav.longevity flag and 4 unit tests. Removed: packages/core/src/longevity.ts (+ test), both the screen and its aurora component, the nav entry, icon, flag and the EN/PL/DE copy. No table — it computed from Signals that already existed, so there is nothing to drop. KEPT: every recovery signal it read. The Mobility & Longevity training GOAL in the plan library also stays — that is a way to train, not a medical claim.", retiredBecause: "Different buyer, different regulatory surface, different brand. Function Health and Superpower are funded and ahead, and a biological-age number is a diagnostic-adjacent claim from a company with no clinician, no validation study and no regulatory posture. The recovery markers it read are already surfaced honestly as readiness — which is a training statement we can actually stand behind." },

  { id: "data-network", area: "Data", status: "shipped", title: "Data network effects (the moat)", detail: "datanet.ts: aggregates consented observations into cohort norms with k-anonymity (≥5), shrinkNorm refits the synthetic priors toward observed data as it accumulates, and refitCalibration re-fits the injury logistic on labeled outcomes (gradient descent from the prior). Admin /api/datanet + a Data Network screen surface the benchmarking-intelligence aggregate (the sellable data layer, not raw rows). The flywheel is wired; it sharpens as the population grows. OFFLINE EVALUATION (Jul 2026): the learning loop now scores itself — datanet.ts gained brierScore (calibration error of any coefficient set on the labeled outcomes), rocAuc (rank-based Mann–Whitney with tie handling — coefficient-free, so it evaluates the heuristic score's ranking power itself) and reliabilityBuckets (predicted-p bins vs observed injury rate — the calibration diagram), all unit-tested; /api/datanet computes them on the stored RiskOutcome set for BOTH the live ModelFit and the documented prior, and returns the fit HISTORY (last 12 ModelFit rows). The Data Network screen renders a Model-evaluation card (AUC + live-vs-prior Brier with an honest 'refit beats the prior / prior still ahead' verdict and honest empty states when a class is missing), a reliability diagram (dots vs the perfect-calibration diagonal, per-bin n in the tooltip), and the fit-history table with the live version chipped. Next: persist refit norms back into the benchmark engine + a federated/clean-room export for federations." },
  { id: "athlete-weeks", area: "Data", status: "shipped", title: "THE NUMBER — labeled athlete-weeks as the company's central metric", detail: "SHIPPED (core/athlete-weeks.ts + GET /api/admin/athlete-weeks + both admin consoles). The audit's finding, in one line: this company optimized for the input that became free (construction) and starved the one that cannot be compressed (elapsed time of real athletes inside the loop) — and it could do so for 239 commits because every metric on the operator console moved when WE worked. Seven equal hero tiles: total users, sessions logged, avg sessions/user, 30d activation. Every one of them is runnable up by one person with no users at all, and the repository is the proof. THE METRIC: a LABELED ATHLETE-WEEK is one athlete × one week in which all three legs of a learnable observation were captured — STATE (a readiness read, a daily check-in, or a wearable state signal: HRV, resting HR, sleep), INTERVENTION (a session that actually projected performed rows into SessionSet — a session document with no sets is not a week of training), and OUTCOME (a measured RESPONSE: the post-session feel/fatigue report, a device recording matched or streamed onto the session, or a dated body measurement). Three legs because two teach nothing: training with no state is a log, state with no outcome is a diary, an outcome with no intervention is weather. The outcome leg deliberately EXCLUDES the training numbers themselves — those are the intervention, and letting them stand in would make every leg true by construction and the metric a second session count. RETAINED, because the week has to belong to someone who came back: a week counts only when the same athlete was already logging within RETENTION_GAP_WEEKS (4) before it; a first week is real data, published separately, and is not yet evidence that anything compounds. THE SURFACE is the scoreboard and the to-do list at once — the figure, the three legs' capture rates, the per-week ledger (banked / first weeks / partial), and the BINDING LEG: the leg the corpus loses the most weeks to, with the honest prize beside it (weeks a single fix recovers is smaller than weeks it blocks whenever a week is missing two). That is what makes it a strategy rather than a scoreboard — it names the next piece of work. The counters are still shown, demoted to neutral, under VANITY_METRICS with the reason each is not the metric written next to it, so nothing drifts back into the hero slot. Definition, retention rule and leg descriptions live in core alone, so the two consoles cannot describe the metric differently. The SQL is one UNION of per-leg week markers folded with bool_or into one row per (athlete, week) — no rows in the lambda, no cap, four weeks of lookback beyond the reported window so the earliest weeks are testable for retention against real history rather than against the edge of the query. Admin-only and aggregate-only: userId groups and tests retention, and is never returned. Weeks are UTC date_trunc Mondays — correct for an aggregate, and never shown to an athlete about their own week. Today it reads zero, and says so in words rather than as a zero dressed up: the clock that cannot be compressed has not started. 23 tests." },

  // ---- Planned features ----
  { id: "coach-layer", area: "Web", status: "shipped", title: "Coach layer", detail: "CoachLink mutual-consent invites (accept/decline), roster, per-client sessions, and coaching notes incl. private (never shown to the client). Coach analytics dashboard computes from the real roster. Authorized by the relationship in the API. On web + mobile (the mobile Coach tab + plan enrollment hit the same API)." },

  // ---- Social layer + coach marketplace ----
  { id: "social-engine", area: "Core", status: "shipped", title: "Social engine (feed – privacy – leaderboard – compare)", detail: "packages/core/src/social.ts — the pure, unit-tested heart of the social surfaces, built ON the existing engines so social numbers match the rest of the app (recap.weeklyRecap, habits.streak, session.totalVolume/bestE1rmByLift, records.newPrsInSession, activity.relativeTime). Exports: normalizeHandle/isValidHandle/suggestHandle (the @handle rules used by both clients + the API); relationTo (derives self|none|following|follower|friend|close from the follow edges — friend = mutual active follow) + canViewResults (the visibility×relation privacy gate: public|followers|private, close-friends override); buildSocialFeed (turns followees' sessions into a sorted feed of completed-session + PR items, each anchored by (subjectType, subjectId) for kudos/comments, close-friends boosted); friendLeaderboard (ranks a circle by weekly volume/sessions/distance/active-days/streak/PRs); compareAthletes (head-to-head on the week's training + all-time bests on shared lifts, with a per-line scoreline); profileStats (lifetime totals for a profile card). 25 unit tests in social.test.ts. Consumed identically by web + mobile via the @hybrid/core barrel." },
  { id: "social-profiles", area: "Web", status: "shipped", title: "Public profiles + @handles + privacy", detail: "Every user can claim a public @handle (SocialProfile: handle, displayName, bio, avatar, visibility, showcase) and choose who sees their RESULTS — PUBLIC (the default, Aug 2026 — it was followers-only before) | followers-only | private — while the profile CARD stays searchable regardless. Every finished workout publishes to the feed automatically, so the default is deliberately open and the athlete opts DOWN: the schema default, the profile PUT's create fallback, both clients' form seeds and the two profile-less read fallbacks (post deep-link, Saved) all say 'public' so no two paths can disagree about one athlete; an update that omits visibility leaves the stored choice alone rather than rewriting it, and existing rows were NOT flipped by the migration — a stored 'followers' may be a choice, and a privacy setting is never loosened by a script. The privacy gate (canViewResults) is enforced server-side on the feed, leaderboard, compare and the profile-stats reads, and mirrored as defense-in-depth RLS. APIs: /api/social/profile (GET my profile + suggested handle + lifetime stats, PUT to claim/update), /api/social/profile/[handle] (GET a card, privacy-gated stats, the viewer's relation + follow state), /api/social/search (find people by handle/name). The public profile is MERGED into the one account Profile ('You') screen — there is no separate 'My profile' nav item. Web: components/social-profile.tsx renders embedded inside components/aurora/profile.tsx (claim/edit the handle + privacy, stats + connections). Mobile: components/aurora/my-social-profile.tsx renders inside components/aurora/profile.tsx ('You' tab). Viewing SOMEBODY ELSE is no longer a drawer or a sheet on either client — it is the individual user page (see user-page). Template-aware on web (radii soften under Aurora); native on mobile. NOTE: run reference/sql-social.sql (tracked as social-schema)." },
  { id: "user-page", area: "Core", status: "shipped", title: "The individual user page — one page per person, coaching included", detail: "A PERSON HAD NO PAGE. They had two PEEKS, and the two disagreed about the same human: the profile drawer (web ProfileDrawer / mobile ProfileModal — avatar, bio, three stats, follow) and, if they coached, an entirely separate storefront (web CoachDetail / mobile CoachModal — headline, programs, reviews, reached only from the marketplace and showing none of their training, level or stats). So a coach was two half-people, with no way from one to the other: the drawer's only coaching affordance was a 'View coaching' button that dumped you on the marketplace INDEX rather than on them. Both peeks were also sheets, which meant neither had an address and neither could open a handle the screen underneath had not already loaded.\n\nNOW THERE IS ONE PAGE, and a coach's page is an athlete's page plus a coaching TAB — coaching is a section of a person, not a different person. Web `?s=user&u=<handle>` (apps/web/components/user-page.tsx, a `u` param on the shell's deep-link addressing, screen `user` with its own return + push/pop like the post screen); mobile `/u/<handle>` (apps/mobile/app/u/[handle].tsx). Core `userPagePath` / `userPageHref` / `userPageUrl` / `userShare` are the one set of addresses, so a share link, a row tap and a review author all point at the same place.\n\nTHE PAGE: the person at page size (avatar, name, verified tick, the earned level chip, @handle, a coach's headline), their bio, the follower/following/sessions counts, the verbs, then tabs — OVERVIEW (volume/streak/sessions, top lifts, and the head-to-head compare panel), COACHING (rating + review count, professional bio, specialties and sports as chips, price note, an explicit taking-clients state, published programs with the week-by-week preview and enrol, then the reviews and the write-a-review box) and ACTIVITY (their recent posts, rendered by the SAME FeedCard both clients already use, with kudos, an inline thread, and a tap through to the post screen).\n\nONE BUTTON. The first cut of the head shipped FOUR buttons (Follow, See coaching, Compare, Share) directly above THREE tab chips of the identical pill shape — seven equal-looking controls in two rows, where two of the rows were not even the same KIND of thing (buttons act, tabs navigate), and the page therefore had no centre. Corrected before it could set: 'See coaching' was DELETED (it navigated to the Coaching tab, which is on screen one row below — a second door to a room whose door you can already see is noise, not help); COMPARE moved INSIDE Overview as an expander at the foot of the figures it fills in (bare ＋, ash not accent, per the app's own exit grammar: it grows in place, it does not leave); SHARE moved to the hero rail's trailing slot, the app's own home for a screen-level utility and where every OS the app ships on puts it. What is left is the single verb the page exists to offer — the follow state — as ONE full-width button (new `full` variant on both clients' button primitives), and the TABS stopped being pills: they are text on a rule with the accent under the active one, so navigation no longer wears the costume of an action. ONE RULEBOOK, IN CORE (packages/core/src/user-page.ts, 22 tests): which tabs exist for a payload (coaching only for a coach, activity only when the viewer may see results), the ONE action and whether it is primary (follow leads for a stranger; a followed page keeps the button but it stops being primary, because unfollow is a thing the page must permit and never a thing it should urge; your own page has no button at all), whether Overview offers the compare expander (canCompareWith — behind the results gate, absent on your own page), who may review (an active client, never yourself — the rule the reviews endpoint already enforced, stated once so neither client offers a box the server will reject), who may enrol, and `userPageRelation`, which folds the pending-request state that lives in `followState` rather than `relation` — the fold four surfaces each had to remember by hand, and two of them had got wrong.\n\nONE READ (GET /api/social/user/[handle]): the card + privacy-gated results, the follow counts, the coaching block, and the timeline, in one request. The privacy gate and the level estimate moved into apps/web/lib/profile-read.ts and the storefront into apps/web/lib/coach-storefront.ts, so the page and the two older endpoints (/api/social/profile/[handle], which stays the LIGHT read the handle-availability check hits on every keystroke, and /api/coaches/[handle]) cannot gate or describe a person differently. The timeline is built by buildSocialFeed over the same inputs the feed uses (90-day window, chronological — a timeline is not a ranked feed) and enriched by the new shared `enrichReactions` helper the feed route now calls too, so a workout reads identically on a person's page and in the stream. Activity is NOT fetched at all when the results gate is shut, rather than fetched and filtered; the coaching block is deliberately outside that gate, since a published storefront is public by definition and a coach with a followers-only log still has to be hireable.\n\nFOLLOW-THROUGH (second pass), closing the holes the first cut left and the three calls it had deferred: (1) EVERY DOOR NOW EXISTS. Your OWN page was unreachable — the `self` state was built and tested with nothing linking to it — so both Profile screens gained a 'View as others see it' door under the HYBRID ID line, which is also the only honest preview of what your visibility setting actually does. Social NOTIFICATIONS ('X started following you', a kudos, a comment) used to dump you at the top of the feed to go looking; they carry the actor's handle already (NotifAction.social.handle) and now open that person, on both clients. (2) THE PAGE PAINTS ON ITS FIRST FRAME. It fetches the person, which is what makes a cold link work — but almost nobody arrives cold, and the row you tapped was already holding the name, handle and avatar. New shared store `person-seed.ts` (bounded to 40, LRU on re-seed, 8 tests): the OPENER seeds what it has, the page peeks it for its first paint, the fetch confirms it a moment later. Identity fields only — nothing privacy-gated is in there, because no caller has it either. Wired at all seven openers on both clients. The web prop is one shared `OpenUser` type so the card can't be dropped at one call site. (3) PEOPLE — the follower/following counts are now doors into the list they count, as a fourth TAB rather than a new screen, so a person still has exactly one address. New read GET /api/social/user/[handle]/people?tab=… (capped at 100, +1 probe for `truncated`, blocks applied in both directions). THE PRIVACY CALL, made deliberately and reusing the EXISTING model rather than inventing a second one: the list rides the same `canViewResults` gate the stats do — a public account's connections are browsable, a followers-only account shows them to its approved followers, a private account to nobody. Rows carry no follow button: a row opens the person, and acting on a person happens on their page, where the one button lives. (4) NO SILENT CAPS. The timeline stops at 20 posts / 90 days and now SAYS so (`activityTruncated` on the payload, an end-of-list line on both clients); the people list says 'the first 100' when it truncates. A list that stops without saying it stopped reads as 'this is everything they have ever done'. Real pagination is deferred as user-page-paging. (5) THE DEAD MARKETPLACE READ, decided rather than reflexively deleted: mobile's `getCoach` CLIENT helper is removed (a client helper is a contract with nobody), but GET /api/coaches/[handle] is KEPT — an endpoint is a contract with app binaries that are already installed, and a TestFlight build from before the user page still opens its coach modal against that URL. It delegates to the shared loader, so it cannot drift from what the page shows, and it can go once no shipped build needs it. ONE SECTION HEAD: the mobile page's own `SectionHead` was deleted in favour of the kit's ASection — mobile's design-token RATCHET (lib/design-tokens.test.ts) caught it as the ninth hand-rolled section header against a ceiling of eight, which is exactly what that ratchet exists to catch. Web has no ASection twin (its section-head standard lives in prose and each screen implements it), so the web page keeps a local head built to the documented grammar; extracting the web component belongs to design-system-unification-sweep rather than here. RETIRED in the same change: ProfileDrawer, CoachDetail, ProfileModal and CoachModal, and every opener rewired to the page — the feed (rows, live strip), the post screen's author, Saved, the leaderboard, Find friends, and the coach directory. i18n EN/PL/DE (`w.user.*`); the dead `w.social.viewCoaching` key removed with the button it named. Verified: core 2708 tests, web typecheck + prod build, mobile typecheck + iOS bundle export." },
  { id: "user-page-paging", area: "Web", status: "shipped", title: "User page — the timeline and the people list page properly", detail: "The user page bounded both of its lists and said so out loud, which was honest but not the same as complete: an athlete with ten years of training could not reach year two from their page, and a coach with 400 followers could not see follower 101. Both now page.\n\nTHE TRAP, and it is the whole reason this was deferred rather than dashed off: the obvious way to page a timeline is to fetch an older WINDOW per page (days 90-180, then 180-270). That is silently WRONG here. `buildSocialFeed` decides whether a session set a record by comparing it against everything earlier IN THE ARRAY IT WAS HANDED, so a page built from only the older slice gives its oldest session nothing to be compared against — and page 3 of somebody's history sprouts first-ever PRs they actually earned years before. Verified by reading the engine, not assumed.\n\nTHE FIX: the feed is built ONCE over the athlete's whole (capped) history and a page is a SLICE of the built list. Record detection always sees the full run-up, so the badges stay honest at any depth. This costs nothing extra — `loadPublicProfile` was ALREADY loading that history for the stats and the level badge, so it is now handed back (PublicProfileRead.sessions) and reused; page 1 makes one query FEWER than the windowed read it replaced, since the separate recentSessionsByUsers call is gone. Later pages re-walk a bounded in-memory list. New shared server helper apps/web/lib/user-timeline.ts owns the build+slice so the page read and the activity endpoint cannot diverge.\n\nTHE CURSOR is keyset, not an offset (packages/core/src/feed-cursor.ts, 14 tests): `(at, id)` as a total order, encoded `<at>.<id>`. An offset describes a position in a list that keeps moving — finish a workout mid-scroll and every subsequent offset shifts by one, so page 2 repeats or skips a row. A keyset cursor names the last thing you SAW, so new items arriving at the top change nothing, and a DELETED cursor item still pages correctly because the cursor is a position in time rather than a reference to a row that has to still exist. The id tiebreak matters: two sessions can share a timestamp (a bulk import, two workouts finished in the same second) and `at` alone would either drop one or loop on it forever. Tests pin the full walk (no repeats, no gaps), the new-item-at-top case, the deleted-cursor case, ties, and junk input degrading to page one — the cursor arrives from a URL, so it is bounded and pattern-checked like every other deep-link value.\n\nENDPOINTS: page 1 of the timeline still rides on GET /api/social/user/[handle] (so opening a person is ONE request) and carries `activityCursor` + `activityCapped`; pages 2+ come from the new GET /api/social/user/[handle]/activity?cursor=…, which re-checks the privacy gate rather than trusting the first call — a cursor is just a string in a URL and must never be a way around the gate. The people list keysets on the Follow edge itself (orderBy createdAt desc + id desc, Prisma cursor + skip 1), and blocks are filtered AFTER the page is cut so a blocked row shortens the page rather than pulling an extra stranger into it and breaking contiguity. Page sizes dropped with paging in place (people 100 → 30, activity 20 → 15): with a door there is no reason to pay for rows nobody scrolled to, and a shorter page reaches the screen sooner.\n\nTHE DOOR, on both clients: a shared LoadMore that APPENDS (rows already read stay put) taking the app's expander grammar — a bare ＋ in ash, no ring, no arrow, because an arrow promises a destination and there isn't one, there is just more of what you are already reading. The honesty rule survives paging rather than being retired by it: when the timeline genuinely ends, it just ends; when it stops because it hit the 400-session history cap, a quiet line says the end is OURS rather than theirs ('that is the last {n} we can reach'). i18n EN/PL/DE; the old activityTruncated / truncated booleans are gone, replaced by the cursors themselves so there is one source of truth for 'is there more'." },
  { id: "social-follow", area: "Web", status: "shipped", title: "Follow + friends + close-friends graph", detail: "An asymmetric follow graph with two richer tiers, exactly as scoped: you FOLLOW anyone (active immediately for public/followers profiles; a follow of a PRIVATE profile lands PENDING until they approve via /api/social/follow/respond); a mutual active follow makes you FRIENDS (derived, no extra table — friends unlock the leaderboard + richer compare); and you can mark a followee a CLOSE FRIEND (a curated inner circle that's boosted to the top of the feed). Follow model = (followerId, followeeId, status active|pending, closeFriend). APIs: /api/social/follow (POST/DELETE), /follow/respond (approve/deny a pending request), /close-friend (toggle), /connections (followers/following/friends/requests), /suggestions (people-you-may-know from friends-of-friends + a shared coach). Web: the discover screen (components/social-discover.tsx, search + suggestions + follow) + the connections tabs inside the account Profile screen. Mobile: app/discover.tsx + the connections tabs inside the Profile ('You') screen." },
  { id: "social-feed", area: "Web", status: "shipped", title: "Activity feed + kudos + comments", detail: "A real cross-athlete activity feed (built by buildSocialFeed from your active-followees' REAL logged sessions, with the PRs detected in them listed ON the workout that set them — no fabricated posts, and never a second card for the same session), with your own activity woven in. Each item carries kudos (👏) + threaded comments, anchored by (subjectType, subjectId) so reactions attach to derived items without a materialized posts table. APIs: /api/social/feed (the engine feed enriched with kudos/comment counts + whether I've cheered each item), /api/social/kudos (toggle), /api/social/comments (GET/POST). Web: components/social-feed.tsx (feed nav) — cards with cheer + inline comments, tap an author → ProfileDrawer, tap the card → the post's own page, with the whole session on it (connect-feed-open-post). Mobile: app/feed.tsx + components/feed-view.tsx with the same. TODAY: there is no preview strip and no stories rail — both were tried and both are gone, and this entry claimed them for longer than they existed. Today's CONNECT surface embeds the REAL feed as a hub tab (SocialFeed in aurora/today.tsx, FeedView in aurora/home.tsx), so the strip that used to summarise it (aurora/feed-preview.tsx) went unmounted and is now deleted (see plans-cover-flow-rehome). The stories rail (aurora/stories.tsx) never survived either: authored ephemera fit a lifting app badly, and the question athletes actually ask — who is training right now — is STATE the app already holds, so presence replaced it (the 'Now training' strip, feed-live.ts + feed-live-strip.tsx on both clients; see connect-feed-live for the full argument). IDENTITY / PRIVACY: a card leads with the athlete's real name (User.name / SocialProfile.displayName) — an email address is NEVER shown; authorCards no longer derives a handle from the email local-part (opaque id-slice fallback instead) and carries a hasProfile flag so the feed hides a synthetic @handle entirely (a profile-less athlete renders as just their name, never '@a1b2c3d4'); feedCardView falls back to '@handle' only for a genuine handle. Push delivery of kudos/follow notifications is the one piece still pending (see push-notifications)." },
  { id: "social-leaderboard", area: "Web", status: "shipped", title: "Friends leaderboard", detail: "A weekly leaderboard across your FRIENDS (mutual follows) + you, ranked by a switchable metric — volume (kg), sessions, distance (km), active days, current streak, or PRs — all computed by friendLeaderboard off everyone's real sessions (privacy already satisfied: friendship is mutual consent). /api/social/leaderboard?metric=. Web: components/social-leaderboard.tsx (leaderboard nav, metal medals, you highlighted, tap a rival → compare). Mobile: app/leaderboard.tsx with a horizontal metric switch." },
  { id: "social-compare", area: "Core", status: "shipped", title: "Head-to-head friend comparison", detail: "compareAthletes (social.ts) scores you vs another athlete line-by-line on the week's training (volume, sessions, distance, active days, streak) PLUS all-time bests on the lifts you BOTH train, with a headline scoreline (who leads more lines). Gated by the same privacy rule as the feed — you must be allowed to see their results. /api/social/compare?handle=. Surfaced inside the profile drawer/modal on both clients (a 'Compare' action from the feed, discover, leaderboard or a profile). Generalizes the math behind the coach-only cross-roster comparison so peers get it too." },
  { id: "coach-marketplace", area: "Web", status: "shipped", title: "Coach marketplace (directory + storefronts + start a program)", detail: "Turns the previously invite-ONLY coaching system into a discoverable marketplace: any user can browse/search a directory of coaches (verified first, with rating + published-program count + sport/specialty filter), open a coach's public STOREFRONT (CoachProfile: headline, bio, specialties, sports, accepting-clients, price note), and START an online program with one click — a CLIENT-initiated enrolment (the inverse of the existing coach→client CoachInvite). Starting a program opens a CoachLink (PENDING, or ACTIVE when the coach set autoAccept) + a ProgramEnrollment, and the coach accepts/declines from their storefront; CoachProgram gains published/summary/level/visibility so a coach lists a program to the world. Reuses the whole existing CoachLink/CoachProgram/Assignment machinery — once active the coach programs against the client exactly as before. APIs: /api/coaches (directory), /api/coaches/[handle] (storefront + published programs + reviews + my state), /api/coaches/enroll, /api/coach/profile (GET/PUT the coach's own storefront), /api/coach/enrollments (list + accept/decline). Web: components/coaches.tsx (coaches nav — directory + coach detail drawer + a 'My coaching' storefront editor for coaches). Mobile: app/coaches.tsx (directory + coach modal + storefront). FREE enrolment in this build; PAID checkout is a separate blocked layer (social-paid-coaching). NOTE: run reference/sql-social.sql." },
  { id: "coach-reviews", area: "Web", status: "shipped", title: "Coach ratings & reviews", detail: "Clients rate (1–5) and review a coach; only an athlete with a real (active/ended) CoachLink may review, one review per coach↔client, and the storefront shows the average + count (verified-client reviews, not anonymous spam). CoachReview model; /api/coaches/[handle]/reviews (GET/POST). Surfaced on the coach storefront drawer/modal on both clients." },
  { id: "coach-rail-marquee", area: "Core", status: "shipped", title: "Follow-a-coach rail — marquee card redesign", detail: "The Today/Explore coach rail (web + mobile aurora/coach-rail.tsx, at parity) redesigned from the cramped 220px chip card to a 290px MARQUEE card — the winning concept from the design/follow-coach-redesign-ideas.html study, applied to every coach as a swipeable slider. Each card: an accent wash bleeding from the top corner, an accent-ringed avatar with the initials + verified check in the coach's accent-text tone, the name in the display face, ONE non-wrapping mono specialty line ('OLYMPIC LIFTING – PEAKING' — replaces the raggedly-wrapping chips), an athlete pull-quote doing the selling (DiscoverCoach.quote, falling back to the coach's own headline for real marketplace coaches until storefronts expose a featured review), and a bottom-pinned proof strip (★ rating / reviews / years coaching — cells drop when unknown). Fixes the four sins of the old card: ragged chip wrapping, too-narrow width, no social proof, and no shared card geometry. Data: DiscoverCoach gains optional quote + years (mapped from the marketplace API when present; seeded on the placeholder people)." },
  { id: "social-feed-posts", area: "Core", status: "shipped", title: "Shareable feed posts (status updates + share-a-PR-card)", detail: "First-class feed posts on top of the derived (session/PR) feed: a user shares a STATUS update or a PR CARD (attach-my-latest-PR computes their current best lift server-side) to their followers. Post model (kind status|pr|workout, text, data Json); reactions reuse the (subjectType,subjectId) anchoring with subjectType 'post' (kudos/comments whitelists extended). buildSocialFeed now merges each author's posts into the sorted feed (close-friend boosted, same as sessions). APIs: POST /api/social/posts (text + attachPr → server computes the PR card via bestE1rmByLift), DELETE /api/social/posts/[id] (also clears its kudos/comments). A composer (status + 🏆 attach-latest-PR) sits atop the Feed on web (social-feed.tsx) + mobile (app/feed.tsx), with delete on your own posts. Posts respect the same follow/privacy/block gate as the rest of the feed. Pure feed-merge unit-tested. NOTE: re-run reference/sql-social.sql (idempotent) to add the Post table. NEXT: a 'Share to feed' button on the post-workout summary (the shareable story-slide moment) + photo attachments." },
  { id: "feed-row-actions", area: "Core", status: "shipped", title: "Feed row — save, share, and the ⋯ menu", detail: "Zone F of a post had two verbs and both were PUBLIC (kudos, comment) — they hand something to the author. The two verbs a reader performs for THEMSELVES were missing, and so was the stream's answer to 'I do not want to see this'. Added on BOTH clients in one change: (1) SAVE — a bookmark on the right of the action row that fills (like the bolt does when cheered) so saved state is readable while scrolling past, not on inspection; per-device, keyed off the (subjectType, subjectId) anchor kudos and comments already use, so a save survives the feed being rebuilt on every load. (2) SHARE — hands the post to the OS: navigator.share on web with a clipboard fallback (a desktop browser gets 'Link copied'), React Native Share on mobile; the message leads with WHO because a share lands in a chat where nobody has the app's context ('Ada Ruiz – 180 kg Back Squat'), joined with an en dash, never a middot. Neither verb carries a COUNT — a save is nobody else's business and a share isn't a score. (3) THE ⋯ MENU at the top-right of the row — a SMALL CARD ANCHORED TO THE GLYPH, not a bottom sheet: follow / mute / not interested / report / block on someone else's post, delete on your own. It shipped as the app's shared Sheet first, which was the cheap answer because the idiom already existed, and it was the wrong weight — a full-width panel sliding up with the whole screen receding behind it, to offer five one-word choices about ONE row, costs the athlete their place in the stream to answer 'mute?'. The card hangs off the pressed glyph and keeps the post and the scroll position visible throughout. One label per row, no explanatory second line: at popover size a description under each label doubles the card's height and turns a glance into a read (the 12 *Sub i18n keys were dropped with it). Positioning is pure CSS on web (a position:relative anchor, right-aligned so it opens leftward off the row's right edge, with a stacking context so the next post can't paint over it) but CANNOT be on mobile — a card drawn inline would be clipped by the FlatList and by the row itself on Android — so mobile renders it in a transparent Modal placed from the ⋯'s measureInWindow rect (collapsable={false}, or RN prunes the view and it can't be measured), right-aligned to the glyph, clamped inside the screen gutter, and FLIPPED above the glyph when there isn't room below. Both dismiss on a press anywhere (a full-page/full-screen catcher — a menu you have to aim at to dismiss is a trap); web also takes Escape and restores focus through the shared useDialog. DELETE NOW ASKS FIRST ON MOBILE TOO: web has always put a confirm in front of it, mobile deleted on the first press of the bare ×, and moving delete into a small anchored menu makes a mishit easier rather than harder — both clients now read the same w.social.deletePostConfirm string. THE CORNER USED TO HOLD A BARE ×: an unlabelled destructive control on your own posts and nothing at all on everyone else's. Delete now lives inside the menu with a label and a consequence line, and the × (and its aria-only key) is gone. THE MENU'S ROWS ARE CORE'S, not the clients': feed-actions.ts `feedMenuActions` returns the list, the order, the label keys and — the part that matters — WHICH ROWS ARE STILL PLACEHOLDERS, so a row added on one client cannot go missing on the other and neither client can quietly decide a placeholder is real. An empty list is a valid answer (my own session or PR row has nothing to offer) and the clients then draw no ⋯ rather than a button onto an empty sheet. PLACEHOLDERS SAY SO: pressing follow/mute/block/report/not-interested tags that row SOON and leaves the sheet open, instead of firing a silent no-op that reads as a broken button — see feed-menu-wiring for the endpoints they will hit. NO 'copy link' row, deliberately: sharing is the action row's own icon and both platforms' share sheets already contain Copy (on mobile it would also need a native clipboard module this build doesn't carry). Glyphs are hand-drawn in the same 16-unit box at the same 1.5 stroke as the bolt and the bubble beside them — an Aurora-set icon is a 72-unit box on its own stroke ramp and would have drawn visibly lighter than its neighbours. Pure model + 16 unit tests in core (feed-actions.test.ts); the per-client stores (apps/*/lib/feed-actions.ts) mirror the notification read-state idiom exactly. EN/PL/DE." },
  { id: "feed-menu-wiring", area: "Core", status: "shipped", title: "The feed ⋯ menu — follow, block and report are real", detail: "Three of the menu's five rows are now WIRED on both clients, against endpoints that already existed: follow/unfollow (/api/social/follow), block (/api/social/block — mutual invisibility, tears down the follow edges both ways) and report (/api/reports). THE FOLLOW ROW IS A TOGGLE, so it had to know the answer before it could name itself: a row that offers to follow someone you already follow makes the menu look like it doesn't know who you are. FeedItemView now carries `relation`, which the feed route was ALREADY deriving for the ranker (relationOf, extracted from signalsFor) and the Saved resolve route derives from the follow edges it already loads for its visibility check — so the label costs no extra query and no client round trip. core's `feedMenuActions` picks 'Unfollow {h}' over 'Follow {h}' off it, and treats an absent relation as not-following (the safe direction — a redundant follow no-ops server-side). REPORTING AIMS AT THE RIGHT THING: a first-class Post is a content row and is filed against directly ('post' added to the /api/reports target whitelist with an existence check, alongside talentProfile/socialProfile/comment), but a session or PR card is DERIVED — there is no content row to file — so on those the menu reports the ATHLETE and the label says 'Report {h}' instead of naming a post that isn't one. Core picks the label; the clients pick the matching target. EVERY REAL ACTION REPORTS ITS OUTCOME IN PLACE — the row swaps to a past-tense tag (Following / Reported) rather than closing on you, which is the same affordance the SOON tag already established. Only block closes the card, because after it there is nothing left to act on. WHAT THE SCREEN DOES ABOUT IT: one `onAuthorChanged` callback per row (not three props) carries a change that belongs to the AUTHOR rather than the row — a follow re-labels every card by that person, a block drops them from the stream immediately, since the server has already made them invisible and leaving the rows up until the next load would be the one place the app disagreed with itself. Both the feed and the Saved screen apply it, on both clients. STILL PLACEHOLDERS, and still saying so: mute needs a column on Follow plus a filter in buildSocialFeed, and 'not interested' is a ranking signal that belongs in feed-rank.ts rather than a hard hide — tracked as feed-menu-remaining. The unit test asserts exactly which rows are real, so flipping a flag without wiring the endpoint fails. EN/PL/DE." },
  { id: "feed-menu-remaining", area: "Core", status: "planned", title: "Feed ⋯ menu — mute and 'not interested'", detail: "The two rows still drawn as placeholders, because each needs state that does not exist. MUTE (keep following, stop seeing the posts) is a column on Follow — `muted` — plus a filter in buildSocialFeed, and a way to unmute (the menu row toggles, like follow does now). NOT INTERESTED is not a hide at all: hard-hiding one post teaches the ranker nothing and the next one like it arrives tomorrow, so it should write a per-(viewer, author or subject) negative signal that feed-rank.ts reads, alongside the interaction counts it already weighs. Flip `placeholder` to false in feed-actions.ts as each lands — feed-actions.test.ts asserts on exactly that set." },
  { id: "feed-saved-screen", area: "Core", status: "shipped", title: "Saved — the shelf behind the bookmark", detail: "The bookmark shipped one change before this screen did, which made it a button that swallowed things: you could mark a session\'s scheme and no surface listed what you had marked. SHIPPED on BOTH clients (web components/social-saved.tsx as an app-shell screen; mobile app/saved.tsx), reached from a bookmark glyph on the feed\'s own tab row — the same glyph that fills on a post opens the shelf — and listed in More under Social (nav.ts `saved`, AURORA_NAV_ICONS bookmark). IT RENDERS THE SAME ROW AS THE FEED (feed-card.tsx, with the same kudos, the same comment thread — Comments is now EXPORTED from each client\'s feed screen rather than copied, since a second copy drifts the moment either is touched): a saved post rendered as some reduced \'saved item\' summary would be a second card model to keep in step, and would lose the figures it was saved for. THE HARD PART IS THE READ. The device holds only the (subjectType, subjectId) KEYS, and /api/social/feed is a 14-day window over people you currently follow — so a Saved screen built by filtering the feed would silently show a subset of what you saved, which is the same swallow-your-bookmarks failure wearing a list view. New endpoint POST /api/social/saved resolves keys against the ROWS instead: it looks up the sessions and posts, re-derives visibility AT READ TIME (blocks + relationTo + canViewResults, defaulting a profile-less author to the app's default, public (kept in lockstep with the schema default so this path can never disagree with a fresh profile row)), and rebuilds each visible author\'s cards through the SAME buildSocialFeed the feed uses over their own history — necessary because a PR card is anchored to its session and PR detection compares against everything before it — then keeps the ones that were actually saved and enriches them with live kudos/comment counts. No schema: it reads existing rows, so nothing here waits on a migration. THE TWO MISS LISTS ARE PART OF THE SCREEN, not an error path: `gone` (the row is deleted — the client prunes it, the only way the list shrinks by itself) and `hidden` (it exists but you may no longer see it — KEPT and counted on screen, because a private switch reverses and forgetting a post over it is the original failure). The key parser is the trust boundary — keys come from device storage, which the user can edit — so the subject type is checked against a closed set and the id is length-bounded before anything reaches a query. Order is the DEVICE\'s (newest save first, core `orderBySaved`), not the server\'s: what the athlete remembers is when they saved a thing, not when it was posted. Pages at 40 keys per request, since resolving one costs the author\'s session history. EN/PL/DE; pure helpers unit-tested in feed-actions.test.ts." },
  { id: "feed-save-server-sync", area: "Data", status: "shipped", title: "Saved posts — the shelf follows the athlete", detail: "The Saved SCREEN ships (feed-saved-screen) and the shelf now MIRRORS to the server so a post saved on the phone is on the laptop. APPLIED: sql-saved-post.sql was run in the Supabase SQL Editor (Aug 2026), so the shelf is live. Schema: prisma model SavedPost — (userId, subjectType, subjectId, savedAt), unique on the triple, indexed (userId, savedAt desc), RLS scoped to the one party who saved it. Deliberately NO ownerId (unlike Kudos, which carries one so the recipient can read their own): a save is private, nobody is notified, and storing the author would create a reason to expose it later. Deliberately NO foreign key to Session/Post either — a 'pr' row's subjectId is the SESSION the PR was set in, so one id column would need two FK targets, and a dangling row already has a defined outcome because the resolve route re-checks existence on every load and returns it as `gone`. Routes: GET/PUT /api/social/saved/sync. OPS, NOT WHOLE-LIST WRITES, and that is the load-bearing decision: a device that pushes its full list cannot express a removal — the server sees a key missing and cannot tell 'the athlete unsaved this' from 'this device hasn't heard yet' — so whole-list writes resurrect unsaved posts from any stale device. THE RECONCILE POLICY IS CORE'S (`reconcileFeedSaved`, unit-tested): the first sync on a device UNIONS local and server (a device may hold saves made before the table existed, and neither side is wrong, so nothing is lost turning it on) and hands the server what it is missing; every sync after takes the server's list wholesale, which is what makes an unsave stick everywhere. A `synced` flag on the stored state is what separates the two — without it the union runs forever. The device copy is NOT a cache: it stays what the UI reads, so a bookmark still fills on the press frame and the shelf still opens offline. Order survives the round trip because a first sync stamps savedAt backwards from now, one ms apart, rather than collapsing the whole shelf onto one timestamp. Both clients reconcile when the feed or the Saved screen opens. UNTIL THE TABLE EXISTS both routes soft-degrade to `unavailable` (P2021/P2010, the pattern the whole social API uses) and each device keeps its own shelf — which is exactly how the bookmark shipped, so nothing breaks before or after." },
  { id: "feed-share-deep-link", area: "Core", status: "shipped", title: "A shared post link lands ON the post", detail: "`feedShareUrl` (core/feed-actions.ts) emits https://hybrid.app/app?s=post&post=<type>:<id> — the shell's own query-string addressing (apps/web/lib/deep-link.ts, which now owns a `post` param; its value carries a colon, so parsing checks it against its own pattern rather than the plain-id one, still bounded). The shell resolves the key on mount and on Back/Forward (`landing`), mirrors it while the post is open and drops it on leaving, so a link survives a refresh, a bookmark and a paste. A key that names no readable subject lands on the FEED rather than on an empty page. Mobile has the matching route (`feedPostPath` → /post?type=…&id=…, app/post.tsx). Both fetch the post itself, so a recipient who has never scrolled past it still opens it — see connect-feed-open-post." },
  { id: "social-i18n", area: "Core", status: "shipped", title: "Localize the social + marketplace surface (EN/PL/DE)", detail: "The whole social/marketplace surface is now EN/PL/DE, matching the rest of the Aurora app. The web social-* screens and their mobile app/* twins share ONE key set so both clients read the same strings. DONE (all shipped): (1) the Today/Explore COACH RAIL (w.explore.coach*); (2) the COMMUNITY CORE — Feed (composer, cheer/comment, empty, delete/error), Discover (find-friends, search, suggestions, the coach reason tag), Leaderboard (title/sub/empty/You), and the shared follow/profile-view primitives (FollowButton relations, Stars 'no reviews', verified-coach tooltip, avatar alt, profile-modal stats, block/report confirms, private-results, top lifts, compare header) — web social-feed/discover/leaderboard/ui/profile.tsx + mobile app/feed,discover,leaderboard.tsx + social-kit.tsx; (3) the COACHES MARKETPLACE — directory (title/sub/tabs/search/empty/cards, 'N programs' plural, '– full'), the coach-detail storefront a client sees (programs, week/day preview, enrol, reviews), and the coach's own storefront editor (field labels + placeholders, accepting-clients/auto-accept, publish, enrolment accept/decline) — web coaches.tsx + mobile app/coaches.tsx; (4) the PROFILE-EDIT / identity settings forms — the focused field editors (name/handle/display-name/bio/email/visibility with live handle-availability copy) and the sectioned Photo/Identity/Contact/Visibility layout — web social-profile.tsx editor + mobile my-social-profile.tsx + app/profile-edit.tsx. All keys in i18n-web/home.ts (w.social.* + w.coaches.* + w.profile.*, EN/PL/DE), reusing common.* for close/cancel/loading/error/save/done. Handle rules, block/report confirms and the taken/available lines interpolate {h}/{n} at the call site." },
  { id: "social-leaderboard-clubs", area: "Core", status: "planned", title: "Club & challenge leaderboards", detail: "The leaderboard is friends-only (mutual follows). Add club/challenge leaderboards — a named group or a time-boxed challenge (e.g. 'most volume in March', a Hyrox prep cohort) anyone can join, ranked by the same friendLeaderboard metrics. Ties into the Org graph (a team is already a group of users) and the accountability loop." },
  { id: "social-avatar-upload", area: "Integrations", status: "blocked", title: "Profile photo upload (avatars)", detail: "The profile editor takes an avatar IMAGE URL (and renders it), but there's no in-app image upload — users must host the image elsewhere. Real upload needs a Supabase Storage bucket + signed-upload policy + an image picker on each client (web file input; mobile expo-image-picker). Initials monograms are the fallback, and the profile-settings-redesign added one-tap branded GRADIENT PRESETS (AVATAR_PRESETS — small PNG data-URIs that persist + render on both clients today) so people have a non-default avatar without a photo. A disabled 'Upload photo (soon)' button is already in the editor UI on both clients; only the actual photo write is blocked.", blockedBy: "A Supabase Storage bucket (e.g. 'avatars') with an RLS upload policy scoped to the owner, then wire the existing 'Upload photo (soon)' button to the upload (web <input type=file> → signed upload; mobile expo-image-picker, which is already installed). The avatarUrl column + rendering + the picker UI already exist." },
  { id: "social-safety", area: "Backend", status: "shipped", title: "Block + report (social safety)", detail: "Block model (mutual invisibility): a blocked pair disappears from each other's feed, search, suggestions, leaderboards and compare, can't follow, and blocking tears down any follow edges both ways. /api/social/block (POST/DELETE). Reporting reuses the existing Report model + admin moderation queue — /api/reports now accepts targetType 'socialProfile' and 'comment' (existence-checked, deduped, rate-limited). Block + Report actions on the profile drawer/modal on both clients. blockedIdsFor() is applied across all the social read paths. NOTE: re-run reference/sql-social.sql (idempotent) to add the Block table." },
  { id: "social-schema", area: "Data", status: "shipped", title: "Social + marketplace schema (Supabase tables + RLS)", detail: "The social/marketplace models (SocialProfile, Follow, Kudos, Comment, CoachProfile, ProgramEnrollment, CoachReview + the new CoachProgram columns) are in prisma/schema.prisma and reference/sql-social.sql (idempotent CREATE TABLE / ALTER + RLS, consistent with the existing app_user_id() policy approach). The agent can't reach Supabase/Postgres from this sandbox, so the SQL must be run by hand. Every /api/social/* and /api/coaches/* route soft-degrades (P2021/P2010) to empty until then, so the app stays deployable. APPLIED: sql-social ran in production via sql-pending.sql (Jul 2026) — the social + marketplace APIs now serve real tables." },
  { id: "social-paid-coaching", area: "Backend", status: "blocked", title: "Paid coaching programs (marketplace checkout)", detail: "The marketplace ships with FREE enrolment (start a program → a coaching relationship, no charge). Charging for an online program/package reuses the already-built billing abstraction; it's gated on the same Stripe credentials as the rest of billing — see `billing` and `full-billing-stripe`. The CoachProfile.priceNote is free-text marketing copy until then.", blockedBy: "A Stripe account + keys in the server env (see the `billing` / `full-billing-stripe` capabilities). Then add a checkout step to /api/coaches/enroll and gate ProgramEnrollment 'active' on payment." },
  { id: "plans-enroll", area: "Backend", status: "shipped", title: "Plan enrollment", detail: "Enrolling in a plan builds a macrocycle from the engine and persists it (POST /api/macrocycles); Periodize renders your enrolled season. Both clients enroll from the Plans screen (web fetch; mobile enrollPlan)." },
  { id: "plans-leave", area: "Backend", status: "shipped", title: "Leave / delete an enrolled plan", detail: "DELETE /api/macrocycles/[id] ends a self-enrolled season (ownership-checked, mirrors /api/sessions/[id]). The athlete explicitly chooses whether to KEEP their logged workouts (default — History/analytics untouched) or ALSO DELETE the Session rows logged during the plan's active window (startedAt → the next-started season, since sessions carry no planId and the plan↔session link is derived); the destructive branch is armed by a typed-DELETE confirm. The plan's PlanDayOverride skips/postpones are cleared either way (best-effort if the table isn't migrated). Surfaced as a QUIET 'Leave plan' text link at the BOTTOM of the enrolled plan's own detail page on BOTH clients (LeavePlanSection in web + mobile aurora/plans.tsx) — deliberately NOT a persistent button (a product decision: an ever-visible exit reads as an invitation to quit and demotivates); the Plans root keeps an info-only 'Current plan' card with no leave affordance. Localized EN/PL/DE (w.train.plans.leave*)." },
  { id: "plans-leave-goal-only", area: "Web", status: "shipped", title: "Leave UI for goal-only enrollments", detail: "A season enrolled WITHOUT a named plan (Macrocycle.planId null — e.g. goal-only onboarding or a coach-programmed season) has no plan detail page to host the quiet leave link, so BOTH clients render the shared LeavePlanSection at the bottom of the Periodize (season) screen for planId-null seasons only (named-plan seasons keep their single exit on the plan's own page). Same keep-vs-delete-history flow + typed-DELETE confirm. Known sliver: on WEB the Periodize screen is gated off the free/casual persona, so a casual + goal-only user has no web leave surface (mobile Periodize is ungated and covers them); acceptable for now — revisit if casual goal-only enrollment becomes a real path." },
  { id: "plan-arbiter", area: "Core", status: "shipped", title: "Plan-as-arbiter — unify Plans + Sport + daily prescription", detail: "Three engines independently answer 'what should I train?' on different horizons: Plans/periodization (engines/periodization.ts) sets the SEASON arc (phase, load vs recovery week); the Sport engine (sports.ts → prescribeForSport) picks today's transferable S&C from the athlete's e1RM; and the daily route (engines/prescription.ts → prescribeSession) emits a session from readiness math. They used to overlap (the same lift twice) or pull against each other (a heavy daily lift in a deload week) with no coordination. reconcilePlan (engines/reconcile.ts, pure + 7 unit tests) makes the enrolled macrocycle PHASE the arbiter: the current microcycle's intensity/volume is the WEEK'S ENVELOPE (via currentPhase()), and the daily + sport movements are dosed WITHIN it — working loads scaled toward the phase intensity, set counts toward the phase volume (clamped 0.6–1.15× load, 0.5–1.2× volume, plate-rounded). Overlap is resolved by priority: the readiness-aware daily primary lift owns its movement and a sport block naming the same lift is dropped as a duplicate; a recovery/deload week trims accessory transfer work and conditioning rounds so a planned deload isn't undone by bolt-on sport volume. Returns one ordered session (strength → sport transfer → conditioning), the envelope + factors, the dropped movements with reasons, and a plain-language why. SURFACED as a shared 'This week' card (components/reconciled-week.tsx) on the web Today AND Periodize screens, and mirrored natively on the mobile Today — phase + envelope + the single reconciled session (each block tagged daily/sport, with what was dropped), built from the enrolled season (currentWeek derived from the macrocycle's startedAt), the daily prescription, and the athlete's saved sport selection. MATERIALIZED: scheduleWeek (reconcile.ts) + reconciledToSessionBlocks lay the reconciled session across the week's training days (even-spread offsets), and a 'Schedule this week' action writes them as self-authored Assignments via POST /api/assignments (athlete = assignedBy = me, capped at 14) — they land on the Calendar (engines/calendar.ts) alongside coach assignments. Because every day comes from ONE phase-arbitrated plan, the dated rows can't collide with each other. The week is VARIED by SEQUENTIAL re-prescription, not a hard-coded rotation: buildTrainingWeek prescribes each training day off a log that already carries the simulated fatigue of the earlier generated days this week (real sessions aged forward, each generated day inserted at its offset), so the engine naturally avoids the muscles it just hammered and the rotation is EMERGENT — a heavy squat today is why tomorrow picks a press. Each day still reconciles against the phase and gets a round-robin share of the sport transfer work; today's wearable reading only colours day 0. The Schedule action uses it (scheduleWeek remains the simple repeat-one primitive). AVAILABILITY-AWARE: days/week is no longer fixed — trainingDaysPerWeek (habits.ts) infers it from the athlete's actual recent cadence (median distinct active days/week), falling back to their onboarding answer (persisted client-side as hybrid.daysPerWeek) before there's history; surfaced in the Schedule button (e.g. '– 4d'). COACH-SIDE: the coach client detail (components/coach.tsx) has a 'Generate a periodized week' action — pick a goal, and the SAME buildTrainingWeek runs on the client's real logged sessions (loads dosed off their numbers, days/week from their cadence) first PERSISTS the macrocycle to the client (POST /api/coach/links/[id]/macrocycle, CoachLink-gated; matching active-coach RLS in reference/sql-coach-macrocycle.sql) so the client's Periodize/Today show the same season the coach programmed against, then assigns the week. RE-SYNC OFF REAL RESULTS: scheduling is idempotent and refreshable — POST /api/assignments accepts replace:true, which clears the upcoming, still-pending self-authored days (today onward, completed + coach days untouched) in one transaction before writing the new set, so re-running regenerates the rest of the week off your ACTUAL logs (not the up-front simulation) and double-tapping never duplicates. AUTO RE-SYNC (event-driven): weekNeedsResync (reconcile.ts) flags a self-scheduled week whose pending future days were generated BEFORE the latest logged session; web Today/Periodize + mobile Today watch for a NEWER session (the screens re-render on a fresh log; mobile reloads on tab focus via useFocusEffect) and, keyed on the latest-session timestamp, regenerate the rest of the week once per new session — so logging a day automatically re-prescribes the remainder off that real result. COACH WEEK PICKER: the coach generator surfaces the season's weeks (Wk N – phase, deload-flagged) so they can program any week's stimulus (peak/deload), not just week 1. 16 reconciler/cadence unit tests. NEXT: nightly server-side regeneration so the week refreshes even if no client opens, and richer coach controls (event-anchored seasons, multi-week blocks)." },
  { id: "i18n", area: "Core", status: "shipped", title: "Localization (EN/PL/DE)", detail: "Shared i18n in core (base dictionary + per-domain web bundles), persisted language switchers on web + mobile, 1562 keys at full EN/PL/DE parity (no missing/extra keys per language). FULL SCREEN COVERAGE on both clients: every web Aurora screen (incl. the onboarding wizard, previously hardcoded EN) and ALL ~28 mobile Aurora screens (Home, Cockpit, AI-coach, Builder, Calendar, Check-in, Competition, Connections, Exercises, Force-plate, Longevity, Nutrition, Onboarding, Performance, Periodize, Profile, Progress, Run-track, Tactical, Talent, Trends, Velocity, Video, Volume, Welcome, global-nav, plus the earlier Today/Train/History/More tabs, live Workout + summary, Plans/Sport/Coach) now resolve through t() — including native dialogs, capability/blocked notes, env hints and empty states. A PL/DE correctness audit also fixed real mistranslations (e.g. PL 'bój'→'ćwiczenie' for lifts, 'płyta siłowa'→'platforma dynamometryczna' for force plate, 'wioślarz'→'wiosłowanie' for rowing; DE 'Hebungen'→'Lifts', the swapped Intermediate/Advanced experience tiers, decimal-comma formatting). Data content (plan descriptions, sport rationales) stays source-language by design. SCOPE: Aurora is now the ONLY template and it is FULLY localized on both clients — every Aurora screen/component plus the settings preference-row labels/descriptions resolve through t(). (The old classic-template screens were deleted in the Aurora-only collapse, so the previously-tracked i18n-classic-template work is moot — there are no classic screens left to localize.) Admin-only screens (apps/*/admin/*) are intentionally English." },

  { id: "i18n-today-redesign-labels", area: "Core", status: "shipped", title: "Localize the Today redesign labels (Performance-State tags + sport-picker sheet)", detail: "The Today redesign's new strings now resolve through t() on BOTH clients at full EN/PL/DE parity. Keys live in the shared dictionary (i18n-web/home.ts, merged into TRANSLATIONS via WEB_BUNDLES, which mobile also consumes through makeTWithOverrides): w.home.quickSport.search / .choose / .close and w.home.today.psTag.readiness / .injury / .velocity / .analytics (HPI stays a literal acronym; the AI-coach tag reuses w.home.today.aiCoach). Web (apps/web/components/aurora/today.tsx + quick-sport.tsx) and mobile (apps/mobile/components/aurora/home.tsx — the Performance-State feature tags — and the shared sheet apps/mobile/components/quick-sport.tsx — title/search/close) all read these keys; no more English literals for the redesign labels." },


  // ---- First-test feedback fixes (workout-history-dashboard) ----
  { id: "workout-archive-delete", area: "Backend", status: "shipped", title: "Archive / delete a workout from History", detail: "Two distinct controls: ARCHIVE excludes a workout from analytics AND hides it (recoverable); DELETE permanently removes it. Session gains a nullable archivedAt; /api/sessions GET excludes archived by default and '?archived=1' returns only archived (the restore view); /api/sessions/[id] PATCH {archived} soft-archives/restores and DELETE permanently removes — both own-rows-only. Archived rows are excluded from ALL session-derived analytics, not just the list: the shared athleteState helper (HPI/readiness/injury risk → /api/state, tactical, longevity), /api/ai-coach, and the coach analytics surfaces (squad, roster, compare, client sessions) all filter archivedAt:null. Admin platform stats + the user's own data export still include them (real history). Surfaced on web History + mobile History (archive/delete per row, an Archived view, restore). NOTE: run reference/sql-history-checkin-anon.sql to add Session.archivedAt." },
  { id: "anon-workout-logging", area: "Backend", status: "shipped", title: "Anonymous (pre-account) workout logging for admin", detail: "A guest training before they have an account now has their workout mirrored to the backend (best-effort, no auth) so an admin sees real pre-signup usage. New AnonSession model (no userId — only an opaque per-install deviceId + platform, not PII); mobile lib/guest.ts POSTs to /api/anon-sessions on every guest save (never blocking the on-device save). Admin reads them at /api/admin/anon-sessions (requireAdmin + audited) and can delete spam/test rows. When the guest later signs up, flushGuestSessions still creates their real Session rows; the anonymous rows are kept as the usage picture. NOTE: run reference/sql-history-checkin-anon.sql to create the AnonSession table." },
  { id: "admin-destructive", area: "Backend", status: "shipped", title: "Admin: delete accounts + any workout", detail: "Admins can now perform destructive moderation: DELETE /api/admin/users/[id] permanently deletes a user account and ALL their data (table-by-table wipe like account/reset, then the User row) — with the same rails as a role change (can't delete yourself or the last admin) and a rate limit; DELETE /api/admin/sessions/[id] removes ANY user's logged workout; DELETE /api/admin/anon-sessions?id removes an anonymous one. Every deletion is rate-limited and written to the AdminAudit trail (actor, action, target, metadata, IP). Surfaced in the Users detail danger-zone + the admin Anonymous-workouts screen." },
  { id: "dashboard-plan-coach-cards", area: "Web", status: "shipped", title: "Dashboard 'Your plan today' + AI-coach card", detail: "The home dashboard's top card is renamed 'Your route today' → 'Your plan today' and now reflects the athlete's GOAL / PERIODIZATION / ROUTINE — when an enrolled macrocycle exists it surfaces the season goal + current phase + week alongside today's prescription (falls back to the readiness-based route when there's no season). The card sits in a horizontally scroll-snapping row: scroll RIGHT to reach a new AI-COACH card (entry point into the server-side AI coach). FULL PARITY: web (components/today.tsx) embeds the inline AskCoach in the card; mobile (app/(tabs)/index.tsx, horizontal paging ScrollView) opens a native AI-coach screen (app/ai-coach.tsx) that POSTs the SAME /api/ai-coach (Claude server-side + engine fallback) and renders the note with source/readiness/HPI chips." },
  { id: "workout-countin-first", area: "Mobile", status: "shipped", title: "Get-ready count-in shows first (no elapsed leak)", detail: "Fixed the live-workout entry: pressing Start used to drop straight into the workout (and tick a stray elapsed second) before the 5→GO count-in appeared, because the count-in was only armed AFTER the async session load. The count-in is now seeded synchronously from the known route source on the first frame for an explicit fresh start, and the elapsed clock is held until the initial restore resolves — so the count-in is the VERY FIRST thing on screen and the clock starts at GO. Resuming an in-progress draft still skips the count-in and keeps its running clock." },
  { id: "plan-visible-from-zero", area: "Core", status: "shipped", title: "Enrolled plan visible from session zero", detail: "Onboarding's enrolled macrocycle used to be invisible until the athlete had logged a workout — the 'Your plan today' / 'This week' cards were gated behind sessions.length>0, so finishing onboarding looked like a no-op. reconcilePlan only needs the macro phase + a (cold-start-capable) daily prescription, so that gate is dropped: from the moment you enroll, the plan card shows today's prescribed session + the phase line (goal – phase – wk N/total) and the reconciled 'This week' card renders the phase envelope + session — on web (today.tsx + reconciled-week.tsx) and mobile (index.tsx). Auto-scheduling/auto-resync still wait for a real logged session (no scheduling off cold-start defaults)." },
  { id: "intake-tailors-prescription", area: "Core", status: "shipped", title: "Experience + equipment tailor the daily session", detail: "Onboarding's experience and equipment answers were previously discarded after the plan recommendation. They are now persisted client-side (hybrid.experience / hybrid.equipment, like hybrid.daysPerWeek — saved on both the enroll AND the 'continue without a plan' paths) and fed into prescribeSession (new opts.experience / opts.equipment, threaded through buildTrainingWeek too). EQUIPMENT picks the movement tier — barbell (full gym), dumbbell variants at home, bodyweight ('BW', rep-driven, no fabricated kg) for minimal — and swaps machine conditioning (rower/bike) for a bodyweight metcon; EXPERIENCE tunes volume + intensity (beginners lighter, more reps, one fewer set; advanced a bit more). The session's 'why' explains the tailoring. Consumed on the web + mobile dashboards (today's plan card + the reconciled week). Pure + unit-tested (movement-tier + experience-dose cases)." },
  { id: "named-plan-drives-today", area: "Core", status: "shipped", title: "Enrolled named plan's day drives 'Your plan today'", detail: "When the athlete enrolls in a REAL uploaded plan (e.g. PR 56's Bodybuilding 4-Day Full Body), that plan's EXACT day now drives the dashboard 'Your plan today' card instead of the engine's algorithmic pick. The enrolled plan id is persisted (Macrocycle.planId, nullable; onboarding sends it; /api/macrocycles stores + returns it; both clients thread it through the macro fetch). New pure core helpers (plan-day.ts): planToday(planId, sessionsLogged) resolves the plan's day for today (walking the plan's days in order, cycling — each logged session advances one day), and planDayToBlocks() converts the day's items into logger SessionBlocks. The card lists the day's exercises (sets×reps – RPE); 'Start' PREFILLS that exact session into the logger — web via a seed-blocks prop on the Logger, mobile via a new /workout?source=plan that rebuilds the day. Goals without a named plan keep the engine prescription. NOTE: run reference/sql-macrocycle-planid.sql to add Macrocycle.planId. Pure + unit-tested." },

  { id: "session-start-hook", area: "Core", status: "shipped", title: "SessionStart hook — deps install + open-backlog surfacing (web)", detail: "A Claude Code (web) SessionStart hook that keeps remote/ephemeral sessions productive and honest — it makes the project test/build out of the box in a fresh container AND puts the open backlog in front of the agent every session. THREE FILES. (a) .claude/settings.json registers a single SessionStart hook of type 'command' pointing at $CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh — the harness runs it once when a session starts (startup/resume/clear/compact), passing event JSON on stdin and $CLAUDE_PROJECT_DIR / $CLAUDE_ENV_FILE / $CLAUDE_CODE_REMOTE in the env. (b) .claude/hooks/session-start.sh (bash, set -euo pipefail) is WEB-ONLY: it exits 0 immediately unless $CLAUDE_CODE_REMOTE='true' (locally deps already exist and context persists, so there's nothing to do). On the web it cd's to $CLAUDE_PROJECT_DIR and then: STEP 1 — if pnpm is on PATH, runs `pnpm install` with all output redirected to /tmp/hybrid-session-install.log (NOT stdout — stdout is reserved for the hook's JSON, and any stray text there would corrupt it; failures are swallowed with `|| true` so a flaky install never blocks the session); this is what makes `pnpm --filter @hybrid/core test`, the web/mobile typecheck and the iOS bundle export work in the otherwise-empty container. STEP 2 — runs `node .claude/hooks/backlog-context.mjs` (best-effort, `|| true`), whose stdout becomes the hook's output. (c) .claude/hooks/backlog-context.mjs is a PURE-NODE parser (ESM, no npm deps, no build/transpile step, runs on the container's node directly): it reads THIS file (packages/core/src/capabilities.ts) as text, scans line-by-line, and for every entry whose status FIELD is planned or blocked (matched by anchoring to the entry's real top-level shape — `{ id:…, area:…, status:` at the START of the line — so prose in a detail/blockedBy string that merely mentions a status never miscounts, including this very entry) it pulls out the id, title (and, for blocked, the blockedBy, truncated to 130 chars so context can't balloon) via a small quote-aware regex; it then prints a single JSON object {hookSpecificOutput:{hookEventName:'SessionStart',additionalContext:'…'}} — the documented SessionStart contract — whose additionalContext is a compact markdown brief: a header explaining what it is + the rule, then a 'PLANNED — not built yet (N)' list and a 'BLOCKED — built, waiting on a credential/decision (N)' list (each '- [id] title [— needs: …]'). If the registry can't be read or nothing is planned/blocked it exits 0 silently (no context added). SHIPPED entries are intentionally excluded, so the brief is only ever the OPEN work. WHY IT EXISTS: deferrals used to be spoken mid-session and lost when the ephemeral web session ended (e.g. mobile account-settings parity sat undone across several PRs because the ask lived only in chat); now the backlog is injected at the top of every session and the project's parity/capabilities rule is enforced in practice — the loop is: defer something → record it HERE as a `planned` entry (never a prose footnote) → the hook resurfaces it next session until it ships. COST / PERF: the hook runs SYNCHRONOUSLY and ONCE at startup (so deps are guaranteed ready before the first turn, avoiding a race where the agent runs tests before install finishes). It therefore adds a one-time startup wait that is almost entirely the `pnpm install` (the node parser is milliseconds), plus a small bounded chunk of context tokens (the planned+blocked brief). It does NOT run per message/turn, so it has ZERO effect on response latency or token-generation speed during the conversation. It can be switched to ASYNC mode (the hook prints an async marker — async:true + asyncTimeout — first and installs in the background) for faster session startup, at the cost of a brief race window early in the session. ACTIVATION: the web loads hooks from the checked-out repo at session start, so this only takes effect for sessions started AFTER it's merged to the default branch. Validated: the hook emits valid SessionStart JSON; core tests + web typecheck stay green with the registry additions." },

  // ---- Deferred backlog (planned) ----
  // Work that was acknowledged-but-not-built in an earlier PR. Recorded here as
  // structured `planned` entries (not prose footnotes) so the SessionStart hook
  // surfaces them every session and they can't get lost between sessions.
  { id: "account-session-listing", area: "Mobile", status: "planned", title: "Active sessions/devices + login history", detail: "The mobile account Security section ships Sign-out-everywhere (global signOut), and the profile-settings-redesign added a 'Where you're logged in' group with a 'This device — signed in now' indicator (the web twin was retired with the web client). What's still missing is the real multi-device LISTING + login-history + per-session revoke: that needs the Supabase service-role admin API (it can't run with the user's own anon session), so build a read-only server route (e.g. /api/account/sessions, service-role, own-user-only) and render the device list under that group. Deferred from account-settings-pro." },
  { id: "mobile-mfa", area: "Mobile", status: "blocked", blockedBy: "Live Supabase auth access — the sandbox blocks the Supabase host, so the enroll→challenge→verify→unenroll round-trip can't be exercised end-to-end. Code is done; unblocks once it can run against a real project (MFA enabled).", title: "Two-factor auth (TOTP) on mobile", detail: "BUILT — parity with web's TOTP MFA. The mobile Security group now renders <MfaSettings/> (apps/mobile/components/aurora/mfa-settings.tsx), mirroring the web flow (apps/web/components/account/mfa.tsx) exactly: supabase.auth.mfa.enroll → challenge → verify → unenroll/listFactors, gated on isSupabaseConfigured(), sharing the core helper isValidTotpCode and the same enroll/verify/remove states + copy. The otpauth QR reuses the hand-rolled QrMatrix from coach-invite.tsx (renders the enroll totp.uri — no QR/svg dependency, iOS export stays green) with the secret shown for manual entry. Verified by typecheck + iOS bundle export; the auth round-trip itself is unverifiable in this sandbox (Supabase host blocked), so this is BLOCKED rather than shipped until it can be exercised against a real project." },
  { id: "notifications-account-sync", area: "Core", status: "shipped", title: "The bell's read state follows the ACCOUNT, not the device", detail: "The read state was per-device (localStorage / AsyncStorage) on the logger-prefs contract — small, idempotent, costing you at worst one extra glance. That argument held right up until notifications-read-live-feel gave a row a DELETE. A badge that disagrees between your phone and your laptop is a nuisance; a notification you deliberately threw away coming back on the other device is the app forgetting something you told it, and every social app of any size stores this against the account. NOW: a NotificationState table (one row per user, owner-only RLS, reference/sql-notification-state.sql) behind /api/notifications/state, read by both clients. WRITES ARE DECISIONS, NOT BLOBS, and the rest follows from that. Syncing whole states forces a merge rule for 'the phone says read, the laptop says unread' that nobody can state without inventing a timestamp per id — so what travels is a NotifOp value (read / unread / dismiss / sweep / markAll), and the server applies it with the SAME @hybrid/core reducer both clients use. Two devices are then reconciled by ARRIVAL ORDER, which is the order the athlete did them in. One reducer, three callers, no way for the server and a client to disagree about what 'mark all read' means. The route clamps the op's `now` to its own clock, because an unclamped stamp from a device with a wrong clock would push the watermark into the future and mark everything read for good. THE STORE IS SHARED TOO (packages/core/src/notif-sync.ts, createNotifSync): the queue semantics are the subtle part, so they are written once and each client supplies four ports (load / save / pull / push) rather than keeping its own copy of the tricky bit. It applies a decision LOCALLY FIRST (a swipe never waits for a round trip), queues the op, and on every server answer REBASES — state = server plus everything not yet confirmed — which is the one line that stops a freshly-swiped row flickering back to read because a poll landed a moment later. A failed push keeps its queue and does NOT re-arm itself (retrying a queue that just failed, immediately, is a spin loop — caught by the first run of the tests); it waits for the next decision, a window focus, an online event, or an app foreground. So a device that cannot reach the server behaves exactly as it did before this change: the old per-device behaviour is now the DEGRADED mode rather than the design. The device cache migrates itself — the old key held a bare NotifReadState, and a blob with no state field IS the state, so nobody re-reads what they had already read. 24 new tests (14 for the sync store, 10 for the ops + the wire normaliser). NOT in scope: the logger + UI preferences are still per-device (see prefs-cross-device-sync), and this is still the in-app list, not APNs — a delivered push clearing itself when you deal with it elsewhere needs push-notifications, which stays blocked on Expo credentials. MIGRATION APPLIED (Aug 2026): reference/sql-notification-state.sql has been run in the Supabase SQL Editor, so the table and its owner-only policy exist and the API answers synced:true. The Prisma model and the DDL were checked field for field — a mismatch there is the QUIET failure, since a broken query lands in the same catch as 'not migrated yet' and the clients would silently carry on per-device rather than erroring. STILL TO CONFIRM ON LIVE DATA, and it cannot happen until this branch reaches main (Vercel deploys main, so /api/notifications/state is not serving anywhere yet): read a row on web, open the phone, and the badge should already agree — plus the offline path, which is airplane-mode a swipe, come back, and watch the queue drain." },
  { id: "prefs-cross-device-sync", area: "Core", status: "planned", title: "Cross-device sync of per-device preferences", detail: "The workout-logger prefs (logger-prefs.ts) and the persona/template/theme choices are stored PER-DEVICE (web localStorage / mobile AsyncStorage), so they don't follow the user across devices. (Notification/privacy PREFS already sync — they live in Supabase auth user_metadata; and the bell's READ state — seen / held-unread / deleted — now syncs per account too, see notifications-account-sync.) Lift the logger + UI prefs into a synced store too: a User-prefs column or user_metadata, read on login with the local store as a fast cache/fallback, so a user's setup matches on web and phone. Deferred from logger-settings + account-settings-pro." },
  { id: "upgrade-pricing", area: "Mobile", status: "planned", title: "Upgrade page — confirm price against live billing products", detail: "The upgrade sheet (mobile aurora/upgrade.tsx; its web twin was retired with the web client) DISPLAYS a fixed price + free-trial copy — $9.99/month with a '7-day free trial – cancel anytime' note and a 'Start free trial' CTA (i18n keys w.account.upgrade.sheet-*/start-trial/trial-note/per-month, en/pl/de). What remains: reconcile that displayed number/trial against the REAL Stripe Price + Apple IAP product once billing is unblocked (see full-billing-stripe / full-billing-iap), and drive it from the live product metadata rather than hardcoded copy so the two can never drift. Needs the live billing products first." },
  // --- Production-readiness audit: remediation backlog (deferred items that need
  // runtime verification or are larger refactors; recorded so they aren't lost) ---
  { id: "web-data-layer", area: "Web", status: "shipped", title: "Client data-layer / query cache (TanStack Query)", detail: "The web app now runs on a shared TanStack Query cache (components/query-provider.tsx) with per-concern hooks over shared keys — use-sessions/use-macrocycle/use-signals/use-biometrics/use-roster/use-checkins — and mutation→invalidate via lib/use-invalidate.ts, so a write on one screen revalidates every consumer of that key instead of leaving private copies stale. SAFE-CACHE CONTRACT (2026-07): lib/read.ts defines the three-state read every data screen consumes — `ready` (a real server answer is in hand), `refreshing` (a fetch is in flight; drives refresh UI ONLY and never gates content) and `failed` (show an error, never emptiness). This closed the class of bug where a screen holding `[]` before its first response could not tell 'no training history' from 'we have not asked yet', and so asserted the zero-state as fact. useSessions/useMacrocycle `loading` was ALSO redefined from `isPending || isFetching` to unknown-only, which fixed the mirror-image bug: a background revalidate used to drop already-rendered content back to a cold-start skeleton. REMAINING: screen-level remount on nav (the `<div key={screen}>` force-remount in app-shell) is untouched — see web-code-splitting." },
  { id: "safe-cache-reads", area: "Core", status: "shipped", title: "Safe cache — screens never state what they don't know", detail: "Fixes the reported \"page loads, shows nothing, then shows things after a while\" behaviour — which was NOT slowness. A screen holding useState([]) before its first fetch answered could not distinguish 'this athlete has no training history' from 'we have not asked yet', so it rendered the ZERO-STATE AS FACT — Performance told an athlete with years of data to \"log a session\", showed \"No season yet\" and \"No active protocols\" — then swapped in HPI 51 a second later. The app was asserting something it did not know and retracting it. THE CONTRACT: a read is now three-state, not a bare value — `ready` (a real server-returned value is in hand: render it, even while refreshing), unknown (render a SKELETON; never a zero-state, empty list or computed number, each of which is an unsupported claim) and `failed` (render an error, never emptiness). Defined once per client and mirrored: apps/mobile/lib/queries.ts (`Read`/`toRead`/`combineReads`) and apps/web/lib/read.ts. `refreshing` drives the pull-to-refresh spinner and NOTHING else — gating content on it is the same lie in reverse, which is exactly what web's `loading: isPending || isFetching` did (a background revalidate dropped rendered content back to a cold-start skeleton); it is now unknown-only. APPLIED: mobile Performance moved off useState+useEffect(Promise.all) onto the shared cache (sessions/signals/macrocycle/checkins), so it renders instantly from cache when arriving from Today and revalidates behind the spinner instead of refetching from nothing; mobile Today derives its cold-start gate from the reads rather than a one-shot flag (also correct after sign-out/cache eviction); web Performance takes sessionsReady/macroReady from the shell and skeletons the Performance State card, the Goal widget and the Season/Set-up widget — including the HEADING, which is itself a claim (\"Set up\" vs \"Season\"). Per-source revalidation replaced the all-or-nothing Promise.all fan-out that committed the whole screen at the slowest of five endpoints. INVALIDATION MADE TOTAL (what makes the cache safe rather than merely fast): new shared keys for macrocycle + check-ins on both clients, and the writes that falsify them now drop them — readiness-face saves invalidate checkins (mobile checkin.tsx + home.tsx, web checkins.tsx + today.tsx), plan enrol/leave invalidate the macrocycle (mobile plans.tsx, percent-program.tsx, leave-plan.tsx; web plans.tsx). A write path with no entry in useRevalidate is now documented as a staleness BUG, not a missing optimisation. ALSO: mobile gained throwing queryMacrocycle/queryCheckins fetchers so a timeout surfaces as a failure instead of resolving to an empty list; and useRefreshAll() gives focus effects a referentially STABLE refresher (an unstable one re-fires while focused → the documented refetch loop)." },
  { id: "day-rollover-reactivity", area: "Core", status: "shipped", title: "\"Today\" re-derives at midnight instead of going stale", detail: "A pre-existing correctness bug, independent of caching but exposed by it. Day-scoped values were computed by calling the clock INSIDE a useMemo keyed only on its data — `new Date().toDateString()` in home/performance todayFeeling, weeklyRecap(sessions, Date.now(), bw) — so nothing recomputed when the calendar day turned over. A phone left backgrounded overnight (the normal case, not an edge case) woke showing yesterday's date in the masthead, yesterday's feeling card, and scaled today's prescription off yesterday's check-in. FIX: core gained `msUntilNextLocalDay()` (DST-safe — steps a whole LOCAL calendar day from local midnight, so 23/25-hour days still land on midnight; always >= 1 so a timer scheduled on it cannot spin), joining the existing day-key convention in day-key.ts. Each client got a `useToday()` hook returning the local day key as a REACTIVE value, re-emitting on two triggers because neither suffices alone: a timer armed to the next local midnight (covers the app being open across the boundary) and a wake signal (mobile AppState→active, web visibilitychange/focus — covers the far more common case where the device slept and JS timers never fired). The setter compares before writing, so a wake on the same day does not re-render. The day key is now an explicit DEPENDENCY of every day-scoped memo, and because a day flip re-renders, render-time date derivations (the masthead date, masthead()) correct themselves too. Wired on both clients: mobile home.tsx + performance.tsx, web today.tsx + performance.tsx. Covered by unit tests in day-key.test.ts (timezone-robust, month/year boundaries)." },
  { id: "mobile-fetch-error-states", area: "Mobile", status: "shipped", title: "Surface fetch errors (stop empty-state-on-failure)", detail: "A failed read no longer masquerades as genuinely-empty data. FOUNDATION: throwing query fetchers (ApiError + fetchJson → querySessions/querySignals) feed the React Query hooks (useSessionsQuery/useSignalsQuery) so useQuery's isError/retry fire; every call has a 15s timeout (lib/fetch.ts) so a hang can't read as a slow load. NOW SHIPPED (2026-07): a shared <FetchError> card — 'Couldn't load' + a connection hint + a soft Retry that re-runs the query (common.loadError/loadErrorHint/retry, EN/PL/DE) — with a TWIN on BOTH clients (apps/mobile/components/aurora/fetch-error.tsx + apps/web/components/aurora/fetch-error.tsx) so the error voice is one idiom. Wired into the marquee empty-state-on-failure surfaces at parity: (1) HOME/TODAY — when the sessions fetch errors with no cached data the daily-loop hero shows the retry card instead of the first-run chooser (which would read as 'new athlete' when the network merely dropped); mobile home.tsx gates on useSessionsQuery.isError, web app-shell threads useSessions().error → AuroraToday fetchError/onRetry. (2) HISTORY — mobile refactored to the shared card; web app-shell threads the same error → AuroraHistory. (3) NUTRITION — the signals-driven day summary shows the retry card instead of a '0 eaten / full target' fresh-day read (mobile useSignalsQuery.isError; web tracks a loadErr on the /api/signals fetch, treating 401 as an honest empty). DELIBERATELY EXEMPT: the guided Check-in WIZARD degrades gracefully — its today's-row prefill is optional and the form still logs offline, so a blocking error card there would harm usability (a false-empty only exists where the screen IS the data). REMAINING: Feed, Coach console and Trends still use the soft imperative-fetch pattern (parity-followups)." },
  { id: "mobile-list-virtualization", area: "Mobile", status: "planned", title: "Virtualize long lists (FlatList/FlashList)", detail: "Every list was .map() inside a ScrollView (no recycling — a heavy user's History mounted hundreds of Views up-front on every focus), and aurora/history.tsx additionally ran prsForSession O(n²) inside the row map. HISTORY DONE (2026-07 audit follow-up): converted to a real FlatList (keyExtractor, initialNumToRender/windowSize/removeClippedSubviews, ListHeaderComponent for the title+toggle+swipe-hint and ListEmptyComponent for the loading/error/empty states) — it's the sole scroller now (AuroraScreen scroll={false} provides the SafeArea/backdrop/entrance chrome) so the list is genuinely virtualized, not nested in a ScrollView; and the PR badge counts are memoized into a single map per data change instead of O(n²) per row. EXERCISE PICKER DONE (follow-up): the workout.tsx exercise-picker modal — which rebuilt the entire movement catalog + all sports and re-rendered every row inside a ScrollView on EVERY keystroke — now flattens the grouped catalog into a memoized item array (headers/subheaders/rows in one PickItem[], recomputed only when the search text / catalog changes, not per render) rendered by a FlatList (windowed, removeClippedSubviews); header labels are translated at render time so the memo is independent of `t`. Typing no longer re-mounts the whole list. SOCIAL FEED DONE (follow-up): app/feed.tsx converted from Screen(ScrollView) + feed.map to a FlatList (composer + title as ListHeaderComponent, loading/empty as ListEmptyComponent, per-item Card as renderItem); the shared Screen gained an opt-in `scroll={false}` prop + a useScreenBottomPad() helper so a screen can supply its own virtualized scroller while keeping the SafeArea/backdrop/keyboard chrome. LEADERBOARD DONE (follow-up): app/leaderboard.tsx ranked list is now a FlatList (inside its Card, which is a plain View so nesting is fine) with the title + metric pills as a fixed header. FOUR high-traffic/unbounded lists virtualized: History, exercise picker, Feed, Leaderboard — the actual scroll/typing-jank sources. REMAINING (deliberately deferred, low value): the marketplace Coaches directory (app/coaches.tsx) — currently small/bounded and a dual-mode (browse + storefront) layout, so a FlatList conversion is invasive with more layout risk than perf reward; revisit if the directory grows. The reusable pattern (Screen `scroll={false}` + useScreenBottomPad) is in place for it." },
  { id: "dynamic-type", area: "Mobile", status: "shipped", title: "Dynamic Type — large accessibility text doesn't break the layout", detail: "POLICY-INVERSION CORRECTED (Aug 2026). The design audit's verdict was that this capability is \"claimed shipped and is policy-inverted\" \u2014 the 1.15 clamp meant for fixed-height chrome had become the value people copied, so it was applied to surfaces that reflow perfectly well. MEASURED, AND THE AUDIT'S OWN FIGURE CORRECTED: 198 of 271 declarations carried the clamp, not the 265 of 270 cited; 66 were already at MAX_FONT_SCALE and 3 pass a caller's value through. The correction is recorded rather than the citation repeated, per the standing rule that a guard misstating its own subject is how a ceiling drifts. WHAT WAS WRONG, concretely: a wrapping macro row (`flexWrap: \"wrap\"`, no height anywhere) clamped every one of its figures; so did section heads, table rows, four kinds of card, and the shared `Chip` and `Kicker` primitives \u2014 all of them padding-sized, none height-sized. Someone who sets Larger Text to 200% was getting 115% on three quarters of the app's text. THE FIX: 55 clamps across 27 files moved to MAX_FONT_SCALE rather than being removed outright \u2014 1.4 is still a ceiling, so the change is bounded and a device round that finds real clipping has one local value to put back. 143 clamps remain, every one of them in a component that declares a hard height or reads a height token. AND THE PROSE POLICY IS NOW ENFORCED: design-tokens.test.ts holds HARD that a FIXED_FONT_SCALE clamp lives in a component with a hard height (the token arm \u2014 HERO., HIT_TARGET, ROW_LEAD, DOCK_RAIL \u2014 is a correction, not a loophole: HeroMetadata sits in the hero's fixed rail and that height lives in core, so a rule looking only for a local `height:` would have failed the app's most obviously fixed chrome), and HARD that neither cap is ever written as a bare number, since a literal 1.15 is the same clamp wearing a disguise and invisible to every count. STILL UNVERIFIED: none of this has been seen on a phone at the largest accessibility sizes, which is the only place the trade between bigger text and truncation is actually settled. \u2014 RN already scales every <Text> with the OS 'Larger Text' / Dynamic Type setting (we never set allowFontScaling={false}), so the gap was never that text wouldn't grow — it was that growth was UNCAPPED, so at the largest accessibility sizes the fixed-height chrome clipped/overflowed and the app became unusable (the floating Aurora nav pill rendered 9.5/8.5px labels with numberOfLines={1} in a hard-height frosted pill; the calendar day-count badge had overflow:'hidden'). Fix: lib/ui.tsx now exports FIXED_FONT_SCALE (1.15) and MAX_FONT_SCALE (1.4) caps; fixed-height surfaces pass maxFontSizeMultiplier={FIXED_FONT_SCALE} so they bound the multiplier while reflowable body text keeps scaling freely. Applied to the shared Kicker/Chip label primitives, both nav-pill labels (side items + Train FAB), and the calendar badge (overflow:'hidden' removed so a scaled digit can't be clipped). Net: the app stays legible AND laid-out from default through the largest Dynamic Type step. CORRECTION (Aug 2026 iOS design audit): this entry OVERSTATED its coverage. The caps were real and correct, but they were applied at 5 of the app's 2,097 <Text> nodes — and one of the surfaces named above, the floating nav pill, no longer exists (the bottom bar became a real NativeTabs). Worse, the caps alone were never sufficient: all 29 of the app's lineHeight values were ABSOLUTE dp, so at Larger Text the OS scales the glyphs and leaves the line box where it was — text collides with itself before it clips, and no multiplier cap fixes that. The prerequisite shipped with `design-token-ratchet`: leading is now a RATIO (leading(size, role)), so a scaled size carries its line box with it. MAX_FONT_SCALE is now applied at the kit primitives so screens inherit it rather than each remembering. THE SWEEP LANDED (see type-leading-derived): all 210 absolute lineHeights that sat on a line with an fs.* token were DERIVED through leading(), so the line box now scales with the glyphs — which was the actual blocker, not the caps. And 110 single-line <Text> nodes gained FIXED_FONT_SCALE: text with numberOfLines={1} cannot reflow by definition, so an uncapped multiplier does not grow the box, it destroys content. Caps went 5 → 122. DELIBERATELY NOT capped: reflowable body text, which SHOULD keep scaling to the full 1.4 — a blanket cap across all 2,079 Text nodes would have been the wrong fix wearing the right number. STILL OPEN, and it needs a device: walking the app at the largest accessibility step to catch the fixed-height chrome that neither rule reaches — rows whose height is set by a sibling, and the 77 remaining absolute leadings that sit on a RAW fontSize and so are gated behind tokenising the size first." },
  { id: "a11y-baseline", area: "Core", status: "shipped", title: "Accessibility baseline — focus, contrast, labels, roles (web + mobile)", detail: "First pass on the a11y audit's highest-impact, systemic gaps, landed on both clients in parity. WEB (globals.css): a global :focus-visible ring (2px lime, !important to beat the ~40 inline outline:none) so keyboard users can see focus (WCAG 2.4.7); a ::placeholder colour set to the theme-aware `ash` token + color-scheme:dark light so placeholders clear AA on the dark fields (they were UA-grey ~3.5:1); aria-labels on icon/glyph-only buttons (× closes, ↑↓⧉ block controls, timer transport, calendar arrows — 16 buttons / 9 files); and purpose-based aria-labels on the login email/password/name/MFA inputs (placeholder-only before → failed 1.3.1/3.3.2/4.1.2). MOBILE: the shared primitives now carry role+state so the fix propagates everywhere — Button (role=button + disabled state), ToggleRow (role=switch + checked/disabled + 10pt hitSlop), the kit ASegment + check-in rating/share controls (role=radio/checkbox + selected/checked, fixing colour-only selection → 1.4.1), and the shared AField input (accessibilityLabel from its placeholder); plus accessibilityRole=button + localized accessibilityLabel (common.back) on the icon-only back buttons across 18 screens and direction-aware labels on the calendar month arrows." },
  { id: "mobile-back-button-parity", area: "Mobile", status: "shipped", title: "Consistent screen-back button (mobile ↔ web parity)", detail: "Every pushed mobile screen now returns via ONE shared control instead of the per-screen back buttons each screen used to hand-roll — so the mobile app matches the web app's single consistent treatment (the RULE: web ↔ mobile parity). NEW PRIMITIVE: `ABack` in apps/mobile/components/aurora/kit.tsx — a 44×44 line-bordered square with the Aurora `back` glyph and a localized accessibilityLabel (common.back), defaulting to router.back() with an optional onPress override for in-screen pops (settings sub-page, admin section canGoBack→/admin fallback). This is the same 44×44 field-radius icon button the web headers use (aurora interval-timer / standalone chrome), so the clients no longer drift. It REPLACES the divergent variants: the 40×40 `‹` glyph button with no a11y label (Find-friends, Edit-profile, Coaches, Feed, Leaderboard), the `← Back`/`← History`/`← Roster` text links (AI coach, coach-apply, session detail, admin section), the hardcoded-'Back' icon buttons (notifications, timer, statistics) and the ~14 duplicated canonical headers (calendar, check-in, nutrition, progress, trends, performance, running, velocity, connections, exercises, longevity, volume, forceplate, video, login). It also ADDS the back button that was MISSING on the hidden-tab routes reached from the More springboard — Plans, Sport, Coach, Cockpit and History — which had none even though their siblings Velocity/Running already did. Genuinely distinct affordances are intentionally kept as-is: the live-logger 'Cancel', the onboarding wizard's prev-step pill (paired with Next), and the admin index '← App' exit link (a replace-to-app, not a stack pop). THEME-AWARE SURFACE: Aurora (dark) keeps the transparent outlined square; Japandi (light) fills it as a SOFT SURFACE (ink2 tile + a touch of depth) because a hollow outline reads as unfinished on the warm paper ground — driven by `scheme` in the mobile ABack and mirrored on web via `--back-surface`/`--back-shadow` tokens ([data-theme=\"light\"] in globals.css) wired into the standalone back buttons (statistics, interval-timer, notifications). FULL SWEEP (follow-up): the first pass only converted EXISTING back buttons and added them to the hidden-tab routes; a subsequent exhaustive audit of every pushed Stack route caught seven more screens that had a header but NO back control at all — Builder, Run-track, Periodize (both the active + no-plan states), Competition, Talent, Tactical and Logger-settings — all now carry `ABack`. The audit rule: any screen reached by push (not a primary nav destination) gets `ABack`; the only header-bearing screens intentionally without it are the primary tabs (Today/Train/More/Profile roots), the auth/funnel screens (welcome/onboarding wizard), and the admin index's '← App' exit link. Mobile typecheck + iOS bundle export green." },
  { id: "a11y-keyboard-dialogs", area: "Web", status: "shipped", title: "Keyboard operability — dialog focus-trap + operable cards", detail: "Second a11y pass, web keyboard correctness. New shared hook lib/use-dialog.ts (focus-in on open, Tab/Shift+Tab trap, Escape-to-close, restore-focus-to-trigger; capture-phase so it wins over inner stopPropagation) wired into all six modal/drawer surfaces — exercise picker (workout-blocks), tour, profile + coach drawers (social-profile/coaches), the Aurora More sheet (pill-nav, gated on its open flag), and the admin add-user modal (which was also missing role=dialog/aria-modal) — each container now role=dialog + aria-modal + tabIndex=-1 (WCAG 2.4.3/2.1.2). Separately, clickable <div>/<span> with onClick but no keyboard path (WCAG 2.1.1) are now operable via role=button + tabIndex=0 + Enter/Space onKeyDown: plan/goal cards (plans), coach-client card (coach), and the Teams org open-athlete / revoke-invite / close controls (org) — the destructive revoke and the ✕ also got aria-labels. (save-routine-card / social-feed / admin agent-hq were already real <button>s.)" },
  { id: "a11y-reduced-motion", area: "Core", status: "shipped", title: "Reduced motion respected on both clients", detail: "WEB: the @media (prefers-reduced-motion: reduce) block, previously scoped to the glass/command selectors, is now a global *,*::before,*::after catch-all that neutralises EVERY animation/transition — ambient blob field, conic rim sweep, Aurora screen-enter, win-pop, hover lifts and inline-styled transitions (near-zero, not 0, so animationend/transitionend still fire). MOBILE: RN doesn't honour the OS Reduce Motion setting automatically, so a new hook lib/use-reduced-motion.ts wraps AccessibilityInfo.isReduceMotionEnabled (with live reduceMotionChanged updates); the two central motion sources now gate on it — the perpetual ambient-blob drift loop (lib/ui.tsx AuroraField) holds still, and the per-screen fade+rise entrance (kit.tsx AuroraScreen) renders at rest. Parity with the web media query." },
  { id: "a11y-tap-targets", area: "Mobile", status: "shipped", title: "Touch targets reach the platform minimum", detail: "The genuinely finger-unfriendly mobile controls — below the iOS 44pt / Android 48dp bar — now extend their touch area with hitSlop (which grows the hittable region without changing the visual layout, the right tool for these absolutely-positioned / tightly-packed controls): the profile avatar edit button (30×30), the workout set-type cycle button (28×30), the routine-builder steppers (32×36, which also gained Decrease/Increase labels) and the command-menu close (34×34, also labelled). NOTE on web: its icon buttons (30×30, 26×26 closes) already clear WCAG 2.2 AA — SC 2.5.8 Target Size (Minimum) is 24×24 — so they were left as-is; 44px there is only the AAA bar (2.5.5)." },
  { id: "a11y-live-regions", area: "Core", status: "shipped", title: "Interval-timer state announced to screen readers", detail: "The interval timer's big clock ticks every second; announcing that would be unbearable, so instead the timer now announces only PHASE transitions (which happen at work/rest boundaries) — a blind user hears \"Work – Round 1/3\", \"Rest – …\", \"Done\" as it progresses, with no per-second spam. WEB: a visually-hidden aria-live=\"assertive\" aria-atomic region whose text is just the phase + round (the seconds aren't in it, so it only re-announces on a boundary), empty while idle. MOBILE: AccessibilityInfo.announceForAccessibility on phase change, deduped via a ref so a phase is spoken once. The two clients mirror each other. (Toasts + async save/error live regions are still open — see a11y-followups.)" },
  { id: "a11y-landmarks", area: "Core", status: "shipped", title: "Screen-reader landmarks, headings + skip link", detail: "WEB: the app-shell already wrapped content in <aside>/<nav>/<main>/<header>; this names them and adds the missing scaffolding for keyboard/SR navigation — the primary <nav> gets an aria-label (\"Primary\"), <main> gets id=\"main\" + tabIndex=-1 as a jump target, and a new visually-hidden-until-focused skip-to-content link (.skip-link in globals.css, localized nav.skipToContent EN/PL/DE) is the first focusable element so users can bypass the sidebar. MOBILE: the shared AHeading screen-title primitive now sets accessibilityRole=\"header\", so VoiceOver/TalkBack rotor heading-navigation works on every Aurora screen at once. (A deeper per-screen heading-LEVEL order audit on web is still possible but the landmark + skip-link baseline is in.)" },
  { id: "a11y-locale-dir", area: "Web", status: "shipped", title: "Document language + direction follow the active locale", detail: "The server renders <html lang=\"en\"> statically, so a PL/DE user's content was mislabelled as English — a real bug, since screen readers pick voice + pronunciation rules from lang. The web LanguageProvider now syncs document.documentElement.lang to the active locale on change, and sets dir via the new @hybrid/core localeDirection(lang) helper (backed by RTL_LANGS, empty today since en/pl/de are all LTR). This both fixes the lang-attribute bug now AND is the single seam an RTL locale would flip the whole document from. Mobile: nothing to wire today — RN VoiceOver/TalkBack read the device locale, and there's no RTL locale to force via I18nManager." },
  { id: "a11y-rtl-migration", area: "Core", status: "planned", title: "Full RTL layout support (logical properties)", detail: "DEFERRED ON PURPOSE, not forgotten. localeDirection/RTL_LANGS + the web dir sync (see a11y-locale-dir) make the app direction-AWARE, but a genuinely mirrored RTL layout needs a physical→logical style migration across BOTH apps — marginLeft/Right→Start/End, left/right absolute offsets, textAlign, the 180°-rotated calendar arrow, row flex assumptions — plus I18nManager.forceRTL + a reload on mobile. That's a large sweep with real regression surface and, critically, NOTHING to verify against while every shipped locale (en/pl/de) is LTR. It should be done as one focused pass IF/WHEN an RTL language (Arabic/Hebrew) is added to LANGS — at which point RTL_LANGS gets the entry and this work is scoped by that locale." },
  { id: "a11y-status-messages", area: "Core", status: "shipped", title: "Async error / notice messages announce to screen readers", detail: "Inline status messages that appear after an async action (login/save/validation errors, \"check your email\" notices, success confirmations) were plain text a screen reader never spoke when they changed — failing WCAG 4.1.3 (Status Messages). Now they carry a live-region role across both clients. WEB: role=\"alert\" on error / mixed success-or-error messages (assertive), role=\"status\" on neutral notices (polite); since the shared <Mono>/<Card> primitives don't forward arbitrary props to the DOM, those messages are wrapped in a role-bearing <div>. ~35 components (login + classic login, account/MFA, check-in, nutrition, builder, run-track, forceplate, upgrade, coach, quick-sport, invite, and the admin console err/msg banners). MOBILE: accessibilityLiveRegion (\"assertive\" errors / \"polite\" notices) + accessibilityRole=\"alert\" on the inline message elements (drives Android TalkBack), plus AccessibilityInfo.announceForAccessibility at the single clean set points (login, settings, coach-apply, social handle, workout) for iOS VoiceOver where live-region has no effect. Native Alert.alert calls (already announced) and always-present helper text were left alone." },
  { id: "a11y-followups", area: "Core", status: "planned", title: "Accessibility audit — remaining items", detail: "Tracked from the full a11y audit, after the baseline + keyboard-dialogs + reduced-motion + tap-targets + live-regions + landmarks + locale-dir + status-messages passes. Remaining, lower-priority: (1) a deeper per-screen heading-LEVEL (h1→h2→h3) order audit on web (landmarks + role=header are in; the exact heading hierarchy per screen isn't audited); (2) full RTL layout — see `a11y-rtl-migration` (deferred until an RTL locale ships); (3) a manual pass at max Dynamic Type / 200% browser zoom to confirm the inferred truncation fixes on real devices." },
  { id: "schema-deletion-cascade", area: "Core", status: "shipped", title: "Account-deletion FK cascade (defense-in-depth)", detail: "DONE in code: every user/relationship FK now carries onDelete Cascade (and SetNull on the self-referential Team.parent) in prisma/schema.prisma, with the exact drop/recreate migration in reference/sql-ondelete-cascade.sql (generated via prisma migrate diff). The app already deletes children explicitly (admin user-delete + /account/reset), so this changes no app behaviour — it guarantees a raw/RLS delete of a User/Org/CoachLink can't orphan rows or be blocked by a dangling FK, which a schema-level security review would otherwise flag. Tables that store userId as a plain String (no relation) are already orphan-safe. APPLIED in production via sql-pending.sql (Jul 2026)." },
  { id: "schema-tenant-isolation-rls", area: "Core", status: "shipped", title: "Tenant isolation — complete + ENABLE row-level security", detail: "The audit's 'no tenant boundary' finding resolves to RLS, not an orgId column: athlete data here is USER-owned (a user can be in several orgs / have a coach), so an orgId column would mis-model it — the org/coach read paths are correctly relationship-based (Membership/CoachLink) and enforced in the API. The real gap was the DB defense-in-depth layer: (1) reference/rls-policies.sql CREATED policies but never ran ENABLE ROW LEVEL SECURITY, so they were INERT; (2) ~10 user-data tables (Signal, Checkin, Connection w/ OAuth tokens, RtpProtocol, RiskOutcome, WorkoutTemplate, OnboardingState, … — three of the tables it covered, VideoAnalysis/Event/TalentProfile, were themselves dropped in the Aug 2026 strategy cuts) had no policy at all. reference/sql-rls-extend.sql fixes both: enables RLS on every sensitive table, adds self-ownership (+ coach-read where the product allows) policies for the user-owned tables, and sets a deny-all baseline for the relational/org/admin/agent/email tables. Safe for the app (Prisma uses the privileged role, which bypasses RLS); it only blocks direct anon-key/PostgREST access. HARDENED (2026-07 audit): the policy LOGIC itself carried escalation bugs that enabling RLS would merely ACTIVATE — now fixed in rls-policies.sql + sql-all.sql: (a) User UPDATE is column-restricted via REVOKE/GRANT so a signed-in user can't PATCH their own role/entitlement to ADMIN through PostgREST; (b) CoachLink insert is forced to PENDING and only the client can activate, so a forged ACTIVE link can't read a victim's data; (c) org/team/membership writes + CoachInvite claim are server-only / own-email-scoped (no self-join-as-OWNER, no anon PII enumeration); (d) ProcessedWebhookEvent gets RLS enabled (was world-writable); (e) a blanket `revoke all on public tables from anon` closes the whole unauthenticated PostgREST surface behind the policies. APPLIED in production via sql-pending.sql (Jul 2026). Spot-check any time with: select relname, relrowsecurity from pg_class where relnamespace='public'::regnamespace and relkind='r' order by relrowsecurity; (every row should be true)." },

  // ---- web↔mobile parity gaps (from the per-screen parity diff) ------------
  { id: "other-sports-today", area: "Core", status: "retired", title: "Other sports on Today — the racket/team/combat block", detail: "Tennis, squash, badminton, five-a-side — everything the loggers stamp as `discipline: \"sport\"` — had been logged since the sport catalog shipped and had NOWHERE to appear. ENDURANCE_DISCIPLINES deliberately excludes the tag (a tennis match is not endurance), so a Tuesday-night habit fed the week's sessions and hours in the \"This week\" card and then vanished. This block, directly under Endurance on Today, is where it lands. ONE LANE PER SPORT, NOT PER DISCIPLINE: every racket/team/combat session carries the SAME tag, so keying off it the way endurance-lanes.ts does would collapse them into a single lane called \"Sport\" — exactly the picker-shaped flattening the endurance block was built to escape. The grouping key is the block's own NAME, matched against OLYMPIC_SPORTS for its icon and category, with a generic marker and a null category for a sport typed by hand (Kabaddi still gets a lane). ONE TILE, NOT A RAIL, PER SPORT: the catalog gives these sports `metrics: TIME` — no distance, no pace, no zones — so a tile carries the four things a timed sport can honestly say about itself (efforts, hours, an 8-week frequency strip, when it was last played) and the block spends its width on the NUMBER of sports rather than the depth of each. Inventing a pace for a squash match to fill a rail would be fabricating a metric the sport does not have. CORE: other-sports.ts — otherSportLanes() (grouping, most-played first with ties broken on RECENCY so two sports at four efforts each are ordered by which one you are still doing; the discipline tag is preferred and the NAME is the fallback, matching the endurance filters, so blocks logged before the loggers stamped a tag still land here), sportWeekBars() (normalised against the lane's own busiest week, same shape volumeBars() returns so both blocks draw bars through identical code), otherSportTotals() (the head reports the block so nobody adds up tiles), OTHER_SPORT_CAP = 4 with an expander. A session outside the 8-week window still counts in the totals; it simply isn't charted. 15 unit tests. BOTH CLIENTS (parity): aurora/other-sports.tsx on web + mobile, rendered straight after AuroraEnduranceLanes and before the Go Full baits. Full-bleed rail on the exercise-widget idiom (margin 0 calc(-1 * var(--page-pad-x)) on web, marginHorizontal -16 on mobile) with matching inner padding. Frequency bars use VIOLET, not teal: teal already means cardio on the lanes directly above, and two blocks in the same colour would read as one. Renders nothing until a sport is logged, so no tile needs an empty state. Each tile opens the Sport screen. i18n EN/PL/DE." , retiredBecause: "SUPERSEDED Aug 2026 by the endurance PAGER (endurance-sport-pager). The block was right that ball sports needed a home and wrong that it had to be a SEPARATE one: `discipline: \"sport\"` is a data-model fact, and giving it its own block with its own grammar, its own cap, its own expander and a parentage line doing fraction arithmetic (\"2H 30MIN OF 2H 52MIN THIS WEEK\") leaked that fact into the layout. Tennis is routinely the sport with the most minutes in it, and it sat below the fold under a heading that said Other. Every sport is now a page in one pager ordered by the volume in the window, so the ball sports lead when they are what you actually train. To revisit it you would need a reason ball sports should be read apart from the rest of an athlete's endurance — and the reason the block gave (they are TIMED, so they carry no distance or pace) is now handled per-page: those slots simply do not render for a sport that has none." },
  { id: "duration-hours-minutes", area: "Core", status: "shipped", title: "A duration reads in hours AND minutes — the decimal-hours sweep", detail: "BUGFIX. 67 minutes of tennis printed \"1.1 h\". Every surface that showed training TIME converted canonical minutes to DECIMAL hours with the same hand-copied expression — `Math.round(minutes / 6) / 10` in five files — and tenths of an hour are not a duration anybody reads: an hour has sixty minutes, not a hundred, so the digit after the point means nothing until you multiply it back. It is lossy where the athlete can see it, too — 67 and 68 minutes both printed 1.1 — and the sport page rounded the same 67 minutes to a flat \"1\" hour, discarding the 7 the athlete had logged. THE FIX IS ONE FUNCTION, not five: core duration.ts owns durationParts() (the total rounded ONCE before the split, so 59.7 minutes can never come out as \"0h 60min\") and formatDuration() -> `1h 17min` / `45min` / `2h` — the minutes dropped on the hour rather than printed as a hollow `0min`. Units are a parameter and default to \"h\"/\"min\"; durationUnits(t) reads the athlete's own pair from w.home.act.uH/uMin, and those two keys now live in core so no screen can quietly pick a different pair. WIRED EVERYWHERE A SPAN IS SHOWN, both clients in the same change: the Other-sports tile footer and its head's parentage share (progress-parentage's parentageHours -> parentageDuration, so the share and the column it decomposes stay the same shape), the endurance lane's all-time SummaryTile (its row label is now TIME rather than H — the figure carries the unit, so the label names the quantity instead of repeating it), the This-week card's Hours column AND the per-sport breakdown beneath it (one formatter for both, so a column and its own groups cannot print one span two ways), and the sport page's hero figure (\"2:15 h\" was a clock time wearing a duration's unit), its Time-logged and This-week totals, its longest-session best and its 8-week average. Cells that carried a separate \"min\" unit now carry none, so the sport page's week label reads \"This week\" for every sport and the w.train.sportPage.minThisWeek key retires; w.home.group.metaOf drops its trailing \" h\" (EN/PL/DE) since both interpolated values bring their own. The two chart READINGS that answer in minutes go the same way — otherSportReading and a timed sport's sportVolumeReading return the duration string with an EMPTY unit, since the figure already carries it and both clients' held-chart readouts already omit an empty one. liveElapsedText (the feed's live ticker) is deliberately left alone — it is already hours-and-minutes, in the terser clock shape a ticking figure wants. 6 new duration tests + 1 sport-page test pinning every timed figure on the page." },
  { id: "progress-cluster-dedup", area: "Core", status: "shipped", title: "Progress cluster — the redundancy sweep (13 findings, both clients)", detail: "A design audit of Today's PROGRESS cluster (design/progress-cluster-redundancy-audit.html) found thirteen places the screen said the same thing twice, plus two defects the repetition was hiding. ROOT CAUSE: consistency wave 3 had each rail head QUOTE the This-week column it decomposes, to prove the two agree. The intent was right; quoting the parent WHOLE is not, because a quote of the whole parent is indistinguishable from a repeat of it. Other sports had it right all along with metaOf ('3.1 of 5.2 h this week') — a SHARE is information, a total is an echo. FIVE RULES, and every fix follows from one: (1) state a scope once, at the level where it stops varying; (2) a quote must add a fraction, never repeat a total — and the anti-drift guarantee belongs in CI, not in a label the athlete has to audit; (3) one discipline, one home, and every rate through formatDisciplinePace; (4) a magnitude must survive its own baseline; (5) one fact, at most two channels. THE TWO DEFECTS. A1: the verdict sentence was chosen by largest raw % change against the four-week mean, guarded only by baseline<=0, so a 0.087 km mean printed '+7849%' — and worse, ranking by raw ratio hands the headline to whichever metric has the SMALLEST denominator, so a lifter who jogs monthly lost the sentence to distance every week. week-verdict.ts now gates on VERDICT_BASELINE_FLOOR (per-metric, canonical units) AND per-metric coverage of the prior windows; a failing metric becomes the FALLBACK rather than being discarded, and past VERDICT_PCT_CEILING (300%) both clients print the step ('0.1 -> 6.8 km') via verdictShowsStep. Ranking stays on the raw percentage so no period the suite already pins changes its sentence. A2: the exercise card handed its canonical sec/km to a hard-coded '/km', showing a swimmer '38:36 /km' while the lane 250dp below printed the same rate as '3:52 /100m'; ExerciseWidgetCard gains `discipline` (resolved via the lanes' own blockDiscipline) and both clients render through formatDisciplinePace. THE SWEEP. Heads: Exercises quotes COVERAGE ('3 of 11 movements', new core movementsTrained over WIDGET_WINDOW_DAYS — which also discloses that the rail is a selection, previously invisible); Endurance drops its quote and gives the slot to its only control, the lane-order toggle, now a bordered ash CHIP instead of accent-coloured text on an orphan row; each lane header drops the efforts count its own first tile prints. Tiles: the shared Tile API now OWNS both scope slots (label = metric + the FIGURE's scope, foot = the CHART's window + delta — and `foot` is OPTIONAL, dropped Aug 2026 on the VolumeTile because its strip DRAWS the window: eight discrete bars, one per week, countable at a glance, so '8 weeks' underneath was the chart's own axis restated in prose. The caption survives only where nothing is countable — TrendTile's pace line, whose footer also carries the delta the window qualifies), so a caller cannot put the window in the label as VolumeTile did ('Volume - 8 weeks' over a footer reading '8 weeks'); SummaryTile -> 'All time', VolumeTile -> 'Volume - this week', TrendTile -> 'Pace - latest week', ZoneTile -> 'Zones - all time'. Card: the four vs* templates drop {v} (they reprinted the column below them); the PR count renders only from three records up — RECONCILED with the records rail that landed in #338 between this branch and its merge: with one or two, both cells sit side by side on one row and a count restates what is in view; from three they are a rail, and you cannot count what you have to scroll, so the total earns its place; the exercise card drops the kind word (colour + metric name already said it) and takes the delta into the freed footer slot, giving the movement name the whole top row; the permanent 'open a figure' instruction retires after the first column is opened (hybrid.today.actHinted). Rail: exerciseWidgetCards gains `deferToLanes` so a discipline with a lane is left out of AUTO-FILL (an explicit favourite still wins); both Today screens pass deferToLanes={isAthlete}. i18n EN/PL/DE: metaWeek and the three exw.kind* keys retired; metaMoves and four lane scope labels added. 18 new unit tests (2283 green), including the reconciliation R2 moved out of the UI: the lanes' this-week distance must sum to the verdict's KM column, and no auto-filled card may share a discipline with a rendered lane." },
  { id: "endurance-today-lanes", area: "Core", status: "retired", title: "Endurance on Today — sport lanes (per-discipline rails)", detail: "The Endurance hub was a destination in More: one discipline at a time behind a picker, which meant a hybrid athlete's endurance was invisible unless they went looking for it, and invisible ALL AT ONCE even then (pick running, lose the swim). It now renders inline on Today — directly under the \"This week\" verdict card, whose KM column these rails break down, so the week's total and its per-sport detail read as one thought (it spent its first months at the FOOT of the scroll, under Nutrition, which put them at opposite ends) — and inverts while it does: EVERY logged discipline gets a lane, and a lane is a full-bleed horizontal rail of that discipline's own analytics — efforts + time, distance, pace, the pace zones, the last effort, ALL of them over ONE window (see below), each over its own trailing 8-week chart. THE SHAPE IS THE POINT: adding a metric WIDENS a lane's rail, it never LENGTHENS the block, so five sports carrying five cards each still cost five rows and the whole endurance read fits under Nutrition without Today growing a second screen. CORE: endurance-lanes.ts — enduranceLanes() (one lane per discipline with logged cardio, derived entirely from the existing running engine: runTotals/weeklyMileage/paceEffortSplit/disciplineSessions, so there is no second definition of a week or an effort), orderLanes()/nextLaneOrder() (the block's ONE control cycles most trained → most recent → longest; 'most trained' is activeDisciplines' own order so moving between Today and the hub never reshuffles the sports), LANE_CAP = 3 with an expander (a fourth lane pushes the block past a screen of its own), laneWeekTotals() RETIRED Jul 2026 with the totals strip it fed: the \"This week\" verdict card higher up Today already states the week for ALL training, and a second cross-sport strip beneath it counted a DIFFERENT population under near-identical labels — \"5 sessions, 3.2 h\" above \"3 efforts, 0.9 h\", with nothing on either saying which was which. The week's distance moved into that card as its own column (shown only for an athlete who logs endurance); per-sport figures stay on the lane, where the lane head names the scope. The block now opens straight on its lanes. ONE WINDOW PER LANE (Aug 2026) — the rail's biggest correction, and it started as a text cut. Five tiles were measuring FOUR different windows: all-time totals, THIS WEEK's volume, the LATEST WEEK's pace, all-time zones, one session. A rail is a comparison instrument — cards on one baseline, swept left to right, saying 'read across these' — and they were not commensurable, so every tile had to declare its own scope in its label: 'Volume – this week', 'Pace – latest week', 'Zones – all time', three compounds per lane, three lanes deep. Those labels were the section apologising in advance for cards that could not be read across. So the scope moved UP: the lane HEAD carries it once (Explore SectionHead grammar — identity left, one mono fact right), every FIGURE obeys it, and every label collapses to its metric (Efforts / Distance / Pace / Zones / Last effort). Two figures had to move to make that true — the volume tile's figure became the lane's all-time distance, which is the number SummaryTile was printing as its `KM` row, so DISTANCE left that tile for the one that owns the metric and draws its history (SummaryTile → EffortsTile, VolumeTile → DistanceTile); and the pace tile's figure became `paceAllTime`, a new lane field summed from the exact `cardioSeconds` over unrounded distance, NEVER from display minutes (device-truth). The `KM` / `TIME` row keys went with the move: they only ever existed because one tile carried three metrics under a label that named none of them, and they were a second reading direction (right-flushed key-value) inside the narrowest card on the screen. The figures now RECONCILE across the rail — 2.94 km in 2h 27min IS 5:00 /100m — which was never true before. HOLDING a chart answers in the FOOT (the chart's own slot) instead of the label, because a metric that changes under a thumb is the fault this fixed; a tile whose foot is empty at rest still reserves the row so filling it shifts nothing. The three old compound strings keep their wording and all three translations as the ACCESSIBLE names — a listener has no lane head in view, so metric-plus-scope is still exactly right to hear. zonePercents() (largest-remainder rounding so the legend always sums to 100 beside a full bar), paceDelta()/paceTrendPoints() (FASTER IS UP on every discipline — one rule works because storage is always canonical seconds-per-km, so a lower number is both a quicker run and a faster ride), volumeBars(), paceAllTime. A lane's pace trend is read off the SAME weeklyMileage buckets its volume bars draw, so two cards in one lane can never disagree about a week. 41 unit tests. BOTH CLIENTS (parity): aurora/endurance-lanes.tsx on web + mobile, rendered straight after AuroraWeekVerdict on Today (today.tsx / home.tsx) and before the Go Full baits. Rails use the exercise-widget bleed (margin 0 calc(-1 * var(--page-pad-x)) on web, marginHorizontal -16 on mobile) with matching inner padding. Charts are hand-rolled SVG on both (recharts stays on the hub) — Today should not pull a chart library into its first paint, and hand-rolling gets the two clients pixel-identical. The block is NOT gated on dayIsToday — an eight-week volume chart is not a property of the day you happen to be scrubbed to. Athlete-gated (the hub's own minPersona), renders nothing until there is endurance logged, so no lane needs an empty state. NAV: the More entry is retired via a new NavItem.promotedTo field rather than deleted — on web the nav id IS the screen id, so removing it would strand the hub. The route, icon, i18n label and persona gate all stay live (navVisibleTo and sanitizePersonaAccess deliberately keep honouring it, and the parity guardrail still resolves it on both clients); only the menu listing goes, and each lane opens the hub — via a TAIL CARD at the end of its rail since Aug 2026, no longer a header link (see rail-tail-exit). The hub keeps what a rail can't carry: the by-move table and the full-size charts. i18n EN/PL/DE." , retiredBecause: "SUPERSEDED Aug 2026 by the endurance PAGER (endurance-sport-pager). The lanes' own bet was that adding a metric should WIDEN a rail rather than lengthen the block — and it held right up until the block cost eighteen bordered surfaces, four horizontal scrollers and four time windows on one screen. Three faults were structural rather than cosmetic: (1) the rails carried `decelerationRate=\"fast\"` but no `snapToInterval`, so every lane came to rest with its third tile clipped mid-number, three cuts at the same x; (2) every tile was the same weight, so a single eight-minute jog drew the same five-card instrument as fourteen swims; (3) the ONE WINDOW PER LANE fix (Aug 2026) corrected the labels but not the section — an ALL TIME lane head still sat over an eight-week chart under a THIS WEEK card. The pager keeps what the lanes got right (per-sport depth derived from the existing engines, no second definition of a week) and drops the rail. To revisit the shape you would need a screen wide enough that five tiles read as a row rather than as a scroller." },
  { id: "endurance-sport-pager", area: "Core", status: "shipped", title: "Endurance on Today — one full-width page per sport, swiped", detail: "SHIPPED on mobile (the user-facing web client was retired Aug 2026, so there is no twin to keep in step). The Endurance section stops being a stack of per-discipline rails and becomes a PAGER: the full screen width per sport, snapped, swiped sideways, ordered by the volume in the window. Replaces endurance-today-lanes AND other-sports-today, both retired above. THE PAGE holds four things — the sport's name, ONE big figure, the eight-week shape, and two or three facts — and nothing else; zones, splits, pace history and every effort already live one tap away on sport-page.tsx, which owns them. MINUTES ARE THE HERO on every page, because minutes are the only measure a swim, a ride and a squash match share; the lanes' headline was distance, which two of five sports do not have. WHAT A SPORT HAS DECIDES WHAT RENDERS: distance and pace appear only on the pages that carry them, and a timed sport shows its LONGEST session instead — no em dash standing in for a metric that never existed. ONE WINDOW, said once in the section's headline and obeyed by every figure on every page: the trailing eight weeks, the same SPORT_PAGE_WEEKS the sport page charts, imported rather than re-declared so the pager and the page it opens cannot disagree. THE CHART IS A RIDGE, not bars — see endurance-ridge-chart below. NO PEEK of the neighbouring page: at phone widths a peek clips the next sport's NAME mid-letter, which is the fault the rails already had, so the pages are the full content width and the DOTS carry the affordance. `pagingEnabled` + `snapToInterval` at the page width means it can never rest between two sports. CORE: sport-pages.ts — sportPages() builds one uniform SportPage per sport trained in the window, walking endurance disciplines through disciplineSessions/weeklyMileage (device-trued upstream) and ball sports through deviceTrueSessions here, keyed by discipline tag for the former and by block NAME for the latter (every racket/team session carries the same \"sport\" tag, so keying off it would collapse them into one page called Sport); window totals are walked off the BLOCKS rather than summed off the rounded weekly buckets, so a derived pace divides exact cardioSeconds over unrounded distance; sportPageTitle() resolves an i18n key or a proper noun so no client branches on kind; sportPagesTotal(). GATING MOVED: the section is gated on the PAGES the window produces, not on all-time history — the lanes kept a heading above a row of zeroes for a sport somebody stopped doing in March. MOBILE: aurora/sport-pages.tsx, full-bleed so the page IS the screen width (a scroller narrower than its page would snap by the wrong interval). 16 core unit tests + 6 render tests. RETIRED WITH IT: the section's LEAD SENTENCE (aurora/endurance-summary.tsx, \"2 sports, mostly Tennis\") — it read the screen's own period while the pages read eight weeks, which would have put the four-windows fault straight back into the section it was cut to fix; and nineteen i18n strings that existed to manage that contradiction (three lane orders, the expander pair, ALL TIME, six compound metric-plus-scope labels, the Other-sports block's own set and w.home.group.metaOf). i18n EN/PL/DE: one new key, endurance.longest." },
  { id: "endurance-ridge-chart", area: "Core", status: "shipped", title: "The eight-week chart is a ridge, not a row of bars", detail: "SHIPPED with endurance-sport-pager. The section drew eight weeks as eight bars, and at rail width they came out four pixels wide with a two-pixel gap — eight objects made out of one shape, which is a texture rather than a chart. Volume over time is a LEVEL, and a level reads as a line; the app already draws a level this way for the pace trend, so this is the mark the section should have carried from the start. CORE: sport-pages.ts ridgeGeometry(values, w, h, pad) returns the stroked line, the same path closed to the baseline for the fill, the newest point (so a client can sit its dot on the end of the line) and a `flat` flag for a window with nothing in it. Two decisions are load-bearing and both are tested: (1) the BASELINE IS h, so a zero week sits on the floor and a gap in training reads as a gap rather than as a low reading; (2) the smoothing is the MIDPOINT CUBIC — each control point shares its endpoint's y — so the curve is smooth in x and cannot overshoot in y. A Catmull-Rom through a spiky week would dip the curve BELOW zero and draw a week of negative training. `pad` keeps the stroke inside the box so a peak week is not clipped at the top. The geometry is core rather than per-client so it is testable and cannot drift. FIVE ALTERNATIVES WERE DRAWN before this one: weekly dots (the most honest on a sport with one session — the only mark that says \"you ran once\" without implying a trend), a volume-and-intensity ribbon, session ticks on a real date axis (needs session rows, not the weekly aggregate), a heat strip (34px instead of 164, but you can rank weeks and not read one), and a single arc against the previous window. The ridge won for a sport trained regularly; the dots remain the strongest candidate if the section ever needs one mark that works everywhere." },
  { id: "endurance-legacy-models-retire", area: "Core", status: "planned", title: "Retire the lane models the pager superseded", detail: "endurance-lanes.ts and other-sports.ts in @hybrid/core have no consumer since the pager shipped: their UI is deleted and sport-pages.ts derives everything it needs from the running engine directly. They are pure and tested, so they are harmless — and a module with no consumer is exactly how this palette grew a `card` nobody painted. THE WORK: move zonePercents() (largest-remainder rounding, still the right helper and still wanted the moment a page carries its intensity split) somewhere it is used, delete enduranceLanes/orderLanes/nextLaneOrder/laneVolumeReading/lanePaceReading/volumeBars/otherSportLanes/sportWeekBars/otherSportReading/otherSportTotals with their 69 tests, and drop the two barrel exports. Kept out of the shipping change deliberately: it is a delete of ~600 lines whose tests document behaviour worth reading once more before it goes, and mixing it into the change that introduced the replacement would have made the diff unreviewable." },
  { id: "rail-tail-exit", area: "Core", status: "shipped", title: "A rail's exit lives at the END of the rail — the tail card replaces every header 'See all'", detail: "A section head's right slot no longer carries a 'See all ›' link. THE PROBLEM, most visible on Today's endurance block: three lanes stacked down one screen (Swimming / Cycling / Running) each put an identical lime 'See all ›' in its header, three accent links on one screen pointing at three different destinations, none of them where the eye or the thumb actually is. The reader is at the RIGHT edge of the cards, having just swiped to the end of the rail, and the only way to act on 'there is more' was to travel back up and left to a link they had already scrolled past. It also spent the head's right slot (which per the Explore SectionHead standard belongs to the section's META) and spent CHARTREUSE — the reserved 'go' colour — once per lane, so the accent stopped signalling anything. THE RULE: an exit belongs at the END OF THE THING, in the SHAPE OF THE THING. A horizontal rail ends in a TAIL CARD; a vertical list ends in a DOOR ROW. The tail card is discovered by the same gesture that exhausts the rail — swipe, run out of content, and the next thing under the thumb is the way in — and it self-documents the rail's length, which a header link could never do. WHAT SHIPPED: a new shared component, aurora/rail-tail.tsx on BOTH clients (props: onOpen, label defaulting to the shared w.explore.seeAll, a11y for the spoken scope a screen reader has no positional cue for, w/minHeight to match the host rail's slot, premium for a ✦ destination behind Full). THE TAIL IS NOT A CARD (Aug 2026): it first shipped wearing the card form — ink2 fill, hairline border, the rail's own radius, the rail's own shadow — on the theory that it should 'belong to the row', and that was wrong twice over. A rail's cards each carry a THING (a coach, a recipe, a business, a metric); the exit carries no thing, so a filled bordered box at the end reads as one more item that turned out to be empty, and the athlete counts it — 'six verified businesses' when there are five. And because every rail sized its own tail to its own card, the same door was drawn at five different widths, radii and shadows: a component that existed to make the exit consistent had props that made it inconsistent. The tail is now CHROMELESS on every rail — no fill, no border, no shadow — just the ringed 44px arrow plate and a mono-uppercase label in ash, centred on the ink; the `radius` and `shadow` props are DELETED rather than defaulted, so no caller can re-card it. `w` stays, because slot width is a SCROLL concern and not a decorative one: under a mandatory snap grid (web scroll-snap, mobile snapToInterval) an odd-width final child puts the content end off the grid and the last snap leaves the tail half-cut. Width is where a rail may differ; the drawing is not. CALLERS, all at web↔mobile parity: the three endurance lanes (112px, minHeight 118/TILE_H) with the lane header now carrying identity ONLY; the Nutrition recipes rail (premium ✦ — the accent moved from the dead header link onto the tail) and the verified-foods rail; the coach rail and the community feed preview, whose two bespoke copies of this drawing are DELETED in favour of the shared one, so there is now a single implementation rather than four. THE EXERCISE-WIDGET RAIL JOINED LAST (Aug 2026), and it was the one the tail rule had missed: Today's favourites rail — the golden-standard bleed rail — still drew its OWN exit, a 132×132 DASHED ghost tile with a ＋ glyph and a chartreuse label, on both clients. Three things were wrong with it and each is the reason the shared tail exists. A dashed box is the 'empty slot / drop here' idiom, so the app's most-seen rail ended in something that read as unfilled rather than onward. ＋ means ADD, and the destination is a screen, so the glyph promised an action the tap doesn't perform. And it spent CHARTREUSE on a standing link — the exact habit this capability retired from every section head, surviving inside the rail because nobody had looked at the last child. It is now the shared tail at w=132/minHeight=132, which also drops the fifth copy of the drawing; the head comments on both clients no longer say 'ghost tile'. ITS LABEL IS THE SHARED 'See all' — the tail used to read 'All exercises & favourites', a bespoke string in a slot every other rail fills with two words, which made the app's most-seen exit the one that looked least like the others; the rail's own head already says 'Exercises', so the long label was restating its neighbour. w.home.exw.allCard is deleted in EN/PL/DE, and the rail's subject moves to the `a11y` prop where a screen reader (which has no 'end of THIS rail' cue) still hears 'See all – Exercises'. EXPANDERS ARE NOT DOORS (same pass): the Other-sports rail ended in the SAME dashed ＋ tile, copied from the exercises one. It is not converted to a RailTail, because it EXPANDS the rail in place rather than opening a screen and an arrow would promise a destination that doesn't exist — it keeps ＋/− and becomes chromeless instead (no dashed box, ash instead of chartreuse), so the ring is what distinguishes a door from an expander and neither is a bordered box. The PRs rail's 'Show all {n}' cell was already borderless and needed no change. THE DOOR ROWS WENT FLAT TOO (Aug 2026), which is what makes this a grammar rather than a one-off: a door is not a thing you own, it is the way OUT of the things you own, so it has no business wearing the ink2 fill, hairline and radius that every card on Today wears — a stack of two of them read as two more cards' worth of content below the week. week-verdict's shared DoorRow (both clients) drops fill, border and radius and reads as type on the ground; its 32px glyph tile becomes a RING. Two hand-rolled copies of that anatomy fold into the shared component in the same pass — Today's 'Go Full' row (a new `premium` prop carries the ✦ in the premium accent, still the only thing wearing it) and, as an expander, the endurance block's 'All sports +N'. THE RING IS THE GRAMMAR, and it is the whole point of doing rails and rows together: a DOOR carries its glyph in a ringed plate (the rail tail's 44px arrow ring, the door row's 32px glyph ring) and an EXPANDER — anything that grows the thing in place instead of opening a screen — carries a BARE ＋/− with no ring (Other-sports' rail tail, the endurance block's All-sports control), with its count in ash rather than chartreuse because the accent is the 'go' colour and an expander never goes anywhere. So: ring = leaves, no ring = grows, and NOTHING in either shape is a bordered box. Rows separate by whitespace, not a rule — a hairline under a GroupMark is exactly the label-plus-rule divider the cluster markers deliberately retired. The coach rail's own header link went with them: its built-in head carried 'Browse all →' beside the title, a SECOND door to the same room at the wrong end of the swipe, so the head now names the rail only and the tail card renders unconditionally — which retired CoachRail's `seeMore` flag (a rail always ends in its exit, so there was nothing left to switch off) on both clients and at both call sites, and left w.explore.browseAll orphaned in EN/PL/DE, now deleted. The food page's 'More from this business' is a vertical LIST, so it takes the door-row form instead: the same hairline and chevron as the food rows above it, mono uppercase in ash, as the list's own last row. Web RailHead's `action` and mobile ASection's `action` become optional / are re-documented as head-level CONTROLS (a filter, a state toggle) — never a rail's 'see all'. No new i18n: w.explore.seeAll / seeMore are reused verbatim. Design alternatives considered are in design/see-all-tail-card-ideas.html. THE RULE IS NOW IN CLAUDE.md ('an exit is never a bordered box — ring = leaves, no ring = grows'), which is where it belongs: this registry records what was BUILT, and an agent starting fresh reads CLAUDE.md, so a rule that lived only here was one nobody would see before drawing the next exit. Three separate changes in one week each rediscovered it independently — the tail sweep, the door-row sweep, and readiness-read-explain (#364), which dropped the bordered circle around its ⓘ on the grounds that 'the glyph IS a ring' — which is the definition of a principle that should be stated once rather than re-derived. The same edit fixed the section-header rule, whose right-slot example still offered 'See all →' as a head action, the exact placement this capability retired." },
  { id: "parity-guardrail", area: "Core", status: "shipped", title: "Nav coverage enforced in CI (was the web↔mobile parity guard)", detail: "Born as the web↔mobile parity guard (one product, two clients — it caught the Running and Analytics screens each shipping on only one client). With the web client retired, the half that can still drift is mobile coverage: apps/web/__tests__/parity.test.ts now asserts every canonical NAV_ITEMS id resolves to a real route in mobile's lib/nav-href.ts. A deliberate gap is allowed ONLY via the ACCEPTED_GAPS allow-list, and each entry must name a capability that is `planned` or `blocked` (asserted against CAPABILITIES), so an escape hatch is always auditable and can never point at a 'shipped' capability. It also fails if an accepted gap silently closes, regression-locks analytics + endurance by name, and sanity-checks the route map parsed non-empty so a refactor can't turn the suite into a vacuous pass. The same file keeps the sheet-elongation and sheet-bottom-pad guards (mobile + core). Runs in the existing CI test job." },
  { id: "mobile-team-surfaces", area: "Mobile", status: "shipped", title: "Team Compare / Squad Monitor on mobile", detail: "SHIPPED — the last web-only nav destinations now exist on mobile: components/aurora/team-compare.tsx and team-monitor.tsx with app/team-compare.tsx and squad.tsx routes, plus their HREF entries, so the More springboard opens real screens instead of tagging them web-only and launching the browser. (The Org screen shipped here too and was later deleted with `org-graph`.) Each runs on the SAME endpoints as its web twin (/api/coach/compare, /api/coach/squad) AND the same shared core engines — athleteSegment/SEGMENT_LABELS for the monitor — so role permissions and segment logic cannot drift between clients: that logic lives in core rather than being re-implemented per client. Where the web layout could not survive a phone it was re-formed rather than shrunk: the monitor's 8-column table becomes one card per athlete carrying the same eight facts, Compare's horizontal bar chart plus 7-column table becomes ranked meters plus stacked per-athlete detail. New lib/api.ts helpers (fetchTeamCompare, fetchSquad) degrade to empty/null rather than throwing, so a non-coach simply sees an empty state. This also removed all three entries from the parity-guardrail test's ACCEPTED_GAPS list — that test fails if an accepted gap silently closes, which is how it should be. Historical context: these were web-only (web aurora/team-compare.tsx, team-monitor.tsx, org.tsx) while mobile had no route at all." },
  { id: "mobile-analytics", area: "Mobile", status: "shipped", title: "Analytics dashboard on mobile (athlete + coach + operator scopes)", detail: "Closes the half of the Analytics parity gap that never existed: mobile had no Analytics screen, so the More springboard tagged it web-only and tapping it OPENED THE BROWSER. app/analytics.tsx + components/aurora/analytics.tsx now render all three scopes off the SAME engines and endpoints the web dashboard uses, so the numbers can't drift: ATHLETE (sessions / total volume / heaviest lift / last readiness, a top-lift heaviest-load trend, volume-per-session over the last 8, and a heaviest-per-lift PR table) via totalVolume/sessionVolume/bestTopLoadByLift/topLoadSeries/liftNames with the user's units + bodyweight lookup; COACH (clients / avg adherence / avg readiness / roster volume, adherence-by-client meters, roster table with readiness colour-coded) via GET /api/coach/roster through the new fetchRoster() in lib/api.ts (RosterRow mirrors web's lib/use-roster.ts row-for-row); OPERATOR (users / MAU / sessions / coaches, plans-by-goal and language-split meters) via GET /api/admin/stats through adminGet(). Scope availability, landing scope, demotion fallback and the privacy note all come from the shared core helpers (see web-dashboards) rather than being re-derived. Charts are dependency-free bars + meters (the trends/endurance/volume idiom) so no charting library ships to the phone. Strings reuse the w.home.analytics.* bundle plus new analytics.subtitle / analytics.scope.* / analytics.privacy.* keys (EN/PL/DE). Reachable from the More springboard via HREF.analytics — the web-only blue dot is gone." },
  { id: "mobile-coach-console-parity", area: "Mobile", status: "shipped", title: "Coach console — client tools parity", detail: "Mobile aurora/coach.tsx ClientDetail (345 lines) is a subset of web's (669). Missing on mobile: roster tag add/remove, per-client template assignment, generate-periodized-week (enroll macro + buildTrainingWeek bulk-assign), and the weekly check-ins + coach-reply section. Bring the mobile ClientDetail to parity with the web programming tools." },
  { id: "mobile-performance-detail", area: "Mobile", status: "shipped", title: "Performance screen — detail parity", detail: "Ported three of the four missing web features to mobile aurora/performance.tsx on the same engines: the Return-to-Play panel (RtpPanel + /api/rtp helpers in lib/api.ts), the injury-table ACWR + top-driver columns, and the Readiness trajectory alongside HPI. The last web-only piece, the anterior/posterior tissue body-map, is CLOSED (Aug 2026): both clients now draw the same shared mannequin from @hybrid/core injury-body.ts via react-native-svg, which the app does depend on — see injury-body-map-primary." },
  { id: "mobile-calendar-layers", area: "Mobile", status: "shipped", title: "Calendar — coach-assignment layer", detail: "Mobile aurora/calendar.tsx rendered only the session heat-grid; web additionally layered /api/assignments (violet dots, assignment detail, a 'Mark done' PATCH), a legend and a Today-jump. Both clients now render the assignment layer identically. The event layer that used to sit alongside it went with `competition-intel` — removed from BOTH clients in the same change, so parity holds." },
  { id: "mobile-nutrition-weight-chart", area: "Mobile", status: "shipped", title: "Nutrition — bodyweight-trend chart", detail: "Web aurora/nutrition.tsx renders a weightTrend line chart and appends a weightChangeKg/28d suffix to the maintenance line; mobile omits both (never imports weightTrend). Add the bodyweight-trend chart to the mobile nutrition screen." },
  { id: "mobile-velocity-capture", area: "Mobile", status: "shipped", title: "Per-set velocity (VBT) input in the mobile logger", detail: "Mobile-logged sessions feed the velocity-profile engine: per-set bar speed (StrengthSet.vel) is entered in the live logger's exercise detail sheet (live-logger-exercise-sheet), at parity with web. The earlier inline m/s cell on the set row was superseded by the sheet in the late-Jul 2026 logger review." },
  { id: "web-logger-prefs", area: "Web", status: "shipped", title: "Expose the full logger preference set on web", detail: "Mobile logger-settings exposes ~17 prefs; web account-settings exposes only 4 (detailed, countWarmups, fractionalVolume, units). The web logger already READS several unexposed ones (plateCalc, autoAdvance, restTimer, restSeconds, keepAwake, haptics) at their stored default, so web users can't change behaviour they're subject to. Surface the shared prefs on web (restNotify/restSound stay native-only)." },
  { id: "parity-audit-2026-07", area: "Core", status: "shipped", title: "Web/mobile parity audit (Jul 2026) - destinations + copy reconciled, guarded by a test", detail: "A full parity sweep against the CANONICAL nav (@hybrid/core nav.ts NAV_ITEMS, 40 destinations), comparing the web app-shell screen switch to the mobile More hub's id->route HREF map. RESULT: all 40 destinations resolve on WEB; 4 do not resolve on MOBILE. Three of those (org, squad, teamcompare) are the already-declared mobile-team-surfaces gap and degrade honestly - the More hub tags them 'WEB' and opens the web app. The fourth is BROKEN, plus three more findings the nav diff alone does not catch: (1) ANALYTICS WAS DEAD ON BOTH CLIENTS - NOW FIXED ON WEB. The web app-shell rendered a MobileOnlyScreen pointer ('Analytics lives in the app - open HYBRID on your phone') while mobile had no analytics route, so its More hub tagged the item 'WEB' and sent the athlete back to the web app: a circular dead end whose copy asserted something untrue. The web implementation existed and compiled the whole time (apps/web/components/aurora/analytics.tsx, ~43 w.home.analytics.* keys) but had ZERO importers - orphaned when analytics was retired from web (see web-dashboards) while the mobile build (mobile-analytics) never happened. RESOLVED: app-shell now renders a persona-scoped AnalyticsScreen (every athlete gets AuroraAthleteAnalytics; a coach also gets AuroraCoachAnalytics off useRoster; an admin also gets AuroraOperatorAnalytics), so the destination works on web again and mobile's 'WEB' tag is truthful rather than circular. The MobileOnlyScreen helper now carries a warning not to point at mobile for a surface mobile has not built. While wiring it, AStat's decorative leading dot was removed per the no-decorative-dot rule. mobile-analytics REMAINS the open gap - this restores the destination, it does not close mobile parity. (2) AI COACH is unreachable on both. apps/web/components/aurora/ai-coach.tsx has zero importers; on mobile the /ai-coach route exists but NOTHING navigates to it (no router.push anywhere). The /api/ai-coach endpoint is live, so this is a missing door on both clients, not a missing feature. (3) COACH-APPLY is NOT a gap (a corrected false positive from the first pass, which inferred it from filenames alone). Mobile gives it a dedicated route (app/coach-apply.tsx, reached from Settings and the More hub); web hosts the SAME flow - intro, the identical 3 steps, credentials field, submit and status readout - inside components/account-settings.tsx under the 'coaching' tab. Same feature, different placement. A filename diff cannot see this, so parity checks must confirm against the rendered flow, not the file tree. (4) COPY-LEVEL DRIFT outside the social surface. The two clients implement the SAME screens off DIFFERENT i18n key namespaces - 448 keys used only by web, 293 only by mobile. Largest clusters: the logger (web w.train.logger 47 + loggerPrefs 17 vs mobile workout/live/summary ~59), plans (web w.train.plans 30 vs mobile plans 14), history (web w.analyze.hist 23 vs mobile history 8), blocks (web w.train.blocks 22). Both clients HAVE these screens, so this is not feature absence - it is two copies of the same copy, which can and does diverge in wording without any test catching it. social-i18n already collapsed the social surface onto ONE shared key set; the same treatment is owed to logger / plans / history / builder. RESOLUTION (all four destination gaps closed): analytics re-wired on web AND built on mobile (components/aurora/analytics.tsx + app/analytics.tsx, all three scopes off the same endpoints, charts in the house Spark/measured-bar idiom rather than a charting library); org, squad and teamcompare BUILT on mobile (components/aurora/org.tsx, team-monitor.tsx, team-compare.tsx + their routes), each on the SAME endpoints and the SAME shared engines as its web twin - buildTeamTree/flattenTree/canManageOrg/canRead/roleScope for Org and athleteSegment/SEGMENT_LABELS for the monitor - so role permissions and segment logic cannot drift, since that logic lives in core rather than in either client; the web tables and <select> dropdowns become stacked cards and tap-through rails, because neither survives a phone's width. The AI coach got a door on BOTH clients: an 'AI coach' module tile on each Cockpit's horizon list (web setScreen('aicoach') -> the previously-orphaned aurora/ai-coach.tsx; mobile router.push('/ai-coach')), matching mobile's existing not-in-nav route. New i18n w.home.cockpit.aiCoach/aiCoachValue and the previously MISSING nav.org (the web sidebar had been falling back to raw English), all EN/PL/DE. VERIFIED: re-running the nav diff now returns EMPTY in both directions - all 40 destinations resolve on web AND on mobile. COPY DRIFT (4) NOW RESOLVED TOO: 77 English strings were reachable through a web-only key AND a different mobile-only key (mobile carried legacy flat namespaces - live.*, log.*, summary.*, plans.*, history.*, coach.*, more.*, quickSport.*, recap.*, settings.*, workout.* - duplicating web's structured w.* keys). All 77 mobile call sites now point at the canonical w.* key and the duplicate entries are deleted from all three locales. CRITICALLY, 20 of the 77 had ALREADY diverged in Polish or German while still reading identically in English - so no English-language review could ever have caught them. One was an outright mistranslation: the Polish for 'No sessions logged yet' used 'zalogowanych' (the logged-IN / authentication sense) instead of 'zapisanych' (recorded). Three web keys turned out to have NO pl/de values at all, where mobile did - those translations were backfilled onto the canonical key rather than dropped. GUARD: packages/core/src/i18n-parity.test.ts fails if any English string becomes reachable through both a web-only and a mobile-only key. It understands runtime-built keys (t(`nav.${id}`)), since a client that resolves a namespace from a variable never names those keys literally and would otherwise look like it wasn't using them. Verified by injecting a duplicate and confirming the test fails on it. DELIBERATELY NOT collapsed: 11 generic one-word labels ('Loading...', 'Saving...', 'PR', 'Pause', 'blocks', ...) that each screen keys for itself - a language may translate the same English word differently by context, so forcing one key would make translation worse. They are allowlisted by VALUE in the test, so a whole sentence can never sneak in under that exemption. METHOD (re-runnable): diff NAV_ITEMS ids against the web screen cases and the mobile HREF map; then diff the t() keys used under apps/web vs apps/mobile. NOT a gap: mfa (mobile-mfa is declared blocked), endurance (deliberately mobile-first, and mobile HAS the route so the web pointer is truthful)." },
  { id: "web-only-features-to-port", area: "Mobile", status: "planned", title: "Web-only features that died with the retired web client — port the ones worth keeping", detail: "The old parity-followups list, re-read after the web retirement: these existed ONLY on web, so deleting the web client deleted them from the product entirely. Port to mobile the ones worth keeping, drop the rest deliberately: plan-detail Rest/RPE columns, sport PR markers, session-detail mixed-distance stat, history readiness chip, trends tonnage chart/column, running efforts column, profile-drawer 'View coaching' link + Top-lifts list, checkin week-streak, settings Simple/Full mode toggle. MOST URGENT: onboarding free-text questions were captured on web only — mobile returns null for text-kind answers, so that data is currently captured NOWHERE." },

  // ---- Strategy review (Jul 2026) — see audit/08-strength-platform-strategy-2026-07.md ----
  // The competitive teardown found four genuinely uncopyable assets and a set of
  // table-stakes gaps that every named competitor already ships. Recorded here so
  // the analysis can't evaporate into prose. NOTE: the same review recommends
  // RETIRING a large share of the shipped surface (tactical, longevity, talent,
  // org graph, competition intel, video intel, force plate, the nutrition stack,
  // the email platform, the agent org, the CMS) — that decision is the founder's,
  // so nothing is marked retired here; see the Kill List in the audit doc.
  { id: "history-import", area: "Core", status: "planned", title: "Import training history (Hevy / Strong / CSV)", detail: "TABLE STAKES + the switching mechanic. The #1 reason a lifter won't change trackers is two years of history. Hevy and Strong both export CSV. Import should reconstruct the full session history, then RECOMPUTE personal records with HYBRID's correct maths — bodyweight-aware tonnage (10 BW pull-ups at 70 kg = 700 kg), both-bells dumbbell tonnage, canonicalized exercise names — and finish on a Wrapped of the athlete's own imported years. The migration tool doubles as the differentiation demo." },
  { id: "live-activity-rest", area: "Mobile", status: "planned", title: "Rest timer as a Live Activity / Dynamic Island", detail: "TABLE STAKES. The rest countdown belongs on the lock screen so the phone goes back in the pocket between sets. Highest delight-per-line-of-code available on iOS; every named competitor either has it or is asked for it. Pairs with the existing rest-haptics + saved-rest work." },
  { id: "home-widget", area: "Mobile", status: "blocked", title: "Home-screen + Lock-screen widgets", detail: "TABLE STAKES, now BUILT and gated behind a flag. A real WidgetKit extension (apps/mobile/targets/widget — @bacons/apple-targets generates the Xcode target during prebuild): home-screen small/medium plus lock-screen circular (the streak) and rectangular (today's title + done state), deep-linking into the app (hybrid://), honest empty state until the app has run once. DATA PATH: the Today screen publishes a compact snapshot (title, plan-day sub, streak, done) through the local Expo module apps/mobile/modules/widget-bridge, which writes it to the App Group's UserDefaults and reloads the WidgetKit timelines — no network in the widget, it renders what the app last knew and says when that's stale. The whole native side is OPT-IN via WITH_APPLE_TARGETS=1 at prebuild (app.config.js) and the release workflow's with_targets input, so the plain release pipeline is untouched until the extra bundle id + App Group exist in the portal. Web parity: none possible — a home-screen widget is an OS surface web does not have (recorded per the parity rule, like device-workout-match's match action). NEXT: an interactive 'start rest timer' intent, and live-activity-rest for the in-session lock screen.", blockedBy: "Run the 'Mobile — build & TestFlight' workflow with with_targets=true. The workflow registers com.hybriddomain.xyz.widget and fetches its profile automatically; if the archive fails on the App Group entitlement, create group.com.hybriddomain.xyz once in developer.apple.com → Identifiers and tick it for the app + widget App IDs (the one step the ASC API key can't do), then re-run. Then verify on-device: add the widget, open the app once, the widget shows today's session + streak; tap → app opens." },
  { id: "watch-app", area: "Mobile", status: "blocked", title: "Apple Watch companion (today at a glance)", detail: "TABLE STAKES since ~2018, v1 now BUILT and gated behind the same flag as home-widget. A real watchOS SwiftUI app (apps/mobile/targets/watch, generated by @bacons/apple-targets at prebuild): today's session title, plan-day sub, streak and done state on the wrist, rendered from the same snapshot the widget uses — delivered over WatchConnectivity applicationContext (an App Group does NOT span two devices; the phone-side push lives in modules/widget-bridge, which re-sends the stored snapshot on session activation so a Watch paired later still fills in). DELIBERATE v1 SCOPE: a glance app, not a logger — 'log from the wrist' and then silent-logging build on this scaffold, and shipping the glance first is what makes the Watch target, signing and data path real before the hard part. Web parity: none possible — an OS surface (recorded per the parity rule). NEXT: wrist logging (sets confirmed from the watch), then silent-logging's set detection.", blockedBy: "Run the 'Mobile — build & TestFlight' workflow with with_targets=true (registers com.hybriddomain.xyz.watchkitapp + fetches its profile; watch targets are the least-trodden path in apple-targets, so expect this first run to need a fix on the mac if Xcode balks). Then verify on-device: install from TestFlight onto a paired Watch, open the iPhone app once, the Watch shows today + streak." },
  { id: "monthly-learned", area: "AI", status: "shipped", title: "\"What we learned about you\" — the model, made visible (monthly)", detail: "THE INVISIBLE MOAT, DEMOED. Everything on this screen was already being computed and every one of them was buried: the adaptive per-muscle ceilings sat behind a disclosure inside a sheet on a card on Performance (see adaptive-mrv), the measured clearance rate was one factor row inside that same disclosure (see recovery-pairs / clearance), and the readiness deficit was a ring that only ever spoke about TODAY (see readiness-deficit-ring). An athlete could log for three months and never be told what the app had learned about them — which is the one thing here no competitor can copy, because it needs THEIR data over time, and it is therefore the reason to keep logging. ONE ENGINE, ONE SCREEN, NO NEW PHYSIOLOGY: engines/learned.ts (`learnedMonth`) is an ASSEMBLY — it calls the same `athleteLandmarks` the Volume screen calls, with the SAME options object (mobile lib/use-volume-model.ts now exports `landmarkOptions` so the athlete half of the resolution is named once and handed out, rather than hand-copied into a second call that could quietly describe a different athlete), replays it week by week over the window via `replayLandmarks`, re-runs `athleteClearance` with the clock moved back for the month-on-month delta (no lookahead: only sessions and reads that existed then), and replays `readinessDeficit` day by day across this window and the one before it. THREE CHAPTERS, and each claim is a `LearnedFinding`: (1) YOUR CEILINGS — one row per muscle the log has actually tested, with the estimate, its stated interval, the qualifying-week count, the trajectory's settled/converging verdict, and the movement over the month; everything untested is named ONCE with the count and what would test it, because seven rows of 'not enough evidence yet' is honest and unreadable. (2) HOW FAST YOU CLEAR A SESSION — the measured index against the population decay curve, with pairs, its interval and the month's change. (3) YOUR READINESS PATTERN — the mean over the window with the athlete's own day-to-day spread, plus the LIMITER as a share of the whole month's deficit (the ring's costs sum to exactly 100 by construction, so summing them across a month is arithmetic, not a second model). THREE RULES THE SCREEN IS HELD TO, each with a test: EVERY CLAIM PRINTS ITS PROVENANCE in the four-layer vocabulary the Volume ladder already teaches (population → profile → observed → manual — reused, not reinvented, so both surfaces resolve one enum and one set of keys); an unproven clearance rate reads 1.0 and is labelled POPULATION AVERAGES, never 'you recover exactly as expected'. EVERY FIGURE SHIPS WITH ITS INTERVAL, and the two kinds are not interchangeable — a BELIEF interval is where the true value probably sits, a SPREAD is where the athlete's days actually fell (it does not narrow with evidence, because it describes them rather than our doubt); a band the evidence closed to one value reads 'pinned by your own weeks at 23' rather than 'probably between 23 and 23', and the limiter's share carries no interval at all because a census of the window is not an estimate. 'NOT ENOUGH EVIDENCE YET' IS A ROW, never an omission and never a hedge: it carries `needKey` — the two qualifying weeks, the two clean pairs of reads, the fortnight of days, or the 'learn from my training' switch being off (which is a DECISION and says so, not an absence). WHICH WAY IS GOOD is decided in core (`learnedSense` → `LEARNED_SENSE_ROLE`), because it is not the same answer per chapter: a ceiling that rises is progress, a clearance RATIO that rises is the opposite (the index is measured against the curve, and lower is faster), and the limiter's share rising is not good either — an arrow drawn off the sign alone would congratulate an athlete for recovering worse. ALSO SHIPPED: `RecoveryIndex` now carries its own lo/hi (feel-timing.ts) — the standard error of the weighted mean over the effective sample size, floored at CLEARANCE_INTERVAL_FLOOR so two pairs that agree to the decimal cannot buy the most confident claim on the screen off the least evidence in the app, and the population corridor (0.85–1.15) while unproven. SURFACE: mobile only, per the mobile-first rule — apps/mobile/app/learned.tsx + components/aurora/learned.tsx, free for everyone (gating it would gate the argument for the product), and the YOU TAB NOW LEADS WITH IT, above the cover banner and the identity block: everything below that is who you are to other people, and this is the only thing on the tab the app itself found out. The lead renders even with no history, because 'your model fills in as you log' is the promise the loop is made of and the athletes who most need to see it are in their first fortnight. Copy in EN/PL/DE (w.learned.*, counts inside prepositional phrases so Polish inflects correctly at every number). The seven muscle-group name keys moved into core as MUSCLE_GROUP_KEY (volume-view.ts) — two screens held a copy each and `posterior`'s key is `musclePosteriorChain`, so any third copy derived from the group names would have printed a raw key for one row in seven. 23 engine tests + a render gate (apps/mobile/test/learned.render.test.tsx) that fails if a figure ever renders without its interval, without its layer, or if a waiting claim is dropped instead of shown." },
  { id: "adaptive-mrv", area: "AI", status: "shipped", title: "Adaptive recoverable volume (per-athlete MRV estimator)", detail: "10x. The population MV/MEV/MAV/MRV table — the same textbook numbers every competitor uses — is now only the FIRST of four layers. engines/landmark-resolve.ts (`athleteLandmarks`) composes them in order, each closer to the individual than the last, and returns the provenance with the numbers so no screen can pass an average off as a measurement: (1) POPULATION, the table; (2) PROFILE (engines/landmark-profile.ts) — MEV/MV scale with a STIMULUS multiplier (training age: a novice grows off ~0.7× what an advanced lifter needs) and MRV with a RECOVERY multiplier (training age up, then chronological age −1.2%/yr past 30, body mass −0.4%/kg past 80, sleep, life stress, energy deficit, and training frequency), with the MAV band re-derived at its proportional position between the athlete's own MEV and MRV so each muscle keeps its shape and the ordering can't invert; every factor is returned with its multiplier and the value that produced it, plus a confidence that rises as the profile fills in; (3) OBSERVED (engines/landmark-adapt.ts) — the ceiling corrected by what actually happened, because a ceiling is only ever found by running into it: weeks carrying at least MAV-high sets are classified TOLERATED (week-on-week e1RM held, post-session fatigue and check-in soreness moderate, reported energy holding up) or OVERREACHED (top sets fell >3%, or fatigue/soreness spiked, or reported energy collapsed — see volume-recovery-feed for the check-in feed); symptoms set an upper bound and beat 'I got away with it', tolerated weeks at/near the ceiling raise it, the move is bounded to ±35% of the prior, and with fewer than two qualifying weeks it returns the prior untouched at zero confidence, carrying the evidence rows that moved it; (4) MANUAL — the athlete's own typed numbers, applied last, always winning. Wired into BOTH Volume screens: a 'Whose numbers are these?' card names the layer, lists the factors as signed percentages, shows the confidence, carries the profile editor (training age, age, body mass, sleep, stress, energy intake, sessions/week — prefilled from the intake answers and the bodyweight log so nothing is typed twice) and the 'learn from my training' switch. Answers the question directly: an 18-year-old 40 kg novice training 3×/wk resolves quads to MEV 6 / MAV 9–14 / MRV 15, a 40-year-old 120 kg veteran on 5×/wk to MEV 9 / MAV 12–17 / MRV 18 — different for opposite reasons (no work capacity yet vs. age and mass), against a population 8 / 12–18 / 20. The estimate now carries its STATED INTERVAL (MrvEstimate.lo/hi): unproven it is the whole ±35% corridor, it narrows as qualifying weeks accumulate (2 weeks → ±3, 4+ → ±1), and each side pins to what the weeks proved — a tolerated week props the bottom (the ceiling is at least what was carried), a symptomatic week caps the top — so the band is honest about WHICH side the evidence nailed down; shown with the adapted ceilings on both Volume screens as e.g. 'Quads 17 (15–19)'. Moat unchanged: the estimate needs longitudinal WITHIN-PERSON data that cannot be bought or scraped, and it compounds." },
  { id: "accent-channel-guard-gaps", area: "Web", status: "planned", title: "Accent text channel \u2014 the 56 sites the guard cannot see", detail: "apps/web/__tests__/accent-text-channel.test.ts enforces that no `color:` resolves to an accent FILL, and it caught 78 sites. Its two regexes only match a LITERAL at the head of the value \u2014 `color: C(\"red\")` and `color: var(--color-red)` \u2014 so two shapes slip through and are still painting glyphs in the fill: a literal inside a larger expression (`color: on ? C(\"lime\") : C(\"ash\")`, the toggle/selected-state idiom) at 37 sites across 23 files, and a COMPUTED accent (`color: C(token)`, `color: C(ZONE_TOKEN[zone])`) at 19 more. Same AA failures the guard was written for: on Kyoto Hour, sand is 1.57:1 and terracotta 3.26:1 against the card, at a 10\u201314px type scale that the large-text exemption never covers. Found while shipping volume-detail-drawer \u2014 the Volume screen had NINE of them, all now moved to `accentText()`, and its accent tokens are typed `AccentKey` so a computed one still resolves through the text channel. NOT swept app-wide in that change: 56 sites across 23 files is its own pass, and it belongs with the guard that should have caught them. TO DO: widen FILL_AS_TEXT to match an accent anywhere in the value expression, decide the rule for a computed accent (type the token as AccentKey and route it through accentText, as Volume now does, rather than trying to resolve it in a regex), then fix the fallout. NOTE: the 56/23 count predates the web retirement — most of those sites were deleted with the web client, so re-count against the remaining admin surfaces when widening the regex. The mobile twin is not affected \u2014 its idiom is txt(C, C.accent), which already maps fill to text tone." },
  { id: "volume-detail-drawer", area: "Core", status: "shipped", title: "Volume detail opens in place — and the provenance is a sheet", detail: "The compact volume block on Performance answered \"5/7 in range\", drew the seven-column week-shape, named the two muscles that were out of range — and then sent the athlete to a DIFFERENT SCREEN to read the detail of the card they were already looking at. Everything the shape raised a question about (the block ramp, Ease off / Add volume, the per-muscle rails) lived one full navigation away, with the shape itself left behind. Two changes on BOTH clients (apps/web/components/aurora/volume.tsx + apps/mobile/components/aurora/volume.tsx). (1) THE DETAIL EASES OPEN UNDERNEATH. The door is now a disclosure: pressing it opens a drawer under the week-shape carrying the block, both prescriptions and the muscle rails, with the chevron rotating rather than swapping glyph so the control reads as one object in two states. Web runs a 0fr → 1fr grid row (globals.css `.motion-drawer`) — a real height animation with nothing measured, so content that grows inside an open drawer just follows; mobile measures the panel and interpolates its height on the SAME `springs.sheet` curve, then hands the height back to `auto` once the opening has settled, which is what keeps an expanding muscle row from being clipped to a height measured before it grew. Both substitute a cross-dissolve under Reduce Motion. The sections are authored ONCE and rendered at two weights through a shared `Panel` (card on the screen, hairline-divided flat section inside the drawer), so the drawer is not a summary of the screen — it IS the screen, off the same components and the same landmark resolve. (2) WHOSE NUMBERS ARE THESE IS DISPATCHED AS A SHEET. The provenance ladder, the completeness meter and 'Show the working' (level, factors, the log's correction, the ceiling replay) were the seventh card at the foot of the reading, where they read as more of the prescription; they now sit behind a door row and open as a Sheet (medium/large detents) from wherever the reading appears — the screen or the drawer — so the question is answered without the athlete losing their place. COST DISCIPLINE: the eight-week per-muscle histories and the landmark REPLAY (one full resolve per week of history) are bought by the tap that reveals them, never at mount; once opened the detail stays mounted, because unmounting it on close would give the collapse nothing to collapse. The first cut gated both on one `deep` flag (`!compact || everOpen`), which spared the compact card on Performance and did NOTHING for the standalone screen, where `deep` is true from mount — so audit/10's two remaining render-cost lines were only half closed. Each pass is now gated on its OWN reader, which is strictly tighter on both surfaces. The REPLAY has exactly one reader (SourceBody, inside the provenance sheet) and is gated on that sheet having been opened, latched so it survives the sheet's closing animation — it was resolving one landmark per week of history at mount to draw four rows behind a collapsed disclosure nobody had asked for. The eight-week CHART lives inside a muscle row's own drawer and only one row is open at a time, so it is computed for the open muscle alone — six of the seven passes drew nothing and the seventh was unopened — HELD through the collapse by the same `useRef`-latched-in-render idiom `lastZone` uses one scope down, because reading `open` straight would empty the chart the instant the row is dismissed and the drawer would shut on nothing. ONE DISCLOSURE, EVERYWHERE: every fold on the surface now runs the same drawer — the compact card's detail, 'Show the working' inside the provenance sheet, a muscle row's MV floor and eight-week chart, and the band definition under a spotlight. Three of those were `{open && \u2026}` conditionals that POPPED inside a card that eases, which read as three different mechanisms rather than one idea at different scales; they are now the shared `Drawer` on both clients. Two things fall out of keeping the content MOUNTED so a collapse has something to collapse: the fold does not render until it has been opened once (seven muscle rows each carrying an eight-week chart is not free on a phone), and a closed drawer is taken out of the focus order and the accessibility tree \u2014 `inert` on web, `accessibilityElementsHidden` + `importantForAccessibility=\"no-hide-descendants\"` + `pointerEvents=\"none\"` on mobile \u2014 because a clipped panel is otherwise still reachable by Tab and by VoiceOver. The spotlight's band is held through its own collapse, so the definition does not blank out the instant the spotlight is dismissed. TWO FUNCTIONAL BUGS FIXED ON THE WAY. (a) Leaving for the model editor from INSIDE the provenance sheet did not close it \u2014 the sheet is a modal over the shell (a Modal in its own native window on mobile), so a route change underneath just swapped the screen it was covering: the athlete tapped 'Training age', the app navigated, and they still saw the panel they tapped from. Both clients now close the sheet on the way out. (b) On mobile that same 'Next' row \u2014 the single most valuable thing the athlete could do to personalize their landmarks \u2014 set an `aboutOpen` flag that NOTHING rendered, so the control did nothing when pressed while its web twin opened the model editor; it now goes where web goes, and BOTH Performance pages now pass the model route into the compact block \u2014 the provenance sheet is reachable from there too, and without a destination that same row rendered its arrow and did nothing on both clients (a control that only ever had a destination while the card lived on the standalone screen). Dead code in both volume screens went with them (the retired in-place editor's leftovers: an unused Stepper, the bodyweight/check-in hooks the shared use-volume-model has owned since the model editor moved out, and six unused type imports). The standalone Volume screen (nav \u2192 Analyze \u2192 Volume, mobile /volume) is unchanged and still carries the same sections as cards. New copy `w.analyze.vol.hideDetail` localized EN/PL/DE. BUG FIXED AFTER THE FIRST SHIP — THE MOBILE DRAWER NEVER OPENED. It measures its panel to know what height to open to, and it took that measurement IN FLOW inside a box clipped to exactly 0. Yoga reads a zero available main size as an at-most-0 CONSTRAINT rather than as an overflow, so the panel measured 0, the height it would open to was 0, and the guard that waits for a measurement bailed on every run — the chevron rotated and the label toggled over a card that never moved. (Any height above zero overflows normally; 0 is the one value that clamps, which is why it survived review.) The drawer is now the mobile kit's shared `ADrawer` (apps/mobile/components/aurora/kit.tsx), which positions the measured panel ABSOLUTELY for every state that pins a height on the drawer — sized against the drawer's width alone, so it measures its true height closed, opening and open — and returns it to flow once the opening settles, which is what keeps the `auto` height that lets an expanding muscle row grow inside an open drawer. The Activity card on Today carried its own copy of the same measured drawer with the same defect (see activity-breakdown) and now runs the shared one. apps/web/__tests__/drawer-measure.test.ts holds both the structure and the single implementation. Web was never affected: its 0fr → 1fr grid row measures nothing." },
  { id: "volume-periodization", area: "Core", status: "shipped", title: "Periodized volume — the MEV → MAV ramp across a block", detail: "Landmarks are the walls of the corridor, not the target; sitting at MRV every week is the classic mistake, because it leaves no room to progress and buries the athlete in fatigue by week three. engines/volume-block.ts turns the landmarks into a WEEK-BY-WEEK prescription: week 1 sits at MEV, the accumulation weeks ramp linearly toward the top of MAV, an optional planned overreach runs the last load week into the MAV→MRV gap (stopping one set short of the ceiling), and the deload week drops to MV. `blockVolumePlan` then reports each muscle against THIS WEEK'S target rather than the static band — the same 12 quad sets read as over target in an introduction week and short in a peak week — while `overCeiling` keeps the MRV ceiling winning over the plan regardless of the ramp. `blockFromMacrocycle` reads the block position straight out of the periodization engine's macrocycle (mesocycle = block, trailing recovery microcycle = deload) so the season plan and the volume ramp stay one thing. On BOTH Volume screens: a 'This block' card draws the whole block as a ramp strip (a low introduction week, the climb, the step down of the deload) with the week's job named, the per-muscle rails carry the week's target as a caret on the same normalised rail, and the block (length, current week, last-load-week style, deload) is editable in place. Off by default — switched off the screen is exactly the landmark view it was." },
  { id: "checkin-honest-metrics", area: "Core", status: "shipped", title: "One tap answers one question (bug fix)", detail: "BUG FIX with real downstream consequences. The one-tap readiness face on Today asked \"how ready do you feel?\" and wrote the picked level into ALL FOUR check-in metrics — energy, sleep, soreness and mood — as though the athlete had reported four measurements. They reported one; the other three were invented and stored indistinguishably from answers actually given. The guided flow had the same defect from the other end: its ratings defaulted to 3 and all four were submitted, so a question walked past without a tap was stored as a middling 3. Neither invention was inert. The volume profile reads mean check-in SLEEP and presents it to the athlete as MEASURED (green field, \"read from your own data\"), and the adaptive-MRV estimator reads soreness and energy as recovery evidence over an 8-week window — so one tap was silently feeding three models numbers nobody had entered, and the app was labelling them measurements. FIXED in core (checkin-flow.ts): `quickCheckinMetrics(rating)` returns exactly `{ energy, sleep: null, soreness: null, mood: null }`, and both clients send it; the guided wizard now tracks which metrics were ACTUALLY answered (touched here, or already non-null in today\'s stored row) and sends null for the rest, with the review card showing a dash rather than a neutral face for anything unanswered — a face would claim a reading that was never given. `checkinRating` already averaged only the metrics present, so a single-metric check-in still resolves to exactly the feeling the athlete picked; `answeredMetrics` / `outstandingMetrics` / `checkinCompleteness` / `firstOutstandingStep` expose the partial state, and every downstream reader already treated null as unknown rather than as a 3. Also folds two smaller drifts: both wizards prefilled by comparing days with their own `new Date().toDateString()` while the rest of the screen used `localDayKey` (two definitions of \"today\" in one component), and the web wizard issued a private GET /api/checkins alongside the shared cache — both now read one cache through one day-key helper. reference/sql-checkin-unfabricate.sql is an OPTIONAL cleanup for rows already written, which nulls the three copied metrics where all four are equal; it is explicitly a heuristic (a genuine flat-3 check-in is indistinguishable) and the contamination otherwise ages out in 4-8 weeks on its own." },
  { id: "checkin-followup-popup", area: "Web", status: "shipped", title: "Today's feeling card — the follow-up is a pop-up", detail: "The card keeps its one row of four faces (the thing that works: a single glanceable question, one tap to answer). What changed is everything after the tap. The rest of the check-in used to expand INLINE inside the card, which made the card grow into a wizard and buried Today\'s other content; it now opens as a POP-UP — Sheet on both clients — that gets the surface it needs to ask three questions properly, and dismissing it leaves honest partial data rather than a half-filled form. The pop-up opens AUTOMATICALLY on the tap, because that is the moment the athlete is most willing to answer more and, since the quick tap no longer fabricates the other three (see checkin-honest-metrics), it is now the only way sleep, freshness and mood ever get real values. It resumes at the first UNANSWERED question via `firstOutstandingStep` rather than the old hardcoded \"start at Sleep\", which was only correct while the tap pretended to answer Energy. The card\'s trigger row shows completeness as \"1 / 4 answered\" plus a four-dot readout naming which metrics are in — a count alone doesn\'t tell you what you would be answering — and switches to a quiet complete state once all four are logged. Web and mobile carry identical logic and copy from core." },
  { id: "checkin-answers-survive", area: "Core", status: "shipped", title: "The check-in keeps the answers you gave (bug fix)", detail: "Three reported symptoms — \"the readiness face doesn't match the check-in\", \"I answered everything and it still feels unsaved\", \"there are dashes after I answered\" — with four independent causes, all of them the app contradicting the athlete about their own data. (1) SILENT DATA LOSS. POST /api/checkins upserts the day, but wrote EVERY column on every save, so any field a sender omitted was stored as null — and each surface sends a different subset. Re-tapping readiness in the afternoon sent {energy, sleep: null, soreness: null, mood: null} and DELETED the three answers logged that morning; submitting the follow-up on web (whose form never loaded the day's extras) deleted the weight, adherence and note every time. The route now PATCHES an existing day: an absent key is left alone, an explicitly-null key clears. Core supplies the wire payloads — `quickCheckinPatch` (the one metric a tap answered) and `checkinMetricPatch` (only the questions actually answered) — and the web wizard now prefills the day's extras the way mobile always did, with CheckinRow widened to name the fields the API had always returned. (2) THE PHANTOM DASHES. The wizard's ratings default to a neutral 3, and the metric card rendered that default as a SELECTED tile under a confident word (\"Okay\"), so pressing Next told the athlete twice they had answered. Nothing was stored — correctly, a default is not a report — and the review card two steps later showed a dash for every question they thought they had just answered. The metric card now mirrors the effort card, which already got this right: untouched, the face is dimmed to 30%, the word reads \"Not answered\", no tile is selected, and the advance button says SKIP (quiet outline) rather than Next (lime pill). The progress bar likewise fills on ANSWERS rather than position, so walking to the end no longer draws a complete bar over a one-answer check-in. The review's dashes became BUTTONS back to their question, under a \"2 / 4 answered — tap a dash to answer that one\" line. (3) THE CONFIRMATION NOBODY SAW. Embedded in Today's follow-up sheet, `onDone` fired on the POST's return and the host closed the sheet — mounting and unmounting the wizard's \"Check-in logged\" card in the same tick. Finishing the check-in looked exactly like the app dropping it. `onDone` now only REFRESHES; the new `onClose` is the dismissal and the athlete presses it, from a confirmation that states the count, swaps to a partial-save line when questions are blank, and offers \"Answer the rest\" straight back to the first one. (4) THE ANSWER THAT CHANGED ITSELF. Today's picker highlighted `checkinFeeling` — the MEAN of every metric present — under \"how ready do you feel?\". Tap Primed, then log sleep 3 / freshness 2 / mood 4, and the card came back highlighting Good and reporting \"you logged Good\": the app overwriting the athlete's own answer with a number they never gave. New `quickCheckinFeeling` reads the metric the question actually writes; the average still feeds the load model, which is a different question that genuinely benefits from more signal. The 6h cooldown moved off `feeling` onto whether the day has a ROW, so a day answered without its readiness question can't lock the very question it is asking. Also unified the VOCABULARY: `checkinMetricWordKey` names the quick metric in the picker's words (Wrecked/Flat/Good/Primed — the face was already the readiness face) instead of the generic scale words, and qEnergy now asks \"How ready do you feel?\" verbatim in EN/PL/DE, so one tap and one card ask one question. The follow-up subtitle dropped its hardcoded \"Three more questions\", which was wrong on every day the athlete trained. Both clients, identical logic and copy from core. SECOND PASS — the same class of bug, everywhere else it lived. (5) EVERY subjectiveReadiness CALLER read the average. The readiness pick drives four things — the picker highlight, the week rail's readiness pill, the load multiplier, and the nudge that QUOTES it back (\"Eased to 94% — you're feeling flat today\", rendered with that feeling's own face) — and all six call sites across both clients (Today/Home, both Cockpits, the web logger's AI quick-start, mobile workout.tsx) passed `checkinFeeling`. So the rail pill said \"Good\" while the card directly beneath it highlighted the Primed face just tapped, and the Cockpit told athletes they felt flat on a day they had described as primed. `checkinFeeling` had ZERO callers that legitimately wanted an average — every one of them wanted the answer — and now has none in app code; it keeps its export and a warning doc-comment for the honest reading it does describe (the day as a whole). (6) TWO STALE-DAY BUGS in web today.tsx's todayFeeling: it called the clock INSIDE the memo with no `today` dependency (a tab open across midnight scaled today's load off yesterday's check-in) and compared days with its own `toDateString()` rather than `localDayKey` — both already fixed for `dayCheckin` twenty lines above it in the same file. Same `toDateString()` drift removed from the web logger and mobile workout.tsx. (7) THE TAP DIDN'T COUNT UNTIL THE SERVER AGREED. The POST invalidates the cache and the follow-up sheet opens in the SAME tick, so every reader below ran on the pre-tap row until the refetch landed: the face just pressed wasn't highlighted, the trigger still read \"0 / 4 answered\", and `firstOutstandingIndex` opened the flow ON THE READINESS QUESTION THAT HAD JUST BEEN ANSWERED — which is how a walked-past default became a dash in the review. Both cards now hold the tap optimistically, scoped to the viewed DAY so switching the rail can't carry it and applied only until the stored row carries a readiness answer of its own, so the server's value wins with no clean-up. (8) The web wizard's prefill effect re-ran on every cache change and re-merged the EXTRAS, so a refetch could undo a note the athlete had just cleared; extras now seed once while the ratings keep merging (they have to — that refetch is what tells the sheet Energy is answered). 11 new core tests." },
  { id: "fitness-level-engine", area: "AI", status: "shipped", title: "Training level read from the bar, not from a tap", detail: "Training age is the strongest single input to the volume model — the only one that scales the STIMULUS end, so it moves MEV as well as MRV — and it came from one onboarding tap. Self-assessment is unreliable in both directions: eight hard months reads as \"intermediate\", a decade of unprogressive gym-going reads as \"advanced\". engines/fitness-level.ts estimates it from the log instead. Relative strength (best e1RM ÷ body mass) on five benchmark lifts — Back Squat, Deadlift, Bench Press, Overhead Press, Front Squat — is compared to published-standard entry ratios for novice / intermediate / advanced / elite, shifted for sex (per-lift factors, ~0.68 upper body to ~0.78 deadlift) and scaled by the same age-development curve benchmarks.ts uses, so a 16-year-old and a 45-year-old hitting the same ratio are not read as the same athlete. The BEST lift sets the level, not the average — training age is what you have built, not a number dragged down by the lift you neglect. Only working sets count, only ≤12-rep efforts (a rep-out is not a max test), only the last 180 days, and names resolve through the movement catalog so a library alias still counts. Confidence rises with how many benchmark lifts are represented (one is a data point, three a picture) and is capped at 0.85, because a ratio is a proxy for training age rather than a measurement of it. `resolveExperience` keeps the athlete's own answer WINNING — the same rule the landmark resolver applies to every manual value — and reports `disagrees` so the UI can show the mismatch and offer the estimate rather than silently overriding. With no bodyweight, no benchmark lift or nothing in the window it returns `basis: \"none\"` and zero confidence: the caller falls back to what the athlete said, never to a default tier. Surfaced on both Volume screens with the lifts and ratios behind it. BOTH HALVES OF A HYBRID ATHLETE: relative strength answers the question for a lifter and says nothing about a runner, so there is a second, independent read. Running performance is normalised to a 5 km equivalent with Riegel (T5 = T x (5/D)^1.06) so a 10 km and a parkrun are comparable — a marathoner is not read as a slow 5 km runner — and scored against published pace standards (6:00 / 5:00 / 4:10 / 3:20 per km for novice / intermediate / advanced / elite, male at peak training age), with the female table 11% slower and the same age-development curve applied, dividing rather than multiplying because for a pace an easier bar is a SLOWER one. Runs under 3 km are not aerobic tests and over 45 km are past what Riegel is good for; both are ignored. Whichever half is stronger sets the level, for the same reason the best lift does, and `basis` always names which halves had data (\"strength\" / \"endurance\" / \"both\" / \"none\") so the card says \"from your best lifts and your best run\" rather than implying it read everything. A runner needs no body mass, since a pace is not relative to one. STILL RUNNING-ONLY, and named rather than guessed: cycling and rowing pace depend on gearing, terrain, drag factor and draft, the same watts produce wildly different splits, and a threshold table over raw speed would be a fiction — a cyclist with no runs and no lifts still gets `basis: \"none\"` and falls back to what they told us. Distinct from benchmarks.ts, which answers a different question (percentile against a sport cohort) and shares only the development curve. THE HALVES ARE KEPT SEPARATE AS WELL AS FOLDED TOGETHER (Aug 2026): the estimate now returns `strengthLevel` and `enduranceLevel` beside the headline, because \"how strong\" and \"how fast\" have different consumers and collapsing them before returning was a real defect. `experience` feeds athleteLandmarks, which sets MEV/MRV — how many hard SETS a muscle can be prescribed — so `resolveExperience` reads the STRENGTH half only. Before the fix a 60 kg athlete running a 1:08 half marathon (elite) who squats 1.06 x bodyweight (novice) was handed `advanced` training age and prescribed an advanced lifter's set counts on the strength of the run; a runner with NO benchmark lift at all got the same. Now a run can never set lifting volume: with no benchmark lift there is no derived training age, the caller falls back to what the athlete stated and then to `unknown`, never to a tier invented from a discipline that cannot speak to it. The headline is unchanged and still takes the BEST of the two halves, so a weak jog never drags a strong lifter down either. Also added `displayLevel()`, which returns null when `basis` is \"none\" — the raw `level` field carries \"untrained\" in the no-data case, so a surface rendering it unguarded would tell a serious cyclist (a discipline the model deliberately cannot read) that they are untrained. Locked by tests over both specialist athletes. SURFACED AT THREE ALTITUDES (Aug 2026): the estimate sat FOUR interactions deep (Performance -> Volume drawer -> provenance sheet -> 'Show the working'), which is a strange place for the one figure every athlete arrives wanting. Now (1) a LEVEL CARD on Performance, between Tissue and This Week's Volume - who the athlete is before what they should train, which is also the causal order since training age is the strongest input to the bands below it. Four beats, each skippable: the level as ONE WORD at 40px (no unit, no context, no reading - most visits end here); five TIER SEGMENTS, lime at 40% for tiers passed and a part-fill for the one in play, carrying which tier AND how far into it; one PLAIN SENTENCE assembled from two clauses, opening on kilos the athlete remembers loading rather than the ratio that drives the engine ('Your 180 kg Deadlift puts you here. Elite begins at 225 kg - 45 kg above your best.'); then the ratios in fine mono and the door to the working. An earlier cut drew a hairline AXIS with a travelling tick and end labels - reverted, because nothing in HYBRID has ever drawn an axis: the app says 'how far along' with a filled track, a lit spine, flex segments or a chip, and the segments are the readiness deficit bar's geometry painted with the provenance ladder's own lit/active rule. (2) A BADGE on the Profile name row beside the membership pill, on both clients. Earned, never claimed - it renders only the log-derived estimate, never the self-assessed onboarding answer, and shows nothing until TWO independent results back it (badgeFor, BADGE_MIN_EVIDENCE), so one heavy single cannot crown a public 'Elite'. ONE WORD only: PR loads are already public tiles on that screen, so publishing the ratio beside them would let anyone divide and recover the athlete's body mass. Accent ramp reuses the palette (ash / chalk / lime accent-text / gold for elite), all AA-guarded per theme. (3) The Volume working is unchanged and is what the card's door opens. New in core: nextThreshold() returns the reach to the next tier in the athlete's own units (kg on the bar, sec/km) with the gap derived from the SAME rounded figures as the target so the arithmetic on screen adds up, and turns into a MARGIN clear of the floor at elite; every evidence row now carries tierFrom/tierTo/progress. New i18n in EN/PL/DE, and the card is specified against the German string since prose is what breaks in translation. Both clients read one useFitnessLevel() hook (web lib/use-fitness-level.ts, mobile lib/use-fitness-level.ts) which useVolumeModel now consumes too - three surfaces, one estimate, so the profile can never claim a level the Performance card contradicts. THE ENDURANCE HALF IS NOW SIX DISCIPLINES, NOT ONE (Aug 2026, engines/endurance-level.ts). THE PRINCIPLE: performance = engine x economy and economy <= 1, so every discipline hands back a LOWER BOUND on the same aerobic capacity. That makes averaging across disciplines not merely unkind but invalid - the mean of two lower bounds is not a lower bound, and an elite run averaged with a hobby swim is false about both. The MAXIMUM is the tightest bound the evidence supports, so an advanced runner who swims for fun keeps her level. THE COMMON CURRENCY: each discipline owns four entry thresholds in its OWN unit (the same shape the five lifts use) and a raw performance is interpolated onto one unitless engine score - novice 25, intermediate 50, advanced 75, elite 100 - which runs PAST 100 so athletes beyond the bar are not flattened into one another, and decays below novice rather than clamping. ADMISSION IS PER MODALITY, NOT PER SPORT: running (unchanged, 3-45 km); swimming (pool freestyle, 100-1500 m, per 100 m at a 400 m equivalent - open water and named other strokes declined); rowing (ERG only, 500 m-10 km - on water is boat class, crew, stream and wind, not fitness); cycling (POWER only, 20-60 min, W/kg - speed is still refused, and a ride without a meter is still unread, but with one it is the cleanest signal of the six); XC skiing (5-50 km, with a SOLO CAP below elite because snow and course vary more than skiers do - another discipline agreeing lifts it); triathlon (a property of the SESSION - swim+bike+run within 12% of a canonical race - checked before the legs so a race is never also read as three weak standalone efforts). THE GATES ACT ON THE GOOD RESULTS, because under a maximum the risk is never that a weak result drags you down but that ONE flattering result lifts you, and six disciplines mean six chances at a lucky reading: a discipline with 2+ efforts speaks with its SECOND BEST (the PR is kept and still shown, it simply does not set the level - a fluke happens once, a capacity twice); a single effort is still read but marked unconfirmed, with reduced confidence and no public badge; each discipline enforces its own valid range. CONFIDENCE COMES FROM CORROBORATION, NOT COUNT, weighted by a TRANSFER matrix (run-ski .80, run-tri .85, run-bike .60, run-swim .30): an elite runner who swims badly is unremarkable and the swim barely moves confidence either way, while an elite runner who SKIS badly is surprising and is FLAGGED, because that pattern is far more often a mis-tagged session or a drifting GPS track than a physiological marvel. Nothing below the maximum ever subtracts from the score - weak results change job from setting the level to certifying it. REJECTED ON PURPOSE: shrinking the max toward the second discipline (max - k x transfer x gap); any k large enough to catch a real outlier also demotes the honest specialist, who is the athlete the design exists to protect. Also fixed here: the strength confidence formula now counts only LIFTS, since a weak run used to be counted as support for a verdict it contradicted, and evidence is ranked by how far through its own tier a result sits rather than by a raw figure, which is the only quantity a deadlift and a 40-minute power effort can be ordered by. Needs CardioBlock.watts (added, optional, written by a paired meter or a device import - never typed) for cycling; every other discipline was already tagged at log time, so the swims and rows already in the database light up with no new logging. 26 unit tests in endurance-level.test.ts. Surfaced on both level cards (the sentence names the discipline and quotes its own unit through one shared enduranceFigure(), so a swim can never be labelled 'per km') and in the admin Engine Room as four new formulas: endurance-score, endurance-admission, endurance-gates, endurance-combine and endurance-transfer. THE BADGE REACHES OTHER ATHLETES (Aug 2026): it was on the owner's own profile only, which is half a social feature. PublicProfileResponse now carries `fitnessLevel` - the WORD and its accent, never the ratio and never the evidence - computed server-side in /api/social/profile/[handle] behind the SAME canViewResults privacy gate the stats use, so a private account's level never leaves the server rather than being filtered by a client that could choose not to filter it. Rendered as a chip beside the handle in web's ProfileDrawer and mobile's social-kit, the slide-over that feed, discover and the leaderboards all reuse. Body mass is read from the latest BodyMetric so the strength half is scoreable; without it the endurance disciplines still score, so a runner or swimmer keeps a badge. ALSO TIGHTENED: badgeFor counted evidence ROWS, so two disciplines holding ONE effort each earned a public badge - two rows and two flukes, exactly the reading the endurance gates exist to distrust. It now additionally requires the result that SET the level to be confirmed (repeated), and the card says so in a line of its own rather than leaving a missing badge unexplained. SEX FINALLY REACHES THE ENGINE (Aug 2026). Every strength and endurance threshold in the app is published for a MALE athlete at peak training age and shifted from there (relative strength x0.68-0.78, pace x1.06-1.12, power x0.85) - and nothing ever PASSED a sex, so estimateFitnessLevel defaulted to \"M\" and silently held every female athlete to the men's bar. For most that cost a whole tier of training age, which then moved MEV and MRV. It now lives on AthleteVolumeProfile beside age and body mass (no migration - the volume profile is device prefs), is offered as a two-option toggle in the volume-model editor on both clients, is threaded through useFitnessLevel, and counts as a real gap in the completeness meter at weight 0.08 (the other weights rebalanced to keep the distribution summing to 1). The server-side public badge USED TO read sex from TalentProfile, the only place it was PERSISTED — but the Talent Graph was retired (Aug 2026), so the public badge now falls back to the male bar while the athlete's own card is still correct. The durable fix is persisting the volume profile server-side, recorded as volume-profile-server-sync. CYCLING POWER CAN NOW ACTUALLY ARRIVE: DeviceWorkout.avgWatts added and projected onto the block by device-truth on the same measured-outranks-typed rule as distance and the second-accurate clock, so a paired power meter lights cycling up the moment the native read ships. TRIATHLON HONOURS AN EXPLICIT TAG: a session the athlete tagged is matched against the canonical distances at a wider 25% tolerance instead of the 12% used for inference - a tag is an assertion and inference is a guess - while still being unable to invent a class, so a tagged sprint stays a sprint." },
  { id: "volume-model-web", area: "Web", status: "shipped", title: "Volume model editor on web", detail: "The volume MODEL — the athlete's own per-muscle landmark profile (MV/MEV/MAV/MRV edited set by set, the numbers that personalize every landmark readout) plus the about-you profile behind it — has its own route on BOTH clients: apps/web/components/aurora/volume-model.tsx (screen id `volume-model`, reached from Volume's header and its provenance card) and apps/mobile/app/volume-model.tsx. Both resolve through the shared lib/use-volume-model hook, so the screen and the route can never compute different landmarks, and both LEAD with the diff — the same profile resolved with and without the athlete's overrides, so every band they have moved shows as a change from the estimate it replaced and can be put back. RECORD CORRECTION (Aug 2026): this shipped as part of the Performance rebuild (`performance-six-surfaces`, follow-up (a)) and neither this entry nor the parity guard's ACCEPTED_GAPS list was updated with it — so the registry claimed a gap that no longer existed, and apps/web/__tests__/parity.test.ts had been failing on main with its own instruction ('listed as an accepted gap but now ships on both — remove it from ACCEPTED_GAPS'). Both are corrected here. The guard working as designed is the reason it was caught." },
  { id: "volume-profile-completeness", area: "Core", status: "shipped", title: "What the model still doesn't know about you", detail: "The personalized landmarks are only as individual as the profile behind them, and between \"population table\" and \"estimated for you\" the athlete had no way to tell which they were looking at or what one more answer would buy. engines/athlete-profile.ts names the gap: every input carries a WEIGHT reflecting how much it actually moves the estimate (training age 0.30, body mass 0.18, down to stress 0.03 — a distribution summing to 1, not a checklist), so the meter is weighted by influence rather than by counting boxes. A progress bar that treats training age and stress as equal is lying about where the athlete's effort pays off. It reports the score, what is missing ordered by value, the single most valuable next answer with an i18n line saying what it unlocks, and which gaps the app could fill itself (`measured`, from landmark-context) so \"we worked it out\" is never shown as missing. PAIRED WITH ATHLETE HEIGHT, tracked separately as `athlete-height` (now shipped — the column is live and the athlete sets it in Profile → Body & progress). BodyMetric.heightCm feeds `frameAdjustedMassKg`, which reads body mass against the frame carrying it instead of as raw kilos: the recovery multiplier docks mass above an 80 kg reference, but 95 kg at 195 cm and 95 kg at 170 cm are not the same load, and the flat rule penalised them identically. Mass is now compared to what the height predicts (a plain BMI-style expectation at 24.5 — deliberately NOT a body-composition model, since the app cannot see composition and pretending otherwise would be the same class of error as inventing a sleep score). Without height the raw-kg rule applies exactly as before, so nothing regresses; the factor still displays the athlete's REAL mass rather than the adjusted figure." },
  { id: "one-feeling-card", area: "Core", status: "shipped", title: "One instrument, asked at two moments", detail: "There were TWO places a feeling was logged: the daily check-in and a separate post-workout prompt. They asked the same question — \"how are you right now\", on the same 1-5 scale, drawn with the same faces — through two code paths with different maths. That is why the Today card could tell an athlete a reading \"isn't counted against your recovery\" while the estimator counted it anyway: one question, two implementations, already disagreeing. The fix was to make it ONE instrument, and that part stands: one scale, one lag model, one threshold, one place the maths lives. What the first pass got wrong was concluding that one instrument meant one MOMENT — the post-workout prompt was deleted outright, which threw away a reading the daily card cannot recover (see two-read-feel, which restores it as the first of two scheduled reads rather than as a second implementation). THE FLOW IS DYNAMIC (core/checkin-flow.ts `checkinSteps`): four daily questions, then ONE EFFORT QUESTION PER SESSION trained that day, then details. A rest day is exactly the four questions it always was. Effort is per-session rather than per-day on purpose — a hard lift and an easy jog on the same day are not one number — and it lands on Session.feel, which is where the effort model, `personalTrainingLog`, fatigue, readiness, ACWR and injury risk have always read it from, so NO MIGRATION and no second source of truth. `stepAnswered` / `dayCompleteness` / `firstOutstandingIndex` walk both halves, so the card's \"2 / 6 answered\" and its dot row count sessions as well as metrics, and the pop-up resumes at the first genuinely unanswered step whichever kind it is; a session already rated on the finish screen shows as answered rather than being asked twice. THE RECOVERY HALF MOVED WITH ITS MATHS: `RecoveryReport` gained `loggedAt` (Checkin.createdAt), landmark-adapt finds the last session that ended before each check-in was written, and runs the feel-timing lag model over it — so the check-in's freshness and energy are discounted for proximity to training exactly as the post-workout answer was. Freshness and energy are folded onto ONE spentness scale and pooled with any session-side reports into a single `recoveryCost`, read by a single threshold. That makes the card's promise true rather than aspirational: a flat reading 90 minutes after squats no longer moves the ceiling, the same reading 20 hours later does, and the floor of the scale still flags either way because the worst possible answer on every metric is past what one session explains. Rows with no write time fall back to the untouched raw rules." },
  { id: "feel-timing", area: "Core", status: "shipped", title: "When you logged the feeling changes what it means", detail: "\"How spent are you?\" is not one question — it is a different question at every hour after the session. Tick \"wrecked\" ten minutes after a heavy squat session and you have described a set of squats. Tick it ten hours later, showered and back at a desk, and you have described a problem. The app stored identical rows for both and every model read them as the same number. core/feel-timing.ts fixes that with one small stated model: RESIDUAL — acute fatigue decays, exponential (tau 6 h) toward a 35% floor standing in for the muscle-damage component that outlives the day, so `expectedResidual(h)` is the fraction still expected to be present at h hours; COST — the report divided by the residual expected at that lag, a timing-independent estimate of what the session actually cost (fatigue 4 at 1 h = 0.83, the same 4 at 10 h = 1.50); WEIGHT — full up to 12 h, then decaying toward 0.25, because a report given days later is recall rather than measurement; READ — the reading classified as immediate (<3 h, a read on the SESSION), sameDay (3-12 h), nextDay (12-36 h, a read on RECOVERY and the most informative one), or stale. Thresholds are written against `cost`, not the 1-5 scale, because 5 is the top of that scale and saturates: a 5 at 1 h and a 5 at 10 h both display as 5 while their costs differ by a third. COST_HIGH (1.15) is calibrated so a hard session logged in the gym is not a flag and the same answer hours later is, and MIN_STRAIN_FATIGUE stops a long lag from inflating \"I feel fine\" into evidence of strain — dividing a small number by a small number must not manufacture a signal. Wired into the adaptive-MRV estimator: a week is judged on the timing-adjusted cost where its sessions carry timestamps and on the untouched raw 1-5 rule where they don't, never on both (the cost already contains the raw value; consulting both would count one report twice). Surfaced in the post-workout prompt on BOTH clients, which now names the read and says what it is measuring, and mirrored on the way IN by the Today feeling card (see feeling-card-redesign). Also fixes the readiness side of the same blind spot: `readinessContext` reads today's check-in against the last session, so \"wrecked\" 90 minutes after training is not scored as a recovery failure. Session.feelLoggedAt is APPLIED in production (sql-pending.sql run, Jul 2026); rows from before it keep NULL and fall back to the raw 1-5 rule, so nothing is retroactively re-interpreted." },
  { id: "checkin-soreness-polarity", area: "Core", status: "shipped", title: "The check-in's soreness column stores FRESHNESS (bug fix)", detail: "BUG FIX + a guard against its whole class. The `Checkin.soreness` column, its Prisma comment and its field name all say 5 = very sore. The stored value is the OPPOSITE: the guided flow asks \"How fresh do your muscles feel?\", the one-tap face on Today writes the picked readiness level into all four metrics, and `checkinRating` averages the four as if higher were better everywhere. Every WRITER agrees that 5 means fresh; only the name disagrees. That is a trap with teeth, because a consumer reading the field by its name gets a plausible, exactly backwards answer — no crash, no type error. The volume estimator shipped with precisely that bug: an athlete reporting \"my legs feel great\" had their training ceiling lowered for it, every day, and the more honestly they checked in the worse it got. Fixed by naming the polarity ONCE in core/checkin-scales.ts (`sorenessFromCheckin` = 6 - stored, plus the inverse and a validated pass-through) and converting at the boundary in landmark-adapt instead of remembering; the Prisma comment now states the real semantics and points at the accessor. Pinned by tests that assert the conversion, the round-trip, and — the part that actually prevents a recurrence — that the accessor still agrees with every writer in the app (the flow's question key, checkinRating's averaging, and the primed-athlete-is-the-least-sore relation), so a future change to any writer breaks the test rather than silently re-inverting a model. THE LABEL NOW AGREES TOO: the metric is called FRESHNESS everywhere a human reads it — the daily card, the guided flow, the coach console, EN/PL/DE — which is the direction the question always ran (\"how fresh do your muscles feel?\") and the direction the value is stored, so the heading no longer means the opposite of the question under it. The coach console was showing the raw stored number under a \"soreness\" label, i.e. exactly backwards, and is now correct without any conversion. Only two things still say `soreness`: the Postgres column and the metric key that has to match it. Nothing translates on the wire; the copy key simply differs from the storage key, which is why `metricLabelKey` exists instead of building an i18n key from the field name. Guarded by a test that resolves the metric's own strings in all three languages and fails if any of them contains a word for soreness — scoped to this scale, since \"watch for soreness\" elsewhere in the app is a symptom being described rather than this metric being mislabelled." },
  { id: "feeling-card-redesign", area: "Web", status: "shipped", title: "Today's feeling card — reads its own answer in context", detail: "Two defects and a redesign. (1) BUG: the card's \"logged X ago\" read `weekOf` — the day the check-in COVERS, which for a back-logged day is that day's NOON — instead of `createdAt`, the moment the row was written. Back-logging yesterday produced \"logged in 4 hours\". Both clients now read createdAt, with weekOf only as the fallback for rows that predate it. (2) The card asked a question and then said nothing about what the answer was worth. A readiness tap 90 minutes after a heavy session and the same tap a day later are opposite readings — the first is the session talking and should NOT be scored as a recovery failure, the second is exactly the signal that should change the plan — and the card presented them identically. It now reads today's answer against the last completed session via core's `readinessContext`/`readinessNoteKey` (the same clock-changes-the-meaning model as feel-timing, applied on the way in) and says which reading it is looking at, in amber when the answer is low and the lag says it counts. Web and mobile carry the identical logic and copy from core, so the two cards cannot drift." },
  { id: "volume-recovery-feed", area: "Core", status: "shipped", title: "Check-in soreness, sleep and energy feed the volume estimator", detail: "Closes the loop the adaptive-MRV estimator was designed around: it read the training log but not the DAILY CHECK-IN, so the cheapest honest recovery signal in the app went unused. Both Volume screens now pass check-ins straight through (web lib/use-checkins.tsx, mobile useCheckinsQuery) as `RecoveryReport[]` — the check-in's own 1–5 scales, unreinterpreted — and they land in two different places, deliberately. (1) THE OBSERVED LAYER: soreness and energy are averaged over the days inside each 7-day window and join post-session fatigue and e1RM drift in classifying a week. A soreness spike (≥4.2) or an energy collapse (≤1.8) now marks a week OVERREACHED on its own, and no week counts as TOLERATED while energy sits under 2.5 — so a ceiling comes down when the athlete says they are buried even while the bar still says otherwise, which is the whole point of asking. (2) MEASURED PROFILE DEFAULTS (engines/landmark-context.ts): mean check-in sleep fills the profile's sleep field, and energy availability was read off the SCALE rather than the food diary — a least-squares body-mass trend over six weeks (needs 3+ weigh-ins spanning 2+ weeks; a single dehydrated morning must not decide whether someone is cutting), classified deficit / maintenance / surplus at ±0.25% of body mass per week, and left NULL when it cannot be fitted rather than defaulting to maintenance (which would silently hand back a recovery multiplier of 1). SUPERSEDED AS THE ONLY PATH (Aug 2026, see nutrition-training-join): the food LOG now leads where it can support a reading and the scale backs it up, with `nutritionBasis` saying which. The scale's argument here was always correct — it is the outcome and the diary is the estimate — but it answers late and sometimes never, and it lost its monopoly rather than its argument. Everything described above is exactly what still runs on the fallback. Every derived value is a DEFAULT, never an override: `withMeasured` layers them under whatever the athlete typed, `measuredFields` reports which ones came from data, and both screens mark those fields — green label, muted value — with a line saying where each came from and that typing over it wins. STRESS IS DELIBERATELY NOT DERIVED: the check-in asks about mood and energy, and relabelling either as 'life stress' would put a number the athlete never gave into a model that scales their training ceiling; mood and energy earn their keep in the observed layer instead, where a flat week is evidence about that week rather than a standing claim about the athlete. Also exposes `useBodyweightPoints()` on both clients (the raw dated measurements, for consumers that need the trend rather than a point lookup)." },
  { id: "athlete-height", area: "Data", status: "shipped", title: "Standing height on the body log", detail: "There was no athlete height anywhere in the schema before this — the existing `height` column is on MediaAsset and means image pixels. BodyMetric.heightCm is LIVE in production (reference/sql-body-height.sql run Jul 2026). THE ATHLETE SETS IT IN PROFILE: Profile → Private → Body & progress opens the log sheet with a HeightRow at the top (both clients), a single field that saves ON ITS OWN — height is a standing fact, not a weigh-in, so it must not have to ride along with one, and nothing else on the form has to be filled in for it to land. It is deliberately NOT one of BODY_METRIC_DEFS: that grid is things you watch move, and charting a height as a trend would be nonsense; `latestHeightCm` reads it as a fact from the newest row that CARRIES one (height is entered once and every later weigh-in leaves it blank, so the newest row almost never holds it), ignoring implausible values. Unit follows the weight preference (kg → cm, lb → in) via core units.ts heightUnitFor/displayHeight/storeHeightCm/fmtHeight, with a ft\'in readback because nobody says \'72 in\'; a value outside 120–230 cm is rejected on both the client and /api/body, since 72 typed into a cm field is a unit mix-up rather than a small athlete. The height flows on into the volume profile as a MEASURED field (measuredProfile({ heightCm }) → withMeasured/measuredFields, fed by the new useAthleteHeight() hook on the shared /api/body fetch), so the Volume \'About you\' editor shows it as measured rather than asking a second time — VOLUME_PROFILE_FIELDS marks heightCm `derivable: true` for that reason, not because anything infers a height. A save calls refreshBodyweight() so the model sees it without a reload. WHAT IT BUYS: heightCm feeds `frameAdjustedMassKg` (engines/athlete-profile.ts), which reads body mass against the frame carrying it instead of as raw kilos — the recovery multiplier docks mass above an 80 kg reference, but 95 kg at 195 cm and 95 kg at 170 cm are not the same load, and the flat rule penalised them identically. Mass is now compared to what the height predicts (a plain BMI-style expectation at 24.5 — deliberately NOT a body-composition model, since the app cannot see composition and pretending otherwise would be the same class of error as inventing a sleep score). The factor still displays the athlete\'s REAL mass rather than the adjusted figure. Surfaced in the admin Engine Room as the `landmark-frame` formula (expression + constants + the note on where height comes from). The model degrades cleanly: an athlete who never sets a height gets the raw-kg rule exactly as before, so nothing regresses." },
  { id: "two-read-feel", area: "Core", status: "shipped", title: "Asked twice about the same session — because one answer cannot say both things", detail: "\"How do you feel?\" answered in the gym and the same question answered the next morning are not two samples of one measurement. They are two different measurements, and the app needs both. THE IMMEDIATE READ is taken at the end of the session, on the finish screen, while the athlete is still next to the bar: effort here is sRPE (the input to session load, ACWR and injury risk) and spentness here is the acute disturbance at its PEAK, which is the only honest anchor for everything that follows. It cannot be recovered later — ask at 09:00 the next day and you get a memory of a feeling, filtered through a night's sleep. THE RECOVERY READ is the daily card, hours later, once the acute spike has had time to drain: this is the one that says whether the session was ABSORBED, and the only one that can move a training ceiling. core/feel-schedule.ts owns the clock and nothing else — `feelSchedule` says which read is due, when the next one opens (`msUntilNextRead`, ready for a local notification) and when asking stops being worth it. The immediate window is 3 h (past that it is no longer describing the session, so the card stops asking rather than collecting a value it would have to discount to nothing); the recovery read opens at 6 h — deliberately inside the same-day band rather than waiting for the next morning, because `expectedResidual(6) ≈ 0.59` means the fast component has largely drained AND an evening session still gets asked before bed — and closes at the 36 h next-day boundary. The immediate read is PER SESSION (two sessions are two efforts and two disturbances); the recovery read is PER DAY, anchored to the last session to finish, because you do not recover from one session at a time. Neither read invents an instrument: the first is the restored post-workout card writing Session.feel/fatigue/feelLoggedAt, the second is the daily check-in that already existed. One question, two moments, no third code path. Both clients: the finish screen and the Wrapped carry the immediate card (which now says, once, WHY there will be a second ask), and the Today card leads with what the recovery read is for when it comes due." },
  { id: "readiness-reads", area: "Core", status: "shipped", title: "The second read — asking again is not editing the answer", detail: "The daily card asked \"how ready do you feel?\" and stored the answer in ONE column, `Checkin.energy`, one row per day. So the second time an athlete opened it the only thing they could do was OVERWRITE the morning's answer — and the card, knowing that, locked its four faces the moment the day's check-in was complete. Both halves were wrong in the same way. \"Flat ninety minutes after squats\" and \"flat fourteen hours later\" are not one answer someone got wrong the first time; they are two measurements, and the SECOND is the one that should move training, while the FIRST is what makes it interpretable. The rest of the engine already knew this — feel-timing divides a report by the residual expected at its lag, feel-schedule schedules a recovery read six hours after the session, recovery-pairs measures the athlete's own clearance rate from PAIRS of reads — and all of it was starved: the store could hold one read a day, so a pair could almost never form. NOW EVERY ANSWER IS APPENDED (model CheckinRead, one row per read, with its own clock and the server's own measurement of hours-since-session), and core/readiness-reads.ts owns what that means. THE GATE (`readGate`): a new read is allowed once BOTH clocks have run out — four hours since the last read (MIN_RELOG_GAP_H, deliberately the same MIN_PAIR_GAP_H the pair model uses, because a re-read closer than that tells the maths nothing and asking for an answer that will be discarded is how a training app becomes a mood tracker) and six hours since a session the last read was taken in the shadow of (POST_SESSION_LOCK_H = the RECOVERY_DUE_H the recovery read was already scheduled at). Worked: session ends 09:00, athlete taps flat at 09:30, cadence would open at 13:30 but the session's recovery read isn't due until 15:00 — so the gate opens at 15:00 and the second answer lands where it is worth something. The day's FIRST read is never gated (an athlete logging how they feel walking out of the gym is telling the truth about that moment, and the card already names what that reading is worth), and the day is capped at four reads. THE PLACEMENT: readiness is the spentness scale read from the other end, so a read is inverted (6 − v) and handed to the SAME `feelReading` — one residual curve, one recall discount, one threshold, so the readiness card and the session card cannot disagree. THE DECISIVE READ: the day is judged on the latest read NOT taken minutes after training (falling back to the latest when every read is confounded) — latest rather than an average, because averaging \"wrecked at 09:30\" with \"good at 20:00\" describes neither moment, and because the whole point of the second read is that it SUPERSEDES the first as a statement of current state. It is what `prescribeSession` scales today's load off, and what the day's `Checkin.energy` column is set to on every write — so every existing reader (volume estimator, load factor, coach view) keeps working with no migration. THE PAIR: a day's first and last read go to the same `recoveryCurve` the session-side pairs use, at a 0.7 weight (same scale, same curve, noisier instrument), so two taps a day now measure this athlete's clearance rate where before they measured nothing. ONE DAY, ONE VOTE: `adaptLandmarks` groups reports by day before pooling and splits a day's weight across its reads — otherwise a training day logged three times would outvote a rest day logged once in every weekly mean, and training days are exactly the days that read low, so read frequency alone would have looked like overreaching. REDESIGNED TO 'ONE NUMBER' (concept 02 of design/readiness-card-concepts.html, six explorations rendered on the same day; states worked through in design/readiness-one-number-states.html). The card now LEADS with the reading that governs the day at display weight — 46px in that feeling's own tone — with its clock and lag as the stamp beside it. Consequences, each forced by a state rather than chosen for looks: the head ASKS ('How ready do you feel?') until there is an answer and then REPORTS ('Readiness'), because once the hero carries the reading the question above it is the same sentence twice; an empty day shows a light DASH, not a zero and not a middling 3, since inventing a reading is the exact failure this card exists to avoid; while the app is asking, the stamp switches from a lag to 'Since 10:00' so the number stops claiming to be current the moment a current one is wanted; the recovery ask drops its tinted panel and rides as the head's mono meta (in One Number the hero owns the top of the card, so a tinted block would be a second hero), and the day-full cap shows as '2 / 4' in the same slot; the readings record moves BEHIND A DOOR, shut by default with the count on it, which is the concept's trade — a card readable from across the room, with the day's story one tap away; and the info disclosure moves up beside the hero, where it explains the reading being shown. The sub-line keeps the one-line rule with a widened ladder: an explicit info tap wins, then what holds the faces, then what a new tap would do, then the ask on an empty day, then the day's trend. A just-tapped value is stamped with NOW rather than inheriting the previous reading's clock. SWEPT TO THE ONE-ACCENT DISCIPLINE (see design-system-unification) after the card had grown: (1) FOUR stacked grey blocks under the faces — a context note, an invitation, a gate reason and a countdown pill — collapse to ONE LINE, because only one of them is ever what the athlete needs at that moment (what holds the faces, else what a tap would do, else what the reading on record is worth); (2) the countdown moves up to the section head as mono right-side meta, which is where the Explore SectionHead standard already puts a scoping fact; (3) TWO lime-tinted surfaces were competing — the recovery ask and the follow-up trigger — so the follow-up ghosts to a hairline whenever the ask is showing: one fill at a time, and the fill belongs to the thing the app is actually asking for; (4) in the readings record the hue now appears EXACTLY ONCE — only the reading that governs the day is drawn in its feeling's colour, the earlier ones neutral, which retires the lime 'counts' label entirely. A cluster of parallel rows may not each wear their own hue, and the faces survive being drawn in ash because the expression is carried by the stroke, not the tint (ReadinessFace gained a `tone` override on both clients for exactly this). Both clients give the day's readings A PLACE OF THEIR OWN — a titled record under a hairline, one row per read carrying the FACE it was given as, the clock time it was given at, and how long after training that was, with the decisive one marked and a line naming the trend. It replaces 'Logged Flat, 5h ago', which is what a value looks like when the app can only hold one; a day holds a sequence now, and the sequence is the interesting part. A read with no session behind it says 'no session' rather than showing a lag of zero, and — when the gate is held — why it is held and when it opens, rather than a dead row of faces. AND WHEN THE GATE OPENS THE FACES GO BLANK: leaving the earlier answer lit invites the athlete to CORRECT it, which is the one thing this card is no longer for, and it is also just false — 'how are you right now' has no answer yet. The reading is not hidden, it stays on the record below with its clock, and the line under the faces says why they are empty and removes the fear that stops people answering twice: 'Answer for right now. Your earlier reading is kept, not replaced.' It also retires the flat 6h check-in cooldown, which gated the wrong thing in both directions: inside a day it was exempt (so the overwrite it should have stopped went through) and across days it blocked logging TODAY because yesterday had been back-logged an hour earlier. The API never 429s on it: inside the gate a readiness write is a CORRECTION of the read on record (same clock, new value), so the guided check-in's Edit can't fail a whole submission over one field. THE ENGINE ROOM CARRIES IT: five new formulas in the admin console's formula sheet, rendered from the code — `read-gate` (the two clocks, and that both numbers are borrowed rather than invented), `readiness-spent` (the 6 - v reflection onto the shared curve, with the worked 0.56-at-1h vs 1.21-at-14h pair), `decisive-read`, `readiness-pair` (the 0.7 discount) and `day-one-vote` — each with a drift guard that evaluates the printed formula against the live engine, so the console cannot quote a number the code no longer runs. A follow-up audit also corrected two things the first pass got wrong: (1) `readReports` stamped the day's freshness/sleep/mood with the NEWEST read's clock, which would have re-read a freshness answer given an hour after training as one given fourteen hours after it and inflated its cost ~2x for no reason but bookkeeping - every value now travels on the clock it was actually answered on, and a day with fewer than two reads is passed through exactly as the engine always saw it; (2) the readiness pair was unreachable - `pairReads` required the session's own post-workout `fatigue` answer, so an athlete who skips the finish card but taps the faces twice measured nothing. It now falls back to a read taken inside the immediate window as the pair's immediate side (at the readiness discount), while still preferring the session's own answer at full weight wherever it exists. APPLIED IN PRODUCTION: reference/sql-checkin-reads.sql was run in the Supabase SQL Editor (Jul 2026) — it created CheckinRead with its RLS policies and backfilled each existing day's stored answer as that day's first read, so the history is not blank and a day logged before the change can still form a pair with a read taken after it. prisma/migrations/20260730120000_checkin_reads is the same change for any environment reconciled onto Prisma migrations." },
  { id: "readiness-read-undo", area: "Core", status: "shipped", title: "A mis-tap is not a measurement — the readiness read can be taken back", detail: "The four faces are a one-tap target sitting in a scrolling card, so the row gets brushed on the way past — and every other property of the card conspired to make that permanent. The read is APPENDED (readiness-reads: nothing is ever overwritten, which is exactly what makes the day's record worth having), the gate shuts behind it for four hours, the follow-up sheet opens on top of it, and the reading goes on to scale the next session's load through readiness-load-adjust. An athlete who brushed \"wrecked\" walking past had no way back and was then prescribed off a reading they never gave. NOW: the read just given stays WITHDRAWABLE for READ_UNDO_MIN (10 min). core/readiness-reads.ts owns the rule — `undoableRead(reads, now)` returns the day's LAST read while it is still inside the window and null otherwise (an earlier read is a measurement the pair model has already used, not a slip; a read stamped slightly ahead of a skewed client clock still counts as just-given). Deliberately an UNDO, not a confirm step: a confirm taxes every honest tap to catch the rare wrong one and turns the card's single gesture into two, while an undo leaves the gesture alone and admits the mistake when the athlete actually notices it. Deliberately not an EDIT either — the row is deleted, not corrected, and the day's column falls back through the same `decisiveRead` the append path uses (an earlier read that had been superseded governs the day again; a day left with no read at all goes back to having none, rather than keeping the withdrawn value). Server: POST /api/checkins/undo re-checks the window on its own clock, so a device hours out — or a replayed request — cannot reach back into the record; the day's reads + the session clock now come from apps/web/lib/checkin-reads.ts so the append path and the undo path can never disagree about what a day holds. BOTH CLIENTS (today.tsx / home.tsx FeelingCard): the withdrawal takes the card's head-level control slot (the Explore SectionHead standard — small mono uppercase on the right of the title row, in chalk, since the accent is the \"go\" colour and this undoes a go), outranking the cooldown countdown while it shows; it hides itself with a single scheduled tick at the moment the window shuts rather than re-rendering every second, it reads the window off the STORED read so it survives a reload, and it shuts the follow-up sheet the tap had opened. Unit-tested in readiness-reads.test.ts." },
  { id: "measured-clearance", area: "AI", status: "shipped", title: "Your own recovery rate, measured against the population curve", detail: "10x, and it is the reason asking twice is worth the extra tap. `expectedResidual` is a POPULATION curve: how fast fatigue drains for people in general. Divide a single report by it and you get that session's cost with timing removed — useful, but it silently assumes this athlete drains at the population rate, which is exactly what the volume model exists to deny. Ask twice about the SAME session and the assumption becomes measurable: both reports are converted to a cost against the same curve, so if the athlete recovers exactly as modelled the two costs come out EQUAL. `recoveryCurve` (core/feel-timing.ts) is their ratio — below the band they drained faster than the curve, above it they are still carrying it long after the model says it should have gone. THE BAND IS NOT A FIXED ±15% ANY MORE, and the reason is the scale rather than the physiology: the athlete answers in whole buttons, so one step is 0.25 of the raw range, and dividing that by a residual that has drained to 0.37 by the next morning spreads ADJACENT REACHABLE ANSWERS about 0.8 apart in ratio terms — against a corridor 0.30 wide. The corridor then sat in the GAP between two buttons: across the immediate answers and gaps this app actually collects, most combinations had no on-track answer available at all, and the four-level readiness picker was worse than the five-level row on the finish screen. An ordinary recoverer was pushed onto fast or slow by rounding and the ceiling moved on the strength of it. `clearanceBandHalf` now widens the band to half a scale step whenever half a step is wider — exactly the condition for “the nearest reachable answer to on-curve reads as on-curve” — with the original ±15% as the floor, so short gaps and costly sessions are unchanged. Each pair carries the `bandHalf` it used so no surface implies precision the pair did not have. THIS FIXES THE LABEL, NOT THE MEAN \u2014 `recoveryIndex` averages the ratios themselves and `clearanceFactor` reads that mean, so neither moved here. The other half shipped separately: see clearance-quantisation-bias, which carries each pair\u2019s real interval through to the index and scales confidence by how much it could actually resolve. That second case is a reading no single report can produce: \"4/5 the next morning\" alone cannot tell you whether the session was brutal or the recovery was poor, and the pair can. NO PERSONAL TIME CONSTANT IS FITTED, deliberately: R(h1)/R(h2) is not monotone in tau, so a single pair admits two values and picking the one nearer the population figure would dress an assumption up as a measurement. The ratio is what the data supports. `athleteClearance` (engines/recovery-pairs.ts) matches check-ins back to sessions and DROPS any pair with a second session inside its gap — train Monday evening, again Tuesday morning, then check in, and reading that as Monday's drain would blame Monday for Tuesday's work and conclude the athlete recovers badly forever. It also refuses pairs closer than 4 h apart (the expected residual barely moves), pairs with an untimed read, and pairs whose immediate report is under 3/5 — an athlete who walked out fine has nothing to drain, and 2 in the gym against 2 the next morning divides 0.26 by 0.61 and reads as badly impaired when nothing happened to them either time. What survives is conservative by design: on a heavy week most days produce no pair at all, and below two clean pairs the index is exactly 1 at zero confidence. It then scales the RECOVERY end of the landmarks only (clearing slowly says nothing about how much work it takes to grow you), bounded to 0.85-1.12 and scaled by confidence, and is applied BEFORE the week classifier so the adaptive estimator bounds itself around a prior that already reflects measured recovery. Surfaced as a \"Measured recovery\" factor on both Volume screens, alongside the profile factors the athlete typed — and marked as measured, not claimed." },
  { id: "clearance-quantisation-bias", area: "AI", status: "shipped", title: "An answer is a bin, and the index now says so", detail: "THE OTHER HALF OF THE QUANTISATION PROBLEM. `clearanceBandHalf` (measured-clearance) fixed the LABEL — it stopped the app calling normal recovery abnormal — but it is display-only: `recoveryIndex` averages the RATIOS and `clearanceFactor` reads that mean, never the label, so the error that reaches prescribed volume survived it. WHAT THE ERROR ACTUALLY IS, measured by Monte-Carlo against this module\u2019s own model rather than argued: the bias is LAG-DEPENDENT, not directional. An athlete exactly on the population curve reads \u22120.05 at a six-hour gap and +0.08 at twenty-four; averaged over the gaps the app collects it is about zero. So the defect was never \u201cthe app scores people slow\u201d \u2014 it is that the SAME athlete reads ~13 points apart depending on what time they happened to tap, which is precisely what feel-timing.ts exists to eliminate. The four-level picker is NOT the culprit (it performs identically to a five-level row, because true spentness rarely reaches the level it cannot express); only a far finer scale helps, and it takes the worst gap error from 0.08 to ~0.03, the remaining 0.02 being the IMMEDIATE read\u2019s own quantisation. THREE CORRECTIONS WERE TRIED AND ARE ALL WORSE, written into feel-timing.ts so the next session does not rebuild them: SHRINK-TO-NULL removes the lag bias perfectly and destroys the feature (a true 1.50 reads 1.06 \u2014 the interval is wider than the effect, so shrinking always lands on 1); INVERSE-VARIANCE WEIGHTING by interval width keeps sensitivity but introduces a \u22120.05\u2026\u22120.14 bias of its own, because narrow bins are not a random subsample and weighting on width is weighting on the value; PER-PAIR CALIBRATION sounds exact and is the worst \u2014 numerator and denominator are both quantised so they compound rather than cancel, worst gap error 0.14 and the spread for one athlete across gaps goes from 0.07 to 0.31. SO THE FIX IS NOT TO CORRECT THE NUMBER, it is to stop pretending it is a point measurement. `ratioBounds` carries each pair\u2019s real interval (`lo`/`hi`/`width`), and those run 0.51 to 2.23 wide against a band of 0.30 \u2014 NO single pair can place itself inside the band it is judged against, and a width BELOW the band is always a clamp (cost saturating at MAX_COST, or the later read at \u201cfresh\u201d), never precision. The index then (1) widens its published interval to include the quantisation, so three pairs agreeing to the decimal no longer buy a near-zero band \u2014 that was the least evidence in the app producing the most confident claim on the screen \u2014 and (2) scales `confidence` by `resolution` (band \u00f7 mean interval, narrowing with \u221apairs), so coarse evidence moves a volume ceiling proportionally less. Three identical coarse pairs used to slam `clearanceFactor` into its 0.85 floor and now move it to 0.916; thirty still reach the floor. The bias remains \u2014 what it can no longer do is move training as though it were signal." },
  { id: "recovery-read-reminder", area: "Mobile", status: "shipped", title: "The second ask finally has a way to reach you", detail: "THE ONE PROMPT IN THIS APP THAT HAS TO TRAVEL, and until now it could not. The app asks about a session twice (two-read-feel). The first ask is free — the athlete is holding the phone on the finish screen. The second, \"how are you NOW\" once the acute spike has drained, is the one that actually moves training, and it had no delivery at all: the in-app bell only exists while HYBRID is already open, so everything it surfaces is something you came back for on your own, and the only push that could travel is the morning nudge fixed at 07:00 with no relationship to when you trained. Train at lunchtime and the read comes due at 18:30; the nudge arrives nineteen hours later and asks \"how are you TODAY\", after a night\u2019s sleep, on the far edge of the window. Meanwhile `msUntilNextRead` had sat in core since the schedule was written, built precisely to hand a client this delay, with nothing calling it. NOW: `recoveryReminderAt` (core/feel-schedule.ts) puts the ask at session end + RECOVERY_DUE_H, CLAMPED into waking hours (08:00\u201321:00 local, `clampToWaking`) \u2014 six hours after an evening session is 03:00, and a notification then does not get a considered answer, it gets the permission revoked along with every other thing this app might ever need to say; the read stays useful for 36 h so the morning costs the model almost nothing. `apps/mobile/lib/recovery-reminder.ts` schedules it on save, and it is a LOCAL notification on purpose: no APNs key, no device token, no server, no cron, so it does not wait on the Apple credentials that block push-notifications. ONE reminder at a time, anchored to the last session to finish \u2014 mirroring feelSchedule\u2019s one recovery prompt per day, because you do not recover from one session at a time \u2014 so a second workout REPLACES the first one\u2019s reminder instead of stacking a second question on one lock screen. Answering cancels it at both readiness write paths (the one-tap card on Today and the full check-in flow): a notification asking for something already given is how a channel earns a mute. The tap carries route /checkin through core\u2019s allow-list, so it lands mid-question rather than on Today with the athlete left to find it. Permission is never PROMPTED here \u2014 the finish screen is a bad moment to spend the one ask on, so an ungranted permission is a silent no-op and enablePush() still owns the asking. AND THE PROMISE IS FINALLY MADE: the finish screen passes `sessionEnd` to FeelPrompt, which it never did \u2014 without it the read classified as \"unknown\", so the line \"We\u2019ll ask once more in a few hours\" was suppressed on the ONE screen where the lag is guaranteed to be zero. A promised notification gets tapped; an unpromised one gets swiped. AND IT IS ANSWERED WITHOUT OPENING THE APP: the notification carries the readiness picker\u2019s four levels as iOS actions (lib/recovery-actions.ts), in the picker\u2019s own order and words, writing through the same `quickCheckinPatch` the card writes \u2014 no second instrument and no second scale. Requiring an app launch to deliver ONE tap on a four-point scale is the largest avoidable tax on whether the read happens at all, and answering in place is also what makes the pair honest: `feelReading` divides a report by the residual expected at its LAG, so an answer given at the moment of the ask has a lag the app knows exactly rather than one that drifted by however long it took to get round to opening the app. The category id is `recoveryread`, NOT the payload kind \u2014 iOS drops a category whose identifier contains \u201c:\u201d or \u201c-\u201d and it fails SILENTLY, arriving with no buttons and no error. `usePushBridge` now ignores any non-default actionIdentifier, so the body-tap navigator and the button handler never both act on one press." },
  { id: "landmark-replay", area: "AI", status: "shipped", title: "Has the ceiling estimate settled? — the adaptive MRV proves itself", detail: "The adaptive estimator produced ONE number: your recoverable ceiling, today. It was tested against synthetic weeks and it was arithmetically correct, and none of that answered the question an athlete actually has, which is whether to believe it. A single number cannot answer that; a TRAJECTORY can. engines/landmark-replay.ts re-runs the estimator at every week of the athlete's own history — same inputs, same rules, only the clock moved back, and strictly no lookahead (each point sees only the sessions and check-ins that existed then) — and the SHAPE of the answer is the evidence: SETTLED (the ceiling stopped moving several weeks ago — it has seen enough of this athlete to stop changing its mind, which is the closest thing to a validated ceiling the app can offer), CONVERGING (still moving, but inside a band you can train in, shown with the movement visible rather than as a confident single figure), UNSETTLED (jumping around — the honest response is to say the estimate is not ready, not to print it), or INSUFFICIENT. A week only counts as evidence if the log moved the ceiling OFF the profile-only answer, by either observed route: the week classifier, or the measured clearance rate. That distinction is load-bearing — an athlete who ramps into a wall and backs off stops producing qualifying weeks (there is no longer enough volume to test a ceiling) while their measured clearance is still holding the ceiling down, and counting only the classifier would report \"not enough evidence\" at the exact moment the app is most confident about them. Equally, a flat line at the prior is NEVER reported as settled. It is a diagnostic, not a second estimator: it calls the same resolver the app uses, pinned by a test asserting the replay's last point IS the number on screen, so it cannot drift from what it is auditing. Proven end to end against a twelve-week block fixture — three squat sessions a week, sets ramping from MEV past the ceiling, a top set that holds while volume is absorbed and slides when it isn't, and a daily check-in tracking the same story: the absorbed athlete's ceiling goes 19 to 22 and settles, the athlete who stops absorbing comes back down to 17 and settles there. Shown on both Volume screens as the trajectory itself (\"19 → 22 → 22 → 22\") plus the verdict, per muscle the log has actually tested." },
  { id: "verified-record", area: "Data", status: "shipped", title: "Verified Strength Record — tiers 0–2 (witness co-signing)", detail: "10x — the biggest single bet in the strategy review, tiers 0–2 now BUILT. Every strength number on the internet is self-reported and therefore worthless; there is no strength equivalent of a verified race time. core/attestation.ts declares the full six-tier ladder (0 claimed, 1 sensed, 2 witnessed, 3 recorded, 4 instrumented, 5 sanctioned) with the evidence each tier actually proves, and grades live from evidence the athlete cannot type: 0 = as logged; 1 = the session carries a matched device recording (HONEST SCOPE: the wearable corroborates the workout — time/duration/HR — not the barbell load; true rep-signature sensing arrives with silent-logging); 2 = a second account co-signed THAT lift. Storage is RecordAttestation (prisma + reference/sql-verified-record.sql, appended to sql-pending.sql as SECTION 4b) — APPLIED in production (the full bundle was run in the Supabase SQL Editor, Aug 2026), so the routes serve a real table and the panel can reach tier 2: one row per witness request per lift per session, the claim (e1RM + top load, kg) SNAPSHOTTED server-side from the session's own blocks at request time so the number the witness signs can never drift, append-only in spirit (status moves once, pending → cosigned/declined, stamped). API: /api/records/attest (GET badges + witness inbox, POST ask-by-@handle — ownership checked, lift verified against the stored blocks, block-list respected, rate-limited) and /api/records/attest/respond (witness-only, pending-only), both soft-degrading until the SQL runs. UI on BOTH clients: the session detail's PR list carries a 'Verified record' panel (tier badge per PR + ask-a-witness), and the social feed opens with the co-sign inbox ('X asks you to confirm you watched their Back Squat at 180 kg' → I watched it / Decline) — every request is also an invite, which is the growth loop. STILL TO VERIFY ON LIVE DATA (the sandbox cannot reach Supabase): one round-trip between two real accounts — ask by @handle → the other account sees the co-sign inbox on the feed → 'I watched it' → the asker's PR badge flips from Claimed/Sensed to Witnessed. Tracked as verified-record-live-check. NEXT: tier badges on share cards + the feed's PR cards, the public READ API (the institutional wedge), tier 3 video capture, and Spotter Protocol as the tier-2 bootstrap." },
  { id: "verified-record-live-check", area: "Data", status: "planned", title: "Verified Strength Record — confirm the round-trip on live data", detail: "RecordAttestation is migrated and the routes serve it, but nothing has yet exercised the loop end to end against real rows — this sandbox cannot reach Supabase, so it could not be. Two real accounts: A opens a session's PR list, asks B to witness a named lift by @handle; B sees the request at the top of their feed (the co-sign inbox — 'A asks you to confirm you watched their Back Squat at 180 kg'); B answers 'I watched it'; A's badge for that lift flips from Claimed/Sensed to WITNESSED, and the snapshotted claim (e1RM + top load) is what B actually signed. Check the refusals too, since they are the part a happy-path test never touches: asking about a lift that isn't in the session's blocks, asking someone who has blocked you, and a second account trying to answer a request addressed to someone else — all three should be refused server-side, not just hidden in the UI." },
  { id: "silent-logging", area: "Mobile", status: "planned", title: "Silent logging — sets detected, not typed", detail: "10x. Hevy's moat is that logging is FAST; a speed moat isn't beaten by being 15% faster, it's beaten by removing the activity. Watch IMU + phone (+ optional earbud motion) detects set start, rep count, tempo and rest boundaries; the athlete confirms by exception. Target: a full session in under five taps. Moat: every confirmed pre-filled set is a LABELED motion example, so rep-detection accuracy compounds daily and a competitor starting later starts at zero. Depends on watch-app." },
  { id: "equipment-identity", area: "Data", status: "planned", title: "Equipment identity — QR-anchored machines, normalized load", detail: "10x. 'Leg press 200 kg' is meaningless: it depends on lever arm, carriage tare, cable ratio and stack increment, which is why cross-gym comparison and true strength standards don't exist in any app. Give every rack/machine an identity (QR sticker or scanned photo) carrying make, model, ratio, tare and increment, and normalize logged load to force at the hands. Makes history gym-portable, makes the verified record and the MRV estimates physically correct, and is a GROUND GAME — a gym-by-gym physical asset (à la Foursquare's venue DB) that can't be copied by shipping code. The sticker is also a permanent free impression in front of every member of that gym." },
  { id: "program-efficacy", area: "Data", status: "shipped", title: "Program Efficacy Index v1 (programs ranked by measured outcome)", detail: "10x + the content engine, v1 SHIPPED. Program selection is the highest-stakes decision a lifter makes and it's made on influencer vibes because nobody has ever published: this program, run by people like you, produced this much strength. core/program-efficacy.ts (pure, tested) judges every closed 12-week enrollment window: OUTCOME = per-lift best e1RM in the window's first three weeks vs its last three, athlete-level = mean across qualifying lifts, program-level = the MEDIAN across athletes (one outlier PR can't buy a ranking); ADHERENCE = logged-days rate against the days the program prescribes, banded high/mid/low; DROPOUT = last logged session before week 8, published with the result and excluded from the median rather than double-punished. Every cohort under K_ANON (5) is suppressed, never estimated — per-lift and per-band rows independently. Served by the PUBLIC unauthenticated GET /api/efficacy (k-anonymous aggregates only, CDN-cached 1 h) and published on the PUBLIC /programs page (apps/web/app/programs) — ranked cards with n, adherence, dropout, per-lift medians, honest 'collecting evidence' shells below the k floor, and the methodology stated in full; auto-updating by construction (recomputed from the live DB, no editor, no cron). IN-APP both plan detail renderers on BOTH clients print the same one-liner ('Measured outcome: +6.2% e1RM in 12 weeks, median, n=23') via core efficacyLine — web MeasuredOutcome in aurora/plans.tsx, mobile components/measured-outcome.tsx — identical copy by construction. NEXT: endurance programs need a pace-based outcome (they honestly show as collecting), coach-authored programs join via ProgramEnrollment, training-age/sex bands once those fields have server-side coverage, claim-based planSchedule adherence, sitemap/robots + OG so the SEO asset actually gets crawled, and a nightly rollup (ModelFit pattern) when the live scan outgrows its caps." },
  { id: "spotter-protocol", area: "Core", status: "planned", title: "Spotter Protocol (the two-person primitive)", detail: "10x, cheap. Lifting is one of the few activities with a structurally necessary second person, and nobody has built software for that relationship. Two devices present → pair → the spotter's device co-signs the set, counts assisted reps separately, and captures video from the right angle; the spotter gets credit on the feed. Every use is an invite, and it's the free bootstrap for verified-record tier 2." },
  { id: "connect-feed-v2", area: "Core", status: "shipped", title: "CONNECT feed v2 — the card system (moments, not receipts)", detail: "The feed's rendering re-founded on the spec (reference/feed-spec.html + reference/feed-design.html). The old feed drew ONE shape for everything — title, prose, identical chips — so a 210 kg PR and a Tuesday accessory day looked the same, which is exactly why a lifting feed gives nobody anything to react to. Now every item carries a MOMENT (p0 interrupts / p1 leads / p2 fills / p3 seasons) and an ARCHETYPE (stat / sets / text), computed ONCE in packages/core/src/feed-card.ts and rendered by two thin twins (apps/web/components/feed-card.tsx, apps/mobile/components/feed-card.tsx) — 30+ card types collapse to a handful of layouts, so a new type is a preset, not a component. What a card now says: a PR leads with its load at fs.stat with the attestation TIER CHIP on the figure's own line (provenance proves the claim; the tick proves the person — tier 0 wears no badge, because absence is the mark and a scarlet letter taxes every beginner), plus the delta over the athlete's OWN previous best and its e1RM; a session leads with its top 2–3 sets over a device-true stat row (duration/volume/sets/HR) where every measured figure carries the watch signature and duration prefers device > session clock > per-set estimate; a lift never trained before is the beginner's record and gets the same p0 weight. Screens on both clients: co-sign inbox first (a person is waiting, and every request is an invite), For you / Following tabs (Following is strictly chronological and strictly other people — the ranked feed only earns trust while an unranked exit exists), a deliberately quiet one-line composer (the workout is the post), optimistic kudos as a filled bolt rather than a grey heart, and a caught-up marker that hands the athlete back to the bar (Go train → the logger) because the KPI is sessions started, not dwell. Headlines are an i18n KEY + the lift it names (EN/PL/DE), so core ships no sentences. On the shared scale (fs/leading/tracking from scale.ts) — the mobile design ratchet is the guard. 14 tests in feed-card.test.ts." },
  { id: "connect-feed-ranking", area: "Backend", status: "shipped", title: "CONNECT feed — ranking v1 (moments over engagement)", detail: "The For-you order, server-side and heuristic: score = M x (1 + A) x (1 + F) x E x D(t) in packages/core/src/feed-rank.ts, applied in /api/social/feed. M (moment) LEADS and is editorial — set in feed-card.ts, never learned — with evidence and rarity modulating WITHIN a class and never across (a tier badge lifts a PR because a claim is being made; the same badge on a session card would just reward owning a watch). A (affinity) counts two-way relationships only: close > friend > following, and being followed BY someone popular earns nothing; interaction history compounds it but saturates at ten. F (fit) is why a stranger can outrank a friend — your coach (live), gym/program/strength band (accepted, not yet modelled, and an absent signal means NO boost, never a guess). E (engagement) is CLAMPED to [0.8, 1.5] and v1 passes no model at all, so it is exactly 1: the clamp is the point, the term breaks ties and cannot set the agenda. D(t) decays per family — a PR half-lives in 72 h, a session in 18 h, because one global half-life is why other feeds read as stale or frantic. Guardrails: max 2 cards per author per view (a prolific week can't own a feed; the dropped cards are still in the chronological Following tab), ties break by recency so the order is deterministic, and every card from someone the viewer does NOT follow carries a `reason` ('Your coach', 'Follows you') rendered in the identity line on both clients — if we can't name the reason in a few words, the card doesn't ship. Also: feedCardView now derives from the card model when given a translator, so Today's preview strip speaks the same language as the feed (a PR reads as a PR in both). 20 tests in feed-rank.test.ts. NEXT (separate capabilities): candidate generation must move off query-time buildSocialFeed before 10^5 users; the learned E term; Inbox/Activity notification split; engagement loops (needs push-notifications); clubs + challenges." },
  { id: "connect-feed-open-post", area: "Core", status: "shipped", title: "One post per workout — the individual post screen", detail: "A workout is ONE post, it has an ADDRESS, and opening it shows the whole session. Three changes, one shape.\n\n(1) ONE POST PER WORKOUT. buildSocialFeed used to emit two items per training session: the workout card, and a separate PR card anchored on the same Session id that named only the heaviest lift and reduced the rest to '3 PRs this session'. That put the same session in the stream twice, split its kudos and its comments across two threads, and still never showed you the second and third records. Now a session emits exactly one item and the records it set are LINES ON IT (feed-card.ts FeedPrLine + `prs` on FeedDetail, built by `prLines` from newPrsInSession, heaviest first, each with its own load, e1RM, previous best and percent over the athlete's own best). The card lists the first two with their figures and their deltas (PrLines on both clients) and counts the rest; the post lists all of them. The post keeps the PR card's editorial weight rather than inheriting a Tuesday's: moment p0 when there is a record, tier 1 earned from a matched device recording, and feed-rank.ts pays the provenance multiplier + the 72 h record half-life on any post carrying `prs`. Reactions follow the merge: `pr` and `session` are ONE subject at the reaction layer (apps/web/lib/social.ts canonicalSubjectType/subjectTypeAliases/reactionKeys — writes canonicalise, reads count both), and core's parseFeedSubjectKey/feedSubjectKey fold `pr:` keys onto their session, so kudos, comments, saved bookmarks and shared links made before the merge all still land.\n\n(2) THE POST IS A SCREEN, NOT A SHEET. It used to be a bottom sheet that borrowed the row's data, which meant it could only ever open a post the feed had already loaded, and nothing could link to one. Now: web `?s=post&post=<type>:<id>` (a `post` param on the shell's deep-link addressing, components/feed-post.tsx via app-shell), mobile `/post?type=…&id=…` (core `feedPostPath`, app/post.tsx). GET /api/social/post/[type]/[id] returns the CARD and the WORKOUT in one privacy-gated read (rebuilt through buildSocialFeed over the author's own history, because detecting a record needs everything they did BEFORE the session), so a shared link works for someone who has never scrolled past it. The feed hands its already-loaded row over as `initial`, so opening from the stream paints instantly. YOUR OWN workout opens the post too — it used to detour into History's session detail, so the thing you shared was not the thing you could see; the manage surface (Wrapped, PRs, edit/archive) keeps one quiet door from the post.\n\n(3) THE WHOLE WORKOUT, WITH ITS FIGURES. core/feed-workout.ts (feedWorkoutView + feedWorkoutStats, 12 tests) is the one computation both clients render. The stat row EXTENDS the card's own sessionStats — same figures in the same order, then the ones a two-cell row never had space for: total reps, distance, pace, the device's kcal. Every exercise carries its own sets, reps, tonnage (through the same sessionVolume engine, so the parts sum to the header) and, for a run, its distance, clock and sport-aware pace. Then the records, then every set with its type badge (W / C / ↓ / n), load, reps, RPE and bar velocity, supersets still labelled A1/A2. Read through deviceTrueSession and derived from the device's own seconds (CLAUDE.md), so a post can never contradict the row it came from. The private post-workout reflection (note / mood / tags / feel) is not selected by the query at all and cannot travel. Shared pieces so the two clients can't drift: FeedActions (zone F) and the Comments thread are ONE component each per client, rendered by the row, the Saved shelf and the post alike." },
  { id: "readiness-one-object", area: "Core", status: "shipped", title: "The readiness ring is ONE object, and it is the demo", detail: "The signature object of the product is the readiness ring: the only figure in the app that accounts for itself — one run of ticks per cause, with `kept` plus every cost summing to EXACTLY 100 by law (engines/readiness-deficit.ts, largest-remainder apportionment, a minimum-arc rule so a cost too small to see is still drawn, and nothing ever allowed to fall off the edge). It was already built. It was not the demo, because there were TWO of it. THE STATE BEFORE: the real ring — arcs, straightened bar, ledger, provenance line, plus private `rolePaint` / `segPaint` / `LedgerRow` copies — lived inline inside apps/mobile/components/aurora/performance.tsx, roughly 120 lines in an 865-line file, behind an entitlement gate and a scroll, opening onto an inline chevron EXPANDER that pushed the rest of the page down. Today — the screen every athlete opens every morning — drew something else entirely: a bare 44dp gauge of the same number, right-aligned on the plan card, with no tick colours (so it accounted for nothing), no label (so nothing said what the number was), no door (so the one figure the product is built to be able to defend could not be questioned), and rendered on exactly ONE of that screen's four branches — an athlete on the date-anchored week rail or in logbook mode never saw it at all. WHAT SHIPPED: (1) aurora/readiness-ring.tsx — the object, once: `ReadinessRing` (the figure scales with the diameter, so no surface can pair a 56dp ring with a 36pt numeral), `ReadinessBar` (the runs straightened, which is what makes 'these parts sum to this number' legible without opening anything), `ReadinessLedger` (baseline 100 → each cost → the score, with the provenance line for the terms a ledger cannot carry — a positive wearable nudge and the heat credit take no arc and no row by construction), `ReadinessDayCard` (the card face: ring, one line naming the limiter, the bar, and the WHOLE card is the hit target with the ⓘ as the affordance), and the exported `segPaint`/`rolePaint`/`keptPaint`. (2) aurora/readiness-day-sheet.tsx — THE EXPLAINED DAY, replacing the expander. A sheet, not an unfold, for three reasons that are one reason: it can be opened from anywhere the ring is drawn; it gets the whole panel, so the ring is drawn at 112 where the runs actually separate rather than at the 56 a card row affords; and it has room for the two things the expander never had — the CLAMP (readiness never reads outside 35..98, so on a wrecked week the arithmetic keeps falling after the number has stopped, and the FLOOR case cannot earn a ledger row because there is nothing left to take, so it says so in a sentence) and what the number actually selects for today (the most-recovered primary movement — stated modestly, because readiness chooses the MOVEMENT and does not scale the bar; the thing that scales the bar is the athlete's own one-tap check-in, which keeps its own separate voice). (3) Today renders the card above the first cluster, under the masthead and over the TRAIN group — read the state, then read the session it selected — on every branch. (4) Performance renders the same card from the same component and opens the same sheet; its private paint helpers and ledger row are deleted. (SUPERSEDED in Aug 2026 on the PLACEMENT only \u2014 the Performance copy is gone, see readiness-performance-drop; the one-object rule and its guard stand, with Today as the single host.) (5) ENGINE FIX, found by the extraction: `readinessVerdict` did not take the heat credit while `readinessDeficit` did, so the two computed different splits of one day — the door's label came off the verdict and the ledger behind it off the deficit, so any day with a logged sauna promised 'Where the 33 went' and opened onto rows summing to 28, with 'Spent 28' printed on the bar between them. It takes `heatAdj` now (optional, defaulting to 0, so unwired callers are unchanged) and both hosts pass the one reading of the day their block already made. (6) THE RATCHET: readiness-object.test.ts fails the build if any second file reaches for `readinessRingSegments` / `readinessRingTicks` / `KEPT_ARC_ALPHA`, re-defines `segPaint` / `rolePaint` / `LedgerRow`, or renders the card without mounting the sheet it opens — mutation-tested by reintroducing a local `segPaint`, which fails it by name. New i18n in EN/PL/DE (w.home.readiness.dayLabel / sheetTitle / sheetSub / splitHead / movesHead / movesPrimary / floorNote); 4 new engine tests pinning the verdict-versus-ring agreement across the whole heat range." },
  { id: "connect-feed-live", area: "Core", status: "shipped", title: "Now training — live presence (the answer to stories)", detail: "Every social product eventually ships stories; in a lifting app they fit badly twice over — they are AUTHORED ephemera (almost nobody will film themselves between sets, so the rail stays empty) and they answer a question nobody asked. The question athletes actually ask is 'who's at the gym right now', and that isn't content, it's STATE the app already holds: a session started and not finished. packages/core/src/feed-live.ts turns that into the strip — no new schema, no authoring step, and NO extra query (the feed already loads recent sessions, and the in-progress ones sit in that same payload). Each entry carries elapsed time, the block they're currently on, and a discipline accent read from the WORK rather than the session's name. Two honesty rules make it trustworthy instead of creepy: (1) presence EXPIRES — people walk out without pressing finish, so a session stops counting as live after LIVE_WINDOW_MIN (180), and we never claim someone is training because a row was left open overnight; (2) it reaches only people the viewer already follows, with the feed's block list applied, because broadcasting 'this person is at their gym now' to strangers is a safety problem no amount of engagement pays for. This also FIXED a real bug: buildSocialFeed was posting in-progress sessions as finished cards — a workout announced before it happened. Those now go to the strip, while a session someone forgot to close still reads as a card dated when they started. Elapsed reads in minutes and rolls to hours (mm:ss is false precision on a value refreshed with the feed). Both clients render it full-bleed per the rail rule, with a pulsing LIVE dot (semantic state, the one place a dot belongs) and the SectionHead idiom; it renders NOTHING when nobody is training, because a rail advertising that your circle is inactive is worse than no rail. 14 tests in feed-live.test.ts. NOT built: 'send fuel' (a tap that taps the athlete's watch mid-set) needs push-notifications; and the strip refreshes with the feed rather than streaming." },

  // ── THE AUGUST 2026 SUBTRACTION PASS ──────────────────────────────────────
  // Sixty-two routes and seventeen admin sections served what is functionally
  // four jobs: log training, log food, see state, follow a plan. These entries
  // record what came out and why, in the order of conviction the audit set.
  { id: "run-track-fake-map", area: "Mobile", status: "retired", title: "The run tracker's route map", retiredBecause: "IT WAS THE ONE SURFACE IN THE PRODUCT THAT LIED. A 200dp card at the top of the run screen drew two coloured dots on a flat fill under the words \"Live route map\", with a note promising GPS \"in the native app build\". Nothing was tracked. The dots were at fixed offsets — the same two, every run, for every athlete — and the app it promised the real thing in was the native build. Every other honest-instrument decision in this codebase (measured flagged as measured, estimated as estimated, \"insufficient\" rather than a fabricated figure) was undermined by the first thing an athlete saw on that screen. The stopwatch, the typed distance and the pace those two produce are real and stay; the distance field's hint now says the distance is typed and that a matched watch recording outranks it, which is what device-truth.ts actually does. TO REVISIT: not by re-drawing a placeholder. A route appears when there is a route — SessionStream already carries GPS traces (see device-workout-match), so the map arrives with expo-location plus a map renderer, drawing what was recorded and nothing else.", detail: "Deleted from apps/mobile/components/aurora/run-track.tsx along with its two i18n strings in all three languages." },
  { id: "analysis-two-doors", area: "Core", status: "shipped", title: "The Analyze group is two doors, not ten", detail: "THE DIAGNOSIS: the analyze nav group carried TEN entries — statistics, performance, analytics, volume, volume-model, exercises, trends, velocity, endurance, history — that between them answered TWO questions: where do I stand right now, and how am I changing. Ten doors onto two questions is how an athlete ends up navigating a filing system instead of reading their own training. WHAT SHIPPED: (1) PERFORMANCE is state now and HISTORY is progress over time, and they are the only two entries the group lists. (2) Everything else is `promotedTo` one of them (or onto Today, where it renders): statistics → history's TREND view, trends and velocity → named doors in Performance's \"Go deeper\" card, analytics and endurance → Today. Promotion is not deletion — the route, icon, label and persona gate stay live, and `navVisibleTo` keeps honouring them. (3) `promotedTo`'s contract is widened and written down: INLINE on the host, or BEHIND A NAMED DOOR that carries its live figure. A screen reachable only by remembering a glyph does not qualify — promoting that hides it. (4) EXERCISES moves to the `train` group. It is the movement PICKER, filed under analysis only because each row also shows that movement's progress, and that one filing decision was a fifth of what made the group read as ten places. Still free for everyone. (5) The standalone VOLUME screen is deleted outright — see `volume-screen-retired`. GUARDED: nav.test.ts asserts the group is exactly [performance, history], HARD rather than as a ratchet, because the failure mode is not one bad screen, it is the eleventh analysis surface arriving because ten looked normal." },
  { id: "volume-screen-retired", area: "Mobile", status: "retired", title: "The standalone Volume screen", retiredBecause: "THE SAME SCREEN TWICE, and the app had already decided which one it wanted. `volume-detail-drawer` moved the block ramp, the prescriptions, the muscle rails and the provenance ladder INTO a drawer under the week-shape on Performance, precisely so the athlete would stop being sent to another screen to read the detail of the card in front of them. The screen that push used to lead to was left standing: identical components, identical numbers, one weight heavier, and — since the drawer landed — with nothing anywhere in the app pointing at it except the All-tools list. WHAT WENT: apps/mobile/app/volume.tsx, the `volume` nav id and route entry, and the component's `top`/`unified`/`compact` props plus the `flat` flag those props threaded through six sub-components to choose between card weight and drawer weight. There is one weight now, so the two cannot drift, because there is no longer a second one. WHAT STAYED: the volume card itself, its drawer, and the volume MODEL editor (a real screen, reached from the card's own control and its provenance sheet). TO REVISIT: only with a reason the drawer cannot serve — and then it needs a door someone can see, not a menu entry.", detail: "apps/mobile/app/volume.tsx, the `volume` nav id and route, and the component's top/unified/compact props and their `flat` plumbing are deleted." },
  { id: "readiness-performance-drop", area: "Mobile", status: "retired", title: "The readiness day card on the Performance tab", retiredBecause: "THE SAME READING TWICE, IN ONE SESSION. Readiness is the figure Today opens on \u2014 the day card sits under the masthead, above the TRAIN group, on every branch of that screen, which is exactly where a daily metric belongs. The Performance tab then drew the SAME card again from the SAME component, roughly two thirds of the way down its state card, behind an entitlement gate and a scroll, opening the SAME sheet. Two copies of one reading is not redundancy the way two renderings of one engine call are harmless: the athlete meets the number, then meets it again several hundred points of scroll later, and nothing on either says which one is the reading \u2014 so the second copy quietly makes the first look provisional. The one-object work (readiness-one-object) fixed the two IMPLEMENTATIONS; it left the two PLACEMENTS, because at the time Performance was where the ring had always lived. Today won that argument by being the screen every athlete opens every morning. WHAT WENT: the `<ReadinessDayCard>` block and its check-in nudge pill, the `<ReadinessDaySheet>` mount, and the day's reading this screen computed only to hand over \u2014 `readinessVerdict`, `readinessDeficit`, `readinessFacts`, `prescribeSession`, the heat and fuel priors and the check-in read that fed them (the whole check-ins and heat/nutrition signal fetches go with them, so the tab makes FOUR reads where it made seven \u2014 sessions, signals, macrocycle, recovery). WHAT STAYED: the readiness SERIES on the fourteen-day trajectory plot \u2014 that is a history, not the day's reading, and it is the only place the two curves can be compared. The one-ring guard (readiness-object.test.ts) is unaffected: it binds every HOST of the card to mounting its sheet, by rule rather than by name, so dropping a host is a legal move and Today remains the one that has it. TO REVISIT: only if Performance ever stops being reachable from a screen that already states readiness \u2014 and then it should be a door to Today's card, not a second one.", detail: "The readiness block, its sheet and the six memos behind it are deleted from apps/mobile/components/aurora/performance.tsx." },
  { id: "mobile-agent-console", area: "AI", status: "retired", title: "Agent HQ + AI agents — the mobile admin console", retiredBecause: "AN EXECUTIVE-AGENT OPERATIONS CONSOLE INSIDE A CONSUMER FITNESS BINARY. ~1,550 lines — the agent org chart, run history, prompt editing, approvals and delegation — compiled into the app every athlete downloads from the App Store, gated at runtime by a role check. The gate was never the issue: an agent-ops console is not a thing a fitness app should CONTAIN. It remains in full on the web operator panel (apps/web/components/admin/agent-hq.tsx, agents.tsx, agent-runs.tsx), which is a page behind an admin login and is one URL away from the same person. NOTE ON THE TWO-SIDED ADMIN RULE (CLAUDE.md): that rule is about not letting the two panels disagree on a feature both have. This is a different question — which BINARY the code ships in — and the exception is recorded at the top of apps/mobile/components/admin/sections.ts, where a web-only section must now say so and say why. TO REVISIT: if agent ops ever genuinely need to happen from a phone, that is a separate operator app, not a hidden section of the consumer one.", detail: "apps/mobile/components/admin/hq.tsx and agents.tsx deleted, with their ADMIN_SECTIONS entries and the whole AI group on that springboard. Unchanged on the web operator panel." },
  { id: "ui-template-switch", area: "Core", status: "retired", title: "The UI template registry (Classic ⇄ Aurora)", retiredBecause: "A UNION OF ONE, GATING BRANCHES THAT COULD NOT RENDER. The classic skin was deleted long ago; the registry was kept \"so the clients' template hooks keep a stable shape\". What survived was `TemplateName = \"aurora\"` and a `resolveTemplate()` that returned \"aurora\" for any input — so every `template === \"aurora\"` in the app was a constant and every else-branch was unreachable code that still had to be read, typechecked and maintained. It had accumulated: a private four-name radius vocabulary in the live logger (the app's most-used screen, whose values turned out to be the kit's own RADIUS.pill and RADIUS.field said in local words), a second icon set in the admin console, a `[data-template]` gate on a page of globals.css, a `<TemplateSync />` mounted in the root layout, a React context provider on mobile, a hook on each client, and two persisted per-device preferences that nothing could ever change. WHAT WENT: theme/templates.ts and its test, apps/mobile/lib/template.tsx, apps/web/lib/use-template.ts, apps/web/components/template-sync.tsx, and every branch they fed. The CSS rules keep `:root` as a qualifier so their specificity against the Tailwind utilities they exist to beat is unchanged. TO REVISIT: a second skin arrives with a second set of renderers and a switch a user can actually move — not with a registry standing in for one.", detail: "theme/templates.ts (+ test), the mobile TemplateProvider, the web useTemplate hook and TemplateSync, and every branch they gated — including the live logger's private radius vocabulary and the [data-template] gate in globals.css." },
  { id: "reanimated-dependency", area: "Mobile", status: "retired", title: "react-native-reanimated (+ react-native-worklets)", retiredBecause: "DECLARED, AND IMPORTED BY NOTHING. Every animation in the app runs on React Native's own `Animated`, by explicit choice recorded at both of the places that would otherwise have reached for reanimated: lib/shared-element.tsx (which hand-rolls a FLIP rather than adopt reanimated's shared transitions, precisely because its peer graph disagreed with the SDK's) and the profile highlight grid. The dependency was in package.json anyway, with `react-native-worklets` beside it as its runtime — so a native framework shipped inside the .app for no caller, and had to be moved in lockstep with the SDK at every upgrade, carrying exactly the dyld-crash risk documented in lib/expo-alignment.test.ts. Removing both took 106 packages out of the tree; the iOS bundle exports clean. TO REVISIT: only through `npx expo install`, with the whole native set, and with a caller.", detail: "Removed from apps/mobile/package.json with their DELIBERATE entries in lib/expo-alignment.test.ts; 106 packages left the tree and `expo export --platform ios` is clean." },
  { id: "hero-motion-spring", area: "Core", status: "retired", title: "The seventh spring (HERO.motion.spring)", retiredBecause: "A SPRING OUTSIDE THE GUARD'S JURISDICTION. motion.ts states the rule — every spring is a token in `springs`, and motion.test.ts holds all of them to the 450ms ceiling, \"so a spring that isn't here is a spring nothing is checking\" — and hero.ts quietly carried `{ response: .42, damping: .86 }` plus a `duration` and a `snapDuration` beside it. It spelled the second parameter `damping` where a `Spring` says `dampingFraction`, so it could not even have typed as one, and nothing read any of the three (`rise` was the only field in that object with a caller). An unchecked spring is how the selection lens shipped 40% over the same ceiling on the most-tapped control in the app, so this one was deleted rather than moved. GUARDED: motion.test.ts now scans core, mobile and web for a `response:` key outside motion.ts and fails on it.", detail: "HERO.motion keeps only `rise`, the one field with a caller. The new guard in motion.test.ts scans for a `response:` key outside motion.ts." },
  { id: "dead-reference-guard", area: "Core", status: "shipped", title: "Every repo path named in a comment has to resolve", detail: "THE DIAGNOSIS: the web client was retired in Aug 2026 and its comments were not. 116 references across 79 files pointed at 96 deleted web files — \"the twin of apps/web/…\", \"mirrors apps/web/…\", \"web parity: apps/web/…\". The dead pointer is the small cost. The large one is that every such line asserted a LIVE PARITY OBLIGATION — keep this in step with the other client — to a client that does not exist, which is how a retired decision gets quietly rebuilt by the next reader. WHAT SHIPPED: the references are rewritten (the mobile file is the standard; where the web file was genuinely the provenance, the sentence says so in the past tense), and apps/web/__tests__/dead-references.test.ts fails on any repo-shaped path in a source comment that does not resolve. It found three more on its first run that the sweep had missed. SCOPED OUT, deliberately: reference/, audit/ and capabilities.ts, which are RECORDS rather than instructions — this file's entries describe what shipped on the day it shipped, and rewriting them to hide the web client would be falsifying the log to satisfy a linter." },
  { id: "colour-system-remediation", area: "Core", status: "planned", title: "Colour system \u2014 the remediation plan from audit/12", detail: "AUDIT DONE, FIXES NOT BUILT. audit/12-color-system-audit-2026-08.md reverse-engineers every colour in the repo and measures it (WCAG + CIEDE2000 computed from the literal values). THE SYSTEM IS SOUND \u2014 one theme, ten tokens, a six-rung ALPHA ladder, a shared semantic layer and CI ratchets that already police hex literals and hand-rolled alphas. What it found is four structural gaps and six live defects. CRITICAL: (1) the stale hairline #2a2d2a \u2014 the exact value tokens.ts names as the bug \u2014 is still live in apps/web/app/error.tsx:60 and global-error.tsx:55, because the mobile crash fallback got error-boundary-palette.test.ts and its six web twins got nothing; (2) the palette fails its OWN distinctness floor \u2014 DISTINCT_ROLE_DE is 18 and lime/amber measure \u0394E 17.4, unguarded because palette.test.ts only tests danger/info/caution, so the go/caution pair (readiness bands, the load ramp, the kcal ring, every trend arrow) is the one adjacency below the bar; (3) two shipping CTAs fail AA \u2014 chalk on violet at 2.67:1 (workout.tsx:2833, the guest-save button) and #fff on red at 3.40:1 (user-recipes.tsx:541, delete); (4) onAccent is guarded against the lime fill only, and would fail on blue at 3.88. HIGH: `card` #151715 and `ink2` #141614 are \u0394E 0.3 apart \u2014 the same colour under two names, and ACard paints ink2 while seven hand-rolled surfaces paint card; four different colours mean 'this number went down' (trends amber, week-verdict red, endurance-summary red, wrapped violet) while statSubTone() sits in core with zero consumers; five placeholder inks, four of them under 3:1. MEDIUM: eleven parallel accent unions (LoadColor, FeelTone, ReadinessAccent, ActivityAccent, FeedAccent, CoachAccent, BadgeAccent, RecipeTint, notes tone, PremiumAccentPreset, team-compare) re-declare subsets of AccentKey and need four separate resolvers; ALPHA has 196 mobile sites and 0 web ones, so web tints through ten ad-hoc color-mix percentages; five scrim colours; no disabled token at all (raw opacity across seven values); repZoneColor inverts the intensity ramp it sits beside; the 19 hand-authored goal hues put 31 of 171 pairs below the project's own \u0394E floor and three below AA. THE SHAPE OF THE FIX, and the reason it is one entry rather than thirty: the primitives are RIGHT and must not change \u2014 what is missing is a semantic layer thick enough to be the only public API (the six roles resolve at 21 call sites while the raw tokens resolve at ~1280), plus the web half of the guard rails mobile already has. Ordered plan, file-by-file with priorities and a value-preserving migration (alias the duplicate, ratchet the direct imports down, land the guards RED first), is \u00a710 of the audit." },
  { id: "pantone-four-palette", area: "Core", status: "shipped", title: "PANTONE four \u2014 palette retint, and the fifth accent + rating gold retired", detail: "THE BRAND PALETTE IS NOW FOUR NAMED PANTONE COLOURS, on request: Wild Lime 13-0540 (#c3d363), Muskmelon 15-1242 (#ec935e), Fleur De Lis 20-0047 (#daa51d) and Lyons Blue 19-4340. Surfaces (ink / ink2 / card / line) and text (chalk / ash) are untouched \u2014 this is an accent retint, not a theme. WHY BLUE IS NOT ITS PANTONE VALUE, which is the one place the set had to bend: Lyons Blue #015871 measures 2.44:1 against ink, below even WCAG 1.4.11's 3:1 floor for non-text marks. It is a TCX chip specified to be read on white; on this ground a 2px chart stroke, a bar, a dot or a border drawn in it is close to invisible. `colors.blue` therefore holds what Lyons Blue RESOLVES TO here \u2014 its exact CIE-Lab hue angle lifted in L* to #2f7893, 3.63:1 on the card, which is the contrast the outgoing teal held (3.59), so every chart already tuned around that number is unchanged. accentText.blue is the same hue lifted further to #6bb4d4 (7.81, against the old 7.78). #015871 itself stays correct as a FILLED SURFACE (chalk on it is 7.21) and is deliberately NOT a token, because nothing draws one yet and a token with no consumer is how the palette grew a `card` nobody paints. THE SWAP FIXED THE PALETTE'S OWN FAILURE, which is the part worth keeping: audit/12 found lime/amber at \u0394E 17.4, under the DISTINCT_ROLE_DE floor of 18 that contrast.ts sets, and unguarded because palette.test.ts tested only danger/info/caution. Wild Lime against Fleur De Lis is 20.8; Muskmelon against Fleur De Lis 19.2; every pair clears 18. So the guard now runs over EVERY role pair rather than a chosen three, and it passes for the first time. Two more guards went in with it: every fill must clear 3:1 on ink (the rule that decides how far Lyons Blue gets lifted, and stops it drifting back down), and a rule asserting the accent set is exactly four. VIOLET IS RETIRED, and the four-colour set is what forced it. It held a steel/slate blue #8296c4 (a lavender before that) and never had one job \u2014 coach, non-premium, comment, fat, season bar, 'not up'. Lifting Lyons Blue put the two blues \u0394E 14.0 apart: two near-identical blues carrying different meanings in one legend, which is the exact failure contrast.ts exists to catch. Its ~120 sites were reassigned by what each one MEANT rather than swept to one colour: a 4th categorical slot \u2192 red (macros `fat`, quick-start, team-compare, the feed/coach rails, the android platform chip); the conditioning/other modality \u2192 amber, which is what kindStroke already said and so a consistency win rather than a substitution; a quiet secondary channel \u2192 blue (comments, coach everywhere including both admin panels, recovery, calendar assignment dots, the analytics stats); the 5th step of the ordered velocity ramp \u2192 the lighter blue tint, which an ordered ramp is allowed to do (contrast.ts on hue repeating where separation is carried by weight). GOLD IS RETIRED TOO: Fleur De Lis IS the gold, and the old #e6c34e sat \u0394E 8.6 from it \u2014 two golds nobody could tell apart, already flagged in audit/12 \u00a75.2. The coach \u2605, the elite badge and the Wrapped trophy all draw in `amber`. ONE LIVE AA FAILURE WENT WITH IT: the guest-save CTA (app/workout.tsx) was chalk on violet at 2.67:1 \u2014 audit/12 \u00a75.5 \u2014 and since violet had to go anyway it became the primary pill, lime with onAccent, 11.89:1. THE STALE COPIES FOLLOWED, because a hardcoded copy of a token that moved is how the periodization chart and the share cards end up drawing last season's palette: periodization's two macrocycles (taper/peak took the retired violet and move to Muskmelon, the sharp end of the wave), story-styles' disc glows, the email template's second chartreuse (#c6f135, which was never the brand value anyway), the liquid-glass accent glow, the web landing/legal/crash pages, and the five goal hues in plans.ts that were copies of a brand accent. Powerlifting's #8296c4 is KEPT and re-commented: it was the fifth accent, that token is retired, so it is now a goal hue in its own right like the fourteen beside it. The other fourteen goal hues are untouched \u2014 re-deriving them is a design pass, not a retint, and stays on the backlog. NOT DONE, deliberately: the 19-hue goal palette still puts 31 of 171 pairs under the \u0394E floor and three under AA (audit/12 \u00a75.8), and the wider colour-system remediation (the semantic layer, the eleven accent unions, the disabled token) is still `colour-system-remediation`. VERIFIED: core 3741 tests, mobile 219 (design-token ratchets + the crash-fallback palette guard, which caught the stale lime literal exactly as designed), web 203, both typechecks, web build, iOS export. The CTA-pill ratchet went 35 \u2192 36 with the reason written down: the guest CTA was always a hand-rolled pill, drawn in violet, and that rule only matches C.lime \u2014 removing the colour it hid behind revealed pre-existing debt rather than adding any. NOT VERIFIABLE HERE: no simulator, so the retint lands visually on the next TestFlight build." },
  { id: "feedback-colour-layer", area: "Core", status: "shipped", title: "Feedback colour \u2014 green succeeded, yellow warned, red failed", detail: "THE ASK: success messages green, warning yellow, error red. MEASURED FIRST, because two of the three turned out to be different problems. By CIE-Lab hue angle, Fleur De Lis is 83\u00b0 \u2014 already the conventional yellow, so `warning` needed a NAME and not a new colour. Wild Lime is 112\u00b0 (a yellow-green) and Muskmelon is 56\u00b0 (an orange): neither reads as succeeded or failed to anyone who has used a computer, so those two are real. WHY IT IS A LAYER APART FROM THE ACCENTS rather than a retint of them: the Pantone four are a DATA palette and the engines spend them as a continuum \u2014 readiness bands, %1RM load, RPE heat, ACWR, the conditioning wave. An outcome is not a reading on a ramp. Painting an elevated ACWR in error-red tells an athlete their training is broken when what it is, is heavy \u2014 the same argument nutrition-picker-gap already makes about going over a calorie target ('a fact about the day and not an injury'). So semantic.ts ROLE_COLOR keeps driving the DATA and theme/feedback.ts drives the FEEDBACK, and the dividing line is stated: a surface belongs to feedback if it reports WHAT JUST HAPPENED, and to ROLE_COLOR if it reports what the numbers say. Scope confirmed with the user before building. TWO NEW HEXES, NOT FOUR: success #4dbc8a (Lab 160\u00b0, 7.62:1 on the card) is the one genuinely new colour \u2014 the brand had no green at all; error #ef5b5b (Lab 28\u00b0, 5.42:1); warning reuses Fleur De Lis and info reuses the Lyons Blue text tone, because inventing a second yellow next to Fleur De Lis is exactly the near-duplicate this palette keeps deleting (see the retired rating gold, \u0394E 8.6). THE RED IS THE TIGHT ONE and the number is the argument: a red has to clear AA on a near-black card AND stay \u0394E 18 from Muskmelon, a bright orange sitting close to it on the wheel. That squeeze is real \u2014 #d94a4a is the better red and reaches only 4.32:1 here. #ef5b5b holds both ends at 5.42:1 and \u0394E 19.5. The headroom is thin ON PURPOSE, which is why the layer ships with 37 tests rather than a comment: each colour AA on card and on ink, onAccent ink at AA when filled, every feedback pair \u2265 \u0394E 18, every feedback colour against every ACCENT \u2265 \u0394E 18 (the two that ARE an accent are skipped BY VALUE, so a future fork into a lookalike yellow starts failing), and a hue-angle assertion that says green is green \u2014 because 'green' is a claim about a hue, not a vibe, and that is the assertion that would have caught the original problem. WHAT MOVED: the 20 form-validation and error messages (they were greppable as a set \u2014 every one carries accessibilityRole='alert', which is the same claim in the accessibility tree that the colour makes on screen), the toast in both voices, APill's commit state (the filled failure pill and the saved check, which had no success colour at all before), destructive confirms and every Delete action, and device sync/connection state \u2014 active/pending/failed is an outcome about a device, not a training reading. Web admin followed for run status, severity and its own Delete buttons, which also retired the off-palette #e06666 that audit/12 \u00a72.6 flagged. WHAT DELIBERATELY DID NOT MOVE: readiness, load intensity, RPE, ACWR, conditioning effort, macros, plan and goal colours \u2014 all still the Pantone four. A BANKED RATCHET: the recipe-delete button's raw '#fff' label became ON_FEEDBACK when that button moved onto the outcome colours, so the hex-literal ceiling drops 61 \u2192 60 \u2014 the shape that rule wants, a literal that turned out to have a name once the surface under it had one. VERIFIED: core 3778 (37 new), mobile 219, web 203, both typechecks, web build, iOS export. No simulator here, so it lands visually on the next TestFlight." },
  { id: "colour-remediation-critical-high", area: "Core", status: "shipped", title: "Colour remediation \u2014 every CRITICAL and HIGH item from audit/12 \u00a710", detail: "THE CRITICAL BLOCK. (1) #2a2d2a \u2014 the value theme/tokens.ts names BY NAME as the stale hairline that 'made every chart hairline draw in a different grey than every border' \u2014 was still drawing the border on apps/web/app/error.tsx and global-error.tsx. Now #242724. Worth recording why the PANTONE retint missed it: that sweep followed the ACCENT hexes and a stale neutral is not an accent. (2) The real finding was never the value though, it was that mobile had two colour guards and web had NONE, so the same copy rotted the same way for months \u2014 so apps/web/__tests__/fallback-palette.test.ts now holds the six files that spell the palette out. Two of them MUST: a crash boundary cannot depend on the stylesheet it may have just failed to load, and global-error.tsx replaces the document, <html> included. The rule is therefore not 'do not hardcode', it is 'every literal you hardcode has to be a colour the palette currently holds'. It also guards its own LIST \u2014 any other page that starts hand-copying fails \u2014 and that half fired immediately on invite/[token]/page.tsx, the local six-value micro-palette from \u00a72.6 (a chalk one step off, an ash \u0394E 9.5 away, a third hairline grey, and ink2 under the name CARD). It is a public landing, not a crash boundary, so it reads the tokens now. (3) apps/web/__tests__/design-tokens.test.ts ports the hex ratchet, pinned at 8 \u2014 my first draft said 9 and the SLACK half corrected it before it landed. It EXCLUDES the six fallback files by design: counting them here would mean one rule pushing a number down while another requires it to stay, which is how a ratchet teaches people to add exemptions. (4) The onAccent guard \u00a75.5 asked for could not just be switched on: `blue` is the darkest fill and near-black on it is 3.92, AA-large not AA. Loosening the whole rule would have licensed a blue button with unreadable ink, so it is two claims instead \u2014 core asserts AA on the three fills that carry ink, and a mobile rule asserts blue is not one of them, matched on the CONTAINER SHAPE (a blue fill at a pill/field/card/inner radius is a button; a 6dp bar, a 5dp dot and a 3dp plot point are not). THE HIGH BLOCK. \u00a75.4, the four colours for 'this number went down': core now has deltaRole() beside statSubTone \u2014 which had sat there with ZERO consumers because the tone\u2192colour half was never written \u2014 and one deltaPaint() on the mobile theme that all four surfaces call. One mapping is safe because the directions are already VALENCE-NORMALISED upstream: statSubTone's contract is that `up` means 'moved the way the athlete wanted', and paceTrend returns 'up' for a pace that DROPS. So `down` is never merely a smaller number, it is always a regression, and takes `danger` \u2014 which three of the four surfaces already drew. It resolves through ROLE_COLOR to Muskmelon and never to FEEDBACK.error: a figure that fell is a reading on the ramp, not a failed operation. \u00a75.6, the five placeholder inks: GHOST_PLACEHOLDER_ALPHA names the SUGGESTED-value tone (a ghost of what the field would hold \u2014 '0', '8', '\u2014' \u2014 deliberately below AA so it cannot read as entered data), which web already had as .ghost-ph and mobile had as an unnamed 0.533. Naming it is what made the other two readable as bugs rather than choices: C.line at 1.20:1 and a raw #3a3d34 at 1.64:1 were both doing the same job at strengths nobody could read. Mobile's 0.533 moves to web's 0.55 so the two clients state one figure. \u00a75.1, the `card` token: RETIRED. #151715 sat \u0394E 0.3 / contrast 1.01 from ink2 \u2014 the same colour under two names \u2014 and the kit never used it, since ACard paints ink2. It survived at 7 hand-rolled mobile surfaces and 13 admin ones, plus CARD_DARK and a local `const CARD` that actually held ink2. All 20 now read ink2 and render byte-identically; --color-card and the CARD export are gone. No deprecation alias: every call site is in this repo, so the ceremony would have been ceremony. AND ONE RETRACTION, which matters more than the fixes: \u00a75.8 claimed repZoneColor INVERTS the intensity ramp. IT DOES NOT, and the finding is wrong. Few reps means a heavy load, so amber at \u22646 reps is the same 'hard' that amber at 75-85% means; the two functions take arguments running in opposite directions (%1RM rises with load, reps fall as it rises) so their listings must be mirror images for the meaning to align \u2014 and they are. Verified against both functions, code left alone, and the reasoning added as a comment on repZoneColor so it is not misread a third time. The audit is corrected in all three places it made the claim. TWO RATCHETS BANKED: hex literals 60 \u2192 59 (the #3a3d34 placeholder), and the alpha-arithmetic floor 119 \u2192 107 \u2014 thirteen of those were the set-editor ghost, and they left the count for the right reason: they read a named constant now, so they are not a raw number at all. STILL OPEN (MEDIUM/LOW, on colour-system-remediation): the eleven parallel accent unions and their four resolvers, ALPHA on web (still 0 sites there, ten ad-hoc color-mix percentages), the five scrims, the disabled token, the 19 goal hues, sessionColor encoding identity, and the dead block in globals.css. VERIFIED: core 3781, mobile 220, web 212, both typechecks, web build, iOS export." },
  { id: "colour-remediation-medium-low", area: "Core", status: "shipped", title: "Colour remediation \u2014 the MEDIUM and LOW remainder of audit/12 \u00a710", detail: "\u00a75.7, ELEVEN ACCENT UNIONS \u2192 ONE. LoadColor, FeelTone, ReadinessAccent, ActivityAccent, FeedAccent, CoachAccent, BadgeAccent, RecipeTint, the mood tone in notes.ts, PremiumAccentPreset and an inline set in team-compare each declared their own subset of the same keys and NONE referenced AccentKey. They are aliases now, derived from semantic.ts \u2014 a new `BrandAccent = Exclude<AccentKey, 'ash'>` for the eight that mean 'the four accents', and `Extract<>` for the two that genuinely use fewer (a mood is never blue, a level badge climbs ash \u2192 chalk \u2192 warm). Type-only, no runtime change: the values were already identical, and the point is that the next palette edit cannot leave eight of them behind. FOUR RESOLVERS \u2192 ONE: percent-program's `loadHex` and settings' `toneColor` were copies of one lookup, now `accentColor(palette, key)` on the mobile theme. AND IT FOUND A LIVE REGRESSION I HAD SHIPPED: `pnpm --filter core test` passes without typechecking (vitest strips types via esbuild), and core's `typecheck` script existed but was not in the gate I had been running \u2014 so device-marks.test.ts still referenced `colors.violet` after the retint, invisible to every check I ran. Core typecheck is in the routine now; that is the process fix, and it is the more useful half of this finding. \u00a75.11, FIVE SCRIMS \u2192 ONE: `SCRIM` in tokens.ts (ink, not #000 \u2014 the ground has a green cast and a pure-black wash reads cold over it), plus `motion.scrimObscure` for the guided tour's heavier one, which was an ad-hoc 0.82 typed at its single call site. Web's three admin modals (#000a, #000b, rgba(0,0,0,.5)) become one `scrim()` at `motion.scrimFlat` \u2014 the number the mobile sheet already uses when its parent does not recede, which is exactly what a web modal does. \u00a75.10, ALPHA REACHES WEB: it had 196 mobile sites and ZERO on web, where fourteen hand-typed color-mix percentages (7/10/11/12/13/20/33/45) were doing the job \u2014 the same 'eight values in one band' drift ALPHA was written to end. `tint()` now takes a 0\u20131 alpha exactly as mobile's `withAlpha` does, so one tint is spelled identically on both clients: `tint(LIME, ALPHA.fill)` / `withAlpha(C.lime, ALPHA.fill)`. 16 web sites on the ladder. \u00a75.12, THE DISABLED TOKEN: there was none, so ~40 controls each typed a number across 0.3\u20130.7. Checking whether that spread encoded anything is what made this TWO tokens rather than one \u2014 it did not (`busy` appeared at both 0.5 and 0.6, and so did `disabled`), but the distinction underneath is real: a DISABLED control is unavailable, a BUSY one is alive and coming back, and drawing them the same tells the athlete a spinner is a dead end. `STATE_OPACITY.disabled` / `.busy`, keyed on the CONDITION's meaning rather than on the number it happened to carry, at the values that already dominated each meaning \u2014 so most sites do not move and none moves by more than 0.1. 38 files. \u00a75.9, PREMIUM AND CAUTION SHARE amber: NOT split, and the reasoning is now in semantic.ts. A fifth accent bought only for commerce would have to clear \u0394E 18 against all four, and the palette is four colours by decision \u2014 a colour that exists so one CTA can be a different yellow is decoration. Where it bites is named (the nutrition hub) and the separation there is carried by FORM: a caution is type on the card, an upgrade is a filled pill. LOW: AuroraIcon's `color` is REQUIRED now \u2014 it defaulted to '#000', 1.08:1 on ink, in a file whose own header argues that a default colour is precisely the bug that shipped white-on-near-black icons (latent, since all 105 callers passed one; a default nobody hits is still one the next caller can hit). ~100 lines of dead CSS deleted from globals.css (--premium-accent*, --accent-text, --back-surface, --back-shadow, --cover-bleed, .aurora-navglass, .start-glow, .ghost-ph, .skip-link) \u2014 the ::view-transition block is LEFT, deliberately: it is motion, not colour, and out of a colour pass's scope. theme/index.ts no longer cites a templates.ts that does not exist or a light theme that was deleted. AND A SECOND NON-FIX, after repZoneColor: \u00a710 L6 listed mobile's cardShadow (0.18) against web's --shadow-card (0.55) as a drift to reconcile. It is not one \u2014 CSS has a SPREAD term the RN shadow model has no equivalent for, and the web value's -12px pulls the shadow in so hard it needs the higher opacity to read at all. Matching the two numbers would make the platforms look LESS alike. Documented on both sides instead. STILL OPEN: the 19 goal hues (a design pass, scoped out by the user), and sessionColor encoding identity by ordinal. VERIFIED: core 3781, mobile 220, web 212, all THREE typechecks, web build, iOS export." },
  { id: "palette-mirror-guard", area: "Web", status: "shipped", title: "The core\u2194globals.css palette mirror, diffed \u2014 and sessionColor retired", detail: "THE GUARD palette.ts SAID OUT LOUD IT DID NOT HAVE. Its header read: 'MIRRORED by apps/web/app/globals.css. Keep the two in lockstep \u2014 the contrast test guards the ratios, but it can only see THIS file.' That last clause was an admission and had been true for as long as both files existed: CSS cannot import a TypeScript object, so every palette value lives twice and the second copy was checked by nobody. It is the #2a2d2a failure one level up \u2014 fallback-palette.test.ts covers the six PAGES that copy values into components; this covers the STYLESHEET, which is the bigger copy and the one every admin screen reads. The precedent was already here (motion-tokens, screen-gutter and drawer-measure all diff globals.css against core); colour was the axis that never got one, which is why it is the axis that drifted. apps/web/__tests__/palette-mirror.test.ts checks BOTH directions \u2014 18 vars must equal their core value, no --color-* may outlive the token it mirrors (exactly what --color-violet and --color-gold would have done), every core token must BE mirrored so a new accent cannot be web-invisible, and --glass-glow-rgb is derived from the accent and compared, because a hand-converted rgb triple is the most rottable form a colour can take: it does not even look like the value it copies. 21 tests. IT CAUGHT SOMETHING ON ITS FIRST RUN, and the finding was the SCANNER: it matched --color-gold inside the very comment explaining that token was retired, because prose that puts a colon after a name reads as a declaration. Comment-blind now, which is the discipline the mobile ratchets already use. SESSIONCOLOR IS RETIRED, the last of \u00a75.8. It gave AM/MID/PM fixed hues and cycled untimed sessions by ordinal so a 'Training 1 / 2 / 3' day read as three colours \u2014 colour encoding IDENTITY, which tokens.ts says the palette no longer does. It was also the third redundant signal for one fact: the marker row already prints the session's NAME and already carries a hairline rule beneath it, so nothing was learnable from the hue that the word did not say. Its stated reason had expired too \u2014 the header argued it kept 'the Plans program table + the Today week-rail' in step across surfaces, but the web client was retired in Aug 2026 and there was exactly one consumer left. A function that exists to keep two things in step, with one of them gone, is a function keeping itself in step. The marker draws in chalk now (it is the block's name, so it reads as a heading) with its volume in ash beside it: hierarchy by weight, which is what the rest of the app does. AND \u00a710 H6 IS CLOSED AS NOT-DOING, with the reasoning written into the audit rather than left as a silent gap. It was \u00a77's headline recommendation \u2014 a full semantic token layer \u2014 and its VALUE was in the specific gaps it would close, every one of which closed under its own name instead: status\u2192feedback.ts, text.disabled\u2192STATE_OPACITY, placeholder\u2192GHOST_PLACEHOLDER_ALPHA, scrim\u2192SCRIM, borders\u2192the ALPHA rungs (now on both clients), one accent vocabulary\u2192AccentKey/BrandAccent. What remains is RENAMING ink\u2192background.primary across ~1280 sites, and for a palette of two surfaces, four accents and two text tones that is churn: an indirection that adds no decision. The audit's own \u00a77 warned against the mirror-image mistake ('do NOT introduce a 50-step primitive ramp \u2014 this product has two surfaces and one accent BY DESIGN') and the argument runs both ways. What would justify revisiting it is named: a SECOND THEME. The moment ThemeName has more than one member, `ink` stops being a value and starts being a role, and the rename pays for itself. The audit's \u00a710 plan tables now carry a Status column, so it reads as a record of what happened rather than a stale to-do list. VERIFIED: core 3780, mobile 220, web 233, all three typechecks, web build, iOS export." },
  { id: "palette-verification-pass", area: "Core", status: "shipped", title: "An outside verification pass \u2014 four corrections taken, one declined, one withdrawn claim", detail: "An independent review checked the palette against the Pantone reference libraries and recomputed every ratio. ITS ARITHMETIC IS IMPECCABLE: every \u0394E76 and every re-keyed contrast figure it predicted reproduces EXACTLY under contrast.ts (Stalactite 13.95, Wild Lime 9.20, Slate Gray 4.92, blue fill 3.04 against #26262a \u2014 all four to the digit). That earned its findings a hearing. (1) THE 633 C CLAIM IS WITHDRAWN, and it is the second time this identification has changed hands in two days. #2f7893 is not PANTONE 633 C: the chip is #007396 (ColorXS, Columbia Omni Studio, Perbang), \u0394E76 5.85 / \u0394E2000 3.28 away, and Encycolorpedia lists NO Pantone for #2f7893. It is nearer 633 U (#27829e, \u0394E76 4.69) and close to neither. AND THE REAL CHIP COULD NOT HAVE BEEN ADOPTED ANYWAY, which is the useful half: #007396 measures 3.61:1 on ink and 2.97 on the card \u2014 it FAILS the 3:1 mark floor this token exists to clear. The choice was never between two chips, it was between an accurate name and a working colour. `blue` is a DERIVED value carrying no chip, exactly as it was before the claim. The caution recorded when 633 C was first accepted \u2014 that a spot-colour hex lookup is a reverse match, not a published sRGB \u2014 was the whole story. (2) THE BLACK BEAUTY ATTRIBUTION IS NOW MARKED DISPUTED, hex kept. References put 19-3911 TCX at #26262a (Colorbook) or #27272a (Color-Name); ours is \u0394E76 2.50 / 3.19 from those. RE-KEYING WAS COSTED, NOT DISMISSED: moving the card to #26262a BREAKS TWO SHIPPED GUARDS \u2014 error text 4.73 \u2192 4.44 (under AA, a fourth derivation, would become #d96b74) and line 1.21 \u2192 1.13 (under the hairline floor, would become #343438) \u2014 and leaves the blue fill on 3.04, i.e. 0.04 over the mark floor. The two sources also disagree with each other, so 'the true chip' is not one number. The narrow honest statement is in tokens.ts: every ratio is true of #212126, which is what renders; whether #212126 IS Black Beauty is the part to doubt, and a Pantone name must never be quoted as the source of a contrast figure. (3) THE ACCENT COUNT WAS GENUINELY MIS-STATED in the spec document \u2014 'four accents' over a list containing Green C, which belongs to the FEEDBACK layer; the fourth brand accent is Lyons Blue. \u00a73 is restructured into brand accents / feedback / neutrals. The code was always right (tokens.ts counts lime/red/amber/blue); the document was not. (4) 'LAB HUE 83\u00b0 IS THE CONVENTIONAL YELLOW' IS RESTATED as a golden yellow ~9\u00b0 warm of unique yellow, which sits at 92\u00b0 in CIELAB / 90\u00b0 in CIECAM02. The measurement was always exact; the label was loose. A TPM caveat goes in beside it: 20-0047 TPM is Pantone\u0027s METALLIC SHIMMERS library, so a hex expresses the base tone only and the metallic quality does not survive translation. (5) THE LOW-VISION NOTE IS ACCEPTED as a standing constraint: at 1.21:1 the card edge is effectively invisible to low-vision users, so grouping must never be carried by the surface change alone. (6) ONE FINDING IS NOT OURS: the report calls out '633 C 9.92 on ink' as impossible. The arithmetic is right \u2014 9.92 would need a negative luminance \u2014 but the document has always said 3.92, four times, and '9.92' appears zero times. The substantive half stands and is now explicit in tokens.ts: 3.92 / 3.23 clears 1.4.11 and fails 1.4.3, so `blue` is a FILL and never normal-size type, which two guards already enforce. (7) ONE RECOMMENDATION DECLINED: merging Muskmelon\u0027s danger channel into Lava Falls\u0027 error channel. The report\u0027s count of six coloured meanings is fair; the merge is not. A READING and an OUTCOME are different statements \u2014 an elevated load is a hard week, not a failure, and painting it error-red would tell an athlete their training is broken when what it is, is heavy. That distinction is why the feedback layer exists apart from ROLE_COLOR; the two are held \u0394E 20.90 apart deliberately. What the report is right about is the labelling, and that is fixed. NO TOKEN VALUE CHANGED \u2014 comment and document only. VERIFIED: core 3785, mobile 220, web 235, three typechecks, web build, iOS export." },
  { id: "blue-is-633c", area: "Core", status: "shipped", title: "The derived blue turns out to be PANTONE 633 C \u2014 recorded, provenance unchanged", detail: "REPORTED BY THE PRODUCT OWNER: #2f7893, which this palette has always documented as 'derived from Lyons Blue', is itself PANTONE 633 C. The supplied Lab (47.16, \u221213.62, \u221221.28) is EXACT against contrast.ts, so the identification is at least self-consistent with the hex. NO VALUE CHANGED \u2014 not the token, not a guard, not a rendered pixel. This is a provenance record. WHY IT IS THE BEST EVIDENCE THE METHOD HAS: the value was derived blind, by the file's own rule (preserve the Lab hue angle, raise L* until it clears the 3:1 mark floor), and only afterwards identified. Lyons Blue sits at L* 34.35 / hue 238.24\u00b0; the token at L* 47.16 / 237.39\u00b0 \u2014 the lift moved lightness by 12.81 and hue by 0.86\u00b0. 'Pantone determines the identity, the UI determines the lightness' walked along a near-constant hue line from one named colour and stopped on another. A derivation that lands on a specified colour is a derivation that was pointed the right way. AND THE EVIDENCE CLASS IS DIFFERENT, which is written into tokens.ts rather than flattened: Black Beauty, Stalactite, Slate Gray, Wild Lime, Muskmelon and Fleur De Lis are TCX chips, which PUBLISH an sRGB value \u2014 the hex IS the specification. 633 C is a COATED SPOT colour with no single canonical sRGB (the conversion depends on rendering intent and source), so this is a REVERSE lookup from the hex rather than a value taken off a chip, and the sandbox has no network to a Pantone reference to check it independently. Recorded because it is true of the value and useful to know; not the reason the value is what it is. THE TOKEN THEREFORE KEEPS ITS DERIVED PROVENANCE, and the spec document gains a FOURTH provenance level rather than promoting blue into 'as specified': 'derived \u2014 and it landed on one', membership of exactly one. Collapsing it into the verbatim tier would claim a chip was consulted, which is not what happened. STILL OPEN: `accentText.blue` #6bb4d4 has not been checked against the Pantone books \u2014 if it also has a name, the blue family is three named colours on one hue line. VERIFIED: core 3785, web 235, typecheck. Comment-only change." },
  { id: "palette-final-spec", area: "Core", status: "shipped", title: "The palette closes \u2014 Slate Gray for `ash`, Poinsettia for the error tone", detail: "THE FINAL SPEC, adopted from the working document. Two moves, and both are the same idea: replace a value somebody eyeballed with a specified one, then measure what it cost. (1) `ash` \u2192 PANTONE 16-5804 TCX SLATE GRAY #8a9691, replacing the custom #8b8f86. IT WENT UP, which is the headline: the old grey was the palette's tightest reading at 4.86:1 on Black Beauty, and palette.ts named it as the number anything lightening the card again would have to re-check first. Slate Gray reads 6.35 on ink and 5.23 on the card, so that floor rose 0.37 and the tightest contrast in the system is no longer a colour nobody chose. WHAT IT COST, written down rather than discovered later: Slate Gray is COOLER \u2014 Lab (61.0, \u22125.3, +1.1) against (58.8, \u22123.2, +4.3), a hue swing from 127\u00b0 to 168\u00b0 \u2014 which moves it toward blue. `ash` vs `accentText.blue` was \u0394E 22.59 and is now 18.31: over the DISTINCT_ROLE_DE floor of 18, but by three tenths, and it is now THE CLOSEST PAIR IN THE WHOLE PALETTE, tighter than amber/red at 19.23. The binding constraint on this palette has therefore moved from CONTRAST to DISTANCE. Second-order: `success` vs `ash` shortened 21.89 \u2192 20.17, since a cooler grey is nearer a green; feedback.ts's note about its nearest neighbour is corrected rather than left stale. (2) `error.text` \u2192 #f0565c, DERIVED FROM PANTONE 17-1654 TCX POINSETTIA #cb3441 \u2014 the error channel is now deliberately TWO Pantones. A fill is found by its edge and its label; text is found by being legible, and one chip is not obliged to be best at both. Lava Falls stays the fill verbatim (2.11:1 as type \u2014 unreadable, and correct as a surface with chalk on it at 7.04). #f0565c holds 4.73:1 on the card, 5.74 on ink, and \u0394E 20.90 from Muskmelon. THAT LAST NUMBER IS WHY THE SOURCE MOVED: the two values it replaces managed \u0394E 19.2 (#dd5f5b) and 18.98 (#e06462), both about a point off the floor, because both were lifts of Lava Falls and Lava Falls does not have that room. Changing the SOURCE bought the headroom that lifting the same chip harder could not. THE SPLIT DID NOT FORK THE FAMILY, and this is the finding that makes it safe: Poinsettia sits at Lab hue 26.1\u00b0 and Lava Falls at 28.9\u00b0, 2.7\u00b0 apart \u2014 so the existing guard, that the error text holds the FILL's hue within 6\u00b0, passes UNCHANGED without having been the thing the value was derived from. A banner and the sentence under it are visibly the same red. A second guard now pins the text to Poinsettia's own hue within 1\u00b0 (it measures 0.02\u00b0), because the 6\u00b0 window alone would accept any red in it, including one nobody derived. THE REJECTED CANDIDATES WERE RE-MEASURED ON EQUAL FOOTING rather than at their raw values, since judging a chip before the lightness adjustment the method calls for is not a fair test: each was lifted until it hit exactly AA on the card, then measured against Muskmelon. Poinsettia 22.2, Lava Falls 20.3, Bronze Mist 19.2, Chili Oil 18.9, Spicy Orange 16.1, Desert Sun 11.3, Mandarin Orange 10.4. So Spicy Orange FAILS the \u0394E floor once lifted \u2014 a harder rejection than 'inferior to Poinsettia' \u2014 Bronze Mist passes \u0394E but sits at hue 83.8\u00b0, which is Fleur De Lis's yellow band and would read as a warning, and Alexandrite #00666c fails as a blue on its own terms at 2.89:1 on ink, under the 3:1 mark floor and worse than the lifted Lyons Blue it was meant to replace. VERIFIED: core 3785, mobile 220, web 235, all three typechecks, web build, iOS export." },
  { id: "ink2-black-beauty", area: "Core", status: "shipped", title: "The card becomes a surface \u2014 PANTONE Black Beauty, and the hairline derived from it", detail: "ON REQUEST: `ink2` \u2192 PANTONE 19-3911 TCX Black Beauty #212126, verbatim. IT FIXES A REAL DEFECT rather than merely renaming one: the card was #141614, \u0394E 2.49 and 1.07:1 from `ink` \u2014 which is not a raised surface, it is the same near-black twice, the identical fault that got `card` (#151715) deleted for sitting \u0394E 0.3 from ink2 one rung further down the same ramp. Black Beauty takes the pair to \u0394E 7.15 / 1.22:1, so a card is finally an object ON the page instead of a border drawn around part of it. `ink` DELIBERATELY DID NOT FOLLOW: Black Beauty is a cool blue-violet, Lab (12.9, +1.3, \u22123.4), and re-seating the page ground on that hue changes nothing anyone can see (`ink` is L* 3.5, where chroma is invisible) while dragging `onAccent`, `SCRIM`, every ON_FEEDBACK ink and both crash fallbacks with it \u2014 all of which merely happen to share that value. Hue reads only on the surfaces light enough to carry it. TWO THINGS BROKE DOWNSTREAM AND BOTH WERE FORCED, not chosen. (1) THE HAIRLINE. #242724 is L* 15.2 against Black Beauty's 12.9 \u2014 **1.06:1**, a card border invisible against its own card, which would have rebuilt the deleted-`card` mistake at the BORDER instead of the fill. `line` is now #2f2f36: Black Beauty's own hue walked up in L* until it restores the border-against-card contrast the app already shipped (1.204 before, 1.206 now), reading 1.47:1 on `ink`. (2) THE ERROR TONE. feedback.error.text #dd5f5b measured 4.47:1 on the new card \u2014 under AA by 0.03 \u2014 and the existing guard caught it, which is exactly why that guard is written against `t.ink2` and not against a number somebody typed. #e06462 is the SMALLEST lift that clears: 4.71:1 on card, 5.72 on ink, \u0394E 18.98 from Muskmelon, still inside the corridor at both ends (under 4.5 it fails as type; brighter and it closes on the danger accent). Second time this value has been re-derived, so the lesson is stated in feedback.ts: an error tone is a FUNCTION of the surface it is read on \u2014 change `ink2` and it is a thing to re-measure, not something that is probably fine. THE GUARD THAT DID NOT EXIST. Nothing in the suite checked that a hairline is VISIBLE against what it borders, which is precisely why the collision was invisible until measured \u2014 `line` is the most-drawn colour after the two surfaces and the only one whose entire job is a RELATIONSHIP, the kind of value a per-token test cannot catch drifting. palette.test.ts now holds `line` against both surfaces and `ink2` against `ink`, at 1.15:1. DELIBERATELY NOT the 3:1 mark threshold the accent fills take: a hairline is not a mark carrying meaning, and at 3:1 it stops being a hairline and becomes the heavy chrome this design language spent years removing. 1.15 sits just under the 1.21 actually shipped, so it fails a collision and passes a deliberate softening. THE COST, on the record: everything read on the card loses ~12% \u2014 chalk 16.83\u219214.83, ash 5.52\u21924.86, lime 11.10\u21929.78, amber 8.12\u21927.16, Muskmelon 7.71\u21926.79, blue-text 7.89\u21926.95. All clear AA; `ash` at 4.86 is the floor and is named in palette.ts as the number anything further has to re-check first. AND palette.ts STOPPED CLAIMING SOMETHING FALSE: its header called this 'a true neutral charcoal ramp' for as long as the surfaces were eyeballed, and the sentence is corrected rather than left contradicting the tokens beneath it. SEVEN HAND-COPIED PALETTES SWEPT, and the seventh was a find: apps/web/lib/email.ts had drifted twice on its own \u2014 card on the retired ink2, border on #2a2c2a (a THIRD stale grey, sibling of the #2a2d2a tokens.ts names by name), body text #e9e9e9 and a raw #ffffff heading instead of chalk. It must stay literal (an email client has no stylesheet of ours) but literal is not licence to be wrong, which is the rule fallback-palette.test.ts already states for the crash boundaries. ONE VALUE DELIBERATELY LEFT: premium-accent.ts's INK_DARK is still #141614, and a comment now says why \u2014 it is a FOREGROUND chosen for AAA on the light sand fill, not the card, and the shared value was always a coincidence. VERIFIED: core 3784, mobile 220, web 235, all three typechecks, web build, iOS export." },
  { id: "chalk-stalactite", area: "Core", status: "shipped", title: "The off-white gets a name \u2014 PANTONE Stalactite replaces the eyeballed #f3f4ef", detail: "ON REQUEST: `chalk` \u2192 PANTONE 11-4101 TCX Stalactite #f7f6f3. THE FIRST PANTONE IN THIS PALETTE THAT NEEDED NO ADAPTATION AT ALL, and that is a fact about which END of the scale it sits on rather than luck. Lyons Blue had to be lifted and Lava Falls had to be split into fill/ink/text, both because a TCX chip is specified to be read on white and this ground is near-black \u2014 a near-WHITE has the opposite problem, which is to say none. Every ratio chalk is asked for went UP: 17.61 \u2192 18.02 on `ink`, 16.45 \u2192 16.83 on `ink2`, and 6.89 \u2192 7.04 as the `ink` on the Lava Falls error fill (feedback.ts's comment updated to match, since that number is quoted there as the reason the fill is allowed to be that dark). IT IS NOT A FIFTH ACCENT and does not reopen tokens.ts's 'four is the whole set' paragraph: `chalk` is the reading surface \u2014 primary type, the wordmark, the ink on the one dark feedback fill \u2014 and carries no state, no role and no meaning. What changed is that the app's off-white stopped being a value somebody picked by eye and became a specified one. WHAT IT ACTUALLY CHANGES ON GLASS, stated because \u0394E 1.66 is the kind of move that gets dismissed as invisible and then is not: the outgoing #f3f4ef sat at Lab (96.0, \u22121.2, +2.3) \u2014 a cool, faintly GREEN off-white \u2014 in a palette whose one accent is already a yellow-green, so every white surface carried a second, weaker green nobody had chosen. Stalactite is (96.9, \u22120.2, +1.5): a hair lighter and very nearly neutral, which leaves the chartreuse as the only green on screen. Visible side by side, invisible alone, which is the size of change a text colour is allowed to make. FOURTEEN COPIES, and the interesting half is where they were: two tokens (tokens.ts, palette.ts), one CSS var, one admin SVG constant, the MOBILE crash fallback's hardcoded CHALK, and SIX web pages that spell the palette out as literals \u2014 the crash boundaries (which may not have a stylesheet when they render), the landing page and the two legal pages. Every one of those six is already held by fallback-palette.test.ts, which derives its allow-list FROM the palette rather than restating it, so the guard followed the token without being touched \u2014 which is exactly what it was written for after audit/12 \u00a75.3 found the retired #2a2d2a hairline still drawing there. palette-mirror.test.ts did the same for globals.css. AND ONE COMMENT WENT STALE IN THE SAME BREATH: the mobile hex ratchet explains why 43 of its literals are #fff/#000 by saying 'chalk is #f3f4ef, a cool off-white' \u2014 a sentence that would have been quietly wrong from this commit on, in the file whose whole job is catching exactly that. VERIFIED: core 3781, mobile 220, web 235, all three typechecks, web build, iOS export." },
  { id: "feedback-pantone-tones", area: "Core", status: "shipped", title: "Feedback \u2014 PANTONE Green C + Lava Falls, and a kind becomes three values", detail: "ON REQUEST: success \u2192 PANTONE Green C #00ab84, error \u2192 PANTONE 18-1552 TCX Lava Falls #9a2b2e. GREEN LANDED UNCHANGED \u2014 Lab hue 168\u00b0, 6.14:1 as type on the card, near-black on it at 6.64, and \u0394E \u2265 21.9 against every accent (its nearest neighbour is `ash`, not another accent). \u0394E 6.6 from the #4dbc8a it replaces, so a shift within the same jade family rather than a new position \u2014 and a colour with a name behind it instead of one derived. LAVA FALLS COULD NOT LAND VERBATIM, and the measurement is the whole story: it is 2.37:1 as type on this ground. That is worse than the Lyons Blue problem rather than the same size of problem, because an ERROR IS MOSTLY TYPE \u2014 twenty form-validation lines, a toast, a field that has gone wrong \u2014 and those are the one thing in the product that must not be hard to read. It is excellent as a SURFACE though: chalk on it is 6.89:1. So the layer changed shape: a FeedbackKind now carries `fill` / `ink` / `text`. Lava Falls is the fill verbatim; `ink` is CHALK for error and near-black for the other three, which is exactly why ink had to become a field rather than stay a convention \u2014 one shared ON_FEEDBACK constant would have put near-black on a dark brick red at 2.56:1. `text` is Lava Falls' own Lab hue angle (29\u00b0) lifted in L* to #dd5f5b. WHY #dd5f5b AND NOT BRIGHTER: the lift is squeezed from both ends \u2014 under ~4.5:1 it fails as type, over ~5.5 it closes on Muskmelon (Lab 56\u00b0), the brand's danger accent. #dd5f5b holds 5.03:1 with \u0394E 19.2 from Muskmelon. Matching the outgoing coral's 5.42 exactly was available (#e56460) and left \u0394E 18.3, 0.3 above the floor; contrast already comfortably AA is not worth spending distinctness on. THE GUARDS FOLLOWED THE SHAPE: text \u2265 AA on card and on ink, ink \u2265 AA on its OWN fill (so the dark fill's chalk and the light fills' near-black are each checked against the right ground), every text pair \u2265 \u0394E 18, every text tone vs every accent \u2265 \u0394E 18, the hue-angle assertion now reads the FILL (the specified Pantone value), and a new one asserts the error text holds the fill's hue within 6\u00b0 \u2014 the lift has to stay a lift OF Lava Falls, not a different red that happens to be legible. Fills are deliberately NOT held to the accents' 3:1-on-ink rule: an accent is a mark drawn ON the page, a feedback fill is a panel that replaces a piece of it, and a panel is found by its edge and its label. 28 call sites swept \u2014 four are genuine fills (the APill failure state, the swipe-delete action, History's delete, the recipe delete button, all of which already carried chalk labels and so were already the right shape for Lava Falls) and the rest are text. AND THE MIRROR GUARD EARNED ITSELF TWICE IN ONE CHANGE: moving success left globals.css holding #4dbc8a and palette-mirror.test.ts failed with exactly that diff; then the three-value split needed three new vars to cross into CSS, and the same test is what makes losing one impossible \u2014 for `error` that would mean an unreadable 2.37:1 banner label. VERIFIED: core 3781, mobile 220, web 235, all three typechecks, web build, iOS export." },
  { id: "day-band-engine", area: "Core", status: "shipped", title: "The day band's ladder — what the top of Today says, and how it decides", detail: "THE ENGINE ONLY; the surface is day-band-surface. packages/core/src/day-band.ts replaces the readiness DAY CARD's one job (state a diagnosis) with a decision: a seven-rung ladder, checked top to bottom, that returns a fill role, a voice, an instruction and one sentence — as i18n KEYS with slots, never sentences, because this is the line most athletes will ever read and it has to speak Polish and German too. THE LADDER, NINE RUNGS as of Aug 2026 (it shipped as seven; `done` and `race` were inserted after — see the two dated notes below): (1) no reading → draw nothing, because a fabricated score under a full-bleed field of colour is a loud way to be wrong; (2) a RACE IS TODAY → say so, and it outranks even the floor; (3) the score is on its FLOOR → deload, and it outranks the rest of the calendar, since a plan that says squat heavy on a day the arithmetic bottomed out is the exact case the band exists to catch; (4) the day is already TRAINED → stop prescribing; (5) something on TOMORROW → protect it; (6) REST, scheduled or from a 6-day streak; (7) TWO trainings due → the band stops naming and starts ORDERING; (8) ONE training → named in its own vocabulary; (9) nothing due → the prescription's freshest system. TWO RULES THAT MUST NOT DRIFT. First, COLOUR NEVER CONTRADICTS THE INSTRUCTION: the fill is readinessRole(kept) so the field and the ring agree by construction, but the two rungs that tell the athlete not to train (protect, and done once it has its answer) would put a chartreuse field over 'match tomorrow' at a readiness of 81, which says two opposite things at once — those rungs return `fill: null` (ground + hairline) and there is no nicer colour that resolves it. Second, ONLY THE CHECK-IN MAY CLAIM A LOAD PERCENTAGE: readiness chooses the movement, it does not scale the bar, so the dose clause comes from rx.readinessAdjust and nothing else in the band mentions a percentage (a test asserts exactly one does). THE ORDER RULE, which is the whole reason a hybrid athlete needs this: with a gym session and a sport on one day, a TISSUE limiter puts the sport first and the bar gives up load; a CONDITIONING limiter puts the bar first while the legs are fresh and the sport stays aerobic. With TWO SPORTS there is no bar to ease, so the most overdue leads and the second is held back. MOST ATHLETES HAVE NO PLAN, and the first cut of this ladder assumed one. rotation() reads the athlete's own log instead — days since each kind against that kind's own cadence — and the band changes VOICE when it is inferring (a scheduled day is asserted, 'Run first, lift after'; an inferred one is offered, 'Run first, then lift'), and it names the KIND rather than a distance nobody prescribed. THE CONFIDENCE FLOOR is the part that matters: inference that guesses is worse than inference that abstains, so rotation() returns nothing — sending the ladder to its last rung, which claims no session at all — on an empty log, a thin one (<6 sessions in 28 days), a STALE one (nothing for >10 days, so an athlete back off a fortnight away is never told a swim is 'due'), or when no kind has a steady cadence. Steadiness is the MEAN deviation of the gaps about the median, not the median deviation: a burst pattern (three days on, twelve off) has a median gap of one day and a median deviation of zero, so a median-of-medians would call the most irregular log in the app perfectly steady. weeklyFixture()/fixtureTomorrow() answer the half of 'what's on tomorrow' a log can actually answer — a kind landing on the same weekday in ≥3 of the last 6 weeks — and deliberately refuse to protect a day for a GYM habit, since missing a routine lift costs nothing. A one-off race cannot be derived and arrives through the DayEvent seam ('declared'), which nothing stores yet. nextDueKind() is the engine half of the one-tap 'not today' correction. bandText()/bandSay() are the ONE renderer both clients call. 70 keys × EN/PL/DE. 24 tests: every floor has a case that puts a log just under it and asserts nothing is claimed; the ladder's order; the order rule both ways plus the two-sport case; the voices; and a copy sweep over every band the ladder can produce asserting each line resolves in all three languages, leaves no slot unfilled, fits its budget (44 chars head / 130 say, with the type arithmetic written down), and never restates the instruction in the sentence under it. Also new: palette.test.ts now sweeps every score 1–98 and asserts the band's fill takes an ink from inkOn() that clears AA — accent/onAccent was guarded as a pair, but the band made amber, Lyons Blue and the danger red into surfaces carrying a 27pt headline, and nothing held those to a bar. AUG 2026 — FOUR THINGS THE LADDER GOT WRONG, all found from one screenshot in which the band read 'You have a game tomorrow. Nothing on the legs today. A walk if you want one.' above a card reading 10.2 t, 20 sets, 94 min and a trap-bar deadlift, to an athlete with no game. (1) IT ASSERTED A GUESS. Rung 3 printed the flattest declarative in the app off three Thursdays in six weeks, while rungs 5–6 carefully changed VOICE between a scheduled day and an inferred one. A fixture now hedges in the head ('Usually a game tomorrow.'), carries its own evidence out of the read (DayEvent.seen — weeks, the lookback, the weekday) and shows it in the sentence ('3 of the last 6 Thursdays, so nothing heavy on the legs today.'), and reports source 'inferred' instead of borrowing the plan's. A plan day and a declared race keep the flat voice, because they are facts. (2) NO STALENESS FLOOR. rotation() has had ROTATION_STALE_DAYS since it was written; the fixture detector, which makes a louder claim about a specific day, had none — so three Thursdays in July protected every Thursday into late August. WeeklyFixture reports daysSince and fixtureTomorrow requires it inside FIXTURE_STALE_DAYS (14): a weekly fixture may miss one week, not two. The detector reports staleness rather than filtering on it, because finding a pattern and deciding it is still live are two questions. (3) THE CORRECTION COULD NOT CORRECT. 'Not today?' called rejectKind(), which drops a kind from the ROTATION, while the fixture read consulted nothing — so the band redrew word for word and quietly suppressed that kind from today's suggestions, which nobody asked for. fixtureTomorrow() now takes the rejected kinds, and the store keeps tomorrow's answers apart from today's. (4) AND THE LADDER NEVER ASKED WHAT THE DAY ALREADY HELD. Every rung below the floor answers 'what should you do today?', which the athlete had answered in the log. NEW RUNG 3, done: doneToday() reads today's sessions, and the band stops prescribing. What it says instead is the point — a finished session has exactly one fact the app cannot measure, so it asks for it ('How did that feel?', with DayBand.ask = 'rate') and states the answer back once it has one ('Hard — and that's what today's load is built from.'). The ask takes the reading's fill because it is an action; the report goes quiet because it is not. A FACT about tomorrow (plan, declared) still outranks it; a GUESS does not. AUG 2026 — THE RACE RUNG, AND THE TWO SOURCES OF 'TOMORROW' THAT WERE MISSING. day-events.ts now holds the whole of what-is-on-a-day, and the host holds none of it: it used to read `plan ? null : fixtureTomorrow(...)`, which quietly meant an ENROLLED athlete got the protect rung from NOTHING AT ALL — the fixture was skipped because they had a plan, and the plan was never asked. THREE SOURCES, ONE ORDER: a DECLARED event (the athlete typed it — day-band-declared-events, now shipped) beats the plan's own COMPETITION day beats a detected FIXTURE, and only the fixture — the only guess of the three — honours a rejection, because 'not today?' withdraws an INFERENCE and a race is corrected by deleting it. A COMPETITION DAY IS NOT A REST DAY, which is what let this hide: a program's race day prescribes no session, so programCalendarDays() reads it as isTraining:false and the schedule handed it on as isRest:true, indistinguishable from a Sunday off — the band said 'Rest day. Day 37 of the plan, and it's there on purpose.' on the morning of a meet. ScheduledDay now carries the program day's structured `kind` (a LABEL is not a fact: 'Race day' is whatever peakLabel says, in whatever language it was authored in, and nothing can branch on it). NEW RUNG 2, race: it outranks even the FLOOR, and the floor's own doctrine is why — the floor exists to catch 'a plan that says squat heavy on a day the arithmetic bottomed out', and a heavy squat can move to Thursday. A start line cannot. The reading is not discarded, it moves into the SENTENCE, where a floored morning changes how the race is RUN rather than whether it happens. It takes the reading's fill (a race is the loudest call to act the app makes; the quiet rungs are only the ones that say don't train), and it yields to the LOG: once the race is logged the band has a report to make, not an instruction to give. AND `blocksKind` CANNOT NAME A PLAN DAY, despite its own comment claiming a plan day 'resolves through exactly the same rule'. A plan's prose run is expanded into a CONDITIONING block carrying the coach's label for the workout — 'Tempo', 'Hills', 'Easy' — so every day of the app's only 9-week 5K program resolved to `gym`, and the band told an enrolled runner a gym session was due on a tempo day. planDayKind() names a day by its blocks when they can name themselves and by the PROGRAM'S DISCIPLINE when they cannot, split on the day's FIRST block (the same idiom blocksKind holds: a brick session is named by what it opened with), so a squat day inside a running plan stays gym work and the run-then-lift day stays a run. The endurance→running line in that map is TRUE OF THIS LIBRARY — one endurance program, and it is a running plan — and is the thing that must change when a cycling or swimming program lands. 23 further tests (day-events.test.ts + the ladder), and the copy sweep now puts a label of exactly EVENT_LABEL_MAX through every head that takes one in all three languages, because the arithmetic behind that budget is worth nothing unless something checks the answer. AUG 2026 — THE BAND STOPPED CHANGING ITS MIND, and it was never choosing at random. Reported as \"sometimes swim is due, sometimes bench press\"; it was a RACE, and every runner was visible. (1) A TOTAL ORDER. rotation() sorted on `b.ratio - a.ratio` alone, and Array#sort is stable — so every tie fell through to the Map's insertion order, which is the order each kind FIRST APPEARS in the 28-day window. Which of two equally-due disciplines led the band was decided by which one the athlete happened to train four weeks ago, and one backdated session flipped it. Four keys now: ratio desc, days-since desc, count desc, TRAINING_KINDS index — the last exhaustive, so no tie reaches the iterator. (2) THE PIN. A total order makes the band deterministic for a GIVEN input; it does not stop the input moving underneath it. pinRotation() promotes whatever the band already named today back to the front, so a refetch-on-focus or a late signal cannot re-rank the day while the athlete is looking at it. Two rules keep it honest: a pinned kind is promoted ONLY while it still qualifies (so the pin reorders an answer and can never invent one), and it SELF-HEALS — train the pinned kind and its ratio falls under DUE_RATIO, it leaves `due`, and the pin stops applying with nothing to clear. The band moves because the DAY moved, which is the only reason it should. (3) THE DECK (dayBandDeck). The engine never had one answer — it has a ranked list and printed the top of it, which is why an athlete watching it change read it as arbitrary. The band discloses the list without becoming a menu: page 1 IS dayBand(input), unchanged, so at rest nothing about the band moves; no deck below two candidates; NAMING RUNGS ONLY (`order`/`single`) because you may page between CANDIDATES and never between VERDICTS — swiping past a floored reading to find a nicer one is exactly what the floor exists to prevent; and three pages HARD, on MAX_DUE's own argument, because a deck otherwise invites the engine to stop making the first answer right. Each page is a whole band built by the same ladder with the pages above it rejected, so every one carries its own evidence line. Also: inkHold() joins inkOn() in contrast.ts — inkOn picks WHICH ink, inkHold picks how far it may be held back on THIS ground, by measurement. It found a live defect: the band drew its sentence at a fixed 0.78 of the fill's ink, which is 7.32:1 on Wild Lime and 3.46:1 on Lyons Blue — under AA, on the fill readinessRole returns for every score from 60 to 79, i.e. the commonest reading in the app. No alpha fixes that one (#2f7893's best ink reaches 4.60:1 at FULL strength), so the honest answer for that ground is no hold-back at all, and inkHold returns 1. palette.test.ts now sweeps the HELD-BACK lines too, not just the head." },
  { id: "week-strip-kind-colour", area: "Mobile", status: "retired", title: "The logbook week strip's bars coloured by what the day WAS", detail: "For a few hours in Aug 2026 the week strip's seven load bars took a colour from the kind of the day's LONGEST session \u2014 bar lime, engine blue, sport amber, via a `kindHue` in day-fold.ts and a `kind` field on LogbookDay. Reverted with the tinted cluster names it shipped beside (see cluster-card-washes); the bars are chalk again, today's alone in lime.", retiredBecause: "RETIRED ON REQUEST, and the argument holds without the request. THE STRIP'S JOB IS THE SHAPE OF A WEEK \u2014 seven widths, read in one glance, which is the whole reason it is seven bars and not seven rows. A second variable painted onto those same seven marks spends that reading to answer a question the strip was never asked: \"was Tuesday a swim or a lift?\" is answered in WORDS by the day's own row, one tap away, and answered better there. Today's lime is not the same kind of signal and stays: it says which day you are on, which is the one thing the strip must state without being read. WHAT WENT WITH IT, deliberately, rather than being left dead: `kindHue` existed only for these bars, and `LogbookDay.kind` only fed `kindHue` \u2014 an unused field with a tie-break and five tests behind it is how a cut feature gets rebuilt by someone who finds the data already there and assumes it was meant to be drawn. ONE THING WORTH KEEPING IF IT EVER RETURNS: the field's tie-break was a TOTAL order (longer session wins; equal minutes go to the one that started later; the id settles the rest), because \"the longest session holds the day\" left a 40-minute run against a 40-minute lift to be decided by input order, and the same week then repainted on a re-sort. Any future version of this needs that property, not just the colour. TO REVISIT: this is a question about whether the strip should carry a second variable at all, which is the owner's call \u2014 not a measurement anyone can settle from a screenshot." },
  { id: "cluster-card-washes", area: "Mobile", status: "retired", title: "The domain hue on the cluster names \u2014 and the card washes behind it", detail: "The colour direction (\"Domains\") gave each of Today's clusters its own accent: the cluster NAME tinted (Recover blue, Progress amber, Explore red) and, unshipped, its card grounds washed in the same hue at ALPHA.wash. Only the names ever shipped, and they no longer do \u2014 GroupMark is chalk on every tier and has no `hue` prop. See retiredBecause.", retiredBecause: "RETIRED ON REQUEST, together with the thing it was the follow-on to. For a few hours in Aug 2026 Today's cluster headlines were tinted by domain \u2014 Recover blue, Progress amber, Explore red \u2014 and this entry held the UNSHIPPED half of that direction: washing each cluster's card grounds in the same hue, parked as planned pending a device round. The owner asked for the names back in chalk, and the argument that they were right to: GroupMark is ONE repeated object down a long scroll, and its job is to read as the same object every time it appears. Three names in three colours are three different things that happen to share a size \u2014 the reader has to work out what each colour MEANS before the name is usable, on the tier whose entire purpose is to be skimmed. Colour in this app marks a SUBJECT (a bar's kind, an endurance lane, a domain mark); the structure that names the subjects stays neutral, which is what every other heading in the product already does. The card washes cannot outlive that: they were the louder version of the same idea, on nine objects instead of three. WHAT WOULD HAVE TO BE TRUE TO REVISIT: not a device round \u2014 the question was never whether an 8% wash is visible. It is whether Today's clusters should be told apart by colour at all, and that is the owner's call to reopen, not a measurement. The `hue` prop was DELETED from GroupMark rather than defaulted off, so no caller can quietly reintroduce it; group-mark.tsx carries the reasoning." },
  { id: "day-field-and-bar", area: "Mobile", status: "shipped", title: "The day field is the whole top of Today, and it folds into a bar", detail: "THE FIELD WAS A STRIPE OF COLOUR UNDER A BLACK HEADER, and on the two rungs that refuse a fill it was a black slab under a black header. Both are fixed by the same change. (1) THE TOP OF THE SCREEN IS ONE OBJECT. The app row (avatar, wordmark, streak, bell), the hub pills, the date and the title now render INSIDE the field, on the day's ground, so the colour of the day is the first thing on screen rather than a band beneath the chrome. The field paints under the status bar by taking the inset back from the shell for itself — the shell still owns it for Performance and Feed, so only Dashboard changes. AppHeader, TodayTabs and HubMasthead each learned a `ground` prop: the COLOUR THEY ARE STANDING ON, never an ink, because the ink is inkOn's answer and passing it would let a caller hand a shared row a tone the palette never measured against that fill. The three hub tabs still render the same components from the same numbers. (2) THE QUIET RUNGS STOP DRAWING AS THE PAGE. A reporting rung washes the hue of its OWN SUBJECT at ALPHA.solid — amber for a calendar fact, blue for recovery, lime for a day already trained (`bandHue` in day-fold.ts) — which is a surface rather than a fill, so it still refuses to read as the call to act that a filled field is. (3) IT FOLDS. `foldProgress` ramps 0→1 across 56–132dp of scroll; the chrome leaves first and travels furthest, the instruction last, so the day's answer outlives the question. The field COLLAPSES as it fades, via a negative bottom margin scaled by the same progress — fading the rows while the box kept its height left a dead slab of colour where the field used to be. Margin rather than height so the field never reflows, and monotonic in the progress so the shrinking content cannot feed back into the scroll that produced it. (4) AND HANDS OVER TO A BAR. AuroraDayBar is the app header's own row compressed into a floating capsule: avatar, wordmark, streak, bell, and nothing else. It arrives on `springs.sheet` from above the screen edge — real travel, with opacity finishing well before the movement does, because an earlier draft animated insets and corner radius at the final position and read as no transition at all. `barLatched` takes its own current state back as an argument: barIn 132 / barOut 96, two numbers rather than one, so a finger resting mid-fold cannot strobe it. WHAT THE BAR DELIBERATELY DOES NOT CARRY, both tried and both removed: a copy of the readiness score (a reading the athlete had just scrolled past, in a slot too small to mean anything) and a scroll-progress rail (chrome reporting on chrome). What survives is the one thing readable without being looked at — which colour today is. SOLID ON EVERY RUNG, NEVER GLASS, and the reasoning is measured rather than tasteful: behind the bar is a near-black page of flat dark cards at 1.22:1, so a blur spends GPU to resolve nothing and lands as a murky solid. The field washes the hue at 16%; the bar carries the same hue at full strength, so the fold CONCENTRATES the day's colour rather than losing it. Contrast swept across every rung the ladder produces — amber 8.70, lime 11.89, red 8.25, blue 4.60 (the tightest, and the only hue where the ink flips to chalk). Design: three published simulations drove it, including a ten-material catalogue that killed the glass option on evidence. RATCHET: hex literals 48 → 47 — the unread badge wrote its count in a literal white, which the field made unreadable, so it reads the ground now. THE MASTHEAD IS HIDDEN INSIDE THE FIELD, TEMPORARILY: 'WEDNESDAY, 19 AUGUST / Today' is held back while the field is carrying the day, because the field already opens with the reading and the instruction and the tab bar already names the screen — a date plus a screen label above them pushed the one sentence that matters down the screen for no answer. One argument (`FIELD_MASTHEAD` in home.tsx), not a deletion. IT STAYS HIDDEN ON A SCRUBBED DAY TOO, and the reason is worth recording because the opposite was the obvious guess. The masthead's contract is that it NAMES THE VIEWED DAY, so hiding it off-today looked like it would leave another day's training drawn under the day's own colour with nothing saying which day it is. It does not: the rail already rings the selected cell and prints its date, and the done floor's count line reads 'N done on FRI 21 AUG' (glanceDoneOn) directly under it. The day is named twice below the field either way — the masthead was the third and quietest of the three. If either of those two ever stops naming the day, the masthead has to come back with it.", },
  { id: "day-band-surface", area: "Mobile", status: "shipped", title: "The day band on Today — the filled field that replaced the readiness card", detail: "SHIPPED. apps/mobile/components/aurora/day-band.tsx renders what core's dayBand() decides (day-band-engine) and decides nothing itself. Full-bleed by the house idiom — negative margins the width of the gutter, matching internal padding. THREE OBJECTS AND NO FOURTH: a numeral, an instruction, one sentence. What it deliberately does NOT wear is the mono STAMP above and the mono META line below that the first design had: three text objects in three faces, each smaller and quieter than the last, where the top labelled a number nobody asked to have labelled ('READINESS 64') and the bottom repeated the headline in figures. The score is a numeral for the same reason a clock has no label. THE INK IS MEASURED: inkOn(fill, [ink, chalk]) rather than the assumed onAccent, guarded across every score by palette.test.ts. The HEAD STEPS DOWN a rung (fs.display → fs.headline) past one line's worth of characters rather than wrapping to a third line or ellipsizing. The ⓘ keeps the affordance and opens the SAME readiness sheet the card opened. THE CORRECTION landed with it: on an inferred day the band offers 'Not today?', which cycles to nextDueKind() and records the rejection in apps/mobile/lib/day-band-prefs.ts — scoped to the local day, because a rejection is a fact about today and not a standing preference, and it is the only training-INTENT signal the app collects. WHAT WENT: ReadinessDayCard is deleted (see readiness-day-card-retired) and with it the bar on the card face; ReadinessBar now draws only inside readiness-day-sheet.tsx, above the ledger that names its runs, and readiness-object.test.ts has a new ratchet that fails any other file drawing it. The one-ring guard follows the object rather than the name: it binds whatever states the day's reading (<AuroraDayBand> today) to mounting the sheet. WHAT WAS DELIBERATELY NOT WIRED AT FIRST, AND WHAT BECAME OF IT — all three of these are now closed, and the record is kept because each was a considered 'no' before it was a yes. (1) The PLAN as a source of 'tomorrow' was refused because a plan day is a training day rather than an EVENT, so feeding every scheduled tomorrow into the protect rung would put 'nothing on the legs today' over an ordinary Tuesday. That was right, and the answer was never the whole plan: PlanDayKind already had `competition`, and a competition day IS an event. Only that one kind is fed (Aug 2026). (2) A DECLARED one-off race — the seam took it and nothing stored one. Now something does: day-band-declared-events, shipped. (3) The band DOES paint under the status bar now: it takes the inset back for itself (`marginTop: -insets.top`) rather than every hub screen owning one, because the field is the top of the screen and not a stripe below it — the entry read the other way round for one release after the fold landed. Design record: three artifacts — ten card redesigns, ten screen-level frames (the band is frame 06), and the live band simulator. AUG 2026 — THE BAND ENDS BECAUSE THE COLOUR ENDS. There is no border anywhere on this field now, in either state. It was a FLAT wash (ALPHA.solid over the day's hue) with a 1px rule across the bottom, and both halves were wrong: a hairline separates two surfaces that BOTH continue, and here the surface stops — at ALPHA.line over amber that rule composited to #503f12, a warm line at roughly three times the page ground's luminance run edge to edge under the tallest object on the screen, which read as a rendering fault because it was one. And a flat 16% of a saturated warm accent over near-black is MUD: L* ~15 with its chroma still attached, enough colour to be noticed and not enough lightness to be a surface. A quiet band is a RAMP now (BAND_WASH, four stops) whose last stop is fully TRANSPARENT — so it resolves to whatever the screen's own ground is rather than to a hardcoded copy of it — and because the bottom dissolves, the top can afford MORE colour than the flat wash carried (0.20 against 0.16): more of its hue and less of an edge at the same time. A filled band is a FIELD and cannot dissolve under its own instruction, so it stays solid through the content and resolves inside a FOOT (BAND_FOOT, 26dp of pad that carries the ramp and nothing else, so no line of type is ever drawn on the fading part). Same 26 either way, which is what keeps the two states one idea. AUG 2026 — ONE MATERIAL, WHICH IS THE ANSWER THE PREVIOUS THREE WERE CIRCLING. The band's seam was chased through four attempts: a 1px rule across the bottom (a hairline separates two surfaces that BOTH continue, and here one stopped); a 26dp scrim fading the accent to the page (a crease where the solid stopped, and a stripe of mud); the same eased over 46dp in OKLab (a better-shaped stripe); then a crisp edge, on the argument that a coloured field ending at an edge is not a seam. All four were finishes on a join. THE JOIN WAS THE PROBLEM. It existed because there were two materials — a bright accent slab resting on a near-black page — and no finish makes Wild Lime meet #0c0d0c invisibly, because the midpoint of that journey is a dark olive in every colour space. So the second material went. EVERY band is now a WASH of the day's hue over the page ground at BAND_WASH's single strength, ramping to a fully transparent last stop: not a panel on the screen, the top OF the screen, tinted. There is no edge to soften, no rule to draw and no state where the two differ. THE ACCENT MOVED TO THE READING. The slab was carrying a real fact — that a rung is ASKING rather than reporting — and the rule it stated (a band that tells you not to train shows no colour of action) had to survive. So it moved onto the object it is about: an acting rung lights the NUMERAL in the day's own hue at full strength, a reporting rung carries no accent anywhere. One numeral's worth of ink instead of a third of the screen, and the deference is the point. Measured across all four hues on the wash's top stop: 5.96–7.73:1, clear of AA for body text let alone a 34pt black figure. WHAT FELL OUT WITH IT: `inkOn` is gone from the band (choosing between two inks was only ever a question a bright slab could ask — type is chalk everywhere now); the Lyons Blue AA problem is gone with the fill that caused it; `fieldGround` is undefined, because the chrome no longer stands on anything bright and does not need warning; and the PANEL GOT SHORTER — every gap down one rung of the `space` scale, ~24dp off the top of the screen, because a coloured slab has to earn its height by being a colour and a tint does not. The folded day BAR stays solid, and that is the one place it should be: it is a floating capsule above the content rather than a field on the page, the two are never on screen together, and being its own material is what makes it read as floating. palette.test.ts sweeps the replacement rather than the thing it replaced — chalk at inkHold's answer on every stop of every hue's wash, and the lit numeral on the ground it sits on. AUG 2026 — THE READING ROW: A FIXED SLOT AND A STATED SCALE. The discipline MARK is the one thing on that row that changes with the rung and with the deck's page — a swim on page one, a barbell on page two, nothing at all on the rungs that name no discipline — and laid out as an ordinary flex child it took its own glyph's width each time, so the numeral beside it shifted. The day's reading, the most fixed fact on the screen, moving a few points sideways on every swipe. The slot is reserved now whether or not anything is in it (MARK.slot), and the glyph is centred inside it. AND THE NUMERAL SAYS ITS SCALE. '69' alone is a figure whose range the reader has to already know; '69/100' is a reading. The denominator is NESTED inside the figure's own Text so it takes that baseline rather than a second row's guess at one, sets in the mono face at fs.title against the figure's 34pt black, and is held back to the band's measured ink — the accent belongs to the figure, and a lit denominator would divide the one signal in two. It is NOTATION rather than copy (/100 is the same in every locale) so it is a literal, but the accessibility label is not: `w.home.band.scale` speaks it as '69 out of 100' in EN/PL/DE, because a screen reader saying 'sixty-nine slash one hundred' is not what the numeral means. AUG 2026 — THE RACE RUNG'S SURFACE: rung 2 draws like any other filled band (a numeral, an instruction, one sentence) and offers no correction, because `source` is `declared` or `plan` and 'Not today?' is gated on `inferred`. Where an event is ENTERED is the CALENDAR, not here — see day-band-declared-events. AUG 2026: the band gained the one control that asks rather than tells — on rung 3 (done) with `band.ask === 'rate'` it renders the same bare mono RATE the done floor wears (no chrome: it opens a sheet, but it is a word in a field rather than the end of a list of things), and the host opens the rating sheet on TODAY's unrated session — not the viewed day's, which the week rail may have scrubbed elsewhere. AUG 2026 — EVERY CONTROL REACHABLE. React Native's `accessible` collapses a view AND ALL of its descendants into ONE element, and the band carried it twice: on the outer field (whenever the deck held a single page) and again on each deck page. So the ⓘ, the deck's dots, RATE and 'Not today?' were all inside a grouped element and none of them could be focused — on the one screen an athlete opens every morning, every affordance was invisible to VoiceOver. The TREE does the grouping now: an inner accessible View holds exactly the head and the sentence (with 'Option 2 of 3' in front of them where a deck exists, because a swipeable band read as one undifferentiated blob is a band with a hidden ranking), each control stays a sibling button, the figure carries its own spoken form, and the discipline mark is hidden from the tree rather than announced as an unlabelled image. Six other `accessible` sites on mobile were swept and are all correct uses, and they are now a REGISTRY rather than a sweep: apps/mobile/lib/a11y-grouping.test.ts finds every `accessible` in the app and makes its file state what that view groups and why nothing inside it is a control, counting them because the trap arrives as a SECOND one in a file that already had a legitimate first. It has to be a source guard: the render gate cannot see this at all, since react-native-web maps `accessible` onto DOM attributes that do not hide a subtree, so the tree that gate asserts on is not the tree the phone builds. The band also gained its own render test (apps/mobile/test/day-band.render.test.tsx) for the part core cannot see — that page 1 carries no commit control and every other page carries exactly one, and that the commit reports the page's OWN index, which the host reads as 'reject everything ranked above this page'. 'Not today?' also became real on the protect rung: it writes a TOMORROW rejection that fixtureTomorrow() honours, so the band drops to the rung below instead of redrawing itself, and it is offered there with no rotation candidate required, because collapsing the rung IS the correction." },
  { id: "day-band-preview", area: "Web", status: "planned", title: "An operator preview of the day band — the second caller bandText() was written for", detail: "FOUND BY ITS OWN COMMENTS, Aug 2026. Four places in shipped code cited a 'band preview in the admin panel' that has never existed: day-band.ts claimed bandText() is 'shared by both clients, so a band on mobile and the same band in the admin preview cannot compose the sentence differently'; day-fold.ts claimed 'three things read this' when two do; home.tsx and day-band.tsx repeated it. The comments are corrected and the thing they described is recorded here instead, because a false comment about a shared renderer is exactly what lets a second renderer get written — dead-references.test.ts only checks repo-shaped PATHS, so a prose claim about a screen that does not exist slips through it. WHAT IT WOULD BE: an admin screen that takes a readiness score, a rotation and a plan/event state, runs dayBandDeck() and draws every page the ladder can produce, in EN/PL/DE, with the copy budgets measured live (BAND_HEAD_MAX 44 / BAND_SAY_MAX 130) and the ramp's own stops rendered. WHY IT IS WORTH BUILDING: the band is the single line most athletes will ever read from this app, it speaks three languages, its ladder has nine rungs and its deck up to three pages — and today the ONLY way to see what any of that says is to be an athlete whose data happens to produce it. Support cannot answer 'why did it tell me to rest', and a copy change is verified by a test suite and nobody's eyes. It also makes bandText()'s one-renderer claim true rather than aspirational. WEB-ONLY, and by the admin rule that has to be said and justified: this is a copy-and-engine inspector for whoever is tuning the ladder, not a capability an athlete's binary should carry — the same argument that keeps Agent HQ off the phone. Record it in apps/mobile/components/admin/sections.ts as web-only when it lands." },
  { id: "plan-day-event-kinds", area: "Core", status: "shipped", title: "A plan day that can say it is a KEY SESSION, not only a competition", detail: "SHIPPED Aug 2026. The band's protect rung took a plan's COMPETITION day and nothing else, which was the whole of what PlanDayKind could express — so a peaking block's final meet was an event and the session the rest of the week is arranged around was just `train`. PlanDayKind now carries `key`, kindLabelFor() gives it a word (a program may override with `keyLabel`), and day-events.ts protects the day before one. IT IS NOT TREATED LIKE A COMPETITION, which is what the planned entry said it would be, and the difference is the point. A race outranks even the FLOOR because a start line cannot be rescheduled; a key session plainly can, and grinding a quality session on a floored reading is the exact case the floor exists to catch. So DayEvent gained `movable`, dayEventToday() narrows to competitions only, and rung 2 refuses a movable day outright. A key session today is simply today's training, which the ladder's ordinary rungs already name through `singleLabel`. IT ALSO SPEAKS DIFFERENTLY. 'Nothing on the legs today' is the instruction before a start line and an overstatement before a tempo run — the athlete's own plan has usually put an easy day in front of it — so a movable event takes sayProtectKey ('Keep today easy. Tomorrow is the one the week is built around.'), which explains that day rather than overruling it. EN/PL/DE, through the copy sweep like every other line. TWO RULES KEEP THE RUNG FROM CONTRADICTING THE PLAN IT IS READING, and they are the same rule from both ends. On PlanDayKind: IF EVERY DAY IS KEY, NONE IS — marking is a statement of RELATIVE importance inside one program, not a difficulty rating, so a block whose every session is hard marks none and there is then no easy day to protect. In day-events.ts: a key day never protects a day that is ITSELF an event. Together they mean the band only ever says 'keep today easy' on a day the program had already made easy. MARKED IN THE LIBRARY: run-5k-beginner-9wk, Tue + Thu — quoting its own progression text, 'Tuesday is the hard interval/hills session and Thursday is the tempo session'. Positional (`keyDays` on buildRunProgram) because that is the shape the source uses; marking 18 cells individually would be the same claim written 18 times with 18 chances to disagree with the sentence above it. The Saturday LONG RUN is deliberately unmarked: the plan already makes Friday easy, so protecting it would tell the athlete something the plan has told them, and a third key day in a five-day week starts to mean 'hard' rather than 'the one the week is built around'. NOT MARKED: oly-soviet-8wk. Its prose designates a peaking WEEK ('week 4 spikes toward maximal singles'), not days, and picking days out of the %-notation — every session that reaches 100%/1 — would be a rule the program never stated. Left alone on purpose, and recorded here so the next person can disagree with the reasoning rather than rediscover the gap. The hypertrophy and conditioning programs designate nothing." },
  { id: "plan-day-test-kind", area: "Core", status: "planned", title: "A TEST day, and the rung that would make it different from a key session", detail: "DELIBERATELY LEFT OUT of plan-day-event-kinds, which shipped `key` alone. A `test` kind would behave identically to `key` everywhere the band currently looks — it would protect the day before it, in the same voice, and be refused by rung 2 for the same reason — so adding it now would be an enum member with no producer and no distinct behaviour. WHAT WOULD MAKE IT REAL is the thing `key` does not need: a test is a MEASUREMENT, and a bad one poisons the block downstream, because the number it produces goes on to scale every percentage in it. So the rung a test day wants is one that REFUSES: on a floored reading the band should say do not test today, move it, in the flat voice the deload rung uses — where a key session on the same reading is merely eased. That is a rung, a piece of copy in three languages, and an argument about how far readiness may overrule a program; it is not a kind. Also needs a producer: no program in the library designates a test day (oly-soviet-8wk names a peaking WEEK, not a session), so this arrives with whichever program has one, or with a DeclaredEvent that can say what KIND of event it is rather than only which discipline." },
  { id: "day-band-declared-events", area: "Data", status: "shipped", title: "Somewhere to declare a race — the half of 'tomorrow' no log can answer", detail: "SHIPPED Aug 2026. The band's protect rung protects the day before something that matters ('Five-a-side tomorrow. Nothing on the legs today.'), and half of that was always derivable: weeklyFixture() finds a kind landing on the same weekday in at least 3 of the last 6 weeks, which is exactly what a Thursday five-a-side is. The other half is derivable by nothing — a half marathon in six weeks leaves no trace in the log until the day it happens — so the athlete has to say so. THE STORE: a DeclaredEvent table (prisma/schema.prisma, reference/sql-declared-events.sql, appended to sql-pending as SECTION 7, plus the fresh-environment migration). `date` is the client's LOCAL day key stored verbatim, PlanDayOverride's idiom — a race is on a calendar date, not at an instant, so the server never reasons about the athlete's timezone. Owner-scoped in the API and again in RLS. THE ROUTE: /api/day-events (GET/POST/DELETE). The READ soft-degrades to [] before the table is migrated, exactly like the other reference-SQL-gated reads; the WRITE deliberately does NOT — it 503s — because unlike a plan-day skip there is no client-side cache standing behind it, and an event the athlete believes they declared and that silently went nowhere is the app losing the one thing it ever asked them for. THE SCREEN: the CALENDAR, because a race is a calendar entry and that screen already knows which day is selected — so the sheet (apps/mobile/components/aurora/declare-event.tsx) never asks for a date it was handed. THREE FIELDS AND NO FOURTH: the day, a discipline, an optional name. No time, no distance, no goal — each would be a field to fill in that changes nothing about what the band says, and the band is this feature's only consumer. The name is optional and capped at EVENT_LABEL_MAX (34), which is arithmetic rather than a round number: it is BAND_HEAD_MAX minus the longest wrapper any locale puts around the slot ('{event} tomorrow.' costs 10 in English), and the copy sweep now puts a label of exactly that length through every head that takes one in EN/PL/DE. An unnamed event falls back to its discipline's own noun, which is already useful. A name that will not fit is TRIMMED rather than rejected. A declared event is corrected by DELETING it, never by the band's 'not today?': that control withdraws an INFERENCE, and this is not one — the calendar row says 'Remove' and means it. On the month grid a declared day takes an AMBER mark, the hue bandHue already gives a calendar fact, so the same event is the same colour wherever it is met. STILL OPEN, and it is the smaller half: a plan day that can say it is a TEST or a KEY SESSION rather than only a competition. PlanDayKind carries `train | active-rest | rest | competition`, so the peak day is expressible and nothing else is." },
  { id: "readiness-day-card-retired", area: "Mobile", status: "retired", title: "The readiness DAY CARD on Today (ring, limiter sentence, straightened bar)", retiredBecause: "IT STATED A DIAGNOSIS. 'Shoulders is the limiter today' is a fact about the athlete's body; it is not a plan, and an athlete opening the app at six in the morning needs to be told what to train. The engine had already picked the day's movement and the freshest energy system — the card just never said so. Two further faults came out with it: the BAR on the card face proved a sum whose parts it could not name (the runs are unlabelled until the ledger names them, so on the face it was one number stated twice beside a strip of colour — 'Kept 64 / Spent 36'), and the only affordance on a block occupying a third of the fold was a 13px ⓘ. The bar moved into the sheet, directly above the ledger that names every run in it, and a ratchet now fails any face that draws it again. WHAT REPLACED IT: the day BAND (day-band-surface), which states what to do and refuses a fill on the days it tells you not to train. TO REVISIT: only if the band's instruction is ever found to be less useful than the diagnosis — in which case the diagnosis belongs INSIDE the band's sentence, not as a second object on the same screen. The ring itself is untouched: ReadinessRing lives on in readiness-ring.tsx and the sheet draws it at 112.", detail: "ReadinessDayCard is deleted from apps/mobile/components/aurora/readiness-ring.tsx; its file keeps a note in its place recording why. Today mounts <AuroraDayBand> where the card was, opening the same <ReadinessDaySheet>." },
  { id: "editorial-serif", area: "Mobile", status: "shipped", title: "ITC Garamond as the editorial voice — one sentence per screen", detail: "The app had two faces and both of them measure: S\u00f6hne for language, S\u00f6hne Mono for figures. Measured off the live DOM of the running app, Today draws 193 mono text nodes against 75 sans and Nutrition 75 against 34 \u2014 268 of 379 faced nodes across the two screens are the MEASURING face. What had no voice was interpretation, so the one sentence per screen that concludes something was set in whatever utility style was nearest: the week verdict's lead was `sans` at `fs.subtitle`, which its own file already warned 'reads as a caption for something else', and the nutrition nudge was `sans` at `fs.body`, the style help text uses. THIS DOES NOT ADD A SENTENCE, it re-ranks one that was already there. THE PAIRING IS MEASURED, not chosen: x-height is 0.523 em in S\u00f6hne and 0.445 em in ITC Garamond Book (read from the shipped binaries with fontTools), so the serif needs 1.176x to sit on the same x-height line \u2014 which is why `fs.editorial` (30) exists beside `fs.display` (26) as a serif-only rung rather than reusing it. x-height over cap-height is 0.728 against 0.710, near-identical, which is what makes the two faces mix at all; a true old-style Garamond at ~0.40 em would have needed 31 percent compensation and still read as an older, smaller voice. WHAT SHIPPED: `fonts.serif` + `cut.serif` + `text.editorial` in core, `fs.editorial` and a `serif` tracking role (-0.008 em \u2014 half what the S\u00f6hne-fitted bands hand back, because ITC Garamond is a phototype-era design already tightly fitted) in scale.ts, `F.serif` + PostScript map + `FACE` entry on mobile, ITCGaramondStd-Bk.ttf loaded on the same blocking pass as S\u00f6hne, and exactly TWO call sites \u2014 week-verdict.tsx's `Lead` (which also drops its bold metric run: the face is the emphasis now) and nutrition-panels.tsx's `NutritionNudgeLine` (whose status glyph and accent stay, because that signal is semantic). ONE WEIGHT, NO ITALIC: an `editorialIt` token was specified and NOT shipped, because nothing consumes it \u2014 a token with no consumer is a lie with a comment attached. RATCHETED in design-tokens.test.ts: only the two sanctioned files may reach for it, one per file, never in a during-training route, never restyled at the call site, and the resolved size never falls under 24dp at any Dynamic Type scale. 22 of the 24 ITC Garamond cuts are deliberately not bundled \u2014 all sixteen Condensed and Narrow faces measure 0.403 em average lowercase advance against Book's 0.506, which is 80 percent, which is Apple Garamond, the face Apple retired in 2002 and the one association this product cannot afford.", },
  { id: "editorial-serif-polish", area: "Mobile", status: "blocked", title: "The editorial serif in Polish and German", blockedBy: "ITC GARAMOND STD HAS NO CENTRAL EUROPEAN COVERAGE. Probed with fontTools, ITCGaramondStd-Bk.ttf carries \u0141 and \u00f3 but NOT \u0105 \u0119 \u015b \u017c \u0144 \u0107 \u017a \u2014 290 cmap entries, Latin-1 plus a handful. S\u00f6hne carries the complete Polish set. The editorial line is precisely the copy that gets localised, so every serif sentence degrades to the sans outside English, which is a strange shape for a product built by a Polish team and is the strongest single argument against the face. UNBLOCKED BY EITHER: a Pro or CE cut of ITC Garamond with full Latin Extended-A (check the licence before buying a second file), or accepting the fallback permanently and buying S\u00f6hne Buch Kursiv so the non-English rendering reads as a deliberate second voice rather than a shrug. Until then `cut.serif` documents English-only and the consumers resolve to `sans` elsewhere.", detail: "vocabulary-pl-de is already a planned capability; this is its typography half. Decide before that sweep starts, not after." },
];

export function capabilitiesByStatus(status: CapabilityStatus): Capability[] {
  return CAPABILITIES.filter((c) => c.status === status);
}
