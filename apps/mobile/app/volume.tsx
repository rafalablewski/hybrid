import AuroraVolume from "../components/aurora/volume";

/**
 * VOLUME — a destination again.
 *
 * It had been folded into the Performance page as six stacked sections, which
 * between them (with Trends) took roughly two thirds of that page's scroll.
 * Performance keeps the hero week-shape and a door; everything else — the block
 * ramp, the prescriptions, the seven muscle rails, the provenance ladder and
 * the landmark editors — lives here, entered on purpose.
 */
export default function Volume() {
  return <AuroraVolume />;
}
