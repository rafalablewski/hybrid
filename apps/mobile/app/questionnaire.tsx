import AuroraQuestionnaire from "../components/aurora/questionnaire";

/**
 * THE QUESTIONNAIRE — the route.
 *
 * It was `/volume-model` until Aug 2026, and the name was the diagnosis: a
 * screen titled after the machine, on which a dozen questions about a person sat
 * at the same weight as thirty-five landmark fields. The questions are what make
 * freshness, readiness, recovery and every prescription this athlete's rather
 * than a textbook's, so they are the screen now; the landmark table is a
 * disclosure inside it.
 */
export default function Questionnaire() {
  return <AuroraQuestionnaire />;
}
